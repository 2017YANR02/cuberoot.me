# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目目标

魔方速拧视频自动复盘工具：输入第三人称速拧视频，输出标准 WCA notation 复盘公式（`F L2 D' L' U R' ...`）。准确率目标 ≥80%，允许人工修正剩余部分。设计与实验记录见 `plan.md` / `roadmap.md` / `human.md`（均为中文，是理解本项目的必读背景）。

## 相机几何（最关键的非直觉前提）

第三人称机位与选手视角是**镜像**关系。相机主要看到的是 **B 面**（正对、大面积），不是 F 面：

| 相机中位置 | 实际面名 | 可见度 |
|-----------|---------|--------|
| 正对（最大） | B | ★★★★★ |
| 画面右侧 | L | ★★★ |
| 画面左侧 | R | ★★ |
| 顶/底边缘 | U / D | ★★ / ★ |
| 远侧 | F | 不可见 |

推论：相机的左 = B 面的右列（索引 47,50,53），相机的右 = B 面的左列（索引 45,48,51）。`greedy_reverse.py` 里 B 面 3×3 提取做了这个镜像重排。**L/R 混淆 + 数据不平衡（R:81, L:29）是准确率的头号瓶颈。**

## 三步流水线

```
视频 → [motion_detect] 分段 → [template_cnn] 分类出概率分布 → [greedy_reverse] 逆推 → 复盘公式
       Step 1              Step 2                          Step 3
```

开发时用手工标注的 `*.splits.txt` 替代 Step 1 输出，隔离误差。核心洞察（`human.md`）：**从 Solved 态逆序倒推**，因为末帧状态唯一确定，每一步都可与视频对照验证 —— 逆向搜索优于正向逐帧猜测。

## 核心文件

| 文件 | 作用 |
|------|------|
| `cube_state.py` | 54-sticker 置换魔方状态机，完整 WCA 符号。**置换表移植自 `functions.cpp`（该文件不在本仓库）**。`python cube_state.py` 跑内置测试。 |
| `template_cnn.py` | Step 2 主流水线：ResNet18(512d)+HS 直方图差分(96d)=608d → PCA+KNN / MLP → 写 `*.probs.json` |
| `greedy_reverse.py` | Step 3 逆推引擎：读 probs.json + 视频帧，从 Solved 逆推每步。当前为贪心单路径，roadmap 计划升级为 beam search |
| `motion_detect.py` | Step 1 运动检测分段：`python motion_detect.py <video.mp4>` |
| `lovo_test.py` / `quick_mlp_test.py` | 评估/调参脚本，从 `features_cache.npz` 加载（不重读视频） |

其余 `*_analyze.py` / `*_debug.py` / `template_match*.py` / `optical_flow.py` / `facelet_track.py` 等是实验性脚本，多数已验证无效（见 `roadmap.md` §11 实验记录），改动前先确认是否是死路。

## 数据格式（`videos/`）

- `*.MP4` — 原始 4K/100fps 视频，单个 500–800MB，已 gitignore
- `*.splits.txt` — **Ground Truth**。首行 `Splits:679:686:...`（帧号，N+1 个点对应 N 段）+ 复盘 notation 行（每个 token 是一段的标签）。`template_cnn.py` 和 `greedy_reverse.py` 都靠这个文件对齐
- `*.probs.json` — 每段的 Top-K 面概率分布（Step 2 输出，Step 3 输入）
- `features_cache.npz` — 608d 特征缓存。**改视频/改 ROI 后必须手动删除重建**，否则用的是过期特征

## Notation 约定（见 `plan.md` 末尾「复盘说明」）

- 涉及：U D L R F r f x y；**不涉及 B E M S z**
- CubeState facelet 顺序：U(0-8) R(9-17) F(18-26) D(27-35) L(36-44) B(45-53)；颜色 id：U=0 R=1 F=2 D=3 L=4 B=5
- `x` 转体不单独分段（与前一动作捆绑，如 `L2'x'` 算 1 段）；`y` 单独分段
- `x y` 转体不计入步数
- `UD'`（中间无空格）= 同时开始/完成，属同一段但算 2 个转动
- `↑↓·` 是指法记号，`...` 是卡顿，解析时都要剥离（`splitByFingering`）
- splits.txt 末尾的 `y2` / `z2` 等是用户加的朝向校正，不对应视频段（`greedy_reverse.parseGT` 里作为 `tailRotations` 分离）

## 常用命令

```bash
python cube_state.py                                    # 跑状态机单元测试
python template_cnn.py                                  # 完整分类流水线（首次~3min 提特征，之后读缓存）
python greedy_reverse.py "videos/3 4.375.MP4.splits.txt" # 单视频逆推复盘 + 与 GT 对比
python lovo_test.py                                     # Leave-One-Video-Out 评估（需先有 features_cache.npz）
python quick_mlp_test.py                                # MLP 超参搜索
```

无 `pyproject.toml` / `requirements.txt`；依赖：`opencv-python`(cv2) `numpy` `torch` `torchvision` `scikit-learn`。脚本从仓库根运行，硬编码相对路径 `videos/`。

## 当前状态与陷阱

### 引擎 (物理语义, 2026-07-06 完成, 已验证)

- **`src/rotation-perms.ts` + `src/anchored-search.ts`**: 三种语义并存, 辨析见 rotation-perms.ts 头注 — ① canonical (cube-state 逐 token apply, 转体=恒等, r≡L); ② conjugated (cube-state 整串 apply); ③ **physical (转体是真实贴纸置换, 中心块动) — 搜索/回放/观测一律用这套**。转体置换经 162 条共轭恒等式对 functions.cpp 面映射表验证。
- **oracle 结论 (5 GT 视频, 仿真观测)**: 物理语义 + 逐段 B 面观测 (噪声 12.5%) → **5/5 锚定, 逐段 99.1%, 4/5 编辑距离 0**(Phase-1 canonical 只有 87.3%): 中途 y'、宽转、U'D、x 捆绑全部精确还原。跑法: `npx tsx scripts/oracle-eval.ts --beam 2048 --mode vis`; 面身份边缘化 + 随机 κ + B/U 漂移版: `--mode visface` (B/U 限面 **5/5 锚定 92.1%**, `--allfaces` 关限面对照 65.9%)
- **敏感性包络 (定提取器 KPI)**: 噪声 0.25 → 5/5 99.1%; 0.35 → 1/5 崩。**整段丢观测是头号杀手**: dropout 0.4 → 0/5 (哪怕格子全对); 每段只见 5/9 格但全覆盖 → 3/5 90%。→ **提取器 KPI: 每边界覆盖率 > 单格准确率 (但准确率也要 ≥75%)**。
- 锚定 ≠ 正确: 短序列存在"另解"碰巧锚定 (如 R F2x' F2x' u'), 靠观测得分区分。y+U/D 同轴组合有等价写法 (R U y' R U ≡ R d R U), 搜索可能输出更紧凑的宽转写法。

### 相机约定 (2026-07-06 金标验证 + 共轭语义修正, 单测锁死)

- **无列镜像**: 相机 3×3 行主序直接对应 B 面 facelet 45..53 同序 (video3 帧 681 逐贴纸金标)。`bface-color.ts` 的列镜像是 Python 旧布局的约定, **勿沿用**。
- **共轭分解 (最关键)**: 相机态 = ρ⁻¹∘R_t∘ρ (GT 系与相机系差常数旋转 ρ)。拆两半:
  - **内侧 (位置重排)**: 被"指派边缘化"吸收 — 合法指派集 = **24 个朝向 ρ 的 B 窗口回拉** `assign[i]=ρ[45+i]` (`FACE_ROT_ASSIGN`)。**勿用 facelet 下标网格 rot90 硬造** — 各面下标布局从外侧看未必行主序, 硬造过一次, 真实数据得分≈随机才查出来 (两种指派集在本状态机布局下恰好数值等价, 但推导必须走窗口回拉)。
  - **外侧 (常数颜色重标 κ, video3 = y2: G↔B/O↔R)**: **不被指派吸收**! 搜索靠 24 朝向初始 beam 吸收 (真路径 S'_t = ω∘R_t, ω=κ), 因此**锚集必须左复合 {ω∘打乱态}** — 右复合 {打乱态∘o} 只在 κ=id 的纯仿真里与之相交, 真实数据下不含真路径 (潜伏 bug 已修, `test/anchored-search.test.ts` κ≠id 用例锁死)。离线指标里 κ 按视频拟合 (24 候选全局最优, `real-eval.ts bestAssign`)。
- **面身份随持握漂移**: 起手慢速期相机看 B 面, 从第 2-3 步起选手前倾, 相机主要看 U 面 (斜视角)。观测模型**面身份边缘化** (`RawFaceObs`/`logRawObs`, 指派集可 `rawFaces` 限面)。oracle `--mode visface` 仿真此设定 (含随机 κ + B/U 漂移)。
- **限面收益 (oracle, 12.5% 噪声, 随机 κ)**: 全 24 指派边缘化 65.9% → B/U 限面 (8 指派) **92.1%, 5/5 锚定** — 边缘化的证据稀释大部分可由持握先验收回。

### 真实提取 (WIP, `src/sticker-blobs.ts` + `scripts/dump-frames.ts` / `real-eval.ts` / `debug-blobs.ts` / `diag-*.ts`)

- 管线: 全程连续 framedump (960×540) → `activityMask` (跨帧稳定性掩码) → 同色连通域色块 → 反光环带重标 (W 块 1.25× 壳层放宽饱和色相投票) → 多色簇 + 尺寸一致性过滤 → RANSAC 仿射基网格 (斜面门槛: |sin|>0.4, det>0.12·medNN²) + **Gauss 基约减** (幺模歧义, 不约减 3×3 窗口会斜) → 顺序多晶格 (≤2 面/帧, 拟合后移除内点再拟合) → 格心采样 → **时间连续性跟踪** (`trackFaceGrid`: 冷检测失败帧用上帧晶格作先验, 平移搜索 + 直接格位采样, 5 连败杀先验 — 逐帧读出率 25%→90%+) → **静止链检测** (跨帧网格一致 ≥2 帧成链, ≤1 格容错 + 空帧桥接 ≤2) → 链共识网格按中心帧归属所在段。
- **搜索观测 = 双端点语义** (`anchored-search.ts rawObservations` 支持每段多网格): 段内任意时刻读数必属段起点态或终点态之一 (拧转前/后; 拧转中非转动层两者皆合), 每网格 max(起点,终点) 段内求和 — 标注滞后 (split=手部动作起点, 拧转滞后 0~8 帧) 的 ±1 归属歧义自动消解; y 段的链归前一非转体段 (y 是整机旋转, 面边缘化天然吸收)。oracle 回归 92.1% 不受影响。
- **观测按静止链而非逐边界强读的原因 (踩过的坑)**: 逐边界就近强读会把**邻段状态**当本段观测 (同一可读帧被归到多个边界), 对搜索是毒药 (比 dropout 更狠); 双端点语义 + 链化从源头消解。
- **现状数字 (2026-07-06 深夜, 诚实)**: 跟踪后逐帧读出率 90%+ (冷 25% + 跟踪 65%), 段有观测率 86.9%; **帧级软覆盖 ±12 达 60%** (信息在流里, 首次踩到包络线); 但**链级准确率只有 ~55% (乱态底噪 48%), SNR 太薄** — 端到端 0/5: 弱观测是负资产 (全喂 8.3% < 门控 len≥4&填≥7 11.4% < 早期少而准 17.5%, 逐段准确率均为未锚定垃圾路径口径)。GOOD 链 (≥85% 匹配) 每视频只有 1-5 条, 无 GT 时挑不出来。**结论: 启发式 CV 已到平台期, 破局点 = 把链级准确率从 55% 提到 75%+ (噪声悬崖), 见 prior_art.md 蓝图**。
- **瓶颈二维分解 (2026-07-06, `real-eval.ts --oraclesingle/--oraclefull/--oracleobs` 网格切, 全 5 视频)**: 用真实覆盖模式 + 可控注入精度的 oracle 观测, 把"覆盖密度"与"逐格精度"两轴解耦 —— ① 真实稀疏覆盖(25/40 边界, 8/40 双端) + **完美精度** → 锚定失败 12.5% (覆盖不足杀); ② 满覆盖(每段单主导面 or 满 B+U) + **完美精度** → **5/5 锚定 90.8%** (引擎本身没问题, 全 5 视频泛化); ③ 满覆盖 + **真实精度 0.62** → **0/5 锚定 10%** (精度在噪声悬崖下方, 全 5 视频泛化); ④ 满覆盖 + 0.62 + **pin(钉死指派)** → 更差 2.5%。**结论: 锚定要覆盖×精度联合达标 (覆盖≈全段 且 逐格≥~75%), 改单轴无用**; 真实管线两轴都欠 (覆盖 25/40 + 精度 0.60), 与 oracle 敏感性包络的噪声悬崖 (0.25→5/5, 0.35→崩) 一致。
- **负结果 (勿重试)**: ① **pin (每路径固定指派隐变量, `rawPinWindows`)** 在噪声下反伤 (0.62 满覆盖 10%→2.5%): 固定隐变量把状态空间 ×16, "错态+碰巧对齐 pin"组合爆炸快于它锐化 SNR, beam 被稀释; drift 变体同理。② **junk 混合鲁棒似然 (`rawJunkProb`, robust logaddexp 地板)** 救不了 60% 精度: 60% 观测不是均匀垃圾 (地板抓不到), 而是系统性偏向似是而非的错误态。③ **perframe 密集逐帧喂搜索 (`--perframe`, visualWeight 0.35)** → 2.5%, 比链式 12.5% 更差: 密集但噪声, 邻帧误分类高度相关不能靠平均消 (印证"弱观测是负资产")。④ **forward 正向搜索 (从打乱锚起, `--forward`)** GT t=0 即死 (未根因, 疑实现或早段证据比设想更弱)。⑤ **每视频静态颜色自标定 (`src/color-calib.ts`, `--calib`/`--calibgt`)** 修不动精度轴: 已知态帧 (打乱/复原/GT 全段) 反标 5682 格样本, 色度盘+亮度特征上逐类对角高斯的样本内天花板仅 65-70%, **LOO 7-NN (无参, 最乐观口径) 也只 69.9-77.6% (合计 73.0%)** — 全在 75% 悬崖下方。混淆是视角/光照条件性的 (R/O/Y 压缩在色相 6/16/32 半度窄带, 反光洗白), 单格颜色特征**本质饱和**, 任何静态分类器/调阈值都无解, 别再试。管线级标定重提取实测还略降 (60.0%→57.8%)。⑥ **软观测 (逐格 6 色概率向量, `RawFaceObs.probs` + `--oraclesoft`)** 同精度下反而大败: 满覆盖 + 真实特征噪声 (GT 样本池抽样) 下 **0/5 锚定 ~5.6%**, 而硬观测均匀噪声 @0.70 有 1/5 锚定 48.9% — 高斯错拟合的"自信押错"格主动喂错误路径, 比硬 miss 的有界惩罚更毒; 回火 τ 数学上 ≡ 全局证据缩放 (逐格常数偏移在假设间相消), 修不了符号错误, 别扫 τ。**推论: 均匀噪声 oracle 低估了难度 — 真实偏向性错误 (R→O 系统性) 下真墙比 75% 更高**。⑦ **每观测光照增益校正 (`scripts/gain-ceiling.ts`, render&compare 内环的真标签最优模拟)** 全 5 视频一致反伤 (LOO k-NN 77.3→74.7 / 73.9→69.2 / 70.3→63.6 / 70.6→64.5 / 70.3→66.0): 混淆**无每帧相干光照成分**, 是每格级的 (贴纸角度镜面反光/运动模糊/局部映照) → **render&compare 的颜色残差通道同样封顶**, 其剩余价值只在几何/时间平滑/亚格结构, 投入前须重估。⑧ **手部关键点第二证据通道 (`hands/`, MediaPipe HandLandmarker, 2026-07-09)** 在本素材上无段级信号: 检测率 91-95% 双手全程在框 (提取无忧), 但 6-way face 分类 LOVO 只 ~48-53% (probs 旧链 57.6%, 墙 75%); **机理: 9 TPS 下双手永远都在动** — r_energy/主动手全类平坦 (峰速手 93% 恒同一只), "哪只手拧的"这个人类直觉信号在段窗粒度不存在; 镜像增广 (L↔R 对称)、world-landmark 3D Kabsch 旋转轴、魔方格坐标投影各 +≤1pt; **视频内 k-fold ≈ LOVO (50±2%) → 是特征不携带信号, 不是跨视频漂移**; 与 probs 融合反降 (w=0.7 56.8% < 纯 probs 58.1%) — 错误相关, 叠不出增量。手部 LSTM 别指望: felikemath 82% 是 3 类慢速场景, 与本素材 6 类 9TPS 链式指法不可比。⑨ **晶格带光流/位移三连败 (`hands/flow_features.py` 三版)**: Farneback 稠密光流死于拧转帧大位移+运动模糊 (能量渗邻带) 与快转晶格跟踪滑脱 (可视化实锤: 格点悬在手上); 相邻帧对条带块匹配死于**灰度 1 格周期孔径** (炫彩贴纸灰度近同亮, 条带只剩黑缝, NCC 在 0/±1/±2 格同优); 静止对静止 RGB 条带死于**面外流入** (B 面上 U 转不是"内容沿条带滑 1 格" — 流入贴纸来自侧面, 仿射晶格延长线出面即折到别的 3D 平面) + 条带一半是手指 + U 面斜条透视过冲采到背景。带级指纹直检 (|d| 冠军带 per face) 每版都散, 分类器 LOVO 全程 47-53% 平台。⑩ **贴纸外观守恒动作打分 v2 (`scripts/conserve-eval.ts`, 2026-07-10)**: 静止链共识 + 色度-only + B/U 双视图 + 全局指派拟合 + 平移边缘化全套做完, 真实 face top1 仅 26.7% (覆盖 56%, 门槛 probs 57.6%)。**打分机器无罪** (神谕阶梯: 全覆盖合成观测 97.6%/100%, 真实覆盖+GT 内容 75-77%), 三个提取层根因 (同态自检实锤, 详见脚本头注): (a) **晶格重获取平移漂移** — 同边界跨链对齐 p50 -30, 加 ±1 格平移搜索 → -10, 而链内 split-half 自距 p50 -1~-5 (特征本身稳): 重获取的 3×3 窗常错位 1 格读到邻位贴纸; (b) **旋转身份不稳** — 好对胜出旋转仅 ~60% 集中 rot0, 每视频全局指派不存在 (16 组合钉死扫描全平坦), 解开每链旋转自由度则共轭歧义回归 (神谕实测段内自由指派把 U 共轭成 R/D/L, 95%→73%); (c) **9 TPS 边界归属歧义** — 运动峰值规则被手部持续运动污染 (已证伪), split+LAG 兜底仍错归 → 前后同态 neutral 饱和平局。**(a)(b) 同时是主管线 55% 链级准确率墙的疑似共因** (错位格读的是真贴纸色 → 系统性"似是而非"错误, 与负⑥观察吻合)。⑪ **晶格重获取一致性修复 (`src/lattice-track.ts` 规范基锚定, 2026-07-10)**: 负⑩根因 (a)(b) 的修复层做完并验证。病理实锤 (`scripts/basis-angle-probe.ts`): 锚定血统内 v1 角范围 150-347°、>30° 跳变 10-35 次/视频 — **~60% 冷拟合是退化晶格** (斜持面 RANSAC 拟出对角基/错 pitch), 逐帧 reorient 只是"在垃圾里连续"。修复 = pass1 每槽恒定规范基 (强拟合池 pitch 带筛 + v1 角折叠 mod90 圆中位) + 基/pitch 对不上判垃圾拦截 + 残差门控 ±1 格 snap (位移 ≈ 整数格才吸, 亚格位移=真实漂移不吸) + 跟踪平移搜索加运动惩罚。**几何层全部修好**: 角范围 → 20-41°、跳变 → 0-4, samecheck 同态对齐 3/5 视频大幅改善 (v2 -30.7→-13.6, v5 -34.4→-21.7), 接受后的冷拟合天然 rot0 恒定身份 (基修 0 次)。**但墙纹丝不动**: 边缘化逐格 55.5→56.4/55.4 (噪声内), e2e 13.5→13.1% 锚定 0/5 对 0/5, 混淆矩阵 (W→O 为首) 修复前后一字不差; conserve face top1 26.7→21.6。**结论: 55% 墙不是几何墙, 是颜色墙** — 几何病理真实存在但对格级准确率是二阶效应 (legacy 基线里垃圾拟合靠自由指派边缘化"幸运匹配"抬分, 掩盖了几何混乱的代价)。v1 期中教训 (勿重蹈): 宽松中心距 snap + 参照逐帧继承 = 把正确拟合扳向漂移参照 + 基随机游走; 槽匹配必须用**原始**中心距 (min-shift 残差距会跨槽误配, U 面吸到 B 槽再毒化参照); samecheck 配对须 cy 过滤跨槽对 (实测污染极小 0-7 对, 但度量已修)。

- **正结果 ⑫ — 先验判决实验 (`scripts/prior-sim.ts`, 2026-07-10), 推翻"结构性终局"**: 负⑪判死的是**似然侧** (把每格读得更准), 先验侧从未试过 — 把搜索先验从"任意步 ~18 分支"(75% 悬崖的隐含前提) 换成"人类 LL 公式字典", 整段候选按总证据排序 (错误做加法, 不像 beam 逐段复利)。设计: 候选 = (ZBLL 472 + PLL 21 案例全公式 1873 条) × y^k 前缀 × 前/后 AUF, 按尾段 STM 数过滤 (段数是观测量), 逆放锚定复原态 (终态约束内建); GT 显式加入候选 (考判别力, 字典收录率单独报告); 证据 = `real-eval --dumpobs` 转储的真实对齐观测 (尾 15 段覆盖 8-11 段, 87-148 读格/视频); 评分 = Σ链 max(双端) Σ格 log P(读|候选态色), 混淆矩阵**留一** (排除被测视频), 变长候选加背景似然公平比; 乱读对照全部沉底 (排名 644-14688) 验证判别力来自真信号。**结果 (真实观测, 全 5 视频)**: 乐观界 (朝向借 GT 对齐) rank 1/1810, 4/1214 (前 3 全为同 case AUF 变体, 区分段恰无观测), 2/1213 (并列同 case), 1/4712, 2/4712; L±2 松弛至 ~19k 候选仍全员 top3。中间界 (`--rotalign`: 面身份已知 + 逐链自由 4 旋转, **比生产的 span 恒定旋转更悲观**) rank 11/7/22/2/2, 同 case 常居榜首。悲观界 (`--freealign`: 逐链自由 24 指派) 崩 (rank 21-5279) → **朝向身份是承重墙, 而这正是负⑪修复构造性提供的** (槽身份 + span 恒定 rot0 + κ 可从开拧前已知打乱静止帧直读)。**结论: 55% 颜色墙压不死字典先验解码 — LL 段在实测噪声下可解 (至同 case AUF 歧义内)**; 且本实验完全没用"LL 起点态必须从已知打乱经合理 F2L 可达"这个跨段一致性巨型过滤器, 生产判别力只会更强。细节: v4 GT 含手滑调整对 `R' R`, 执行原样序列以 margin 12-13 夺魁 → 生产候选生成须支持调整对增广; GT 精确序列仅 1/5 在字典内, 但不碍判别。追加 (同日): 倒向锚定本就是引擎默认 (正向=负④), "从后往前"的正确颗粒度是整段公式而非单步 (单步倒推同样撞 55%≪75% 悬崖, 复利对称); **整色性第二通道**实测有信号 (同真色格对读一致 45.3% vs 异真色 22.7%, ~2:1/对; GT 整色面主色占比≥75% 命中 38% vs 混色面误报 10%), 但**分类后标签**的 all-pairs 折入打分中性 (`--pairw` 0.25/1 均有帮有伤 — 标签对一致性相对似然模型无新信息), 真增量在**分类前原始色度相似对** (双误分类仍可判同色, 绕开 R/O/Y 色相塌缩) → 生产解码器须直接吃原始色度, 记为设计要求; κ 标定可从收尾静止帧直读 (复原态+手离开, 比开拧前更稳, 两头可双锚)。追加 2 (同日): **生产对齐界扫描 — 零 GT 对齐模型全军覆没, 差距精确定位在"逐链面身份"**。转储加 span/cx/cy 字段后依次实测 (L 已知 REAL rank, v1-v5): `--pinalign` 前段 pin 迁移 52/1099/1110/76/2 (F2L↔LL 换握破坏迁移, 仅 v5 完美); `--spanalign` 每 span 自由指派对 (24×24 联合拟合, 链自选窗口) 291/41/70/265/59; `--winalign` 空间 2-means 窗口聚类 (窗口分离 48-109px, 身份生产可得) × 尾段单指派 286/408/361/1323/258; `--epochs 2` 时间片 DP (换握=离散时间事件 → 指派分段常量, 每候选每窗口精确解) 423/332/309/38/36; `--freealign` 482/267/418/21/83。对照 `--rotalign` (借 GT 面身份) 11/7/22/2/2 → **逐链面身份 = 唯一分水岭**。机理: GT 最优对齐的面序列在尾段时间轴上非连续 (v1: B→L→B→D→F→R→L), 即**当前颜色精度下连 GT 拟合都定不了面** — LL 期四侧面下 2/3 整色 (旋转/邻面简并) + R/O/Y 塌缩 (块色互混), 一切"恒定性/时间片"自拟合模型注定够不着。**生产化两条硬前提** (缺一即卡 rank 数百): (a) **几何面身份** — 提取层估计未记录整体旋转 (换握/倾斜 → 虚拟 rotation token, 维持相机系状态诚实), 纯光学、与颜色无关; (b) **原始色度通道**破 R/O/Y 塌缩 (追加 1 的设计要求) — 块色可辨则颜色侧面身份亦复活, 两轴互补。

- **正结果 ⑬ — 倒推解码器试点 (`scripts/backward-decode.ts`, 2026-07-10, 用户"从还原帧倒推"方案)**: 从收尾静止帧 (复原态+手离开, 全片唯一免费锚) 起, 逐边界倒推束搜索, **零 GT 对齐** (追加 2 的面身份缺口由"锚点继承 + 相机系状态吸收转体"绕过)。架构: ① 收尾链整色投票 → 上窗口面身份 (实测选手放下魔方时有一次未记录旋转, v2 下窗口 F→B ≈ y2 → 下窗口面放开为上窗口 4 邻面种子); ② 候选动作 = 公式后缀树 (ZBLL+PLL × y 前缀 × 前后 AUF, STM L±2; 自由 36 步法在盲区边界指数爆炸 — 深度 3 盲区 114 万 vs 字典 5 千, 已判死); ③ 双通道证据: 静止链颜色 (留一混淆) + 老管线 probs 逐段面概率 (`--probs 1`, 独立通道压巧合, v2 rank 50→10) + ±1 格平移边缘化 (`--shift -1.5`, 防负⑩(a) 漂移伪信号, rank 10→7); ④ FREE_WIN (链自由选两锚定窗口, 位置聚类分不开 10px 级窗口重叠)。**结果 (4 ZBLL 视频, beam 32768)**: 真解全程存活到倒推终点 4/4 (从未掉出束), 完赛排名 7/1904, 59/1623, 26/3700, 39/3260 (top 0.4-3.6%); F2L-solved 停机信号有效 (终深 top50 中 30-36 条已 F2L 解)。**未达 rank 1 的残差已定位**: 尾边界网格漂移伪信号 (v2 深度 1 `WWOWWOWWO` 右列 O 恰似 R' 前态, 错误候选白赚 ~4 分) + 55% 颜色噪声的逐边界 ±5 分随机 — 前者要提取层修 (原始色度/漂移检测), 后者要更多边界证据 (F2L 夹逼延伸解码)。执行连续性时间先验查过不成立: AUF 后起手天然有 ~390ms 停顿, 对手的 y2 恰好都塞在那里。跑法: `npx tsx scripts/backward-decode.ts --video 2 --freewin --beam 32768 --probs 1 --shift -1.5` (`--nodict` 自由模式对照, `--debug` 逐链明细)。
  - **追加 (2026-07-10, `--full` 模式, commit e3b0b4dea7+386e617958)**: 用已知打乱 (= 全解⁻¹, 生产已知打乱输入; 已验 Sstate+全解→复原) 作终点锚, LL 后续倒推 F2L 再排。两趟设计 (统一束在 LL→F2L 过渡处爆炸: 每 LL 候选生 36 自由子, 高分错案在盲区淹没真解 → 改 top-K **同长** (stm=L, 长度由分段已知) 候选各自独立续 F2L, 按 llScore+f2lScore 排; 同长门槛修过拟合偏置——短候选靠自由搜索白占便宜)。**证实 F2L 延伸方向对但有天花板**: 真解稳拿全场最佳 F2L 匹配 (v2 cap16 F2L -102 vs 冒牌 -108), 但自由 F2L 搜索**过拟合** (给每个候选硬凑贴合噪声的路径, 深度越大找补空间越大), 真解领先随深度**先升后降** (cap12 +5.7 / cap16 +6.5 峰 / cap24 +1.6 缩水), 峰值 ~+6.5 < LL 颜色噪声欠债 ~10 → 真解 7→6, 到顶爬不到 1。终点锚硬过滤 (两头对推) 不划算: "从打乱端往前推"半程撞朝向识别墙 (打乱端无干净锚——人拿起在看在转朝向未知——正是倒推从还原端专门绕开的负⑪难题)。**判决: 搜索侧到顶 (字典⑫ + F2L 延伸⑬ 都进前 1% 但都卡 55% 颜色墙), rank 1 / ≥80% 的前置硬条件 = 提取层 (原始色度破 R/O/Y / 几何朝向估计), 非搜索花招。**

- **正结果 ⑭ — kNN(Lab)颜色分类器: 逐视频标定下把 obs 精度 +6.6pt → 倒推解码 v2 rank 7→1 (2026-07-10, `--knncls`)**: 负⑤判死的是"色度盘+亮度上**逐类对角高斯**天花板 65-70%", 但同批格 kNN-LOO 74% — 高斯欠债 ~19pt (曲香蕉状类 + 极不平衡拖垮参数模型)。解码器不需过 75% 锚定悬崖 (正⑫⑬), 补分类器即纯增益。`color-calib.ts` 加 `buildKnn`/`knnClassify` (Lab 特征, 每类上限采样压不平衡, 松拒判 rejectD2=400); `calibClassify` 在场优先走 kNN。**关键修复 = 解耦分类与链形成**: kNN 帧间抖动破坏 `agree()` 链一致 → 覆盖崩 (覆盖是头号锚定杀手); 链仍用稳定 vivid `colors` 形成/agree, 只共识投票用 `knnColors` (`FaceObservation.knnColors`, real-eval pass2 就地附到 pass1 网格, 不重提取——重提取丢覆盖)。**obs 级逐格 (dump read vs GT 对齐 gt, 全 5 视频)**: vivid 58.9% → 同视频 GT kNN **65.5%** (+6.6); 倒推解码排名 (beam 32768, 留一混淆) v2 **7→1** (✓无 GT 对齐倒推出 ZBLL)、v4 26→15、v5 39→7; v3 59→356 反伤 (长度偏置: kNN 更大证据量级让更短冒牌先完赛少累积边界罚——搜索侧问题, 未动)。opt-in `--knncls` (默认高斯路不变, 64 测全绿)。`--dumpblocks` 转储逐格原始像素块供 `color-lab.ts` 离线扫策略。
  - **生产 legit (零本视频 GT) 迁移全线证伪 (2026-07-10, commit 待)**: +6.6pt 增益**须逐视频标定**, 静态跨视频迁移一分都收不回。(a) **纯 legit** (只打乱观察期帧+复原帧反标, `--calib`): 颜色饥饿——观察期斜视角 omega 对不上被拒判, 实际只剩复原后单面, 每视频仅 1-2 色, 池化 5 视频**完全无 R** (W 占 205/466), 留一 45.9%/macro 30.8% (`legit-pool-probe.ts`)。(b) **跨视频 GT-pool 留一** (`--calibpool`: 其它视频 GT 样本训全局 kNN 应用到本视频 = "标注语料训色部署新视频"): 干净格 64.1%/macro 63.7% (满 6 色均衡), 但真实 obs 塌回 **58.0% = vivid** (漂移在难格上吃光迁移增益), 解码排名**全 4 视频反伤** (v2 35 v3 236 v4 36 v5 530, 均差于 vivid 7/59/26/39——obs 打平但错误结构不同 + 留一混淆更毒)。(c) **白平衡归一化** (gray-world 全格均值零标签 / white-anchor W 格中位, `wb-pool-probe.ts`): 反降 (64.1→59.5/57.0, Y 塌到 10%) — 这些视频漂移主因**非白平衡** (同机同光), 归一反毁判别信息。**结论: 分类器选择 (kNN>高斯) 确能 +6.6pt 并解锁 rank 1, 部分推翻负⑤的"静态分类器无解"框架——但增益锁死在逐视频白平衡; 任何静态跨视频/legit 迁移 obs 顶格 =vivid、解码更差。**
  - **EM 自训练也证伪 (2026-07-10, `em-probe.ts`+`candidate-f-probe.ts`)**: "首解→伪标签→refit→重解"这条唯一剩路离线判死。① 天花板/抗噪: 富样本 kNN 伪标签错 f%, obs 精度 f≤~25% 才可能胜 vivid、f≥30% 跌破 (kNN 平均噪声, 退化 f=20% 掉 ~3-4pt / f=30% 掉 ~9pt)。② **realistic f 实测 = 64.8%** (`candidate-f-probe.ts`: vivid v2 首解 top-1 vs GT 逐 LL 边界 B 面态色差 70/108) ——远超 25%, 且 top-6 全是同一**错**公式家族 (ZBLL S/AS 71, 真解不同 S case 排第 7), **两解连 LL 起点态都不一致**: vivid 噪声大到解码器认错 LL case。→ EM 从"自信错"种子自举 = 伪标签在 LL 边界随机错 → refit 出垃圾颜色 → 无逃逸 (确认偏置实锤)。**根因: 颜色丰富的边界 (LL/全解) 恰是 vivid 认不出的 (f=65%), 认得出的边界 (打乱/复原锚) 又颜色饥饿 (1-2 色, 负⑤(a)) —— 无自举立足点。** **最终定论: 逐视频颜色标定的 +6.6pt 增益无任何零-GT 路径可达 (静态迁移 obs=vivid, EM 自训练 f=65% 崩); 颜色这条彻底封死, 唯一剩下的破局杠杆 = 几何朝向估计 (与颜色无关的第二信号: 换握/倾斜→虚拟 rotation token, 提供面身份, 正是⑫追加 2 / ⑬ 标的另一前置硬条件)。**

- **正结果 ⑮ — 倒数第一步判定器 (用户"先只判末一步"方案, 2026-07-10, `scripts/last2-decode.ts`): 4/5 末步排名 1**。任务 = 只用尾窗证据把末一步完全判定 (36 步法候选)。多轮探针 (`last-move-probe`/`tail-probe`/`tailhd-probe`) 的路标发现: (a) **分辨率墙部分真实** — 末两段 (9TPS ~13 帧) 在 960×540 下近零可读网格, 同帧 4K 原片肉眼直读 (55% 颜色墙是在降采样 16× 的画面上量的); 1080p 重提取尾窗覆盖 4→23 网格, 但检测器在 1080p 反脆 (复原后整面同色粘连超 maxArea, `lattice-track` 已加 maxArea 透传)。**最优解 = 全程 960 跟踪定位 (尾窗覆盖 ~100%, 窗口局限提取才是缺链主因) + HD 帧格心重采色**。(b) **尾窗晶格错位半格~一格且跨面** (4K 目检实锤) 是几何主噪声, 亚格精修 (格内一致性+格缝暗线) 会跳上邻面/整色面等优对齐, 已试已撤。(c) **halo 渗色 = 真信号**: 边缘格采到的邻面贴纸 (负⑩(a) "错位读真色") 恰是 U 系方向判别信息, 按 cubie 签名 (6 外层转动的被动集, 同签名=同块) 推导邻面 partner 建模, 全线小幅提升。(d) **logo 旋转通道 (新, 54-facelet 置换模型表达不了中心旋转!)**: U 系转动把白心 GAN logo 原地转 ±90/180, 非 U 层不转 — `logo-probe2.ts` 蓝墨自定位 (晶格无关; 按环亮度选帧 — 糊帧蓝贴纸拖影墨量反而大, 选墨量大会锁错) + 图像轴 4×90° NCC (±24° 细扫吸倾斜) + 多帧对平均, **方向 4/4 全对** (v2 U2'→180 / v3 U'→270 / v5 U→90 / v4 R'→0), 融合后 v2 19→2、v3 6→3、v5 5→2。(e) **末两步联合解码** (36×36 对, m1 边缘化): 单判末步信息不足 (U 窗口对 U 系前态全纯白), 方向信息在 S0 期链、S0 需联合 (m1,m2) 才定。(f) 宽层收尾先验 -1 (u vs U 收尾在尾窗证据下只差整体旋转原理不可分, 真实速拧几乎全外层收尾; 生产中已知打乱锚全局朝向后自动失效)。**最终: v2/v3/v4/v5 末步排名 1 (margin 0.5-1.4), v1 (u' R2') 败 (rank 25)** — v1 是三重灾难叠加: 白面不可见 (logo 通道缺席) + probs 末段自信错 (R2'→U=0.82, 好视频它是助力、错时是毒药) + 尾窗是放下翻面的下窗口 (收尾投票 F→B 翻面, 种子集排除真指派; 对面进种子已试 — 自由度反噬 v3, 已撤)。教训: 逐深度往前推进需要每步 margin 稳压噪声, v1 型 (无 logo + 下窗口) 的破局仍是几何朝向/侧贴纸 3D halo。跑法: `npx tsx scripts/logo-probe2.ts` (产 `.tmp/logo-measure.json`) → `npx tsx scripts/last2-decode.ts [--diag]`; 前置 framedump + obs-dump。
- **正结果 ⑯ — 正⑮ 三杠杆全管线化 (2026-07-11, `--hdres`/`--hdrefine` + `src/hd-refine.ts`/`src/halo.ts` 共享模块 + backward-decode `--bleed`/`--logo`): 记分板 v2 rank 1 / v4 2 / v5 6 / v3 58 / v1 780**。(a) **HD 重采全管线化** (`real-eval --hdres 1920x1080`, ffmpeg spawn 流式逐帧零磁盘缓存, 格心按 960 晶格坐标重采): obs 逐格 58.9→59.2 (+0.3) — **分辨率墙对"已成链"几乎无增益** (静链本就读到真色), 墙体现在快速/糊帧区链根本不成形; +亚格精修 (`--hdrefine`, ±0.4 格粗细搜格内一致性+格缝暗线) → 60.6 (+1.4 真实)。**分辨率轴 2x 封顶**: v3 原生 4K+精修 57.0 = 1080p+精修 57.0, 别再往上推。(b) **HD 色参与链形成 (`--hdagree`) 负结果**: obs 打平 (60.7) 但解码 v3/v5 灾崩 (rank 1225/292) — 链形成必须用稳定 vivid 分类器, HD 逐帧读色在过渡区更易伪成链 (弱观测负资产再证)。(c) **HD 特征 kNN 跨视频池化部分复活正⑭(b)**: 精修后块中位 RGB 作特征 (`FaceObservation.hdRgb`, 样本池 `-hd-` 后缀隔离), 跨视频池 obs 58.0(死)→**62.9** (距同视频上界 64.4 仅 1.5pt) — 但解码全线反伤 (v2 14 v3 128 v4 死 v5 220, 均差于 vivid 7/59/26/39): **obs 增益≠解码增益第三次证实** (证据量级/错误结构扰动搜索的长度偏置, 搜索侧欠一个长度归一化, 未动)。(d) **halo+logo 进字典搜索** (backward-decode `--bleed 0.35` 边缘格邻面渗色混合 + `--logo 40` 深度 1 末步 logo 转角, 均默认 0 保持旧行为): vivid 基底 v2 **rank 1**/1516 (零 GT 倒推出 ZBLL 第 2 例)、v4 41→**2**/2940 (margin -0.1)、v5 39→**6**/2425、v3 59→**58**/1291、v1 **780**/2825 (无 logo 证据+深度 13 亏 -16.9, 三重灾难未解)。**坑: 字典 token 有 `U2'` 写法 (≡U2), logoLL 须先查 "2" 再查尾 `'`** (踩过: U2' 被判成 U')。(e) **vivid vs hdr dump 基底无普适赢家**: v2 双基底皆 rank 1, v3 vivid 58 < hdr 82, v5 vivid 6 < hdr 13, v4 并列 2 — **暂定 vivid 为字典搜索默认基底**, hdr 的价值在特定病理 (v2) 与 kNN 特征源。跑法: `real-eval --video N --dumpobs .tmp/obs-vN.json` → `backward-decode --video N --freewin --beam 32768 --probs 1 --shift -1.5 --bleed 0.35 --logo 40` (前置 `logo-probe2.ts` 产 `.tmp/logo-measure.json`)。
- **负结果 ⑰ — 几何朝向估计: 晶格粒度判死 (2026-07-11, `scripts/orient-probe.ts`, 阶段 1 探针按判死线枪毙, 未建跟踪器)**。动机 = 正⑫追加 2 的"唯一分水岭"(逐链面身份) 走颜色无关第二信号。两种形式都上不了预设 0.8 判死线: (a) **间隙事件检测** (标签 = 间隙跨真解转体/宽层动作, 每视频仅 2-4 个) 链级几何差 (Δ基角/Δpitch/Δ中心/间隙帧数/span断裂) 组合 AUC 仅 **0.63** — 机理: u/y 转体前后晶格还在原屏幕位置原基, 变的只是内容, 静态几何量不载此信息; (b) **逐链姿态分类** (仿射基 aspect/shear/pitch/位置 → kNN-LOO 判 GT 面) 高置信链**低于多数类基线** (v2 60%<70%, v4 29%<57%), 全部链池化 45% vs 基线 35% — 3×3 仿射拟合的形态噪声 (半格错位/手遮挡/距离变化混杂) 淹没倾斜度信号。**副产物两个重要重框定**: ① 链级 GT 拟合面标签大部分是噪声 (相邻对 61% "变面", 物理不可能; 高置信过滤后骤稳) — 追加 2 的"GT 面序列非连续"很大程度是**标签噪声**而非魔方乱转; ② 但 4K 目检 (v4 win0 六帧高置信链) 实锤**面身份确实随手腕连续变化** (±45° 晃动, 同窗口 U/B/F 切换, 完全不记谱) — "离散换握事件"模型不成立, 面身份是连续量; 恒定性/时间片模型 (正⑫追加 2 全灭) 的败因从"颜色定不了面"升级为"物理上就没有恒定性"。**剩余几何选项 (未试, 高成本低置信)**: 从原始像素做整魔方姿态估计 (轮廓/双面四边形/棱线), 非晶格特征 — 与负⑦"render&compare 剩余价值在几何"同框, 投入前须重估。转储链已带 f0/f1/ang/pitch/motion/rot/basis 字段可复用。**追加 (同日): 选项② "v1 放下翻面靶向修" 也测死** (backward-decode `--inhand`, 手中收官近固色链投票面身份替代放下后 finals 票, 默认关闭、A/B 对照复现基线): v1 617/-34.3 vs 780/-29.2 无效 — 复盘发现 v1 收尾 Y 票在其相机系本就映射 B 面 = 放下前真上窗口面, **旧种子从来就含真指派对, v1 病根不在种子而在深度 13 的 -16.9 (LL 起点边界系统性误读 = 55% 色墙本体)**; v3 236 vs 58 反伤 — 收尾 W×9 全票本来是对的, 手中票规则作废它反而稀释, 且下窗口面随手腕连续变 (本条①), 钉死单面必丢链。**至此 v1/v3 的残差全部归因色墙, 无已知靶向修**; 记分板 (v2 1 / v4 2 / v5 6 / v3 58 / v1 780) 即当前技术水位。 (pin/drift/junk/forward/soft) + `real-eval.ts` (oracle 四模 + calib 两遍/`--knncls` kNN/`--calibpool` 跨视频池 + `--dumpobs`/`--dumpblocks`/`--dumpsamples` 转储) + `scripts/knn-ceiling.ts` + `scripts/gain-ceiling.ts` + `hands/` (uv 项目: 关键点提取/分类器/带位移/调试可视化) + `scripts/dump-hand-segments.ts` / `dump-cube-quads.ts` (段标签 + 每帧晶格 dump) + `scripts/conserve-eval.ts` (守恒打分 + 神谕阶梯 + 同态自检诊断) + `src/lattice-track.ts` (共享提取环: 规范基锚定默认开, `--noanchor` 复刻 legacy, `--nosnap`/`--notrackref` 消融) + `scripts/basis-angle-probe.ts` (基角漂移探针) + `scripts/prior-sim.ts` (先验判决: 字典候选 × 真实观测排序, 乐观/rot/pin/span/win/epoch/free 七对齐界; 输入在 `.tmp/`, 头注有重建命令) + `scripts/color-lab.ts` (离线扫聚合策略×色彩空间×LOO-kNN, 消费 `--dumpblocks` 块) + `scripts/legit-pool-probe.ts` / `wb-pool-probe.ts` / `obs-acc-cmp.ts` (正⑭ legit 迁移三负结果的可复现证据: 池化颜色分布/LOO、白平衡归一、三 dump obs 逐格对比), 供未来复用。real-eval / conserve-eval 的逐帧提取已统一走 `extractTrackedFrames`。
- **已证伪的微调**: minArea 220→130 (小噪声块搅碎晶格); GT 最优选帧 (天花板也只 57-66%, 选帧不是瓶颈); 链零容错 agree (跟踪帧单格闪烁需要容错, 覆盖 9→2); 多网格全喂 (垃圾链把真路径挤出 beam)。
- **陷阱**: ① 背景中位数**不能**从边界帧建 (魔方整场悬在画面中央, 中位被魔方色污染, 会吃掉同色贴纸) — 用 activityMask; ② 反光核心到饱和色间有去饱和过渡带 (s 25~110), 环带投票必须放宽饱和阈; ③ 选手穿亮绿外套 + 计时器黄 logo + 垫角魔方图案, 全靠 activityMask + 多色簇滤掉; ④ `*.framedump.*` gitignored (~700MB/视频), 重建跑 `npx tsx scripts/dump-frames.ts`; ⑤ ffmpeg select 表达式 ~100 项解析崩 ("Cannot allocate memory") — 现用单一 between 区间已规避。
- **下一步方向 (2026-07-10 正⑫后: 先验侧破局, 路线 = 字典先验分层解码)**: 负⑤-⑪把**似然侧**判死 (单格特征天花板 73%, 素材物理, 用户不再供片) — 但⑫证明解码不需要 75% 逐格精度, 需要的是把候选空间从 18^n 压到解法语法级 + 整段总分排序。路线图: ① **LL 解码器生产化**: 候选生成 (ZBLL/PLL/OLL 字典 + AUF/y + 手滑调整对增广), 对齐不借 GT (κ 从开拧前静止帧直读已知打乱; 槽/span 旋转身份走 lattice-track; 候选内旋转记账在逆放里天然处理), 尾段起点对 L 边缘化 (±2 已验可行); ② **F2L 逐对扩展**: 已知打乱正向 + 已解 LL 反向夹逼, 每对候选 = F2L 公式字典 × 槽序, 跨段一致性过滤强 (LL 起点态可达性是⑫还没用上的巨型过滤器); ③ 若②卡: 整解法联合解码 (cross/xcross 生成 + 分层 beam)。已知风险 (⑫追加 2 后升级为**前置硬条件**): 逐链面身份不是"工程化收尾"而是**未解问题** — 七界扫描证明现有跟踪信息 (span/窗口空间位置/时间片恒定性) 全部不够, ①动工前必须先落地 (a) 整体旋转估计 (几何面身份) 或 (b) 原始色度通道 (破 R/O/Y → 颜色面身份) 之一; 字典收录率靠调整对/变体增广兜 (v4 手滑序列可辨), 旧残项 (跟踪帧读色升级/render&compare/运动模糊) 仍是低期望, 只在①②卡精度时再回头。**2026-07-10 更新 (⑬追加): ② F2L 延伸已实测 → 自由搜索过拟合天花板 ~+6.5 < LL 颜色欠债 ~10, 搜索侧判死; 前置硬条件 (提取层原始色度 / 几何朝向) 坐实为唯一破局点, 后续别再在搜索侧加花招。** **2026-07-10 再更新 (⑭): 提取层原始色度这条已首次踩实——kNN(Lab) 逐视频标定 obs +6.6pt 直接把 v2 顶到 rank 1 (证明"更准的颜色分类器"就是杠杆, 无需几何朝向); 但增益锁死逐视频白平衡, 静态跨视频/legit 迁移收不回 (obs=vivid、解码更差), 白平衡归一化反降。→ **生产化真正卡口从"搜索花招"收窄为"逐视频颜色标定如何零 GT 获得"——而这条 2026-07-10 也测死了** (⑭: 静态迁移 obs=vivid, EM 自训练 realistic f=65% 崩, 无自举立足点)。**故颜色路彻底封死, 唯一剩的破局杠杆 = 几何朝向估计** (与颜色无关: 提取层估整体旋转→虚拟 rotation token→面身份, 纯光学; 面身份是⑫追加 2 七界扫描证明的唯一分水岭)。旧残项 (render&compare/运动模糊/手部) 维持低期望。**2026-07-11 更新 (⑰): 几何朝向的晶格粒度形式也判死** (事件检测 AUC 0.63 / 逐链姿态分类≤多数类; 且目检实锤面身份随手腕连续变 ±45°, 恒定性物理上不存在)。剩余未试选项按期望排序: ① 原始像素整魔方姿态估计 (轮廓/双面四边形, 高成本, 与负⑦几何残值同框, 投入前重估); ② v1 型放下翻面的靶向修复 (不需全程几何, 只桥接收尾一段); ③ 接受现状记分板 (LL 段 2/5 rank1 + 4/5 top6), 把精力移到全解准确率的其它侧。**

### 旧 Python 链 (冻结存档)

- Step 2 分类 ~60–63% 逐段准确率撞天花板; 根本瓶颈是 229 样本 + 极端不平衡（`roadmap.md` §11 有完整失败实验清单，别重复踩）
- Step 1 自动分段与手标差距大（video 3: 自动 12 段 vs 手标 45 段），oracle 实验全部基于手标分段；生产化需分段容错（搜索已支持空段/复合段候选）
- ROI 坐标（`template_cnn.py` / `greedy_reverse.py`）为 4K 硬编码且**固定 ROI 已被目检否决**(魔方大范围移动), TS 侧一律整帧降采样 + 逐帧定位
