var e={id:`grand-slam`,titleZh:`大满贯 — 同一项目集齐三冠 + WR`,titleEn:`Grand Slam — three-tier podium plus a WR in one event`,badgeZh:`选手`,badgeEn:`Person`,introZh:["一个 `(选手, 项目)` 算「大满贯」需要四件事同时成立:在该项目登上**世界锦标赛**(WC)的决赛领奖台、**所在大洲冠军赛**的决赛领奖台、**所在国家锦标赛**的决赛领奖台,并且在职业生涯**至少一次**刷新过该项目的 single 或 average WR。","门槛高度叠加 — 既要常年保持世界顶级,又要在主场国和大洲都拿到名次,还得有那么一刻是地球最快。`onlyFirst=1` 进一步只留三个领奖台都是金牌(pos=1)的人,这是「全金大满贯」。"],introEn:['A `(person, event)` qualifies as a "Grand Slam" when **four** things hold at once: a final-round podium at a **World Championship** (WC), a final-round podium at the person\'s **Continental Championship**, a final-round podium at their **National Championship**, and **at least one** WR (single or average) at some point in their career in that event.','The bar stacks — sustained world-top form plus winning at home and on the continent, with a moment of literally being fastest on Earth. The `onlyFirst=1` toggle further restricts to people whose three podiums were all gold (pos=1) — the "all-gold" slam.'],stats:[{value:`3 + 1`,labelZh:`台阶`,labelEn:`Tiers`,hintZh:`WC + 大洲 + 国锦 领奖台,外加任一 WR`,hintEn:`WC + continental + national podium, plus any WR`},{value:`17`,labelZh:`覆盖项目`,labelEn:`Events covered`,hintZh:`当前 17 个官方项目逐个独立计算`,hintEn:`Computed independently per active event`},{value:`pos ≤ 3`,labelZh:`领奖台口径`,labelEn:`Podium criterion`,hintZh:`只看 finals(round_type c/f)`,hintEn:"Finals only — `round_type_id` in c/f"},{value:`1 d`,labelZh:`API 缓存`,labelEn:`API cache`,hintZh:`/v1/wca/grand-slam 走 24h Cache-Control`,hintEn:`/v1/wca/grand-slam served with 24h Cache-Control`}],sourceZh:["CI 端读 WCA dump 的 `results` + `championships` + `eligible_country_iso2s_for_championship`,本地折叠成 `wca_grand_slam` 一行 = `(wca_id, event_id)`。冠军赛分三类:`world` / `_continent` 前缀 / 国家 iso2;`greater_china` 等多国共享冠军赛走 eligibility 表反查。运行时只读 PG。"],sourceEn:["CI reads `results` + `championships` + `eligible_country_iso2s_for_championship` from the WCA dump, then folds them into `wca_grand_slam` keyed by `(wca_id, event_id)`. Championships split three ways: `world`, leading-underscore continent codes, and ISO2 country codes; multi-country championships (e.g. `greater_china`) resolve via the eligibility table. The server only reads PG."],sourceCode:{lang:`sql`,body:`SELECT gs.wca_id, gs.event_id, gs.best_value, gs.avg_value,
       gs.has_wr, gs.is_only_first,
       gs.world_champ_comp_id, gs.world_champ_pos,
       gs.continental_champ_comp_id, gs.continental_champ_pos,
       gs.national_champ_comp_id, gs.national_champ_pos,
       p.name AS person_name
FROM wca_grand_slam gs
JOIN wca_persons p ON p.wca_id = gs.wca_id
WHERE gs.event_id = ?
ORDER BY gs.best_value NULLS LAST;`},steps:[{titleZh:`挑出决赛领奖台行`,titleEn:`Filter to final-round podium rows`,bodyZh:"只看 `round_type_id ∈ {c, f}` 且 `pos BETWEEN 1 AND 3` 的 `results` 行。预赛 / 半决无效 — 必须是 finals。",bodyEn:"Keep `results` rows where `round_type_id ∈ {c, f}` and `pos BETWEEN 1 AND 3`. Heats / semis don't count — must be finals."},{titleZh:`按 comp 分到三档`,titleEn:`Route each podium to one of three tiers`,bodyZh:"`compId ∈ worldChampComps` → WC 档;`continentalChampComps.get(compId) === person 的大洲` → 大洲档;`nationalChampComps.get(compId) === person 国籍`(或 `multiCountryNatComps` 包含)→ 国锦档。同档多次留 `pos` 最小那次。",bodyEn:"`compId ∈ worldChampComps` → WC tier; `continentalChampComps.get(compId) === person's continent` → continental tier; `nationalChampComps.get(compId) === person's country` (or the multi-country set contains it) → national tier. If the person podiums multiple times in the same tier, keep the row with the smallest `pos`."},{titleZh:`独立扫一遍 WR`,titleEn:`Separately sweep for WR`,bodyZh:'同一 `(person, event)` 任意一行的 `regional_single_record = "WR"` 或 `regional_average_record = "WR"` 即标 `has_wr = TRUE`。不限轮次 / 比赛类型 — 一次 WR 终生有效。',bodyEn:'Any row for the same `(person, event)` with `regional_single_record = "WR"` or `regional_average_record = "WR"` flips `has_wr = TRUE`. No round or comp-type restriction — one WR is enough, forever.'},{titleZh:`四交集 → 大满贯`,titleEn:`Four-way intersection → Grand Slam`,bodyZh:"一行进 `wca_grand_slam` 必须 `worldChampPos / contChampPos / natChampPos` 全部非空,**并且** `hasWrSingle || hasWrAvg`。`is_only_first = (worldChampPos = 1 AND contChampPos = 1 AND natChampPos = 1)`。",bodyEn:"A row lands in `wca_grand_slam` only if `worldChampPos / contChampPos / natChampPos` are all non-null **and** `hasWrSingle || hasWrAvg`. Then `is_only_first = (worldChampPos = 1 AND contChampPos = 1 AND natChampPos = 1)`."},{titleZh:`请求时 join 出 enriched 行`,titleEn:`Enrich at request time`,bodyZh:"`/v1/wca/grand-slam?event=&onlyFirst=` 从 `wca_grand_slam` 按 `event_id` + 可选 `is_only_first` / `has_wr` 过滤,join `wca_persons` / `wca_competitions` / `wca_countries` 补齐姓名 / 比赛名 / iso2,按 `best_value` 升序最多返回 5000 行。",bodyEn:"`/v1/wca/grand-slam?event=&onlyFirst=` filters `wca_grand_slam` by `event_id` plus optional `is_only_first` / `has_wr`, joins `wca_persons` / `wca_competitions` / `wca_countries` for names + iso2, and returns up to 5000 rows sorted by `best_value`.",highlight:!0}],edgesZh:['大洲冠军赛归属看选手**当前**国籍对应的大洲(`persons.country_id` → `countries.continent_id`),不是比赛举办地。换国籍的选手可能"重新具备资格"。','"领奖台" 严格按 `pos` 取(WCA 官方裁定),不重算成绩 — 即使有人 DNF average 但 `pos = 3` 也算上。',"`greater_china` 等多国共享冠军赛通过 `eligible_country_iso2s_for_championship` 反查;台湾 / 香港 / 澳门选手在 China Championship 上的领奖台计入国锦档。","`onlyFirst` 不要求三场是同一年 — 只要三场都拿过金牌即可,时间跨度不限。"],edgesEn:['Continental tier is decided by the person\'s **current** nationality → continent (`persons.country_id` → `countries.continent_id`), not by where the comp was held. People who changed citizenship may "re-qualify".',"Podium uses raw `pos` (WCA official placement); we don't recompute from values. A DNF'd average with `pos = 3` still counts.","Multi-country championships (e.g. `greater_china`) resolve via `eligible_country_iso2s_for_championship`; podiums by TW / HK / MO competitors at China Championship credit their national tier.","`onlyFirst` does not require the three golds to be in the same year — across a career is fine."],related:[{id:`sum-of-ranks`,titleZh:`全项目排名`,titleEn:`Sum of Ranks`,hintZh:`另一个"全能选手"指标 — 把世界排名相加`,hintEn:`Another all-rounder lens — sum of world ranks instead of podiums`},{id:`all-events-done`,titleZh:`全项目达成`,titleEn:`All Events Done`,hintZh:`从首参赛到 17 项都至少完赛一次的耗时`,hintEn:`Days from first comp to having at least one result in all 17 events`},{id:`wr_aoxr`,titleZh:`AoXR — 跨轮平均`,titleEn:`AoXR — across-round average`,hintZh:`同样靠 WR 列表筛人 — has_wr=TRUE 选手集合的交集`,hintEn:`Also keyed off the WR list — overlaps the has_wr=TRUE cohort`},{id:`grand-slam`,toStat:!0,titleZh:`直接打开大满贯排名`,titleEn:`Open the Grand Slam table`,hintZh:`看实际选手 / 项目切换 / 全金过滤`,hintEn:`Live table + event picker + only-gold toggle`}]},t={id:`all-results`,titleZh:`全部成绩排名 — 全 11M 行的可分页搜索`,titleEn:`All Results — paginated search over every WCA result`,badgeZh:`查询`,badgeEn:`Lookup`,introZh:["把 WCA dump 里的全部 single + average 成绩展平成一张 `wca_results_flat` 表(~11M 行,无 cap),让前端按项目 / single 还是 average / 国家 / 年 / 月 / 选手或比赛名搜索,翻页深至几百万也得在 <300 ms 内返回。","关键挑战在**深分页**:`ORDER BY value LIMIT/OFFSET` 走 OFFSET 1,000,000 时,朴素查询要先 join 三张表再丢弃前 100 万行,要 10 s+。这里用「派生表 + late-join 走 id PK」+ `INCLUDE (id)` 索引,让内子查询走 Index Only Scan 不回表,外层再 enrich 100 行。"],introEn:["Flattens the WCA dump's single + average results into one `wca_results_flat` table (~11M rows, no cap) so the client can filter by event / single vs average / country / year / month / person or comp name and paginate millions of rows deep in under 300 ms.","The hard part is **deep pagination**: `ORDER BY value LIMIT/OFFSET` with `OFFSET 1,000,000` would naïvely join three tables and then discard the first million rows — 10 s+. The route uses a **derived-table + late-join on the id PK** pattern with an `INCLUDE (id)` index so the inner subquery is Index-Only and the outer only enriches 100 rows."],stats:[{value:`~11 M`,labelZh:`行数`,labelEn:`Rows`,hintZh:`single + average 各占一行`,hintEn:`One row each for single and average`},{value:`6`,labelZh:`主索引`,labelEn:`Main indexes`,hintZh:`wrf_main / country / wca_id / comp_id / year / comp_lookup`,hintEn:`wrf_main / country / wca_id / comp_id / year / comp_lookup`},{value:`< 300 ms`,labelZh:`OFFSET 1 M`,labelEn:`At OFFSET 1 M`,hintZh:`late-join + INCLUDE(id) + VACUUM 后 Heap Fetches = 0`,hintEn:`late-join + INCLUDE(id) + VACUUM → Heap Fetches = 0`},{value:`200`,labelZh:`页大小上限`,labelEn:`Page size cap`,hintZh:`MAX_SIZE 防大查询拖慢服务`,hintEn:`MAX_SIZE prevents huge pulls from starving the server`}],sourceZh:["CI 端逐 `event_id` 流式扫描 `results`,每行可能产出 0 / 1 / 2 条 TSV(`best > 0` 一条 single,`average > 0` 再一条 average);末尾 3 列 `round_type_id / format_id / record_tag` 是为 `/comp` 页 fast-path 准备的,无 cap。`load.sql` 在 PG 上 `DROP + CREATE + COPY`,完成后 `VACUUM (ANALYZE)` 让 visibility map 干净 — 否则 Index Only Scan 还得回表,深页 OFFSET 又退化。"],sourceEn:["CI streams `results` per `event_id`, emitting 0 / 1 / 2 TSV rows per source row (one single if `best > 0`, one average if `average > 0`); the trailing `round_type_id / format_id / record_tag` cols feed the `/comp` page fast-path — no cap anywhere. `load.sql` does `DROP + CREATE + COPY` then `VACUUM (ANALYZE)` so the visibility map is clean; without that, Index Only Scan re-fetches the heap and deep OFFSET regresses."],sourceCode:{lang:`sql`,captionZh:`深分页 late-join — 内子查询只走 INCLUDE(id) 覆盖索引,外层用 id PK 回表`,captionEn:`Deep-page late-join — inner subquery rides INCLUDE(id), outer joins via id PK`,body:`SELECT q.value, q.wca_id, t.person_country_id, co.iso2,
       t.comp_id, c.name AS comp_name, t.comp_date, t.attempts, p.name
FROM (
  SELECT t.id, t.value, t.wca_id
  FROM wca_results_flat t
  WHERE t.event_id = ? AND t.is_avg = ?
  ORDER BY t.value ASC, t.wca_id ASC
  LIMIT ? OFFSET ?
) q
JOIN wca_results_flat t ON t.id = q.id
JOIN wca_persons p ON p.wca_id = t.wca_id
LEFT JOIN wca_countries co ON co.id = t.person_country_id
LEFT JOIN wca_competitions c ON c.id = t.comp_id
ORDER BY q.value ASC, q.wca_id ASC;`},steps:[{titleZh:`基础过滤:event + type`,titleEn:`Base filter: event + type`,bodyZh:"`event_id` 校验 `VALID_EVENTS`(21 个含废止项目),`type` 只接 `single` / `average`;`333mbf` 拒绝 average。这两个值进 `wrf_main` 索引前缀。",bodyEn:"`event_id` validated against `VALID_EVENTS` (21 incl. discontinued); `type` is `single` or `average` only; `333mbf` rejects `average`. Both values form the leading prefix of `wrf_main`."},{titleZh:`附加过滤:country / year / month`,titleEn:`Optional filters: country / year / month`,bodyZh:"`country` 走 2 字符 ISO2 → 反查 `wca_countries.id`;`year` 落到派生列 `comp_year`(STORED `EXTRACT(YEAR FROM comp_date)::SMALLINT`),`month` 走 `EXTRACT(MONTH FROM comp_date)`。`year` 命中 `wrf_year` 索引;month 是 post-filter。",bodyEn:"`country` accepts 2-char ISO2, resolved via `wca_countries.id`. `year` hits the generated column `comp_year` (`STORED EXTRACT(YEAR FROM comp_date)::SMALLINT`); `month` is `EXTRACT(MONTH FROM comp_date)` as a post-filter. `year` rides the `wrf_year` index."},{titleZh:`自由文本 q:两张表 ILIKE`,titleEn:`Free-text q: ILIKE two tables`,bodyZh:"`q` 同时在 `wca_persons.name` 和 `wca_competitions.name` 上 `ILIKE %q%`,各 LIMIT 200,union 进一个 `IN (...)` 列表喂给主查询的 `(wca_id IN ... OR comp_id IN ...)`。两边都空 → 直接返回 `total: 0`。",bodyEn:"`q` ILIKEs both `wca_persons.name` and `wca_competitions.name` with `LIMIT 200` each. Their ids feed `(wca_id IN ... OR comp_id IN ...)` in the main predicate. Both empty → short-circuit `total: 0`."},{titleZh:`派生表内子查询走 Index Only Scan`,titleEn:`Inner derived query stays Index-Only`,bodyZh:"内层只 `SELECT id, value, wca_id` — 三列都覆盖在 `wrf_main INCLUDE (id)` 里。`ORDER BY value, wca_id LIMIT ? OFFSET ?` 在索引上线性走,OFFSET 1 M 也只 ~250 ms,`Heap Fetches: 0`(前提 VACUUM 过)。",bodyEn:"Inner query only `SELECT id, value, wca_id` — all three columns live inside `wrf_main INCLUDE (id)`. `ORDER BY value, wca_id LIMIT ? OFFSET ?` walks the index linearly; OFFSET 1 M still ~250 ms with `Heap Fetches: 0` (assuming a recent VACUUM)."},{titleZh:`外层用 id PK 回表 + enrich + 总数`,titleEn:`Outer joins via id PK, enrich, count`,bodyZh:"`JOIN wca_results_flat t ON t.id = q.id` 走 PK,只回 100 行;再 join 三张字典表补 `name` / `comp_name` / `iso2`。并发跑一条 `COUNT(*)` 给 `total`(相同 WHERE)。",bodyEn:"`JOIN wca_results_flat t ON t.id = q.id` rides the PK and only resolves 100 rows; three lookup joins add `name` / `comp_name` / `iso2`. A separate `COUNT(*)` with the same WHERE produces `total`.",highlight:!0}],edgesZh:["`OFFSET 1 M+` 是真实负载 — 数据集大到要这种写法。直接 `ORDER BY + LIMIT/OFFSET + 三表 JOIN` 单写法在 PG 上深页要 10 s+,被慢日志钉过。","`person_country_id` 是**该次比赛时**选手的国籍(per result 列),不是当前国籍 — 国旗在历史成绩里展示的是当年的旗。","`q` 同时空 + 没条件时全表扫,但 LIMIT 200 防止 ILIKE 命中 100k 选手时 IN list 爆掉。","`333mbf` 没有 average — 请求 `type=average` 直接 400,不静默返空。"],edgesEn:["`OFFSET 1 M+` is real workload — the table is big enough that this pattern is required. Plain `ORDER BY + LIMIT/OFFSET + 3 joins` regresses to 10 s+ on PG and showed up in slow logs.","`person_country_id` is nationality **at the time of the comp** (a per-result column), not current nationality — historical rows show the flag worn back then.","`q` empty + no other filter is allowed (full scan), but ILIKE results are LIMIT-200 so a query matching 100k persons doesn't blow up the `IN` list.","`333mbf` has no average — `type=average` returns 400 rather than silently empty."],related:[{id:`cohort-ranks`,titleZh:`参赛届别排名`,titleEn:`Cohort Ranks`,hintZh:`同库的另一种切片:按首参赛年分组`,hintEn:`Same dataset sliced by first-competition year`},{id:`wr_aoxr`,titleZh:`AoXR — 跨轮平均`,titleEn:`AoXR — across-round average`,hintZh:`更窄的衍生指标 — 单场比赛多轮 average 平均`,hintEn:`A narrower derivative — averaging round averages within one comp`},{id:`all-results`,toStat:!0,titleZh:`直接打开全成绩表`,titleEn:`Open the All Results table`,hintZh:`看翻页 + 过滤 + 搜索的实际表现`,hintEn:`Live table — pagination + filters + free-text search`}]},n={id:`cohort-ranks`,titleZh:`参赛届别排名 — 按"首参赛年"分组排名`,titleEn:`Cohort Ranks — leaderboards bucketed by year of first comp`,badgeZh:`排名`,badgeEn:`Ranking`,introZh:[`把所有选手按**首次参赛年份**(cohort)分组,然后在每组内部按每项目的**生涯累积最佳**排名。"2014 届"就是把 2014 年第一次报名 WCA 比赛的人挑出来,看他们今天在 333 single 的相对位置。`,'看一个选手要不要给"届别同期最强"的标签 — 是同年入场的人里最快的,而不是世界总榜上的排位。`country` 参数下排名按国家排;否则按世界排。'],introEn:[`Bucket every competitor by the **year of their first competition** (cohort), then rank within each bucket by their **lifetime best** per event. The "2014 cohort" is everyone whose first WCA registration was in 2014 — and where they currently stand on 333 single relative to peers from that year.`,'Answers "fastest of their year" — local ranking within the cohort rather than the global leaderboard. With a `country` param the rank becomes a within-country rank inside the cohort; without it, world rank.'],stats:[{value:`~10 M`,labelZh:`行数`,labelEn:`Rows`,hintZh:`cohort × event × single/average × person`,hintEn:`cohort × event × single/average × person`},{value:`2003+`,labelZh:`cohort 范围`,labelEn:`Cohort range`,hintZh:`WCA 复办年起,1982 唯一一场不纳入 cohort 分组`,hintEn:`From the WCA-revival year; the lone 1982 comp is not a cohort here`},{value:`生涯累积`,labelZh:`排名口径`,labelEn:`Ranking metric`,hintZh:`取每人到今天为止的 PB,不是某一年内的最佳`,hintEn:`Lifetime PB to date — not best-within-year`},{value:`wr / cr`,labelZh:`两列预排好`,labelEn:`Two ranks stored`,hintZh:`world_rank / country_rank 都预计算,按 country 切换无需重排`,hintEn:`world_rank + country_rank precomputed — country toggle = re-pick column`}],sourceZh:["CI 端先扫一次 `results JOIN competitions` 算每人 `first_comp_date`,再在主循环里按 cohort 分组对生涯累积 PB 排序,`assignRanks()` 同分并列同名次,落到 `wca_cohort_ranks(cohort_year, event_id, is_avg, wca_id, value, country_id, world_rank, country_rank)`。"],sourceEn:["CI first scans `results JOIN competitions` to derive each person's `first_comp_date`. In the main event loop, lifetime-best per person gets bucketed by cohort and sorted; `assignRanks()` handles ties (equal value → equal rank). Result lands in `wca_cohort_ranks(cohort_year, event_id, is_avg, wca_id, value, country_id, world_rank, country_rank)`."],sourceCode:{lang:`sql`,body:`SELECT cr.wca_id, cr.value, cr.country_id, co.iso2,
       cr.world_rank, cr.country_rank, p.name
FROM wca_cohort_ranks cr
JOIN wca_persons p ON p.wca_id = cr.wca_id
LEFT JOIN wca_countries co ON co.id = cr.country_id
WHERE cr.cohort_year = ?
  AND cr.event_id = ?
  AND cr.is_avg = ?
  /* AND cr.country_id = ? */
ORDER BY cr.world_rank ASC, cr.wca_id ASC
LIMIT ? OFFSET ?;`},steps:[{titleZh:`算每人 first_comp_date`,titleEn:`Derive each person's first_comp_date`,bodyZh:"`SELECT r.person_id, r.competition_id, c.start_date FROM results r JOIN competitions c ORDER BY c.start_date, r.competition_id` 顺扫,per pid 留最早一行 → `firstComp.get(pid).year` 当作 cohort。",bodyEn:"`SELECT r.person_id, r.competition_id, c.start_date FROM results r JOIN competitions c ORDER BY c.start_date, r.competition_id`, then keep the first row per person → `firstComp.get(pid).year` is the cohort."},{titleZh:`生涯累积 PB`,titleEn:`Carry forward lifetime PB`,bodyZh:"主循环按 event 处理 `results`(按 `start_date, id` 升序),per (event, person) 维护 `Acc { best, avg, country }`,只在严格更小时更新。结果 = 每人到今天为止的 single PB / average PB。",bodyEn:"The main loop iterates `results` per event (sorted by `start_date, id`), maintaining `Acc { best, avg, country }` per (event, person), updating on strict improvement. Net result: every person's lifetime single PB / average PB."},{titleZh:`按 cohort 分桶 + 排名`,titleEn:`Bucket by cohort, rank`,bodyZh:"`for (cohortYear, persons in cohort)`:`sort by val ASC`,`assignRanks()` 同时给 `world_rank`(列表整体)和 `country_rank`(同国内子计数器)。同分并列 — `wr` 不增加,但 cohort 总计数继续。",bodyEn:"For each `(cohortYear, persons_in_cohort)` group: sort by `val ASC`, then `assignRanks()` assigns both `world_rank` (over the full list) and `country_rank` (per-country running counter). Ties share a rank but the running counter keeps stepping."},{titleZh:`查询时选 world_rank 或 country_rank 排序`,titleEn:`At query time, sort by world_rank or country_rank`,bodyZh:"`/v1/wca/cohort-ranks?cohort=&event=&type=&country=` 走 `coh_world` 或 `coh_country` 索引。`country` 给定 → `ORDER BY country_rank` 且加 `country_id = ?` 过滤;否则 `ORDER BY world_rank`。",bodyEn:"`/v1/wca/cohort-ranks?cohort=&event=&type=&country=` rides either `coh_world` or `coh_country`. With `country` set, `ORDER BY country_rank` plus `country_id = ?`; otherwise `ORDER BY world_rank`.",highlight:!0}],edgesZh:[`cohort = **首参赛年**,与选手出生年 / 注册 WCA-ID 年都无关 — 一个 2018 第一次比赛的选手永远是 2018 届,即便有 2019 改的 WCA-ID 也不变。`,`生涯累积 = 今天为止,不是 cohort 当年内 — "2010 届的 333 第一"看的是这个人至今 PB,不是他 2010 年内的成绩。`,"`333mbf` 没有 average — `type=average` 400,与 all-results 同口径。","同分并列(`assignRanks()` 用 `prevVal` 比较);PG 端 ORDER BY 加 `wca_id ASC` 兜底,翻页稳定。"],edgesEn:[`Cohort = **year of first comp**, unrelated to birth year or WCA-ID issue date — someone whose first comp is 2018 stays in the 2018 cohort even if their WCA-ID was issued in 2019.`,`Lifetime cumulative = up to today, NOT confined to the cohort year — "fastest of 2010" uses today's PB, not their 2010-season best.`,"`333mbf` has no average — `type=average` returns 400, same as all-results.","Ties share a rank (`assignRanks()` compares `prevVal`); PG ORDER BY adds `wca_id ASC` for stable pagination."],related:[{id:`all-results`,titleZh:`全部成绩排名`,titleEn:`All Results`,hintZh:`同源数据,不分 cohort 的全局视图`,hintEn:`Same source, global view without cohort bucketing`},{id:`sum-of-ranks`,titleZh:`全项目排名`,titleEn:`Sum of Ranks`,hintZh:`世界排名衍生指标 — 17 项相加`,hintEn:`Another rank-derived metric — sums world ranks across 17 events`},{id:`cohort-ranks`,toStat:!0,titleZh:`直接打开届别排名`,titleEn:`Open the cohort table`,hintZh:`看届别 / 项目 / 国家切换`,hintEn:`Live table — cohort / event / country switches`}]},r={id:`success-rate`,titleZh:`项目成功率 — solved / attempted`,titleEn:`Success Rate — solved ÷ attempted`,badgeZh:`选手`,badgeEn:`Person`,introZh:['每人每项目算一个比率:`solved / attempted`,其中 `attempted` 计入所有非 DNS 的 `best`(`best != 0`,含 DNF 和 valid),`solved` 只数 `best > 0`(valid)。直观地说就是"举手了的次数里成功的占比"。',"主要用在盲拧 / FMC / 大盲这种高 DNF 项目 — `333bf` 默认进页面,3 次起步就能上榜。每项目排名按 `pct_x10000` 降序,同分再按 `attempted` 降序(尝试多的同分排前)。"],introEn:['Per person, per event: `solved / attempted`. `attempted` counts every non-DNS `best` (i.e. `best != 0`, including DNFs); `solved` counts only `best > 0` (valid). Intuitively: "of all times they showed up to the table, what fraction stuck".',"Mainly used for blind / FMC / big-blind — high-DNF events. `333bf` is the default; minimum 3 attempts to qualify. Leaderboard sorts by `pct_x10000` descending, tie-broken by `attempted` descending (more tries at the same percentage wins)."],stats:[{value:`~2 M`,labelZh:`行数`,labelEn:`Rows`,hintZh:`每 (event, person, attempted ≥ 3)`,hintEn:`One row per (event, person) with attempted ≥ 3`},{value:`≥ 3`,labelZh:`最小尝试`,labelEn:`Min attempts`,hintZh:`默认门槛,API 可调高`,hintEn:`Default floor; API param lets you raise it`},{value:`pct × 10⁴`,labelZh:`存整数`,labelEn:`Stored as integer`,hintZh:`9999 = 99.99%,稳定排序`,hintEn:`9999 = 99.99% — stable integer sort`},{value:`DNS 排除`,labelZh:`best = 0 跳过`,labelEn:`best = 0 skipped`,hintZh:`没动手不算 attempted`,hintEn:`Not-attempted rows don't count`}],sourceZh:["CI 在主循环里 per `(event, person)` 维护 `[solved, attempted]`:`if (r.best > 0) solved++; if (r.best !== 0) attempted++`。整 17 项扫完一次性写 `wca_success_rate(event_id, wca_id, country_id, solved, attempted, pct_x10000)`,`pct_x10000 = round(solved/attempted * 10000)`。`attempted < 3` 直接不输出。"],sourceEn:["In the main event loop, per `(event, person)`: `if (r.best > 0) solved++; if (r.best !== 0) attempted++`. After all 17 events, dump to `wca_success_rate(event_id, wca_id, country_id, solved, attempted, pct_x10000)` where `pct_x10000 = round(solved/attempted * 10000)`. Rows with `attempted < 3` are dropped."],sourceCode:{lang:`sql`,body:`SELECT sr.wca_id, sr.country_id, co.iso2,
       sr.solved, sr.attempted, sr.pct_x10000, p.name
FROM wca_success_rate sr
JOIN wca_persons p ON p.wca_id = sr.wca_id
LEFT JOIN wca_countries co ON co.id = sr.country_id
WHERE sr.event_id = ?
  AND sr.attempted >= ?
ORDER BY sr.pct_x10000 DESC,
         sr.attempted DESC,
         sr.wca_id ASC
LIMIT ? OFFSET ?;`},formulae:[{labelZh:`公式`,labelEn:`Formula`,expr:`pct = solved / attempted,  attempted = #{r : r.best ≠ 0},  solved = #{r : r.best > 0}`,bodyZh:"`r.best = 0` 是 DNS(根本没尝试),不进分母;`r.best = -1` 是 DNF(尝试了但失败),进分母不进分子。",bodyEn:'`r.best = 0` means "did not start" — excluded from the denominator. `r.best = -1` means DNF — counted in the denominator but not the numerator.'}],steps:[{titleZh:`逐 result 扫,只看 best`,titleEn:`Iterate results, look at best only`,bodyZh:"CI 主循环里每行 `result` 检查 `r.best`:`0` → skip(DNS);`-1` → `attempted++`(DNF);`> 0` → `solved++; attempted++`。average 列在这里**不参与**计数。",bodyEn:"In the CI event loop, inspect `r.best`: `0` → skip (DNS); `-1` → `attempted++` (DNF); `> 0` → `solved++; attempted++`. The `average` column is **not** factored in here."},{titleZh:`按 (event, person) 累计`,titleEn:`Accumulate per (event, person)`,bodyZh:"`successAcc: Map<event, Map<pid, [solved, attempted]>>`。同人同项目的所有比赛所有轮次都加在一起 — 没有时间窗 / cohort 切片。",bodyEn:"`successAcc: Map<event, Map<pid, [solved, attempted]>>`. All rounds across all comps for the same `(event, person)` aggregate into one cell — no time window or cohort slicing."},{titleZh:`过滤 + 百分比定点化`,titleEn:`Filter, fixed-point percentage`,bodyZh:"`attempted < 3` 丢掉(避免一次 fluke);`pct_x10000 = round(solved/attempted * 10000)`,稳定整数排序避免浮点同分判定漂移。",bodyEn:"Drop `attempted < 3` (avoid one-fluke entries); `pct_x10000 = round(solved/attempted * 10000)` — stable integer ordering, no float-equality drift on ties."},{titleZh:`查询端复合排序`,titleEn:`Compound sort at query time`,bodyZh:"`ORDER BY pct_x10000 DESC, attempted DESC, wca_id ASC`:同百分比下尝试多的优先(更可信);最后 `wca_id` 兜底翻页稳定。`sr_event` / `sr_event_country` 索引覆盖。",bodyEn:"`ORDER BY pct_x10000 DESC, attempted DESC, wca_id ASC`: at equal percentage, more tries wins (more reliable); `wca_id` is the stable-pagination tiebreaker. Covered by `sr_event` / `sr_event_country`.",highlight:!0}],edgesZh:["`r.best = 0` 是 WCA 编码里的 DNS(Did Not Start),不能跟 DNF 混。`attempted` 不计入 DNS — 否则一个登记了但没上手的轮次会拖低比例。","attempts(5 次 raw)不直接看 — 用最终 `best`。一个 attempts = [4321, -1, -1, -1, -1] 的轮次 `best = 4321 > 0`,算 1 个 solved + 1 个 attempted。",`不区分 single / average。333bf / 444bf / 555bf / 333mbf 这类项目的 average 行根本不存在(WCA 规则),mo3 也是聚合统计形式,这里只看 best。`,`榜单按百分比看是高分高;不同项目 baseline 差异大 — 333 几乎人均 99%+,333mbf 60% 已是顶级。`],edgesEn:["`r.best = 0` is DNS (Did Not Start) in WCA encoding — distinct from DNF. DNS doesn't count as `attempted`, otherwise registering and no-showing would tank your ratio.","We don't inspect raw attempts — we use the final `best`. A round with attempts = [4321, -1, -1, -1, -1] has `best = 4321 > 0`, so 1 solved + 1 attempted.",`Not split by single vs average. Blind events (333bf / 444bf / 555bf / 333mbf) have no average rows by WCA rule; mo3 is the format. We just count best rows.`,`Higher = better; baselines vary wildly across events — near-100% for 333, ~60% is elite for 333mbf.`],related:[{id:`dnf_rate_by_event`,titleZh:`DNF 率(按项目)`,titleEn:`DNF rate by event`,hintZh:`整体角度 — 同样的数据按事件聚合`,hintEn:`Event-level aggregate of the same numerator/denominator`},{id:`most_solves_before_bld_success`,titleZh:`盲拧首成功前 DNF 数`,titleEn:`DNFs before first BLD success`,hintZh:`盲拧专属姐妹指标 — 首成功的尝试代价`,hintEn:`BLD-specific sibling — DNFs before the first valid solve`},{id:`success-rate`,toStat:!0,titleZh:`直接打开成功率榜`,titleEn:`Open the Success Rate table`,hintZh:`看默认 333bf + 项目切换 + 国家过滤`,hintEn:`Live table — defaults to 333bf, event + country pickers`}]},i={id:`all-events-done`,titleZh:`全项目达成 — 集齐 17 项所用的天数`,titleEn:`All Events Done — days from first comp to all 17 events checked off`,badgeZh:`选手`,badgeEn:`Person`,introZh:['一个人需要多久 — 从他/她的首场 WCA 比赛起算 — 才能在**当前 17 个官方项目**里每项都至少有一次 valid 成绩(`best > 0`)?这个"首次集齐"的最早日期减去首参赛日期就是 `days_to_complete`。','页面默认按 `days_to_complete ASC` 排,看"最快全勤"。未集齐的人(`is_done = FALSE`)按 `done_count` 降序排,看离全勤还差几项。'],introEn:['How long does it take, counting from a person\'s first WCA competition, to post at least one valid result (`best > 0`) in **every one of the 17 currently-active events**? `days_to_complete` is the earliest such "all done" date minus their first-comp date.',"Default sort is `days_to_complete ASC` — fastest to full sweep. Incomplete people (`is_done = FALSE`) sort by `done_count` descending — how close to the finish line."],stats:[{value:`17`,labelZh:`目标项目`,labelEn:`Target events`,hintZh:`ACTIVE_EVENTS 当下口径,废止项目不计`,hintEn:`Current ACTIVE_EVENTS set — discontinued events not counted`},{value:`~150 k`,labelZh:`行数`,labelEn:`Rows`,hintZh:`每个参加过 WCA 的人一行`,hintEn:`One row per person with any WCA result`},{value:`best > 0`,labelZh:`达成口径`,labelEn:`Done criterion`,hintZh:`一次 valid single 就算这个项目达成`,hintEn:`Any single valid result counts the event as done`},{value:`max(date)`,labelZh:`达成日 = 17 个首达成日的最晚`,labelEn:`Achievement date = max of 17 firsts`,hintZh:`最后一项的首达成日就是大功告成日`,hintEn:`Latest first-result among the 17 events seals the achievement`}],sourceZh:["CI 端 per `(person, event ∈ ACTIVE_EVENTS)` 维护一个 `first_done_date`,在主循环里看到 `r.best > 0` 时取 `min(curr, r.compDate)`。扫完后每人有 17 个日期数组,`doneCount = 非空数`,`isDone = (doneCount === 17)`,`maxDate = max(那 17 个)`,`days_to_complete = (maxDate − first_comp_date)`。"],sourceEn:["CI maintains per `(person, event ∈ ACTIVE_EVENTS)` a `first_done_date`. In the main loop, when `r.best > 0`, take `min(curr, r.compDate)`. After scanning all events: each person has 17 dates → `doneCount = non-empty count`, `isDone = (doneCount === 17)`, `maxDate = max(those 17)`, `days_to_complete = (maxDate − first_comp_date)`."],sourceCode:{lang:`sql`,body:`SELECT aed.wca_id, aed.country_id, co.iso2,
       aed.done_count, aed.is_done,
       aed.first_comp_id, aed.first_comp_date,
       aed.achievement_comp_id, aed.achievement_comp_date,
       ac.name AS achievement_comp_name,
       aed.days_to_complete, aed.total_comp_count, p.name
FROM wca_all_events_done aed
JOIN wca_persons p ON p.wca_id = aed.wca_id
LEFT JOIN wca_competitions ac ON ac.id = aed.achievement_comp_id
WHERE aed.is_done = TRUE
ORDER BY aed.days_to_complete ASC, aed.wca_id ASC
LIMIT ? OFFSET ?;`},formulae:[{labelZh:`公式`,labelEn:`Formula`,expr:`days = max₍e ∈ 17₎ firstDone(p, e) − firstComp(p)`,bodyZh:"`firstDone(p, e)` = 选手 p 在项目 e 上首次 `best > 0` 的比赛日期;若有任一项目无成绩 → 全无定义,`is_done = FALSE` 且 `days_to_complete IS NULL`。",bodyEn:"`firstDone(p, e)` = the comp date of person p's first `best > 0` in event e; if any of the 17 is missing → undefined overall, with `is_done = FALSE` and `days_to_complete IS NULL`."}],steps:[{titleZh:`识别首参赛日 / 项目`,titleEn:`Resolve first-comp date and id`,bodyZh:"CI 预扫一次 `results JOIN competitions ORDER BY start_date, comp_id`,per pid 留最早一行 → `firstComp.compId / date / year`。后续 `personCompSet` 同时收集所有参赛 comp 用于 `total_comp_count`。",bodyEn:"CI presweep: `results JOIN competitions ORDER BY start_date, comp_id`, keep the earliest row per pid → `firstComp.compId / date / year`. `personCompSet` simultaneously tracks all attended comps for `total_comp_count`."},{titleZh:`17 项 first-done 数组`,titleEn:`17-slot first-done array`,bodyZh:"主循环里,`r.best > 0 && eventIdx != null` 时:`personEventFirstDone[pid][eventIdx]` 没值就填,有值且 `r.compDate < curr` 就更新。`eventIdx` 来自 `EVENT_INDEX`(对齐 `ACTIVE_EVENTS` 顺序)。",bodyEn:"In the main loop, when `r.best > 0 && eventIdx != null`: fill `personEventFirstDone[pid][eventIdx]` if empty, or overwrite when `r.compDate < curr`. `eventIdx` comes from `EVENT_INDEX`, aligned with `ACTIVE_EVENTS` order."},{titleZh:`聚合 done_count + maxDate`,titleEn:`Aggregate done_count + maxDate`,bodyZh:"扫完所有 event 后,per pid 对 17 元数组算:`doneCount = 非空数`,`maxDate = 最晚日期`,`isDone = (doneCount === 17)`。",bodyEn:"After all events scanned, per pid the 17-element array yields `doneCount = non-empty count`, `maxDate = latest of those dates`, `isDone = (doneCount === 17)`."},{titleZh:`回查达成比赛`,titleEn:`Locate the achievement comp`,bodyZh:"`isDone` 时在 `personCompSet[pid]` 里找 `compInfo.startDate === maxDate` 的任一 comp → `achievementCompId`;`daysToComplete = floor((maxDate − firstCompDate) / 86400000)`。",bodyEn:"When `isDone`, scan `personCompSet[pid]` for any comp whose `compInfo.startDate === maxDate` → `achievementCompId`. Then `daysToComplete = floor((maxDate − firstCompDate) / 86400000)`."},{titleZh:`查询端两种排序`,titleEn:`Two sort orders at query time`,bodyZh:"`/v1/wca/all-events-done?onlyDone=&country=`:`onlyDone=1`(默认) → `days_to_complete ASC`;`onlyDone=0` 全集 → `done_count DESC, days_to_complete ASC NULLS LAST`。`aed_done` / `aed_country_done` 索引覆盖。",bodyEn:"`/v1/wca/all-events-done?onlyDone=&country=`: `onlyDone=1` (default) → `days_to_complete ASC`. `onlyDone=0` (full set) → `done_count DESC, days_to_complete ASC NULLS LAST`. Covered by `aed_done` / `aed_country_done`.",highlight:!0}],edgesZh:['"17 项" 是**今天**的口径(`ACTIVE_EVENTS`)。Magic / Master Magic / 333ft / 333mbo 等已废止项目不计 — 历史上没人需要拧 21 项才算"全勤"。',"只要 single 有效就算 — 不要求 average。这对盲拧 / 大盲(没 average 的项目)友好,否则 `333mbf` 永远算不上。",'同一项目的"首次有效"取最早 — 一个选手 2008 年 4x4 DNF / 2010 才 valid,我们把 2010 那场算成 `firstDone[4x4]`。',"`country` 过滤是按选手**当前**国籍(`country_id`),不是首参赛时国籍 — 换国籍的选手会跟着挪。"],edgesEn:['"17 events" is **today\'s** set (`ACTIVE_EVENTS`). Magic / Master Magic / 333ft / 333mbo and other discontinued events are out — no one historically had to clear 21 events to count as "full sweep".',"Singles only — average not required. Friendly to blind / multi-blind (no average rows), otherwise `333mbf` would lock everyone out.","First-valid takes the **earliest** valid result — someone who DNF'd 4x4 in 2008 and only solved valid in 2010 has `firstDone[4x4] = 2010`.","`country` filter uses **current** nationality (`country_id`), not nationality at first comp — citizenship changes follow."],related:[{id:`shortest_time_to_get_all_singles`,titleZh:`集齐所有 single 的最短用时`,titleEn:`Shortest time to get all singles`,hintZh:`stats-build 老指标,同概念另一种实现`,hintEn:`Equivalent stats-build classic — parallel implementation of the same idea`},{id:`shortest_time_to_get_all_singles_and_averages`,titleZh:`集齐所有 single + average`,titleEn:`Shortest time, singles + averages`,hintZh:`更严格版本 — 还要每项 average 也至少一次`,hintEn:`Stricter sibling — additionally requires an average in each event`},{id:`grand-slam`,titleZh:`大满贯`,titleEn:`Grand Slam`,hintZh:`另一个"全能"维度 — 三级冠军 + WR`,hintEn:`Another all-rounder lens — three-tier podiums + WR`},{id:`all-events-done`,toStat:!0,titleZh:`直接打开全项目达成榜`,titleEn:`Open the All Events Done table`,hintZh:`看默认 onlyDone 模式 + 国家切换`,hintEn:`Live table — default onlyDone view + country switcher`}]},a={id:`sum-of-ranks`,titleZh:`全项目排名 — 17 项世界排名之和`,titleEn:`Sum of Ranks — sum of world ranks across all 17 events`,badgeZh:`排名`,badgeEn:`Ranking`,introZh:[`每个选手在 17 个官方项目里各有一个世界排名(没成绩的项目用"该项目参赛人数 + 1"作为惩罚),把这 17 个数加起来就是 Sum of Ranks。**越小越全能** — 数值低代表又多又强。`,'默认按全 17 项 single 排;`type=average` 切到 average 视图,`country` 给定时换成 country_rank 之和;`events` 参数可以挑子集(`333,222,444`)。子集模式用 CTE 实时算每个项目的参赛人数,缺项的"参赛人数 + 1"也走子集的范围。'],introEn:[`For each person, every one of the 17 official events has a world rank (events without any result get a penalty rank of "participants + 1"). The sum across all 17 is Sum of Ranks. **Lower = more well-rounded** — small number means broad + strong.`,'Default view = all 17 events × single. `type=average` flips to averages, `country` swaps in country_rank sums, and `events` lets you pick a subset (e.g. `333,222,444`). Subset mode uses a CTE to count participants per event inline; the "participants + 1" penalty for missing events is computed within the chosen scope.'],stats:[{value:`17`,labelZh:`事件位`,labelEn:`Event slots`,hintZh:`与 ACTIVE_EVENTS 一一对应,固定顺序`,hintEn:`One per ACTIVE_EVENTS slot, fixed order`},{value:`~300 k`,labelZh:`行数`,labelEn:`Rows`,hintZh:`person × (single|average)`,hintEn:`One row per (person, is_avg)`},{value:`participants + 1`,labelZh:`缺项惩罚`,labelEn:`Missing-event penalty`,hintZh:`比该项目最后一名再差一名`,hintEn:`One worse than the last-ranked competitor of that event`},{value:`wr / cr`,labelZh:`存两套排名`,labelEn:`Two rank arrays`,hintZh:`ranks_world + ranks_country 各 17 元素`,hintEn:`ranks_world + ranks_country — 17 INTEGER[] each`}],sourceZh:["CI 端先 `accByEvent[event] → Acc { best, avg }` 累积每人每项 PB,然后对每个 event 各跑一次 `assignRanks(sortedList)` 同时给 world_rank + country_rank;最后 per person 把 17 项的 wr / cr 装进 `INTEGER[]`,缺项当场补 `participants + 1` 计入 total。**领奖台过滤**走 `best_final_pos`(跨所有 event 的 final round MIN(pos)):未登台 = `=0 OR >3`,殿军之王 = `=4`。落 `wca_person_ranks(wca_id, is_avg, country_id, events_done, total_world_rank, total_country_rank, best_final_pos, ranks_world[], ranks_country[])`。"],sourceEn:["CI accumulates per-event PB via `accByEvent[event] → Acc { best, avg }`. For each event, `assignRanks(sortedList)` assigns both world_rank + country_rank in one pass. Per person, the 17 ranks land in two `INTEGER[]` columns; missing events absorb a `participants + 1` penalty in the precomputed total. **Podium filtering** uses `best_final_pos` (MIN(pos>0) across every event's final-round results): no-podium = `=0 OR >3`, fourth-place king = `=4`. Stored in `wca_person_ranks(wca_id, is_avg, country_id, events_done, total_world_rank, total_country_rank, best_final_pos, ranks_world[], ranks_country[])`."],sourceCode:{lang:`sql`,captionZh:`子集模式 — CTE 算每个选中项目的参赛人数,缺项 fallback 到 ep.pX + 1`,captionEn:`Subset mode — CTE counts participants per selected event; missing slots fall back to ep.pX + 1`,body:`WITH ep AS (
  SELECT SUM(CASE WHEN ranks_world[1] > 0 THEN 1 ELSE 0 END) AS p0,
         SUM(CASE WHEN ranks_world[2] > 0 THEN 1 ELSE 0 END) AS p1
         /* ... more selected events ... */
  FROM wca_person_ranks WHERE is_avg = ?
)
SELECT pr.wca_id, p.name, pr.ranks_world,
       (CASE WHEN pr.ranks_world[1] > 0 THEN pr.ranks_world[1] ELSE ep.p0 + 1 END
      + CASE WHEN pr.ranks_world[2] > 0 THEN pr.ranks_world[2] ELSE ep.p1 + 1 END
       /* ... */) AS subset_total
FROM wca_person_ranks pr CROSS JOIN ep
JOIN wca_persons p ON p.wca_id = pr.wca_id
WHERE pr.is_avg = ?
ORDER BY subset_total ASC;`},formulae:[{labelZh:`公式`,labelEn:`Formula`,expr:`totalRank(p) = Σₑ (rank(p, e)  if rank > 0 else participants(e) + 1)`,bodyZh:'`e` 跑遍 17 项(或子集);`rank(p, e) = 0` 表示该项目无成绩,fallback 到"参赛人数 + 1"(比最后一名再差一名)。',bodyEn:'`e` ranges over all 17 events (or chosen subset). `rank(p, e) = 0` indicates no result in that event, fallback to "participants + 1" (one worse than the bottom-ranked competitor).'}],steps:[{titleZh:`17 项各自排一次`,titleEn:`Rank each event once`,bodyZh:"per `ACTIVE_EVENTS[i]`,从 `accByEvent` 取 `Acc`,sort by PB,`assignRanks()` 给同分并列 world_rank + country_rank。`eventParticipantsSingle[i]` / `eventParticipantsAvg[i]` 记参赛人数。",bodyEn:"For each `ACTIVE_EVENTS[i]`, pull `Acc` from `accByEvent`, sort by PB, run `assignRanks()` to assign world + country rank with tie handling. `eventParticipantsSingle[i]` / `eventParticipantsAvg[i]` remember each event's competitor count."},{titleZh:`装 17 元数组 + 缺项惩罚`,titleEn:`Build 17-slot arrays + missing penalty`,bodyZh:"per (pid, is_avg):17 元数组 `ranksW[i] = eventRanks[i].get(pid)?.wr ?? 0`(同 ranksC);total 加的是 `wr` 或缺项时的 `participants + 1`。`333mbf` 无 average → average 视图里该位永远 0 且跳过累加。",bodyEn:"Per (pid, is_avg): build 17-slot `ranksW[i] = eventRanks[i].get(pid)?.wr ?? 0` (likewise `ranksC`). The total adds either `wr` or the `participants + 1` penalty for missing slots. `333mbf` has no average — that slot stays 0 and is skipped in the average-view total."},{titleZh:`记 best_final_pos / events_done`,titleEn:`Record best_final_pos / events_done`,bodyZh:"主循环每条 result 命中 `round_type_id IN ('c', 'f')` 且 `pos > 0` 时,跨 event 更新该人的 `best_final_pos = MIN(pos)`。0 = 从未在任何 final 拿过有效成绩。`events_done` 计 `wr > 0` 的项目数。`/v1/wca/sum-of-ranks?hidePodium=1` → `best_final_pos = 0 OR > 3`;`?bestMisser=4` → `best_final_pos = 4`(殿军之王)。",bodyEn:"In the main loop, every result with `round_type_id IN ('c', 'f')` and `pos > 0` updates that person's `best_final_pos = MIN(pos)` across events. 0 = never produced a valid final-round result. `events_done` counts slots with `wr > 0`. `/v1/wca/sum-of-ranks?hidePodium=1` → `best_final_pos = 0 OR > 3`; `?bestMisser=4` → `best_final_pos = 4` (fourth-place king)."},{titleZh:`全 17 项 → 走预计算列`,titleEn:`All 17 events → use precomputed total`,bodyZh:"默认没传 `events`(或传了全集):`ORDER BY total_world_rank`(无 country)或 `total_country_rank`(有 country),走 `pr_total` / `pr_country_total` 索引,毫秒级。",bodyEn:"No `events` param (or all 17): `ORDER BY total_world_rank` (no country) or `total_country_rank` (with country), riding `pr_total` / `pr_country_total`. Sub-millisecond."},{titleZh:`子集 → CTE + array indexing 实时算`,titleEn:`Subset → CTE + array indexing on the fly`,bodyZh:"`events` 传了真子集:CTE `ep` 算每个选中项目的参赛人数;主查询用 `CASE WHEN ranks_X[i+1] > 0 THEN ... ELSE ep.pX + 1 END` 累加 `subset_total`,再 `ORDER BY` 它。CTE 不受 `hidePodium` 影响 — 参赛人数是固有属性。",bodyEn:"A real subset was passed: CTE `ep` counts participants per selected event; the main query builds `subset_total` with `CASE WHEN ranks_X[i+1] > 0 THEN ... ELSE ep.pX + 1 END` and `ORDER BY subset_total ASC`. The CTE is **not** affected by `hidePodium` — participant count is intrinsic to the event.",highlight:!0}],edgesZh:["`333mbf` 在 average 视图里**跳过**,不参与累加 — 它没有 average 这种概念;真用 17 项的 average sum 会让它单挑一类人。",`缺项惩罚 = 该项目"参赛人数 + 1",不是固定大数。这避免冷门项目(参赛人数少)和热门项目权重失衡 — 跳过 333 损失约 30 万排名,跳过 333mbf 只损失几千。`,'`hidePodium` 只过滤主查询行,不改 CTE 的参赛人数 — 否则隐藏一个 podium 选手会让其他人的"缺项罚"变小,语义混乱。','`country` 给定 → ORDER BY `total_country_rank`,返回的 `ranks` 数组也切到 `ranks_country` — 同一行下"按国家看排名"是一致 view。'],edgesEn:["`333mbf` is **skipped** in the average view — there's no average concept for it; summing 17 averages would single it out as a class of its own.",`Missing-event penalty = that event's "participants + 1", not a fixed large number. Prevents weighting imbalance between rare events (few participants) and crowded ones — skipping 333 costs ~300k ranks, skipping 333mbf costs a few thousand.`,"`hidePodium` filters the main query rows but does **not** affect the CTE's participant count — otherwise hiding a podium person would shrink everyone else's missing-event penalty, garbled semantics.",'With `country` set, both `ORDER BY total_country_rank` and the returned `ranks` array switches to `ranks_country` — single consistent "national view" within the row.'],related:[{id:`grand-slam`,titleZh:`大满贯`,titleEn:`Grand Slam`,hintZh:`另一种"全能"过滤 — 看顶尖一行;sum-of-ranks 看分布`,hintEn:`Sibling all-rounder filter — Grand Slam is the elite cut; sum-of-ranks shows the spread`},{id:`all-events-done`,titleZh:`全项目达成`,titleEn:`All Events Done`,hintZh:`前置条件 — 没集齐 17 项,sum 里会扛缺项惩罚`,hintEn:`Precondition — anyone short of 17 events eats the missing-event penalty here`},{id:`cohort-ranks`,titleZh:`参赛届别排名`,titleEn:`Cohort Ranks`,hintZh:`单项目按届别看,sum-of-ranks 全项目无届别`,hintEn:`Single event by cohort; sum-of-ranks aggregates all events without cohorting`},{id:`sum-of-ranks`,toStat:!0,titleZh:`直接打开 Sum of Ranks`,titleEn:`Open Sum of Ranks`,hintZh:`看默认 17 项 + 子集 picker + hidePodium 切换`,hintEn:`Live table — default 17 events + subset picker + hidePodium toggle`}]},o={"grand-slam":e,"all-results":t,"cohort-ranks":n,"success-rate":r,"all-events-done":i,"sum-of-ranks":a},s={wr_aoxr:{id:`wr_aoxr`,titleZh:`AoXR — 单场比赛平均的均值`,titleEn:`AoXR — Average across rounds of a single competition`,badgeZh:`世界纪录`,badgeEn:`World record`,introZh:['`AoXR` 把一场比赛里**所有轮次**的 average 再做一次平均。X = 1, 2, 3, 4,分别对应"恰好打了 1/2/3/4 轮"的情况。比赛圈内最常用 `Ao3R`(预赛 + 半决 + 决赛)和 `Ao4R`(加一个 1/4 决)。',`它度量一个选手「单场稳定输出」的能力 —— 不像 single 那样靠手气,也不像 ao5 那样有掐尾。被 4 轮挤进来的选手,平均都得过硬。`],introEn:["`AoXR` is the **average of the averages** a competitor posted across all rounds in a single competition. X ∈ {1, 2, 3, 4}, depending on how many rounds they actually competed in.",`It measures within-comp consistency: not luck-of-the-single, and broader than ao5 because every round counts.`],stats:[{value:`4`,labelZh:`档位数`,labelEn:`Tiers`,hintZh:`Ao1R / Ao2R / Ao3R / Ao4R`,hintEn:`Ao1R / Ao2R / Ao3R / Ao4R`},{value:`17`,labelZh:`覆盖项目`,labelEn:`Events covered`,hintZh:`有 average 的全部 WCA 项目`,hintEn:`All WCA events with averages`},{value:`排名+历史`,labelZh:`两个视图`,labelEn:`Two views`,hintZh:`当前榜 + WR 进化时间线`,hintEn:`Current leaderboard + WR evolution timeline`},{value:`< 1 ms`,labelZh:`页内查询`,labelEn:`Lookup time`,hintZh:`预计算 JSON,无服务器调用`,hintEn:`Precomputed JSON, no backend calls`}],sourceZh:["直接读 WCA 开发者 dump 的 `results` 表 — 每行一个轮次成绩;join `persons`(`sub_id = 1` 取主身份)和 `competitions`(取 `start_date` / `cell_name`)。只看 `average > 0`(过滤 DNF 和未提交)。"],sourceEn:["Reads the WCA developer-dump `results` table — one row per round result — joined to `persons` (`sub_id = 1`) for the main identity and `competitions` for `start_date` / `cell_name`. Filters `average > 0` (drops DNFs and not-attempted)."],sourceCode:{lang:`sql`,body:`SELECT event_id, average, round_type_id,
       competition_id, person_id,
       competition.start_date
FROM results
JOIN persons ON persons.wca_id = results.person_id
            AND persons.sub_id = 1
JOIN competitions ON competitions.id = results.competition_id
WHERE average > 0
ORDER BY start_date;`},steps:[{titleZh:`按 (比赛, 选手) 分组`,titleEn:`Group by (competition, person)`,bodyZh:"把同一选手同一场比赛的所有轮次 average 收集到一组。键 = `competition_id:person_id`。",bodyEn:"Collect all round averages a single person posted at one competition. Key = `competition_id:person_id`."},{titleZh:`按轮次 type 排序 + 选 X 档`,titleEn:`Sort by round type, pick tier`,bodyZh:'组内按 `round_type_id` 排:`1/d → 2/e → 3/g → c/f`(预赛、半决、决赛、综合);再按"组里恰好有 X 个轮次"拆成 4 个 sub-stat(Ao1R / Ao2R / Ao3R / Ao4R)。',bodyEn:"Within a group, sort by `round_type_id` (1/d → 2/e → 3/g → c/f). Bucket each group by its round count X (1, 2, 3, or 4) — each bucket feeds one of Ao1R / Ao2R / Ao3R / Ao4R."},{titleZh:`算数平均`,titleEn:`Take the mean`,bodyZh:`组内 average 直接取**算术平均**(不像 ao5 不掐尾),得到一个数 = 该选手在该场比赛的 AoXR。`,bodyEn:`Plain **arithmetic mean** of the in-group averages (no trim like ao5) gives one number — that person's AoXR at that comp.`},{titleZh:`排名:每人最佳值前 10`,titleEn:`Ranking: per-person best, top 10`,bodyZh:`每个选手保留**最佳一个 AoXR**(最小的 = 最快);按项目排到 top 10。`,bodyEn:`For each person, keep their single best (smallest) AoXR; sort across persons, top 10 per event.`},{titleZh:`WR 历史:严格刷新`,titleEn:`WR history: strict refresh`,bodyZh:"把所有 (人, 比赛, AoXR) 按 `start_date` 正序扫描;只保留**严格刷新**当前最小值的那条(平 WR 不算)。这条规则就是 `filterWrHistory(strict=true)`。",bodyEn:"Scan all (person, comp, AoXR) records by `start_date`; keep only entries that strictly improve on the running minimum (ties don't count). Implemented by `filterWrHistory(strict=true)`.",highlight:!0}],formulae:[{labelZh:`公式`,labelEn:`Formula`,expr:`AoXR(p, c) = (1/X) · Σᵢ averageᵢ(p, c, roundᵢ)`,bodyZh:`p = 选手,c = 比赛,roundᵢ = 第 i 轮(按 type 排序),averageᵢ = 该轮 average。X = 该选手在该比赛的轮次数。`,bodyEn:`p = person, c = competition, roundᵢ = i-th round (sorted by type), averageᵢ = that round's average. X = number of rounds the person ran in that comp.`},{labelZh:`相邻 WR 进步百分比`,labelEn:`Gain % between consecutive WRs`,expr:`gain = (prev − cur) / prev × 100%`,bodyZh:`在 History 视图里 "Improvement" 列展示;首条没有 prev,留空。`,bodyEn:`Shown in the "Improvement" column of the History view; first row has no prev so the cell is blank.`}],edgesZh:["只算 `average > 0` 的轮次。DNF 平均(value = -1)和未提交(value = 0)被排除 — 一个选手即使打了 4 轮但有一轮 DNF,会被算成 Ao3R 而不是 Ao4R。",`同日两条不同比赛的 AoXR 不会合并 — 每条都是独立"事件"。`,`与 WCA 官方"Average of X solves"(ao5/mo3)完全不同 — 这里 X 是**轮次数**,不是单次还原数。`,"`sub_id = 1` 过滤副身份(改名、换国籍的选手 dump 里会有多行 persons,只取主行)。"],edgesEn:["Only rounds with `average > 0` count. DNF averages (-1) and not-attempted (0) are excluded — someone who ran 4 rounds with one DNF'd average lands in Ao3R, not Ao4R.",`Two AoXRs on the same date from different comps are not merged — each comp is its own event.`,`Different from WCA "Average of X solves" (ao5 / mo3) — here X counts **rounds**, not solves.`,"`sub_id = 1` filters out alt identities (renames, country changes — only the primary person row is kept)."],related:[{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`同库的 WR 指标总集 — single/average/bao5/wao5/mo5/bpa/wpa/...`,hintEn:`Sibling stat — all WR metrics: single/average/bao5/wao5/mo5/bpa/wpa/...`},{id:`average_of`,titleZh:`滚动平均 (Rolling Average)`,titleEn:`Rolling Average`,hintZh:`跨比赛的 N-次滑动平均,对照口径`,hintEn:`Sliding N-window average across comps — sibling concept`},{id:`best_round`,titleZh:`最佳轮次`,titleEn:`Best round`,hintZh:`单轮 average 排名(不跨轮平均)`,hintEn:`Single round average leaderboard (no cross-round avg)`},{id:`wr_aoxr`,toStat:!0,titleZh:`直接打开 AoXR 排名`,titleEn:`Jump to AoXR ranking`,hintZh:`看实际数据 / 项目切换`,hintEn:`Live data + event picker`}]},wr_current:{id:`wr_current`,titleZh:`当前世界纪录`,titleEn:`Current world records`,badgeZh:`世界纪录`,badgeEn:`World record`,introZh:["把所有 `regional_single_record = 'WR'` 或 `regional_average_record = 'WR'` 的轮次成绩拉出来,按 (项目, single/average) 取当前**仍未被刷新**的最小值。一行 = 一条当前 WR 持有事件,附 single 的全部 attempt 明细。",`"当前"意味着只显示**今天仍然站着的那条**:历史上同一项目可能有几十条 WR,这里只看最新的。`],introEn:["Pulls every round flagged `regional_single_record = 'WR'` or `regional_average_record = 'WR'`, then per (event, single/average) keeps the minimum value — i.e. the WR that **still stands today**.",`One row per currently-standing record, with full attempt breakdown for singles. Historic WRs that have since been broken are not shown.`],stats:[{value:`17 × 2`,labelZh:`行数(满载)`,labelEn:`Rows (full)`,hintZh:`每项目 single + average,无并列`,hintEn:`Single + average per event, no ties`},{value:`WR`,labelZh:`过滤条件`,labelEn:`Filter`,hintZh:`regional_single/average_record = WR`,hintEn:`regional_single/average_record = WR`},{value:`asc`,labelZh:`并列排序`,labelEn:`Tie order`,hintZh:`同值取更早的 start_date`,hintEn:`Earlier start_date wins ties`},{value:`attempts`,labelZh:`附明细`,labelEn:`Extra detail`,hintZh:`5 次 attempt 全展示`,hintEn:`All 5 attempts shown`}],sourceZh:["查 `results` 表,filter 任意一种 WR 标志;join `persons`(`sub_id = 1`)和 `competitions` 取人名 / 比赛名 / 日期。Single 的 attempts 用 `GROUP_CONCAT` 子查询拼出来,前端 split 后逐次格式化(避免多盲值含空格被错误拆分)。"],sourceEn:["Reads `results` filtered by either WR flag; joins `persons` (`sub_id = 1`) and `competitions` for name / comp / date. The single's attempts come from a `GROUP_CONCAT` subquery on `result_attempts`, then the client splits and formats each attempt (string split avoids the MBLD space-encoding quirk)."],sourceCode:{lang:`sql`,body:`SELECT r.event_id, r.best AS single, r.average,
       r.regional_single_record, r.regional_average_record,
       (SELECT GROUP_CONCAT(ra.value ORDER BY ra.attempt_number)
          FROM result_attempts ra WHERE ra.result_id = r.id) AS attempts,
       p.name, c.cell_name, c.start_date
FROM results r
JOIN persons p ON p.wca_id = r.person_id AND p.sub_id = 1
JOIN competitions c ON c.id = r.competition_id
WHERE r.regional_single_record = 'WR'
   OR r.regional_average_record = 'WR'
ORDER BY r.event_id, c.start_date;`},steps:[{titleZh:`拉出所有 WR 标志行`,titleEn:`Pull every WR-flagged result`,bodyZh:"一条 `results` 行可能 single 是 WR、average 是 WR、或两者都是。SQL 用 OR 把这三种都收进来,后续 transform 分两个口径分别看。",bodyEn:"A single `results` row may have a WR single, a WR average, or both. The SQL OR captures all three; downstream transform splits single vs average and processes each independently."},{titleZh:`按项目分组,各自取最小值`,titleEn:`Group by event, keep the min`,bodyZh:"遍历每个项目:single 看 `r.best`,average 看 `r.average`。在该项目内取**数值最小**的那条 = 当前 WR。",bodyEn:"For each event: single uses `r.best`, average uses `r.average`. Within that event's WR-flagged rows, the minimum value wins — that's the standing record."},{titleZh:`并列时选更早的`,titleEn:`Tie-break by earlier date`,bodyZh:"同值并列(常见于 333fm / 333bf 单次)按 `start_date` 升序保留 — 先设出来的优先;但所有并列条目都会被输出,前端表格按项目分组展示。",bodyEn:"Ties (common in 333fm / 333bf singles) are sorted ascending by `start_date` — the earliest holder lists first; every tied row is still emitted, so the table shows all co-holders."},{titleZh:`格式化 single 的 attempts`,titleEn:`Format single's attempts`,bodyZh:"只有 single 行才展示 5 次 attempt 明细 — 把 `GROUP_CONCAT` 出来的逗号字符串 split 后逐次过 `SolveTime.clockFormat()`。average 行的 attempts 跟单次 WR 的 attempts 是同一份,展示在 single 那行就够了。",bodyEn:"Singles get the per-attempt breakdown: split the comma-joined `GROUP_CONCAT` then run each through `SolveTime.clockFormat()`. Average rows reuse the same attempt list as the single — showing it on the single row only avoids duplication."},{titleZh:`输出 (项目, Single/Average, 时间, 人, 日期, 比赛)`,titleEn:`Emit (event, single/average, time, person, date, comp)`,bodyZh:"每条 WR 一行,日期格式化为 `YYYY-MM-DD`,人和比赛名都带 markdown 链接到 WCA 官网。",bodyEn:"One row per standing WR; date as `YYYY-MM-DD`; person / comp rendered as markdown links to the WCA site.",highlight:!0}],edgesZh:[`WR 标志由 WCA 官方在 dump 里直接打 — 不需要本地重算,但意味着新打破的 WR 要等下次 dump 刷新才显示。`,"Single 和 average 各自独立 — 一行 `results` 可能 single=WR、average 不是,或反之。",`并列的 single WR(尤其 333fm 历史 22 / 333bf 极快单次)全部展示,前端表格不去重。`,"该 stat 没有 history 视图 — 历史演化看 `wr_metric` 的 Single / Average 子项。"],edgesEn:[`WR flags come baked into the WCA dump — no re-computation here, but newly-broken records only appear after the next dump refresh.`,"Single and average are independent — one `results` row can be a WR single but not a WR average (or vice-versa).",`Tied singles (notably old 333fm = 22 moves, blazing 333bf singles) are all listed; no dedup at the UI layer.`,"No history view here — for time-series, see the Single / Average panels under `wr_metric`."],related:[{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`WR single / average 的历史演化时间线`,hintEn:`WR single / average evolution over time`},{id:`wr_dominance`,titleZh:`屠榜 (Dominance)`,titleEn:`Dominance`,hintZh:`当前 WR 持有者领先第二名多少 — 配套视角`,hintEn:`How far the standing holder leads #2 — companion angle`},{id:`grand-slam`,titleZh:`大满贯`,titleEn:`Grand slam`,hintZh:`同时持有 single + average WR 的稀有事件`,hintEn:`Rare moments of holding both single and average WR`},{id:`wr_current`,toStat:!0,titleZh:`直接打开当前 WR 表`,titleEn:`Jump to current WR table`,hintZh:`17 项 × {single, avg} 一目`,hintEn:`17 events × {single, avg} at a glance`}]},wr_metric:{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,badgeZh:`世界纪录`,badgeEn:`World record`,introZh:[`把"从一轮 5 次还原中能算出来的所有数"打包成一个聚合页:除了官方 single / average,还有 BAo5(5 中 3 最好均值)、WAo5(5 中 3 最差)、Mo5(裁不裁尾的总均值)、BPA / WPA(只看前 4 次的最好 / 最差可能 ao5)、Median、Best/Worst Counting、Worst、Variance、Best/Avg ratio 共 13 个指标。`,`同一份原始数据(每轮 5 次 attempt)被反复榨干 — 选手"运气"与"稳定性"的对照在这一页上一览。前端把 13 个指标按 Basic / Composite / Distribution 三组下拉切换。`],introEn:[`Bundles every derivable metric from a round's 5 solves into one aggregator: official Single / Average plus BAo5 (best 3 of 5), WAo5 (worst 3 of 5), Mo5 (mean of all 5, no trim), BPA / WPA (best / worst possible average from the first 4), Median, Best / Worst Counting, Worst, Variance, and Best/Avg Ratio — 13 metrics in total.`,`One raw input (5 attempts per round) repeatedly squeezed to surface luck vs consistency. The UI groups the 13 metrics into Basic / Composite / Distribution dropdowns.`],stats:[{value:`13`,labelZh:`指标子类`,labelEn:`Metric subclasses`,hintZh:`每个一份 RoundMetric 子类`,hintEn:`Each one a RoundMetric subclass`},{value:`3`,labelZh:`分组`,labelEn:`Groups`,hintZh:`Basic / Composite / Distribution`,hintEn:`Basic / Composite / Distribution`},{value:`2`,labelZh:`视图`,labelEn:`Views`,hintZh:`排名 + WR 历史`,hintEn:`Ranking + WR history`},{value:`EVENTS_WITH_AO5`,labelZh:`覆盖项目`,labelEn:`Event scope`,hintZh:`只看正式 ao5 项目(BLD/MBLD/FMC 除外)`,hintEn:`Official ao5 events only (excl. BLD / MBLD / FMC)`}],sourceZh:["本类自身**不查 SQL** — `query()` 返回空串。每个子类(`wr_bao5` / `wr_mo5` / `wr_bpa` / ...)各自走 `RoundMetric` 基类,基类用一份 batch SQL 把所有 ao5 项目的 attempt 拉出来共享,子类只覆写 `computeMetric(values)` 决定 5 个数怎么算。","聚合层把 13 份 `toJson()` 结果按 id 包成 `MetricPanel[]`,再附 `metricGroups` 元数据让前端渲染分组下拉。"],sourceEn:["This class issues **no SQL of its own** — `query()` returns ``. Each child (`wr_bao5` / `wr_mo5` / `wr_bpa` / ...) hooks into the `RoundMetric` base class, which runs one batch SQL across all ao5 events and shares the attempt rows. Each child only overrides `computeMetric(values)` to define how the 5 numbers collapse.","The aggregator wraps the 13 children's `toJson()` outputs as `MetricPanel[]` keyed by id, and adds `metricGroups` metadata so the UI can render group dropdowns."],sourceCode:{lang:`ts`,body:`// 13 个子类按 id 注册,逐个 import + run
for (const def of METRIC_DEFS) {
  const mod = await def.module();
  const inst = new (Object.values(mod).find(v => typeof v === 'function'))();
  const sub = await inst.toJson();
  metricPanels.push({
    id: def.id, labelEn: def.label,
    labelZh: sub.titleZh, panels: sub.panels,
  });
  if (global.gc) global.gc();   // 子统计完释放,防 OOM
}`,captionZh:`聚合层 — 逐子类 import + GC`,captionEn:`Aggregator — child-by-child import + GC`},steps:[{titleZh:`基类 batch SQL 抓 attempts`,titleEn:`Base class batch-loads attempts`,bodyZh:"`RoundMetric` 父类对所有 batch 子类 share 一份 SQL:每轮 attempt 用 `GROUP_CONCAT` 拼成字符串。所有 11 个 batch 子类共用这份缓存,避免 11 倍重复查询。",bodyEn:"`RoundMetric` shares one SQL across all batch-mode children — round attempts come back as a `GROUP_CONCAT` string. The cached rows feed all 11 batch children, dodging 11× redundant queries."},{titleZh:`子类定义"怎么从 5 个数得到 1 个"`,titleEn:"Each child defines `computeMetric(values)`",bodyZh:`只覆写一个方法:接收一个 length-5 的数组(可能含 DNF=-1),返回一个数或 null。例如 BAo5 = 前 3 小求均值;WAo5 = 后 3 大求均值;BPA = 前 4 个的 best-3 均值。`,bodyEn:`A child overrides one method: take a length-5 array (DNFs as -1), return a number or null. e.g. BAo5 = mean of the smallest 3; WAo5 = mean of the largest 3; BPA = best-3 of the first 4.`},{titleZh:`排名:每项目 Top 10 + WR 历史`,titleEn:`Rank: per-event top 10 + WR history`,bodyZh:"基类对每个子类输出双视图:Ranking(每人最佳指标值,项目 Top 10)和 History(按时间扫描严格刷新当前最小值的事件序列)。WR 历史走 `filterWrHistory(strict=true)`。",bodyEn:"Base class emits two panels per child: Ranking (per-person best metric, top 10 by event) and History (chronological scan keeping strict improvements). WR history uses `filterWrHistory(strict=true)`."},{titleZh:`聚合层拼成 MetricPanel[]`,titleEn:`Aggregator wraps into MetricPanel[]`,bodyZh:"13 份 sub `toJson()` 输出按 METRIC_DEFS 顺序串成数组,每项含 `id / labelEn / labelZh / panels`。前端用 `metricPanels[i].id` 在 dropdown 切换。",bodyEn:"The 13 sub `toJson()` outputs concatenate in METRIC_DEFS order; each entry holds `id / labelEn / labelZh / panels`. The UI matches `metricPanels[i].id` against the dropdown."},{titleZh:`附 metricGroups → UI 分三组下拉`,titleEn:`Emit metricGroups → UI three-section dropdown`,bodyZh:`Basic = {single, average}, Composite = {bao5, wao5, mo5, bpa, wpa}, Distribution = {median, bestc, worstc, worst, variance, ratio}。前端按这个分组渲染下拉,而不是 13 个扁平选项。`,bodyEn:`Basic = {single, average}, Composite = {bao5, wao5, mo5, bpa, wpa}, Distribution = {median, bestc, worstc, worst, variance, ratio}. UI renders a grouped dropdown rather than 13 flat items.`,highlight:!0}],formulae:[{labelZh:`BAo5 / WAo5 / Mo5`,labelEn:`BAo5 / WAo5 / Mo5`,expr:`BAo5 = (s₁+s₂+s₃)/3   WAo5 = (s₃+s₄+s₅)/3   Mo5 = (s₁+...+s₅)/5`,bodyZh:`其中 s₁ ≤ s₂ ≤ ... ≤ s₅ 为本轮 5 次升序排序后的成绩。5 次必须全有效(Mo5 / WAo5),BAo5 至少 3 次有效。`,bodyEn:`with s₁ ≤ s₂ ≤ ... ≤ s₅ being the round's 5 solves sorted ascending. Mo5 / WAo5 need all 5 valid; BAo5 needs at least 3 valid.`},{labelZh:`BPA / WPA(决赛前预测口径)`,labelEn:`BPA / WPA (pre-last-solve hypothetical)`,expr:`BPA = mean(best 3 of {s₁,s₂,s₃,s₄})   WPA = mean(worst 3 of {s₁,s₂,s₃,s₄})`,bodyZh:`只用前 4 次:第 5 次还没打,BPA = 假设第 5 次完美时能拿的 ao5 下限;WPA = 假设第 5 次崩盘时的上限。比赛镜头里常见"BPA 锁纪录"的来源。`,bodyEn:`Uses only the first 4: BPA = best ao5 achievable if solve 5 lands perfectly; WPA = worst ao5 if solve 5 implodes. The "BPA-locked" tension you see on stream comes from here.`}],edgesZh:["所有指标只覆盖**有 ao5 的项目**(`EVENTS_WITH_AO5`);BLD / MBLD / FMC 没有 5 次组,这页不参与。","BPA / WPA 用前 4 次,顺序敏感:第几次 attempt 由 `attempt_number` 决定,SQL 里 `GROUP_CONCAT(ORDER BY attempt_number)` 保证顺序。",`Worst Counting 要求至多 1 次 DNF(否则 ao5 本身 DNF);Worst 要求 5 次全有效。`,`Median 计算 ≥ 3 次 DNF 时返 null(不输出),否则取排序后 index=2 — DNF 会"挤"中位数向更高有效成绩偏。`],edgesEn:["All metrics target **ao5 events only** (`EVENTS_WITH_AO5`); BLD / MBLD / FMC have no 5-solve grouping and don't participate.","BPA / WPA are order-sensitive on the first 4 attempts: ordering comes from `attempt_number`, which SQL preserves via `GROUP_CONCAT(... ORDER BY attempt_number)`.",`Worst Counting tolerates at most 1 DNF (else the ao5 itself is DNF); Worst Solve requires all 5 valid.`,`Median returns null with ≥ 3 DNFs; otherwise picks the sorted index 2 — DNFs shift the median up to a higher valid solve.`],related:[{id:`wr_aoxr`,titleZh:`AoXR`,titleEn:`AoXR`,hintZh:`跨轮平均(单场比赛级),对照单轮指标`,hintEn:`Cross-round average (per-comp) — companion to per-round metrics`},{id:`wr_current`,titleZh:`当前 WR`,titleEn:`Current WR`,hintZh:`只看 single / average 两子项的当前持有`,hintEn:`Just the single / average panels frozen at today`},{id:`wr_dominance`,titleZh:`屠榜`,titleEn:`Dominance`,hintZh:`同维度看"领先程度"`,hintEn:`Same dimensions, but measuring the lead size`},{id:`wr_metric`,toStat:!0,titleZh:`直接打开 Metric 总表`,titleEn:`Jump to Metric explorer`,hintZh:`13 个指标 × 项目下拉`,hintEn:`13 metrics × event dropdown`}]},wr_dominance:{id:`wr_dominance`,titleZh:`屠榜 (Dominance)`,titleEn:`Dominance`,badgeZh:`世界纪录`,badgeEn:`World record`,introZh:[`"屠榜"度量当前榜首多远地甩开第二名。具体来说:在某项目全历史的成绩列表里,**当前最强**的选手 P 有多少条成绩**严格快于**所有其他选手的最佳成绩?那个数量 = dominance。`,`P 把前 5 / 前 10 / 前 N 都自己占了 = 高分屠榜;P 只赢出第二名 0.01 秒 = 极低 dominance,即便他确实是 WR 持有者。指标双维度(single / average)× 双视图(当前排名 / 历史演变),后者画出"屠榜深度"在时间轴上的演化。`],introEn:[`Dominance measures how far the top seat is from the rest. Concretely: across an event's full history, how many of the **current #1**'s individual results are **strictly faster than every other person's best**? That count is dominance.`,`P claims #1 through #N alone with all of their own marks = high-score domination; P beats #2 by 0.01s = near-zero dominance even though they hold the WR. Two metrics (single / average) × two views (live ranking / WR evolution), with the history view drawing how that depth grew.`],stats:[{value:`count`,labelZh:`主指标`,labelEn:`Primary metric`,hintZh:`P 严格快于 others_best 的成绩数`,hintEn:`Count of P's results strictly < others' best`},{value:`2 + 2`,labelZh:`面板`,labelEn:`Panels`,hintZh:`Single/Average × Ranking/History`,hintEn:`Single/Average × Ranking/History`},{value:`~5 M`,labelZh:`扫描行 (333)`,labelEn:`Rows scanned (333)`,hintZh:`逐 event 加载 result_attempts`,hintEn:`Per-event scan of result_attempts`},{value:`O(log n)`,labelZh:`更新成本`,labelEn:`Update cost`,hintZh:`每选手有序数组 + 二分插入`,hintEn:`Per-person sorted array + binary insert`}],sourceZh:["Single 走 `result_attempts`(每行一次 attempt),Average 走 `results.average`(每轮一行);都按 `start_date` 升序逐 event 扫一遍,**不带** person/comp 链接字符串(瘦行,500 万行省 ~2GB)。",'扫完后维护每选手的有序数组 + 当前 best;在"日期切换"边界检查 dominance 是否刷新历史最大值。最后一次性 `IN (...)` 查少量链接拼出来。'],sourceEn:["Single mode reads `result_attempts` (one row per solve), Average mode reads `results.average` (one row per round). Both scan per-event in `start_date` order, **without** embedded person/comp link strings (slim rows save ~2GB at 5M rows).","During the scan, maintain a sorted array + running best per person; at each date boundary check whether dominance just hit a new max. Final markdown links are resolved with one `IN (...)` query over the surviving few dozen IDs."],sourceCode:{lang:`sql`,body:`-- Single (瘦行)
SELECT result.person_id, ra.value,
       result.competition_id AS comp_id, competition.start_date
FROM result_attempts ra
JOIN results result ON result.id = ra.result_id
JOIN competitions competition ON competition.id = result.competition_id
WHERE ra.value > 0 AND result.event_id = '<event>'
ORDER BY competition.start_date;`},steps:[{titleZh:`逐 event 加载瘦行,按日期升序`,titleEn:`Per-event load slim rows, sorted by date`,bodyZh:"Single 看 `result_attempts.value`,Average 看 `results.average`。**只取 person_id / value / comp_id / start_date 四列** — 不在 SQL 里拼 markdown 链接(后置查询),避免百万行字符串膨胀。",bodyEn:"Single ← `result_attempts.value`; Average ← `results.average`. **Just four columns**: person_id / value / comp_id / start_date — markdown links are resolved later, dodging string bloat across millions of rows."},{titleZh:`每人维护有序数组 + best`,titleEn:`Per-person sorted array + best`,bodyZh:'边扫边二分插入(`bisectLeft`),保持 O(log n) 维护"每人的所有有效成绩排序数组"`pv` + 当前 best `pb`。',bodyEn:"Stream through, binary-insert each value (`bisectLeft`) into `pv[person]` (sorted array of all valid times for that person). Maintain `pb[person]` = running best."},{titleZh:`日期切换边界:算 dominance`,titleEn:`On date boundary: compute dominance`,bodyZh:"当 `start_date` 跳到下一天时,扫一遍 `pb`:找全局最快选手 P + 次快值 `others_best`;在 P 的有序数组里 `bisectLeft(others_best)` 得到 P **严格快于** others_best 的成绩数。这就是当日的 dominance count。",bodyEn:"When `start_date` rolls forward, scan `pb`: find global leader P + the second-best across everyone else (`others_best`). Then `bisectLeft(pv[P], others_best)` = number of P's results strictly faster than `others_best`. That's the day's dominance count."},{titleZh:`历史:严格刷新才记一条`,titleEn:`History: keep strict refreshes only`,bodyZh:"日期切换时,如果 count 严格大于 `maxDom`,记一条 WR event:当前 P / 当前 comp / 当前日期 + 该选手**首次达到屠榜**的比赛(`firstDom`)。后期算 improvement = `+(count - prev.count)` 和持续时长(days)。",bodyEn:"On boundary, if `count > maxDom` strictly, log a WR event: current P, comp, date, plus the comp where that person **first reached domination** (`firstDom`). Later compute improvement = `+(count - prev.count)` and run-length days."},{titleZh:`最后一次性 IN(...) 解链接,落 JSON`,titleEn:`Resolve markdown links in one IN(...) pass`,bodyZh:"所有 event 扫完,把累计到的 person/comp ID 集合用 `IN (...)` 一次查 `persons` / `competitions` 拼 markdown 链接;然后把瘦行的 ID 替换成链接字符串,生成 4 个 panel(2 指标 × {ranking, history})。",bodyEn:"After all events finish, batch-resolve every accumulated person_id / comp_id with one `IN (...)` query against `persons` / `competitions` to build markdown links; substitute slim-row IDs with link strings; emit 4 panels (2 metrics × {ranking, history}).",highlight:!0}],formulae:[{labelZh:`Dominance count`,labelEn:`Dominance count`,expr:`dom(P, t) = | { v ∈ results(P, ≤t) : v < min_{Q ≠ P} best(Q, ≤t) } |`,bodyZh:`t = 当前时间点。即:截至 t 时,选手 P 名下严格快于"任何其他选手最佳成绩"的成绩条数。`,bodyEn:`t = current point in time. The count of P's results (up to t) that are strictly faster than the best result of any other person (up to t).`}],edgesZh:["**平 (tied) 成绩不计入屠榜** — `bisectLeft` 找的是第一个 `>= target` 的位置,等于 `others_best` 的成绩不算 P 的赢分。",`Single 和 Average 是完全独立的两份扫描 — Single 在 attempt 级(同轮 5 次都算),Average 在轮级。`,'"日期边界"是按 `start_date` 字符串切的 — 同日多场比赛会合并在一个 boundary 同时结算,不会拆成两次。',"榜首换人时 `firstDom` 也会重置 — 新王朝从 0 开始累计,展示的不是该人成绩从首条开始而是从他**夺榜**那天起的累计。"],edgesEn:["**Tied results don't count** — `bisectLeft` returns the first `>= target` index, so values equal to `others_best` don't add to P's margin.",`Single and Average run as fully independent scans — Single at attempt-granularity (all 5 solves of each round count), Average at round-granularity.`,"Date boundaries split on `start_date` strings — multiple comps on the same day collapse into one boundary, not two.","When the leader changes, `firstDom` resets — the new reign counts from the day they took the top seat, not from their first-ever result."],related:[{id:`wr_current`,titleZh:`当前 WR`,titleEn:`Current WR`,hintZh:`P 是谁/拿什么成绩 — 屠榜的对象`,hintEn:`Who P is and their headline time — the subject of dominance`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`13 个 metric 的 WR 演化序列`,hintEn:`WR evolution across 13 derived metrics`},{id:`longest_standing_records`,titleZh:`持续最久的纪录`,titleEn:`Longest-standing records`,hintZh:`WR 多久不被刷 vs 屠榜多深,两个稳定性视角`,hintEn:`How long unbroken vs how deep the lead — two stability lenses`},{id:`wr_dominance`,toStat:!0,titleZh:`直接打开屠榜表`,titleEn:`Jump to Dominance table`,hintZh:`看每项目当前 #1 屠了多少`,hintEn:`See how deep each event's #1 dominates`}]},wr_non_pr:{id:`wr_non_pr`,titleZh:`非 PR 的纪录`,titleEn:`Non-PR records`,badgeZh:`世界纪录`,badgeEn:`World record`,introZh:[`正常情况下,WR = PR(个人纪录) — 你打出来比谁都快,自然也比自己以前快。但偶尔会反着:选手在某轮成绩**不是自己最快**,却仍然好过史上其它所有人,也就是说他自己以前的某条更好成绩才是真正历史最快。这页找的就是这种"虽不是 PR 但仍最强"的成绩。`,`常见场景:大佬连发数轮,中后段一个 ao5 比自己之前的 PB 慢,但仍超过其他人最佳。该 ao5 不刷新 WR(因为他自己更快的那条已经是 WR 了),但它仍是**所有非 PR 成绩里**的最强。统计输出双视图:每人的最佳 non-PR(排名) + WR 进化(历史)。`],introEn:[`Usually WR = PR — beating everyone implies beating yourself. But occasionally a result is **not the person's best yet still better than everyone else** — i.e. their own earlier result is the true historical leader. This page surfaces those "not-PR-but-still-king" results.`,`Typical setup: a top cuber posts several rounds in a row; a mid-event ao5 is slower than their PB but faster than every other person's best. That ao5 doesn't set a new WR (their own faster one already holds it), but it is still **the strongest result among all non-PR results**. Two views: per-person best non-PR (ranking) + non-PR WR evolution (history).`],stats:[{value:`val > pb`,labelZh:`入选条件`,labelEn:`Inclusion rule`,hintZh:`严格慢于自己当前 PB 才算 non-PR`,hintEn:`Strictly slower than own running PB`},{value:`2 + 2`,labelZh:`面板`,labelEn:`Panels`,hintZh:`Single/Average × Ranking/History`,hintEn:`Single/Average × Ranking/History`},{value:`%`,labelZh:`历史 Δ`,labelEn:`History Δ`,hintZh:`相邻 non-PR WR 的提升百分比`,hintEn:`Gain % between consecutive non-PR WRs`},{value:`O(n)`,labelZh:`单次扫描`,labelEn:`Single scan`,hintZh:`每选手维护一个 PB 数`,hintEn:`One running PB per person`}],sourceZh:["逐 event 查所有 `results.best` / `results.average` > 0 的行(同时取两列减少查询次数),join `persons` + `competitions` 取链接。按 `(start_date, result.id)` 升序扫一遍。","Single 和 Average 共用这份原始数据,各走一遍 `computeNonPr` — 维护两份 `pb` Map(每选手的最佳)和 `bestNonPr` Map(每选手的最佳非 PR)。"],sourceEn:["Per event, pull every row with `results.best > 0` or `results.average > 0` (both columns in one query), join `persons` + `competitions` for link strings, then iterate ascending by `(start_date, result.id)`.","Single and Average reuse the same row set — each runs `computeNonPr` independently, maintaining its own `pb` map (running personal best) and `bestNonPr` map (best non-PR per person)."],sourceCode:{lang:`sql`,body:`SELECT result.person_id, result.best, result.average,
       person.country_id,
       <person_link>, <comp_link>,
       competition.start_date
FROM results result
JOIN persons person ON person.wca_id = result.person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = result.competition_id
WHERE result.event_id = '<event>'
  AND (result.best > 0 OR result.average > 0)
ORDER BY competition.start_date, result.id;`},steps:[{titleZh:`按 (日期, 行 id) 升序扫`,titleEn:`Scan ascending by (date, row id)`,bodyZh:"同日多比赛之间没有严格序;`result.id` 当 tiebreaker 给一个确定性顺序。`pb` 跟 `bestNonPr` 在扫描中累加。",bodyEn:"Same-day comps lack a strict order; `result.id` serves as a deterministic tiebreaker. `pb` and `bestNonPr` accumulate as the scan progresses."},{titleZh:`val ≤ pb → PR,跳过`,titleEn:`val ≤ pb → it's a PR, skip`,bodyZh:"如果当前值 ≤ 该选手当前 PB(包括首次出现 / 刷新 / 平 PB),更新 `pb[pid] = val` 然后**跳过** — 这条不属于 non-PR。",bodyEn:"If current value ≤ their running PB (first appearance, refresh, or tie), set `pb[pid] = val` and **skip** — this row isn't a non-PR."},{titleZh:`val > pb → 进 bestNonPr`,titleEn:`val > pb → enter bestNonPr`,bodyZh:'当前值严格慢于 pb = 是 non-PR。如果 `val < bestNonPr[pid].value`(或首次),更新 `bestNonPr[pid]`。同时维护"non-PR WR"`wrBest` — 跨人比较,记录全局最好的非 PR 成绩。',bodyEn:"Value strictly slower than pb = it's a non-PR. If `val < bestNonPr[pid].value` (or first time), update `bestNonPr[pid]`. Also maintain a cross-person `wrBest` — track the strongest non-PR ever seen."},{titleZh:`排名:bestNonPr 取最快前 10`,titleEn:`Ranking: top 10 fastest bestNonPr`,bodyZh:`每人保留**最快的 non-PR**;按值升序排,每项目 top 10 输出 [rank, person, time, country, date, comp]。`,bodyEn:`Keep each person's single fastest non-PR; sort ascending, take top 10 per event, output [rank, person, time, country, date, comp].`},{titleZh:`历史:wrBest 演化 → 倒序展示`,titleEn:`History: wrBest evolution → reverse-chrono`,bodyZh:'scan 中追加到 wrRecords 的每条都是当时的"非 PR WR";最后倒序输出 + 计算相邻 improvement `(prev-cur)/prev × 100%` 和持续天数。',bodyEn:"Each `wrRecords` entry marks a non-PR WR moment at the time; reverse the list for newest-first, then compute adjacent improvement `(prev-cur)/prev × 100%` plus run-length days.",highlight:!0}],formulae:[{labelZh:`非 PR 判定`,labelEn:`Non-PR predicate`,expr:`isNonPR(P, t, v) ⟺ v > min{v' : v' ∈ results(P, < t)}`,bodyZh:`P = 选手,t = 时间,v = 当前成绩。当前成绩严格大于此前所有自己成绩的最小值时,该成绩是 non-PR。`,bodyEn:`P = person, t = time, v = current value. Strictly greater than the min of all the same person's prior results = non-PR.`}],edgesZh:["**平 PB 算 PR 不算 non-PR** — `val <= pb` 时进入 PR 分支,只有 `val > pb` 才进 non-PR 池。","Single 和 Average 各自独立 — 同一行 `results` 里 best 可能是 PR、average 是 non-PR,反之亦然。",'WR History 的"Improvement"百分比公式:`(prev - cur) / prev × 100%`;首条留空。',`排名展示每人**最快的一条** non-PR,不展示其余 non-PR — 同一选手在历史里有几十次刷自己最好但仍不刷 PB 的轮次,这里只看最强那条。`],edgesEn:["**Ties (val == pb) count as PR, not non-PR** — `val <= pb` takes the PR branch; only `val > pb` enters the non-PR pool.","Single and Average are tracked independently — within one `results` row, best could be a PR while average is non-PR (or vice-versa).",'History "Improvement" % uses `(prev - cur) / prev × 100%`; first row blank.',`Ranking shows each person's single fastest non-PR only — a cuber may have hundreds of non-PR-but-close rounds; only their strongest surfaces here.`],related:[{id:`wr_current`,titleZh:`当前 WR`,titleEn:`Current WR`,hintZh:`PR = WR 的正常情况对照`,hintEn:`The "PR = WR" normal case for comparison`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`WR 历史正轨`,hintEn:`The standard WR history track`},{id:`wr_newcomer`,titleZh:`新人首场`,titleEn:`Newcomer first`,hintZh:`另一类反直觉 WR — 第一场就破纪录`,hintEn:`Another counter-intuitive WR slice — record on debut`},{id:`wr_non_pr`,toStat:!0,titleZh:`直接打开 Non-PR 表`,titleEn:`Jump to Non-PR table`,hintZh:`看哪些项目出过这种事`,hintEn:`See which events have seen this`}]},wr_newcomer:{id:`wr_newcomer`,titleZh:`新人首战 (Newcomer)`,titleEn:`Newcomer first WR`,badgeZh:`世界纪录`,badgeEn:`World record`,introZh:[`"新人首战"看选手**在某项目最早一次接触**时拿出的成绩 — 即在他人生第一场该项目比赛上的成绩。该值越快越罕见,越接近(甚至超过)历史 WR 越离谱。`,`页面有两个数据源:**首次还原 (1st-solve)** = 该选手该项目首场比赛**第一轮的第一次 attempt**(或 average,看指标);**首场比赛 (1st-comp)** = 该选手该项目首场比赛**所有轮次中最快的一次** — 1st-comp 给选手稍多机会(同场多轮),1st-solve 是真正的"开局即上限"。两个数据源 × 两个指标(single / average) = 4 组面板,每组都有 Ranking + History。`],introEn:[`"Newcomer first WR" looks at what a cuber posts the **very first time they touch the event** in competition — the rarer / faster the value, the more astonishing.`,`Two sources: **1st-solve** = the very first attempt (or average, per metric) in their **first round of their first comp** for that event; **1st-comp** = the fastest result across **all rounds of that first comp** — 1st-comp grants more chances within the comp, 1st-solve is the "debut single-shot." Two sources × two metrics (single / average) = 4 panel groups, each with Ranking + History.`],stats:[{value:`2 × 2 × 2`,labelZh:`维度`,labelEn:`Dims`,hintZh:`metric × source × view`,hintEn:`metric × source × view`},{value:`tmp table`,labelZh:`first_comp 索引`,labelEn:`first_comp index`,hintZh:`临时表 + (person, event, date) 索引`,hintEn:`Temp table + (person, event, date) idx`},{value:`ROW_NUMBER()`,labelZh:`选首轮`,labelEn:`Pick first round`,hintZh:`按 round_type_id ∈ {1,0,d} 排,rn=1`,hintEn:`Sort by round_type_id ∈ {1,0,d}, take rn=1`},{value:`strict <`,labelZh:`WR 历史`,labelEn:`WR history`,hintZh:`严格递减扫描`,hintEn:`Strictly decreasing scan`}],sourceZh:["先建临时表 `tmp_first_comp` = 每 (person_id, event_id) 的最早 `start_date`,加 `(person_id, event_id, earliest_date)` 复合索引。","1st-solve:`ROW_NUMBER() OVER (PARTITION BY person, event ORDER BY CASE round_type ∈ {1,0,d} THEN 0 ELSE 1 END)` 选首轮;single 看 `result_attempts.value` 且 `attempt_number = 1`,average 看 `results.average`。","1st-comp:在首场比赛的所有轮次里 `ROW_NUMBER() OVER (PARTITION BY person, event ORDER BY best/average)` 取最快;两组都按 `(event, value)` 升序输出。"],sourceEn:["First build a temp table `tmp_first_comp` = each (person_id, event_id)'s earliest `start_date`, with composite index `(person_id, event_id, earliest_date)`.","1st-solve: `ROW_NUMBER() OVER (PARTITION BY person, event ORDER BY CASE round_type ∈ {1,0,d} THEN 0 ELSE 1 END)` picks the first round; single reads `result_attempts.value WHERE attempt_number = 1`, average reads `results.average`.","1st-comp: across all rounds of the first comp, `ROW_NUMBER() OVER (PARTITION BY person, event ORDER BY best/average)` picks the fastest; both queries output ordered by `(event, value)` ascending."],sourceCode:{lang:`sql`,body:`CREATE TEMPORARY TABLE tmp_first_comp AS
SELECT r.person_id, r.event_id, MIN(c.start_date) AS earliest_date
FROM results r JOIN competitions c ON c.id = r.competition_id
GROUP BY r.person_id, r.event_id;

-- 1st-solve(single)
SELECT person_id, event_id, ra.value AS first_result, ...
FROM results r
JOIN result_attempts ra ON ra.result_id = r.id AND ra.attempt_number = 1
JOIN tmp_first_comp fc USING (person_id, event_id)
WHERE c1.start_date = fc.earliest_date AND ra.value > 0
QUALIFY ROW_NUMBER() OVER (PARTITION BY person_id, event_id
  ORDER BY CASE WHEN round_type_id IN ('1','0','d') THEN 0 ELSE 1 END) = 1;`},steps:[{titleZh:`建 tmp_first_comp 索引`,titleEn:`Build tmp_first_comp index`,bodyZh:`每选手每项目的最早 comp 日期 → 临时表;加 (person, event, date) 索引,后面两组 SQL 都靠它做 join 谓词。`,bodyEn:`For each (person, event), compute earliest comp date → temp table; add (person, event, date) index. Both downstream SQLs join on this.`},{titleZh:`1st-solve:取首场首轮第 1 attempt`,titleEn:`1st-solve: first attempt of first round of first comp`,bodyZh:'首场比赛里的"首轮" = `round_type_id ∈ {1,0,d}` 的轮次(预赛 / 第 1 轮);single 看该轮 `attempt_number = 1` 的 value,average 看该轮 average。`ROW_NUMBER()` 决胜并列。',bodyEn:"The \"first round\" within the first comp = a round with `round_type_id ∈ {1,0,d}` (qualification / round 1); for single take that round's `attempt_number = 1` value, for average take that round's average. `ROW_NUMBER()` breaks any ties."},{titleZh:`1st-comp:首场比赛所有轮中最快`,titleEn:`1st-comp: fastest across all rounds of first comp`,bodyZh:'首场比赛里可能打 1–4 轮,这里在全部轮次中 `MIN(best)` / `MIN(average)` 拿最快的那条 — 选手在第一场比赛"能拿出的最好水准"。',bodyEn:'A first comp may include 1–4 rounds; this picks `MIN(best)` / `MIN(average)` across all of them — the "peak day-1 performance."'},{titleZh:`排名:项目内 Top 10`,titleEn:`Ranking: top 10 per event`,bodyZh:`每项目按 first_result 升序前 10。展示 single 时附 5 次 attempt 明细;average 类似但不展开。`,bodyEn:`Top 10 per event ascending by first_result. Single rows include the 5-attempt breakdown; average rows omit it.`},{titleZh:`WR 历史:严格递减扫描`,titleEn:`WR history: strictly decreasing scan`,bodyZh:"按 (start_date, first_result) 升序排序所有(person, event, first_result);维护 `minSoFar`,只保留严格 `< minSoFar` 的条目。最后倒序输出,首条 = 当前最强首战。",bodyEn:"Sort all (person, event, first_result) ascending by (start_date, first_result); maintain `minSoFar`; keep only strictly `< minSoFar`. Reverse for newest-first; top row = current strongest debut.",highlight:!0}],edgesZh:[`**两个数据源不会合并** — 1st-solve 和 1st-comp 是不同口径,某人可能在 1st-solve 榜很高但 1st-comp 名次普通(因为同场后续轮没刷得更快)。`,'`round_type_id ∈ {1, 0, d}` 是 WCA 数据 dump 的"首轮"约定:1 = combined round 1,0 = round 1,d = qualification round。其他 `c f g e` 等都是后续轮。',`FMC 不走这页(其 round_type 划分跟普通项目不同),average 算的是 mean of 3。`,"WR History 用 `< minSoFar`(严格小于) — 跟其他 stat 一致,不展示并列。Improvement 用 `(prev - cur) / prev × 100%`,保留 1 位小数。"],edgesEn:[`**The two sources don't merge** — 1st-solve and 1st-comp are independent. A cuber can rank high on 1st-solve yet middling on 1st-comp (their later same-comp rounds didn't go faster).`,'`round_type_id ∈ {1, 0, d}` is the WCA dump\'s "first round" convention: 1 = combined-final round 1, 0 = round 1, d = qualification. `c f g e` etc. are subsequent rounds.',`FMC works here too but its "average" means mean-of-3, not ao5 trimmed; round type filtering still applies.`,"WR history uses `< minSoFar` (strict) — consistent with other stats, ties not shown. Improvement % uses `(prev - cur) / prev × 100%` to 1 decimal."],related:[{id:`wr_current`,titleZh:`当前 WR`,titleEn:`Current WR`,hintZh:`横向对比"首战成绩 vs 当时 WR"`,hintEn:`Compare debut value vs the standing WR`},{id:`wr_non_pr`,titleZh:`非 PR 的 WR`,titleEn:`Non-PR WR`,hintZh:`另一类反直觉 WR 视角`,hintEn:`A different counter-intuitive WR slice`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`正轨 WR 演化`,hintEn:`The standard WR evolution track`},{id:`wr_newcomer`,toStat:!0,titleZh:`直接打开新人榜`,titleEn:`Jump to Newcomer table`,hintZh:`看 4 个 panel 切换`,hintEn:`Browse the 4 panel combinations`}]},average_of:{id:`average_of`,titleZh:`滚动平均 (Rolling Average)`,titleEn:`Rolling Average`,badgeZh:`世界纪录`,badgeEn:`World record`,introZh:[`把选手生涯里**所有官方 attempt** 按时间排成一长串,在上面滑一个长度 N 的窗口算裁剪均值,N ∈ {3, 5, 12, 25, 50, 100, 1000}。每个窗口产出一个数,选手生涯里最小的那个 = 他这套 AoN 的 PB。这跟 WCA 官方的"一轮 5 次裁剪均值"不一样 — 这里 N 是**跨比赛 / 跨轮次**滑动的。`,`它考的是**长期稳定输出** — Ao1000 的 PB 持有者基本意味着这个人有 1000 次官方还原都保持高水准。前端按 N 切下拉,每个 N 都有自己的 Ranking + WR History。聚合层把 7 个 AverageOfX 子类拼成一个 stat。`],introEn:[`Lay out a cuber's **entire official attempt history** in chronological order, slide a window of size N over it computing the WCA trimmed mean — N ∈ {3, 5, 12, 25, 50, 100, 1000}. Each window emits a number; the smallest across their career = their AoN PB. Unlike the WCA official "trimmed mean of 5 within one round," this N slides **across comps and rounds**.`,`It probes long-horizon consistency — an Ao1000 PB means 1000 consecutive official attempts at top form. UI dropdown switches N; each N gets its own Ranking + WR History. The aggregator stitches 7 AverageOfX children into one stat.`],stats:[{value:`7`,labelZh:`N 档`,labelEn:`N tiers`,hintZh:`3 / 5 / 12 / 25 / 50 / 100 / 1000`,hintEn:`3 / 5 / 12 / 25 / 50 / 100 / 1000`},{value:`top 15~2000`,labelZh:`候选门槛`,labelEn:`Candidate top-N`,hintZh:`按项目变化(333 top 15, 333bf top 2000)`,hintEn:`Varies by event (333: 15; 333bf: 2000)`},{value:`5%`,labelZh:`裁剪比例`,labelEn:`Trim ratio`,hintZh:`每端 ceil(N × 5%) 个,共 ~10%`,hintEn:`ceil(N × 5%) per side, ~10% total`},{value:`1×`,labelZh:`SQL 查询`,labelEn:`SQL queries`,hintZh:`7 个子类共享同一份 row 缓存`,hintEn:`7 children share one cached row set`}],sourceZh:['基类 SQL 抓"候选选手"的全部 attempt:候选 = (每项目 average PB Top N 的选手) UNION (历史 single / average WR 持有者)。Top N 阈值按项目设:`333 top 15` / `333bf top 2000` / 多数 30 / FMC pyram clock 等 300。',`7 个子类(Ao3 ~ Ao1000)**共享同一份原始查询** — 第一个子类查完缓存,后面 6 个 直接复用,避免 6 × 60s 重复。FMC 项目的"成绩"是 moves,乘 100 统一成厘秒。`],sourceEn:["Base SQL pulls **all attempts** of candidate cuber — candidates = (per-event top-N by average PB) UNION (single / average WR holders ever). Top-N varies: `333: 15`, `333bf: 2000`, most events 30, FMC / pyram / clock / etc. 300.",`The 7 children (Ao3 ... Ao1000) **share one cached row set** — first child runs the SQL, others reuse, dodging 6× ~60s redundant queries. For FMC, move counts are ×100 to map onto the centisecond domain.`],sourceCode:{lang:`sql`,body:`SELECT person_id, country_id, event_id,
       GROUP_CONCAT(ra.value ORDER BY ra.attempt_number) AS attempts,
       competition_link, competition.start_date
FROM results result
JOIN persons person ON person.wca_id = person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN round_types round_type ON round_type.id = round_type_id
JOIN ( /* candidate = top-N UNION WR holders */ ) candidates USING (event_id, person_id)
WHERE event_id NOT IN ('333mbf', '333mbo')
ORDER BY competition.start_date, round_type.rank;`},steps:[{titleZh:`一次查 attempts → 共享缓存`,titleEn:`One attempts query → shared cache`,bodyZh:"基类 `getSharedQueryRows()` lazy load 一份大查询;7 个子类(Ao3 ~ Ao1000)直接复用 — 不用查 7 次。每次子类跑完不立刻清,等聚合层最后 `clearSharedCache()`。",bodyEn:"`getSharedQueryRows()` lazy-loads one giant query; the 7 children reuse it — no 7× re-queries. Cache survives across children, gets `clearSharedCache()`'d after the aggregator finishes."},{titleZh:`按 (项目, 选手) 分组拉直 attempt`,titleEn:`Group by (event, person), flatten attempts`,bodyZh:"在每个项目下,每选手所有 round 按 `(start_date, round_type.rank)` 升序;每轮 attempts 字符串 split 后逐个 push 到 `solves[]` — DNF 标记为 `Infinity`(裁剪后留在尾巴),`0` 跳过(未提交)。",bodyEn:"Within each event, each cuber's rounds sort ascending by `(start_date, round_type.rank)`; each round's attempts split and stream into `solves[]` — DNF → `Infinity` (gets trimmed off if possible), `0` skipped (not-attempted)."},{titleZh:`滑动窗口 + WCA 裁剪均值`,titleEn:`Sliding window + WCA trimmed mean`,bodyZh:"`solves.length == N` 时算一次裁剪均值:排序,两端各去 `ceil(N × 5%)`,剩下取算术均值。如果裁后还含 `Infinity` → 整个均值 DNF。然后窗口 `shift()` 一格。",bodyEn:"When `solves.length == N`, compute the trimmed mean: sort, drop `ceil(N × 5%)` from each side, average the rest. If a trimmed window still contains `Infinity` → DNF the whole average. Then `shift()` the window."},{titleZh:`每人记 best + pbHistory`,titleEn:`Per person: best + pbHistory`,bodyZh:"每选手维护当前最佳 AoN(`best`) + 历史 PB 列表 (`pbHistory`)。pbHistory 不存 solves 数组(Ao1000 × 多 PB 会爆),立刻 format 成 csv 字符串。",bodyEn:"Each cuber holds a `best` AoN + a `pbHistory` list. To avoid memory blowup (Ao1000 × many PBs), pbHistory stores formatted csv strings instead of solve arrays."},{titleZh:`排名 + WR 历史输出`,titleEn:`Emit ranking + WR history`,bodyZh:"每项目 top 10 按 best AoN 升序;WR History 把所有人的 pbHistory 合并按 `(endDate, value)` 升序扫,只留严格刷新当前最小的 — 最后倒序展示。",bodyEn:"Top 10 per event by best AoN ascending. WR History merges everyone's pbHistory, sorts by `(endDate, value)`, keeps strict improvements only, reverses for newest-first.",highlight:!0}],formulae:[{labelZh:`WCA 裁剪均值 (window = N)`,labelEn:`WCA trimmed mean (window = N)`,expr:`AoN = (1 / (N - 2k)) · Σ sᵢ for i ∈ (k, N − k],  k = ⌈N × 0.05⌉`,bodyZh:"s₁ ≤ s₂ ≤ ... ≤ s_N 为窗口内排序后成绩;裁剪掉两端各 k = `⌈N × 5%⌉` 个,剩下算术平均。N=5 → k=1(WCA 标准 ao5);N=1000 → k=50。",bodyEn:"s₁ ≤ s₂ ≤ ... ≤ s_N is the sorted window; drop k = `⌈N × 5%⌉` per side, arithmetic mean the rest. N=5 → k=1 (the WCA standard ao5); N=1000 → k=50."}],edgesZh:[`**跨比赛跨轮**,跟 WCA 官方 ao5(同一轮 5 次)完全不同。AoN 的窗口可能横跨好几年。`,"MBLD / MBLD-old (`333mbf` / `333mbo`)不参与 — 它们的 attempt 结构不同。",`裁剪比例 5% 是写死的;N=3 时 k=1(去掉最快最慢);N=5 时 k=1(等价 WCA ao5);N=1000 时 k=50。`,`WR History 候选只包含 top-N + WR 持有者(SQL 已过滤),所以"全民最大 Ao1000" 的真正持有者不一定在 — 但这页只关心强者侧,无伤大雅。`],edgesEn:[`**Cross-comp, cross-round** — not the same as the WCA-official ao5 (within one round). An AoN window can span multiple years.`,"MBLD / MBLD-old (`333mbf` / `333mbo`) excluded — their attempt structure differs.",`Trim ratio hard-coded to 5%; N=3 → k=1 (drops the single fastest + slowest); N=5 → k=1 (= WCA standard ao5); N=1000 → k=50.`,`WR-history candidates = top-N + WR holders only — the absolute "biggest Ao1000 of all humans" may live outside this set; this page focuses on the strong end where it matters.`],related:[{id:`wr_aoxr`,titleZh:`AoXR`,titleEn:`AoXR`,hintZh:`单场跨轮平均(N 不固定,跟比赛轮数挂钩)`,hintEn:`Within-comp cross-round average (N = comp's round count)`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`单轮内的衍生指标`,hintEn:`Within-round derived metrics`},{id:`consecutive_sub_5_average`,titleZh:`连续 sub-5 平均`,titleEn:`Consecutive sub-5 averages`,hintZh:`另一种"持续稳定"度量 — 连续 N 次破阈值`,hintEn:`Another "sustained-form" lens — N consecutive sub-threshold averages`},{id:`average_of`,toStat:!0,titleZh:`直接打开滚动平均`,titleEn:`Jump to Rolling Average`,hintZh:`7 个 N 档下拉切换`,hintEn:`Dropdown across 7 N values`}]},consecutive_sub_5_average:{id:`consecutive_sub_5_average`,titleZh:`连续 sub-5 平均`,titleEn:`Consecutive sub-5 averages`,badgeZh:`3x3x3`,badgeEn:`3x3x3`,introZh:[`在 3x3x3 项目上,某选手把所有官方轮次的 average 按时间排成一串后,**连续都 < 5.00 秒**的最长段有多长?这就是这条 stat。一段被中断 = 出现任意一次 ≥ 5.00 的 average(或 DNF)。`,`它跟 Ao1000 测的"长期稳定"不同 — 这里要求**每一轮都过线**,一次失手就归零;比起均值,更像高强度抗压性指标。前端双视图:Ranking(每人最长 streak Top 100) + History(WR 演变倒序)。`],introEn:[`For 3x3x3 only: take a cuber's sequence of official-round averages in chronological order — what's the longest consecutive run where every single one is **< 5.00 seconds**? That's the metric. A streak breaks on any ≥ 5.00 average (or DNF).`,`Unlike Ao1000's "long-horizon mean", this is **every-round-must-clear** — one slip resets to zero. More of a sustained-pressure metric than a smoothing one. Two views: Ranking (each person's longest, top 100) + History (newest-first WR evolution).`],stats:[{value:`500`,labelZh:`阈值(厘秒)`,labelEn:`Threshold (cs)`,hintZh:`5.00 秒 = 500 厘秒`,hintEn:`5.00s = 500 centiseconds`},{value:`333 only`,labelZh:`范围`,labelEn:`Scope`,hintZh:`只看 event_id = 333`,hintEn:`event_id = 333 only`},{value:`count > 1`,labelZh:`streak 下限`,labelEn:`Min streak`,hintZh:`至少连续 2 次才算`,hintEn:`Need ≥ 2 to count`},{value:`top 100`,labelZh:`Ranking 展示`,labelEn:`Ranking shown`,hintZh:`每人最长 streak,Top 100`,hintEn:`Per person's longest, top 100`}],sourceZh:["查 `results` 全部 333 行,join `persons` + `competitions` + `round_types`;按 `(person_id, start_date, round_type.rank)` 升序排,保证同人成绩按真实时间 + 同日同比赛按轮次顺序。",`内存里按 person_id 分组,对每个选手逐行扫一遍,维护一个"当前 streak"对象。average 严格 < 500 且 > 0 → 计数 +1 同时记录起 / 止比赛;否则若 count > 1 收尾入库,重新置零。`],sourceEn:["Pull every 333 row, join `persons` + `competitions` + `round_types`; sort `(person_id, start_date, round_type.rank)` ascending to enforce per-person chronology and per-day round order.",`Group in memory by person_id, scan per person maintaining a "current streak" object. average strictly < 500 and > 0 → bump count, update end-comp; otherwise commit if count > 1 and reset.`],sourceCode:{lang:`sql`,body:`SELECT result.person_id, person_link, result.average,
       competition.cell_name AS competition_name,
       competition.id AS competition_id,
       competition.start_date
FROM results result
JOIN persons person ON person.wca_id = result.person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = result.competition_id
JOIN round_types round_type ON round_type.id = result.round_type_id
WHERE result.event_id = '333'
ORDER BY result.person_id, competition.start_date, round_type.rank;`},steps:[{titleZh:`查 333 全部 average,按 (人, 时间) 排序`,titleEn:`Pull all 333 averages, sort by (person, time)`,bodyZh:"`ORDER BY person_id, start_date, round_type.rank` 让单选手内成绩呈严格时间序;后续扫描不再需要再排。",bodyEn:"`ORDER BY person_id, start_date, round_type.rank` enforces strict per-person chronology; downstream scan needs no re-sorting."},{titleZh:`按 person_id 拆组逐选手扫`,titleEn:`Split by person_id, scan each`,bodyZh:"内存里 `byPerson: Map<pid, rows>` 拆好后释放原始 rows(配合 `global.gc()`);逐选手单独跑 streak 检测。",bodyEn:"`byPerson: Map<pid, rows>` partitions in memory; release raw rows (paired with `global.gc()`) and process each cuber independently."},{titleZh:`avg < 500 → 累加;否则收尾`,titleEn:`avg < 500 → extend; else commit`,bodyZh:"当前 streak:`avg > 0 && avg < 500` 时 count += 1,更新 endComp/endDate(并在 count == 1 时记录 startComp);否则若 count > 1 把这段写出,重新 newStreak()。最后一段也别忘了 commit。",bodyEn:"For each row: `avg > 0 && avg < 500` → count += 1, update endComp/endDate (and set startComp on count == 1); else if count > 1, commit the streak and reset. After the loop, commit the trailing run too."},{titleZh:`Ranking:每人取最长一条,Top 100`,titleEn:`Ranking: each person's longest, top 100`,bodyZh:"`bestByPerson` 把同选手多段 streak 折叠到最长那条;按 count 降序前 100 输出。一行 = [count, person_link, startComp_link, endComp_link]。",bodyEn:"`bestByPerson` collapses one cuber's many streaks to their longest; sort descending by count, take top 100. Row = [count, person_link, startComp_link, endComp_link]."},{titleZh:`History:WR 演变倒序`,titleEn:`History: WR evolution reversed`,bodyZh:"把所有 streak 按 `(endDate, count)` 升序排;扫描时记录 `maxCount`,只留 `count >= maxCount && count > 1` 的条(注意是 `>=` — 这里允许并列也记一次);最后 reverse 输出最新在上。",bodyEn:"Sort all streaks ascending by `(endDate, count)`; scan tracking `maxCount`, keep entries with `count >= maxCount && count > 1` (note `>=` — ties allowed here); reverse for newest-first.",highlight:!0}],formulae:[{labelZh:`streak 定义`,labelEn:`Streak definition`,expr:`streak(P) = max { k : ∃ i, aᵢ, aᵢ₊₁, ..., aᵢ₊ₖ₋₁ all < 5.00 }`,bodyZh:`(aⱼ) = P 的 333 average 按时间序列;最长的"全部 < 5.00"连续子序列长度 = streak。`,bodyEn:`(aⱼ) = P's 333 averages in chronological order; the longest run of consecutive entries all < 5.00 = streak length.`}],edgesZh:["**项目固定 3x3x3**(`event_id = '333'`),其他项目即便有 sub-5 average 也不参与。",`阈值是**严格小于 500**(即 < 5.00 秒) — 5.00 整数算"未过线"中断 streak。DNF 也算中断。`,"WR History 用 `>=`(允许并列也记)— 跟其他大多数 WR history 的 strict `<` 不同,这是这条 stat 的特殊行为。",`count == 1 不入库 — 也就是说一次 sub-5 average 单独出现不算一段 streak,至少要连续 2 次。`],edgesEn:["**3x3x3 only** (`event_id = '333'`); other events with sub-5 averages don't apply.",`Threshold is **strictly < 500** (i.e. < 5.00s) — a 5.00 flat counts as a break. DNFs break too.`,"WR history uses `>=` (ties allowed) — different from most other WR histories which use strict `<`. This is a quirk of this stat.",`count == 1 isn't stored — a single isolated sub-5 average doesn't count as a streak; need ≥ 2 consecutive.`],related:[{id:`average_of`,titleZh:`滚动平均 (Rolling Average)`,titleEn:`Rolling Average`,hintZh:`另一类持续表现指标,但允许个别失手`,hintEn:`A different sustained-form lens — tolerates occasional slips`},{id:`longest_streak_of_personal_records`,titleZh:`最长 PR streak`,titleEn:`Longest PR streak`,hintZh:`"连续刷 PB" 的姊妹 streak 指标`,hintEn:`Sibling streak metric — consecutive PB refreshes`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`单轮指标视角`,hintEn:`Within-round metric lens`},{id:`consecutive_sub_5_average`,toStat:!0,titleZh:`直接打开 streak 排名`,titleEn:`Jump to streak leaderboard`,hintZh:`看谁连续多少场过线`,hintEn:`See who has held the line for how long`}]}},c={best_potential_fmc_mean:{id:`best_potential_fmc_mean`,titleZh:`最佳潜在 FMC 平均`,titleEn:`Best potential FMC mean`,badgeZh:`潜在最佳`,badgeEn:`Potential`,introZh:["`333fm` 的 mean of 3 = 三次手写解步数取算术平均。这个统计反过来问:在某一**轮**(competition × round)里,如果一个人把他/她那一轮的**每个 attempt slot 都换成全场最好的那一发**,平均能压到多少?",`换句话说,把同一轮所有选手的 attempt 1 全收齐取最小,attempt 2 同样,attempt 3 同样,(min1 + min2 + min3) / 3 就是"这一轮在理论上达成过的最佳 FMC 平均"。它衡量的是**轮次本身的难度系数** —— 打乱好不好打,而不是某个选手。`],introEn:["`333fm` mean-of-3 is just the arithmetic mean of three hand-written move counts. This stat flips the question: for one **round** (competition × round), if you took **the best attempt-1 from anyone in that round, the best attempt-2 from anyone, and the best attempt-3 from anyone**, what would the resulting mean be?","Concretely: aggregate attempt 1 across all competitors in the round to its `MIN`, same for attempts 2 and 3, then `(min1 + min2 + min3) / 3`. It is a measure of **how easy the scrambles were**, not how good any one solver is."],stats:[{value:`mean-of-3`,labelZh:`聚合口径`,labelEn:`Aggregation`,hintZh:`三个 attempt 各取全场最小再求均值`,hintEn:`Per-attempt minimum across the round, then averaged`},{value:"`333fm`",labelZh:`仅此项目`,labelEn:`Only event`,hintZh:`Fewest Moves 是 WCA 里唯一手写步数的项目`,hintEn:`FMC is the only hand-written move-count event in WCA`},{value:`100`,labelZh:`榜单深度`,labelEn:`Rows returned`,hintZh:`按 mean 升序取前 100 个 round`,hintEn:`Top 100 rounds by mean ascending`},{value:`整数`,labelZh:`步数为整数`,labelEn:`Integer values`,hintZh:"WCA dump 里 `333fm` 步数直接存,不像计时项 ×100",hintEn:`FMC values stored as integer moves, no ×100 like timed events`}],sourceZh:["`results` + `result_attempts` 两表 join。先按 `(competition_id, round_type_id)` 分组,对每个 `attempt_number ∈ {1, 2, 3}` 单独取 `MIN(value)`(只算 `value > 0`,过滤 DNF/DNS/未提交);再把三个最小值相加除以 3。`333fm` 不需要乘 100 — dump 里这个项目存的就是步数本身。"],sourceEn:["Joins `results` and `result_attempts`. Groups by `(competition_id, round_type_id)`; per attempt slot (1, 2, 3) it takes `MIN(value)` filtered to `value > 0` (drops DNF/DNS/unattempted). Then `(min1 + min2 + min3) / 3`. `333fm` move counts are stored as integers — no ×100 like timed events."],sourceCode:{lang:`sql`,body:`SELECT (best1 + best2 + best3) / 3 AS mean,
       best1, best2, best3, competition_id, round_type_id
FROM (
  SELECT
    MIN(CASE WHEN ra.attempt_number = 1 AND ra.value > 0 THEN ra.value END) best1,
    MIN(CASE WHEN ra.attempt_number = 2 AND ra.value > 0 THEN ra.value END) best2,
    MIN(CASE WHEN ra.attempt_number = 3 AND ra.value > 0 THEN ra.value END) best3,
    r.competition_id, r.round_type_id
  FROM results r
  JOIN result_attempts ra ON ra.result_id = r.id
  WHERE r.event_id = '333fm'
  GROUP BY r.competition_id, r.round_type_id
) t
WHERE LEAST(best1, best2, best3) IS NOT NULL
ORDER BY mean
LIMIT 100;`},steps:[{titleZh:`按 (比赛, 轮次) 拆桶`,titleEn:`Bucket by (competition, round)`,bodyZh:"`results` join `result_attempts`,只留 `event_id = 333fm`。把每一条 attempt 按 `(competition_id, round_type_id)` 装进对应的桶。",bodyEn:"`results` joined with `result_attempts`, filtered to `event_id = 333fm`. Each attempt row is dropped into the bucket keyed by `(competition_id, round_type_id)`."},{titleZh:`逐 attempt slot 取全场最小`,titleEn:`Per-slot minimum`,bodyZh:'在每个桶内,attempt 1、attempt 2、attempt 3 三个槽位各跑一次 `MIN(value WHERE value > 0)`。这一步 = "把全轮选手在 attempt i 上的最好成绩抽出来"。',bodyEn:'Within each bucket, run `MIN(value WHERE value > 0)` three times — once per attempt slot. This pulls out "the best attempt-1 by anyone in this round", and same for slots 2 and 3.'},{titleZh:`过滤完整桶`,titleEn:`Keep only complete buckets`,bodyZh:"`WHERE LEAST(best1, best2, best3) IS NOT NULL` — 必须三个 attempt 都有有效成绩,否则不算 mean。预赛 / DNF 一片的轮次会自动被剔掉。",bodyEn:"`WHERE LEAST(best1, best2, best3) IS NOT NULL` — all three slots must have at least one valid attempt. Rounds where some attempt slot is all-DNF / all-unattempted are dropped."},{titleZh:`算潜在 mean 排前 100`,titleEn:`Compute potential mean, top 100`,bodyZh:"`mean = (best1 + best2 + best3) / 3`,按 mean 升序 `LIMIT 100`。注意:这个 mean 完全可能没有任何**单个选手**真实拿到过 — 它是三人各贡献一槽的虚拟最佳。",bodyEn:"`mean = (best1 + best2 + best3) / 3`, ordered ascending, `LIMIT 100`. Note: no single solver may ever have posted this mean — it is a virtual best assembled from three different people, one per slot.",highlight:!0}],formulae:[{labelZh:`公式`,labelEn:`Formula`,expr:`potentialMean(c, r) = (mₐ + m_b + m_c) / 3,  mₖ = min{vₖ(p) : p ∈ round(c, r), vₖ(p) > 0}`,bodyZh:`c = 比赛,r = 轮次,p = 选手,vₖ(p) = 选手 p 在 attempt slot k 上的步数。mₖ 是该 slot 在该轮的全场最小步数。`,bodyEn:`c = competition, r = round, p = competitor, vₖ(p) = solver p's move count on attempt slot k. mₖ is the per-slot minimum across the whole round.`}],edgesZh:[`"潜在"是关键 — 三个 slot 的最佳可能来自三个不同的人,所以这条 mean 并不是任何选手本人达成过的成绩。`,"只看 `value > 0`:DNF (-1)、DNS (-2)、未提交 (0) 全被剔。三个 slot 全 DNF 的轮次根本进不了榜。",`FMC 的 value 列存的就是整数步数,**没有** ×100 编码 — 不要去乘 100,会把所有 mean 放大成两位小数后离谱的数。`,"`MIN` 而非 `MAX`:FMC 步数越**少**越好,跟 BLD points 编码恰好相反。"],edgesEn:[`"Potential" is the key word — the three per-slot best results may come from three different people, so this mean is not necessarily one any individual ever achieved.`,"Only `value > 0` counts: DNF (-1), DNS (-2), unattempted (0) are dropped. A round where some slot is entirely DNF cannot appear.","For FMC the `value` column stores integer move counts directly — there is **no** ×100 encoding. Do not multiply by 100 or the displayed mean will be off by two orders of magnitude.","`MIN`, not `MAX`: FMC is lower-is-better, unlike BLD points which are higher-is-better."],related:[{id:`best_round`,titleZh:`最佳轮次`,titleEn:`Best round`,hintZh:`真实达成过的轮次 top-3 之和,可对照"潜在"差距`,hintEn:`Actually-achieved top-3 sum per round — contrast with "potential"`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`其它 mean / average / single 的 WR 视角`,hintEn:`Other mean / average / single WR perspectives`},{id:`most_frequent_results`,titleZh:`最常出现的成绩`,titleEn:`Most frequent results`,hintZh:"同样用 `result_attempts` 拆 attempt slot",hintEn:"Also unrolls per-attempt values via `result_attempts`"},{id:`best_potential_fmc_mean`,toStat:!0,titleZh:`打开潜在 FMC mean 榜`,titleEn:`Jump to potential FMC mean board`,hintZh:`看实际榜单`,hintEn:`Live leaderboard`}]},best_round:{id:`best_round`,titleZh:`最佳轮次`,titleEn:`Best round`,badgeZh:`轮次`,badgeEn:`Round`,introZh:['`best_round` 找的是**某一轮(competition × round_type)的 top 3 成绩之和**最小的那些轮 —— 一群选手挤在同一组 / 同一场决赛里同时打出顶级表现的"神级 round"。',"BLD 系列(333bf / 444bf / 555bf / 333mbf)用 single,其他项目用 average。`333mbf` 因为 WCA value 编码是越小越好但**业内排序**用 points 倒序,所以单独走一条 transform 路径。"],introEn:["`best_round` finds rounds (one row = one `competition × round_type`) with the smallest **sum of the top 3 results in that round** — these are the legendary rounds where a cluster of competitors all posted elite times back-to-back.","BLD events (333bf / 444bf / 555bf / 333mbf) rank by single; all other events rank by average. `333mbf` gets a separate transform path because its WCA `value` encoding is lower-is-better but the natural ranking is by **points** descending."],stats:[{value:`21`,labelZh:`项目`,labelEn:`Events`,hintZh:`每个项目独立一节,top 10 轮`,hintEn:`One section per event, top 10 rounds each`},{value:`top 3`,labelZh:`取多少人`,labelEn:`Per round`,hintZh:"`ROW_NUMBER ≤ 3` 取前三",hintEn:"`ROW_NUMBER ≤ 3` keeps the top three"},{value:`single / average`,labelZh:`排序口径`,labelEn:`Rank field`,hintZh:`BLD 用 best;其他用 average`,hintEn:`BLD uses best; others use average`},{value:`points DESC`,labelZh:"`333mbf` 特例",labelEn:"`333mbf` special",hintZh:`另查一次,按 points 之和倒序`,hintEn:`Queried separately, sorted by points sum descending`}],sourceZh:["逐项目跑参数化 SQL(`event_id = '%s'`)。窗口函数 `ROW_NUMBER() OVER (PARTITION BY competition_id, round_type_id ORDER BY <best | average>)` 在每个 round 内给选手排号;`MAX(CASE WHEN row_num = k THEN ...)` 透视成\"第 k 名\"宽表,再把 1/2/3 名结果相加得到 `result_sum`,按 sum 升序 `LIMIT 10`。"],sourceEn:["Runs parameterized SQL per event (`event_id = '%s'`). A window function `ROW_NUMBER() OVER (PARTITION BY competition_id, round_type_id ORDER BY <best | average>)` numbers competitors inside each round; `MAX(CASE WHEN row_num = k THEN ...)` pivots into \"kth-place\" columns. The 1st/2nd/3rd results sum to `result_sum`, sorted ascending, `LIMIT 10`."],sourceCode:{lang:`sql`,body:`SELECT competition_id, round_type_id, event_id,
  MAX(CASE WHEN row_num = 1 THEN best_result END) first_result,
  MAX(CASE WHEN row_num = 2 THEN best_result END) second_result,
  MAX(CASE WHEN row_num = 3 THEN best_result END) third_result,
  first_result + second_result + third_result AS result_sum
FROM (
  SELECT competition_id, round_type_id, event_id, person_id,
    CASE WHEN event_id IN ('333bf','444bf','555bf','333mbf')
         THEN best ELSE average END AS best_result,
    ROW_NUMBER() OVER (
      PARTITION BY competition_id, round_type_id
      ORDER BY CASE WHEN event_id IN ('333bf','444bf','555bf','333mbf')
                    THEN best ELSE average END
    ) AS row_num
  FROM results
  WHERE event_id = '%s' AND best_result > 0
) ranked
GROUP BY competition_id, round_type_id
ORDER BY result_sum
LIMIT 10;`},steps:[{titleZh:`挑排序字段`,titleEn:`Pick the rank field`,bodyZh:'BLD 三项 + multi-BLD 用 `best`(single 值越小越好);其它项目用 `average`(ao5 的官方均值)。在 `CASE` 表达式里一次解决,排序和"宽表透视"都用同一个表达式。',bodyEn:"BLD trio + multi-BLD rank by `best` (single, lower is better); all other events rank by `average` (the official ao5 mean). A single `CASE` expression handles both the `ORDER BY` and the pivot column."},{titleZh:`每 round 内排名`,titleEn:`Rank within each round`,bodyZh:"`ROW_NUMBER() OVER (PARTITION BY competition_id, round_type_id ORDER BY <rank field>)` 给每条结果在所属 round 内分配 1, 2, 3, ... 的名次。只看正值(`best_result > 0`,排除 DNF/DNS/未提交)。",bodyEn:"`ROW_NUMBER() OVER (PARTITION BY competition_id, round_type_id ORDER BY <rank field>)` numbers each row within its round (1, 2, 3, ...). Only positive values are considered (`best_result > 0`, dropping DNF/DNS/unattempted)."},{titleZh:`透视成宽表`,titleEn:`Pivot to wide table`,bodyZh:"`MAX(CASE WHEN row_num = 1 THEN person_id END) first_id` 等等 — 把 long 表压成每 round 一行、第 1/2/3 名各自占三列。`third_result IS NOT NULL` 过滤掉不足三人完赛的轮次。",bodyEn:"`MAX(CASE WHEN row_num = 1 THEN person_id END) first_id` etc. — collapses the long table to one row per round with three columns each for the top three solvers. `third_result IS NOT NULL` drops rounds where fewer than three completed."},{titleZh:`加和取前 10`,titleEn:`Sum, sort, top 10`,bodyZh:"`result_sum = first + second + third`,按和升序 `LIMIT 10`,每个项目独立一节。",bodyEn:"`result_sum = first + second + third`, ordered ascending, `LIMIT 10`, one section per event."},{titleZh:"`333mbf` 单独一条 transform",titleEn:"`333mbf` separate transform path",bodyZh:"Multi-BLD 的 `value` 编码越小越好(攻击数 / 时间 / 解出数全塞一个数),但人眼习惯按 **points** 倒序(尝试 N 个、解出 M 个、错 N−M)。`queryResults` 跳过 `333mbf`,`toJson` 单独查 + 按 `points` 之和倒序排,再合并回去。",bodyEn:"Multi-BLD `value` is encoded lower-is-better (a single packed number for attempted / time / solved), but humans rank it by **points** descending. `queryResults` skips `333mbf`; `toJson` queries it separately and sorts by sum of `points` descending before merging.",highlight:!0}],edgesZh:["BLD 项目按 `single` 排,其它按 `average`:这是项目固有规则,不可调。","不足 3 人的 round 被过滤(`third_result IS NOT NULL`)。所以小赛事冷门项目可能整项目都没有上榜行。","`333mbf` 的 sum 列显示的是 **points 之和**,不是 WCA value 之和 — 跟其它项目的 clock-format 步调不同,看的时候要意识到单位差异。","同一场比赛的预赛 / 决赛可以同时上榜(它们是两个不同的 `round_type_id`)。"],edgesEn:["BLD events rank by `single`, all others by `average` — fixed by event, not user-configurable.","Rounds with fewer than 3 finishers are filtered (`third_result IS NOT NULL`). Niche events at tiny competitions may have zero rows.","`333mbf` displays a **points sum**, not a WCA-value sum — a different unit from clock-format times, so don't cross-compare.","A first-round and a final from the same competition can both appear (they have distinct `round_type_id`s)."],related:[{id:`best_potential_fmc_mean`,titleZh:`最佳潜在 FMC 平均`,titleEn:`Best potential FMC mean`,hintZh:`同样以"轮次"为单位的潜在视角`,hintEn:`Round-keyed sibling stat, but "potential" rather than achieved`},{id:`wr_aoxr`,titleZh:`AoXR (跨轮)`,titleEn:`AoXR (across rounds)`,hintZh:`反过来:同一选手跨多轮取平均`,hintEn:`Opposite axis: averages across rounds for one person`},{id:`yearly_rankings`,titleZh:`年度排名`,titleEn:`Yearly rankings`,hintZh:`一年时间窗内的最佳成绩`,hintEn:`Best results within a single year`},{id:`best_round`,toStat:!0,titleZh:`打开 Best round 榜`,titleEn:`Jump to Best round board`,hintZh:`21 项目 top 10`,hintEn:`21 events, top 10 each`}]},most_frequent_results:{id:`most_frequent_results`,titleZh:`最常出现的成绩`,titleEn:`Most frequent results`,badgeZh:`频次`,badgeEn:`Frequency`,introZh:[`把 WCA 历史上**所有 attempt 值**(每条结果有 1 ~ 5 次 attempt,3x3 ao5 = 5 次,FMC mean = 3 次,等等)拆开,看哪些**具体厘秒值**出现得最多。`,'结果出奇地有趣 —— 整十、整百的"round 数"明显堆积(如 3x3 的 `10.00`、`12.00`),这是计时器停秒时**人眼对齐**的副作用;FMC 因为整数步数,频次分布天然密集;BLD 的 DNF 几乎主导分布(但被 `> 0` 过滤掉了),所以榜上只看到成功成绩。'],introEn:[`Unroll every attempt value (each result has 1 ~ 5 attempts: 3x3 ao5 = 5, FMC mean = 3, single events = 1) across all WCA history, then count which exact **centisecond values** show up most often.`,"The result is oddly fun: round-number times pile up (e.g. `10.00` and `12.00` on 3x3), which is a side effect of how timer-stoppers eye the readout; FMC has dense integer values by construction; BLD is dominated by DNF, but `> 0` filtering means the board shows only successes."],stats:[{value:"`> 0`",labelZh:`只算成功`,labelEn:`Successful only`,hintZh:`DNF (-1)、DNS (-2)、未提交 (0) 全跳`,hintEn:`DNF (-1), DNS (-2), unattempted (0) all skipped`},{value:`21 项目`,labelZh:`逐项目分桶`,labelEn:`Per-event buckets`,hintZh:`不同项目厘秒分布完全不同,不混着排`,hintEn:`Different events have wildly different distributions; not pooled`},{value:`top 10`,labelZh:`榜单深度`,labelEn:`Rows per event`,hintZh:`每个项目取最热门 10 个具体值`,hintEn:`Per event: the 10 most-frequent exact values`},{value:`~M 级`,labelZh:`总 attempt 量`,labelEn:`Total attempts`,hintZh:`WCA 历史所有 attempt — 数千万级`,hintEn:`Tens of millions of attempts across WCA history`}],sourceZh:["只 `SELECT` 出 `event_id` 和 attempts 列(通过 `ATTEMPTS_SUBQUERY` 把 `result_attempts` 表的 1-5 个 attempt 拼成 comma-separated string),其它过滤、拆分、计数都在 TS 里跑。`event_id != '333mbo'`(旧 multi-BLD old style,已被 `333mbf` 取代)。"],sourceEn:["SQL only `SELECT`s `event_id` and a comma-separated `attempts` string (built by `ATTEMPTS_SUBQUERY` over the `result_attempts` table, joining 1 ~ 5 attempts into one cell). Splitting, filtering, and counting all happen in TS. Excludes `event_id = '333mbo'` (legacy multi-BLD, superseded by `333mbf`)."],sourceCode:{lang:`sql`,body:`SELECT
  event_id,
  \${ATTEMPTS_SUBQUERY} AS attempts  -- 1~5 attempts joined as 'v1,v2,v3,v4,v5'
FROM results result
WHERE event_id != '333mbo';

-- TS side:
--   for each row: attempts.split(',').map(Number)
--   for each value v > 0: counts.set(v, counts.get(v) + 1)
--   sort by count desc, top 10 per event`},steps:[{titleZh:`attempts 拼串`,titleEn:`Concatenate attempts`,bodyZh:"`ATTEMPTS_SUBQUERY` 在 `result_attempts` 表里按 `result_id` group + `GROUP_CONCAT(value ORDER BY attempt_number)`,得到形如 `'1234,1100,1450,1380,1290'` 的串。WCA 表设计本身没有 `value1..value5` 列,值散在 `result_attempts` 里一行一条。",bodyEn:"`ATTEMPTS_SUBQUERY` does a per-`result_id` `GROUP_CONCAT(value ORDER BY attempt_number)` over `result_attempts`, yielding strings like `'1234,1100,1450,1380,1290'`. (The WCA schema does **not** have `value1..value5` columns — each attempt is its own row in `result_attempts`.)"},{titleZh:`按项目分桶 + 拆 attempt`,titleEn:`Bucket by event + split attempts`,bodyZh:"`EVENTS_ENTRIES.map`,每个项目一节。`row['attempts'].split(',').map(Number)` 拆出 1-5 个 attempt;`if (v > 0)` 过滤掉 DNF(-1)、DNS(-2)、未提交(0)。",bodyEn:"`EVENTS_ENTRIES.map` builds one section per event. `row.attempts.split(',').map(Number)` extracts the 1 ~ 5 attempts; the `v > 0` filter drops DNF (-1), DNS (-2), and unattempted (0)."},{titleZh:"`Map<number, number>` 计数",titleEn:"`Map<number, number>` count",bodyZh:"`valueCounts.set(v, (valueCounts.get(v) ?? 0) + 1)` — 每见到一次 v 就 ++。注意 `v` 是**原始厘秒/步数**整数,所以 `10.00` (1000)、`10.01` (1001)、`10.02` (1002) 被算成三个不同 key。",bodyEn:"`valueCounts.set(v, (valueCounts.get(v) ?? 0) + 1)` — increment per occurrence. `v` is the **raw centisecond / move-count integer**, so `10.00` (1000), `10.01` (1001), `10.02` (1002) are three distinct keys."},{titleZh:`排序 + top 10 + 格式化`,titleEn:`Sort, top 10, format`,bodyZh:"`[...valueCounts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 10)`,然后用 `SolveTime(eventId, 'single', v).clockFormat()` 把整数转成展示串(`10.00` / `33` 等)。",bodyEn:"`[...valueCounts.entries()].sort((a, b) => b[1] - a[1]).slice(0, 10)`, then `SolveTime(eventId, 'single', v).clockFormat()` turns the integer into a display string (`10.00`, `33`, etc.).",highlight:!0}],edgesZh:["`333mbo` (multi-BLD old style) 整体被排除 — 它在 2009 年已被 `333mbf` 取代,数据点太少且编码不同。",`DNF / DNS / 未提交不计入频次,所以即使 BLD 项目里 DNF 大量出现,榜单也只展示成功 attempt。`,'FMC 的 attempt 是步数(integer),所以容易出现极高频整数(如 `28`、`29`、`30`);timed 项目则是厘秒,整 `.00`、`.50` 偏多反映"读秒手动停"的人因偏差。',`同一 result 的 5 个 attempt 全都进入计数 — 不只是 best / 不只是 average 用到的 3 个。`],edgesEn:["`333mbo` (multi-BLD old style) is excluded outright — superseded by `333mbf` in 2009, with too few data points and a different encoding.",`DNF / DNS / unattempted are not counted, so even though BLD events have abundant DNFs in raw data, the board shows only successful attempts.`,'FMC attempts are integer move counts, producing high-frequency exact values like `28`, `29`, `30`. Timed events are centiseconds — over-representation of `.00` and `.50` reflects human "stop-the-timer" perception bias.',"All 5 attempts of a result are counted — not only the `best`, not only the 3 that survive ao5 trimming."],related:[{id:`best_potential_fmc_mean`,titleZh:`最佳潜在 FMC 平均`,titleEn:`Best potential FMC mean`,hintZh:"同样用 `result_attempts` 拆 attempt slot",hintEn:"Also unrolls `result_attempts` per slot"},{id:`dnf_rate_by_event`,titleZh:`各项目 DNF 率`,titleEn:`DNF rate by event`,hintZh:`反过来:看被这里过滤掉的 DNF 占比`,hintEn:`Mirror view: how big a fraction were the filtered-out DNFs`},{id:`smallest_diff_between_single_and_average`,titleZh:`最小单平差`,titleEn:`Smallest single-average diff`,hintZh:`另一类"巧合数值"统计`,hintEn:`A different kind of "coincidental value" stat`},{id:`most_frequent_results`,toStat:!0,titleZh:`打开 Most frequent results`,titleEn:`Jump to live frequency board`,hintZh:`21 项目分布`,hintEn:`21 event distributions`}]},moving_average:{id:`moving_average`,titleZh:`移动平均`,titleEn:`Moving average`,badgeZh:`近期`,badgeEn:`Recent`,introZh:["`moving_average` 给每个选手 × 项目算一个**指数加权移动平均 (EMA)**:把该选手所有 official ao5 平均按比赛日期顺序排列,从早到晚扫一遍,新值权重 `1 − α`,老值衰减系数 `α`。这里 `α = 0.8`,意味着最近 ~5 个 average 占主导。",`它不像 official ao5 / ao12 那样"只在一个比赛里"取连续 N 次还原 —— 它跨**整个职业生涯**滑动,衡量"这位选手最近这阵子在该项目上水平有多稳"。career 不足 5 个 ao5 的选手会被排除(数据太少没意义)。`],introEn:["`moving_average` computes an **exponentially-weighted moving average (EMA)** per `(person, event)` pair: walk through every official ao5 average that solver has posted, in date order, weighting new values by `1 − α` and decaying old ones by `α`. Here `α = 0.8`, so the most recent ~5 averages dominate.",`Unlike official ao5 / ao12 (a single-comp window of consecutive solves), this glides over an entire **career**, measuring "how strong has this solver been lately at this event". People with fewer than 5 career ao5 averages are excluded (not enough signal).`],stats:[{value:`α = 0.8`,labelZh:`衰减系数`,labelEn:`Decay factor`,hintZh:`老成绩每滑一步衰减 0.8 倍`,hintEn:`Each step back in time discounts by 0.8`},{value:`~5`,labelZh:`有效窗口`,labelEn:`Effective window`,hintZh:`最近 5 个 average 总权重约占 2/3`,hintEn:`Most recent 5 averages contribute ~2/3 of total weight`},{value:`≥ 5`,labelZh:`最低 average 数`,labelEn:`Min averages`,hintZh:`少于 5 个 ao5 的选手不上榜`,hintEn:`Solvers with under 5 career averages are dropped`},{value:`50`,labelZh:`榜单深度`,labelEn:`Rows per event`,hintZh:`top 50 / 项目`,hintEn:`Top 50 per event`}],sourceZh:["`results` join `persons`(`sub_id = 1`)、`competitions`、`round_types`,**按 `start_date` + `round_type.rank` 全局排序**(职业生涯时间线)。只取 `average > 0`,排除 BLD 系列(`333bf / 333mbf / 333mbo / 444bf / 555bf`,因为它们的 single 才是排名口径)。`average_of_x` 的 sibling 思路用纯 SQL 滑动窗口;这里 EMA + 偏差校正只能在 TS 里跑。"],sourceEn:["`results` join `persons` (`sub_id = 1`), `competitions`, `round_types`, **ordered globally by `start_date` then `round_type.rank`** (career timeline). Filters to `average > 0` and drops BLD events (`333bf / 333mbf / 333mbo / 444bf / 555bf`, which are ranked by single, not average). Sibling stat `average_of_x` runs as pure-SQL sliding windows; here EMA + bias correction is done in TS."],sourceCode:{lang:`sql`,body:`SELECT
  person.name, person.wca_id, event_id, average
FROM results result
JOIN persons person ON person.wca_id = person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN round_types round_type ON round_type.id = round_type_id
WHERE average > 0
  AND event_id NOT IN ('333bf','333mbf','333mbo','444bf','555bf')
ORDER BY competition.start_date, round_type.rank;

-- TS: per (person, event) accumulate
--   avg = avg * α + (1 - α) * new_value      (α = 0.8)
--   bias-correct:  corrected = avg / (1 - αⁿ)`},steps:[{titleZh:`时间序排好整张表`,titleEn:`Sort the whole table chronologically`,bodyZh:"SQL 一次性按 `start_date, round_type.rank` 全排,TS 端按 `(person, event)` 收桶即可,无需在 TS 里再排。`round_type.rank` 保证同一比赛内预赛 → 半决 → 决赛顺序。",bodyEn:"SQL pre-sorts the entire table by `start_date, round_type.rank`. The TS side just needs to bucket by `(person, event)` — no re-sorting. `round_type.rank` ensures qualification → semifinal → final ordering within a comp."},{titleZh:`逐人迭代 EMA`,titleEn:`Iterate EMA per person`,bodyZh:"从 `avg = 0` 起步,每读一个新 average `num`:`avg = avg * α + (1 − α) * num`。`α = 0.8` 意味着老值每步衰减到 80%,新值仅占 20% — 但因为持续累加,最近 5 个值贡献了约 2/3 总权重。",bodyEn:"Start `avg = 0`. For each new `num`: `avg = avg * α + (1 − α) * num`. With `α = 0.8`, old values decay to 80% per step and the new one contributes 20% — but accumulated over time, the most recent 5 values contribute ~2/3 of total weight."},{titleZh:`偏差校正`,titleEn:`Bias correction`,bodyZh:'初期 `avg` 被 `0` 拖低(只跑了 1-2 个值时,衰减项还没"填满"),需要除以 `1 − αⁿ`(n = 已见值数量)来抵消。等价于 Adam optimizer 里那个 `1 − β₁ᵗ` 修正。`Math.round` 取整成厘秒 long-int。',bodyEn:'Early on, `avg` is dragged down by its `0` start (the decay term has not "filled in" yet). Dividing by `1 − αⁿ` (n = number of samples seen) cancels that — same trick as Adam optimizer\'s `1 − β₁ᵗ`. Rounded to a centisecond long-int.'},{titleZh:`过滤不足 5 + 排序取 top 50`,titleEn:`Filter < 5, sort, top 50`,bodyZh:"`.filter(([, avgs]) => avgs.length >= 5)` — 不足 5 个 average 的人不上榜。再按 EMA 升序取 50。每项目独立一节(BLD 5 项目整组缺席)。",bodyEn:"`.filter(([, avgs]) => avgs.length >= 5)` drops solvers with fewer than 5 averages. Sort ascending, take top 50. One section per event (BLD events absent as a group).",highlight:!0}],formulae:[{labelZh:`EMA 递推 + 偏差校正`,labelEn:`EMA recurrence + bias correction`,expr:`avgₜ = α · avgₜ₋₁ + (1 − α) · numₜ ;  EMA = avg_n / (1 − αⁿ)`,bodyZh:"α = 0.8,n = 该选手已发布的 average 数。先迭代,最后一步除以 `1 − αⁿ` 校正初期欠拟合。",bodyEn:"α = 0.8, n = number of averages the solver has posted. Iterate, then divide the final value by `1 − αⁿ` to correct the warm-up underestimate."}],edgesZh:["BLD 系列(`333bf / 333mbf / 333mbo / 444bf / 555bf`)整组被排除 — 它们以 single 为排名口径,放进 EMA 没意义。","`< 5 averages` 整段被剔。所以新人即使 5 个 ao5 飙到 sub-7 也得攒够数才上榜。",`EMA 对**单次大失误**记忆很短(几个 average 之后就忘了),所以"最近退步"的选手比 ao12/ao50 更快从榜上掉下来。`,'不像 `average_of_x` 那种"连续 N 次官方还原"的固定窗口口径 — EMA 是软窗口,没有"截断"。'],edgesEn:["BLD events (`333bf / 333mbf / 333mbo / 444bf / 555bf`) are excluded as a group — they're ranked by single, so EMA-on-averages is meaningless.",`Solvers with fewer than 5 averages are dropped. So a newcomer flying through 5 sub-7 ao5's still needs more career data before they appear.`,`EMA has very short memory for a single bad blowup (it fades within a few averages). Recent decliners drop off this board faster than they would from ao12 / ao50.`,"Unlike `average_of_x` (a hard window of N consecutive official attempts), EMA is a soft window — there is no cliff cutoff."],related:[{id:`average_of`,titleZh:`滚动平均 (ao3 ~ ao1000)`,titleEn:`Rolling Average (ao3 ~ ao1000)`,hintZh:`硬窗口连续 N 次官方还原,对照 EMA 软窗口`,hintEn:`Hard-window sibling: N consecutive official solves, vs EMA's soft window`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`更广义的 WR 指标族(bao5 / wao5 / mo5 / ...)`,hintEn:`Broader WR-metric family (bao5 / wao5 / mo5 / ...)`},{id:`yearly_rankings`,titleZh:`年度排名`,titleEn:`Yearly rankings`,hintZh:`时间窗硬切到一年,而非 EMA 软衰减`,hintEn:`Hard one-year cutoff, opposite of EMA's decay`},{id:`moving_average`,toStat:!0,titleZh:`打开 Moving average 榜`,titleEn:`Jump to Moving average board`,hintZh:`看每项目 top 50`,hintEn:`Per-event top 50`}]},smallest_diff_between_single_and_average:{id:`smallest_diff_between_single_and_average`,titleZh:`最小的单次与平均差距`,titleEn:`Smallest difference between a single and an average`,badgeZh:`巧合`,badgeEn:`Coincidence`,introZh:["在同一个 ao5 round 里,一个选手的**单次最佳 (best)** 和**平均 (average)** 应该总是 `best ≤ average`(因为 average 是去头去尾后剩 3 次的均值)。差距 `diff = average − best` 总是 ≥ 0。","这条 stat 找的是 diff 最小的那些 round —— 极端情况是 `diff = 0`(平均的 3 次计入还原恰好都跟 best 相同),次极端是 `diff = 0.01`(一厘秒)。这类记录通常发生在低位数选手 spam 一串极接近的还原时,或者大项目(`777` / `666`)上整组成绩浮动很小。"],introEn:["Within a single ao5 round, a solver's **best (single)** is always ≤ their **average** (because the average is the trimmed mean of the middle 3 of 5 attempts). So `diff = average − best` is always ≥ 0.","This stat picks out the rounds with the smallest such diff. The extreme case is `diff = 0` (the 3 counting solves happen to equal the single); the next is `diff = 0.01` (one centisecond). Often these come from low-time solvers spamming near-identical times, or from big-cube events (`777`, `666`) where the whole set moves together."],stats:[{value:"`avg − best`",labelZh:`定义`,labelEn:`Definition`,hintZh:`同一 round 的 average 减 single`,hintEn:`average minus single from the same round`},{value:`top 10`,labelZh:`榜单深度`,labelEn:`Rows per event`,hintZh:`按 diff 升序取前 10`,hintEn:`Top 10 by diff ascending`},{value:`剔除 333fm`,labelZh:"`333fm` 不入榜",labelEn:"`333fm` excluded",hintZh:`FMC 整数步数,diff = 0 太常见无意义`,hintEn:"FMC integer moves — `diff = 0` is trivial and noisy"},{value:`tiebreak`,labelZh:`三键排序`,labelEn:`Three-key sort`,hintZh:`diff → avg → single,确保稳定输出`,hintEn:`diff → avg → single, deterministic ordering`}],sourceZh:["`results` join `persons` (`sub_id = 1`)、`competitions`。SQL 只 SELECT `best`、`average` 和身份信息;`diff` 在 TS 端算。`event_id != '333fm'`(FMC 整数步数,diff=0 极常见,没区分度);`average > 0`(排除 DNF average / 未提交)。"],sourceEn:["`results` joined with `persons` (`sub_id = 1`) and `competitions`. SQL `SELECT`s `best`, `average`, and identity fields; `diff` is computed in TS. Filters: `event_id != '333fm'` (FMC integer values make `diff = 0` trivial), `average > 0` (drops DNF averages and unattempted)."],sourceCode:{lang:`sql`,body:`SELECT
  event_id,
  best AS single,
  average,
  person.name,
  person.wca_id,
  competition.cell_name,
  competition.id AS competition_id
FROM results
JOIN persons person ON person.wca_id = person_id AND sub_id = 1
JOIN competitions competition ON competition.id = competition_id
WHERE event_id != '333fm' AND average > 0;

-- TS: diff = average - single
--   sort by (diff ASC, average ASC, single ASC)
--   slice 0..10 per event`},steps:[{titleZh:`拉成对 (single, average)`,titleEn:`Pair (single, average)`,bodyZh:'从 `results` 取每一条 ao5 round 的 `best` 与 `average`。注意 WCA dump 里 `best` 列 = 该 round 的最佳 attempt(即"单次"列),`average` = ao5 / mo3 的官方均值。',bodyEn:'For each ao5 round in `results`, pull `best` and `average`. WCA dump convention: `best` column = the round\'s best attempt (i.e. the "single"), `average` = the ao5 / mo3 mean.'},{titleZh:`过滤口径`,titleEn:`Apply filters`,bodyZh:"`event_id != '333fm'` — FMC 整数步数容易 single 与 average 完全相同,`diff = 0` 没有区分度。`average > 0` 排除 DNF 平均(WCA value `-1`)和未提交(`0`)。",bodyEn:"`event_id != '333fm'` — FMC integer-move semantics make `diff = 0` too common to be meaningful. `average > 0` drops DNF averages (value `-1`) and unattempted (`0`)."},{titleZh:`计算 diff`,titleEn:`Compute diff`,bodyZh:"`diff = Number(average) − Number(single)`,单位是**厘秒**(可正可零,理论上不会为负 — single 是 ao5 内最佳)。注意:展示时 `(diff / 100).toFixed(2)` 转秒,但内部排序用整数厘秒避免浮点误差。",bodyEn:"`diff = Number(average) − Number(single)`, units are **centiseconds** (always ≥ 0 in theory — single is the round's best). For display: `(diff / 100).toFixed(2)` to seconds; internal sort stays on integer centiseconds to avoid float jitter."},{titleZh:`三键排序`,titleEn:`Three-key sort`,bodyZh:"`diff ASC → average ASC → single ASC` — diff 相同时偏好 average 更低的 round(更难达成),再相同时偏好 single 更低。这种 tiebreak 让大量 `diff = 0` 的行有确定排序。",bodyEn:"`diff ASC → average ASC → single ASC` — among ties on diff, prefer rounds with lower average (harder), then lower single. This deterministic tiebreak orders the (often many) `diff = 0` rows."},{titleZh:`每项目取 top 10`,titleEn:`Per-event top 10`,bodyZh:"`EVENTS_ENTRIES.map`,21 项目(剔除 `333fm`)每项目独立切前 10。`SolveTime(eventId, type, v).clockFormat()` 把厘秒转成展示串。",bodyEn:"`EVENTS_ENTRIES.map`, one section per event (FMC excluded), top 10 each. `SolveTime(eventId, type, v).clockFormat()` formats centiseconds for display.",highlight:!0}],formulae:[{labelZh:`差值`,labelEn:`Diff`,expr:`diff = average − single,  diff ≥ 0`,bodyZh:"WCA ao5 规则:5 次 attempt 去最快和最慢,剩 3 次取算术平均。所以 `single ≤ 任一中间 3 次 ≤ average`(忽略 DNF 边界)。",bodyEn:"WCA ao5 rule: drop fastest and slowest of 5 attempts, average the middle 3. Therefore `single ≤ any of the middle 3 ≤ average` (ignoring DNF edge cases)."}],edgesZh:["`333fm` 整体被排除 — FMC 是整数步数,`28 / 28 / 28 / 30 / 29` → single=28, mean=28.67,这种 `diff = 0` 在每场 FMC 都有,放进来淹没榜单。","BLD 系列只有 `333bf` / `444bf` / `555bf` 有 average(`mo3`),`333mbf` 没有 average;BLD 的 `mo3` 没去头尾,所以 `single ≤ mo3` 仍然成立但口径不同。","diff 用厘秒整数算,展示用 2 位小数 — `diff = 0.00` 和 `diff = 0.01` 在榜上同时存在很正常。",`不是"最相似 single 和 average"在 WCA 历史的查询 — 而是**同一 round 内**的差。两个不同 round 的"巧合一致"不会上榜。`],edgesEn:["`333fm` is excluded as a whole — integer move counts make `28 / 28 / 28 / 30 / 29` → single=28, mean=28.67 trivially common; if not excluded it would swamp every other event.","Of the BLD family, only `333bf / 444bf / 555bf` have averages (`mo3`); `333mbf` has none. BLD `mo3` does not trim head/tail, but `single ≤ mo3` still holds (different semantics).","Internally diff is integer centiseconds; the display rounds to 2 decimals — `diff = 0.00` and `diff = 0.01` happily coexist on the board.",`This is **within a single round**, not "most similar single and average across history" — two coincidentally-equal values in different rounds are not eligible.`],related:[{id:`most_frequent_results`,titleZh:`最常出现的成绩`,titleEn:`Most frequent results`,hintZh:`同样在"巧合数值"题材`,hintEn:`Sibling stat in the "value coincidence" theme`},{id:`best_round`,titleZh:`最佳轮次`,titleEn:`Best round`,hintZh:`round 级别的另一视角:top 3 之和`,hintEn:`Round-level sibling: top-3 sum view`},{id:`average_of`,titleZh:`滚动平均`,titleEn:`Rolling Average`,hintZh:`ao5 / mo3 计算口径详解`,hintEn:`Detail on the ao5 / mo3 semantics this stat relies on`},{id:`smallest_diff_between_single_and_average`,toStat:!0,titleZh:`打开最小单平差榜`,titleEn:`Jump to Smallest diff board`,hintZh:`21 项目 top 10`,hintEn:`21 events, top 10 each`}]},yearly_rankings:{id:`yearly_rankings`,titleZh:`年度排名`,titleEn:`Yearly rankings`,badgeZh:`年度`,badgeEn:`Yearly`,introZh:["`yearly_rankings` 是把 `Rankings` 抽象基类用 `WHERE YEAR(competition.start_date) = YEAR(CURDATE())` 切到**当年**得到的子统计 —— 每项目 × {Single, Average} 两个榜,各 top 10。",'这个 stat 是识别"当年统治力"的最快窗口:谁今年开了挂、谁新爬上 sub-X、哪个项目今年集体破纪录。和 `wr_current` 不同(那是全时段 WR),这里**只看 2026 这一年发生的成绩**。'],introEn:["`yearly_rankings` is the `Rankings` abstract base specialized to **the current calendar year** via `WHERE YEAR(competition.start_date) = YEAR(CURDATE())` — producing two boards per event ({Single, Average}), top 10 each.",'This is the quickest window into "who is dominant this year": who is breaking out, who just dropped sub-X, which events are seeing record clusters. Different from `wr_current` (all-time WR) — this filter is strictly **results that happened in 2026 itself**.'],stats:[{value:`当年`,labelZh:`时间窗`,labelEn:`Time window`,hintZh:"`YEAR(start_date) = YEAR(CURDATE())`",hintEn:"`YEAR(start_date) = YEAR(CURDATE())`"},{value:`21 × 2`,labelZh:`面板数`,labelEn:`Panels`,hintZh:`21 项目,每项目 Single + Average`,hintEn:`21 events, Single + Average each`},{value:`top 10`,labelZh:`每榜深度`,labelEn:`Per panel`,hintZh:`每榜 top 10`,hintEn:`Top 10 per panel`},{value:`人去重`,labelZh:`每人取最佳`,labelEn:`Best per person`,hintZh:`同一人多条只留最快那条`,hintEn:`Same solver, multiple rows → only the fastest is kept`}],sourceZh:["extends `Rankings`(共用基类),`condition = 'WHERE YEAR(competition.start_date) = YEAR(CURDATE())'`。`Rankings.query()` 从 `results` join `persons` (`sub_id = 1`)、`countries`、`competitions`,带出 single / average / attempts / 比赛 / 国籍信息。当前年口径意味着每元旦 0:00 整张榜清零重建。"],sourceEn:["extends the shared `Rankings` base class with `condition = 'WHERE YEAR(competition.start_date) = YEAR(CURDATE())'`. `Rankings.query()` joins `results`, `persons` (`sub_id = 1`), `countries`, `competitions`, pulling single / average / attempts / comp / nationality. The \"current year\" cutoff means the board resets at every January 1st."],sourceCode:{lang:`sql`,body:`SELECT
  event_id,
  best AS single,
  average,
  \${ATTEMPTS_SUBQUERY} AS attempts,
  person.name, person.wca_id,
  country.name AS country,
  competition.cell_name, competition.id
FROM results result
JOIN persons person ON person.wca_id = person_id AND person.sub_id = 1
JOIN countries country ON country.id = person.country_id
JOIN competitions competition ON competition.id = competition_id
WHERE YEAR(competition.start_date) = YEAR(CURDATE());

-- TS (Rankings.transform):
--   for each event × ('single' | 'average'):
--     filter value > 0
--     sort by SolveTime.compareTo
--     dedupe by person (keep best)
--     slice 0..10`},steps:[{titleZh:`时间窗过滤`,titleEn:`Filter to the current year`,bodyZh:"`WHERE YEAR(competition.start_date) = YEAR(CURDATE())` — 只放行当年的比赛结果。SQL 端一次过滤完,后续逻辑都共享 `Rankings` 基类的实现。",bodyEn:"`WHERE YEAR(competition.start_date) = YEAR(CURDATE())` lets through only this year's competition results. The SQL handles the filter once; everything downstream reuses the shared `Rankings` base."},{titleZh:`展开 event × type`,titleEn:`Expand event × type`,bodyZh:"`EVENTS_ENTRIES.flatMap` 把每个项目展开成 `[Single, Average]` 两个 panel(共 ~42 个面板,部分项目 average panel 为空,如 `333mbf`)。",bodyEn:"`EVENTS_ENTRIES.flatMap` expands each event into a `[Single, Average]` pair (≈ 42 panels total — some events have an empty average panel, e.g. `333mbf`)."},{titleZh:`正值过滤 + SolveTime 排序`,titleEn:`Positive filter + SolveTime sort`,bodyZh:"`Number(r[type]) > 0` 排除 DNF/DNS/未提交。然后用 `SolveTime.compareTo` 排序(BLD points 项目走升序仍然语义正确,因为基类内部已封好)。",bodyEn:"`Number(r[type]) > 0` drops DNF / DNS / unattempted. Then `SolveTime.compareTo` sorts — for BLD-points events the comparator is encapsulated correctly inside `SolveTime` so the calling code remains uniform."},{titleZh:`人去重`,titleEn:`Dedupe by person`,bodyZh:'`Set<person_link>`,从已排序列表中扫,首次出现即记入,后续重复 skip。等价于"每人保留**今年内**最快的那条"。',bodyEn:'`Set<person_link>` walks the sorted list and records each solver on first encounter, skipping duplicates. Equivalent to "keep this year\'s fastest record per person".'},{titleZh:`取 top 10 + 渲染`,titleEn:`Take top 10 + render`,bodyZh:'`slice(0, 10)`,每行附带 attempts 串("**32.10** 33.45 30.20 ...")便于看到组成。每个 (event, type) 一个 section。',bodyEn:'`slice(0, 10)`. Each row attaches the attempts string ("**32.10** 33.45 30.20 ...") so viewers can see the composition. One section per `(event, type)`.',highlight:!0}],edgesZh:['"当年"= `YEAR(CURDATE())`,即 build 时的服务器日历年。跨年那一刻整张榜从零开始重新累积。',"`sub_id = 1` 过滤副身份(改名 / 换国籍 / 多 WCA ID 的 dump 行)。","BLD multi (`333mbf`) 没有 average,所以它的 Average panel 永远是空 — 渲染时整 panel 不会显示。",`每人去重是"全年最佳",不是"每场比赛最佳" — 不是按 round / comp / round_type 分。一个人哪怕全年比了 30 场,这里也只算他/她最快的那一条。`],edgesEn:['"Current year" = `YEAR(CURDATE())` at build time on the server clock. On New Year the board resets and re-accumulates from zero.',"`sub_id = 1` filters alt identities (renames, country changes, multiple WCA IDs in the dump).","BLD multi (`333mbf`) has no average, so its Average panel is always empty — and won't render.",`Dedupe is "year-best per person", not per-comp or per-round. Even if someone competes 30 times in the year, only their single fastest result appears.`],related:[{id:`wr_current`,titleZh:`现役世界纪录`,titleEn:`Current world records`,hintZh:`全时段口径,对照"今年口径"`,hintEn:`All-time WR, contrast with this year-only view`},{id:`wr_metric`,titleZh:`指标 (Metric)`,titleEn:`Metric`,hintZh:`WR 指标族 — single/average/bao5/wao5/...`,hintEn:`WR metric family — single / average / bao5 / wao5 / ...`},{id:`moving_average`,titleZh:`移动平均`,titleEn:`Moving average`,hintZh:`EMA 软窗口 vs 一年硬窗口`,hintEn:`EMA soft window vs hard one-year cutoff`},{id:`yearly_rankings`,toStat:!0,titleZh:`打开 Yearly rankings`,titleEn:`Jump to Yearly rankings`,hintZh:`21 项目 × {Single, Average} 共 ~42 面板`,hintEn:`21 events × {Single, Average} ≈ 42 panels`}]}},l={best_result_off_podium:{id:`best_result_off_podium`,titleZh:`最佳非领奖台成绩 — 离奖牌最近的眼泪`,titleEn:`Best result not providing a podium`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:[`一个选手在决赛打出了**几乎可以拿奖牌**的成绩,但因为另外三个人更快、最后只排到第 4 或更后 —— 这条 stat 就是给这些"差一点"的成绩留个名字。`,`换句话说,它把"项目历史上**没能进领奖台的最快成绩**"按项目列出来。可以读作"奖牌门槛的天花板"。`],introEn:[`When a competitor posts a result in a final that would have medalled at almost any other competition, but three other people were faster on the day — this stat names that pain.`,`In other words: the **fastest result per event that did NOT make the podium**. Read it as the soft ceiling above which finishing 4th still hurts.`],stats:[{value:`17`,labelZh:`覆盖项目`,labelEn:`Events`,hintZh:`所有 WCA 项目分组排`,hintEn:`Grouped per WCA event`},{value:`pos > 3`,labelZh:`过滤条件`,labelEn:`Filter`,hintZh:`排除 1/2/3 名`,hintEn:`Drops 1st / 2nd / 3rd`},{value:`决赛`,labelZh:`轮次范围`,labelEn:`Round scope`,hintZh:`只看 round_type c / f`,hintEn:`Only finals (c, f)`},{value:`Top 10`,labelZh:`每项目保留`,labelEn:`Per event`,hintZh:`按主成绩升序前 10`,hintEn:`Top 10 by primary metric`}],sourceZh:["从 `results` 表筛 `round_type_id IN ('c', 'f')`(综合决赛 / 决赛)且 `pos > 3`,join `persons`(`sub_id = 1` 取主身份)、`competitions`(取赛事名)、以及 `preferred_formats` + `formats`(拿到 `sort_by` / `sort_by_second`,决定该项目按 single 还是 average 排)。"],sourceEn:["From `results` filter `round_type_id IN ('c', 'f')` (combined final / final) and `pos > 3`. Join `persons` (`sub_id = 1` for primary identity), `competitions` for the cell_name, and `preferred_formats` + `formats` to read `sort_by` / `sort_by_second` — these decide whether the event ranks by single or by average."],sourceCode:{lang:`sql`,body:`SELECT format.sort_by, format.sort_by_second,
       results.event_id,
       best AS single, average,
       person.name, competition.cell_name,
       pos AS place
FROM results
JOIN persons person ON person.wca_id = person_id
                   AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN preferred_formats pf ON pf.event_id = results.event_id
                         AND pf.ranking = 1
JOIN formats format ON format.id = pf.format_id
WHERE round_type_id IN ('c', 'f') AND pos > 3;`},steps:[{titleZh:`按项目分组`,titleEn:`Group by event`,bodyZh:`遍历 17 个 WCA 项目,每个项目独立排榜 —— 不同项目用不同主成绩(single vs average),不能混排。`,bodyEn:`Iterate over all 17 WCA events; each event has its own leaderboard because the primary metric (single vs average) differs by event.`},{titleZh:`解析主 / 次成绩`,titleEn:`Parse primary / secondary metric`,bodyZh:'从 `preferred_formats` 读这个项目的 `sort_by`:333fm = "average",大多数项目 = "average",333bf / 333mbf / 333ft = "single"。`sort_by_second` 是平局打破字段。',bodyEn:'Read the event\'s `sort_by` from `preferred_formats`: 333fm = "average", most events = "average", 333bf / 333mbf / 333ft = "single". `sort_by_second` breaks ties.'},{titleZh:`按主成绩升序`,titleEn:`Sort ascending by primary`,bodyZh:'`SolveTime.compareTo` 知道 DNF / DNS 怎么排;先比主,主相等再比次。结果越小越靠前 —— 我们要的就是"最快的非领奖台"。',bodyEn:"Use `SolveTime.compareTo` (it knows how DNF / DNS sort): compare primary first, then secondary on tie. Smaller is better — we want the fastest off-podium."},{titleZh:`取前 10`,titleEn:`Take top 10`,bodyZh:"排序后 `slice(0, 10)` 截前 10,组装成 `[选手, single, average, 比赛, 名次]` 五列;主成绩在表里加粗。",bodyEn:"After sort, `slice(0, 10)` truncates to 10 rows of `[person, single, average, competition, place]`. Primary metric is bolded in the rendered table.",highlight:!0}],edgesZh:["只看决赛 (`round_type c / f`) —— 预赛 / 半决里第 4 名不算,即使时间很快也无视。",'`pos > 3` 是 WCA 官方给的名次,平局并列规则已包含在内(并列第 3 仍然算第 3,不算"非领奖台")。',"DNF 主成绩(`value = -1`)排序时被推到最末,所以不会污染榜单(`SolveTime` 类负责)。","`sub_id = 1` 过滤改名 / 换国籍生成的副 person 行,避免重复。"],edgesEn:["Only finals (`round_type c / f`) — fast 4th-place finishes in early rounds don't count.","`pos > 3` is WCA's official placement (ties handled at the source — a co-3rd is still 3rd, not off-podium).","DNF primary results sort last via `SolveTime.compareTo`, so they don't pollute the top of the off-podium list.","`sub_id = 1` filters out alternate person rows from renames / country changes."],related:[{id:`worst_result_on_podium`,titleZh:`领奖台最差成绩`,titleEn:`Worst result on podium`,hintZh:`镜像 stat — 拿了奖牌的最慢成绩`,hintEn:`Mirror stat — the slowest results that still medalled`},{id:`most_4th_places`,titleZh:`最多第四名`,titleEn:`Most 4th places`,hintZh:`按选手算"差一点"的累积次数`,hintEn:`Per-person tally of near-misses`},{id:`best_result_off_podium`,toStat:!0,titleZh:`直接打开排名`,titleEn:`Jump to the leaderboard`,hintZh:`看每个项目的"差点拿奖"完整 top 10`,hintEn:`Full per-event top 10 of off-podium bests`}]},complete_competition_winners:{id:`complete_competition_winners`,titleZh:`完全比赛冠军 — 包圆一场赛事`,titleEn:`Complete competition winners`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:[`某场比赛举办了 N 个项目,有一个选手把这 N 个项目的金牌**全部拿走** —— 这条 stat 就是收集这样的"全包"事件。`,`事件数 N 越大越罕见。早期 weekend 小赛 5 - 8 项还可以个人通吃;现代 20 + 项目的大赛要全包需要从 333 到 333mbld 全能,几乎不可能。`],introEn:[`When a single competition holds N events and one person wins gold in **every** one of them — that's a complete competition win.`,`Bigger N = rarer. Small weekend comps of 5 - 8 events occasionally see a sweep; a modern 20+-event regional being swept by one cuber would require fluency from 3x3 through 3x3 multi-blind and effectively never happens.`],stats:[{value:`pos = 1`,labelZh:`入选条件`,labelEn:`Filter`,hintZh:`每个项目都拿第一`,hintEn:`Gold in every event`},{value:`HAVING = 1`,labelZh:`关键 SQL`,labelEn:`SQL key`,hintZh:`比赛里"第一名集合"只有 1 人`,hintEn:`Set of 1st-place people has size 1`},{value:`决赛`,labelZh:`轮次范围`,labelEn:`Round scope`,hintZh:`只看 round_type c / f`,hintEn:`Finals only`},{value:`DESC`,labelZh:`排序`,labelEn:`Sort`,hintZh:`项目数多者优先`,hintEn:`More events first`}],sourceZh:["内层在 `results` 上按 `competition_id` 分组,过滤 `round_type_id IN ('c', 'f') AND pos = 1 AND best > 0`,然后 `GROUP_CONCAT(DISTINCT person_id)` + `HAVING COUNT(DISTINCT person_id) = 1`。这一步保证\"该比赛所有冠军都是同一个人\"。","外层再 join `persons` / `competitions` / `countries` 拿展示字段;按 `events_count DESC` 排序。"],sourceEn:["The inner query groups `results` by `competition_id`, filters `round_type_id IN ('c', 'f') AND pos = 1 AND best > 0`, then `GROUP_CONCAT(DISTINCT person_id)` with `HAVING COUNT(DISTINCT person_id) = 1` — ensuring every gold at that comp belongs to the same person.","The outer query joins `persons` / `competitions` / `countries` for display, sorted by `events_count DESC`."],sourceCode:{lang:`sql`,body:`SELECT events_count, person.name,
       country.name, competition.cell_name
FROM (
  SELECT competition_id,
         GROUP_CONCAT(DISTINCT person_id) AS person_id,
         COUNT(DISTINCT event_id)         AS events_count
  FROM results
  WHERE round_type_id IN ('c', 'f')
    AND pos = 1
    AND best > 0
  GROUP BY competition_id
  HAVING COUNT(DISTINCT person_id) = 1
) AS sweeps
JOIN persons person ON person.wca_id = person_id AND sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN countries country ON country.id = person.country_id
ORDER BY events_count DESC, person.name;`},steps:[{titleZh:`按比赛收集所有金牌`,titleEn:`Collect all golds per comp`,bodyZh:'内层 `GROUP BY competition_id`,把这场比赛每个项目的"第一名 person_id"汇总。',bodyEn:"Inner query `GROUP BY competition_id`; for each comp, gather the person_id of every event's 1st place."},{titleZh:`只留"一人通吃"`,titleEn:`Keep only single-person sweeps`,bodyZh:"`HAVING COUNT(DISTINCT person_id) = 1` —— 集合大小必须正好 1。两人各拿几块金牌 = 不算。",bodyEn:"`HAVING COUNT(DISTINCT person_id) = 1` — the set of gold-medallists must have size exactly 1. Two people splitting golds doesn't qualify."},{titleZh:`数项目数`,titleEn:`Count events`,bodyZh:'`COUNT(DISTINCT event_id)` 就是这场比赛的项目数 N —— 也是包圆的"难度"。',bodyEn:"`COUNT(DISTINCT event_id)` is the comp's event count N — also the sweep's difficulty."},{titleZh:`关联展示字段并排序`,titleEn:`Join display fields and sort`,bodyZh:"外层 join 把 wca_id / cell_name / country.name 拼成 markdown 链接,按 `events_count DESC` 然后按 person.name 字典序兜底。",bodyEn:"Outer join attaches wca_id / cell_name / country.name as markdown links; sorts by `events_count DESC` then person.name as tiebreaker.",highlight:!0}],edgesZh:['`best > 0` 排除 DNF —— 即使你"获得"了第一名而成绩是 DNF,这条记录被过滤(很少见但理论可能,比如全员 DNF 时按 best 顺序仍有 pos = 1)。',`\`round_type_id IN ('c', 'f')\` 只看最终轮,避免预赛"局部第一"误判。`,`没有项目数下限,N = 1 的"独项目比赛"也会被包含(理论上单项目赛拿冠军就算包圆)。表头按 N 倒排,这些天然沉底。`,"`sub_id = 1` 让某选手换国籍后,只用主行匹配,不重复出现。"],edgesEn:['`best > 0` excludes DNF results — even if a "DNF gold" technically exists, it doesn\'t qualify.',"`round_type_id IN ('c', 'f')` restricts to actual finals; first-place finishes in early rounds don't count.",`No floor on N — a one-event comp where you win the only event is technically a sweep. They sink to the bottom in the N-DESC sort naturally.`,"`sub_id = 1` keeps renames / country-changes from generating duplicate rows."],related:[{id:`most_podiums_at_single_competition`,titleZh:`单场比赛登台最多`,titleEn:`Most podiums at single comp`,hintZh:`只要求"登台",不要求都金 — 门槛更低`,hintEn:`Same comp scope but counts any podium, not just gold`},{id:`most_finals`,titleZh:`进入决赛最多`,titleEn:`Most finals`,hintZh:`生涯级累积口径`,hintEn:`Career-level finals tally`},{id:`complete_competition_winners`,toStat:!0,titleZh:`直接打开包圆名册`,titleEn:`Jump to the sweep registry`,hintZh:`看每一场被某选手完全征服的比赛`,hintEn:`Every comp ever swept by one person`}]},most_4th_places:{id:`most_4th_places`,titleZh:`最多第四名 — "伴娘"榜`,titleEn:`Most 4th places`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:[`决赛第 4 名 —— 离奖牌一步之遥,却拿不到任何东西。这条 stat 按选手统计"决赛 4th"出现的总次数。`,`叫"伴娘"(bridesmaid)是社区习惯。在比赛抽奖意义上没有任何价值,但它揭示了一个选手"长期处于顶尖决赛圈但常被前 3 压住"的画像。`],introEn:[`4th place in a final — one slot from a medal, but no hardware. This stat counts career-wide "final 4ths" per person.`,`Known as the "bridesmaid" stat. It rewards no one, but it does sketch a portrait of cubers who routinely make finals only to be pipped on the day.`],stats:[{value:`pos = 4`,labelZh:`过滤条件`,labelEn:`Filter`,hintZh:`只数第 4 名`,hintEn:`Counts only 4th place`},{value:`final = 1`,labelZh:`轮次范围`,labelEn:`Round scope`,hintZh:`只统计决赛(round_types.final 标志)`,hintEn:`Finals only via round_types.final`},{value:`Top 100`,labelZh:`保留人数`,labelEn:`Cutoff`,hintZh:`LIMIT 100,过滤长尾`,hintEn:`LIMIT 100, drops long tail`},{value:`per person`,labelZh:`聚合粒度`,labelEn:`Granularity`,hintZh:`一行一个选手`,hintEn:`One row per cuber`}],sourceZh:["内层在 `results` 上按 `person_id` 分组,join `round_types` 取 `final = 1`,然后过滤 `pos = 4 AND best > 0`,`COUNT(*)` 得到该选手生涯第 4 总数。`ORDER BY ... DESC LIMIT 100`。","外层 join `persons`(`sub_id = 1`)拼 markdown 链接。"],sourceEn:["Inner query groups `results` by `person_id`, joins `round_types` for `final = 1`, filters `pos = 4 AND best > 0`, and `COUNT(*)` is the per-person career 4th tally. `ORDER BY ... DESC LIMIT 100`.","Outer query joins `persons` (`sub_id = 1`) to build the markdown link."],sourceCode:{lang:`sql`,body:`SELECT 4th_places_count, person.name
FROM (
  SELECT person_id AS wca_id,
         COUNT(*) AS 4th_places_count
  FROM results
  JOIN round_types round_type
    ON round_type.id = round_type_id
  WHERE round_type.final = 1
    AND pos = 4
    AND best > 0
  GROUP BY person_id
  ORDER BY 4th_places_count DESC
  LIMIT 100
) AS counts
JOIN persons person ON person.wca_id = counts.wca_id
                   AND person.sub_id = 1
ORDER BY 4th_places_count DESC;`},steps:[{titleZh:`锁定决赛`,titleEn:`Restrict to finals`,bodyZh:"join `round_types` 表,只保留 `final = 1` 的行(WCA round 系统里 c / f / 1 round comps 都打 final 标志)。",bodyEn:"Join `round_types` and keep `final = 1` rows (c / f / single-round comps all carry the final flag)."},{titleZh:`过滤"有效第 4 名"`,titleEn:`Filter valid 4th places`,bodyZh:'`pos = 4 AND best > 0` —— pos 是官方名次,`best > 0` 排除整轮 DNF 的"运气名次"(全员 DNF 时仍按提交顺序填 pos)。',bodyEn:"`pos = 4 AND best > 0` — pos is official placement, `best > 0` filters out all-DNF rounds where someone gets a hollow 4th."},{titleZh:`按选手 COUNT`,titleEn:`Aggregate per person`,bodyZh:"`GROUP BY person_id` + `COUNT(*)`,得到生涯累积。一个比赛同一项目只会有一个决赛,所以不会重复计数。",bodyEn:"`GROUP BY person_id` + `COUNT(*)` produces the career tally. Each comp × event has only one final, so no double counting."},{titleZh:`截 top 100 + 拼接展示`,titleEn:`Take top 100 and decorate`,bodyZh:"内层 `ORDER BY ... LIMIT 100`,外层 join `persons` 加 markdown person 链接;最终再做一次 `ORDER BY` 保证渲染顺序稳定。",bodyEn:"Inner `ORDER BY ... LIMIT 100`; outer joins `persons` for markdown links. A final `ORDER BY` keeps render order stable.",highlight:!0}],edgesZh:[`只看决赛 —— 预赛 / 半决出第 4 名不算(WCA 早期偶有"奇数轮"也通过 final flag 涵盖)。`,'`best > 0` 关键 —— 决赛 DNF 时 pos 仍按提交顺序填到 4,这条会被过滤掉,以免"运气 4th"灌水。',`所有项目混在一起 cumulatively 累计,不分单 / 平均 —— 一个 3x3 单 4 + 一个 333bf 4 = 2。`,`LIMIT 100 是渲染体面值,真实 4 次的人远不止 100;长尾不可见。`],edgesEn:["Finals only — fast 4ths in earlier rounds don't count (the `round_types.final` flag also covers odd single-round comps).","`best > 0` is critical — when an entire final is DNF, pos still fills to 4 by submission order; filtering shields the count from these hollow 4ths.",`All events pooled — a 3x3 4th and a 3x3-blind 4th both count, tally = 2.`,`LIMIT 100 is a display cap; the long tail of "4 once" people is invisible by design.`],related:[{id:`best_result_off_podium`,titleZh:`最佳非领奖台成绩`,titleEn:`Best result off podium`,hintZh:`同主题的"成绩侧"口径 — 最快的 4th 是哪条`,hintEn:`Sibling — the fastest 4th-or-worse times`},{id:`most_finals`,titleZh:`进入决赛最多`,titleEn:`Most finals`,hintZh:`不限名次,看决赛出场次数`,hintEn:`Same scope but any placement counts`},{id:`most_4th_places`,toStat:!0,titleZh:`直接打开伴娘榜`,titleEn:`Jump to the bridesmaid list`,hintZh:`看 top 100 完整名单`,hintEn:`Live top-100 leaderboard`}]},most_competitions_before_winning:{id:`most_competitions_before_winning`,titleZh:`首次获胜前参加最多比赛 — 漫长的等待`,titleEn:`Most competitions before winning`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:[`一个选手在某个项目上**第一次**赢下决赛之前,已经参加了多少场举办了该项目的比赛?这条 stat 就是数这个等待时长 —— 按"场数"计,不按"年数"。`,`名字里"win"指任何项目决赛拿第一,不是 WR。所以这条 stat 既能描述大牌选手"在自己强项里多久才登顶",也能给"小项目专家终于熬出头"留个画面。`],introEn:[`For each event, count how many comps (that held that event) a person attended before their **first ever final win** in it. Measured in competitions, not in years.`,`"Win" here means 1st place in a final — not WR. So the stat captures both top cubers slowly grinding into a main event, and small-event specialists who finally got their gold.`],stats:[{value:`17`,labelZh:`覆盖项目`,labelEn:`Events`,hintZh:`按项目独立计算`,hintEn:`Computed per event`},{value:`first 1st`,labelZh:`触发条件`,labelEn:`Trigger`,hintZh:`首次决赛 pos = 1 AND best > 0`,hintEn:`First final pos = 1 AND best > 0`},{value:`start_date`,labelZh:`时间轴`,labelEn:`Timeline`,hintZh:`按比赛开始日期排序`,hintEn:`Ordered by comp start_date`},{value:`Top 10`,labelZh:`每项目保留`,labelEn:`Per event`,hintZh:`等待最长的 10 人`,hintEn:`10 longest waits per event`}],sourceZh:['从 `results` 拉所有行,join `persons` / `competitions` / `round_types`;`ORDER BY start_date` 保证时间正序。然后在 TS 里按"项目 → 选手 → 比赛"嵌套分组,扫到第一次"决赛 + pos = 1 + best > 0"为止,这次比赛**之前**参加的场数就是答案。'],sourceEn:['Pull all rows from `results`, join `persons` / `competitions` / `round_types`, `ORDER BY start_date` for time ordering. In TypeScript, nest-group by event → person → comp, scan in chronological order until the first "final + pos = 1 + best > 0" — the count of comps **before** that one is the answer.'],sourceCode:{lang:`sql`,body:`SELECT person.name,
       competition.cell_name,
       event_id,
       pos AS place,
       round_type.final AS is_final,
       best
FROM results
JOIN persons person ON person.wca_id = person_id
                   AND sub_id = 1
JOIN competitions competition
  ON competition.id = competition_id
JOIN round_types round_type
  ON round_type.id = round_type_id
ORDER BY start_date;`},steps:[{titleZh:`时间正序拉所有 result 行`,titleEn:`Pull all rows in time order`,bodyZh:"SQL 不做聚合,只是 `ORDER BY start_date`。每行带 `event_id` / `person_id` / `competition_id` / `pos` / `is_final` / `best`。",bodyEn:"No SQL aggregation — just `ORDER BY start_date`. Each row carries `event_id` / `person_id` / `competition_id` / `pos` / `is_final` / `best`."},{titleZh:`按项目筛 → 按选手分桶`,titleEn:`Filter per event, bucket per person`,bodyZh:"`EVENTS_ENTRIES.map` 遍历 17 个项目;每个项目内 `Map<personLink, rows[]>`,行的相对顺序保留(因为 SQL 已按时间排过)。",bodyEn:"`EVENTS_ENTRIES.map` iterates 17 events. Inside, `Map<personLink, rows[]>` — the row order is preserved because SQL already sorted by time."},{titleZh:`同人内按比赛再分桶`,titleEn:`Inside person, bucket by comp`,bodyZh:'一个人在一场比赛里可能跑多轮,只能按"比赛是不是赢了"来标 —— 用 `byComp` Map + `compOrder` 数组保住时间序。',bodyEn:'A person may run multiple rounds at one comp; we only need to know "did the comp end in a win" — track with a `byComp` Map plus a `compOrder` array to keep chronology.'},{titleZh:`每场打"是否获胜"标签`,titleEn:`Tag each comp with won-flag`,bodyZh:"`won = compRows.some(r => is_final === 1 && place === 1 && best > 0)`。一场比赛只要在该项目决赛里有一行 pos = 1 就算赢。",bodyEn:"`won = compRows.some(r => is_final === 1 && place === 1 && best > 0)`. A single 1st-place final row in that event flags the whole comp as a win."},{titleZh:`找首次 won 的下标`,titleEn:`Find first won index`,bodyZh:'`findIndex(c => c.won)` 给出"第几场起赢了" —— 这个下标 = 之前参加的场数。倒序取 top 10 后输出。',bodyEn:"`findIndex(c => c.won)` returns the index of the breakthrough — that index = number of prior comps. Sort descending, take top 10 per event.",highlight:!0}],edgesZh:['没在该项目里赢过的人会被 `findIndex` 返回 -1,直接跳过 —— 这是设计:我们要的是"赢了之后回头数等了多久",还没赢的没法算。','`is_final === 1 AND place === 1 AND best > 0` 三条必须同时成立,纯 DNF 决赛的"幽灵冠军"被滤掉。','"参加了该项目的比赛"用 `eventRows.filter(r => event_id === eventId)` 隐式定义 —— 不在该项目上场的比赛不计入等待长度。',`sub_id = 1 + 一个选手即使换 wca_id 也只看主行,etc.(若 wca_id 真换了则视为两个人 — WCA 极少改 wca_id)。`],edgesEn:["People who never won in an event return -1 from `findIndex` and are skipped — by design: this stat counts the wait before a win, so non-winners are uncountable.","`is_final === 1 AND place === 1 AND best > 0` all required; ghost-wins from fully-DNF'd finals are dropped.",`"Attended a comp that held the event" is implicitly defined by \`eventRows.filter(r => event_id === eventId)\` — comps where the person didn't enter that event don't add to the wait.`,"`sub_id = 1` collapses renames / country changes into one person (true wca_id changes — vanishingly rare — would still split)."],related:[{id:`most_finals`,titleZh:`进入决赛最多`,titleEn:`Most finals`,hintZh:`类似时间纵深的累积量,但不限"赢"`,hintEn:`Career-depth tally, no win requirement`},{id:`most_4th_places`,titleZh:`最多第四名`,titleEn:`Most 4th places`,hintZh:`另一种"差一点"的画像`,hintEn:`A different shade of near-misses`},{id:`most_competitions_before_winning`,toStat:!0,titleZh:`直接打开排名`,titleEn:`Jump to the leaderboard`,hintZh:`看每个项目"等最久"的 10 人`,hintEn:`10 longest waits per event`}]},most_finals:{id:`most_finals`,titleZh:`进入决赛最多 — 长青指标`,titleEn:`Most finals`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:[`一个选手生涯里进过多少次决赛?项目不分,名次不限 —— 只要决赛轮里有他的成绩,就算一次。`,`它是"出勤稳定 + 排名靠前(才能晋级到决赛)"的复合指标。长期高水平选手很容易就到几百,而 single-event 玩家会受限于只跑一个项目。`],introEn:[`How many finals has a cuber appeared in over their career? Any event, any placing — if their name is in a finals round, it counts.`,`A compound proxy for "competes a lot" + "ranks high enough to advance." Long-term top cubers easily clear several hundred; single-event specialists are capped by the events they enter.`],stats:[{value:`final = 1`,labelZh:`过滤条件`,labelEn:`Filter`,hintZh:`round_types.final 标志`,hintEn:`round_types.final flag`},{value:`COUNT(*)`,labelZh:`聚合`,labelEn:`Aggregate`,hintZh:`每行 = 一次决赛轮成绩`,hintEn:`Each row = one finals appearance`},{value:`Top 100`,labelZh:`保留`,labelEn:`Cutoff`,hintZh:`LIMIT 100`,hintEn:`LIMIT 100`},{value:`all events`,labelZh:`项目混合`,labelEn:`All events`,hintZh:`不分单 / 平均 / 项目`,hintEn:`Pooled across single / average / event`}],sourceZh:["内层在 `results` 上按 `person_id` 分组,join `round_types` 只保 `final = 1`,`COUNT(*)` = 决赛出场总数。`ORDER BY ... DESC LIMIT 100` 截前 100。","外层 join `persons` 加 markdown 链接。"],sourceEn:["Inner: group `results` by `person_id`, join `round_types` for `final = 1`, `COUNT(*)` is the total finals appearances. `ORDER BY ... DESC LIMIT 100`.","Outer: join `persons` for markdown links."],sourceCode:{lang:`sql`,body:`SELECT finals_count, person.name
FROM (
  SELECT person_id AS wca_id,
         COUNT(*) AS finals_count
  FROM results
  JOIN round_types round_type
    ON round_type.id = round_type_id
  WHERE round_type.final = 1
  GROUP BY person_id
  ORDER BY finals_count DESC
  LIMIT 100
) AS people_with_finals
JOIN persons person ON person.wca_id = people_with_finals.wca_id
                   AND person.sub_id = 1
ORDER BY finals_count DESC;`},steps:[{titleZh:`锁定决赛行`,titleEn:`Restrict to finals rows`,bodyZh:"join `round_types`,只保 `final = 1`。WCA 把综合决赛(c)、决赛(f)、以及单轮比赛唯一的 round 都打了 final 标。",bodyEn:"Join `round_types`, keep `final = 1`. WCA tags combined finals (c), finals (f), and the only round of single-round comps all as final."},{titleZh:`按选手计数`,titleEn:`Count per person`,bodyZh:"`GROUP BY person_id` + `COUNT(*)`。同一场比赛同一项目只贡献 1 行 result(决赛只跑一次),所以不会重复。",bodyEn:"`GROUP BY person_id` + `COUNT(*)`. A given comp × event only produces one finals row, so no double-count."},{titleZh:`降序截 100`,titleEn:`Sort desc, take 100`,bodyZh:"`ORDER BY finals_count DESC LIMIT 100`,渲染体面值。",bodyEn:"`ORDER BY finals_count DESC LIMIT 100` for a readable leaderboard."},{titleZh:`拼接展示`,titleEn:`Decorate`,bodyZh:"外层 join `persons` 拼出 markdown 链接给前端;最终一次 `ORDER BY` 保稳定。",bodyEn:"Outer join attaches markdown person links; a final `ORDER BY` keeps render order deterministic.",highlight:!0}],edgesZh:['不过滤 `best > 0` —— DNF 决赛也算一次出场。因为这条 stat 数的是"进决赛的事件",不是"赢决赛"。',`所有项目混合 —— 跑 17 个项目的 generalist 会自然占便宜;single-event 选手再强也无法靠一个项目挤进前 100。`,`LIMIT 100 是渲染体面值,长尾不可见。`,"`sub_id = 1` 合并改名 / 换籍生成的副 person 行。"],edgesEn:['No `best > 0` filter — a DNF\'d final still counts. This stat measures "showed up to a final", not "won".',`All events pooled — generalists who enter 17 events have a structural advantage over single-event specialists.`,`LIMIT 100 is a display cap; long tail invisible.`,"`sub_id = 1` collapses rename / country-change alt rows."],related:[{id:`most_4th_places`,titleZh:`最多第四名`,titleEn:`Most 4th places`,hintZh:`决赛细分:专数第 4 名`,hintEn:`Subset of finals — just 4th places`},{id:`most_podiums_at_single_competition`,titleZh:`单场比赛登台最多`,titleEn:`Most podiums at single comp`,hintZh:`同主题但单比赛粒度`,hintEn:`Same theme but per-comp granularity`},{id:`most_competitions_before_winning`,titleZh:`首次获胜前参加最多比赛`,titleEn:`Most comps before winning`,hintZh:`时间纵深的另一种切法`,hintEn:`A different depth-of-career cut`},{id:`most_finals`,toStat:!0,titleZh:`直接打开决赛榜`,titleEn:`Jump to the finals leaderboard`,hintZh:`看 top 100 选手`,hintEn:`Live top 100`}]},most_podiums_at_single_competition:{id:`most_podiums_at_single_competition`,titleZh:`单场比赛登台最多 — 一站收割`,titleEn:`Most podiums at a single competition`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:[`一个选手在**同一场比赛**里同时登上多少个项目的领奖台(1 / 2 / 3 名)?这条 stat 找的就是这种"一站收割"事件。`,`门槛设在 ≥ 10 个领奖台,所以名单基本上是"在 15 + 项目大赛上对全部能跑的项目都拿到了奖牌"的传奇时刻。`],introEn:[`How many podium finishes (1st / 2nd / 3rd) did one person collect at a single comp? This stat enumerates those one-comp harvests.`,`Threshold ≥ 10 podiums, so the list is essentially legendary moments where someone medalled in basically every event of a 15 + event regional.`],stats:[{value:`pos ∈ {1, 2, 3}`,labelZh:`过滤条件`,labelEn:`Filter`,hintZh:`只数领奖台`,hintEn:`Podium positions only`},{value:`≥ 10`,labelZh:`入选门槛`,labelEn:`Threshold`,hintZh:`HAVING podiums_count >= 10`,hintEn:`HAVING podiums_count >= 10`},{value:`决赛`,labelZh:`轮次范围`,labelEn:`Round scope`,hintZh:`只看 round_type c / f`,hintEn:`Finals only (c, f)`},{value:`per (人 × 赛)`,labelZh:`聚合粒度`,labelEn:`Granularity`,hintZh:`GROUP BY person × competition`,hintEn:`GROUP BY person × competition`}],sourceZh:["内层在 `results` 上按 `(person_id, competition_id)` 分组,过滤 `round_type_id IN ('f', 'c') AND best > 0 AND pos IN (1, 2, 3)`,`COUNT(*)` 得到该选手在该场的领奖台总数。","`HAVING podiums_count >= 10` 砍掉门槛以下;外层 join `persons` / `competitions` 拼链接。比赛链接指向 `/results/podiums` 子页,直接给读者看现场。"],sourceEn:["Inner: group `results` by `(person_id, competition_id)`, filter `round_type_id IN ('f', 'c') AND best > 0 AND pos IN (1, 2, 3)`, `COUNT(*)` is podiums at that comp.","`HAVING podiums_count >= 10` drops below-threshold pairs; outer joins `persons` / `competitions` for markdown. The comp link points at `/results/podiums` so readers can verify in situ."],sourceCode:{lang:`sql`,body:`SELECT podiums_count, person.name, competition.cell_name
FROM (
  SELECT COUNT(*)       AS podiums_count,
         person_id, competition_id
  FROM results
  WHERE round_type_id IN ('f', 'c')
    AND best > 0
    AND pos IN (1, 2, 3)
  GROUP BY person_id, competition_id
  HAVING podiums_count >= 10
  ORDER BY podiums_count DESC
) AS counts
JOIN persons person ON person.wca_id = person_id AND sub_id = 1
JOIN competitions competition ON competition.id = competition_id;`},steps:[{titleZh:`按 (人, 比赛) 分组`,titleEn:`Group by (person, comp)`,bodyZh:`不同比赛的领奖台**不合并**;一个人在 weekend × 2 各拿 5 块 ≠ 一次 10 块。`,bodyEn:`Podiums from different comps are **not pooled**; 5 + 5 across two weekends ≠ 10 in one comp.`},{titleZh:`过滤领奖台 + 排除 DNF`,titleEn:`Filter podium + drop DNFs`,bodyZh:"`pos IN (1, 2, 3) AND best > 0`。`pos` 是 WCA 官方名次,平局并列规则已包含,所以并列第 3 仍然算第 3。",bodyEn:"`pos IN (1, 2, 3) AND best > 0`. `pos` is WCA's official placement (ties baked in — co-3rd is still 3rd)."},{titleZh:`只看决赛`,titleEn:`Finals only`,bodyZh:`\`round_type_id IN ('f', 'c')\`(决赛 / 综合决赛)。预赛 / 半决"局部前 3"不计入领奖台。`,bodyEn:"`round_type_id IN ('f', 'c')` (final / combined final). Early-round top-3s don't count as podiums."},{titleZh:`门槛 ≥ 10`,titleEn:`Threshold of 10`,bodyZh:'`HAVING podiums_count >= 10` —— 把日常奖牌不当回事,只看真正"一站收割"。',bodyEn:"`HAVING podiums_count >= 10` — filters away routine medal counts, keeping only true harvests."},{titleZh:`拼接展示并按数量倒序`,titleEn:`Decorate, sort desc`,bodyZh:"外层 join 拼 markdown,比赛链接直指 `/results/podiums` 子页给读者验证;`ORDER BY podiums_count DESC`。",bodyEn:"Outer joins build markdown; comp link goes to the comp's `/results/podiums` subpage for verification. `ORDER BY podiums_count DESC`.",highlight:!0}],edgesZh:['`best > 0` 排除 DNF —— pos 仍能是 1 / 2 / 3 但成绩是 DNF 的"虚拟领奖"会被滤掉(`SolveTime` value = -1 表示 DNF)。',`只看决赛,不看预赛 —— 例如 WC 那种多轮比赛只算最后一轮的名次。`,`不分项目地累计 —— 一个人在 333 拿 3 + 444 拿 3 + ... 可以快速堆到 ≥ 10。`,'`HAVING podiums_count >= 10` 是硬截断,9 块奖牌的"差一点"不可见。'],edgesEn:["`best > 0` excludes DNF podiums — pos can be 1 / 2 / 3 with a DNF result; those are filtered (`SolveTime` -1 = DNF).",`Finals only — multi-round comps like Worlds count only the final round's placing.`,`All events pooled into the count — a 3 on 3x3 + 3 on 4x4 + ... can reach 10 fast.`,"`HAVING podiums_count >= 10` is hard — a 9-podium near miss is invisible."],related:[{id:`complete_competition_winners`,titleZh:`完全比赛冠军`,titleEn:`Complete competition winners`,hintZh:`更严格的版本:不是"登台",而是"全包金牌"`,hintEn:`Stricter sibling — every gold, not just any podium`},{id:`most_finals`,titleZh:`进入决赛最多`,titleEn:`Most finals`,hintZh:`生涯级累积口径`,hintEn:`Career-level finals tally`},{id:`most_podiums_together`,titleZh:`最多共同登台次数`,titleEn:`Most podiums together`,hintZh:`对子 / 三人组合视角`,hintEn:`Pairs / triples view`},{id:`most_podiums_at_single_competition`,toStat:!0,titleZh:`直接打开榜单`,titleEn:`Jump to the leaderboard`,hintZh:`看每一场被"一站收割"的比赛`,hintEn:`Every one-comp harvest in history`}]},most_podiums_together:{id:`most_podiums_together`,titleZh:`最多共同登台次数 — 对子与三人组`,titleEn:`Most podiums together`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:[`同一场比赛、同一个项目的领奖台上,A 和 B 一起站过几次?——三人组同理。这条 stat 找出最常"组团登台"的对子和三人组。`,`它捕捉的是地区高手圈的稳定共生关系:同一片大陆 / 同一国的顶尖选手会在多场比赛里反复同台。结果分 Pairs(对子)和 Triples(三人组)两组。`],introEn:[`How often have A and B (and C) appeared on the same podium of the same event at the same comp? This stat enumerates the most frequent podium pairs and triples.`,`It surfaces the stable co-stardom of regional top circles: same continent's top cubers keep ending up on the same stage. Output split into Pairs and Triples.`],stats:[{value:`pos ≤ 3`,labelZh:`入选条件`,labelEn:`Filter`,hintZh:`领奖台名次`,hintEn:`Podium positions`},{value:`决赛`,labelZh:`轮次范围`,labelEn:`Round scope`,hintZh:`round_types.final = 1`,hintEn:`round_types.final = 1`},{value:`2 / 3`,labelZh:`组合大小`,labelEn:`Combo size`,hintZh:`从领奖台 3 人里取 C(3, k)`,hintEn:`C(podium people, k) for k in {2, 3}`},{value:`Top 100`,labelZh:`每分组保留`,labelEn:`Per bucket`,hintZh:`Pairs 100 + Triples 100`,hintEn:`Pairs 100 + Triples 100`}],sourceZh:["从 `results` join `persons` / `round_types`,过滤决赛 + pos ≤ 3 + `best > 0`;按 `(event_id, competition_id)` 分组,用 `GROUP_CONCAT(person.name ORDER BY person.name)` 把这场比赛该项目的领奖台名单拼成字符串。",'TS 端解析为人名数组,对每个"领奖台名单"用组合数学 `C(arr, k)` 生成所有 2 元 / 3 元子集,在 Map 里累加频次,最后按频次降序取 top 100。'],sourceEn:["SQL joins `results` to `persons` / `round_types`, filters finals + pos ≤ 3 + `best > 0`, groups by `(event_id, competition_id)`, and `GROUP_CONCAT(person.name ORDER BY person.name)` collapses that event's podium roster into a single string.","TS side splits into a name array, generates every k-combo via `combinations(arr, k)` for k ∈ {2, 3}, tallies into a Map, sorts desc, takes top 100 per bucket."],sourceCode:{lang:`sql`,body:`SELECT GROUP_CONCAT(
         person.name ORDER BY person.name
       ) AS people
FROM results
JOIN persons person ON person.wca_id = person_id
                   AND person.sub_id = 1
JOIN round_types round_type
  ON round_type.id = round_type_id
WHERE round_type.final = 1
  AND pos <= 3
  AND best > 0
GROUP BY event_id, competition_id;`},steps:[{titleZh:`收集每个领奖台`,titleEn:`Collect each podium`,bodyZh:"`GROUP BY event_id, competition_id` —— 每一行 = 该比赛该项目的领奖台名单(按字母排序,后面好做 key)。",bodyEn:"`GROUP BY event_id, competition_id` — each row = the podium roster of one (comp × event), names alphabetized so combos give canonical keys."},{titleZh:`拼名字成字符串`,titleEn:`Concatenate names`,bodyZh:"`GROUP_CONCAT` 拼成 `Alice,Bob,Carol`;TS 端 `.split(',')` 还原。",bodyEn:"`GROUP_CONCAT` glues into `Alice,Bob,Carol`; TS does `.split(',')` to rebuild the array."},{titleZh:`取 k 元组合`,titleEn:`Generate k-combinations`,bodyZh:"`combinations(arr, k)` 递归取出 `C(n, k)` 个子集;n 通常是 3 (三人领奖台),所以 k = 2 得 3 对,k = 3 得 1 组。并列时 n 可大于 3。",bodyEn:"`combinations(arr, k)` recursively enumerates `C(n, k)`; n is usually 3 (three medallists) → k = 2 gives 3 pairs, k = 3 gives 1 triple. Ties can push n > 3."},{titleZh:`累加频次`,titleEn:`Tally frequencies`,bodyZh:"Map<key, count>,key = 名字 join `&`(因为已字母排序,组合 key 唯一)。",bodyEn:"Map<key, count>, key = names joined by `&` (alphabetical sort upstream makes the key canonical)."},{titleZh:`降序取 top 100 输出两个桶`,titleEn:`Sort desc, take top 100 per bucket`,bodyZh:`分 Pairs(k = 2)和 Triples(k = 3)两组,各自 sort + slice(0, 100)。`,bodyEn:`Two buckets — Pairs (k = 2) and Triples (k = 3) — each sort + slice(0, 100).`,highlight:!0}],formulae:[{labelZh:`每场领奖台贡献的组合数`,labelEn:`Combinations per podium`,expr:`pairs_per_podium = C(n, 2),  triples_per_podium = C(n, 3)`,bodyZh:`n = 该领奖台人数,通常 3(因平局可大于 3)。所以一个 3 人领奖台贡献 3 对 + 1 三人组。`,bodyEn:`n = people on that podium, usually 3 (ties can push higher). A standard 3-person podium contributes 3 pairs and 1 triple.`}],edgesZh:['只看决赛 + `pos <= 3` + `best > 0` —— DNF 不算"登台",虚拟同台被排除。',"`sub_id = 1` 让改名 / 换籍后的副 person 行不污染。一个换过国籍的选手仍按同一个人累计。",`平局并列第 3 时 podium 可能有 4 人,所有 C(4, 2) = 6 对都计入 —— 这就是为啥某些"低水平区域大赛"对子频次格外高。`,`GROUP_CONCAT 默认有长度上限(MySQL 1024 字节);WCA 单领奖台通常 3 - 5 人,远不超,实际安全。`],edgesEn:["Finals only + `pos <= 3` + `best > 0` — DNFs don't count, no ghost co-podiums.","`sub_id = 1` collapses renames / country changes — a person who switched country still accumulates as one identity.",`Tied 3rds can make a podium hold 4 people; all C(4, 2) = 6 pairs are counted — explains why low-depth regions sometimes show abnormally tight pairs.`,`GROUP_CONCAT has a default 1024-byte cap in MySQL; podiums of 3 - 5 names stay well under, so this is safe in practice.`],related:[{id:`most_podiums_at_single_competition`,titleZh:`单场比赛登台最多`,titleEn:`Most podiums at single comp`,hintZh:`同主题但单人 × 单赛粒度`,hintEn:`Same theme — per-person, per-comp granularity`},{id:`complete_competition_winners`,titleZh:`完全比赛冠军`,titleEn:`Complete competition winners`,hintZh:`一个人包圆一场比赛的所有金牌`,hintEn:`Sibling — one person sweeping gold in a comp`},{id:`most_podiums_together`,toStat:!0,titleZh:`直接打开 pairs / triples`,titleEn:`Jump to pairs / triples`,hintZh:`看共同登台最多的对子和三人组`,hintEn:`Live top-100 of co-podium pairs and triples`}]},worst_result_on_podium:{id:`worst_result_on_podium`,titleZh:`领奖台最差成绩 — 也能拿奖`,titleEn:`Worst result providing a podium`,badgeZh:`荣誉`,badgeEn:`Honors`,introZh:['这条 stat 是 `best_result_off_podium` 的镜像:在决赛里拿到 1 / 2 / 3 名,但成绩**特别慢**的 top 10。读它能看到"奖牌门槛的地板"。',`出现的情况一般是:小比赛参赛人少、难项目(444bf / 555bf / 333mbld / 333fmc)成绩差距大、或者全场翻车只剩你成绩成形。`],introEn:["Mirror of `best_result_off_podium`: 1st / 2nd / 3rd in a final but with the **slowest** result. Reading this gives the soft floor of the podium.",`Typical setups: small comps with few entrants, hard events (444bf / 555bf / multi-blind / FMC) where spread is huge, or a comp-wide meltdown where you're the only one who finished.`],stats:[{value:`17`,labelZh:`覆盖项目`,labelEn:`Events`,hintZh:`所有 WCA 项目`,hintEn:`All WCA events`},{value:`pos ≤ 3`,labelZh:`过滤条件`,labelEn:`Filter`,hintZh:`只看 1 / 2 / 3 名`,hintEn:`Only 1st / 2nd / 3rd`},{value:`排除 DNF`,labelZh:`主成绩有效`,labelEn:`Primary valid`,hintZh:`isComplete() 过滤`,hintEn:`isComplete() filter`},{value:`Top 10`,labelZh:`每项目保留`,labelEn:`Per event`,hintZh:`正序后 reverse 取 top 10`,hintEn:`Sort asc, reverse, slice 10`}],sourceZh:["从 `results` 筛 `round_type_id IN ('c', 'f') AND pos <= 3`,join `persons` / `competitions` / `preferred_formats` / `formats`。后两个用来读 `sort_by` / `sort_by_second`,决定该项目按 single 还是 average 排。","比赛链接指向 `/results/podiums#eXXX` 子页,直接跳到该项目的领奖台板块。"],sourceEn:["From `results` filter `round_type_id IN ('c', 'f') AND pos <= 3`. Join `persons` / `competitions` / `preferred_formats` / `formats` — the last two yield `sort_by` / `sort_by_second` for primary / secondary metric per event.","Comp link points at `/results/podiums#eXXX` to jump straight to that event's podium section."],sourceCode:{lang:`sql`,body:`SELECT format.sort_by, format.sort_by_second,
       results.event_id,
       best AS single, average,
       person.name, competition.cell_name,
       pos AS place
FROM results
JOIN persons person ON person.wca_id = person_id
                   AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN preferred_formats pf ON pf.event_id = results.event_id
                         AND pf.ranking = 1
JOIN formats format ON format.id = pf.format_id
WHERE round_type_id IN ('c', 'f') AND pos <= 3;`},steps:[{titleZh:`按项目分组`,titleEn:`Group by event`,bodyZh:"`EVENTS_ENTRIES.map` 遍历 17 项,每项独立排榜(主成绩字段不同,不能混)。",bodyEn:"`EVENTS_ENTRIES.map` iterates all 17 events. Each event ranks independently because the primary metric differs."},{titleZh:`解析 SolveTime + 过滤无效`,titleEn:`Build SolveTime + filter incomplete`,bodyZh:'每行包成 `{ single, average, sort_by, sort_by_second, ... }`;过滤 `primary.isComplete()` —— 主成绩为 DNF / DNS 的剔除,避免"DNF 也算最差"的伪记录。',bodyEn:'Wrap each row as `{ single, average, sort_by, sort_by_second, ... }`. Drop rows where `primary.isComplete()` is false — keeps DNF / DNS primaries out of the "worst" tally.'},{titleZh:`正序排 + 反转`,titleEn:`Sort ascending, reverse`,bodyZh:'按 `SolveTime.compareTo` 主升,次升,然后 `.reverse()` 把数组首尾翻过来 —— 现在数组头就是"最慢仍登台"。',bodyEn:"Sort primary asc then secondary asc via `SolveTime.compareTo`, then `.reverse()` — the array head is now the slowest podium time."},{titleZh:`取 top 10 + 主成绩加粗`,titleEn:`Slice 10, bold primary`,bodyZh:"`slice(0, 10)`;输出 `[选手, single, average, 比赛, 名次]`,主成绩字段渲染时加 `**` 标记。",bodyEn:"`slice(0, 10)`; output `[person, single, average, competition, place]` with the primary metric wrapped in `**` for bolding.",highlight:!0}],edgesZh:[`排除主成绩 DNF / DNS —— 否则一堆"DNF 拿奖"会占满表(全场翻车时 pos 仍是 1)。次成绩可以是 DNF。`,"`pos <= 3` 用的是 WCA 官方名次,平局并列已包含 —— 并列第 3 也能进表。","`sub_id = 1` 滤副 person 行。",`按项目分组所以"333 拿亚军 30 秒"和"333mbld 拿亚军 99/99"不可比 —— 看的是每个项目自己的地板。`],edgesEn:[`Primary DNF / DNS dropped — otherwise the table fills with "DNF gold" rows from all-meltdown finals where pos still goes 1 / 2 / 3. Secondary may be DNF.`,"`pos <= 3` is WCA's official placement; ties are baked in (a co-3rd appears).","`sub_id = 1` filters alternate person rows.",`Per-event grouping means "30s 3x3 silver" vs "99/99 multi-blind silver" never share a row — read each event's own floor.`],related:[{id:`best_result_off_podium`,titleZh:`最佳非领奖台成绩`,titleEn:`Best result off podium`,hintZh:`镜像 stat —— 没进领奖台的最快成绩`,hintEn:`Mirror stat — fastest results that missed the podium`},{id:`most_podiums_at_single_competition`,titleZh:`单场比赛登台最多`,titleEn:`Most podiums at single comp`,hintZh:`同口径下的"登台"另一切片`,hintEn:`Another slice of the podium concept`},{id:`worst_result_on_podium`,toStat:!0,titleZh:`直接打开排名`,titleEn:`Jump to the leaderboard`,hintZh:`看每个项目"奖牌地板"top 10`,hintEn:`Live per-event top 10 of slowest podiums`}]}},u={competitions_per_year_by_person:{id:`competitions_per_year_by_person`,titleZh:`每年每人比赛数`,titleEn:`Competitions per year by person`,badgeZh:`选手`,badgeEn:`Person`,introZh:[`把每个选手在 WCA 的"职业年限"摊平,再算他们参加比赛的**年均频率**。一个 6 年里打了 90 场的选手 = 15 场/年。`,`只看绝对数会让本地大佬被全球巡飞党碾压;这条指标把"短期狂飙"和"长期高频"区分开 —— 想上榜既要场次,也要跑得久。`],introEn:[`Flattens each cuber's WCA career length and computes the **average competitions per year**. Six years with 90 comps = 15 / year.`,`Absolute counts favour decade-long travellers; this metric separates "short burst" from "sustained high frequency" — you need both volume and longevity to chart.`],stats:[{value:`≥ 1 yr`,labelZh:`入榜门槛`,labelEn:`Eligibility`,hintZh:`从首场到今天满 1 年`,hintEn:`At least 1 year since first comp`},{value:`Top 100`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`按年均场次降序`,hintEn:`Sorted by comps/year desc`},{value:`CURDATE()`,labelZh:`年限基准`,labelEn:`Career end`,hintZh:`dump 的当天,不是末场比赛日`,hintEn:`Dump's today, not last comp date`}],sourceZh:["`results` 表 `DISTINCT competition_id`(同场多项目只算一场);`competitions.start_date` 取最早值;年限 = `(CURDATE() - MIN(start_date)) / 365.25`。"],sourceEn:["`results` with `DISTINCT competition_id` (one comp counts once regardless of events). Earliest `competitions.start_date` per person. Years = `(CURDATE() - MIN(start_date)) / 365.25`."],sourceCode:{lang:`sql`,body:`SELECT
  COUNT(DISTINCT competition_id) competitions,
  (DATEDIFF(CURDATE(), MIN(start_date)) / 365.25) years,
  person_id
FROM results
JOIN competitions ON competitions.id = competition_id
GROUP BY person_id
HAVING years >= 1
ORDER BY competitions / years DESC
LIMIT 100`},steps:[{titleZh:`按选手聚合`,titleEn:`Aggregate per person`,bodyZh:"一行 `results` = 一个项目轮次;先 `COUNT(DISTINCT competition_id)` 把多项目同场塌缩成 1。",bodyEn:"Each `results` row is one event-round; `COUNT(DISTINCT competition_id)` collapses multi-event same-comp entries to 1."},{titleZh:`量职业年限`,titleEn:`Measure career span`,bodyZh:"`MIN(start_date)` = 首场比赛 → 与 `CURDATE()` 求差,换算 365.25 天/年(含闰年补偿)。",bodyEn:"`MIN(start_date)` = first comp → difference with `CURDATE()` divided by 365.25 (leap-year averaged)."},{titleZh:`过滤新人`,titleEn:`Filter newcomers`,bodyZh:'`HAVING years >= 1` 把"刚出道 3 个月跑了 8 场"的爆发型剔掉 —— 否则分母太小,频率虚高。',bodyEn:'`HAVING years >= 1` removes "8 comps in first 3 months" outliers — small denominator would inflate the rate.'},{titleZh:`降序取 100`,titleEn:`Sort, take top 100`,bodyZh:"按 `competitions / years` 降序,LIMIT 100。结果保留两位小数,便于平局区分。",bodyEn:"Order by `competitions / years` desc, LIMIT 100. Two decimals retained to break ties.",highlight:!0}],formulae:[{labelZh:`公式`,labelEn:`Formula`,expr:`rate(p) = comps(p) / ((today − firstCompDate(p)) / 365.25)`,bodyZh:"`comps` 是去重场次,`firstCompDate` 是该选手最早一场的 `start_date`。",bodyEn:"`comps` is distinct count, `firstCompDate` is earliest `start_date` for the person."}],edgesZh:[`分母用 dump 的当天,不是"末场到首场";一个 2008 出道、2012 后退役的选手仍按 18 年计算,频率会被稀释。`,"同一周末跨地两场算两场 —— `competition_id` 不同即不同场。","副身份(改名 / 换国籍,`sub_id > 1`)不参与 — 只取主行。"],edgesEn:[`Denominator uses today, not last-comp-date — a 2008 retiree from 2012 is still divided by 18 years, diluting the rate.`,"Two cross-region comps in one weekend count separately — different `competition_id` = different comp.","Alt identities (rename / country change, `sub_id > 1`) excluded — only the primary `persons` row."],related:[{id:`shortest_time_to_reach_milestone_in_comps_count`,titleZh:`最快达里程碑场次`,titleEn:`Fastest to N comps`,hintZh:`短窗口爆发量化 —— 跟年均频率互补`,hintEn:`Short-window bursts — complement to the annual rate`},{id:`most_attended_competitions_in_single_month`,titleZh:`单月最多场次`,titleEn:`Most in a month`,hintZh:`更极端的"窗口"指标`,hintEn:`Even tighter window`},{id:`most_distinct_dates_competed_on`,titleZh:`不同参赛日期`,titleEn:`Distinct dates`,hintZh:`另一种"出勤量"口径`,hintEn:`Alternative attendance metric`},{id:`competitions_per_year_by_person`,toStat:!0,titleZh:`查看实时数据`,titleEn:`Jump to live data`,hintZh:`当前榜单 + 项目筛选`,hintEn:`Current leaderboard + filters`}]},longest_competitions_path:{id:`longest_competitions_path`,titleZh:`最长比赛路径`,titleEn:`Longest competitions path`,badgeZh:`选手`,badgeEn:`Person`,introZh:[`把每位选手按时间顺序参加的比赛连成一条折线,**地球大圆距离**逐段求和。决定排名的不是飞行航线,而是赛事之间的直线距离 —— 一个跨大陆来回比环球一圈短得多。`,`榜首通常是常年欧美亚穿梭的「赛季巡游党」,一年能堆出几十万公里。`],introEn:[`Connect each cuber's comps in chronological order, then sum **great-circle distances** segment by segment. Not flight paths — straight-line geodesics between venues.`,`The top spots usually go to year-round Eurasia / Americas commuters who rack up several hundred thousand km a year.`],stats:[{value:`Haversine`,labelZh:`距离公式`,labelEn:`Distance formula`,hintZh:`球面大圆,R = 6371 km`,hintEn:`Spherical great-circle, R = 6371 km`},{value:`1000`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`top 1000 选手`,hintEn:`Top 1000 cubers`},{value:`lat/lon ÷ 10⁶`,labelZh:`坐标精度`,labelEn:`Coord precision`,hintZh:`WCA dump 存的是 micro-degree`,hintEn:`WCA dump stores micro-degrees`}],sourceZh:["`results` 取 `DISTINCT (person_id, competition_id)` —— 同场多项目算一次;join `competitions` 拿场馆经纬度,按 `start_date, end_date` 升序排成时间线。坐标在 dump 里是 micro-degree(整数 × 10⁶),要除回。","排除大陆 FMC 比赛的虚拟国家 (`XA, XE, XF, XM, XN, XO, XS, XW`),它们的经纬度不可靠。"],sourceEn:["Pull `DISTINCT (person_id, competition_id)` from `results` — multi-event same-comp counts once. Join `competitions` for venue lat/lon, sort by `start_date, end_date`. Coords are stored as micro-degrees (int × 10⁶); divide back.","Continental FMC virtual countries (`XA, XE, XF, XM, XN, XO, XS, XW`) are excluded — their coords aren't real."],sourceCode:{lang:`sql`,body:`SELECT person_link,
       RADIANS(latitude / 1000000) lat,
       RADIANS(longitude / 1000000) lon
FROM (SELECT DISTINCT person_id, competition_id FROM results) pc
JOIN persons ON wca_id = person_id AND sub_id = 1
JOIN competitions ON competitions.id = competition_id
WHERE country_id NOT IN ('XA','XE','XF','XM','XN','XO','XS','XW')
ORDER BY start_date, end_date`},steps:[{titleZh:`按选手分组`,titleEn:`Group by person`,bodyZh:"SQL 拉回所有 (选手, 比赛, 坐标) 行,TS 端按 `person_link` 收成 Map,组内已按时间排好。",bodyEn:"SQL returns all (person, comp, coord) rows; TS buckets by `person_link` into a Map — each group is already time-sorted."},{titleZh:`相邻两点 Haversine`,titleEn:`Haversine on adjacent pairs`,bodyZh:"对每个选手的 N 个坐标,算 N − 1 段。公式:`a = sin²(Δφ/2) + cos φ₁ cos φ₂ sin²(Δλ/2); d = 2R · atan2(√a, √(1−a))`。",bodyEn:"For each cuber's N coords, compute N − 1 segments. `a = sin²(Δφ/2) + cos φ₁ cos φ₂ sin²(Δλ/2); d = 2R · atan2(√a, √(1−a))`."},{titleZh:`段距累加`,titleEn:`Sum the segments`,bodyZh:"不裁剪、不去重 —— 跨海回头跨过同一格也算两次距离。`Math.round` 到整数公里,千位空格分隔。",bodyEn:"No clipping or dedup — flying back over the same square counts twice. `Math.round` to whole km, thin-space thousand separators."},{titleZh:`降序取 1000`,titleEn:`Sort desc, take 1000`,bodyZh:`榜单深 1000 比常规 100 深一档,因为这是地理"积分"指标,有大量长尾爱好者。`,bodyEn:`Top 1000 is deeper than the usual 100 — this is a geographic accumulator with a long tail of casuals.`,highlight:!0}],formulae:[{labelZh:`Haversine 距离`,labelEn:`Haversine distance`,expr:`d = 2R · atan2(√a, √(1−a)),  a = sin²(Δφ/2) + cos φ₁ cos φ₂ sin²(Δλ/2)`,bodyZh:`φ = 纬度弧度,λ = 经度弧度,R = 6371 km。给出大圆(球面最短路径)长度。`,bodyEn:`φ = lat in radians, λ = lon in radians, R = 6371 km. Yields great-circle (spherical shortest path) length.`}],edgesZh:[`相邻两场同一场馆 → 距离 0;同一城市不同场馆 → 几 km,仍计入。`,`球面假设忽略地形/航路;不代表真实飞行里程,但在 WCA 巡游级距离尺度上误差可忽略。`,"跨日多日比赛只在路径里出现一次(`competition_id` 维度去重)。"],edgesEn:[`Two consecutive comps at the same venue → 0 km; same city different venues → a few km, still counted.`,`Spherical assumption ignores terrain / actual flight routes; not real flown distance but negligible error at the scale of WCA touring.`,"Multi-day comps appear once on the path (deduped by `competition_id`)."],related:[{id:`most_visited_countries`,titleZh:`去过最多国家`,titleEn:`Most countries visited`,hintZh:`广度 vs 路径长度 —— 互补口径`,hintEn:`Breadth vs path length — complement`},{id:`most_visited_continents`,titleZh:`去过最多大洲`,titleEn:`Most continents visited`,hintZh:`更粗粒度的广度`,hintEn:`Coarser breadth metric`},{id:`most_competitions_abroad`,titleZh:`海外参赛最多`,titleEn:`Most comps abroad`,hintZh:`另一种"跨国"量化`,hintEn:`Another cross-border measure`},{id:`longest_competitions_path`,toStat:!0,titleZh:`查看实时路径榜`,titleEn:`Jump to live ranking`,hintZh:`查看具体 km 与选手`,hintEn:`See km and cubers`}]},longest_streak_of_competitions_in_own_country:{id:`longest_streak_of_competitions_in_own_country`,titleZh:`在本国最长连续参赛`,titleEn:`Longest streak of comps in own country`,badgeZh:`选手`,badgeEn:`Person`,introZh:[`把一国所有比赛按时间排成队列;一个选手只要**漏掉**其中任何一场,本国连续记录就清零。可以理解成"在自己家门口的全勤率"。`,'只看本国(`competition.country_id = person.country_id`),所以海外参赛的间隔不打断连胜 —— 适合衡量真正"扎根本地圈"的选手。'],introEn:[`Queue every comp in a country by date; the moment a cuber **misses** one, the streak resets. Think "perfect-attendance run in your home country".`,"Limited to in-country comps (`competition.country_id = person.country_id`), so overseas trips do not break it — captures cubers truly anchored to the local scene."],stats:[{value:`本国`,labelZh:`范围`,labelEn:`Scope`,hintZh:`比赛地 = 选手注册国`,hintEn:`Comp country = person's registered country`},{value:`Top 100`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`按连胜降序`,hintEn:`Sorted by streak desc`},{value:`严格全勤`,labelZh:`中断条件`,labelEn:`Break condition`,hintZh:`一场缺席 → 计数清零`,hintEn:`One miss → counter zero`}],sourceZh:["`results` 去重 (`DISTINCT person_id, competition_id`),join 到 `persons`(`sub_id = 1`) 和 `competitions`(取 `cell_name`),用 `competition.country_id = person.country_id` 过滤本国比赛。`countries.name` 取国家名展示。按 `start_date` 升序。"],sourceEn:["`results` deduped on `(person_id, competition_id)`, joined to `persons` (`sub_id = 1`) and `competitions` (with `cell_name`), filtered by `competition.country_id = person.country_id`. Country name from `countries.name`. Ordered by `start_date`."],sourceCode:{lang:`sql`,body:`SELECT person_link, competition_link, country.name country
FROM (SELECT DISTINCT person_id, competition_id FROM results) pc
JOIN persons person ON person.wca_id = person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN countries country ON country.id = competition.country_id
WHERE competition.country_id = person.country_id
ORDER BY competition.start_date`},steps:[{titleZh:`按国家分桶`,titleEn:`Bucket by country`,bodyZh:`每个国家拿到一条按时间排好的比赛序列 + 每场的出席选手名单。`,bodyEn:`Each country gets a time-sorted sequence of comps plus the attendee list per comp.`},{titleZh:`维护两张表`,titleEn:`Maintain two tables`,bodyZh:"`currentByPerson` = 进行中的连胜;`longestByPerson` = 历史最长。每遇新比赛,先把新出现的人 init,然后 check 在桌选手是否到场。",bodyEn:"`currentByPerson` = in-progress streaks; `longestByPerson` = historical best. For each new comp, init new attendees, then check if tracked people showed up."},{titleZh:`到场 +1 / 缺席结算`,titleEn:`Present +1 / absent settle`,bodyZh:"到场 → `current.count += 1`,刷新 longest;缺席 → 把 `current` 写回历史,从 currentByPerson 删除 —— 这一刻连胜被打断。",bodyEn:"Present → `current.count += 1`, refresh longest. Absent → write `current` to history, remove from currentByPerson — that's the moment the streak breaks."},{titleZh:`收集 + 取 100`,titleEn:`Collect + take 100`,bodyZh:`所有国家所有选手的 longest 全收集,降序排,取 top 100。最早 / 中断比赛同时记录。`,bodyEn:`Gather every person's longest across countries, sort desc, take top 100. First and break-comp logged.`,highlight:!0}],edgesZh:[`"漏掉一场"的判定是基于该国所有比赛 —— 你不参加的话,即便是 1000 公里外的小赛也会断 streak。`,"只看主身份(`sub_id = 1`);改国籍的选手要按改后那一段算。","streak 仍在进行中(没断过)也会被收录 —— `lastCompetition` 留空。"],edgesEn:[`"Miss" is judged against every comp in that country — even a small 1000 km away one breaks the streak if you skip.`,"Primary identity only (`sub_id = 1`); after a country change, only the post-change run counts.","Ongoing streaks (never broken) are included — `lastCompetition` left blank."],related:[{id:`longest_streak_of_personal_records`,titleZh:`连续破 PR 比赛`,titleEn:`Streak with PR each comp`,hintZh:`从"全勤"换到"刷成绩"`,hintEn:`Attendance → performance streak`},{id:`longest_streak_of_podiums`,titleZh:`连续登台`,titleEn:`Streak of podiums`,hintZh:`另一种"连胜"`,hintEn:`A different kind of streak`},{id:`most_competitions_abroad`,titleZh:`海外参赛最多`,titleEn:`Most comps abroad`,hintZh:`反向口径 —— 不在本国`,hintEn:`Opposite scope — outside home country`},{id:`longest_streak_of_competitions_in_own_country`,toStat:!0,titleZh:`查看实时连胜榜`,titleEn:`Jump to live streaks`,hintZh:`具体选手 + 起止比赛`,hintEn:`Cubers + start/end comps`}]},longest_streak_of_personal_records:{id:`longest_streak_of_personal_records`,titleZh:`最长连续个人纪录参赛记录`,titleEn:`Longest streak of comps with a PR`,badgeZh:`选手`,badgeEn:`Person`,introZh:[`一个选手按时间参加比赛,**只要某场比赛刷了至少一个 PR**(任意项目的 single 或 average),streak 就 +1;一场没刷,清零。`,`它衡量的是"持续进步"的硬度 —— 上榜不需要某一项目特别强,但你得在每场比赛里都从某个项目里榨出新 PR。`],introEn:[`Walk a cuber's comps in order — **as long as a comp produces at least one PR** (any event's single or average), the streak ticks up; one comp without a PR resets it.`,`A "sustained improvement" metric — you don't need to be top in any event, but you need to extract a fresh PR from somewhere at every comp.`],stats:[{value:`single + avg`,labelZh:`PR 类型`,labelEn:`PR types`,hintZh:`两者任一刷新即 +1`,hintEn:`Either resets +1`},{value:`Top 100`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`按连胜降序`,hintEn:`Sorted by streak desc`},{value:`≤ 历史最佳`,labelZh:`刷 PR 判定`,labelEn:`PR check`,hintZh:"`val <= pb` 平 PR 也算",hintEn:"`val <= pb` — ties count"}],sourceZh:['`results` 全表 join `persons` / `competitions` / `round_types`,按 `start_date, round_type.rank` 升序。每行带 `event_id`、`best`(single)、`average`。无论几轮,只要轮内的 best/average 刷新该项目 PB,这一比赛即算"有 PR"。'],sourceEn:['Full `results` joined to `persons` / `competitions` / `round_types`, sorted by `start_date, round_type.rank`. Each row has `event_id`, `best` (single), `average`. Any round breaking the per-event PB counts the whole comp as "had a PR".'],sourceCode:{lang:`sql`,body:`SELECT person_link, competition_link,
       event_id, best single, average
FROM results
JOIN persons person ON person.wca_id = person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN round_types round_type ON round_type.id = round_type_id
ORDER BY competition.start_date, round_type.rank`},steps:[{titleZh:`按选手 → 比赛 嵌套分组`,titleEn:`Bucket by person → comp`,bodyZh:'外层按 `person_link` 分,内层把同场所有项目轮次收拢为一个"比赛快照"。',bodyEn:'Outer bucket: `person_link`. Inner bucket: gather all event-rounds at one comp into a single "comp snapshot".'},{titleZh:`维护 per-event PB`,titleEn:`Track per-event PB`,bodyZh:"`pbsByEvent` 存每个项目的 single PB 和 average PB(都从 Infinity 起);遍历比赛快照内所有结果。",bodyEn:"`pbsByEvent` holds single-PB and average-PB per event (both start at Infinity); iterate every result in the snapshot."},{titleZh:`判定"有 PR 的比赛"`,titleEn:`Decide "comp had a PR"`,bodyZh:"任一项目任一类型 `val > 0 && val <= pb` 即触发刷新 + 标 `competitionWithPb = true`。注意 `<=`,平 PR 也算。",bodyEn:"Any event × type where `val > 0 && val <= pb` fires the refresh and flips `competitionWithPb`. `<=` means ties count."},{titleZh:`streak 推进 / 重置`,titleEn:`Advance / reset`,bodyZh:"有 PR → `count += 1`,刷新 longest;无 PR → 关闭 current,下一比赛重新开 streak。",bodyEn:"PR found → `count += 1`, refresh longest; no PR → close current, open fresh on next comp.",highlight:!0}],edgesZh:['平 PR 算,因为 `<=`;严格意义上"刷新"的口径要 `<` —— 这里宽松。',`只要该比赛**任一项目任一指标** PR,整场算 PR 比赛;打 6 项 PR 跟打 1 项 PR 计数相同。`,"`best = -1` / `0`(DNF / 未提交)不会触发 PR,因为 `val > 0` 守门。"],edgesEn:["Ties count (we use `<=`); strict-improvement would need `<` — looser here.",`**Any event × any type** PR makes the whole comp a "PR comp"; 6-event PRs and 1-event PRs score the same.`,"`best = -1` / `0` (DNF / not attempted) cannot trigger a PR — `val > 0` gate."],related:[{id:`longest_streak_of_podiums`,titleZh:`连续登台`,titleEn:`Streak of podiums`,hintZh:`相同形态,换成"领奖台"判定`,hintEn:`Same shape, swap PR for podium`},{id:`longest_streak_of_competitions_in_own_country`,titleZh:`本国连续参赛`,titleEn:`Streak in own country`,hintZh:`"全勤"型 streak`,hintEn:`Attendance-style streak`},{id:`longest_time_to_sub_10`,titleZh:`最长 sub-10 时长`,titleEn:`Longest time to sub-10`,hintZh:`另一面 —— "进步慢"的故事`,hintEn:`Flip side — slow-improvement story`},{id:`longest_streak_of_personal_records`,toStat:!0,titleZh:`查看实时连胜榜`,titleEn:`Jump to live streaks`,hintZh:`具体选手 + 起止比赛`,hintEn:`Cubers + start/end comps`}]},longest_streak_of_podiums:{id:`longest_streak_of_podiums`,titleZh:`最长连续登台记录`,titleEn:`Longest streak of podiums`,badgeZh:`选手`,badgeEn:`Person`,introZh:[`锁定**单个项目**:从某场比赛起,只要决赛拿前 3 就 +1;某场决赛掉出前 3,streak 结束。计数粒度是 (选手, 项目) —— 一个选手能同时在 3x3 和 Square-1 上各自维持 streak。`,`"忽略未举办该项目的比赛"是个关键放宽:如果一场没办 Square-1,Square-1 的 streak 不会因为缺席而中断。`],introEn:[`Single-event lens: from some comp onward, every final placing top 3 ticks +1; a final outside top 3 ends it. Counter granularity is (person, event) — one cuber can sustain parallel streaks across 3x3 and Square-1.`,`"Ignore comps that didn't hold the event" is the key relaxation — a comp without Square-1 doesn't kill a Square-1 streak.`],stats:[{value:`决赛 + 前 3`,labelZh:`登台判定`,labelEn:`Podium rule`,hintZh:"`is_final = 1 AND pos ≤ 3`",hintEn:"`is_final = 1 AND pos ≤ 3`"},{value:`(人 × 项目)`,labelZh:`streak 粒度`,labelEn:`Streak key`,hintZh:`同人不同项目各自计数`,hintEn:`Same person, different events, separate counters`},{value:`best > 0`,labelZh:`有效成绩`,labelEn:`Must complete`,hintZh:`全 DNF 即便排到第 3 也不算`,hintEn:`All-DNF final doesn't qualify`},{value:`Top 100`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`按连胜降序`,hintEn:`Sorted by streak desc`}],sourceZh:["`results` 全表 join `events` / `persons` / `competitions` / `round_types`;每行带 `round_type.final`(是否决赛)、`pos`(名次)、`best`(single)。按 `start_date, round_type.rank` 升序 —— 同场内预赛在决赛之前。"],sourceEn:["`results` joined to `events` / `persons` / `competitions` / `round_types`. Each row has `round_type.final` (is-final flag), `pos` (place), `best` (single). Ordered by `start_date, round_type.rank` — earlier rounds precede finals within a comp."],sourceCode:{lang:`sql`,body:`SELECT event.name event_name, person_link, competition_link,
       round_type.final is_final, pos place, best single
FROM results
JOIN events event ON event.id = event_id
JOIN persons person ON person.wca_id = person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN round_types round_type ON round_type.id = round_type_id
ORDER BY competition.start_date, round_type.rank`},steps:[{titleZh:`按 (选手, 比赛, 项目) 三层分组`,titleEn:`Group by (person, comp, event)`,bodyZh:"外层按选手,内层按 `(comp, event)` 拼一个 key;同 key 内多个 row = 该项目的多个轮次。",bodyEn:"Outer by person; inner by `(comp, event)` key — multiple rows in a key = multiple rounds."},{titleZh:`取最后一轮`,titleEn:`Take the last round`,bodyZh:"`groupRows[groupRows.length - 1]` —— 因为 SQL 已经按 `round_type.rank` 排,组末就是决赛(若 `is_final = 1`)。",bodyEn:"`groupRows[groupRows.length - 1]` — SQL already orders by `round_type.rank`, so the last row is the final (if `is_final = 1`)."},{titleZh:`登台判定 / 计数`,titleEn:`Podium check / advance`,bodyZh:"`is_final && place ≤ 3 && single > 0` → `current.count += 1`;否则把 current 写回 podiumsStreaks,把该项目从 `currentByEvent` 删 → 下次重新开 streak。",bodyEn:"`is_final && place ≤ 3 && single > 0` → `current.count += 1`; otherwise commit current, remove the event from `currentByEvent` — fresh streak next time."},{titleZh:`收集 + 取 100`,titleEn:`Collect + take 100`,bodyZh:`把每个 (人, 项目) 的所有 streak(包括仍在进行中的)平铺,按 count 降序取 100。`,bodyEn:`Flatten every (person, event) streak (including ongoing), sort desc by count, take 100.`,highlight:!0}],edgesZh:['"未办该项目的比赛"自动跳过 —— 该 (人, 项目) 在那场没有 row,循环不接触 `currentByEvent.event`。',"决赛全 DNF (`single = -1` 或 `0`)即便名次 ≤ 3 也不算,因为 `single > 0` 守门。",`只看决赛 —— 半决赛拿第 1 进决赛拿第 5,这场算"无登台"。`],edgesEn:["Comps that don't hold the event are skipped automatically — no row, loop never touches `currentByEvent.event`.","All-DNF finals (`single = -1` or `0`) don't count even if place ≤ 3 — `single > 0` gate.",`Finals only — winning a semi but bombing the final = "no podium" for that comp.`],related:[{id:`longest_streak_of_personal_records`,titleZh:`连续破 PR`,titleEn:`Streak with PR each comp`,hintZh:`同形态,换成"刷 PR"判定`,hintEn:`Same shape, swap podium for PR`},{id:`longest_streak_of_competitions_in_own_country`,titleZh:`本国连续参赛`,titleEn:`Streak in own country`,hintZh:`"全勤"型 streak`,hintEn:`Attendance-style streak`},{id:`longest_streak_of_podiums`,toStat:!0,titleZh:`查看实时连胜榜`,titleEn:`Jump to live streaks`,hintZh:`按项目筛选 + 选手`,hintEn:`Filter by event + cubers`}]},longest_time_to_sub_10:{id:`longest_time_to_sub_10`,titleZh:`最长达到 sub-10 三阶平均的时间`,titleEn:`Longest time to sub-10 3x3x3 average`,badgeZh:`选手`,badgeEn:`Person`,introZh:[`只看**已经突破** 3x3 sub-10 平均的选手,量他们从**首场比赛**到**首次 sub-10**之间的年数。越靠榜首,代表花的时间越久,故事性越强。`,`sub-10 在 3x3 圈里是公认的"高手准入线",拖了十几年才打进来的选手,通常都是回归 / 中年组逆袭,非常有意思。`],introEn:[`Restricted to cubers who **eventually** broke 3x3 sub-10 average — measures years from their **first comp** to their **first sub-10 average**. Top of the leaderboard = longest journey, best story.`,`Sub-10 is the recognized "high-level" line; people who took 10+ years to crack it are usually comeback cubers or veterans clawing their way back.`],stats:[{value:`< 1000 cs`,labelZh:`sub-10 阈值`,labelEn:`Sub-10 cutoff`,hintZh:"`average < 1000`(厘秒 = 10.00 秒)",hintEn:"`average < 1000` (centiseconds = 10.00 s)"},{value:`事件 = 333`,labelZh:`项目`,labelEn:`Event`,hintZh:`仅 3x3x3`,hintEn:`3x3x3 only`},{value:`365.25`,labelZh:`年化系数`,labelEn:`Year basis`,hintZh:`日差 / 365.25`,hintEn:`Day diff / 365.25`},{value:`Top 100`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`按年数降序`,hintEn:`Sorted by years desc`}],sourceZh:["三个子查询交叉 join:① 曾达 sub-10 的选手集合(`event_id = 333 AND average > 0 AND average < 1000`);② 每人首场比赛日期;③ 每人首次 sub-10 日期。差值 / 365.25 即年数。"],sourceEn:["Three subqueries joined: ① set of cubers who ever hit sub-10 (`event_id = 333 AND average > 0 AND average < 1000`); ② each person's first comp date; ③ each person's first sub-10 date. Diff / 365.25 = years."],sourceCode:{lang:`sql`,body:`SELECT person_link,
       DATEDIFF(first_sub10.start_date, first_comp.start_date) / 365.25 years
FROM (SELECT DISTINCT person_id FROM results
      WHERE event_id='333' AND average > 0 AND average < 1000) sub10
JOIN (SELECT person_id, MIN(start_date) start_date FROM results
      JOIN competitions ON id=competition_id GROUP BY person_id) first_comp USING (person_id)
JOIN (SELECT person_id, MIN(start_date) start_date FROM results
      JOIN competitions ON id=competition_id
      WHERE event_id='333' AND average > 0 AND average < 1000
      GROUP BY person_id) first_sub10 USING (person_id)
ORDER BY years DESC LIMIT 100`},steps:[{titleZh:`筛 sub-10 俱乐部`,titleEn:`Filter the sub-10 club`,bodyZh:"只保留至少有一次 `average < 1000` 的选手 —— `DISTINCT person_id` 子查询。",bodyEn:"Keep only cubers who hit `average < 1000` at least once — `DISTINCT person_id` subquery."},{titleZh:`取首场比赛日`,titleEn:`Get first comp date`,bodyZh:'`MIN(start_date)` 跨所有项目,**不限 333** —— 比"3x3 入坑日"早,因为可能先打过别的项目。',bodyEn:'`MIN(start_date)` across **all events** — earlier than "3x3 debut" since they might have played another event first.'},{titleZh:`取首次 sub-10 日`,titleEn:`Get first sub-10 date`,bodyZh:"同 `MIN(start_date)` 但加 `event_id = 333 AND average < 1000` 过滤,得到首次破线的那场比赛。",bodyEn:"Same `MIN(start_date)` but filtered by `event_id = 333 AND average < 1000` — the first comp crossing the line."},{titleZh:`年化 + 降序`,titleEn:`Annualize + sort desc`,bodyZh:"`DATEDIFF / 365.25` 给小数年;按年数降序取 100,显示两位小数。",bodyEn:"`DATEDIFF / 365.25` for fractional years; sort desc, take 100, display two decimals.",highlight:!0}],edgesZh:[`"首场比赛"含所有项目,所以哪怕你只为参加 BLD,然后 5 年后才打 sub-10 3x3,这 5 年也算进来。`,`只挑事后已 sub-10 的人 —— "永远 sub-10 不了"的选手不上榜。`,"首次 sub-10 是按**比赛日期**而非具体轮次时刻;同日先慢轮再快轮 → 用该比赛 `start_date`。"],edgesEn:[`"First comp" spans all events — even if you debuted only for BLD and didn't touch 3x3 for 5 years, those 5 years count.`,`Only cubers who eventually broke sub-10 are listed — never-sub-10 cubers don't appear.`,"First sub-10 is by **comp date**, not exact round time; slow-then-fast same day → that comp's `start_date`."],related:[{id:`longest_streak_of_personal_records`,titleZh:`连续破 PR`,titleEn:`Streak with PR each comp`,hintZh:`持续进步 vs 单点突破`,hintEn:`Sustained improvement vs single milestone`},{id:`shortest_time_to_get_all_singles`,titleZh:`最快全项目 single`,titleEn:`Fastest to all singles`,hintZh:`反方向 —— "最快"达成`,hintEn:`Opposite direction — "fastest" to`},{id:`longest_time_to_sub_10`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`具体年数 + 选手`,hintEn:`Years + cubers`}]},most_attended_competitions_in_single_month:{id:`most_attended_competitions_in_single_month`,titleZh:`单月参赛最多`,titleEn:`Most comps attended in a single month`,badgeZh:`选手`,badgeEn:`Person`,introZh:["日历月维度的疯狂窗口:在**某个自然月**(1 号到月末)内,一个选手参加了多少场比赛。`GROUP BY person_id, YEAR(start_date), MONTHNAME(start_date)` 就把月份切片,直接 `COUNT`。","入榜阈值是 4(`HAVING ≥ 4`),意味着平均一周一场,已经是相当疯狂的频率;榜首通常在 6-8 场之间。"],introEn:["Calendar-month explosion: how many comps a cuber attended within a **single natural month** (1st through month-end). `GROUP BY person_id, YEAR(start_date), MONTHNAME(start_date)` slices by month, plain `COUNT`.","Cutoff is 4 (`HAVING ≥ 4`) — roughly weekly cadence, already insane. The top sits around 6-8."],stats:[{value:`≥ 4`,labelZh:`入榜阈值`,labelEn:`Cutoff`,hintZh:"`HAVING attended_within_month >= 4`",hintEn:"`HAVING attended_within_month >= 4`"},{value:`自然月`,labelZh:`窗口`,labelEn:`Window`,hintZh:`1 号 ~ 月末,跨年不合并`,hintEn:`1st to month-end, year-bounded`},{value:`DISTINCT`,labelZh:`去重`,labelEn:`Dedup`,hintZh:`同场多项目 → 1 场`,hintEn:`Multi-event same comp → 1`}],sourceZh:["`results` 去重 (`DISTINCT competition_id, person_id`) join `competitions`,按 (选手, 年, 月) 分组 `COUNT`。`GROUP_CONCAT` 把同月所有比赛 markdown link 拼成列表展示。"],sourceEn:["`results` deduped on `(competition_id, person_id)` joined to `competitions`, grouped by (person, year, month) with `COUNT`. `GROUP_CONCAT` concatenates all comps that month as markdown links."],sourceCode:{lang:`sql`,body:`SELECT COUNT(*) attended_within_month, person_id,
       MONTHNAME(start_date) month_name,
       YEAR(start_date) competitions_year,
       GROUP_CONCAT(... competition cell_name ... ORDER BY start_date)
FROM (SELECT DISTINCT competition_id, person_id FROM results) r
JOIN competitions ON competitions.id = competition_id
GROUP BY person_id, YEAR(start_date), month_name
HAVING attended_within_month >= 4
ORDER BY attended_within_month DESC`},steps:[{titleZh:`去重 (人, 场)`,titleEn:`Dedup (person, comp)`,bodyZh:"同一场打多个项目只算一行 —— `DISTINCT competition_id, person_id` 子查询。",bodyEn:"Multi-event same comp collapses to one row — `DISTINCT competition_id, person_id` subquery."},{titleZh:`按 (人, 年, 月) 分桶`,titleEn:`Bucket by (person, year, month)`,bodyZh:'`YEAR(start_date)` + `MONTHNAME(start_date)` 双键防止跨年合并 —— "Jan 2024" 和 "Jan 2025" 是两桶。',bodyEn:'`YEAR(start_date)` + `MONTHNAME(start_date)` joint key prevents cross-year merging — "Jan 2024" vs "Jan 2025" are separate buckets.'},{titleZh:`COUNT + 阈值`,titleEn:`COUNT + cutoff`,bodyZh:"`COUNT(*)` 给每桶场次;`HAVING ≥ 4` 滤掉常规节奏。",bodyEn:"`COUNT(*)` per bucket; `HAVING ≥ 4` trims normal cadence."},{titleZh:`降序展示比赛列表`,titleEn:`Sort desc + show list`,bodyZh:"按场次降序,同分时按姓名;附上 `GROUP_CONCAT` 拼好的具体比赛 markdown 链接,方便点开看。",bodyEn:"Sort by count desc, ties by name; attach `GROUP_CONCAT`-built markdown links for inspection.",highlight:!0}],edgesZh:[`"自然月"切割比"30 天滑窗"宽松 —— 月底 30 号 + 月初 1 号 + 2 号其实是连续 3 天,但跨月切成两桶各 1 + 2。`,"只看比赛 `start_date`,多日赛延伸到下个月也只按起始月算。",`跨年最疯狂的"夏 + 秋 + 冬"季节性堆赛,本指标看不到 —— 见单周指标更密。`],edgesEn:[`Calendar slice is laxer than "rolling 30 days" — Jun-30 + Jul-1 + Jul-2 is 3 consecutive days but gets split into two buckets (1 + 2).`,"Comp `start_date` only — multi-day spilling into the next month still counts under start month.",`Doesn't capture summer→autumn→winter back-to-back streaks across the year — the weekly metric goes denser.`],related:[{id:`most_attended_competitions_in_single_week`,titleZh:`单周最多场次`,titleEn:`Most in a week`,hintZh:`更紧的滑窗`,hintEn:`Tighter rolling window`},{id:`competitions_per_year_by_person`,titleZh:`年均频率`,titleEn:`Annual frequency`,hintZh:`长期 vs 短期爆发`,hintEn:`Long-term vs short-burst`},{id:`most_distinct_dates_competed_on`,titleZh:`不同参赛日期`,titleEn:`Distinct dates`,hintZh:`另一面 —— 日期覆盖度`,hintEn:`Flip side — date coverage`},{id:`most_attended_competitions_in_single_month`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`看每月谁堆得最猛`,hintEn:`Who stacked the most that month`}]},most_attended_competitions_in_single_week:{id:`most_attended_competitions_in_single_week`,titleZh:`单周内最多参加比赛数`,titleEn:`Most comps attended in a single week`,badgeZh:`选手`,badgeEn:`Person`,introZh:['把比赛对齐到 ISO **周**(周一为周首):用 `WEEKDAY()` 反推该比赛所在周的第 0 天和第 6 天,然后按 (人, 周) 分组 `COUNT`。窗口比"自然月"紧 4 倍。',`榜首 3 场已经很硬 —— 周六多个赛区同日开,选手得提前安排好场地切换。`],introEn:["Align comps to the ISO **week** (Monday-first): use `WEEKDAY()` to back-compute the week's day-0 and day-6, then group by (person, week) with `COUNT`. 4× tighter than the monthly window.",`Top entries at 3 are already brutal — multiple Saturday venues on the same day means tight venue-hopping.`],stats:[{value:`≥ 3`,labelZh:`入榜阈值`,labelEn:`Cutoff`,hintZh:"`HAVING attended_within_week >= 3`",hintEn:"`HAVING attended_within_week >= 3`"},{value:`周一 - 周日`,labelZh:`周边界`,labelEn:`Week edges`,hintZh:`ISO,WEEKDAY = 0 即周一`,hintEn:`ISO, WEEKDAY=0 = Monday`},{value:`DISTINCT`,labelZh:`去重`,labelEn:`Dedup`,hintZh:`同场多项目 → 1 场`,hintEn:`Multi-event same comp → 1`}],sourceZh:["`results` 去重 join `competitions`;`DATE_ADD(start_date, INTERVAL -WEEKDAY(start_date) DAY)` 把 start_date 拨回当周周一,作为周锚点。按 (人, week_start, week_end, year) 分组。"],sourceEn:["`results` deduped, joined to `competitions`. `DATE_ADD(start_date, INTERVAL -WEEKDAY(start_date) DAY)` snaps `start_date` to that week's Monday — used as the week anchor. Group by (person, week_start, week_end, year)."],sourceCode:{lang:`sql`,body:`SELECT COUNT(*) attended_within_week, person_id,
       DATE_ADD(start_date, INTERVAL -WEEKDAY(start_date) DAY) week_start_date,
       DATE_ADD(start_date, INTERVAL 6-WEEKDAY(start_date) DAY) week_end_date
FROM (SELECT DISTINCT competition_id, person_id FROM results) r
JOIN competitions ON competitions.id = competition_id
GROUP BY person_id, week_start_date, week_end_date, YEAR(start_date)
HAVING attended_within_week >= 3
ORDER BY attended_within_week DESC`},steps:[{titleZh:`去重 (人, 场)`,titleEn:`Dedup (person, comp)`,bodyZh:`同月同样的处理 —— 多项目同场塌缩为 1。`,bodyEn:`Same as the monthly version — multi-event same comp → 1.`},{titleZh:`锚定 ISO 周`,titleEn:`Anchor to ISO week`,bodyZh:"`WEEKDAY` 在 MySQL 里 0 = 周一;减回去即得该周周一,加 6 即得周日。这是分桶的钥匙。",bodyEn:"`WEEKDAY` in MySQL: 0 = Monday; subtract back to the week's Monday, add 6 for Sunday. These are the bucket keys."},{titleZh:`按 (人, 周) 分组 COUNT`,titleEn:`Group by (person, week) + COUNT`,bodyZh:"`YEAR(start_date)` 进 GROUP BY 防止跨年同周的边界 case 合并(虽然概率极低,守一手)。",bodyEn:"`YEAR(start_date)` joins the GROUP BY to guard the unlikely-but-possible cross-year same-week-num collision."},{titleZh:`日期格式化展示`,titleEn:`Pretty-print dates`,bodyZh:"TS 端把 `week_start_date / week_end_date` 用 `d&nbsp;Mon&nbsp;YYYY` 格式渲染,避免日期换行。",bodyEn:"TS side formats `week_start_date / week_end_date` as `d&nbsp;Mon&nbsp;YYYY` to prevent line wraps.",highlight:!0}],edgesZh:[`周边界是 ISO(周一首日),不是美式(周日首日);跨周末的双天比赛只算起始日所在周。`,`周末跨大洲多场是常见上榜套路:北美早场 + 欧洲晚场 + 亚洲后日,3 场刚好。`,`同月版本能漏掉跨月的连续周,这个版本能补;反过来同一周内 3 场可能跨不同月。`],edgesEn:[`Week boundary is ISO (Mon-first), not US (Sun-first); a weekend two-day comp counts under the week of its start date only.`,`Crossing continents over a single weekend is the classic top-rank pattern: NA morning + EU evening + Asia next day = 3.`,`The monthly version can miss cross-month consecutive weeks; this one catches them — and the same 3 in a week may straddle two months.`],related:[{id:`most_attended_competitions_in_single_month`,titleZh:`单月最多场次`,titleEn:`Most in a month`,hintZh:`宽 4 倍的窗口`,hintEn:`Window 4× wider`},{id:`competitions_per_year_by_person`,titleZh:`年均频率`,titleEn:`Annual frequency`,hintZh:`长期视角`,hintEn:`Long-term view`},{id:`longest_competitions_path`,titleZh:`最长比赛路径`,titleEn:`Longest path`,hintZh:`空间 vs 时间 紧凑度`,hintEn:`Spatial vs temporal density`},{id:`most_attended_competitions_in_single_week`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`具体周 + 比赛列表`,hintEn:`Week + comp list`}]},most_competitions_abroad:{id:`most_competitions_abroad`,titleZh:`海外参赛最多`,titleEn:`Most competitions abroad`,badgeZh:`选手`,badgeEn:`Person`,introZh:['一行最朴素的 `result.country_id != competition.country_id` 完成。每条 `results` 行存了**选手在那一刻的注册国** —— 所以改国籍前后的"海外"判定会跟着变,符合直觉。',`榜首通常是住在小国但全球巡飞的玩家;欧盟区互访也大量贡献 —— 一周内打三国并不少见。`],introEn:['One terse `result.country_id != competition.country_id` does it. Each `results` row stores the **cuber\'s country at that moment** — so the "abroad" flag follows country changes naturally, as expected.',`Top spots are typically small-country residents who travel globally; intra-EU hopping contributes too — three countries in a week isn't rare.`],stats:[{value:`COUNT(DISTINCT)`,labelZh:`聚合`,labelEn:`Aggregate`,hintZh:`场次去重`,hintEn:`Distinct comp count`},{value:`Top 100`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`按海外场次降序`,hintEn:`Sorted by abroad count desc`},{value:`排除 8 个虚拟国`,labelZh:`过滤`,labelEn:`Filter`,hintZh:`XA/XE/XF/XM/XN/XO/XS/XW`,hintEn:`XA/XE/XF/XM/XN/XO/XS/XW`}],sourceZh:["`results result` 直接 join `competitions`;`WHERE result.country_id != competition.country_id` 即海外。`COUNT(DISTINCT competition_id)` 防止同场多项目重复计数。",`过滤掉大陆 FMC 的虚拟国家 —— 它们没有实际地理位置,把"代表大陆参加的虚拟国家比赛"标成海外会失真。`],sourceEn:["`results result` joined to `competitions`; `WHERE result.country_id != competition.country_id` = abroad. `COUNT(DISTINCT competition_id)` avoids multi-event double-counting.",`Continental FMC virtual countries are filtered out — they have no geo, marking them "abroad" would skew things.`],sourceCode:{lang:`sql`,body:`SELECT person_id, COUNT(DISTINCT competition_id) competitions_abroad
FROM results
JOIN competitions ON competitions.id = competition_id
WHERE result.country_id != competition.country_id
  AND competition.country_id NOT IN
      ('XA','XE','XF','XM','XN','XO','XS','XW')
GROUP BY person_id
ORDER BY competitions_abroad DESC
LIMIT 100`},steps:[{titleZh:`逐行打"海外"标签`,titleEn:`Tag each row "abroad"`,bodyZh:"WCA dump 的 `results.country_id` 是该选手**当时**的国籍快照 —— 改国籍后老 row 不会被回写,所以历史海外标记精确。",bodyEn:"WCA dump's `results.country_id` snapshots the cuber's country **at that time** — country changes don't rewrite history, so old abroad flags stay correct."},{titleZh:`排除虚拟大陆国`,titleEn:`Exclude continental virtuals`,bodyZh:"`XA/XE/XF/XM/XN/XO/XS/XW` 用于洲际 FMC,没有真实地理 —— 这些行不参与统计。",bodyEn:"`XA/XE/XF/XM/XN/XO/XS/XW` are placeholders for continental FMC, no real geo — drop these."},{titleZh:`去重场次 + 聚合`,titleEn:`Dedup + aggregate`,bodyZh:"`COUNT(DISTINCT competition_id)` 把同场多项目折成 1。",bodyEn:"`COUNT(DISTINCT competition_id)` collapses multi-event same comp."},{titleZh:`降序取 100`,titleEn:`Sort desc, take 100`,bodyZh:"按海外场次降序;外层 join `persons` 拿名字。",bodyEn:"Order by abroad count desc; outer join `persons` for names.",highlight:!0}],edgesZh:[`改国籍的选手:旧国 + 新国时期分别贡献"海外"判定 —— 各时段都按当时注册国比对,合理。`,`一个旅居者长期住在 B 国但还挂 A 国国籍 → 在 B 国所有比赛都被标海外,实际却是"主场"。`,"`competition.country_id != result.country_id`,**不**和 `persons.country_id`(当前国籍)比 —— 这是历史准确性 vs 当前快照的取舍。"],edgesEn:[`Country-changers: pre-change and post-change periods each contribute abroad counts against their then-current country — fair.`,`A long-term resident in country B still flagged as A → all B comps mark abroad, even though B is the daily home.`,"Compared against `competition.country_id != result.country_id`, **not** `persons.country_id` (current) — historical accuracy over present snapshot."],related:[{id:`most_visited_countries`,titleZh:`去过最多国家`,titleEn:`Most countries visited`,hintZh:`相关 —— 海外场次 ≥ 海外国家数`,hintEn:`Related — abroad comps ≥ abroad countries`},{id:`longest_competitions_path`,titleZh:`最长比赛路径`,titleEn:`Longest path`,hintZh:`空间距离视角`,hintEn:`Spatial-distance angle`},{id:`longest_streak_of_competitions_in_own_country`,titleZh:`本国连续参赛`,titleEn:`Streak in own country`,hintZh:`反向 —— 完全本地化`,hintEn:`Inverse — fully local`},{id:`most_competitions_abroad`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`具体海外场次 + 选手`,hintEn:`Abroad counts + cubers`}]},most_completed_solves:{id:`most_completed_solves`,titleZh:`最多完成还原数`,titleEn:`Most completed solves`,badgeZh:`选手`,badgeEn:`Person`,introZh:['WCA 不存"成功还原数"汇总字段 —— 这条统计深入到 `result_attempts` 子表,逐 attempt 数 `value > 0` 的次数,再按 6 个维度(比赛/选手/国家/大洲/年份/项目)聚合 top 20。','是个 `GroupedStatistic` —— 一个 SQL 拉全表,TS 端 6 次重分组。"还原数"是绝对值排名,跟比赛数 / PR 数都不一样 —— 一个 BLD 多盲选手一场可能贡献两位数 solve。'],introEn:['WCA stores no "completed solves" aggregate — this drilldown hits the `result_attempts` subtable, counts `value > 0` per attempt, then re-aggregates across 6 dimensions (comp / person / country / continent / year / event) for top-20 each.',"A `GroupedStatistic` — one SQL pull, 6 TS regroups. Absolute volume, distinct from comp count or PR count — a MBLD specialist can rack double digits per comp."],stats:[{value:`6`,labelZh:`聚合维度`,labelEn:`Dimensions`,hintZh:`比赛 / 人 / 国 / 洲 / 年 / 项目`,hintEn:`Comp / person / country / continent / year / event`},{value:`Top 20`,labelZh:`每段深度`,labelEn:`Per-section depth`,hintZh:`每个维度独立 top 20`,hintEn:`Each section is independent top 20`},{value:"`value > 0`",labelZh:`成功定义`,labelEn:`Completed = `,hintZh:`DNF (-1) / 未尝试 (0) 不算`,hintEn:`DNF (-1) / not-attempted (0) excluded`}],sourceZh:["主表 `results` join `result_attempts`(用相关子查询数 `value > 0` 和 `value = -1`),配 `persons / competitions / countries / continents / events`。一次 SQL 把每个 result 的 completed/dnf 数算清楚,TS 端按 6 个 field 各自再 GROUP BY。"],sourceEn:["Main `results` joined to `result_attempts` via correlated subqueries (`COUNT WHERE value > 0` and `value = -1`), plus `persons / competitions / countries / continents / events`. One SQL pulls per-result completed/dnf; TS regroups by 6 fields."],sourceCode:{lang:`sql`,body:`SELECT
  (SELECT COUNT(*) FROM result_attempts
   WHERE result_id = result.id AND value > 0) completed_count,
  (SELECT COUNT(*) FROM result_attempts
   WHERE result_id = result.id AND value = -1) dnfs_count,
  competition_link, person_link, country.name country,
  continent.name continent,
  YEAR(competition.start_date) year, event.name event
FROM results result
JOIN persons person ON wca_id = person_id AND sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN countries / continents / events ...`},steps:[{titleZh:`逐 result 数 attempt`,titleEn:`Per-result attempt count`,bodyZh:"相关子查询(correlated subquery)对每个 `result.id` 二次扫 `result_attempts`,分别数 `value > 0`(完成)和 `value = -1`(DNF)。",bodyEn:"Correlated subquery rescans `result_attempts` per `result.id`, counting `value > 0` (completed) and `value = -1` (DNF) separately."},{titleZh:`主表带维度字段`,titleEn:`Carry dimension fields`,bodyZh:`同 row 拼上比赛 link / 人 link / 国名 / 大洲名 / 年份 / 项目名 —— 后续 TS 端选哪个字段就按哪个聚合。`,bodyEn:`Same row carries comp link / person link / country / continent / year / event — TS picks one of these as the regrouping key per section.`},{titleZh:`TS 6 次重分组`,titleEn:`TS regroup × 6`,bodyZh:"Map<key, {completed, dnfs}>:对每个维度,key = 该字段值;`completed += completed_count, dnfs += dnfs_count`。",bodyEn:"Map<key, {completed, dnfs}>: for each dimension, key = field value; `completed += completed_count, dnfs += dnfs_count`."},{titleZh:`排序 + top 20 + 加粗`,titleEn:`Sort + top 20 + bold`,bodyZh:`主键 completed 降序,平局比 attempts 升序(同 completed 时尝试少的赢),再次平局按 key 字母序;completed 数字加粗显示。`,bodyEn:`Primary sort: completed desc; tiebreak: attempts asc (fewer attempts wins at same completed); final: alpha by key. Completed cell bolded.`,highlight:!0}],edgesZh:['MBLD 在 `result_attempts` 里只占一两条 attempt 行,但 cube 数能上 60+ —— 本指标按 attempt 数,所以 MBLD 单场只贡献 1-2 算"完成",不按 cube 数算。','`value > 0` 包括所有完成,不区分项目 —— FMC 的"分数"也算 1 次完成。',`DNF 不扣分,但作为 attempts 计入分母 —— 平局时少 DNF 的赢。`],edgesEn:['MBLD shows as 1-2 attempt rows in `result_attempts`, but represents 60+ cubes — this metric counts attempts, so MBLD contributes 1-2 "completed" per comp, not cube count.','`value > 0` covers everything completed regardless of event — FMC scores count as 1 "completed" too.',`DNF doesn't subtract but counts in attempts (denominator) — tiebreaker favors fewer DNFs.`],related:[{id:`most_solves_before_bld_success`,titleZh:`盲拧成功前尝试数`,titleEn:`Solves before BLD success`,hintZh:`"失败 attempts"的反指标`,hintEn:`Failed-attempts angle`},{id:`most_distinct_dates_competed_on`,titleZh:`不同参赛日期`,titleEn:`Distinct dates`,hintZh:`volume 的另一种量化`,hintEn:`Alternative volume metric`},{id:`competitions_per_year_by_person`,titleZh:`年均频率`,titleEn:`Annual frequency`,hintZh:`频率 vs 总量`,hintEn:`Rate vs total`},{id:`most_completed_solves`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`6 维度切换`,hintEn:`6-dimension toggle`}]},most_distinct_dates_competed_on:{id:`most_distinct_dates_competed_on`,titleZh:`最多不同参赛日期`,titleEn:`Most distinct dates competed on`,badgeZh:`选手`,badgeEn:`Person`,introZh:["把多日比赛展开成每一天 —— 用 SQL inline `nums (0..9)` 表 cross-join 出每场比赛的所有具体日期(`start_date + n` ≤ `end_date`),然后按 `MM/DD` 月日去 distinct。一年里 366 天哪些天参过赛?",`榜上的人通常都打满全年大多数月份;再 TS 端把日期按月份归类,显示每月的覆盖百分比 —— 一目了然看出"夏天密集 vs 全年均衡"。`],introEn:["Explode multi-day comps day-by-day — SQL cross-joins an inline `nums (0..9)` table to enumerate every concrete date a comp covers (`start_date + n` ≤ `end_date`), then distincts by `MM/DD`. Which calendar days have you ever competed on?",`Cubers on this leaderboard typically cover most months of the year; the TS side then groups dates by month and shows per-month coverage % — instantly readable for "summer-heavy vs year-balanced" patterns.`],stats:[{value:`≥ 100`,labelZh:`入榜阈值`,labelEn:`Cutoff`,hintZh:`至少 100 个不同日期`,hintEn:`At least 100 distinct dates`},{value:`MM/DD`,labelZh:`日期 key`,labelEn:`Date key`,hintZh:`跨年同日合并(2020-05-01 = 2024-05-01)`,hintEn:`Same MM/DD across years merge`},{value:`0 - 9 天`,labelZh:`比赛长度上限`,labelEn:`Max comp span`,hintZh:"SQL inline `nums` 表枚举",hintEn:"SQL inline `nums` enumeration"}],sourceZh:["`competitions` 与 `nums (0..9)` 表 cross-join,把每场 N 日比赛展开为 N 行(限制 `start_date + n ≤ end_date`)。和 `results` join 后,选手参加的每个具体日期都有一行。再 `DISTINCT MM/DD` 跨年合并 → `GROUP_CONCAT` 收尾。"],sourceEn:["`competitions` cross-joined with an inline `nums (0..9)` table explodes each N-day comp into N rows (constrained by `start_date + n ≤ end_date`). Joined to `results`, every concrete competing date becomes a row. `DISTINCT MM/DD` merges across years, then `GROUP_CONCAT` collects."],sourceCode:{lang:`sql`,body:`SELECT COUNT(DISTINCT competition_date) attended_dates,
       person_id,
       GROUP_CONCAT(DISTINCT competition_date
                    ORDER BY competition_date ASC SEPARATOR ',') dates_list
FROM (
  SELECT person_id, DATE_FORMAT(comp_date, '%m/%d') competition_date
  FROM results
  JOIN (SELECT id, DATE_ADD(start_date, INTERVAL n DAY) comp_date
        FROM competitions
        JOIN nums ON DATE_ADD(start_date, INTERVAL n DAY) <= end_date) c
       ON c.id = results.competition_id) d
GROUP BY person_id
HAVING attended_dates >= 100`},steps:[{titleZh:`展开多日比赛`,titleEn:`Explode multi-day comps`,bodyZh:"`nums` 子查询枚举 0-9,cross-join `competitions`,用 `DATE_ADD(start_date, INTERVAL n DAY) <= end_date` 砍掉不存在的日期。多日比赛因此变成多行。",bodyEn:"`nums` subquery enumerates 0-9, cross-joined to `competitions`; `DATE_ADD(start_date, INTERVAL n DAY) <= end_date` trims non-existent days. Multi-day comps expand into multiple rows."},{titleZh:`日期 → MM/DD`,titleEn:`Date → MM/DD`,bodyZh:"`DATE_FORMAT(.., '%m/%d')` 抹掉年份 —— 2020 跟 2024 同月日合并。",bodyEn:"`DATE_FORMAT(.., '%m/%d')` strips year — 2020 and 2024 same date merge."},{titleZh:`按选手 COUNT DISTINCT`,titleEn:`Per-person COUNT DISTINCT`,bodyZh:"GROUP BY `person_id`,数 distinct 日期;`HAVING >= 100` 砍掉常规选手。",bodyEn:"GROUP BY `person_id`, count distinct dates; `HAVING >= 100` trims casual cubers."},{titleZh:`TS 按月归类 + 百分比`,titleEn:`TS regroup by month + %`,bodyZh:'`dates_list` "01/05,01/12,..." 按月份分组;每月百分比 = `days.length / DAYS_IN_MONTH[m]`(2 月按 29 天)。',bodyEn:"`dates_list` `01/05,01/12,...` is regrouped by month; per-month % = `days.length / DAYS_IN_MONTH[m]` (Feb at 29 days).",highlight:!0}],edgesZh:["`nums` 上限 9 → 不支持 11 日及以上比赛(目前 WCA 没有,稳)。",'`MM/DD` 抹年份 —— 跨年同日不重复计数,所以"在多少天打过赛"是跨多年合并后的去重数。',`2 月按 29 天作分母 —— 非闰年的 2/29 在 5 个月份按数据合理性挂掉,但 % 显示偏低 1/29 不修。`],edgesEn:["`nums` capped at 9 → no support for 11+ day comps (none in WCA, safe).",'`MM/DD` discards year — same day across years dedup, so "days competed on" is multi-year-merged distinct.',`Feb uses 29 days as denominator — non-leap 2/29 rows just don't exist, but % shown is slightly underweighted (1/29) — not corrected.`],related:[{id:`competitions_per_year_by_person`,titleZh:`年均频率`,titleEn:`Annual frequency`,hintZh:`日期 vs 场次`,hintEn:`Days vs comps`},{id:`most_attended_competitions_in_single_month`,titleZh:`单月最多场次`,titleEn:`Most in a month`,hintZh:`密度 vs 覆盖度`,hintEn:`Density vs coverage`},{id:`most_completed_solves`,titleZh:`最多完成还原`,titleEn:`Most completed solves`,hintZh:`volume 兄弟统计`,hintEn:`Sibling volume metric`},{id:`most_distinct_dates_competed_on`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`按月份覆盖看分布`,hintEn:`Per-month coverage view`}]},most_solves_before_bld_success:{id:`most_solves_before_bld_success`,titleZh:`盲拧成功前最多尝试次数`,titleEn:`Most solves before first BLD success`,badgeZh:`选手`,badgeEn:`Person`,introZh:["盲拧四项目(`333bf / 444bf / 555bf / 333mbf`)逐 attempt 展开,按时间顺序铺一条该选手的尝试链;**找到第一个 `value > 0`** 的下标 = 之前的失败数。0 就是第一次就成。",`榜首通常是几十次甚至上百次 DNF 才憋出一次成功 —— 盲拧的"成功率坡道"超陡,跟普通项目完全不同。`],introEn:["For each BLD event (`333bf / 444bf / 555bf / 333mbf`), expand attempts in time order into a flat list per cuber; **find the first `value > 0`** — its index = failures before. 0 means success on the very first attempt.",`Top spots are dozens or hundreds of DNFs before finally landing one — BLD has a brutal success-rate ramp unlike any speed event.`],stats:[{value:`4`,labelZh:`项目`,labelEn:`Events`,hintZh:`333bf/444bf/555bf/333mbf`,hintEn:`333bf/444bf/555bf/333mbf`},{value:`DNF 计入`,labelZh:`失败定义`,labelEn:`Failure`,hintZh:"`value = -1`(DNF) 算尝试,`0` 不算",hintEn:"`value = -1` (DNF) counts, `0` does not"},{value:`Top 20`,labelZh:`每段深度`,labelEn:`Per-section depth`,hintZh:`4 个项目各 top 20`,hintEn:`4 events × top 20`}],sourceZh:["`results` 限 BLD 四项目,join `persons / competitions / round_types`,带 `ATTEMPTS_SUBQUERY`(把 `result_attempts.value` 用逗号拼成字符串)。按 `start_date, round_type.rank` 升序 —— 给 TS 端一条时间正确的 attempt 串。"],sourceEn:["`results` limited to the 4 BLD events, joined to `persons / competitions / round_types`, with `ATTEMPTS_SUBQUERY` (concatenates `result_attempts.value` as comma-separated). Ordered by `start_date, round_type.rank` — TS gets a time-correct attempt chain."],sourceCode:{lang:`sql`,body:`SELECT event_id, person_link, \${ATTEMPTS_SUBQUERY} AS attempts
FROM results
JOIN persons person ON wca_id = person_id AND sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN round_types round_type ON round_type.id = round_type_id
WHERE event_id IN ('333bf','444bf','555bf','333mbf')
ORDER BY competition.start_date, round_type.rank`},steps:[{titleZh:`按项目过滤`,titleEn:`Filter by event`,bodyZh:`只看 BLD 四项目 —— OH/FMC/普通项目都不在范围。`,bodyEn:`BLD-only — OH / FMC / regular events out of scope.`},{titleZh:`拼 attempts 字符串`,titleEn:`Concatenate attempts`,bodyZh:"`ATTEMPTS_SUBQUERY` 把每个 result 的 1-5 个 attempt value 用 `,` 拼起来,SQL 一行即一 result。",bodyEn:"`ATTEMPTS_SUBQUERY` joins each result's 1-5 attempt values with `,`. One SQL row per result."},{titleZh:`TS 端按 (项目, 人) 平铺`,titleEn:`TS flatten by (event, person)`,bodyZh:`每个 (项目, 人) 把所有 result 的 attempts 串展开成一维数组,**保留 -1(DNF)和 >0(成功),丢 0(skip)和 -2(DNS)**。`,bodyEn:`Per (event, person), flatten every result's attempts into a 1D array, **keeping -1 (DNF) and >0 (success), dropping 0 (skip) and -2 (DNS)**.`},{titleZh:`findIndex 找首胜`,titleEn:`findIndex first success`,bodyZh:'`allAttempts.findIndex(v => v > 0)` 即"前面失败几次"。从未成功 → 不上榜。',bodyEn:'`allAttempts.findIndex(v => v > 0)` = "how many failures before". Never succeeded → not listed.',highlight:!0}],edgesZh:['`value = 0`(未尝试 / 退场)被排除 —— 它们不是真正的"失败",别污染计数。',`只统计**最终成功过**的选手 —— 从未成功的不进榜,但他们其实可能更"努力"。这是上榜口径的取舍。`,"MBLD 的 attempt 跟 BLD 用同 `value` 编码,但语义不同(MBLD value = 难度分),`> 0` 仍能正确判定为成功。"],edgesEn:["`value = 0` (skip / withdrawn) is dropped — not a real failure, would pollute the count.",`Only cubers who **eventually succeeded** are listed — never-succeeded cubers (possibly the hardest workers!) are absent. Tradeoff in the leaderboard scope.`,"MBLD attempts share the `value` encoding with BLD but mean a difficulty score, not time; `> 0` still correctly flags success."],related:[{id:`most_completed_solves`,titleZh:`最多完成还原`,titleEn:`Most completed solves`,hintZh:`反面 —— 成功 attempt 总量`,hintEn:`Inverse — total successful attempts`},{id:`longest_time_to_sub_10`,titleZh:`最长 sub-10 时长`,titleEn:`Longest time to sub-10`,hintZh:`相似的"坚持型"叙事`,hintEn:`Similar perseverance narrative`},{id:`most_solves_before_bld_success`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`4 项目分段`,hintEn:`4 events broken out`}]},most_visited_continents:{id:`most_visited_continents`,titleZh:`去过最多大洲参赛`,titleEn:`Most continents visited`,badgeZh:`选手`,badgeEn:`Person`,introZh:["一句 `COUNT(DISTINCT continent_id)` 做完;入榜阈值 4 —— 7 大洲里去过一半以上。WCA 没有南极洲赛事,所以理论最大值是 6。","同样排除 `_Multiple Continents`(洲际 FMC 的虚拟 continent),保证 count 都是真正的洲。"],introEn:["One `COUNT(DISTINCT continent_id)` does it; cutoff is 4 — over half of the 7 continents. WCA has no Antarctica comps, so the theoretical max is 6.","Excludes `_Multiple Continents` (the continental-FMC virtual continent) to keep counts to real continents."],stats:[{value:`6`,labelZh:`理论上限`,labelEn:`Theoretical max`,hintZh:`7 大洲 − 南极 = 6`,hintEn:`7 continents − Antarctica = 6`},{value:`≥ 4`,labelZh:`入榜阈值`,labelEn:`Cutoff`,hintZh:`半数以上大洲`,hintEn:`Majority of continents`},{value:`COUNT(DISTINCT)`,labelZh:`聚合`,labelEn:`Aggregate`,hintZh:"`continent_id` 去重数",hintEn:"Distinct `continent_id`"}],sourceZh:['`results` join `competitions` 拿场地国,再 join `countries` 拿洲 id;`WHERE continent_id != "_Multiple Continents"` 排掉虚拟洲,`GROUP BY person_id` + `COUNT DISTINCT continent_id`。'],sourceEn:['`results` joined to `competitions` for venue country, then `countries` for continent id; `WHERE continent_id != "_Multiple Continents"` drops the virtual continent. `GROUP BY person_id` + `COUNT DISTINCT continent_id`.'],sourceCode:{lang:`sql`,body:`SELECT person_id, COUNT(DISTINCT continent_id) visited_continents
FROM results
JOIN competitions ON competitions.id = competition_id
JOIN countries ON countries.id = competition.country_id
WHERE continent_id != "_Multiple Continents"
GROUP BY person_id
HAVING visited_continents >= 4
ORDER BY visited_continents DESC`},steps:[{titleZh:`比赛国 → 大洲`,titleEn:`Comp country → continent`,bodyZh:"`countries.continent_id` 给出每个比赛地所属洲;洲际 FMC 比赛属于 `_Multiple Continents`,排掉。",bodyEn:"`countries.continent_id` gives the continent of each venue; continental-FMC belongs to `_Multiple Continents`, filtered out."},{titleZh:`COUNT DISTINCT`,titleEn:`COUNT DISTINCT`,bodyZh:`一个选手在欧洲打 50 场和打 1 场,这指标都算 1 个大洲;广度不是深度。`,bodyEn:`50 European comps vs 1 European comp both count as 1 continent for this metric — breadth not depth.`},{titleZh:`阈值 + 降序`,titleEn:`Cutoff + sort desc`,bodyZh:"`HAVING >= 4` 砍掉普通选手;按 distinct 大洲数降序。",bodyEn:"`HAVING >= 4` trims regulars; sort by distinct continent count desc.",highlight:!0}],edgesZh:[`WCA 没有南极洲比赛,所以 6 是真实上限;能跑满 6 的人极少。`,"同一个洲多个国家不会因为多了一个洲 count —— 用 `continent_id` 而不是 `country_id`。","`_Multiple Continents` 排除后,洲际 FMC 邮寄赛对该指标没贡献。"],edgesEn:[`No Antarctica comps, so 6 is the real ceiling; almost nobody pulls all 6.`,"Multiple countries on the same continent don't add — we count `continent_id`, not `country_id`.","With `_Multiple Continents` excluded, continental-FMC mail-ins contribute nothing here."],related:[{id:`most_visited_countries`,titleZh:`去过最多国家`,titleEn:`Most countries visited`,hintZh:`更细粒度`,hintEn:`Finer granularity`},{id:`most_competitions_abroad`,titleZh:`海外参赛最多`,titleEn:`Most comps abroad`,hintZh:`场次 vs 大洲数`,hintEn:`Comps vs continents`},{id:`longest_competitions_path`,titleZh:`最长比赛路径`,titleEn:`Longest path`,hintZh:`距离视角的版本`,hintEn:`Distance angle`},{id:`most_visited_continents`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`具体大洲数 + 选手`,hintEn:`Continent counts + cubers`}]},most_visited_countries:{id:`most_visited_countries`,titleZh:`去过最多国家参赛`,titleEn:`Most countries visited`,badgeZh:`选手`,badgeEn:`Person`,introZh:['比"最多大洲"细一档:`COUNT(DISTINCT competition.country_id)`。上榜 top 100。同样排除 8 个洲际 FMC 虚拟国家,保证每条数都是真国家。',`跟"海外参赛"不一样 —— 这里包括**本国**(本国 = 1 个国家),而"海外"完全排除本国。所以一个完全本地化的选手在这条至少有 1 个国家。`],introEn:["One step finer than continents: `COUNT(DISTINCT competition.country_id)`. Top 100. Same 8 continental-FMC virtual countries excluded.",`Differs from "comps abroad" — this **includes home country** (home = 1 country), while "abroad" excludes it. A fully-local cuber still has at least 1 country here.`],stats:[{value:`Top 100`,labelZh:`榜单深度`,labelEn:`Leaderboard depth`,hintZh:`按国家数降序`,hintEn:`Sorted by country count desc`},{value:`COUNT(DISTINCT)`,labelZh:`聚合`,labelEn:`Aggregate`,hintZh:"`competition.country_id` 去重数",hintEn:"Distinct `competition.country_id`"},{value:`排除 8 虚拟国`,labelZh:`过滤`,labelEn:`Filter`,hintZh:`洲际 FMC 占位国`,hintEn:`Continental-FMC placeholders`}],sourceZh:["`results` join `competitions`,过滤掉 8 个虚拟国;`GROUP BY person_id, COUNT DISTINCT competition.country_id`。注意是**比赛国**,跟选手国籍无关。"],sourceEn:["`results` joined to `competitions`, 8 virtual countries filtered; `GROUP BY person_id, COUNT DISTINCT competition.country_id`. Note **comp country**, not cuber's registered country."],sourceCode:{lang:`sql`,body:`SELECT person_id, COUNT(DISTINCT competition.country_id) visited_countries
FROM results result
JOIN competitions competition ON competition.id = competition_id
WHERE competition.country_id NOT IN
      ('XA','XE','XF','XM','XN','XO','XS','XW')
GROUP BY person_id
ORDER BY visited_countries DESC
LIMIT 100`},steps:[{titleZh:`比赛国 distinct`,titleEn:`Distinct comp countries`,bodyZh:"`competition.country_id` 是 ISO 国家码;同一国家多场比赛只算 1。",bodyEn:"`competition.country_id` is ISO country code; multiple comps in one country count as 1."},{titleZh:`排虚拟洲际国`,titleEn:`Drop virtual continents`,bodyZh:`XA/XE/XF/XM/XN/XO/XS/XW 8 个洲际 FMC 用 placeholder,不是真国家,排掉。`,bodyEn:`XA/XE/XF/XM/XN/XO/XS/XW are continental-FMC placeholders, not real countries — dropped.`},{titleZh:`按人聚合 + 降序`,titleEn:`Aggregate per person + sort`,bodyZh:"`GROUP BY person_id`,`COUNT DISTINCT`,降序 LIMIT 100;外层 join `persons` 拿名字。",bodyEn:"`GROUP BY person_id`, `COUNT DISTINCT`, sort desc, LIMIT 100; outer join `persons` for names.",highlight:!0}],edgesZh:[`本国算作 1 个国家 —— 跟"海外参赛"指标互补(海外 = 总国家数 - 1,前提是去过本国)。`,"没有 `HAVING` 阈值 —— 直接拉 top 100,因为只去过 1 国的人太多,排序后自然落到尾部。",`改国籍前后比赛地都按"比赛 country_id"算,所以不受 selfish 改名影响。`],edgesEn:[`Home country counts as 1 — complementary to "comps abroad" (abroad = total countries − 1, given you've competed at home).`,"No `HAVING` cutoff — straight to top 100; people who've only competed in 1 country naturally drop to the tail.","Comp country is invariant under cuber's country-change events — uses `competition.country_id` not `result.country_id`."],related:[{id:`most_visited_continents`,titleZh:`去过最多大洲`,titleEn:`Most continents visited`,hintZh:`粗一档`,hintEn:`Coarser version`},{id:`most_competitions_abroad`,titleZh:`海外参赛最多`,titleEn:`Most comps abroad`,hintZh:`场次 vs 国家数`,hintEn:`Comps vs countries`},{id:`longest_competitions_path`,titleZh:`最长比赛路径`,titleEn:`Longest path`,hintZh:`距离视角`,hintEn:`Distance angle`},{id:`most_visited_countries`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`具体国家数 + 选手`,hintEn:`Country counts + cubers`}]},name_parts_count:{id:`name_parts_count`,titleZh:`姓名词数统计`,titleEn:`Name parts count`,badgeZh:`人口学`,badgeEn:`Demographic`,introZh:["一个看起来很冷门、实际超有信息量的人口学指标:把每个 WCA 注册名按空格拆,数有几个 part。中文括号本地化名 `(耿暄一)` 在拆分前用正则 `/ \\(.*\\)/` 扔掉。",`英语圈名字大多 2-3 part,西班牙语系 4 part 很常见(双名 + 双姓),阿拉伯语系传统命名能上 5 + part。所以这条指标也是"哪些 part 数典型来自哪些国家"的 fingerprint。`],introEn:["A seemingly niche but highly informative demographic stat: split each WCA registered name on whitespace, count parts. Parenthesized local names like `(耿暄一)` are stripped via regex `/ \\(.*\\)/` before splitting.",`English-speaking names cluster at 2-3 parts; Spanish-speaking traditions often hit 4 (double given + double family); Arabic-traditional names can reach 5+. The metric doubles as a country-origin fingerprint per part count.`],stats:[{value:`空格`,labelZh:`分隔符`,labelEn:`Separator`,hintZh:"`name.split(' ')`",hintEn:"`name.split(' ')`"},{value:`Top 5`,labelZh:`每段国家数`,labelEn:`Top countries shown`,hintZh:`每个 part-count 的 top 5 国家`,hintEn:`Per part-count, top 5 countries`},{value:"`sub_id = 1`",labelZh:`身份过滤`,labelEn:`Identity filter`,hintZh:`主行,改名 / 副身份不算`,hintEn:`Primary row only`}],sourceZh:["只用 `persons` 表:`name` + `country_id` join `countries.name`,过 `sub_id = 1`。SQL 拉全表,TS 端拆词、分组、统计。"],sourceEn:["Only `persons`: `name` + `country_id` joined to `countries.name`, filtered `sub_id = 1`. SQL pulls the full table; TS handles splitting, grouping, counting."],sourceCode:{lang:`sql`,body:`SELECT person.name, country.name country_name
FROM persons person
JOIN countries country ON country.id = country_id
WHERE sub_id = 1`},steps:[{titleZh:`剥本地名 + split`,titleEn:`Strip local name + split`,bodyZh:`\`name.replace(/ \\(.*\\)/, '')\` 去括号本地名 → \`split(' ')\` 数 part。"Xuanyi Geng (耿暄一)" → ["Xuanyi", "Geng"] = 2 part。`,bodyEn:`\`name.replace(/ \\(.*\\)/, '')\` strips parenthesized local → \`split(' ')\` counts parts. "Xuanyi Geng (耿暄一)" → ["Xuanyi", "Geng"] = 2 parts.`},{titleZh:`按 part 数分桶`,titleEn:`Bucket by part count`,bodyZh:`Map<partsCount, Person[]>;同时记国家名,便于第二步聚合。`,bodyEn:`Map<partsCount, Person[]>; carry country alongside for next aggregation.`},{titleZh:`每桶 top 5 国家`,titleEn:`Top 5 countries per bucket`,bodyZh:"对每 partsCount,内部按国家计数;取 top 5,显示 `Country (x.xx %)` —— 占该 partsCount 群体的比例。",bodyEn:"Within each partsCount, count by country; take top 5, formatted `Country (x.xx %)` — share of that part-count subset."},{titleZh:`按 partsCount 升序`,titleEn:`Sort by partsCount asc`,bodyZh:`1 part(单名)→ 2 → 3 → ...,自然地分布观感。`,bodyEn:`1 part (mononym) → 2 → 3 → ..., natural distribution feel.`,highlight:!0}],edgesZh:[`括号本地化只剥**带前导空格**的括号 —— "Name(Local)" 不带空格不剥;实际 WCA 都规范化为 "Name (Local)",安全。`,`"O'Brien"、"de la Rosa" 这种含撇号 / 连字符 / 前缀词的 part 数会偏多 —— 没语言学拆分,纯空格切。`,"`split(' ')` 在多空格上会产生空 part —— WCA 数据干净,实际不会触发。"],edgesEn:[`Local-name strip only removes parens **with a leading space** — "Name(Local)" without space stays; WCA always normalizes to "Name (Local)" though, safe.`,`"O'Brien", "de la Rosa" with apostrophes / hyphens / particles will inflate counts — pure whitespace split, no linguistic awareness.`,"`split(' ')` on multiple spaces yields empty parts — WCA data is clean enough that this never fires in practice."],related:[{id:`most_visited_countries`,titleZh:`去过最多国家`,titleEn:`Most countries visited`,hintZh:`另一个跨国维度`,hintEn:`Another cross-country angle`},{id:`name_parts_count`,toStat:!0,titleZh:`查看实时分布`,titleEn:`Jump to live distribution`,hintZh:`各 part 数 + top 国家`,hintEn:`Each part-count + top countries`}]},shortest_time_to_reach_milestone_in_comps_count:{id:`shortest_time_to_reach_milestone_in_comps_count`,titleZh:`最短时间达到比赛数里程碑`,titleEn:`Shortest time to N comps milestone`,badgeZh:`选手`,badgeEn:`Person`,introZh:[`一个 SQL 拉所有 (选手, 场, 日期) 排序,TS 端对 9 个里程碑(5/10/25/50/100/150/200/250/300)各算一次:**第 N 场比赛日 − 首场比赛日 + 1 天**。`,'是 `competitions_per_year_by_person` 的"短窗口"对照 —— 那条是终身年化率,这条只看"前 N 场用了多少天",更能体现"冲量期"的烈度。'],introEn:[`One SQL pulls all (cuber, comp, date) sorted; TS computes 9 milestones (5/10/25/50/100/150/200/250/300) each: **date of N-th comp − date of 1st + 1 day**.`,"Short-window counterpart to `competitions_per_year_by_person` — that one is lifetime annualized; this one captures pure burn-rate during the run-up."],stats:[{value:`9`,labelZh:`里程碑数`,labelEn:`Milestones`,hintZh:`5/10/25/50/100/150/200/250/300`,hintEn:`5/10/25/50/100/150/200/250/300`},{value:`Top 20`,labelZh:`每段深度`,labelEn:`Per-section depth`,hintZh:`每个里程碑独立 top 20`,hintEn:`Each milestone separate top 20`},{value:`+1 day`,labelZh:`日数补正`,labelEn:`Day offset`,hintZh:`首尾都计入`,hintEn:`Inclusive of both endpoints`}],sourceZh:["`results` 取 `DISTINCT (person_id, competition_id, start_date)`,按 `start_date` 升序。TS 端按选手分组拿到日期数组 `dates[]`;`dates[count-1]` 是第 N 场,与 `dates[0]` 求差。"],sourceEn:["`results` with `DISTINCT (person_id, competition_id, start_date)`, sorted by `start_date`. TS groups by person to get `dates[]`; `dates[count-1]` is the N-th comp, diffed against `dates[0]`."],sourceCode:{lang:`sql`,body:`SELECT person_link, start_date
FROM (SELECT DISTINCT person_id, competition_id, start_date
      FROM results
      JOIN competitions ON competitions.id = competition_id) cd
JOIN persons person ON person.wca_id = person_id AND sub_id = 1
ORDER BY start_date`},steps:[{titleZh:`拉时间线`,titleEn:`Pull the timeline`,bodyZh:`SQL 端只输出 (人, 日期) 行,内部按日期升序 —— 后续 TS 端 push 到数组天然就是时序。`,bodyEn:`SQL only emits (person, date) rows, internally date-sorted — TS just pushes into an array and gets time order for free.`},{titleZh:`按选手收集 dates[]`,titleEn:`Bucket dates[] per person`,bodyZh:`Map<person, Date[]>;每个数组长度 = 该选手的总场次。`,bodyEn:`Map<person, Date[]>; array length = total comps for that cuber.`},{titleZh:`逐里程碑算天数`,titleEn:`Days per milestone`,bodyZh:'`dates.length >= count` 才参与;`days = floor((dates[count-1] − dates[0]) / 24h) + 1`(+1 让"同日 2 场"得 1 而非 0)。',bodyEn:'`dates.length >= count` to qualify; `days = floor((dates[count-1] − dates[0]) / 24h) + 1` (+1 so "2 comps same day" = 1, not 0).'},{titleZh:`每里程碑 top 20`,titleEn:`Top 20 per milestone`,bodyZh:`9 个 GroupedStatistic section,每段独立 sort 升序取 20。`,bodyEn:`9 GroupedStatistic sections, each sorted asc + top 20.`,highlight:!0}],edgesZh:['"+1" 让首尾同日(理论上 5 场同日)算 1 天而不是 0 —— 跟 `competitions_per_year_by_person` 用 365.25 的精确口径不同。',`里程碑顺序按 count 升序输出(5 → 300);UI 端要展示完整光谱。`,`部分项目特别多 / 高频选手在 5 场里程碑上几乎全是同周末连开 —— top 20 经常 1-3 天。`],edgesEn:['"+1" makes same-day endpoints (theoretical 5 comps in a day) = 1 day not 0 — different from `competitions_per_year_by_person` which uses precise 365.25.',`Milestones output asc by count (5 → 300); UI displays the full spectrum.`,`For 5-comp milestone, high-cadence cubers often back-to-back the same weekend — top 20 commonly 1-3 days.`],related:[{id:`competitions_per_year_by_person`,titleZh:`年均频率`,titleEn:`Annual frequency`,hintZh:`终身视角的对照`,hintEn:`Lifetime counterpart`},{id:`most_attended_competitions_in_single_month`,titleZh:`单月最多场次`,titleEn:`Most in a month`,hintZh:`更短窗口`,hintEn:`Shorter window`},{id:`most_attended_competitions_in_single_week`,titleZh:`单周最多场次`,titleEn:`Most in a week`,hintZh:`最短窗口`,hintEn:`Shortest window`},{id:`shortest_time_to_reach_milestone_in_comps_count`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`9 个里程碑切换`,hintEn:`Toggle across 9 milestones`}]},shortest_time_to_get_all_singles:{id:`shortest_time_to_get_all_singles`,titleZh:`最短时间获得所有项目单次成绩`,titleEn:`Shortest time to get all singles`,badgeZh:`选手`,badgeEn:`Person`,introZh:['"全 single"(每个**当前官方**项目都拿过一个有效 single)选手的最短达成时间。子查询用 `events.rank < 900` 滤掉退役项目,`COUNT(DISTINCT event_id) = (SELECT COUNT(*) FROM events WHERE rank<900)` 锁定候选人。',`榜首通常都是一年内全打卡的"全能加速党";最快可在数月内完成,但需要参加涵盖罕见项目(7x7 / FMC / Multi-Blind)的比赛 —— 地区少了根本凑不齐。`],introEn:["Cubers who got a valid single in **every current official event**, sorted by time-to-completion. Subquery filters retired events via `events.rank < 900`, then `COUNT(DISTINCT event_id) = (SELECT COUNT(*) FROM events WHERE rank<900)` locks candidates.",`Top spots usually finish within a year; the absolute fastest take months, but you need comps that hold rare events (7x7 / FMC / Multi-Blind) — sparse regions just can't close the set.`],stats:[{value:`rank < 900`,labelZh:`官方项目过滤`,labelEn:`Official filter`,hintZh:`退役项目 rank ≥ 900`,hintEn:`Retired events at rank ≥ 900`},{value:`best > 0`,labelZh:`有效 single`,labelEn:`Valid single`,hintZh:`DNF 不算`,hintEn:`DNF doesn't count`},{value:`首场 → 最后项目`,labelZh:`时间口径`,labelEn:`Time span`,hintZh:`不止 333 入坑那天`,hintEn:`Not just 3x3 debut`}],sourceZh:['子查询锁定"全 single"选手 → 外层 join 全部 results,逐 (人, 项目) 找最早 `best > 0` 的日期。最后一个项目完成的日期减去首场比赛日期即天数。',"历史上 `Object.keys(EVENTS).length` 用 21(含退役)的写法会让 HAVING 永远不满足 → 改用 `(SELECT COUNT(*) FROM events WHERE rank<900)` 子查询匹配 SQL 端过滤。"],sourceEn:['Subquery isolates "all-singles" cubers → outer join pulls all their results, finds the earliest `best > 0` per (person, event). Last event\'s completion date minus first comp date = days.',"Historical note: using `Object.keys(EVENTS).length = 21` (with retired) would never satisfy the HAVING — switched to `(SELECT COUNT(*) FROM events WHERE rank<900)` subquery to match the SQL filter."],sourceCode:{lang:`sql`,body:`SELECT event_id, person_link, start_date, best
FROM (SELECT person_id FROM results
      JOIN events ON events.id = event_id
      WHERE rank < 900 AND best > 0
      GROUP BY person_id
      HAVING COUNT(DISTINCT event_id) =
             (SELECT COUNT(*) FROM events WHERE rank<900)) all_events
JOIN results r ON r.person_id = all_events.person_id
JOIN persons person ON wca_id = r.person_id AND sub_id = 1
JOIN competitions ON id = competition_id
ORDER BY start_date`},steps:[{titleZh:`锁"全 single"候选人`,titleEn:`Lock "all-singles" candidates`,bodyZh:"子查询 `HAVING COUNT(DISTINCT event_id) = (SELECT COUNT(*) FROM events WHERE rank<900)` —— 当前官方项目全部至少一个 `best > 0`。",bodyEn:"Subquery `HAVING COUNT(DISTINCT event_id) = (SELECT COUNT(*) FROM events WHERE rank<900)` — at least one `best > 0` in every current event."},{titleZh:`拉全 results 时间线`,titleEn:`Pull full results timeline`,bodyZh:"外层 join 候选人的所有 results;TS 端按选手分组,首条即首场比赛(`start_date` 升序保证)。",bodyEn:"Outer pulls every result for candidates; TS groups by person, first row = first comp (`start_date` ascending guarantees)."},{titleZh:`每项目最早成功日`,titleEn:`Earliest success per event`,bodyZh:"Map<eventId, Date>:遍历 personRows,只保留 `best > 0` 的最早日期。最后一个完成项目的日期是 Map 里最大值。",bodyEn:"Map<eventId, Date>: iterate personRows, keep earliest `best > 0` date per event. Last-completed event = max value in the Map."},{titleZh:`日数差升序`,titleEn:`Day-diff ascending`,bodyZh:"`days = floor((lastEventDate − firstDate) / 24h)`;按天数升序,无 LIMIT(榜单不裁,UI 端分页)。",bodyEn:"`days = floor((lastEventDate − firstDate) / 24h)`; sort asc, no LIMIT (UI paginates).",highlight:!0}],edgesZh:[`"首场比赛"含所有项目 —— 你打过 OH 再回头补 3x3 single,起算点是 OH 那场。`,`只看**当前**官方项目;退役项目(magic / clock 老规则等,rank ≥ 900)不要求。`,"`best > 0` 排 DNF,因此 DNF 拼到最后凑数不算 —— 真要成功一次。"],edgesEn:[`"First comp" spans all events — if you did OH first then circled back for 3x3 single, the clock starts at the OH comp.`,`Only **current** official events; retired events (old magic / clock-rule, rank ≥ 900) not required.`,"`best > 0` excludes DNFs — last-event DNFs don't patch the set, must be a real success."],related:[{id:`shortest_time_to_get_all_singles_and_averages`,titleZh:`全 single + 全 average`,titleEn:`All singles + averages`,hintZh:`加 average 维度,门槛更高`,hintEn:`Adds average dimension, harder`},{id:`longest_time_to_sub_10`,titleZh:`最长 sub-10 时长`,titleEn:`Longest time to sub-10`,hintZh:`反方向 —— "最慢"故事`,hintEn:`Opposite — "slowest" story`},{id:`shortest_time_to_reach_milestone_in_comps_count`,titleZh:`最快达比赛里程碑`,titleEn:`Fastest to N comps`,hintZh:`另一种"最快"达成`,hintEn:`Another "fastest to" stat`},{id:`shortest_time_to_get_all_singles`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`具体天数 + 选手`,hintEn:`Days + cubers`}]},shortest_time_to_get_all_singles_and_averages:{id:`shortest_time_to_get_all_singles_and_averages`,titleZh:`最短时间获得所有项目的单次和平均成绩`,titleEn:`Shortest time to all singles + averages`,badgeZh:`选手`,badgeEn:`Person`,introZh:['比"全 single"更狠的成就:**还要补齐每个有平均的项目的 average**。3x3 Multi-Blind 没 average,所以 average 集合的目标值是 `当前项目数 − 1`。',`凑 average 比凑 single 难 —— BLD 系列要打过 ≥ 2 次成功才能算"有 average"(成功 / 3 attempts);MBLD 直接不参与 average 维度。所以这个榜的天数普遍是 single 榜的 1.5-3 倍。`],introEn:['A harsher version of "all singles": **also fill every event-with-average\'s average**. 3x3 Multi-Blind has no average, so the average target is `current event count − 1`.',`Averages are harder than singles — BLD events need ≥ 2 successes (success / 3 attempts) to register an average; MBLD opts out of the average dimension entirely. Day counts here run 1.5-3× the singles board.`],stats:[{value:`singles N + avg (N − 1)`,labelZh:`目标`,labelEn:`Target`,hintZh:`MBLD 无 average`,hintEn:`MBLD has no average`},{value:`best > 0 / average > 0`,labelZh:`有效门槛`,labelEn:`Validity`,hintZh:`各自最早成功日`,hintEn:`Earliest success per type`},{value:`取最大日期`,labelZh:`收尾日`,labelEn:`Closing date`,hintZh:`singles 和 averages 中最晚的`,hintEn:`Latest across both sets`}],sourceZh:["双子查询交 join:① 全 single 候选(`COUNT DISTINCT event_id = N`);② 全 average 候选(`COUNT DISTINCT event_id = N − 1`,因 MBLD 无 avg)。两条都满足 → 拉全 results 进入 TS 计算。"],sourceEn:["Two subqueries intersected: ① all-singles candidates (`COUNT DISTINCT event_id = N`); ② all-averages candidates (`COUNT DISTINCT event_id = N − 1` since MBLD has no avg). Both pass → outer pulls all results into TS."],sourceCode:{lang:`sql`,body:`SELECT event_id, person_link, start_date, best, average
FROM (SELECT person_id FROM results JOIN events ON id=event_id
      WHERE rank<900 AND best>0
      GROUP BY person_id
      HAVING COUNT(DISTINCT event_id)=
             (SELECT COUNT(*) FROM events WHERE rank<900)) singles
JOIN (SELECT person_id FROM results JOIN events ON id=event_id
      WHERE rank<900 AND average>0
      GROUP BY person_id
      HAVING COUNT(DISTINCT event_id)=
             (SELECT COUNT(*) FROM events WHERE rank<900) - 1) avgs USING(person_id)
JOIN results r USING(person_id)
JOIN persons person ON wca_id=person_id AND sub_id=1
JOIN competitions ON id=competition_id
ORDER BY start_date`},steps:[{titleZh:`双子查询交集`,titleEn:`Intersect two subqueries`,bodyZh:'同时满足"全 single"和"全 average"才进入候选;`= N − 1` 是为了排除 MBLD 没 average 的客观事实。',bodyEn:'Must satisfy both "all singles" and "all averages" subqueries; `= N − 1` excludes MBLD which has no avg by definition.'},{titleZh:`收集 best + average 日期`,titleEn:`Collect best + average dates`,bodyZh:"TS 端遍历两遍 personRows:第一遍按 `best > 0` 取每项目最早日;第二遍按 `average > 0`。两组日期数组合并。",bodyEn:"TS iterates personRows twice: once collecting earliest `best > 0` per event, once for `average > 0`. Concatenate the two date lists."},{titleZh:`最大日 − 首场日`,titleEn:`Max date − first date`,bodyZh:`firstSuccesses 数组里取最大值即"最后一块拼图"完成日;减去首场比赛日。`,bodyEn:`max of firstSuccesses = "last piece" date; subtract first comp date.`},{titleZh:`升序展示`,titleEn:`Sort asc`,bodyZh:`按天数升序;无 LIMIT。这条天数通常远高于 single 榜,因为 average 拼图含 BLD 系列。`,bodyEn:`Days asc, no LIMIT. Counts typically far above the singles board because the average set includes BLD events.`,highlight:!0}],edgesZh:["BLD 系列(`333bf/444bf/555bf`)只有 3 attempts,要 ≥ 2 成功才会产生 average → 这一关常常卡住挑战者数月。",'MBLD 因没 average 完全不在 average 候选条件里(`N − 1`);3x3 FMC 有 average("三次平均"),要凑齐。',"`firstSuccesses` 含同事件 single + average 各自最早日 —— 两者日期可不同(先 single 后 average 很常见)。"],edgesEn:["BLD events (`333bf/444bf/555bf`) get only 3 attempts each, need ≥ 2 successes to log an average — this gate stalls many cubers for months.",'MBLD opts out entirely (`N − 1` target); 3x3 FMC averages ("mo3") are part of the set.',"`firstSuccesses` holds per-event single AND average earliest dates — they often differ (single first, average later)."],related:[{id:`shortest_time_to_get_all_singles`,titleZh:`最快全 single`,titleEn:`Fastest to all singles`,hintZh:`更松的目标`,hintEn:`Looser target`},{id:`most_solves_before_bld_success`,titleZh:`盲拧成功前尝试数`,titleEn:`Solves before BLD success`,hintZh:`BLD average 卡门坎`,hintEn:`BLD average is the choke point`},{id:`longest_time_to_sub_10`,titleZh:`最长 sub-10 时长`,titleEn:`Longest time to sub-10`,hintZh:`反面 —— "最慢"`,hintEn:`Inverse — "slowest"`},{id:`shortest_time_to_get_all_singles_and_averages`,toStat:!0,titleZh:`查看实时榜`,titleEn:`Jump to live ranking`,hintZh:`具体天数 + 选手`,hintEn:`Days + cubers`}]}},d={average_event_count_by_competition:{id:`average_event_count_by_competition`,titleZh:`每场比赛平均参赛项目数`,titleEn:`Average event count by competition`,badgeZh:`赛事`,badgeEn:`Competition`,introZh:[`每场比赛里,每位选手平均报名/参赛了多少个项目?把比赛内所有选手的"参赛项目数"做算术平均,就得到这一项指标。`,`数值高一般意味着两件事:一是赛程齐(项目摆得多),二是选手愿意报(多项目热情高)。小邀请赛 / 单项目专场会偏低,综合性大赛偏高。`],introEn:[`For each competition, how many events did its competitors participate in on average? Arithmetic mean of "events run per person" across everyone at that comp.`,`A high value usually means two things at once: the schedule was broad, and competitors actually opted into many of those events. Single-event or invitational comps land low; big general-purpose meets land high.`],stats:[{value:`AVG`,labelZh:`聚合方式`,labelEn:`Aggregation`,hintZh:`算术平均`,hintEn:`Arithmetic mean`},{value:`top 100`,labelZh:`榜单长度`,labelEn:`Leaderboard size`,hintZh:`按平均项目数降序`,hintEn:`Top 100 by mean event count`},{value:`2 位小数`,labelZh:`精度`,labelEn:`Precision`,hintZh:`toFixed(2)`,hintEn:`toFixed(2)`},{value:`4 列`,labelZh:`表头`,labelEn:`Columns`,hintZh:`比赛 / 均值 / 选手数 / 国家`,hintEn:`Competition / mean / competitors / country`}],sourceZh:["只读 `results` 表 —— 一行 = 某选手在某比赛某项目的某轮成绩。先把 `(competition_id, person_id)` 分组,`COUNT(DISTINCT event_id)` 得到每人在该比赛的项目数;再外层按比赛聚合一次,取 `AVG`。"],sourceEn:["Reads only the `results` table — one row per (person, competition, event, round). Inner group by `(competition_id, person_id)` with `COUNT(DISTINCT event_id)` gives per-person event count at that comp; outer group by competition takes the `AVG`."],sourceCode:{lang:`sql`,body:`SELECT
  competition.cell_name,
  AVG(event_count) average_event_count,
  COUNT(*) competitors,
  country.name country
FROM (
  SELECT competition_id, person_id,
         COUNT(DISTINCT event_id) event_count
  FROM results
  GROUP BY competition_id, person_id
) AS competitors_with_event_count
JOIN competitions competition ON competition.id = competition_id
JOIN countries country ON country.id = competition.country_id
GROUP BY competition_id
ORDER BY average_event_count DESC
LIMIT 100;`},steps:[{titleZh:`按 (比赛, 选手) 分组`,titleEn:`Group by (competition, person)`,bodyZh:'在 `results` 上做内层 GROUP BY `(competition_id, person_id)`,行数 = 每场比赛参与的"选手 × 1"。',bodyEn:"Inner `GROUP BY (competition_id, person_id)` on `results`. Each output row = one competitor at one comp."},{titleZh:`数项目数`,titleEn:`Count distinct events`,bodyZh:"组内 `COUNT(DISTINCT event_id)` —— 同一人同一项目多轮只算一次。",bodyEn:"`COUNT(DISTINCT event_id)` per group — multiple rounds of the same event collapse to one."},{titleZh:`外层按比赛聚合`,titleEn:`Outer group by competition`,bodyZh:"外层把内层结果按 `competition_id` 再分一次组,取 `AVG(event_count)` 作为该比赛的平均项目数,同时 `COUNT(*)` 出选手数。",bodyEn:"Outer `GROUP BY competition_id` over the inner result, taking `AVG(event_count)` as the mean and `COUNT(*)` as competitor count."},{titleZh:`挂上国家 / 比赛链接`,titleEn:`Join country and comp link`,bodyZh:"JOIN `competitions` 拿 `cell_name` 做链接显示,JOIN `countries` 拿国名作右侧列。",bodyEn:"JOIN `competitions` for the display `cell_name` (used in the WCA link) and `countries` for the right-most column."},{titleZh:`降序取前 100`,titleEn:`Sort and take top 100`,bodyZh:"`ORDER BY average_event_count DESC LIMIT 100`,均值前留 2 位小数。",bodyEn:"`ORDER BY average_event_count DESC LIMIT 100`; the mean is rendered with two decimals.",highlight:!0}],edgesZh:["没报名但出现在 `results` 表的轮次才算 —— 报了名但没去现场(DNS 整轮)的选手不计入。",`"项目数"是 distinct event,不是 distinct 轮数 —— 一个人在 3x3 打了 3 轮和 1 轮算同等贡献。`,`榜首通常是早期小型综合赛 —— 项目数稳,人少时均值容易往上推。`],edgesEn:["Only rounds present in `results` count — registered-but-no-show competitors (full DNS) do not contribute.",'"Event count" is `DISTINCT event_id`, not round count — someone doing 3 rounds of 3x3 contributes the same as someone doing 1.',`The top of the board is usually small early-era multi-event meets — broad schedule plus low headcount pushes the mean up.`],related:[{id:`competitions_per_year_by_country`,titleZh:`每年每国比赛数`,titleEn:`Competitions per year by country`,hintZh:`另一种"赛事密度"切片`,hintEn:`Sibling slice of competition density`},{id:`most_records_at_single_competition`,titleZh:`单场比赛最多纪录`,titleEn:`Most records at a single competition`,hintZh:`另一种"单场强度"指标`,hintEn:`Another single-comp intensity metric`},{id:`average_event_count_by_competition`,toStat:!0,titleZh:`查看实时榜单`,titleEn:`Jump to live data`,hintZh:`完整 top 100`,hintEn:`Full top 100`}]},competition_days_count_by_region:{id:`competition_days_count_by_region`,titleZh:`按区域统计比赛天数`,titleEn:`Competition days count by region`,badgeZh:`赛事`,badgeEn:`Competition`,introZh:['每场比赛持续 1 至若干天(`end_date - start_date + 1`)。把所有比赛的天数按"地区"分桶,算每个地区的**平均比赛天长**和总场次,得到一张三档(World / Continents / Countries)分组表。',`直观回答:同样办比赛,哪些地方更倾向于多日赛?在多人多项目的大赛区里,多日是标配;小区域 / 项目少的地方一日完结。`],introEn:["Each comp spans 1 or more days (`end_date - start_date + 1`). Bucket every comp by region, take the **mean days per comp** and total count — output is a three-tier grouped table (World / Continents / Countries).",`Cleanly answers: where do organizers lean toward multi-day comps? Big multi-event regions almost always need >1 day; small regions or single-event meets wrap in a day.`],stats:[{value:`3 档`,labelZh:`分组维度`,labelEn:`Group tiers`,hintZh:`World / Continents / Countries`,hintEn:`World / Continents / Countries`},{value:`+1 天`,labelZh:`区间口径`,labelEn:`Range convention`,hintZh:`含首尾两端`,hintEn:`Inclusive of both ends`},{value:`排除虚拟`,labelZh:`排除项`,labelEn:`Excludes`,hintZh:`7 个 X* 虚拟国家 + 多洲`,hintEn:`7 X* virtual countries + multi-continent`},{value:`mean ↓`,labelZh:`排序`,labelEn:`Sort`,hintZh:`组内按均值降序`,hintEn:`Within tier by mean DESC`}],sourceZh:["只读 `competitions` 表,逐行算 `DATEDIFF(end_date, start_date) + 1` 作为比赛天数;JOIN `countries` / `continents` 取人话名字。过滤掉 7 个 X 开头的虚拟国家代码(`XA/XE/XF/XM/XN/XO/XS/XW`)和 `_Multiple Continents`。"],sourceEn:["Reads only `competitions`, computing `DATEDIFF(end_date, start_date) + 1` per row. Joins `countries` / `continents` for display names. Filters the 7 virtual `X*` country codes (`XA/XE/XF/XM/XN/XO/XS/XW`) and `_Multiple Continents`."],sourceCode:{lang:`sql`,body:`SELECT
  (DATEDIFF(end_date, start_date) + 1) days,
  country.name country,
  continent.name continent
FROM competitions
JOIN countries country ON country.id = country_id
JOIN continents continent ON continent.id = continent_id
WHERE country_id NOT IN ('XA','XE','XF','XM','XN','XO','XS','XW')
  AND continent_id != "_Multiple Continents";`},steps:[{titleZh:`逐场算天数`,titleEn:`Compute per-comp days`,bodyZh:"`days = DATEDIFF(end_date, start_date) + 1`。同日比赛 = 1 天,跨周末两日赛 = 2 天。",bodyEn:"`days = DATEDIFF(end_date, start_date) + 1`. Same-day = 1, weekend two-day = 2."},{titleZh:`过滤虚拟地区`,titleEn:`Filter virtual regions`,bodyZh:"WCA dump 里有 7 个 `X*` 占位国家代码(给一些跨域比赛用的虚拟值)和 `_Multiple Continents`,统计前先剔掉。",bodyEn:"The WCA dump uses 7 `X*` placeholder country codes (for cross-region edge cases) plus `_Multiple Continents` — drop these before tallying."},{titleZh:`三档 group 键`,titleEn:`Three group keys`,bodyZh:"TypeScript 端用三个 `groupFn`:`World` 全合并、`Continents` 按 `continent.name`、`Countries` 按 `country.name`。",bodyEn:"TS-side three `groupFn`s: `World` collapses everything, `Continents` keys on `continent.name`, `Countries` on `country.name`."},{titleZh:`组内累加`,titleEn:`Accumulate within group`,bodyZh:"每组记 `totalDays` 和 `count`;`mean = totalDays / count`。",bodyEn:"Each group tracks `totalDays` and `count`; `mean = totalDays / count`."},{titleZh:`排序并输出三表`,titleEn:`Sort and emit three tables`,bodyZh:"组内按 `mean` 降序排,同值时按地区名 `localeCompare`;输出 `[ header, rows ]` 元组列表,前端三个 tab 渲染。",bodyEn:"Within each tier sort by `mean` DESC, breaking ties by region name `localeCompare`. Emit a list of `[header, rows]` tuples — the UI renders one tab per tier.",highlight:!0}],edgesZh:[`"天数"含首尾两端 —— 单日赛是 1,不是 0。`,"`country_id NOT IN (...)` 排除 7 个 X 代码,这些是 WCA 给跨域赛用的虚拟国别,统计上没有意义。",`World 档只有 1 行,均值是全球所有比赛的总天数除以总场次。`],edgesEn:[`"Days" is inclusive of both endpoints — a one-day comp is 1, not 0.`,"The `country_id NOT IN (...)` clause drops 7 virtual codes WCA uses for multi-country edge cases — they are not meaningful in regional stats.",`The World tier has a single row — the mean is total days across all comps divided by total comp count.`],related:[{id:`competitions_per_year_by_country`,titleZh:`每年每国比赛数`,titleEn:`Competitions per year by country`,hintZh:`按时间维度看赛事密度`,hintEn:`Comp density on the time axis`},{id:`competitions_count_by_week`,titleZh:`每周比赛数量`,titleEn:`Competitions count by week`,hintZh:`同一份赛事数据另一种切法`,hintEn:`Same data sliced by ISO week`},{id:`average_event_count_by_competition`,titleZh:`每场比赛平均项目数`,titleEn:`Average event count by competition`,hintZh:`与天数强相关`,hintEn:`Strongly correlated with days`},{id:`competition_days_count_by_region`,toStat:!0,titleZh:`查看实时榜单`,titleEn:`Jump to live data`,hintZh:`三档地区切换`,hintEn:`Three-tier region switcher`}]},competitions_count_by_week:{id:`competitions_count_by_week`,titleZh:`每周比赛数量`,titleEn:`Competitions count by week`,badgeZh:`赛事`,badgeEn:`Competition`,introZh:['把每场比赛归到它 `start_date` 所在的 ISO 周(周一开始、周日结束),数一数那一周全球开了多少场。降序排,能直接读出历史上最忙的几个"魔方周末"。',`通常榜首集中在每年 6 月底到 8 月初(暑期 + WC 年的 Worlds 前夕),个别极端值出现在区域性大型集中办赛的周。`],introEn:['Bucket every comp by the ISO week containing its `start_date` (Monday → Sunday). Count comps per week, sort DESC — the top of the board surfaces the busiest "cubing weekends" in history.',`The top weeks typically cluster around late June to early August (summer plus pre-Worlds in WC years), with occasional spikes from coordinated regional pushes.`],stats:[{value:`周一`,labelZh:`周起点`,labelEn:`Week starts on`,hintZh:`ISO 8601`,hintEn:`ISO 8601`},{value:`show_at_all`,labelZh:`过滤`,labelEn:`Filters`,hintZh:`排除隐藏比赛`,hintEn:`Hidden comps excluded`},{value:`cancelled IS NULL`,labelZh:`过滤`,labelEn:`Filters`,hintZh:`排除已取消`,hintEn:`Cancelled comps excluded`},{value:`"List" 链接`,labelZh:`末列`,labelEn:`Last column`,hintZh:`深链接到 WCA 该周筛选`,hintEn:`Deep-link to WCA weekly filter`}],sourceZh:["只读 `competitions` 表 —— 已取消的(`cancelled_at IS NOT NULL`)和未公开的(`show_at_all = 0`)排除。在 SQL 里直接用 `DATE_ADD(start_date, INTERVAL(-WEEKDAY(start_date)) DAY)` 推回当周周一,再 `+6` 拿周日。"],sourceEn:["Reads only `competitions`, excluding cancelled (`cancelled_at IS NOT NULL`) and hidden (`show_at_all = 0`) entries. SQL derives Monday with `DATE_ADD(start_date, INTERVAL(-WEEKDAY(start_date)) DAY)` and Sunday with `+6`."],sourceCode:{lang:`sql`,body:`SELECT
  COUNT(*) competitions_count,
  DATE_ADD(start_date, INTERVAL(-WEEKDAY(start_date)) DAY) week_start_date,
  DATE_ADD(start_date, INTERVAL(6 - WEEKDAY(start_date)) DAY) week_end_date,
  CONCAT('[List](https://www.worldcubeassociation.org/competitions',
         '?state=custom&from_date=', MIN(start_date),
         '&to_date=', MAX(end_date), ')') list_link
FROM competitions
WHERE show_at_all = 1 AND cancelled_at IS NULL
GROUP BY week_start_date, week_end_date
ORDER BY competitions_count DESC, week_start_date DESC;`},steps:[{titleZh:`过滤可见 + 未取消`,titleEn:`Filter visible and non-cancelled`,bodyZh:"`show_at_all = 1 AND cancelled_at IS NULL` —— 草稿、测试赛、取消赛都排除。",bodyEn:"`show_at_all = 1 AND cancelled_at IS NULL` — drafts, tests, and cancellations excluded."},{titleZh:`回推所在周`,titleEn:`Snap to enclosing week`,bodyZh:"`WEEKDAY()` 返回 0..6(周一..周日)。减它得周一、加 `6-` 得周日,把 `start_date` 精确锁到一周。",bodyEn:"`WEEKDAY()` returns 0..6 (Mon..Sun). Subtracting it lands on Monday; `6 - WEEKDAY` lands on Sunday — pinning `start_date` to its containing ISO week."},{titleZh:`按周分组计数`,titleEn:`Group and count per week`,bodyZh:"`GROUP BY week_start_date, week_end_date`,`COUNT(*)` 就是该周比赛数。",bodyEn:"`GROUP BY week_start_date, week_end_date` then `COUNT(*)` gives comps in that week."},{titleZh:`拼 WCA 列表深链接`,titleEn:`Build WCA deep-link`,bodyZh:'用 `MIN(start_date)` / `MAX(end_date)` 拼 `state=custom&from_date=...&to_date=...`,点 "List" 直接跳 WCA 该周筛选页。',bodyEn:'Use `MIN(start_date)` / `MAX(end_date)` to build `state=custom&from_date=...&to_date=...` — the "List" link jumps to that week filtered on the WCA site.'},{titleZh:`降序输出 + 日期格式化`,titleEn:`Sort DESC and format dates`,bodyZh:"主键 `competitions_count DESC`,同票数按周降序;前端把 `Date` 渲染成 `5 Jan 2025`(无前导零,英文月缩写)。",bodyEn:"Primary key `competitions_count DESC`, ties broken by week DESC. The renderer formats dates as `5 Jan 2025` (no leading zero, abbreviated month).",highlight:!0}],edgesZh:["一场跨周的多日赛只按 `start_date` 归一周 —— 周一开始周二结束的不会被分到两周。","过滤了 `cancelled_at IS NOT NULL`,2020 疫情期间的取消高峰不会冒出来。",'"List" 列的 WCA URL 参数 `state=custom` 不会跟未来未公开比赛混在一起。'],edgesEn:["Multi-day comps that cross a week boundary stick to the week of `start_date` — a Mon-start, Tue-end comp is not split across two weeks.","Cancelled comps (`cancelled_at IS NOT NULL`) are filtered, so 2020-era cancellation spikes don't surface here.",'The "List" URL uses `state=custom`, which does not pull in future hidden comps.'],related:[{id:`most_attended_competitions_in_single_week`,titleZh:`一周参赛最多的选手`,titleEn:`Most competitions in single week`,hintZh:`同时间窗的选手维度`,hintEn:`Same time window, per-person view`},{id:`most_attended_competitions_in_single_month`,titleZh:`一月参赛最多的选手`,titleEn:`Most competitions in single month`,hintZh:`放宽到月窗口`,hintEn:`Same idea, monthly window`},{id:`competition_days_count_by_region`,titleZh:`按区域比赛天数`,titleEn:`Competition days by region`,hintZh:`同一份赛事数据按地区分`,hintEn:`Same comp data sliced by region`},{id:`competitions_count_by_week`,toStat:!0,titleZh:`查看实时榜单`,titleEn:`Jump to live data`,hintZh:`逐周完整列表`,hintEn:`Full week-by-week list`}]},competitions_per_year_by_country:{id:`competitions_per_year_by_country`,titleZh:`每年每国比赛数`,titleEn:`Competitions per year by country`,badgeZh:`赛事`,badgeEn:`Competition`,introZh:[`把一国的总比赛场次 ÷ 该国首场比赛至今的年数,得到"该国年均办赛数"。年数 < 1 的国家(刚加入 WCA 不足 1 年)被剔除,避免分母过小爆栏。`,`注意:分母是**自己**第一场比赛起算的年数,不是 WCA 历史总年数 —— 这样新生赛区不会因为"WCA 已经办了 20 年而我才办 3 年"而被压低。`],introEn:[`Total comps in a country ÷ years since its very first comp. Countries with under 1 year of history are excluded to keep the denominator sane.`,`The denominator counts from **that country's** first comp, not from WCA's overall start — so a freshly active region is not penalized by the global 20+ year history.`],stats:[{value:`years ≥ 1`,labelZh:`门槛`,labelEn:`Threshold`,hintZh:`首场满 1 年才上榜`,hintEn:`First comp ≥ 1 year ago`},{value:`/ 365.25`,labelZh:`年长度`,labelEn:`Year length`,hintZh:`含闰年平均`,hintEn:`Average incl. leap years`},{value:`CURDATE()`,labelZh:`终点`,labelEn:`End date`,hintZh:`取数据 dump 当天`,hintEn:`Date of dump load`},{value:`2 位小数`,labelZh:`精度`,labelEn:`Precision`,hintZh:`comps/year + years`,hintEn:`comps/year + years`}],sourceZh:['内层:`results` JOIN `competitions`,按 `competition.country_id` 分组。`COUNT(DISTINCT competition_id)` 算该国比赛数,`DATEDIFF(CURDATE(), MIN(start_date)) / 365.25` 算"从首场到今天"的年数。`HAVING years >= 1` 滤掉新国。',"外层:除一下得到 `competitions_per_year`,JOIN `countries` 取国名,按比例降序输出。"],sourceEn:["Inner: `results` JOIN `competitions`, group by `competition.country_id`. `COUNT(DISTINCT competition_id)` for comp count, `DATEDIFF(CURDATE(), MIN(start_date)) / 365.25` for years since first comp. `HAVING years >= 1` filters fresh regions.","Outer: divide for `competitions_per_year`, JOIN `countries` for the display name, sort DESC by rate."],sourceCode:{lang:`sql`,body:`SELECT
  competitions / years competitions_per_year,
  competitions, years, country.name country
FROM (
  SELECT
    COUNT(DISTINCT competition_id) competitions,
    (DATEDIFF(CURDATE(), MIN(start_date)) / 365.25) years,
    competition.country_id
  FROM results result
  JOIN competitions competition ON competition.id = competition_id
  GROUP BY competition.country_id
  HAVING years >= 1
) AS data_by_country
JOIN countries country ON country.id = country_id
ORDER BY competitions_per_year DESC;`},formulae:[{labelZh:`公式`,labelEn:`Formula`,expr:`rate(country) = competitions(country) / years_since_first(country)`,bodyZh:"`years_since_first = (today - first_comp_date) / 365.25`,不足 1 年的国家剔除。",bodyEn:"`years_since_first = (today - first_comp_date) / 365.25`. Countries under 1 year are dropped."}],steps:[{titleZh:`收集每国比赛集合`,titleEn:`Collect per-country comp set`,bodyZh:'从 `results` 起,只为了拿到"真实办过的比赛"(竞赛日历里挂了但 0 人出赛的不算)。JOIN `competitions` 拿到 `country_id` 和 `start_date`。',bodyEn:"Starting from `results` (not `competitions` directly) ensures only comps that actually ran are counted. JOIN `competitions` for `country_id` and `start_date`."},{titleZh:`数比赛 + 找首场`,titleEn:`Count comps and find first date`,bodyZh:"组内 `COUNT(DISTINCT competition_id)`(同一比赛多轮多人不重复),`MIN(start_date)` 得首场日期。",bodyEn:"`COUNT(DISTINCT competition_id)` per group (multi-round, multi-person rows collapse), `MIN(start_date)` for the first comp date."},{titleZh:`算"自首场以来"年数`,titleEn:`Compute years since first comp`,bodyZh:"`years = DATEDIFF(CURDATE(), MIN(start_date)) / 365.25`。除以 365.25 而非 365 是为了平摊闰年。",bodyEn:"`years = DATEDIFF(CURDATE(), MIN(start_date)) / 365.25`. Dividing by 365.25 (not 365) amortizes leap years."},{titleZh:`过滤新国`,titleEn:`Drop fresh regions`,bodyZh:'`HAVING years >= 1` —— 否则一个 6 个月前刚开第一场的国家会算出 "12 场/年" 这种伪信号。',bodyEn:'`HAVING years >= 1` — otherwise a country whose first comp was 6 months ago could read as "12 comps/year", a false signal.'},{titleZh:`相除并降序`,titleEn:`Divide and sort DESC`,bodyZh:"外层 `competitions / years`,JOIN `countries` 拿可读国名,`ORDER BY competitions_per_year DESC`。",bodyEn:"Outer divides the two, JOIN `countries` for display name, `ORDER BY competitions_per_year DESC`.",highlight:!0}],edgesZh:['从 `results` 起 join 比从 `competitions` 起 join 严:挂在日历但 0 人出赛的"幽灵比赛"不会算进 `competitions` 计数。','`years` 是连续小数(不是整数),所以 "2.37 年办 18 场" 算出来是 7.59/年。','`CURDATE()` 是 SQL 引擎当下的时间,每次重跑 dump 都会前进 —— 国家排名会有微弱的"自然衰减",新一年没办赛比例会下降。'],edgesEn:['Joining from `results` (not from `competitions`) is stricter — "ghost" comps listed but with zero competitors don\'t inflate the count.','`years` is fractional (not integer), so "18 comps in 2.37 years" yields 7.59/year.',"`CURDATE()` advances with each dump rebuild, giving rates a slight natural decay if a country sits idle for a year."],related:[{id:`competition_days_count_by_region`,titleZh:`按区域比赛天数`,titleEn:`Competition days by region`,hintZh:`另一种"地区赛事密度"`,hintEn:`Alt region-density slice`},{id:`competitions_count_by_week`,titleZh:`每周比赛数量`,titleEn:`Competitions per week`,hintZh:`时间维度的对照`,hintEn:`Time-axis counterpart`},{id:`average_event_count_by_competition`,titleZh:`每场比赛平均项目数`,titleEn:`Avg events per comp`,hintZh:`"广度"维度,与"密度"配套看`,hintEn:`Breadth dimension, complements density`},{id:`competitions_per_year_by_country`,toStat:!0,titleZh:`查看实时榜单`,titleEn:`Jump to live data`,hintZh:`按国家完整排序`,hintEn:`Full country ranking`}]},dnf_rate_by_event:{id:`dnf_rate_by_event`,titleZh:`各项目 DNF 率`,titleEn:`DNF rate by event`,badgeZh:`项目`,badgeEn:`Event`,introZh:["每个项目的 DNF(Did Not Finish)次数除以**实际尝试次数**,得到该项目的 DNF 率。这里 attempts 排除掉两类不算手:`value = 0`(未提交占位)和 `value = -2`(DNS,根本没上)。",`BLD / FMC / MBLD 这些"高难项目"DNF 率显著高于 3x3:盲拧靠记忆完整度,FMC 有 1 小时时限和步数上限,MBLD 一份成绩里有任一立方体失败就整轮 DNF。`],introEn:["For each event, DNF count ÷ **actual attempts**. Attempts excludes two flavors of non-solve: `value = 0` (placeholder for not-submitted) and `value = -2` (DNS — no-show).",`BLD / FMC / MBLD sit well above 3x3: blindfolded depends on full memorization, FMC has a 1-hour cap with move limits, MBLD DNFs the whole attempt if any cube fails.`],stats:[{value:`value=-1`,labelZh:`DNF 编码`,labelEn:`DNF encoding`,hintZh:`result_attempts.value`,hintEn:`In result_attempts.value`},{value:`value=-2`,labelZh:`DNS 编码`,labelEn:`DNS encoding`,hintZh:`排除在分母外`,hintEn:`Excluded from denominator`},{value:`value=0`,labelZh:`空槽`,labelEn:`Empty slot`,hintZh:`一轮 5 attempts 未填满`,hintEn:`5-attempts round not filled`},{value:`%`,labelZh:`展示`,labelEn:`Display`,hintZh:`2 位小数百分比`,hintEn:`2-decimal percentage`}],sourceZh:["走 `results` JOIN `result_attempts` —— 后者一行 = 一次单尝试(WCA dump 的细粒度表)。按 `event_id` 分组,SUM 出 dnfs(`value = -1`)与 attempts(`value NOT IN (-2, 0)`),前端 TS 端再算比例并按降序排。"],sourceEn:["JOIN `results` to `result_attempts` (one row = one solve attempt — the fine-grained WCA dump table). Group by `event_id`, SUM the DNFs (`value = -1`) and attempts (`value NOT IN (-2, 0)`); TS side then computes the ratio and sorts DESC."],sourceCode:{lang:`sql`,body:`SELECT
  r.event_id,
  SUM(CASE WHEN ra.value = -1 THEN 1 ELSE 0 END) dnfs,
  SUM(CASE WHEN ra.value NOT IN (-2, 0) THEN 1 ELSE 0 END) attempts
FROM results r
JOIN result_attempts ra ON ra.result_id = r.id
GROUP BY r.event_id;`},formulae:[{labelZh:`DNF 率`,labelEn:`DNF rate`,expr:`dnf_rate = dnf_count / attempted`,bodyZh:"`dnf_count = COUNT(value = -1)`;`attempted = COUNT(value ≠ -2 且 ≠ 0)` —— 排除 DNS 和未填槽。展示时乘 100 加 `%`。",bodyEn:"`dnf_count = COUNT(value = -1)`; `attempted = COUNT(value ≠ -2 and ≠ 0)` — excludes DNS and empty slots. Multiplied by 100 with `%` for display."}],steps:[{titleZh:`展开单 attempt 维度`,titleEn:`Expand to per-attempt rows`,bodyZh:"`results` 一行是一轮成绩(含 best/average),真实的 5 个单尝试要去 `result_attempts` 里看 —— JOIN 后行数 ≈ 5× results。",bodyEn:"`results` is one row per round (with best/average); the individual 5 attempts live in `result_attempts`. JOIN multiplies row count by ~5."},{titleZh:`辨认三种值`,titleEn:`Recognize three value types`,bodyZh:"`value > 0` 真成绩(厘秒/步数/移动数等编码),`= -1` DNF,`= -2` DNS(未上),`= 0` 一轮 5 个 attempt 没填满的空槽。",bodyEn:"`value > 0` real result (centisecond / move / FMC encoding); `= -1` DNF; `= -2` DNS (no-show); `= 0` empty slot when a 5-attempt round wasn't fully filled."},{titleZh:`SUM 出两个数`,titleEn:`SUM out two numbers`,bodyZh:"`SUM(CASE WHEN value = -1 THEN 1 ELSE 0 END)` = DNF 数;`SUM(CASE WHEN value NOT IN (-2, 0) THEN 1 ELSE 0 END)` = 实尝试数(含 DNF)。",bodyEn:"`SUM(CASE WHEN value = -1 THEN 1 ELSE 0 END)` = DNF count; `SUM(CASE WHEN value NOT IN (-2, 0) THEN 1 ELSE 0 END)` = real attempts (including DNFs)."},{titleZh:`按项目分组`,titleEn:`Group by event`,bodyZh:"`GROUP BY r.event_id`,每个 WCA 项目一行。",bodyEn:"`GROUP BY r.event_id` — one row per WCA event."},{titleZh:`前端算比例并排序`,titleEn:`Compute and sort in TS`,bodyZh:'TS 端 `100 * dnfs / attempts`,`toFixed(2) + " %"`;按 DNF 率降序,`EVENTS[id]` 映射成项目中文名。',bodyEn:'TS computes `100 * dnfs / attempts`, formats as `toFixed(2) + " %"`, sorts DESC by rate, and maps `event_id` through `EVENTS[id]` for display.',highlight:!0}],edgesZh:['分母**不含** DNS(`-2`)和空槽(`0`)—— 真没上 / 没填的不算"尝试"。改了分母口径会让所有数都偏低。',`一轮记录 = 至多 5 个 attempt,但很多项目只有 1-3 attempt(BLD / FMC 等)。聚合时单 attempt 是原子,不是单轮。`,`MBLD 项目的 attempt 是"复合 attempt"(一组 cube),DNF 由整体判定;比例不是"每立方体 DNF 率"。`],edgesEn:['Denominator **excludes** DNS (`-2`) and empty slots (`0`) — those are not real "attempts". Changing the denominator would shift every event down.',`A round can hold up to 5 attempts but many events use only 1-3 (BLD / FMC etc.). Aggregation is per-attempt, not per-round.`,`MBLD attempts are compound (one bundle of cubes) — DNF is judged at the bundle level. The rate is not "per-cube DNF rate".`],related:[{id:`most_completed_solves`,titleZh:`最多完成单次数`,titleEn:`Most completed solves`,hintZh:`"实尝试 - DNF"的全选手榜`,hintEn:`Successful attempts leaderboard`},{id:`most_solves_before_bld_success`,titleZh:`BLD 首次成功前的尝试`,titleEn:`Solves before BLD success`,hintZh:`DNF 集中的盲拧角度`,hintEn:`BLD-focused take on DNF density`},{id:`mbf_average`,titleZh:`MBF 平均`,titleEn:`MBF average`,hintZh:`MBLD 完成度细项`,hintEn:`MBLD attempt-completeness companion`},{id:`dnf_rate_by_event`,toStat:!0,titleZh:`查看实时榜单`,titleEn:`Jump to live data`,hintZh:`21 个项目排序`,hintEn:`All 21 events ranked`}]},most_records_at_single_competition:{id:`most_records_at_single_competition`,titleZh:`单场比赛最多纪录`,titleEn:`Most records at a single competition`,badgeZh:`赛事`,badgeEn:`Competition`,introZh:['一场比赛里,某位选手刷了几个区域纪录?WCA `results` 表两个字段 `regional_single_record` / `regional_average_record` 标记该轮的 single / average 是否破了 NR/CR/WR 中的某一级。把同一人同一场内的所有标记数一遍就是该人在该场的"破纪录数"。',"分 World / Continental / National 三档:World 只算 `WR`;Continental 把所有 CR(含 WR 因为 WR 必同时 ≥ CR)算进去;National 再加 `NR`。每档 top 20(含并列)。"],introEn:["How many regional records did one person set at a single comp? WCA `results` carries two flags — `regional_single_record` and `regional_average_record` — marking whether that round's single/average broke an NR/CR/WR. Sum the flags for each (person, comp) pair to get their record count at that comp.","Three tiers — World / Continental / National. World counts only `WR`; Continental counts all CRs (WR auto-qualifies since WR ≥ CR); National adds `NR` on top. Top 20 per tier with ties."],stats:[{value:`3 档`,labelZh:`层级`,labelEn:`Tiers`,hintZh:`World / Continental / National`,hintEn:`World / Continental / National`},{value:`2 字段`,labelZh:`记录列`,labelEn:`Record columns`,hintZh:`single + average 各 1 个`,hintEn:`single + average flags`},{value:`top 20`,labelZh:`每档`,labelEn:`Per tier`,hintZh:`含并列`,hintEn:`Ties included`},{value:`sub_id=1`,labelZh:`过滤`,labelEn:`Filter`,hintZh:`只主身份`,hintEn:`Primary identity only`}],sourceZh:['只读 `results` JOIN `persons (sub_id = 1)` JOIN `competitions` 三张表。`WHERE` 卡 `regional_single_record` 或 `regional_average_record` 非空,把所有"该轮破了某级纪录"的行抽出来 —— 后续在 TS 端按 (人, 比赛) 分组并按层级累加。'],sourceEn:["Read-only JOIN across `results`, `persons (sub_id = 1)`, and `competitions`. `WHERE` keeps rows where either `regional_single_record` or `regional_average_record` is non-empty — TS side then groups by (person, comp) and tallies per tier."],sourceCode:{lang:`sql`,body:`SELECT
  regional_single_record,
  regional_average_record,
  person.name, person.wca_id,
  competition.cell_name, competition.id
FROM results
JOIN persons person
  ON person.wca_id = person_id
 AND person.sub_id = 1
JOIN competitions competition
  ON competition.id = competition_id
WHERE (regional_single_record IS NOT NULL AND regional_single_record != '')
   OR (regional_average_record IS NOT NULL AND regional_average_record != '');`},steps:[{titleZh:`只抽"刷新"行`,titleEn:`Pull only "record-setting" rows`,bodyZh:"WHERE 至少一个 record 字段非空 —— `regional_*_record` 是 `WR / CR / NR` 中的代码(空串 = 没破)。",bodyEn:"WHERE at least one record flag is non-empty — `regional_*_record` holds `WR / CR / NR` code (empty string = none)."},{titleZh:`过滤副身份`,titleEn:`Filter to primary identity`,bodyZh:"`persons.sub_id = 1` —— 改名/换国籍的选手 dump 里有多行,只保留主行,避免重复计数。",bodyEn:"`persons.sub_id = 1` — the dump holds multiple rows for rename/country-change cases; keep only the primary to avoid double counts."},{titleZh:`前端定义三档代码集`,titleEn:`Define three tier code-sets`,bodyZh:'TypeScript 端的 `levels`:`World = ["WR"]`;`Continental = ["AfR","AsR","NAR","SAR","ER","OcR","WR"]`;`National = ["NR","AfR",...,"WR"]`(因为 WR 总是同时 ≥ CR ≥ NR)。',bodyEn:'TS `levels` map: `World = ["WR"]`; `Continental = ["AfR","AsR","NAR","SAR","ER","OcR","WR"]`; `National = ["NR","AfR",...,"WR"]` (since WR is automatically also CR and NR).'},{titleZh:`按 (人, 比赛) 累加`,titleEn:`Tally per (person, comp)`,bodyZh:"组键 `person_link|||results_link`,组内对 single 和 average 各检查一次:若该字段值落在当前档的代码集里,`count += 1`。",bodyEn:"Group key `person_link|||results_link`. Within a group, check both `single` and `average` against the tier's code-set — `count += 1` for each hit."},{titleZh:`排序并取 top 20(含并列)`,titleEn:`Sort and take top 20 with ties`,bodyZh:'按 `count DESC`;`takeTopNWithTies` 取 20,边界并列全部一起留下(避免"第 20 名打平却被剪掉"的不公)。',bodyEn:'Sort by `count DESC`; `takeTopNWithTies` keeps the top 20 plus any ties at the cutoff (avoids "tied at #20 but cut off" unfairness).',highlight:!0}],edgesZh:[`WR 行同时也是 CR、也是 NR —— 三档的代码集层层包含,确保高档纪录在所有低档里都计入。`,`一行可能 single 和 average **都**破了纪录,记为 2 个纪录(不是 1)。`,"`sub_id = 1` 过滤副身份后,某位曾改名的选手的所有比赛仍归到主名下,不会被切碎。","只看 `regional_*_record` 标记,不二次校验数值 —— 若 WCA 后续 retroactive 撤销某条纪录,统计会随 dump 更新跟随。"],edgesEn:[`A WR row is automatically also a CR and an NR — the three tier code-sets nest, so higher-tier records always count in the lower-tier boards.`,`A single row can break **both** single and average records — that counts as 2, not 1.`,"After `sub_id = 1` filtering, a competitor who later renamed is still aggregated under one identity, not split.","Reads only the `regional_*_record` flags — no numeric re-verification. If WCA retroactively voids a record, the next dump rebuild reflects it."],related:[{id:`longest_streak_of_world_records`,titleZh:`连续比赛刷 WR`,titleEn:`Longest streak of WRs`,hintZh:`跨比赛的"连续刷新"`,hintEn:`Cross-comp consecutive WRs`},{id:`records_in_most_events`,titleZh:`在最多项目上有纪录`,titleEn:`Records in most events`,hintZh:`项目覆盖维度`,hintEn:`Event-coverage take`},{id:`most_podiums_at_single_competition`,titleZh:`单场最多领奖台`,titleEn:`Most podiums at one comp`,hintZh:`同"单场密度"另一种切`,hintEn:`Sibling single-comp density stat`},{id:`most_records_at_single_competition`,toStat:!0,titleZh:`查看实时榜单`,titleEn:`Jump to live data`,hintZh:`三档完整 top 20`,hintEn:`Full top-20 per tier`}]}},f={best_medal_collection_from_abroad_by_country:{id:`best_medal_collection_from_abroad_by_country`,titleZh:`各国海外最佳奖牌收藏`,titleEn:`Best medal collection from abroad by country`,badgeZh:`国家`,badgeEn:`Country`,introZh:['统计**每个国家的选手在本国之外**比赛上拿到的金 / 银 / 铜数。"本国"判定基于选手国籍 (`results.country_id`) 与比赛举办国 (`competitions.country_id`) 的对比 —— 不一致即"海外"。',`这是衡量一个国家"远征实力"的硬指标:常驻强国主场刷牌简单,真到客场能拿牌才说明选手层厚。`],introEn:["Counts gold / silver / bronze each country's competitors have earned **outside their home country**. \"Abroad\" is decided by comparing the cuber's national identity (`results.country_id`) against the competition's host country (`competitions.country_id`).",`It measures away-game depth: home medals are easy for established regions; medals abroad require a real talent pool willing and able to travel.`],stats:[{value:`金 > 银 > 铜`,labelZh:`排序口径`,labelEn:`Sort key`,hintZh:`先金牌降序,平了看银 / 铜`,hintEn:`Golds desc, silver / bronze tiebreaker`},{value:"`pos ∈ {1,2,3}`",labelZh:`领奖台判定`,labelEn:`Podium check`,hintZh:"直接读 `results.pos`(WCA 已算好)",hintEn:"Reads `results.pos` (WCA-precomputed)"},{value:"`c / f` 轮次",labelZh:`只看决赛`,labelEn:`Final rounds only`,hintZh:`Final + Combined Final`,hintEn:`Final + Combined Final`},{value:"`best > 0`",labelZh:`排 DNF`,labelEn:`DNF filter`,hintZh:`best ≤ 0 = DNF / DNS / 未提交`,hintEn:`best ≤ 0 = DNF / DNS / unattempted`}],sourceZh:["`results` 表 join `competitions`,加条件 `competition.country_id != result.country_id` 筛出海外成绩;再过 `round_type_id IN ('c', 'f')` 只看决赛轮 (`f` = Final, `c` = Combined Final)。`pos` 由 WCA 在每轮 round 内预先计算 (1 / 2 / 3 / ... 名次),`pos ∈ {1,2,3}` 即领奖台。"],sourceEn:["`results` joined to `competitions` with `competition.country_id != result.country_id` to keep abroad-only rows. Restrict to final rounds via `round_type_id IN ('c', 'f')` (`f` = Final, `c` = Combined Final). `pos` is WCA-precomputed per round (1 / 2 / 3 / ...); `pos ∈ {1,2,3}` means podium."],sourceCode:{lang:`sql`,body:`SELECT result.country_id,
       SUM(IF(pos = 1, 1, 0)) gold,
       SUM(IF(pos = 2, 1, 0)) silver,
       SUM(IF(pos = 3, 1, 0)) bronze
FROM results
JOIN competitions ON competitions.id = competition_id
WHERE round_type_id IN ('c', 'f')
  AND best > 0
  AND competitions.country_id != results.country_id
GROUP BY result.country_id`},steps:[{titleZh:`过滤海外决赛行`,titleEn:`Filter abroad final rows`,bodyZh:'保留 `country_id` 不一致 + `round_type_id IN (c, f)` + `best > 0` 的 `results` 行 —— 即"决赛 + 完赛 + 客场"。',bodyEn:"Keep `results` rows where the two `country_id` differ, the round is `c` or `f`, and `best > 0` — final, completed, away."},{titleZh:`按选手国家分组`,titleEn:`Group by competitor country`,bodyZh:"注意分组键是 `results.country_id` (选手国籍),不是比赛举办国。",bodyEn:"Group by `results.country_id` (competitor nationality), not the host country."},{titleZh:`三种奖牌分别 SUM`,titleEn:`Sum each medal tier`,bodyZh:"`SUM(IF(pos = 1, 1, 0))` 数金牌,同理银 / 铜。一个领奖台 = 3 行 (3 个项目分别拿牌)。",bodyEn:"`SUM(IF(pos = 1, 1, 0))` for gold, similarly silver / bronze. One podium across an event = one row per medalist."},{titleZh:"join `countries` 取国名",titleEn:"Join `countries` for display name",bodyZh:"把 `country_id` 换成可读国名;按 `gold DESC, silver DESC, bronze DESC, name` 排序 —— 经典奖牌榜规则。",bodyEn:"Resolve `country_id` to a display name; sort by `gold DESC, silver DESC, bronze DESC, name` — classic medal-table rule.",highlight:!0}],edgesZh:[`"海外"按举办国判定,不是地理远近 —— 大陆内邻国 (中日韩互访) 也算海外。`,`只看 Final + Combined Final 两类决赛轮 —— 预赛 / 半决名次不计入奖牌。`,'WCA 的 `pos` 在 DNF 也会赋值 (按 best 排),所以再加 `best > 0` 排掉 "DNF 拿第一"。',`"奖牌"沿用 WCA 标准 —— 决赛前 3 即领奖台,即使决赛 < 8 人也算 (规则未要求最低人数)。`],edgesEn:[`"Abroad" is country-of-comp vs country-of-cuber, not geographic distance — neighboring countries (CN/JP/KR cross-visits) still count.`,`Only Final / Combined Final rounds matter — semi-final or earlier round placings are not medals.`,'WCA assigns `pos` even to DNFs (sorted by best), so the extra `best > 0` filter strips DNF "1st place".',`Standard WCA medal definition — top 3 in final is a podium even if fewer than 8 competitors entered.`],related:[{id:`best_medal_collection_from_abroad_by_person`,titleZh:`个人版`,titleEn:`Per-person version`,hintZh:`同口径,按选手汇总`,hintEn:`Same metric, aggregated by person`},{id:`world_championship_podiums_by_country`,titleZh:`世锦赛奖牌(国家)`,titleEn:`Worlds podiums by country`,hintZh:`只看世锦赛的版本`,hintEn:`Worlds-only counterpart`},{id:`world_records_by_country`,titleZh:`各国 WR 总数`,titleEn:`WRs by country`,hintZh:`另一个国家维度指标`,hintEn:`Sibling country-level metric`},{id:`best_medal_collection_from_abroad_by_country`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`完整国家列表`,hintEn:`Full country leaderboard`}]},best_medal_collection_from_abroad_by_person:{id:`best_medal_collection_from_abroad_by_person`,titleZh:`个人海外最佳奖牌收藏`,titleEn:`Best medal collection from abroad by person`,badgeZh:`选手`,badgeEn:`Person`,introZh:["同上,但分组键换成 `person_id` —— 一个选手在所有海外比赛累计的金 / 银 / 铜数。",`能上榜的基本都是"靠飞机刷牌"的远征型选手:本国比赛拿再多牌不算,只有跨境拿牌才进分子。`],introEn:["Same as the country version but grouped by `person_id` — gold / silver / bronze a single cuber has racked up at competitions outside their home country.",`The leaderboard is dominated by frequent-flyer travelers — home-country medals don't count, only cross-border podiums.`],stats:[{value:`100`,labelZh:`榜单长度`,labelEn:`Leaderboard size`,hintZh:"`LIMIT 100`",hintEn:"`LIMIT 100`"},{value:"`sub_id = 1`",labelZh:`只取主身份`,labelEn:`Primary identity only`,hintZh:`改名 / 改国籍走副 row,忽略`,hintEn:`Renames / nationality changes go to alt rows, ignored`},{value:`金 → 银 → 铜`,labelZh:`排序优先级`,labelEn:`Tiebreaker order`,hintZh:`同国家榜`,hintEn:`Same as country leaderboard`},{value:`决赛 + 完赛`,labelZh:`入榜门槛`,labelEn:`Inclusion filter`,hintZh:"`round_type_id IN (c, f)` + `best > 0`",hintEn:"`round_type_id IN (c, f)` + `best > 0`"}],sourceZh:['同 by_country,只是 `GROUP BY person_id`,然后 join `persons` (`sub_id = 1` 取主身份) 拿展示名。`country_id` 改名不变,但选手改国籍后副行的 `country_id` 也会跟着新,所以"海外"判定仍然准确。'],sourceEn:['Same query as the country version but `GROUP BY person_id`, then joined to `persons` (`sub_id = 1` for the primary row) for display name. Even when a cuber changes nationality the alt person row\'s `country_id` updates, so the "abroad" check stays accurate.'],sourceCode:{lang:`sql`,body:`SELECT person_id,
       SUM(IF(pos = 1, 1, 0)) gold,
       SUM(IF(pos = 2, 1, 0)) silver,
       SUM(IF(pos = 3, 1, 0)) bronze
FROM results
JOIN competitions ON competitions.id = competition_id
WHERE round_type_id IN ('c', 'f') AND best > 0
  AND competitions.country_id != results.country_id
GROUP BY person_id
ORDER BY gold DESC, silver DESC, bronze DESC LIMIT 100`},steps:[{titleZh:`同 by_country 的 WHERE`,titleEn:`Same WHERE as by_country`,bodyZh:"`round_type_id IN (c, f)` + `best > 0` + 跨国比赛过滤,沿用国家榜过滤逻辑。",bodyEn:"Same `round_type_id IN (c, f)` + `best > 0` + cross-border filter as the country leaderboard."},{titleZh:"`GROUP BY person_id`",titleEn:`Aggregate by person`,bodyZh:`每个选手一行;同一场比赛多个项目奖牌都计入。`,bodyEn:`One row per cuber; multiple medals from one comp (different events) all stack.`},{titleZh:"join `persons` 取主身份",titleEn:"Join `persons` for primary identity",bodyZh:"WCA dump 里改名 / 国籍变化会有多行 `persons`,`sub_id = 1` 是当前展示行。",bodyEn:"WCA dump stores rename / nationality changes as alt rows; `sub_id = 1` is the currently-displayed identity."},{titleZh:`排序 + LIMIT 100`,titleEn:`Sort + LIMIT 100`,bodyZh:`同 by_country 排序;只输出前 100 控制 JSON 体积。`,bodyEn:`Same sort order as the country leaderboard; capped at 100 rows to keep the JSON small.`,highlight:!0}],edgesZh:["选手改国籍后,旧比赛的 `result.country_id` **不会回填** —— 历史奖牌按当时国籍判定海外/主场,符合直觉。",`同一选手在同一比赛拿多个项目金牌会累计 (e.g. 3x3 + 2x2 + OH 都金牌 = +3 金)。`,`Limit 100 截断意味着尾部选手在这里看不到,但完整 by_country 加总能反推。`],edgesEn:["When a cuber changes nationality, the historical `result.country_id` is **not back-filled** — old medals stay tagged with the country they held at the time, which matches intuition.",`Multiple medals at one comp stack (gold in 3x3 + 2x2 + OH at the same event = +3 golds).`,`The hard limit of 100 hides the long tail; the country aggregate is complete.`],related:[{id:`best_medal_collection_from_abroad_by_country`,titleZh:`国家版`,titleEn:`Per-country version`,hintZh:`把人 → 国家的汇总`,hintEn:`Person → country aggregation`},{id:`world_championship_podiums_by_person`,titleZh:`世锦赛奖牌(选手)`,titleEn:`Worlds podiums by person`,hintZh:`只在世锦赛上的领奖台`,hintEn:`Worlds-only podiums per cuber`},{id:`world_records_by_person`,titleZh:`个人 WR 数`,titleEn:`WRs by person`,hintZh:`另一个个人维度指标`,hintEn:`Sibling per-person metric`},{id:`best_medal_collection_from_abroad_by_person`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`Top 100`,hintEn:`Top 100`}]},current_world_records_by_country:{id:`current_world_records_by_country`,titleZh:`各国当前世界纪录数量`,titleEn:`Current world records by country`,badgeZh:`国家`,badgeEn:`Country`,introZh:['不依赖 `regional_single_record = WR` 历史标记 (那是当时打破时贴上的,改 dump 后旧 WR 会留 "WR" 但已被超越);而是**直接重算每个项目的当前最佳**,持有者所属国家 +1。',`所以一个选手如果同时持有 single + average WR,会贡献 2 条;同国多人也合并到该国名下。`],introEn:["Does not trust the historical `regional_single_record = WR` marker (which sticks even after the WR is broken). Instead **recomputes the current best per event** and credits its holder's country.",`A cuber holding both single + average WR contributes 2; multiple holders in one country roll up under that country.`],stats:[{value:`single + average`,labelZh:`两类纪录`,labelEn:`Two record types`,hintZh:`每个项目最多 2 个当前 WR`,hintEn:`Up to 2 active WRs per event`},{value:"`e.rank < 900`",labelZh:`只看现役项目`,labelEn:`Active events only`,hintZh:`排除已停办项目`,hintEn:`Discontinued events excluded`},{value:"`MIN(best/average)`",labelZh:`WR 判定`,labelEn:`WR check`,hintZh:`该项目全局最小 = 当前 WR`,hintEn:`Global min per event = current WR`},{value:`GROUP_CONCAT`,labelZh:`同国合并`,labelEn:`Per-country join`,hintZh:`该国 WR 持有者列表`,hintEn:`Country's WR holders rendered as list`}],sourceZh:["两步嵌套查询:外层先在 `results` 上 `GROUP BY person_id, event_id` 算每人每项的 PB,再 `GROUP BY event_id` 算全局 WR,内连接 `PB = WR` 的行 = 当前 WR 持有者。Single 和 average 各跑一遍 UNION ALL,再 join `persons` 拿国家,最后按 `country_id` 汇总。","`events.rank < 900` 排除 magic / mmagic / 333mbo / 333ft 等已停办项目 (WCA 给停办项目 rank ≥ 900)。"],sourceEn:["Two-step nested query: inner `GROUP BY person_id, event_id` for per-person PB, another `GROUP BY event_id` for the global WR, inner-join where `PB = WR` to find current holders. Single and average each run separately, UNIONed, then joined to `persons` for country, finally aggregated by `country_id`.","`events.rank < 900` strips discontinued events (magic / mmagic / 333mbo / 333ft); WCA assigns those rank ≥ 900."],sourceCode:{lang:`sql`,body:`-- single WR holders (average mirrors this)
SELECT DISTINCT ps.person_id
FROM (SELECT person_id, event_id, MIN(best) AS pb
      FROM results WHERE best > 0
      GROUP BY person_id, event_id) ps
JOIN (SELECT event_id, MIN(best) AS wr
      FROM results WHERE best > 0
      GROUP BY event_id) wr
  ON ps.event_id = wr.event_id AND ps.pb = wr.wr
JOIN events e ON e.id = ps.event_id AND e.rank < 900;`},steps:[{titleZh:`算每人每项 PB`,titleEn:`Compute per-person PB`,bodyZh:"`GROUP BY person_id, event_id` + `MIN(best)`,过 `best > 0` 排 DNF。average 用 `MIN(average)` 同理。",bodyEn:"`GROUP BY person_id, event_id` with `MIN(best)`, filter `best > 0` to drop DNFs. Average mirrors via `MIN(average)`."},{titleZh:`算全局 WR`,titleEn:`Compute global WR`,bodyZh:"同 `results` 表 `GROUP BY event_id` + `MIN(best)` —— 每项目一个 WR 值。",bodyEn:"Same `results` table, `GROUP BY event_id` + `MIN(best)` — one WR value per event."},{titleZh:`找等值持有者`,titleEn:`Match holders`,bodyZh:"内连接 `PB = WR` 拿到当前持有人;并列 WR (多人同值) 都算进去。",bodyEn:"Inner join `PB = WR` extracts current holders; ties (multiple cubers at the same time) all qualify."},{titleZh:`UNION ALL single + average`,titleEn:`UNION single + average`,bodyZh:`两个子查询合并 —— 同时持有 single + average WR 的选手出现 2 行,自然 +2。`,bodyEn:`Merge both subqueries — a cuber holding single + average WRs appears twice, contributing 2.`},{titleZh:`按国家汇总`,titleEn:`Roll up by country`,bodyZh:"`COUNT(*)` 算该国 WR 数,`GROUP_CONCAT(DISTINCT person.name ...)` 拼持有者名单。",bodyEn:"`COUNT(*)` for the country's WR tally; `GROUP_CONCAT(DISTINCT person.name ...)` for the holder list.",highlight:!0}],edgesZh:[`"当前"= dump 时间点 (每月更新);新 WR 打破后下一次 dump 才会反映。`,`并列 WR (同值多人) 都算,所以个别项目可能 2-3 个国家同时各 +1。`,'停办项目 (`e.rank ≥ 900`) 整个被排除 —— 那些 "永久 WR" 不参与现役榜。',"与 `world_records_by_country` 区别:那个是历史累计,这个只数当前持有。"],edgesEn:[`"Current" means as of the dump snapshot (refreshed monthly); a new WR shows up only after the next dump.`,`Tied WRs (multiple holders at the same value) all count, so an event can credit 2-3 countries simultaneously.`,'Discontinued events (`e.rank ≥ 900`) are dropped entirely — those "permanent WRs" don\'t appear here.',"Different from `world_records_by_country` — that counts all-time WR history; this counts only what's currently held."],related:[{id:`world_records_by_country`,titleZh:`历史 WR 累计(国家)`,titleEn:`All-time WR count (country)`,hintZh:`同一维度的累计版`,hintEn:`Cumulative all-time counterpart`},{id:`world_records_by_person`,titleZh:`历史 WR 累计(选手)`,titleEn:`All-time WR count (person)`,hintZh:`换个体维度`,hintEn:`Per-person version`},{id:`wr_current`,titleZh:`当前 WR 一览`,titleEn:`Current WR snapshot`,hintZh:`逐项目列出当前 WR 值`,hintEn:`Per-event WR value listing`},{id:`current_world_records_by_country`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`国家 + 持有人`,hintEn:`Country + holders`}]},delegated_competition_per_year:{id:`delegated_competition_per_year`,titleZh:`每年代表比赛数`,titleEn:`Delegated competitions per year`,badgeZh:`代表`,badgeEn:`Delegate`,introZh:[`WCA Delegate 是每场官方比赛的现场监督,亲自到场签字盖章。本表按"代表年均量"排序 —— 该代表的总代表场数 ÷ 服役年数。`,`服役年数定义为**首次代表 → 末次代表**的时间跨度 (DATEDIFF 算天数 ÷ 365.25),并非 WCA Delegate 任命书的合同期。门槛:至少代表过 5 场,排掉新晋。`],introEn:[`A WCA Delegate is the on-site supervisor of each official competition. This table ranks by yearly throughput — total delegated comps ÷ years of service.`,`Service span = first delegated comp → last (DATEDIFF days ÷ 365.25), not the formal Delegate appointment window. Floor: at least 5 delegated comps to qualify (filters out new appointees).`],stats:[{value:`≥ 5`,labelZh:`入榜门槛`,labelEn:`Inclusion floor`,hintZh:`少于 5 场不上榜`,hintEn:`Under 5 — excluded`},{value:`365.25`,labelZh:`年长度`,labelEn:`Days per year`,hintZh:`含闰年的平均年长`,hintEn:`Leap-aware average year`},{value:"`show_at_all = 1`",labelZh:`可见比赛`,labelEn:`Visible comps`,hintZh:`排掉草稿 / 隐藏`,hintEn:`Excludes draft / hidden`},{value:"`cancelled_at IS NULL`",labelZh:`排已取消`,labelEn:`Drop cancelled`,hintZh:`签了又取消的不算`,hintEn:`Signed but cancelled excluded`}],sourceZh:['`competition_delegates` 表 (一场比赛多 Delegate = 多行) join `competitions` 过滤 `show_at_all = 1 AND cancelled_at IS NULL AND start_date < CURDATE()` (即"对外可见、未取消、已开始")。`GROUP BY delegate_id`,统计 `COUNT(DISTINCT competition_id)` (避免一人在一场被记两次) + `DATEDIFF` 算跨度。',"`users.wca_id` join `persons` 拿展示名;输出 `delegated_per_year = count / years`。"],sourceEn:["`competition_delegates` (multiple delegates per comp = multiple rows) joined to `competitions`, filtered to `show_at_all = 1 AND cancelled_at IS NULL AND start_date < CURDATE()` (public, not cancelled, already started). `GROUP BY delegate_id` with `COUNT(DISTINCT competition_id)` (avoids double-counting same delegate on one comp) and `DATEDIFF` for span.","`users.wca_id` joined to `persons` resolves display names; output is `delegated_per_year = count / years`."],sourceCode:{lang:`sql`,body:`SELECT COUNT(DISTINCT competition_id) delegated,
       (DATEDIFF(MAX(end_date), MIN(start_date)) / 365.25) years,
       delegate_id
FROM competition_delegates
JOIN competitions ON competitions.id = competition_id
WHERE show_at_all = 1
  AND cancelled_at IS NULL
  AND start_date < CURDATE()
GROUP BY delegate_id
HAVING delegated >= 5`},steps:[{titleZh:`过滤可见 + 未取消 + 已开始`,titleEn:`Filter visible + not cancelled + started`,bodyZh:`草稿比赛 / 取消比赛 / 未来比赛全排掉 —— 只算"真正办过"的。`,bodyEn:`Drafts / cancelled / future comps all excluded — only "actually happened" counts.`},{titleZh:`COUNT(DISTINCT competition_id)`,titleEn:`Count distinct comps`,bodyZh:`Distinct 防一场比赛多角色 (e.g. Delegate + Organizer) 在 join 后产生重复行。`,bodyEn:`Distinct guards against double-counting when a delegate also held other roles at the same comp.`},{titleZh:`服役跨度 = MAX(end) − MIN(start)`,titleEn:`Span = MAX(end) − MIN(start)`,bodyZh:"`DATEDIFF` 拿天数 / 365.25 = 年。注意 `MIN(start_date)` 配 `MAX(end_date)` —— 首场起算到末场结束。",bodyEn:"`DATEDIFF` days ÷ 365.25 = years. Pair `MIN(start_date)` with `MAX(end_date)` — from first comp's start to last comp's end."},{titleZh:"`HAVING delegated >= 5`",titleEn:`Threshold ≥ 5`,bodyZh:`少于 5 场的"刚上任 + 跨度极短"会算出离谱年均值,直接砍掉。`,bodyEn:`New appointees with very short spans would compute absurd yearly rates — cut at 5 minimum.`},{titleZh:"`delegated / years` 降序",titleEn:`Sort by delegated / years`,bodyZh:`最终排序键 = 单位年代表场数,Top 即"最高效"代表。`,bodyEn:`Final sort key is comps-per-year; the top is the most productive Delegate.`,highlight:!0}],edgesZh:[`服役跨度按"代表过的比赛"算,不是 WCA 内部任命期 —— 间断不计入分母 (但通常 Delegate 不会真正断签)。`,"若代表只签过 1 场,DATEDIFF 是 0,会除零;`HAVING ≥ 5` 顺带防了。","副代表 (Trainee) 在 `competition_delegates` 里也是一行,会算进总数 —— 包含见习经历。"],edgesEn:[`Service span is from first to last delegated comp, not the formal Delegate appointment period — gaps in between count toward the denominator.`,"A delegate with only 1 comp would divide by 0 days; the `HAVING ≥ 5` floor handles that.","Trainee delegates also appear in `competition_delegates`, so trainee shifts count toward the total."],related:[{id:`most_delegated_competitions`,titleZh:`代表总场次榜`,titleEn:`Total delegated comps`,hintZh:`不除年数,纯累计`,hintEn:`No yearly rate, pure cumulative`},{id:`delegated_competition_per_year`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`年均效率排名`,hintEn:`Comps-per-year ranking`}]},first_r_is_wr:{id:`first_r_is_wr`,titleZh:`首次破纪录即世界纪录`,titleEn:`First record is a World Record`,badgeZh:`稀有`,badgeEn:`Cinematic`,introZh:[`一个选手生涯里**第一次打破任何"地区纪录"** (NR / CR / WR,WCA 术语统称 regional record) 时,如果那条同时是世界纪录,就上榜。`,`极戏剧的事件:大多数选手是先破 NR,再爬到 CR,再到 WR;少数天才级 / 项目极冷门的情况下,一上来就 WR。`],introEn:[`A cuber qualifies when **their very first regional record** (NR / CR / WR — WCA collectively calls these "regional records") was simultaneously a World Record.`,`Dramatic and rare: most cubers stair-step from NR → CR → WR; only prodigies or competitors in niche events skip straight to WR on debut.`],stats:[{value:`ROW_NUMBER()`,labelZh:`取首次`,labelEn:`First-only pick`,hintZh:`按 start_date 分窗排序`,hintEn:`Window-rank by start_date`},{value:`rn = 1`,labelZh:`过滤条件`,labelEn:`Filter`,hintZh:`只看每人首条纪录`,hintEn:`Keep first-record-per-person only`},{value:`single ∪ average`,labelZh:`两类合并`,labelEn:`Both metric types`,hintZh:`UNION ALL 后一起排`,hintEn:`UNION ALL, then window-rank together`},{value:"`record = WR`",labelZh:`入榜条件`,labelEn:`Inclusion`,hintZh:`首条恰好是 WR`,hintEn:`First record is a WR`}],sourceZh:["把 `regional_single_record IS NOT NULL` 和 `regional_average_record IS NOT NULL` 两个子集 UNION ALL,每行带 `start_date` 和 record 类型 (NR / CR / WR);窗口函数 `ROW_NUMBER() OVER (PARTITION BY person_id ORDER BY start_date)` 给每人按时间编号,取 `rn = 1`。",`最后 \`WHERE record = 'WR'\` 留下"首条就是 WR"的人。`],sourceEn:["UNION ALL the two subsets (`regional_single_record IS NOT NULL` and `regional_average_record IS NOT NULL`) — each row carries `start_date` and the record type (NR / CR / WR). A window function `ROW_NUMBER() OVER (PARTITION BY person_id ORDER BY start_date)` numbers each person's records chronologically; keep `rn = 1`.","Final `WHERE record = 'WR'` filters to those whose first record happened to be a WR."],sourceCode:{lang:`sql`,body:`SELECT *, ROW_NUMBER() OVER (
         PARTITION BY person_id ORDER BY start_date
       ) AS rn
FROM (
  SELECT person_id, event_id, 'single' AS type,
         best AS result, regional_single_record AS record, ...
  FROM results WHERE regional_single_record IS NOT NULL
  UNION ALL
  SELECT ..., 'average', average, regional_average_record, ...
  FROM results WHERE regional_average_record IS NOT NULL
) all_records
WHERE rn = 1 AND record = 'WR'`},steps:[{titleZh:`聚合所有 regional record`,titleEn:`Pool all regional records`,bodyZh:"`regional_single_record` 和 `regional_average_record` 两列(NR / CR / WR / 空)联合,每条非空都成一行。",bodyEn:"Both `regional_single_record` and `regional_average_record` columns (NR / CR / WR / null) become rows when non-null."},{titleZh:`按人按时间编号`,titleEn:`Window-rank per person by date`,bodyZh:"`ROW_NUMBER() OVER (PARTITION BY person_id ORDER BY start_date)` 给每个选手的所有 record 按时间编号 (1, 2, 3, ...)。",bodyEn:"`ROW_NUMBER() OVER (PARTITION BY person_id ORDER BY start_date)` numbers each cuber's record events 1, 2, 3, ... chronologically."},{titleZh:"取 `rn = 1`",titleEn:"Keep `rn = 1`",bodyZh:`只留下每人**最早**那一条 regional record;同日多条都算"首次"。`,bodyEn:`Keep only each person's **earliest** regional record; multiple records on the same date all qualify as "first".`},{titleZh:"过滤 `record = WR`",titleEn:"Filter `record = WR`",bodyZh:`在首条 record 里只挑级别 = WR 的 —— 上榜的就是这群人。`,bodyEn:`From the first-record set, keep only those tagged WR — that's the leaderboard.`,highlight:!0}],edgesZh:['"首次"按 `start_date` 排,同日多场比赛 / 同场多项目的并列首条都会进 `rn = 1` (单条记录里 single + average 同时算 NR/CR/WR 也会拆 2 行)。',`只要 record **不是空字符串 / NULL** 就算 regional record;NR / CR / WR 三级都收进 UNION,再过滤。`,'WCA 的 `regional_*_record` 是当时打破时的标记,不会因后续被超越而清除 —— 历史 WR 不会"降级"。'],edgesEn:['"First" is `start_date` ordering; same-day records (multiple comps, or single + average flagged together) all tie for `rn = 1`.',"Any non-empty `regional_*_record` value counts (NR / CR / WR all enter the UNION before the WR filter).",'WCA\'s `regional_*_record` markers are stamped at time-of-record and stay there even after the WR is broken — historical WRs are never "demoted".'],related:[{id:`world_records_by_person`,titleZh:`个人 WR 总数`,titleEn:`WRs by person`,hintZh:`一个 WR 不稀奇,首条就是才稀奇`,hintEn:`One WR is common; debut WR is rare`},{id:`records_in_most_events`,titleZh:`在最多项目破纪录`,titleEn:`Records in most events`,hintZh:`广度对比`,hintEn:`Breadth counterpart`},{id:`longest_standing_records`,titleZh:`最长保持纪录`,titleEn:`Longest-standing records`,hintZh:`纪录之后的生命周期`,hintEn:`What happens to a record afterwards`},{id:`first_r_is_wr`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`完整名单 + 日期 + 比赛`,hintEn:`Full list with dates and comps`}]},longest_standing_records:{id:`longest_standing_records`,titleZh:`最长保持纪录`,titleEn:`Longest standing records`,badgeZh:`寿命`,badgeEn:`Longevity`,introZh:['把所有曾经的 regional record (`AfR` / `AsR` / `ER` / `NAR` / `OcR` / `SAR` / `WR`) 按"在被打破前活了多少天"排序。仍在保持的纪录,用今天减打破日。',`分 7 个区域看 (World + 6 大洲),每区取 Top 10。WR 在每个洲都重复出现 —— 因为 WR 同时也是该洲的洲纪录。`],introEn:["For every regional record (`AfR` / `AsR` / `ER` / `NAR` / `OcR` / `SAR` / `WR`), measures how many days it stood until being broken. Active records use today as the upper bound.",`Split across 7 regions (World + 6 continents), Top 10 each. WRs appear in every continent block — a WR is automatically that continent's continental record too.`],stats:[{value:`7`,labelZh:`区域分组`,labelEn:`Region blocks`,hintZh:`World + 6 大洲`,hintEn:`World + 6 continents`},{value:`10`,labelZh:`每区域条数`,labelEn:`Rows per region`,hintZh:`降序天数前 10`,hintEn:`Top 10 by days standing`},{value:`OFFICIAL_EVENTS`,labelZh:`只看现役项目`,labelEn:`Active events only`,hintZh:`排除已停办`,hintEn:`Discontinued excluded`},{value:`today / 打破日`,labelZh:`截止时间`,labelEn:`End boundary`,hintZh:`尚未被破 = 今天`,hintEn:`Still active → today`}],sourceZh:["从 `results` 拉所有 `regional_single_record` 或 `regional_average_record` 是 `WR/AfR/AsR/ER/NAR/OcR/SAR` 之一的行;join `persons` (`sub_id = 1`) + `competitions` + `countries` + `continents` 拿洲信息。SQL 只做拉数据,寿命计算全在 JS 的 `transform()` 里。",`transform 里按 (区域, 类型, 项目) 分组,组内按时间排,找"比当前更好的下一条"的日期差;没有更好 = 今天 − 当前日 = 仍活着。`],sourceEn:["Pulls all rows from `results` whose `regional_single_record` or `regional_average_record` is `WR/AfR/AsR/ER/NAR/OcR/SAR`; joins `persons` (`sub_id = 1`) + `competitions` + `countries` + `continents` for continent info. SQL only collects data — longevity is computed in JS `transform()`.",`In transform, group by (region, type, event), sort by date, find "next better result" date diff; no better → today minus current date (i.e. still active).`],sourceCode:{lang:`sql`,body:`SELECT regional_single_record, regional_average_record,
       best single, average,
       person.name, competition.start_date,
       event_id, continent.name continent
FROM results
JOIN persons ON persons.wca_id = person_id AND persons.sub_id = 1
JOIN competitions ON competitions.id = competition_id
JOIN countries ON countries.id = results.country_id
JOIN continents ON continents.id = countries.continent_id
WHERE regional_single_record IN ('AfR','AsR','ER','NAR','OcR','SAR','WR')
   OR regional_average_record IN ('AfR','AsR','ER','NAR','OcR','SAR','WR')`},steps:[{titleZh:`拉历史 record 行`,titleEn:`Pull every record-flagged row`,bodyZh:`WHERE 子句包含 7 个 regional record code;一行可能同时 single + average 都标 (拆 2 条处理)。`,bodyEn:`WHERE clause includes all 7 regional record codes; a single row can carry both single + average markers (handled as 2 entries).`},{titleZh:`按 (区域, 项目, 类型) 分桶`,titleEn:`Bucket by (region, event, type)`,bodyZh:"`regionRecords` 表:每个区域看哪些 code (e.g. Asia 看 `AsR + WR`,因为 WR 持有者必然在亚洲打出来才同时进 AsR)。",bodyEn:"A `regionRecords` map: each region accepts which codes (e.g. Asia includes `AsR + WR`, since a WR set in Asia also counts as AsR)."},{titleZh:`找"下一条更好"`,titleEn:`Find next-better record`,bodyZh:"组内时间正序,对每条 r,`find(r2 => r2.value < r.value)` 拿到打破它的下一条;返回那条的 `start_date`。",bodyEn:"Sorted by date within group; for each row r, `find(r2 => r2.value < r.value)` returns the row that broke it; that row's `start_date` is the end boundary."},{titleZh:`没有更好 = 仍活着`,titleEn:`No-better → still active`,bodyZh:`当前活着的纪录用今天截止;在 UI 里通常会标"仍在保持"。`,bodyEn:`Still-active records use today as the end boundary; UI typically annotates "still active".`},{titleZh:`天数降序 + Top 10`,titleEn:`Sort by days, Top 10`,bodyZh:"`days = ⌊(end − start) / 1d⌋`,按 days 降序;每区域只输出前 10 条 (加粗显示 days)。",bodyEn:"`days = ⌊(end − start) / 1d⌋`, sorted desc; emit only the Top 10 per region (with days bolded).",highlight:!0}],edgesZh:['"被打破"判定基于值严格更小 (`Number(r2[type]) < Number(r[type])`),平了不算打破。',`同人多次破自己的纪录,每条都算独立条目,寿命算到下次他自己又破。`,'`OFFICIAL_EVENTS` 过滤:`333mbo` / `magic` 等停办项目不进榜 —— 它们的"永久纪录"不公平。',`WR 持有人通常在自己洲也持有 CR,所以同一条 WR 会在 World + 其洲 2 个区块各出现一次。`],edgesEn:["Broken-by is strictly less-than (`Number(r2[type]) < Number(r[type])`); ties don't count as broken.",`Self-improvements split into multiple entries — each older record's span ends when its holder improves it.`,'`OFFICIAL_EVENTS` filter drops discontinued events (`333mbo`, `magic`, ...) — their "permanent records" would unfairly dominate.',`A WR holder also typically owns the matching CR, so the same WR row appears twice — once under World, once under its continent.`],related:[{id:`longest_streak_of_world_records`,titleZh:`连续 WR 时长`,titleEn:`WR streak duration`,hintZh:`同一人连续持有,不同口径`,hintEn:`Same person consecutive holds, different angle`},{id:`first_r_is_wr`,titleZh:`首条即 WR`,titleEn:`First record is WR`,hintZh:`纪录的开端 vs 终点`,hintEn:`Birth of a record vs longevity`},{id:`world_records_by_person`,titleZh:`个人 WR 总数`,titleEn:`WRs by person`,hintZh:`广度 vs 深度`,hintEn:`Breadth vs depth`},{id:`longest_standing_records`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`7 区域 Top 10`,hintEn:`7 regions Top 10`}]},longest_streak_of_world_records:{id:`longest_streak_of_world_records`,titleZh:`同一项目同一类型最长连续世界纪录`,titleEn:`Longest streak of world records (same event, same type)`,badgeZh:`连击`,badgeEn:`Streak`,introZh:[`在某个项目某个类型 (single 或 average) 下,**连续**由同一选手打破的 WR 链。每次有别人插进来打破,streak 就断。`,`示例:某选手在 3x3 single 上 5 次刷新自己的 WR (中间没人挤进来),即为长度 5 的 streak。`],introEn:[`A consecutive chain of WRs in one event-type (single or average) all set by **the same cuber**. The streak breaks the moment anyone else sets a WR in that event-type.`,`Example: a cuber improves their own 3x3 single WR 5 times in a row with nobody else WR-ing in between — a length-5 streak.`],stats:[{value:`> 1`,labelZh:`入榜门槛`,labelEn:`Inclusion floor`,hintZh:`至少 2 条连续才算 streak`,hintEn:`At least 2 consecutive to qualify`},{value:`日期 + 成绩`,labelZh:`同日 tiebreak`,labelEn:`Same-day tiebreak`,hintZh:`同日多 WR 按成绩降序`,hintEn:`Same date sorted by result desc`},{value:`Years`,labelZh:`排序主键`,labelEn:`Sort key`,hintZh:`末次 − 首次 WR 时长`,hintEn:`End - start span`},{value:`停办项目`,labelZh:`特殊处理`,labelEn:`Special case`,hintZh:`末次 WR 截至项目最后一场`,hintEn:`Endpoint = event's final comp`}],sourceZh:["SQL 只拉所有 `regional_*_record = WR` 行 + 必要 join (`persons sub_id = 1`, `competitions`)。streak 检测在 JS 的 `transform()` 里:对每个 (event, type) 按日期排序,顺序扫,跟同一人连续就 `count += 1`,换人就开新 streak。",'停办项目 (`333ft / magic / mmagic / 333mbo`) 的 streak 终点是该项目最后一场比赛 (`toJson` 里预查一次),不是"至今" —— 因为项目已死,不可能再有 WR。'],sourceEn:["SQL only pulls all `regional_*_record = WR` rows plus necessary joins (`persons sub_id = 1`, `competitions`). Streak detection lives in JS `transform()`: for each (event, type), sort by date and scan — same cuber as the previous WR? `count += 1`. Different cuber? Start a new streak.",'Discontinued events (`333ft / magic / mmagic / 333mbo`) terminate the final streak at the event\'s last comp (prefetched in `toJson`) — not "still active", since no new WR can occur.'],sourceCode:{lang:`sql`,body:`SELECT regional_single_record, regional_average_record,
       best single, average,
       person.name, competition.start_date,
       event_id, competition.cell_name
FROM results
JOIN persons ON persons.wca_id = person_id AND persons.sub_id = 1
JOIN competitions ON competitions.id = competition_id
WHERE regional_single_record = 'WR'
   OR regional_average_record = 'WR'`},steps:[{titleZh:`拉所有 WR 行`,titleEn:`Pull every WR row`,bodyZh:`single + average WR 都包括,行里同时带项目 + 选手 + 日期 + 比赛。`,bodyEn:`Includes both single + average WRs, each row carrying event + person + date + comp.`},{titleZh:`按 (项目, 类型) 拆桶`,titleEn:`Split by (event, type)`,bodyZh:`streak 不跨项目也不跨 single↔average:3x3 single 和 3x3 average 各自独立 streak。`,bodyEn:`Streaks don't cross events or cross single↔average: 3x3 single and 3x3 average track independently.`},{titleZh:`排序 + 顺序扫`,titleEn:`Sort + sequential scan`,bodyZh:"同日 tiebreak 按 `result desc` (成绩差的在前 / 成绩好的在后) —— 因为同日多条 WR 必然是渐进刷新。",bodyEn:`Same-day tiebreak sorts result desc (worse first, better last) — same-day multiple WRs are by definition stepwise improvements.`},{titleZh:`换人 = 断 streak`,titleEn:`Different cuber = streak break`,bodyZh:"`current.person === prev.person` 则 `count += 1`,否则封档,记录 `endDate` + `lastCompetition`。",bodyEn:"If `current.person === prev.person`, `count += 1`; otherwise close out, recording `endDate` + `lastCompetition`."},{titleZh:`按 years 降序输出`,titleEn:`Sort by years, output`,bodyZh:"years = (end − start) / 365.25。`count > 1` 才入榜,降序排,加上停办项目的特殊截止。",bodyEn:"years = (end − start) / 365.25. Only `count > 1` qualifies; sorted desc; discontinued events apply the special endpoint.",highlight:!0}],edgesZh:['streak 不要求"严格刷新":WCA `regional_*_record = WR` 本身就是当时贴的标 (能贴说明改进了),所以扫到的每条都已经是 WR。',`同一人在不同项目同时连击 → 拆成 2 个 streak (每项目独立)。`,`"仍在保持"的活 streak 用 today 算 years;实际可能下个月就被破,数字会变。`,'停办项目走 `DISCONTINUED_EVENTS = [333ft, magic, mmagic, 333mbo]` 的特殊路径 —— 这些项目最后的 WR 持有人会用项目"死亡日"截止。'],edgesEn:["Streaks don't require strict improvement: WCA's `regional_*_record = WR` marker only attaches when it's actually a new WR, so every row is by definition a WR-tier improvement.",`One cuber WR-streaking in two events simultaneously → split into 2 separate streaks (per-event isolated).`,`"Active" streaks use today for the years calc; numbers shift as the streak continues — or ends.`,"Discontinued events follow the `DISCONTINUED_EVENTS = [333ft, magic, mmagic, 333mbo]` path — final WR holders' streaks close at the event's last competition."],related:[{id:`longest_standing_records`,titleZh:`单条纪录寿命`,titleEn:`Single-record lifespan`,hintZh:`单条 vs 连续多条`,hintEn:`One record vs consecutive chain`},{id:`world_records_by_person`,titleZh:`个人 WR 总数`,titleEn:`WRs by person`,hintZh:`总数 vs 连击`,hintEn:`Total count vs streak`},{id:`first_r_is_wr`,titleZh:`首条即 WR`,titleEn:`First record is WR`,hintZh:`另一种戏剧化指标`,hintEn:`Another cinematic metric`},{id:`longest_streak_of_world_records`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`项目 × 类型完整列表`,hintEn:`Full event × type table`}]},most_delegated_competitions:{id:`most_delegated_competitions`,titleZh:`代表比赛最多`,titleEn:`Most delegated competitions`,badgeZh:`累计`,badgeEn:`Cumulative`,introZh:[`每个 WCA Delegate 历史累计代表过的比赛数。和"每年代表比赛数"是同一份基础数据,只换排序键 —— 这里看总量,那里看效率。`,"过滤口径一致:已开始的可见、未取消比赛;Delegate 角色基于 `competition_delegates` 表 (一场比赛多 Delegate = 多行)。"],introEn:[`Per-Delegate cumulative count of competitions they've overseen. Same dataset as "delegated competitions per year" but ranked by total, not yearly rate.`,"Same filter: started, visible, not-cancelled comps. Delegate roles read from `competition_delegates` (one comp with multiple delegates = multiple rows)."],stats:[{value:"`COUNT(DISTINCT)`",labelZh:`去重统计`,labelEn:`Distinct count`,hintZh:`防同人多角色重复`,hintEn:`Guards against multi-role rows`},{value:`show_at_all = 1`,labelZh:`只看公开比赛`,labelEn:`Public comps only`,hintZh:`排掉草稿`,hintEn:`Drafts excluded`},{value:`cancelled_at IS NULL`,labelZh:`排已取消`,labelEn:`Drop cancelled`,hintZh:`签了又取消不算`,hintEn:`Signed-then-cancelled excluded`},{value:`start_date < CURDATE()`,labelZh:`排未来场`,labelEn:`Past only`,hintZh:`未开始不计入`,hintEn:`Future comps excluded`}],sourceZh:["`competition_delegates` join `competitions` 三过滤后 `GROUP BY delegate_id`,`COUNT(DISTINCT competition_id)` 得累计场数;join `users` + `persons` 拿到 wca_id 和展示名。","不像 per_year 版本需要 `HAVING >= 5` —— 这里直接全列,新人 1 场也算一行。"],sourceEn:["`competition_delegates` joined to `competitions`, filtered, then `GROUP BY delegate_id` with `COUNT(DISTINCT competition_id)` for the total; `users` + `persons` joins resolve wca_id and display name.","Unlike the per-year version, there's no `HAVING >= 5` — new delegates with even 1 comp appear."],sourceCode:{lang:`sql`,body:`SELECT COUNT(DISTINCT competition_id) delegated_count,
       delegate_id
FROM competition_delegates
JOIN competitions ON competitions.id = competition_id
WHERE show_at_all = 1
  AND cancelled_at IS NULL
  AND start_date < CURDATE()
GROUP BY delegate_id
ORDER BY delegated_count DESC`},steps:[{titleZh:`同 per_year 的过滤`,titleEn:`Same filter as per_year`,bodyZh:"`show_at_all = 1 AND cancelled_at IS NULL AND start_date < CURDATE()`。",bodyEn:"`show_at_all = 1 AND cancelled_at IS NULL AND start_date < CURDATE()`."},{titleZh:`COUNT(DISTINCT competition_id)`,titleEn:`Distinct comp count`,bodyZh:`同人在同场多角色 (Delegate + Organizer + Trainee Delegate ...) 只算 1 次。`,bodyEn:`Same person at one comp with multiple roles (Delegate + Organizer + Trainee Delegate ...) counts only once.`},{titleZh:`不加 HAVING 阈值`,titleEn:`No HAVING threshold`,bodyZh:"`per_year` 要 `>= 5` 防分母过小;这里看累计,1 场也是 1 场。",bodyEn:"`per_year` requires `>= 5` to guard against tiny denominators; here we count totals, so even 1 comp counts."},{titleZh:`降序输出 + WCA list 链接`,titleEn:`Sort desc + WCA list link`,bodyZh:"附上 `https://www.worldcubeassociation.org/competitions?delegate=<id>` 让用户能跳过去看完整列表。",bodyEn:"Each row links to `https://www.worldcubeassociation.org/competitions?delegate=<id>` for the full WCA list view.",highlight:!0}],edgesZh:["Trainee Delegate (见习代表) 也是 `competition_delegates` 的一行,会被计入 —— 累计数包含见习经历。",`Senior Delegate / 区域代表角色不影响计数 —— 只要在那场比赛挂名 Delegate 就 +1。`,`从未代表过的人不上榜 —— 不是"所有 Delegate 列表",而是"代表过至少 1 场"。`],edgesEn:["Trainee delegates show as rows in `competition_delegates` and count toward the total — trainee shifts included.",`Senior Delegate / regional roles don't affect counting — any delegate-tagged appearance = +1.`,`Delegates who never delegated don't appear — this is "delegated ≥ 1 comp", not "all delegates list".`],related:[{id:`delegated_competition_per_year`,titleZh:`年均代表场数`,titleEn:`Yearly delegated rate`,hintZh:`同源数据,换排序键`,hintEn:`Same source, different sort key`},{id:`most_delegated_competitions`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`Top 代表累计场数`,hintEn:`Top delegates by cumulative comps`}]},potentially_seen_world_records:{id:`potentially_seen_world_records`,titleZh:`可能目击过的世界纪录`,titleEn:`Potentially seen world records`,badgeZh:`现场`,badgeEn:`On site`,introZh:[`一个选手参加过 WR 诞生的同一场比赛,**理论上**能现场见证它 (不一定真在场地里;同比赛同日就算)。每场每出 1 个 WR = 该比赛所有参赛选手 +1。`,`这是个"参与度 × 时代"的混合指标 —— 早入坑 + 跑勤的人见证最多,跟绝对实力关系不大。`],introEn:[`A cuber registered at the same comp as a WR-setting performance could **in principle** witness it (not literally in the same hall; same comp / same days is enough). Each WR set at a comp adds +1 to every registered cuber there.`,`A "participation × era" hybrid — early adopters who travel a lot top the list, with little correlation to skill.`],stats:[{value:`100`,labelZh:`榜单长度`,labelEn:`Leaderboard size`,hintZh:"`LIMIT 100`",hintEn:"`LIMIT 100`"},{value:"`HAVING wrs_count > 0`",labelZh:`只看有 WR 的比赛`,labelEn:`WR-containing comps only`,hintZh:`没出 WR 的比赛跳过`,hintEn:`Comps with no WR skipped`},{value:`DISTINCT`,labelZh:`人 × 比赛去重`,labelEn:`Person × comp distinct`,hintZh:`同场多项目不重复`,hintEn:`Multi-event registration deduped`},{value:`WR/比赛 求和`,labelZh:`汇总方式`,labelEn:`Aggregation`,hintZh:`到场 WR 数对人累加`,hintEn:`WRs-per-comp summed per person`}],sourceZh:["两个子查询:(1) 每场比赛的 WR 数 (`COUNT IF(regional_single_record = WR) + IF(regional_average_record = WR)`),(2) 每人参加过哪些比赛 (`SELECT DISTINCT person_id, competition_id`)。","join 后 `SUM(wrs_count) GROUP BY person_id` —— 这个人参加的每场比赛的 WR 数之和。"],sourceEn:["Two subqueries: (1) WRs-per-comp (`COUNT IF(regional_single_record = WR) + IF(regional_average_record = WR)`), (2) comps-each-person-attended (`SELECT DISTINCT person_id, competition_id`).","Join then `SUM(wrs_count) GROUP BY person_id` — total WRs across all comps that cuber attended."],sourceCode:{lang:`sql`,body:`SELECT person_id, SUM(wrs_count) potentially_seen_wrs_count
FROM (SELECT DISTINCT person_id, competition_id FROM results) AS attendance
JOIN (
  SELECT competition_id,
         SUM(IF(regional_single_record = 'WR', 1, 0)
           + IF(regional_average_record = 'WR', 1, 0)) wrs_count
  FROM results
  GROUP BY competition_id
  HAVING wrs_count > 0
) AS wrs_by_comp USING (competition_id)
GROUP BY person_id LIMIT 100`},steps:[{titleZh:`算每场比赛的 WR 数`,titleEn:`Count WRs per comp`,bodyZh:"内层 `GROUP BY competition_id`,数 `regional_single_record = WR` + `regional_average_record = WR` —— 一行可能 2 个 WR (single + average 同时刷)。",bodyEn:"Inner `GROUP BY competition_id`, count `regional_single_record = WR` + `regional_average_record = WR` — one result row can contribute 2 (single + average broken together)."},{titleZh:`过滤有 WR 的比赛`,titleEn:`Keep only WR-containing comps`,bodyZh:"`HAVING wrs_count > 0` —— 没出 WR 的比赛直接丢,不参与 join,加速 + 减小输出。",bodyEn:"`HAVING wrs_count > 0` — comps with no WR are dropped before the join, speeding things up and shrinking output."},{titleZh:`取每人参加比赛集合`,titleEn:`Collect each person's comps`,bodyZh:"`SELECT DISTINCT person_id, competition_id FROM results` —— 一人在一场算 1 次,不管打几个项目。",bodyEn:"`SELECT DISTINCT person_id, competition_id FROM results` — one cuber per comp = 1, regardless of how many events they entered."},{titleZh:`join + SUM 累加`,titleEn:`Join + SUM`,bodyZh:'每个 (person, comp) 行附带该比赛的 wrs_count;`GROUP BY person_id SUM(...)` 就是"理论目击 WR 总数"。',bodyEn:'Each (person, comp) row carries that comp\'s wrs_count; `GROUP BY person_id SUM(...)` gives "potentially seen WR count".',highlight:!0}],edgesZh:[`"目击"是理论值 —— 选手实际不一定在该轮在场,可能在午餐 / 没看屏幕。`,`同场 single + average 双 WR 算 2 次 —— 即使是同一个选手同一轮,也都贡献。`,`只统计 WR;NR / CR 不算 —— 想拓宽换 records_in_most_events 思路。`,`Top 100 截断:尾部几千名选手 ≥ 1 的有很多,看不见。`],edgesEn:[`"Witnessed" is purely theoretical — the cuber may have been at lunch or not watching the right station.`,`A single comp with both single + average WR counts as 2 — even if same cuber same round.`,`WRs only; NR / CR don't count — a broader view would need a different metric.`,`LIMIT 100 truncation: many thousands of cubers with ≥ 1 sit invisible below.`],related:[{id:`world_records_by_person`,titleZh:`个人 WR 数`,titleEn:`WRs by person`,hintZh:`自己创造 vs 现场目击`,hintEn:`Set vs witnessed`},{id:`world_championship_records`,titleZh:`世锦赛纪录`,titleEn:`Worlds records`,hintZh:`世锦赛上的 WR 子集`,hintEn:`Worlds-only WR subset`},{id:`longest_streak_of_world_records`,titleZh:`连续 WR 持有`,titleEn:`WR streak`,hintZh:`另一类 WR 视角`,hintEn:`Another WR-centric lens`},{id:`potentially_seen_world_records`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`Top 100 现场参与者`,hintEn:`Top 100 on-site attendees`}]},records_in_most_events:{id:`records_in_most_events`,titleZh:`在最多项目中打破纪录`,titleEn:`Records in the highest number of events`,badgeZh:`广度`,badgeEn:`Breadth`,introZh:["每个选手在多少**不同项目**上打破过 regional record (历史上,不限当前)。分 3 个级别看:World (`WR`) / Continental (`AfR/AsR/ER/NAR/OcR/SAR/WR`) / National (`NR + 上级`)。","高级别天然包含低级别 —— `WR` 必然也是该洲 CR、该国 NR,所以 World 榜的人在 Continental / National 榜上一定项目数 ≥。"],introEn:["Per cuber: how many **distinct events** they've ever set a regional record in (any historical record, not just current). Three tiers shown: World (`WR`) / Continental (`AfR/AsR/ER/NAR/OcR/SAR/WR`) / National (`NR + above`).","Higher tiers cascade into lower: a `WR` is automatically also that continent's CR and that country's NR, so the World leaderboard cuber appears in Continental / National with event count ≥."],stats:[{value:`3`,labelZh:`级别`,labelEn:`Tiers`,hintZh:`World / Continental / National`,hintEn:`World / Continental / National`},{value:`20`,labelZh:`每级条数`,labelEn:`Rows per tier`,hintZh:`Top 20`,hintEn:`Top 20`},{value:`历史全口径`,labelZh:`不限当前`,labelEn:`All-time`,hintZh:`过去 WR 即使现已超越也算`,hintEn:`Old WRs count even if since broken`},{value:"`event.rank` 排序",labelZh:`项目列展示`,labelEn:`Events list order`,hintZh:`WCA 官方项目顺序`,hintEn:`Official WCA event order`}],sourceZh:['SQL 拉所有 `regional_single_record` 或 `regional_average_record` 非空的行,带 `person` + `event` 信息;具体哪些 code 算"该级别"在 JS `transform()` 里按 `levels` 字典过滤。',"同一选手在同项目多次破不同级别纪录,JS 用 `Set<event_name>` 去重 —— 一个项目数 1 次。"],sourceEn:["SQL pulls every row with non-empty `regional_single_record` or `regional_average_record`, carrying `person` + `event`; which codes qualify per tier is decided in JS `transform()` via the `levels` dict.","Multiple records by same cuber in same event dedupe via JS `Set<event_name>` — one event counted once."],sourceCode:{lang:`sql`,body:`SELECT regional_single_record, regional_average_record,
       person.name, event.name event_name
FROM results
JOIN persons person ON person.wca_id = person_id AND sub_id = 1
JOIN events event ON event.id = event_id
WHERE (regional_single_record IS NOT NULL AND regional_single_record != '')
   OR (regional_average_record IS NOT NULL AND regional_average_record != '')
ORDER BY event.rank`},steps:[{titleZh:`拉所有带 record 标记的行`,titleEn:`Pull every record-flagged row`,bodyZh:"WCA `regional_*_record` 列在打破时贴标,空字符串 / NULL 表示没破;两个非空条件 OR 拉全集。",bodyEn:"WCA stamps `regional_*_record` at break-time; empty/NULL = no break. Two non-empty conditions OR'd pull the full set."},{titleZh:`按级别过滤 code`,titleEn:`Filter codes per tier`,bodyZh:"World tier 只接 `WR`;Continental 接 `WR + 6 个 CR code`;National 再加 `NR`。",bodyEn:"World tier accepts `WR` only; Continental adds the 6 continental codes; National adds `NR` too."},{titleZh:`按选手 Set<event> 去重`,titleEn:`Per-cuber dedupe via Set<event>`,bodyZh:"`byPerson: Map<person, Set<event>>`;一个项目无论破多少次只算 1。",bodyEn:"`byPerson: Map<person, Set<event>>`; an event counted once no matter how many times broken."},{titleZh:`按 events.size 降序,Top 20`,titleEn:`Sort by events.size, Top 20`,bodyZh:'`results.size` 即"破纪录涉及项目数",降序取 Top 20。',bodyEn:'`results.size` = "number of events with records"; sort desc and take Top 20.',highlight:!0}],edgesZh:['"项目"按 `event.name` 字符串去重 —— 已停办项目 (magic / 333mbo) 都计入,因为是历史口径。',`NR 在小国家相对容易拿,所以 National tier 榜单偏向"项目多 × 国家小"的选手;3 个级别一起看更公平。`,"`sub_id = 1` 过滤改名 / 国籍变化的副行,避免同人多算。"],edgesEn:['"Event" deduped by `event.name` string — discontinued events (magic / 333mbo) all count, since this is historical.',`NR is comparatively easy in small countries, so the National tier tilts toward "many events × small country" cubers — read all 3 tiers together for fairness.`,"`sub_id = 1` filter drops alt-identity rows from renames / nationality changes to prevent double-counting."],related:[{id:`world_records_by_person`,titleZh:`个人 WR 总数`,titleEn:`WRs by person`,hintZh:`广度 vs 深度`,hintEn:`Breadth vs depth`},{id:`first_r_is_wr`,titleZh:`首条即 WR`,titleEn:`First record is WR`,hintZh:`入门即 WR vs 多项目均刷`,hintEn:`Debut WR vs many-event sweep`},{id:`longest_standing_records`,titleZh:`最长保持纪录`,titleEn:`Longest-standing records`,hintZh:`广度 vs 单条寿命`,hintEn:`Breadth vs single-record longevity`},{id:`records_in_most_events`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`3 级别 Top 20`,hintEn:`3 tiers Top 20`}]},winned_week_count:{id:`winned_week_count`,titleZh:`获胜周数`,titleEn:`Winned week count`,badgeZh:`周王`,badgeEn:`Week king`,introZh:[`把时间切成 ISO 周 (周一 ~ 周日),对每个项目数:这个选手在多少周内做出了**当周全球最快单次**。`,`"周王" —— 看的不是历史总分,而是周与周的统治力。一个 WR 持有几年可能就几个 winned weeks (因为后面有别人小幅打破),而高频出战的选手可能积累大量。`],introEn:[`Slices time into ISO weeks (Mon-Sun). Per event, counts: in how many weeks did this cuber post the **globally fastest single of that week**.`,`"Week king" — measures week-by-week dominance, not all-time totals. A long-standing WR holder might only have a few winned weeks (anyone else beating it for one week ends the streak), while a frequent flyer accumulates many.`],stats:[{value:`ISO 周`,labelZh:`时间粒度`,labelEn:`Time bucket`,hintZh:`周一 00:00 ~ 周日 23:59`,hintEn:`Mon 00:00 - Sun 23:59`},{value:`只看 single`,labelZh:`指标`,labelEn:`Metric`,hintZh:"`best` 列,不看 average",hintEn:"`best` only, no average"},{value:`20`,labelZh:`每项目条数`,labelEn:`Per-event rows`,hintZh:`Top 20`,hintEn:`Top 20`},{value:`并列计数`,labelZh:`同周多人`,labelEn:`Co-winners`,hintZh:"`DISTINCT (event,best,week)`",hintEn:"`DISTINCT (event,best,week)`"}],sourceZh:["内层 SQL:对每周 (`WEEKDAY` + `DATE_ADD` 算周一 / 周日) 算 `MIN(best)` —— 该周该项目的最佳单次。外层 join 回 `results`,找出 `best = week_best` 的选手 ,`GROUP BY event_id, person_id` 数 winned_weeks。","排序在 JS `transform()` 里完成:按项目分组,各取 Top 20。"],sourceEn:["Inner SQL: for each ISO week (`WEEKDAY` + `DATE_ADD` to derive Mon / Sun bounds), compute `MIN(best)` — the week's best single per event. Outer joins back to `results`, finds cubers matching `best = week_best`, `GROUP BY event_id, person_id` counts winned_weeks.","Sorting happens in JS `transform()` — group by event, take Top 20 each."],sourceCode:{lang:`sql`,body:`-- per-week best per event (inner)
SELECT event_id, MIN(best) week_best,
       DATE_ADD(start_date, INTERVAL(-WEEKDAY(start_date)) DAY) week_start
FROM results
JOIN competitions ON competitions.id = competition_id
WHERE best > 0
GROUP BY event_id, week_start

-- (outer) match cubers to week_best, count distinct weeks won
SELECT event_id, person_id,
       COUNT(DISTINCT event_id, best, week_start_date) winned_weeks
FROM ... GROUP BY event_id, person_id`},steps:[{titleZh:`推 ISO 周边界`,titleEn:`Derive ISO week bounds`,bodyZh:"`week_start = start_date - WEEKDAY(start_date)` (周一), `week_end = start_date + (6 - WEEKDAY)` (周日)。",bodyEn:"`week_start = start_date - WEEKDAY(start_date)` (Monday), `week_end = start_date + (6 - WEEKDAY)` (Sunday)."},{titleZh:`算每周每项目 MIN(best)`,titleEn:`Compute per-week per-event MIN(best)`,bodyZh:'`GROUP BY event_id, week_start_date`,得"该周该项目最快单次"。',bodyEn:'`GROUP BY event_id, week_start_date`, yielding "fastest single per event per week".'},{titleZh:`join 回 results 找持有者`,titleEn:`Join back to find holder(s)`,bodyZh:'匹配 `best = week_best` + 比赛日期落在周窗内 —— 不只一人时,大家都算"周王" (并列)。',bodyEn:"Match `best = week_best` + comp date in the week window — multiple matches all win the week (tied co-kings)."},{titleZh:`COUNT DISTINCT 防同周多场`,titleEn:`COUNT DISTINCT for multi-comp weeks`,bodyZh:"`COUNT(DISTINCT event_id, best, week_start)` —— 同周该选手在多场比赛都跑到 week_best,也算 1 周。",bodyEn:"`COUNT(DISTINCT event_id, best, week_start)` — if a cuber matched week_best at multiple comps in the same week, still 1 week."},{titleZh:`按项目降序 Top 20`,titleEn:`Sort desc, Top 20 per event`,bodyZh:"JS 里按 `event_id` 桶分,每桶 winned_weeks 降序前 20。",bodyEn:"JS buckets by `event_id`, each bucket sorted desc by winned_weeks, Top 20.",highlight:!0}],edgesZh:["ISO 周以周一为起点(用 `WEEKDAY()`,周一 = 0,周日 = 6) —— 跨年 W52/W01 都按数学推,不走 ISO `YEARWEEK()` 复杂规则。",`没有比赛的周不计入 (没人跑出 week_best) —— 比如圣诞 / 疫情期间会有"空周"。`,`同周并列 (多人同值) 全部算赢家,不分先后日期 —— 这是 WCA "regional records 同分并列"的延伸。`,`只看 single,不算 average —— 类似指标用 average 会受 5 次/掐尾规则影响,口径复杂。`],edgesEn:["ISO weeks start Monday (`WEEKDAY()` = 0 for Mon, 6 for Sun) — year boundaries W52/W01 follow simple date math, not the formal ISO `YEARWEEK()` rule.",`Weeks with no comps don't count (no one posted week_best) — Christmas / pandemic gaps produce empty weeks.`,`Same-week ties all win — extending WCA's "tied regional records both count" rule.`,`Single only, not average — average's 5-solve trim rules complicate the metric.`],related:[{id:`world_records_by_person`,titleZh:`个人 WR 数`,titleEn:`WRs by person`,hintZh:`WR vs 周王`,hintEn:`WR vs week king`},{id:`wr_dominance`,titleZh:`WR 支配度`,titleEn:`WR dominance`,hintZh:`长期统治力`,hintEn:`Long-term dominance`},{id:`longest_streak_of_world_records`,titleZh:`连续 WR 链`,titleEn:`WR streak`,hintZh:`连续刷新 vs 周冠数`,hintEn:`Consecutive holds vs week counts`},{id:`winned_week_count`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`项目切换 + Top 20`,hintEn:`Event picker + Top 20`}]},world_championship_podiums_by_country:{id:`world_championship_podiums_by_country`,titleZh:`各国世锦赛领奖台次数`,titleEn:`World Championship podiums by country`,badgeZh:`世锦赛`,badgeEn:`Worlds`,introZh:[`只看 WCA World Championship (官方世锦赛,每 2 年一届) 的决赛领奖台,按选手国籍汇总金 / 银 / 铜。`,'通过 `championships` 表里 `championship_type = world` 标识 —— WCA dump 用这张表标注每场比赛"是否是某届世锦赛"。'],introEn:[`Counts decision-round podiums at WCA World Championships (every 2 years) only, aggregated by competitor nationality (gold / silver / bronze).`,'A comp is "Worlds" if `championships.championship_type = world`. WCA\'s dump uses this table to tag each comp with its championship status.'],stats:[{value:"`championship_type = world`",labelZh:`世锦赛过滤`,labelEn:`Worlds filter`,hintZh:`排掉 nationals / continentals`,hintEn:`Excludes nationals / continentals`},{value:`决赛轮`,labelZh:`只看金银铜`,labelEn:`Final-round only`,hintZh:"`round_type_id IN (c, f)`",hintEn:"`round_type_id IN (c, f)`"},{value:"`pos ∈ {1,2,3}`",labelZh:`领奖台`,labelEn:`Podium`,hintZh:`WCA 预算 pos`,hintEn:`WCA-precomputed pos`},{value:`金 → 银 → 铜`,labelZh:`排序键`,labelEn:`Sort key`,hintZh:`奥运奖牌榜风格`,hintEn:`Olympic medal-table style`}],sourceZh:["`results` join `competitions` join `championships` (内连接,只留 Worlds 比赛)。同 medal_from_abroad 套路:决赛轮 + 完赛 + `SUM IF(pos = ...)` 三档分别汇总;`GROUP BY result.country_id` 按选手国籍。"],sourceEn:["`results` joined to `competitions` joined to `championships` (inner join keeps Worlds only). Same medal_from_abroad pattern: final rounds + completed + `SUM IF(pos = ...)` for each tier; `GROUP BY result.country_id` by competitor nationality."],sourceCode:{lang:`sql`,body:`SELECT result.country_id,
       SUM(IF(pos = 1, 1, 0)) gold_medals,
       SUM(IF(pos = 2, 1, 0)) silver_medals,
       SUM(IF(pos = 3, 1, 0)) bronze_medals
FROM results
JOIN competitions ON competitions.id = competition_id
JOIN championships ON championships.competition_id = results.competition_id
WHERE round_type_id IN ('c', 'f')
  AND best > 0
  AND championship_type = 'world'
GROUP BY result.country_id`},steps:[{titleZh:"join `championships` 锁 Worlds",titleEn:"Join `championships`, filter Worlds",bodyZh:"内连接自动过滤掉非世锦赛比赛;再 `championship_type = world` 排掉洲锦赛 / 国锦赛。",bodyEn:"Inner join automatically drops non-championship comps; `championship_type = world` further excludes continentals / nationals."},{titleZh:`保留决赛轮 + 完赛`,titleEn:`Keep finals + completed`,bodyZh:"`round_type_id IN (c, f)` + `best > 0`,同 by_abroad。",bodyEn:"`round_type_id IN (c, f)` + `best > 0`, same as by_abroad."},{titleZh:`三档分别 SUM`,titleEn:`Sum each medal tier`,bodyZh:"`SUM(IF(pos = 1, 1, 0))` 金,同理银 / 铜。",bodyEn:"`SUM(IF(pos = 1, 1, 0))` for gold, similarly silver / bronze."},{titleZh:`按选手国籍汇总`,titleEn:`Roll up by competitor country`,bodyZh:"`GROUP BY result.country_id` —— 即使该届世锦赛在美国办,中国选手拿金牌算 CN gold。",bodyEn:"`GROUP BY result.country_id` — if Worlds is in the US but a CN cuber wins gold, it counts as CN gold.",highlight:!0}],edgesZh:[`世锦赛办过的届数有限 (1982 第一届,2003 复办后每 2 年一届),所以总样本远小于"海外奖牌";顶部国家以少胜多。`,"`championships` 表对每场比赛只挂 1 个 championship_type;WC + CC 双挂的情况 (一个比赛同时是世锦赛和洲锦赛) 极少。",'同 medal_from_abroad,`best > 0` 防 DNF "登顶"。'],edgesEn:[`Worlds has happened sparingly (1982 first, biennial since 2003), so sample size is much smaller than abroad medals; top-country totals are concentrated.`,"`championships` table assigns one championship_type per comp; a single comp tagged both Worlds + Continental is essentially nonexistent.","Same as medal_from_abroad, `best > 0` strips DNF-as-1st cases."],related:[{id:`world_championship_podiums_by_person`,titleZh:`世锦赛奖牌(选手)`,titleEn:`Worlds podiums by person`,hintZh:`换个体维度`,hintEn:`Per-person version`},{id:`world_championship_records`,titleZh:`世锦赛纪录`,titleEn:`Worlds records`,hintZh:`世锦赛 WR 子集`,hintEn:`Worlds-WR subset`},{id:`best_medal_collection_from_abroad_by_country`,titleZh:`海外奖牌(国家)`,titleEn:`Abroad medals (country)`,hintZh:`所有海外赛 vs 仅世锦赛`,hintEn:`All abroad vs Worlds only`},{id:`world_championship_podiums_by_country`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`国家奖牌榜`,hintEn:`Country medal table`}]},world_championship_podiums_by_person:{id:`world_championship_podiums_by_person`,titleZh:`世锦赛领奖台次数(选手)`,titleEn:`World Championship podiums by person`,badgeZh:`世锦赛`,badgeEn:`Worlds`,introZh:["同 by_country,但 `GROUP BY person_id`,按选手累计世锦赛金 / 银 / 铜。一个选手在多届 Worlds 多个项目累计,数字可以很大。","同 medal_from_abroad,过滤是决赛 + 完赛 + `championship_type = world`。"],introEn:["Same as by_country, but `GROUP BY person_id` — gold / silver / bronze a single cuber has accumulated across Worlds editions. Numbers can climb high when one cuber wins multiple events across multiple Worlds.","Same filter chain as medal_from_abroad: final round + completed + `championship_type = world`."],stats:[{value:`sub_id = 1`,labelZh:`主身份`,labelEn:`Primary identity`,hintZh:`过滤改名 / 国籍变化副行`,hintEn:`Filters alt rows from renames / nationality`},{value:"`championship_type = world`",labelZh:`世锦赛过滤`,labelEn:`Worlds filter`,hintZh:`同 by_country`,hintEn:`Same as by_country`},{value:`金 → 银 → 铜`,labelZh:`排序`,labelEn:`Sort`,hintZh:`奥运奖牌榜风格`,hintEn:`Olympic medal-table style`},{value:`决赛 + 完赛`,labelZh:`入榜条件`,labelEn:`Inclusion`,hintZh:"`round_type_id IN (c, f)` + `best > 0`",hintEn:"`round_type_id IN (c, f)` + `best > 0`"}],sourceZh:["同 by_country,改 `GROUP BY person_id`;join `persons` (`sub_id = 1`) 拿展示名;无 LIMIT —— 不像 abroad 截到 100,Worlds 因为样本小本来就短。"],sourceEn:["Same as by_country, `GROUP BY person_id`; join `persons` (`sub_id = 1`) for display name; no LIMIT — unlike abroad which caps at 100, Worlds is short by nature."],sourceCode:{lang:`sql`,body:`SELECT person_id,
       SUM(IF(pos = 1, 1, 0)) gold_medals,
       SUM(IF(pos = 2, 1, 0)) silver_medals,
       SUM(IF(pos = 3, 1, 0)) bronze_medals
FROM results
JOIN competitions ON competitions.id = competition_id
JOIN championships ON championships.competition_id = results.competition_id
WHERE round_type_id IN ('c', 'f')
  AND best > 0
  AND championship_type = 'world'
GROUP BY person_id`},steps:[{titleZh:`同 by_country 的过滤`,titleEn:`Same filter as by_country`,bodyZh:"`championship_type = world` + 决赛 + 完赛。",bodyEn:"`championship_type = world` + final-round + completed."},{titleZh:"`GROUP BY person_id`",titleEn:`Aggregate by person`,bodyZh:`同 medal_from_abroad_by_person 套路。`,bodyEn:`Same pattern as medal_from_abroad_by_person.`},{titleZh:"join `persons` 主身份",titleEn:"Join `persons` primary",bodyZh:"`sub_id = 1` 防同人多行重复;改国籍的选手保留当前国名展示。",bodyEn:"`sub_id = 1` prevents duplicate rows from identity changes; cubers who changed nationality show current country."},{titleZh:`排序输出`,titleEn:`Sort + emit`,bodyZh:"`gold DESC, silver DESC, bronze DESC, name`;Worlds 样本小,Top 10 就能看到主要面孔。",bodyEn:"`gold DESC, silver DESC, bronze DESC, name`; Worlds sample is small, the Top 10 already covers most familiar names.",highlight:!0}],edgesZh:['选手国籍变化:dump 里 `result.country_id` 是当时国籍,不会回填;但 `persons.country_id` (sub_id = 1) 显示当前国籍。"国家"列可能跟历史身份不符。',`同届世锦赛多项目多领奖台 → 多次累计 (无去重)。`,`Worlds 样本量小,前 5 大概覆盖一半 —— 头部效应明显。`],edgesEn:["Nationality changes: `result.country_id` is at-time, never back-filled; `persons.country_id` (sub_id = 1) is current. The displayed country may not match the country at the time of the medal.",`Multiple events at one Worlds = multiple medal accumulations (no per-comp dedup).`,`Worlds sample is small; the top 5 cover about half the total — heavy long tail.`],related:[{id:`world_championship_podiums_by_country`,titleZh:`世锦赛奖牌(国家)`,titleEn:`Worlds podiums by country`,hintZh:`聚合到国家维度`,hintEn:`Country-level aggregation`},{id:`world_championship_records`,titleZh:`世锦赛纪录`,titleEn:`Worlds records`,hintZh:`Worlds 的 WR 子集`,hintEn:`Worlds-only WR subset`},{id:`best_medal_collection_from_abroad_by_person`,titleZh:`海外奖牌(选手)`,titleEn:`Abroad medals (person)`,hintZh:`更广口径`,hintEn:`Broader-scope counterpart`},{id:`world_championship_podiums_by_person`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`选手奖牌榜`,hintEn:`Person medal table`}]},world_championship_records:{id:`world_championship_records`,titleZh:`世锦赛纪录`,titleEn:`World Championship records`,badgeZh:`世锦赛`,badgeEn:`Worlds`,introZh:[`类似奥运纪录:在所有 WCA 世锦赛上,每个项目的最佳 single 和 average。**不要求是 WR** —— 即使比当时 WR 慢,也算"世锦赛纪录"。`,`换言之:把全 Worlds 的成绩缩成 (项目, 类型) 二维,各取最快。`],introEn:[`Like Olympic records: for every event, the best single and best average ever achieved across all WCA Worlds. **Not required to be a WR** — even if slower than the prevailing WR, it's still the "Worlds record".`,`In effect: shrink all Worlds results to a (event, type) 2D grid, keeping the fastest each.`],stats:[{value:`2`,labelZh:`类型`,labelEn:`Types`,hintZh:`Single + Average`,hintEn:`Single + Average`},{value:"`SolveTime` 比较",labelZh:`DNF 安全`,labelEn:`DNF-safe`,hintZh:`不简单 MIN,要 isComplete()`,hintEn:`Not naive MIN — uses isComplete()`},{value:`EVENTS_ENTRIES`,labelZh:`官方项目顺序`,labelEn:`Official event order`,hintZh:`WCA event sort`,hintEn:`WCA event sort`},{value:"`championship_type = world`",labelZh:`世锦赛过滤`,labelEn:`Worlds filter`,hintZh:`同 podium 系列`,hintEn:`Same as podium siblings`}],sourceZh:['SQL 只拉所有 Worlds 比赛 (`championships.championship_type = world`) 的 `results` + 选手 + 国家 + 比赛信息。"找最快"在 JS `transform()` 用 `SolveTime.compareTo()` 比较 (DNF 自动排到最后)。'],sourceEn:['SQL pulls all Worlds (`championships.championship_type = world`) results + person + country + comp info. "Find fastest" runs in JS `transform()` via `SolveTime.compareTo()` (DNF auto-sorts to last).'],sourceCode:{lang:`sql`,body:`SELECT event_id,
       person.name, competition.cell_name,
       country.name country_name,
       best single, average
FROM results
JOIN persons person ON person.wca_id = person_id AND person.sub_id = 1
JOIN competitions competition ON competition.id = competition_id
JOIN countries country ON country.id = person.country_id
JOIN championships ON championships.competition_id = results.competition_id
WHERE championship_type = 'world'`},steps:[{titleZh:`拉所有 Worlds 成绩`,titleEn:`Pull all Worlds results`,bodyZh:"inner join `championships` + `WHERE championship_type = world`;不限决赛 (预赛 / 半决也算 —— 那一轮若打出最快,也是世锦赛纪录)。",bodyEn:"Inner join `championships` + `WHERE championship_type = world`. Doesn't restrict to finals — semi-finals / first rounds qualify if they happened to be the fastest."},{titleZh:`按 (项目, 类型) 桶分`,titleEn:`Bucket by (event, type)`,bodyZh:"外层循环 `[Single, Average]`,内层 `Map<event, {result, row}>` 追踪当前最佳。",bodyEn:"Outer loop on `[Single, Average]`, inner `Map<event, {result, row}>` tracks current best."},{titleZh:"`SolveTime.compareTo()` 比较",titleEn:"`SolveTime.compareTo()` comparison",bodyZh:"不直接比 `Number(best)` —— `SolveTime` 帮你处理 DNF 编码 (-1) / 未提交 (0) / FMC / MBLD 等特殊指标。",bodyEn:"Doesn't directly compare `Number(best)` — `SolveTime` handles DNF encoding (-1) / unattempted (0) / FMC / MBLD special metrics."},{titleZh:`按 EVENTS_ENTRIES 顺序输出`,titleEn:`Emit per EVENTS_ENTRIES order`,bodyZh:`按 WCA 官方项目顺序 (3x3 → 2x2 → 4x4 → ...) 排序输出 —— 每项目 1 行。`,bodyEn:`Output in WCA official event order (3x3 → 2x2 → 4x4 → ...) — one row per event.`},{titleZh:"过滤 `isComplete()`",titleEn:"Filter via `isComplete()`",bodyZh:"`!entry.result.isComplete()` (即 DNF) 的项目不输出 —— 譬如某项目所有 Worlds 历史 average 都 DNF (罕见),那个项目不上表。",bodyEn:"`!entry.result.isComplete()` (i.e. DNF) — events where all Worlds-history averages are DNF (rare) get skipped.",highlight:!0}],edgesZh:[`"世锦赛纪录"≠ WR;比 WR 慢的成绩照样可以是 WCR (e.g. 某项目 WR 在某届非 Worlds 比赛打出,Worlds 上的最佳 < WR)。`,`不限决赛 —— 预赛打出超快单次也算 (虽然实际罕见,因为决赛通常是表现高点)。`,"FMC / MBLD 的 average 计算用 `SolveTime` 内部规则;MBLD `best` 用 score 编码 (尝试 / 完成 / 时间),不要直接看数值。"],edgesEn:[`"Worlds record" ≠ WR; a slower-than-WR result can still be the WCR (e.g. an event's WR was set at a non-Worlds comp; the best at Worlds is slower than the WR).`,`Doesn't restrict to finals — prelim heats can hold the record (rare in practice since finals usually peak).`,"FMC / MBLD averages use `SolveTime` internal rules; MBLD `best` encodes score (attempted / solved / time), don't read raw number."],related:[{id:`world_championship_podiums_by_country`,titleZh:`世锦赛奖牌(国家)`,titleEn:`Worlds medals (country)`,hintZh:`同源,看奖牌而非纪录`,hintEn:`Same source, medals not records`},{id:`world_championship_podiums_by_person`,titleZh:`世锦赛奖牌(选手)`,titleEn:`Worlds medals (person)`,hintZh:`同源,选手维度`,hintEn:`Same source, per-person`},{id:`world_records_by_person`,titleZh:`个人 WR 总数`,titleEn:`WRs by person`,hintZh:`WCR vs WR`,hintEn:`WCR vs WR`},{id:`world_championship_records`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`Single + Average 双表`,hintEn:`Single + Average dual table`}]},world_records_by_country:{id:`world_records_by_country`,titleZh:`各国世界纪录数量`,titleEn:`World records by country`,badgeZh:`国家`,badgeEn:`Country`,introZh:["**历史所有** WR 按选手国籍累加 (不像 `current_world_records_by_country` 只数当前)。同一选手刷自己 5 次 = 5 个 WR;刷一次 single + 一次 average = 2 个。","附带按年份累计的时间线 —— `/globe` 的 WR choropleth + year slider 用这个;`/wca/world_records_by_country` 页本身的年份 slider 也是。"],introEn:["Counts **all-time** WRs by competitor nationality (unlike `current_world_records_by_country` which counts only currently held). One cuber improving their own WR 5 times = 5 WRs; one single + one average WR = 2.","Ships with a per-year cumulative timeline — used by `/globe` WR choropleth + year slider, and the year slider on the `/wca/world_records_by_country` page itself."],stats:[{value:`YEAR(start_date)`,labelZh:`年份切片`,labelEn:`Year bucketing`,hintZh:`WCA 比赛起始日为准`,hintEn:`Based on comp start date`},{value:`single + average`,labelZh:`WR 计数`,labelEn:`WR counting`,hintZh:`每行至多 2 个 WR`,hintEn:`Up to 2 WRs per row`},{value:`years[]`,labelZh:`稠密年份轴`,labelEn:`Dense year axis`,hintZh:`空年也占位`,hintEn:`Empty years held as placeholders`},{value:`cumulative{}`,labelZh:`累计稠密数组`,labelEn:`Cumulative dense array`,hintZh:`每国一个 years.length 数组`,hintEn:`One years.length array per country`}],sourceZh:["SQL `GROUP BY country.id, year`,`SUM(IF(... = WR, 1, 0))` 数 WR 次数。`toJson()` 后处理:把 (country, year, count) 稀疏行转成稠密 `cumulative: {country: number[]}` —— 索引 i 对应 years[i] 年累计值。","`years[]` 从最早 WR 年到当前年 (`minYear` .. `new Date().getFullYear()`);中间没 WR 的年也占一格,保证时间轴连续。"],sourceEn:["SQL `GROUP BY country.id, year`, `SUM(IF(... = WR, 1, 0))` counts WRs. `toJson()` post-processing: convert sparse (country, year, count) rows into a dense `cumulative: {country: number[]}` — index i corresponds to years[i] cumulative.","`years[]` spans `minYear` .. current year; empty years still take a slot to keep the timeline contiguous."],sourceCode:{lang:`sql`,body:`SELECT country.name AS country,
       YEAR(comp.start_date) AS year,
       SUM(IF(regional_single_record = 'WR', 1, 0)
         + IF(regional_average_record = 'WR', 1, 0)) AS wrs
FROM results
JOIN competitions comp ON comp.id = results.competition_id
JOIN countries country ON country.id = results.country_id
WHERE regional_single_record = 'WR' OR regional_average_record = 'WR'
GROUP BY country.id, year
ORDER BY year, country.name`},steps:[{titleZh:`过滤 WR 行`,titleEn:`Filter WR-only rows`,bodyZh:"`WHERE regional_single_record = WR OR regional_average_record = WR` —— 排掉 NR / CR / 空。",bodyEn:"`WHERE regional_single_record = WR OR regional_average_record = WR` — drops NR / CR / blank."},{titleZh:`按 (国家, 年) 分组`,titleEn:`Bucket by (country, year)`,bodyZh:"`YEAR(comp.start_date)` 切年;SUM 两列 IF 加起来 —— 同行 single + average WR 双开算 2。",bodyEn:"`YEAR(comp.start_date)` for year; SUM both `IF` columns — single + average on same row contribute 2."},{titleZh:`推稠密 years[]`,titleEn:`Derive dense years[]`,bodyZh:"JS 算 `minYear`,从 minYear 到当前年逐年 push;空年保留为 0。",bodyEn:"JS computes `minYear` and pushes year by year up to current; empty years stay 0."},{titleZh:`累计扫描`,titleEn:`Cumulative scan`,bodyZh:"每国一个数组,`cum += byYear.get(y) ?? 0`,产生 N 年长度的累计序列。",bodyEn:"Per country, `cum += byYear.get(y) ?? 0`, yielding N-year cumulative sequence."},{titleZh:`主 rows = 截至最后一年`,titleEn:`Main rows = final-year total`,bodyZh:"`rows = cumulative.map(arr => arr[last])`,按总数降序 + 国名字母序 (tiebreak);0 国家过滤。",bodyEn:"`rows = cumulative.map(arr => arr[last])`, sorted desc + country name tiebreak; countries with 0 filtered out.",highlight:!0}],edgesZh:[`历史 WR 不会因后续超越而清除标记 —— 算的就是"曾经的 WR",所以同一项目 WR 进化史里每条都进总数。`,'"国家"按打破 WR 时的选手国籍 (`result.country_id` 当时值);改国籍后旧 WR 仍归原国。',`WCA dump 是历史口径,WR 标记跟"破纪录顺序"绑死 (并列 WR 都打标);所以同值并列时 2 个国家各 +1。`,"`/globe` 把 `cumulative` 一帧帧映射成 choropleth,时间轴回放 1982 → 今天的 WR 集中度演化。"],edgesEn:[`Historical WR markers don't get removed when broken — this is "ever-WR" count, so every entry in an event's WR evolution feeds the total.`,"Country = competitor nationality at time of record (`result.country_id` at-time); changing nationality doesn't reattribute old WRs.",`WCA dump treats record markers as "did this break the WR" (ties both tagged); so tied WRs credit 2 countries each +1.`,"`/globe` maps `cumulative` frame-by-frame onto a choropleth — time slider replays 1982 → today WR concentration."],related:[{id:`current_world_records_by_country`,titleZh:`当前 WR(国家)`,titleEn:`Current WRs (country)`,hintZh:`累计 vs 当前`,hintEn:`All-time vs current`},{id:`world_records_by_person`,titleZh:`个人 WR 总数`,titleEn:`WRs by person`,hintZh:`同口径,选手维度`,hintEn:`Same metric, per-person`},{id:`first_r_is_wr`,titleZh:`首条即 WR`,titleEn:`First record is WR`,hintZh:`稀有事件子集`,hintEn:`Rare-event subset`},{id:`world_records_by_country`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`国家榜 + 年份 slider`,hintEn:`Country leaderboard + year slider`}]},world_records_by_person:{id:`world_records_by_person`,titleZh:`个人世界纪录数量`,titleEn:`World records by person`,badgeZh:`选手`,badgeEn:`Person`,introZh:["历史累计 —— 每个选手生涯破过多少次 WR (single + average 各计 1 次)。和 by_country 同源,只换 `GROUP BY`。",`榜首通常是个位数双位数,因为 WR 是稀缺资源 (整个 WCA 每年也就几十个新 WR)。`],introEn:["All-time per-cuber WR break count (single + average each contribute 1). Same source as by_country, only `GROUP BY` swap.",`Top values usually in single / double digits — WRs are scarce (the entire WCA produces only a few dozen new WRs per year).`],stats:[{value:`single + average`,labelZh:`两列各算 1`,labelEn:`Each column = 1`,hintZh:`同行可计 2`,hintEn:`Same row can be 2`},{value:"`HAVING > 0`",labelZh:`过滤 0`,labelEn:`Drop zeros`,hintZh:`没破过 WR 的人不上榜`,hintEn:`Cubers with 0 WRs excluded`},{value:`sub_id = 1`,labelZh:`主身份`,labelEn:`Primary identity`,hintZh:`同其他 person 榜`,hintEn:`Same as other person leaderboards`},{value:`name 字母序 tiebreak`,labelZh:`同分排序`,labelEn:`Tied-name sort`,hintZh:`相同 WR 数按字母`,hintEn:`Same count → alphabetical`}],sourceZh:["`results` 算 `SUM(IF(regional_single_record = WR, 1, 0) + IF(regional_average_record = WR, 1, 0))`,`GROUP BY person_id` + `HAVING > 0`。join `persons` (`sub_id = 1`)。"],sourceEn:["`results` aggregates `SUM(IF(regional_single_record = WR, 1, 0) + IF(regional_average_record = WR, 1, 0))`, `GROUP BY person_id` + `HAVING > 0`. Joined to `persons` (`sub_id = 1`)."],sourceCode:{lang:`sql`,body:`SELECT person_id,
       SUM(IF(regional_single_record = 'WR', 1, 0)
         + IF(regional_average_record = 'WR', 1, 0)) wrs_count
FROM results
GROUP BY person_id
HAVING wrs_count > 0
ORDER BY wrs_count DESC, person.name`},steps:[{titleZh:"`SUM IF` 数 WR",titleEn:"`SUM IF` to count WRs",bodyZh:`单行 single + average 都标 WR 时贡献 2;只标一个贡献 1;都没标 0。`,bodyEn:`Same row with both single + average WR contributes 2; one tag = 1; neither = 0.`},{titleZh:"`GROUP BY person_id`",titleEn:`Aggregate per person`,bodyZh:`每人一行,累计所有项目所有时间。`,bodyEn:`One row per cuber, accumulating across all events and all time.`},{titleZh:"`HAVING wrs_count > 0`",titleEn:"`HAVING wrs_count > 0`",bodyZh:`没破过 WR 的人 (绝大多数 WCA 选手) 排掉;否则榜单几十万人。`,bodyEn:`Cubers who never broke a WR (the vast majority of WCA) excluded; otherwise the leaderboard would be hundreds of thousands long.`},{titleZh:`降序 + name 字母 tiebreak`,titleEn:`Sort desc + name tiebreak`,bodyZh:"同 WR 数 → 按姓名字母排;join `persons` 拿主身份名。",bodyEn:"Same WR count → alphabetical by name; `persons` join brings primary identity.",highlight:!0}],edgesZh:[`同人同场刷自己 = 累计;e.g. 同场预赛 + 半决 + 决赛 single 都破 WR = 3 个 WR。`,`WR 持有人改国籍不影响 WR 计数,只影响 by_country 归属。`,"与 `records_in_most_events` (项目广度) 不同 —— 这里数次数,那边数不同项目数。",'没单独"当前"版,要看当前 WR 数走 `current_world_records_by_country` (但那是国家维度,WCA 没出当前 WR by person 的官方榜)。'],edgesEn:[`Same cuber improving themselves all count: prelim + semi + final single WR at one comp = 3 WRs.`,`WR-holder changing nationality doesn't affect their personal WR count, only by_country attribution.`,"Different from `records_in_most_events` (event breadth) — here we count occurrences, not distinct events.",'No separate "current" version for persons; current-WR breakdown goes through `current_world_records_by_country` (country-level only).'],related:[{id:`world_records_by_country`,titleZh:`各国 WR 数`,titleEn:`WRs by country`,hintZh:`聚合到国家`,hintEn:`Aggregated to country`},{id:`records_in_most_events`,titleZh:`项目广度`,titleEn:`Records breadth`,hintZh:`次数 vs 项目数`,hintEn:`Count vs distinct events`},{id:`longest_streak_of_world_records`,titleZh:`连续 WR 链`,titleEn:`WR streak`,hintZh:`WR 是否连续`,hintEn:`Whether WRs are consecutive`},{id:`first_r_is_wr`,titleZh:`首条即 WR`,titleEn:`First record is WR`,hintZh:`WR 入门姿势`,hintEn:`How they entered the WR club`},{id:`world_records_by_person`,toStat:!0,titleZh:`打开实时榜单`,titleEn:`Jump to live data`,hintZh:`Top WR 大佬`,hintEn:`Top WR holders`}]}},p={...o,...s,...c,...l,...u,...d,...f};function m(e){return e in p}export{m as n,p as t};