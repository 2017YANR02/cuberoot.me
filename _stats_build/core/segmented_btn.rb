# NOTE: 通用分段控件按钮样式——所有选择器按钮共用的外观
# 消费方在按钮上加 class="segmented-btn metric-btn" 即可
# metric-btn / source-btn / stat-tab 等前缀类名仅做 JS 作用域隔离
module SegmentedBtn
  # NOTE: CSS 已迁至 assets/css/stats_ui.css，此方法保留签名但返回空
  def segmented_btn_styles
    ""
  end
end
