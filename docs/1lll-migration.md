# 1LLL 公式库迁移

把作者自编的 Google Sheet(3915 个 1LLL case、5080 条公式、外加镜像/逆/最优/对称性等元数据)
迁进站上的 `alg_sets` / `alg_cases`,替换现有的 speedcubedb 抓取数据。

Status: **先导任务已灌入生产(§6');Phase 0 状态轨道 join 已完成、零残留(§6'')。表侧数据尚未导入。**
Owner: 站长(Ruimin Yan)。文档随进度更新,§9' 是进度日志。

> **Case 数的 GROUND TRUTH 在 §5 —— 任何一期跑完都拿它对账,对不上就是那一期错了。**

---

## 1. 数据源

| | |
|---|---|
| 表格 | `https://docs.google.com/spreadsheets/d/1I2McWXVQZxGRVmrVEJDub9r_NPTNdPs6C7jriodyG1E` |
| 导出 | `https://docs.google.com/spreadsheets/d/<id>/export?format=xlsx`(公开可读,免鉴权) |
| 主表 | sheet `1LLL`,3915 行 × 80 列 |
| 自定义函数 | Apps Script named lambdas(`DELETE_AUF` / `HTM` / `QTM` / `GEN` / `MIRRORLR` / `ROTATEY` / `SIMPLIFY` …),源码不进 xlsx,但**调用式和计算结果都进** |

**xlsx 同时含公式层和值层**:`openpyxl(data_only=False)` 读公式,`data_only=True` 读 Google 烘进去的缓存结果。
自定义函数被导出成 `=IFERROR(__xludf.DUMMYFUNCTION("HTM(EXPANDALG(...))"), 16.0)` —— 调用文本和结果都在。

唯一丢值的是 `BK`–`BQ` 七个开发辅助列(`ROTATE(Z2,"y")` 之类),不参与迁移。

### 关键列

| 列 | 含义 |
|---|---|
| `Self` | case 编号 1–3916(缺 0005 = LL 连跳)。**主键** |
| `Name` | OLLCP 名,如 `AB3` = OLL `A` + 角换 `B` + 识别特征 `3`(十六进制 1–9,A,B,C) |
| `Subset` | `PLL` / `ZBLL-{U,T,L,H,Pi,S+,S-}` / `1LLL` / `ELL-{A,B,X}` |
| `OLL` / `CP` | 字母制 OLL 名 + 角换类型 |
| `Scramble (alg of inv case)` | 打乱 = **逆 case 的公式**,且已被 `DELETE_AUF` 剥掉起手 AUF。⚠ 见 §6 |
| `Self alg` | 公式,多条按 `\n` 分行 |
| `SH` / `SQ` | 本条公式的 STM(=HTM) / QTM 步数 |
| `Type` | 叠加公式类型(`OO`=OLL+OLL、`OP`=OLL+PLL、`CC`=conjugate commutator …)。**取决于首条公式**,仅部分 case 有 |
| `Mirror` / `Inv` / `IM` | 镜像 / 逆 / 镜像逆 case 的**编号**(+ 各自公式列) |
| `SH*`/`SQ*`/`H*`/`Q*` + 各自 scramble | 四套最优解及步数 |
| `COEP alg` / `COEP scramble` | 仅 474 行(ZBLL+PLL)有 |
| `C_n` / `Self-Mirror` / `Self-Inv` / `Any sym` / `Full sym` / `Anti-sym` / `No sym` | 对称性 |
| `Self gen` | 生成元集合,如 `FLlMU`(大小写不敏感排序;同字母的先后**按公式里谁先出现**,见 §4.5) |
| `doc No.` / `Old No.` | 旧编号(存 meta 备查) |

**不迁移**:`Yiheng Progress`(他人学习进度)、`Train`(本人学习进度,见 §8 后置)、`V3.PDF`/`V4.PDF`(旧版公式)。

---

## 2. 公式记号规范

站上 `/sim` **已经有这套记号**(`app/[lang]/sim/engine/hands/FINGERTRICKS.md`),表和站说的是同一种语言。

| 记号 | 含义 | 出现次数 |
|---|---|---|
| `↑` / `↓` / `·` | **换握记号**:上手(拇指起手 U 面)/ 下手(D 面)/ 回 home 握。占一个播放步驱动手部动画,**对魔方状态零作用** | 1192 / 558 / 395 |
| `[oh]` | 单手公式 | 333 |
| `[ft]` | 脚拧公式 | 15 |
| `[key]` | 虚拟键盘公式(模拟魔方用) | 10 |
| `[big]` | 高阶魔方公式 | 6 |
| `[fmc]` | 最少步公式 | 19 |
| `=` 行首 | 上一条公式的**等价写法** | 36 |
| `(...)` | 分组,无语义 | — |

手别由**空白**定(FINGERTRICKS §2):记号紧贴后续字符(`↑U`)= 右手;后随空白(`↑ U`)= 左手;`↑↑ U` = 双手。
→ **转写工具不得增删记号周边空格。**

**无空格连写**:表里写 `UD'`、`M'R'`、`R'L'`、`R2'`。cubing.js 解析不了,必须先 tokenize:

```js
const TOKEN = /([RLUDFBrludfbMSExyz]w?)(\d*)('?)/g;
```

`EXPANDALG` 只处理 `(...)2` / `(...)3` 重复。**没有**交换子 / 共轭的方括号写法(`[R U R', F]`)。

---

## 3. Case 身份:Z4 × Z4 轨道 ★

**这是整个迁移的理论地基,搞错了就全盘贴错位。**

LL 状态数 = `4!·3³·4!·2³ / 2`(奇偶约束) = **62208**。
ZBLL(棱已定向)= `62208 / 8` = 7776 个状态。

而 ZBLL 公认是 **493** 个 case。验算:`7776 / 16 = 486`,加上对称不动点(Burnside)→ **正好 493**。

⟹ **case = 状态在 (前 AUF × 后 AUF) = Z4 × Z4 下的轨道。**

同理 1LLL:`62208 / 16 = 3888` + 对称不动点 → **3916**(含连跳)。✓ 与表一致。

**推论**:
- 公式 `A` 和 `U^a · A · U^b` 解**同一个 case**。
- 一条公式的 `y2` 共轭 = `(前 U2, 后 U2)`,**仍是同一个 case**(已实测:表里 `PLL-U+` 的公式正是 `PLL-U-` 的 y2 共轭)。
- 站上的 `alg_cases.setup` 是该轨道的某个**代表元**;每条存库公式都是"无末尾 AUF"的规范形式
  (`lib/alg_validation.ts:88-93` 明令禁止末尾 U-family move)。

---

## 4. 已实证的变换规则

⚠ 下面每条都是**跑 cubing.js 实测**得出的,不是推导。别改成"看起来更对"的版本。

### 4.1 镜像(MIRROR):交换轴上两面,取反 amount —— **但沿轴的 slice 和转体豁免** ★

```
LR(M 平面)镜像:R↔L, r↔l, Rw↔Lw
                 每个 move 的 amount 取反,**唯独 `M` 和 `x` 原样不动**
S 平面:F↔B …,豁免 `S` / `z`      E 平面:U↔D …,豁免 `E` / `y`
```

**这不是约定,是被逼出来的**(cubing.js 的 move 代数,已实测,不是假设):

```
l = L M   且   r = R M'
镜像把 L 送到 R',把 l 送到 r' = (R M')' = R' M
⟹  R' · mirror(M) = R' M  ⟹  **mirror(M) = M**
同理 x = R M' L'  ⟹  mirror(x) = L' M' R = x
```

直觉:`M` 的方向跟着 `L` 定,`x` 的方向跟着 `R` 定 —— 而 M 平面镜像交换的正是 L 和 R。
参照方向翻了一次,move 自己又翻一次,**两次抵消**。S/z 和 E/y 在各自的轴上同理。

> **🐛 「全部取反」是错的,而且线上真的在错。** 2026-07-13 实测:按「全部取反」镜像,
> 9 条分解恒等式里**破 3 条**;站上 9 个 LL 集合的 7771 条公式里,M 镜像**毁掉 590 条**、
> S 镜像毁掉 340 条 —— 凡是带 `M` 或 `x` 的(M 层 PLL、所有侧握的 A/E-perm)全是垃圾。
> `/sim` 的两个镜像按钮一直在吐这些垃圾。**已修**(`lib/cube3.ts` + `tests/cube3_mirror.test.ts`)。
>
> 旧测试抓不到:它只问「镜像后是不是仍只动一层」—— 一条**错但合法**的公式照样能过。
> 而且它的 M 层样本是 H-perm / Z-perm,`M2` 取反等于自身,压根没被考验到。
> 更糟的是它把 bug 写成了断言(`expect(mirrorAlg('M','M')).toBe("M'")`)。
> **新测试改成钉死规则的那条性质:镜像复合 move == 镜像它的分解式**(9 条恒等式 × 3 个轴),
> 外加一条来自文件之外的判据:**Ua-perm 镜像必须得到 Ub-perm**。

> ⚠ `new Move('R', 0).toString() === 'R'` —— amount 是 4 的倍数时是恒等 move,但打印出来是**真转动**。
> 表里有 `L4'` / `R4`,必须先滤掉 `amount % 4 === 0` 的,否则会凭空造出一个 R。

> **`mirrorAlg` 错过两轮,别再来第三轮。** 反射是反定向的,所以「每个 move 都要翻」这个直觉
> **对了一半**:它治好了第一轮的病(旧实现只给 `{R,L,r,l,Rw,Lw,M,x}` 取反,`U/D/F/B/E/S/y/z`
> 全漏了,T-perm 镜像出来 D 层和中层都被破坏),却**矫枉过正**成了第二轮的病 —— 把 `M` 和 `x`
> 也翻了。真相是:反定向没错,但 `M`/`x` 的**参照方向**也跟着翻,两次抵消。上面那条推导才是终局。
>
> 独立复核(2026-07-13):从 kpuzzle 自己推出 corner/edge 的镜像 slot 映射,唯一 ground truth
> 是六个**面转**的镜像(不预设任何 slice 规则),该 oracle 独立判定 `mirror(M) = M`、`mirror(x) = x`。
> 2000 条随机公式 × 三个轴:新版 **2000/2000**,旧版 **1116/2000**。

> **🐛 `mirrorAlg` 还有两个两版都没修的 bug(2026-07-13 一并修掉)**:
> - `new Move(f, amount)` **丢层号**:`2R` → `L'`、`3Rw` → `Lw'`、`2-3r` → `l'`。改用 `.modified()`。
>   /sim 的镜像按钮只 gate 了 `!isSq1 && !isTwistyMode`,**4x4 / 5x5 上真的能点到**。
> - `if (amount % 4 === 0) continue` **把 `R4` 直接删掉**。`R4` 群元素是恒等,但它是**真实的物理动作**
>   (R 层转满一圈),表里是**故意写的**。实测 `new Move('L', -4).toString() === "L4'"` —— cubing.js
>   自己序列化就是对的,那条丢弃纯属自伤。只有 `amount === 0` 才需要拦(它会打印成真转动 `"R"`)。

> ### ★ 「表侧 MIRRORLR 有系统性 bug」—— **这个怀疑被证伪了**(2026-07-13)
>
> 我在 Phase 0 写过「表里的 `MIRRORLR` 很可能也漏翻了 U/F/B/D」。**错了。** 全表普查:
>
> | | |
> |---|---|
> | 形状匹配的镜像候选对 | **2214** |
> | 逐字正确的镜像 | **2170** |
> | 只差一个**起手 AUF**(在 16 折轨道内 ⟹ 同 case,**合法**) | **42** |
> | 坏镜像 | **2**(= 同一个事件的对合双向) |
>
> 唯一那个事件就是 `1/PLL-U+ ⇄ 2/PLL-U-`。硬证据:坏镜像会把 **94%** 的公式变成不保 F2L 的
> 垃圾(400 条采样只活下来 25 条)—— 真要系统性用了它,Phase 0 会捞出**上千条**非法公式;
> 实际只捞出 1 条(3048/I3R5),而那条跟镜像无关。
>
> **真相反而更有用:站长的镜像生成器是对的,错的是喂给它的源行。** 19 条坏公式里 **9 条**
> 能在全表里反查到「它是某行公式的**正确**镜像」—— 源行本身就错位,镜像忠实地把错误复制到了对侧。
>
> ⟹ **镜像自洽 ≠ 正确,这是个校验盲区。** `3419/O-U8` 和 `3347/O+U9` 互为逐字正确镜像,
> 却双双错位(公式其实分别属于 `3280/M-U1` 和 `3208/M+U1`),因此**通过**了镜像全量校验。
> 别指望用镜像关系兜住整对错位。

### 4.2 整体旋转(ROTATE):family 重映射表

`y` 一步(转写自表里的 `ROTATEY`,已实测正确):

```
R→F  U→U  F→L  D→D  B→R  L→B
r→f  u→u  f→l  d→d  b→r  l→b        (Rw/Uw/… 同理)
x→z  y→y  z→x'  E→E  M→S'  S→M      (' = amount 取反)
```

### 4.3 消去起手转体:`y^k` 起手 ⟹ **body 按 `y^-k` 重映射**

| 起手 | body 重映射 |
|---|---|
| `y` (y¹) | y³ |
| `y2` (y²) | y² |
| `y'` (y³) | y¹ |

实测:站上 **3348 条**起手带 y 且精确解开 setup 的公式,重映射后 **3348/3348 全部仍然成立**。

> ⚠ **裸的 `y`→`U` 字面替换是错的**:5099 条样本里成立 0 次。删掉 y 也是 0 次。
> `y` 除了转顶层,还把 D 层 / 中层 / 中心块一起转了。
>
> 但这不代表做不到 —— 见 §4.3'。body 保持原样是**可以**的,只要补上末尾 AUF。

### 4.3' 消去起手 `y`,**body 一个字节不改** ★

这是先导任务的理论地基。它不是经验规律,是一条恒等式。

**引理.** `y = U · Dw'`(`Dw` = 底下两层)。两个因子作用在不相交的层上,所以可交换,且 `y^k = U^k · Dw^-k`。
实测:`y == U Dw'`、`y2 == U2 Dw2`、`y' == U' Dw`,三条全 true。

**定理.** 若公式体 `A` 的**净群元素与 `Dw` 可交换**,则对任意状态 `S`

```
S · U^k · A · U^-k   ==   ( S · y^k · A ) · y^-k
```

即**改写后的公式与原公式到达完全相同的状态,只差一个整体旋转 `y^-k`**。

*证明.* `S·y^k = S·U^k·Dw^-k`,故 `S·U^k = S·y^k·Dw^k`。于是
`S·U^k·A·U^-k = S·y^k·(Dw^k·A)·U^-k = S·y^k·A·(Dw^k·U^-k) = (S·y^k·A)·y^-k`,
中间一步用了 `A` 与 `Dw^k` 可交换,末一步用了 `y^-k = U^-k·Dw^k`。∎

**这条定理不依赖任何「本集合算不算还原」的判据** —— 判据只要是旋转不变的(站上一律容忍 24 个整体旋转),
改写就对 OLL / PLL / COLL / CMLL / Anti PLL / OLLCP / ZBLL / 1LLL **一视同仁地成立**。
所以先导任务**不需要**给每个集合写"还原判据",只要一个纯群论测试。

#### 判据(唯一的合法性条件)

```
可改写  ⟺  A 与 Dw 可交换
        ⟺  A = (只动顶层块的置换) ∘ (y 轴旋转)
        ⟺  body 没有净 x/z 旋转
```

正规的 LL 公式天然满足(保 F2L 就等于"支撑集 ⊆ 顶层",与 `Dw` 支撑集不交 ⟹ 可交换),
**唯一的例外是 body 带净 x/z 转体的那批**。注意两个容易踩的坑:

- **净 `y` 无所谓**。`R U R' U' R' F R F' y` 动了中心块,但 `y` 与 `Dw` 同轴、照样交换 ⟹ 可改写。
  所以「body 只动顶层块」是**充分不必要**条件,别拿它当判据。
- 判据必须直接测 `Dw^k·A == A·Dw^k`。在 12 条 body × 3 个 k 上,它对 oracle 的预测是 **36/36**。

实测 9 个 LL 集合共 **5099** 条起手带 y 的公式(oll 119 / pll 29 / coll 86 / cmll 1 / anti-pll 29 /
ollcp 747 / zbll 1120 / 1lll 2915 / ell 53):

| | |
|---|---|
| **可改写** | **5081** |
| 必须跳过(body 含净 x/z) | **18** |

改写式一律 `y^k A → U^k A U^-k`,body 一个字节不改。跳过的 18 条例:
`pll Aa: y' x L2 D2 L' U' L D2 L' U L'`、`zbll ZBLL L 6: y2 z U' R2 U' L' U R2 U' L U2`、
`ollcp OLLCP10 2: y' z U R U' R U R' U F' U' F R' U`。它们的 `x`/`z` 是必需的握持方式,保留原样。

判据经独立复核:在 5567 条可解析公式上与实测 oracle **一致 5567/5567,零分歧**。

#### 收尾 AUF:**存进库,前端显示时剥掉** ★(站长 2026-07-13 拍板)

| 层面 | 内容 |
|---|---|
| DB / API | `U R U R' U' R' F R F' U'` —— **完整公式**,`setup + alg` 精确还原 |
| 前端显示 / 复制 | `U R U R' U' R' F R F'` —— `displayAlg()` 剥掉末尾那个 U |
| 播放器 / 缩略图 / recon 查表 | **完整公式**,动画停在还原态 |
| 校验器 | 判据 = 「setup + alg 精确还原」,不放宽 |

**为什么不存"剥好的规范形、播放时现算 AUF"**(这是我最初的方案,被审计推翻):

- **推不出来。** OLL / COLL / CMLL / OLLCP **没有**"整体还原"这个判据可用 —— 实测 **1173 条**公式
  对本阶段正确却不还原整个魔方(它们的 setup 只是**首条**公式的逆,替代公式落在别的顶层排列上)。
  播放器无法统一推导收尾 AUF,得给每个集合单写目标判据。
- **失败模式更糟。** 存完整公式时,某处漏接 `displayAlg` 只是多显示一个 U(难看);
  存剥好的形式时,某个播放器漏补 AUF 就是**魔方没还原**(算错)。
- 站长原话是「**在前端显示** 最后的 U 不要有」—— 本来就是显示层规则。

⟹ 不变式:**`setup + alg` == 目标态**。任何消费方直接用库里那串都天然正确。
`displayAlg()`(`lib/alg_display.ts`)是纯字符串操作,不过 cubing.js —— 括号 / `=` 标记 /
`·↑↓` 指法记号全部原样保留。接了三处:`AlgCategoryView`(公式表 + 复制)、`AlgsPanel`(/sim 列表)、
`PllPerformerOverlay`。**管理端(AlgEditor / ValidationReportModal)故意不接** —— 那里要看库里的真值。

#### ⚠ `sticker.kind` **不是**"能不能改写"的判据

实测 API:12 个 3x3 集合是 `kind: "face"`,但**只有 `pll` / `zbll` / `1lll` / `ell` / `anti-pll`
真的以整体还原为目标**。

- `oll` / `coll` / `cmll` / `ollcp` 是 `face`,但只完成本阶段(1173 条不还原整方)。
- **`vls` / `wv` / `sv` 也是 `face`,可它们是最后一槽(F2L)集合** —— 公式净元素动 D 层和 E 层。
  按 `kind==='face'` 筛选做改写会**毁掉 77 条 VLS/WV/SV 公式**。
- 非 LL 集合(f2l / adv-f2l / zbls / cls / vls / wv / sv / fruf / eo4a)共 474 条起手 y 公式,
  **改写成功率 0/474 —— 全毁**。它们绝不能进改写脚本。

改写脚本的过滤条件 = **显式的 9 个 LL 集合名单 ∩ §4.3' 的群论判据**,两道都要。

#### 校验器的两条规则(`lib/alg_validation.ts`)

1. `face`:`setup + alg` 必须精确还原(容忍 24 个整体旋转)。
   **不再单独拦末尾 U** —— U 转不是整体旋转,`setup+A` 与 `setup+A+U` 不可能同时还原,
   所以过了还原判据的末尾 U **必然是载荷性的收尾 AUF**。旧规则对 face 是死代码,而且它和
   "必须还原"**互斥**:需要收尾 AUF 的公式两种写法都存不进来(旧 bug,已修)。
2. `f2l`:判据不看顶层,U 转对它毫无影响 ⟹ 末尾的 U **永远**是多余的,继续拦。

#### 先导任务的实际盘子

| 集合 | cases | 公式 | 起手 y | 末尾多余 U |
|---|---|---|---|---|
| oll | 57 | 228 | 119 | 0 |
| pll | 21 | 84 | 29 | 0 |
| coll | 40 | 160 | 86 | 0 |
| cmll | 42 | 168 | 1 | 0 |
| anti-pll | 22 | 83 | 29 | 4 |
| ollcp | 342 | 1115 | 747 | 4 |
| zbll | 472 | 1789 | 1120 | 54 |
| 1lll | 3400 | 4084 | 2915 | 34 |
| ell | 25 | 99 | 53 | 0 |
| **合计** | **4421** | **7810** | **5099** | **96** |

**17 个例外**:body 里含 `x`/`z` 转体(`y' x L2 D2 …` 这类 A-perm / E-perm,必须侧握执行)。
body 里有转体 ⟹ "顶层"不再是 U 面,U 转补不回来。**这些保留原样**,它们的 `x` 是必需的握持方式。

```
pll   Aa         y' x L2 D2 L' U' L D2 L' U L'
pll   Ab         y' x L U' L D2 L' U L D2 L2
pll   E          y x' L' U L D' L' U' L D L' U' L D' L' U L D
zbll  ZBLL U 41  y2 x L2 D2 L' U2 L D2 L' U2 L'
zbll  ZBLL L 4   y2 R U R' U' z' y' R U R' F' R U R' U' R' F R2 U' R' U' F
zbll  ZBLL L 6   y2 z U' R2 U' L' U R2 U' L U2
…(共 17 条,完整清单见先导任务的产出报告)
```

### 4.4 剥离起手 AUF(表里的 `DELETE_AUF`)

```
=TRIM(REGEXREPLACE(alg, "^U2'?|^U'|^U", ""))
```

### 4.5 计步(表里的 `SH` / `SQ`)—— **照写照执行,不折 mod 4** ★

表把它们叫 HTM / QTM,但按定义那是 **STM / SQTM**:表里 slice 记 **1** 步,不是 2 步。

```
SH  = STM   转体 0;其余**每个写出来的 token** 记 1     →  R4 = 1、R3 = 1、R2 = 1、M = 1
SQ  = SQTM  转体 0;其余记 **|写出来的 amount|**        →  R4 = 4、R3 = 3、R2 = 2、R' = 1
GEN         非转体 family 字母,大小写不敏感排序
DELETE_AUF  剥掉起手 U-family 转
```

**★ `R4` / `L4'` 是站长故意写的,不是脏数据(站长 2026-07-13 明确:「R4 这些你要保留的」)。**
它的群元素是恒等,但它是一个**真实的物理动作**(R 层转满一圈)—— 对指法 / 手部动画 / TPS 全都算数。
所以度量是「照写照执行」,**不能**按 mod 4 折成恒等,更不能把这个 move 删掉。
(我一度打算把它当脏数据报出去,并在镜像里 `amount % 4 === 0` 就丢弃 —— 那是在**乱改公式**,已撤。)

**GEN 的排序 tie-break 是「首次出现顺序」,不是大小写规则**:
`PLL-F` 的 `R' F R f' …` → `FfRSU`(F 先出现),`PLL-Y` 的 `f' U f R' …` → `fFRU`(f 先出现)。
同一对字母,顺序相反 —— 靠稳定排序天然拿到。(旧文档写的 `FlLMU` 是记错,表里实际是 `FLlMU`。)

计步管线**顺序有讲究**:首行 → 剥署名 → `DELETE_AUF` → `EXPANDALG` → tokenize → 计步。
`DELETE_AUF` 必须在 `EXPANDALG` **之前**,否则 `(U R …)2` 里的那个 U 会被当成起手 AUF 剥掉。

#### 全量验收(`core/packages/alg-build/verify_sheet_metrics.mjs`)

| | 结果 |
|---|---|
| 首条公式可解析 | **3915 / 3915** |
| `SH` (STM) | **3914 / 3915** |
| `SQ` (SQTM) | **3914 / 3915** |
| `Self gen` | **3915 / 3915** ✓ |

唯一那行分歧(`71/TLA`)是**表格 typo**:单元格开头多了一个空格 —— `DELETE_AUF` 是
`TRIM(REGEXREPLACE(…))`,正则跑在 `TRIM` **之前**,`^U'` 被那个空格挡住没匹配上,起手 AUF 没剥掉,
所以表里是 16 / 20 而不是 15 / 19。**算出来的值是对的,表的值是错的。**

`rotation?` 不是「含转体」而是「**净**转体 ≠ 恒等」(全表只有 8 行是 1)—— 需要 cubing.js,不是纯记号度量;
实测吻合 3908/3915(7 个 miss 全是已知坏公式)。`has move?` 试了六个假说全不中,它挨着
`Train` / `Yiheng Progress` 那堆,判定为**表自己的工作流辅助列**,不迁移。

#### 站上原来有 **9 个**各自为政的计步器,其中一个是错的

| 位置 | 实际语义 | 病 |
|---|---|---|
| `lib/recon-stats.ts` `htm` | 字符法 = STM | **`Rw` 数成 2 步** —— 剥离集里没有 `w` |
| `lib/cube3.ts` `countMoves` | cubing.js leaf 数 = ETM | 名字骗人 |
| `lib/recon-alg-utils.ts` `countMovesExpanded` | 空格分词 = ETM | 连写 `M'R'` 数成 1 步 |
| `math/god` `MetricExplainer` | 写死示例上的内联 | `/2$/` 抓不到 `R2'`;STM 直接写死 = HTM |
| `timer/_lib/storage/export_csv.ts` `qtmOf` | 内联 | M/E/S **记 0 步** |
| `math/demigod` `LiveSampler` `countHTM` | 空格分词 | — |
| `shared/scramble_length.ts` `countQtm` | WCA 打乱专用 | 域内正确,**保留** |
| `lib/sq1-metrics.ts` | SQ1 七度量 | 域内正确,**保留** |
| `lib/cube222-metric.ts` / `pyram-metric.ts` | 求解搜索深度 | 不是记号计步,**保留** |

**根因一句话:字符法只在「宽块写小写 `r`」时正确** —— 表里正是这么写的,recon 里写 `Rw`。
前 6 个已收进 `@cuberoot/shared/alg-notation`(零依赖 tokenizer);后 3 个是各自领域的正确度量,不该硬并。

---

## 5. 目标结构

### ★ Case 数 —— **GROUND TRUTH**(站长给定,任何一步都不许偏离)

```
1LLL                              3915
PLL                                 21
ZBLL (excluding 21 PLL)            472
ELL  (excluding 4 EPLL)             25
1LLL (excluding ZBLL, EPLL)       3397
```

**21 + 472 + 25 + 3397 = 3915**,四块互不重叠、恰好铺满。这五个数是全局不变量:
每一期跑完都要拿它对账,对不上就是**这一期错了**,不许改数迁就实现。

表侧自证(`Subset` 列直方图,实测):`1LLL` 3397 / `ZBLL-*` 472(6×72 + H 40)/ `PLL` 21 /
`ELL-A` 12 + `ELL-B` 8 + `ELL-X` 5 = 25。逐格吻合。

### 站上现状 vs 迁移目标

**★ `ell` set 站上早就有(25 个,speedcubedb 那套),不是新建的。**(2026-07-13 订正 —— 我先前写的
「新建 set」是错的,Phase 0 的 join 一开始也漏掉了它。)

站上 4 个 set 共 **3918** 个 case,但只有 **3896 个不同的态**:

| 重复 | 个数 |
|---|---|
| `1lll` 内部:`1LLL 4 63 == 4 64`、`1LLL 5 21 == 5 22` | 2 |
| `ell` 的 20 个(棱块两翻的 `ELL-A` 12 + `ELL-B` 8)与 `1lll` 里的**同态重复** | 20 |

⟹ 表侧真正缺的 = 3915 − 3896 = **19**,**全部 Subset=1LLL**。

| set | 现在 | 目标(GT) | 动作 |
|---|---|---|---|
| `pll` | 21 | **21** | 只换公式 |
| `zbll` | 472 | **472** | 只换公式 |
| `ell` | **25** | **25** | **case 一个不动**,只换公式 + 补 meta |
| `1lll` | **3400** | **3397** | −20(ELL-A/B 从 1lll 删掉,它们在 `ell` 里已有)+ 17(OLL 20 组里非 ELL 的新 case);另有 2 个重复 case 改造成 `DS-` / `O+` |
| 合计 | 3918 | **3915** | |

```
3400 − 20 + 17 = 3397 ✓        21 + 472 + 25 + 3397 = 3915 ✓
```

**这四块恰好互不重叠、铺满 3915,别搞混**:

- `ELL ⊂ ZBLL`?**不。** ELL = 角块全还原、棱块任意(**含棱块翻色**);ZBLL = 棱块已定向。
  两者的交集正是 `EPLL`(Ua/Ub/Z/H 4 个),而 EPLL 已经被 `pll` 那 21 个收走。
  所以 `ell` 里剩下的 25 个**棱块全是翻色的**,不在 ZBLL 内。
- **`ell` 的 25 个横跨多个 OLL 组**:`ELL-A`/`ELL-B`(棱块两翻)落在 28+ 组,`ELL-X`(四翻)落在 **OLL 20**。
  所以「组」是**状态的属性,不是 set 的属性** —— join 必须按每个 case 自己的**朝向类**分桶,
  按 set 名分桶会造出一个虚构的「ELL 组」,然后 22 个 X 组的行去抢 25 个 ell 槽位,17 个匹配不上。
- 站上 `1lll` 的 subgroup 是 `01`–`19` + `28`–`57`,**整组缺 20**(21–27 在 `zbll` set 里,OLL 跳过在 `pll` 里)。
  OLL 20 组一共 22 个 case,其中 5 个(`ELL-X`)站上在 `ell` 里,另 17 个站上**完全没有**。

### 命名

- `alg_cases.name` **保持现状不动**(`1LLL 1 1` / `ZBLL U 1` / `Aa`)。
  它是 `alg_submissions.case_name` 的 join key,**改名会孤儿化用户投稿**。
- OLLCP 名(`AB3`)+ 数字号(`0703`)存进 `meta`,UI 用 `displayAlgCaseName()` 改成
  **OLLCP 主标题 + 现有名副标题**。字母制优先,数字制保留。
- 分组:字母制为主,数字制保留(切换器)。

### Schema 改动

```sql
ALTER TABLE alg_cases ADD COLUMN meta JSONB;   -- 一条 migration
```

`meta` 装:OLLCP 名 / 数字号 / 6 套打乱 / STM·QTM / 四套最优 / 镜像·逆·镜像逆 case 号 /
叠加类型 / 对称性 / 生成元 / doc No. / Old No.

`algs`(已是 JSONB,`AlgEntry[][]`)加字段 **无需 migration**,只改 shared 类型 + UI:

```ts
type AlgEntry = {
  alg: string; algHtml?: string; altId?: string; ytId?: string;
  tags?: ('oh'|'ft'|'fmc'|'big'|'key')[];      // 新增
  source?: 'cuberoot' | 'speedcubedb';          // 新增
}
```

### 公式合并顺序

**站长的公式排最前,speedcubedb 的在后**。去重时命中就删 speedcubedb 那条。
规范化键:剥括号 + 剥换握记号 `↑↓·` + 剥标签 + 剥 `=` + 归一化写法(`R2'`→`R2`、`Rw`↔`r`)+ 压空格。

⚠ **禁止用"魔方状态等价"去重** —— H-perm 的 2-gen 版和 M 层版状态完全等价,但是两条值得各自保留的公式。

---

## 6. ⚠ 已知数据问题(表侧)

对 5080 条公式跑了「打乱 + 公式 = 还原」的全量校验(允许前 AUF、后 AUF、末尾整体转体):

| | 首条公式 (3915) | 备选公式 (1165) |
|---|---|---|
| 只需前 AUF | 2857 | 349 |
| 还需后 AUF | 969 | 715 |
| **任何 AUF 组合都解不开** | **89** | **101** |

- **"还需后 AUF"不是错误** —— 按 §3,它们只是写在不同 U 朝向下,仍是同一 case 的合法解。
- **190 条"解不开"要逐条查。** 已定位一类:**自逆 case**(`Inv` = `Self`)的打乱被 `DELETE_AUF`
  推导坏了。例:`LUB`(#177)的打乱 = 它自己的公式;`PLL-U+` 的公式其实是 `PLL-U-` 的 y2 共轭
  (两者解同一个 case)。

> **结论:不要拿表里的 `Scramble` 列当 case 身份的依据。**
> case 身份 ← 站上已有的 `setup`(按状态轨道 join);缺失的 22 个 ← `setup = inverse(已验证的首条公式)`。

---

## 6'. 先导任务 —— **已完成并灌入生产(2026-07-13)**

站长要求:**顶层公式集不得以整体转体 `y` 起手,且 body 不得有任何改动。**

### 规范形

```
U^lead + body + U^trail
  lead  = 起手 y 的 k(与 body 自己的起手 U 合并)
  trail = 优先取"能让 setup + alg 精确还原"的那个;取不到就退回"状态保持"的那个
  body  = 中段,一个字节不改
```

`trail` 这条两段式规则不需要"本集合算不算还原"的知识,一条通吃九个集合:
真·整还原的 5 个集合总能取到"精确还原"的 trail(顺手补上缺的收尾 AUF);
`oll`/`coll`/`cmll`/`ollcp` 的替代公式压根不整还原,自动退回"状态保持",原样等价。

脚本:`core/packages/alg-build/canonicalize_ll_algs.mjs`(`--sql` 出 SQL)
本地验证:`core/packages/alg-build/dryrun_ll_algs.mjs`(灌 pg13 → 套 SQL → 读回来逐条验)

### 变更量(9 个 LL 集合,7808 条公式;数字为**修完 4 条坏数据之后**重跑的)

| | 数量 |
|---|---|
| 起手 y → U(body 不变) | **5083** |
| 补齐收尾 AUF(本来就缺,与 y 无关) | **550** |
| 跳过(净旋转带 x/z,「顶层」已不是 U 面,U 转补不回来) | **15** |
| 无需改 | 2160 |
| 受影响的 case(SQL UPDATE 条数) | **3518** |

那 15 条跳过的**全部合法**,不是坏数据:body 的净旋转带 x 或 z 分量(例 `pll Aa` 的
`y' x L2 D2 …`),执行完「顶层」已不在 U 面上,U 转补不回那个 y —— 原样保留是对的。

### 本地 pg13 dry run(灌 pg13 → 套 SQL → 读回来逐条验)

| 判据 | 结果 |
|---|---|
| A. 魔友看到的一个字没变 | **5633 / 5633** |
| B. 精确还原 或 状态与原公式一致 | **5633 / 5633** |
| C. 5 个整还原集合全部精确还原 | **6123 / 6123** |
| D. 起手还留着 y 的 | **0** |

灌完生产后**直接对线上 API 复验**:7808 条公式,整还原集合 0 条不还原,起手 y 只剩上面那
15 条合法的。Playwright 开 `/zh/alg/3x3/pll`:83 条公式,**末尾 U 零条**,起手 y 只剩 3 条
(正是 PLL 那 3 条 x-握姿的)。

### ★ 顺带揪出的既有数据 bug(改写没造成 —— 是新不变式把它们照出来了)

四条**全部已修并灌入生产**(`core/packages/alg-build/fix_bad_ll_cases.mjs`,`--dryrun` 走 pg13)。
坏 case 的 `setup` / `sticker` / `alg` 是**从同一份坏公式互推出来的**(setup = inverse(坏 alg)),
三者自洽地一起错 —— **库内没有独立判据**,所以每条都得找**库外**的判据,不许同源自证:

| 位置 | 毛病 | 判据 | 修法 |
|---|---|---|---|
| `pll` **Ub** alg[2] (id 4322)<br>`R2' U R U R' U' R3 U' R' U R'` | 是 alg[3] `y2 R2 U R U R' U' R' U' R' U R'` **掉了起手 y2** 的重复条目(`R2'`=`R2`、`R3`=`R'`,逐步相同)。转体换的是公式作用的**物理块**,不是收尾角度 —— 任何 AUF 都救不回来 | 两条公式的置换**逐一相同** | **删掉** |
| `ollcp` **OLLCP10 2** alg[3]<br>`y' z U R U' R U R' U F' U' F R' U` | 末尾 `U` 应是 `U'` | 全部单 token 变体里**唯一**一个既保 F2L 又与 alg[0] 落同一轨道的 | 末尾 `U` → `U'` |
| `1lll` **1LLL 2 28** (id 201) | 末尾 `r` 应是 `r'` | 四条**独立**判据同时成立(见下) | 改 `r`→`r'`,`setup` 按修好的公式重建 |
| `1lll` **1LLL 5 42** (id 431) | 末尾多出一个 `B'`;alg[1] 起手 `r' R` 展开就是 `M`(`r'R = R'MR = M`),把 F2L 直接切开,无解 | 同上(但 setup 坏得更深,单 token 修不回来 —— 靠其余三条定案) | 删掉 `B'`,`setup` 重建,alg[1] **删掉** |

两条 1LLL 用的四条库外判据(必须同时成立,任一不过就报出来交给站长、绝不猜):

1. 修好的公式**保 F2L**,且是**唯一**这样的单 token 修复;
2. 它解的 case 态在枚举里是**空位**(不撞 3396 个健康态里的任何一个);
3. 独立修 `setup` 得到**同一个**态,且该态的 OLL(朝向,**对 AUF 归一**)与**同组兄弟**一致;
4. 落在该组**还空着**的 AUF 类上 —— 一个 OLL 下有 4!·4!/2 = 288 个置换,对 AUF 归一 = **72 类**,
   正好等于组大小,所以「组内枚举完整性」是个**封闭**判据。

> 坑:朝向数组是**按槽位**存的,U 转会把它整体轮换。不对 AUF 归一的话,同一个 OLL 会显出
> 4 种「朝向」,组判据直接失效(我第一版就栽在这)。

### ★ 另外两处既有问题(**没修** —— 补一个缺失 case = 凭空编公式,那是表格迁移的活)

`1lll` 里有两对 case 是**同一个 LL 态**:`1LLL 4 63 == 1LLL 4 64`、`1LLL 5 21 == 1LLL 5 22`。
它们各占掉一个位置,于是第 4 组和第 5 组各**少了一个真 case**(健康 case 只覆盖 71 / 70 个 AUF 类)。
Phase 0 的态-轨道 join 会把它们对上。

顺带摸清了站内 `1lll` 的结构,Phase 0 直接能用:**49 个组**(OLL 1–19 + 28–57),缺的
**OLL 20–27 正是棱块已朝向的那批 = ZBLL / PLL 的地盘** —— 与站长那张表的
「1LLL(不含 ZBLL, EPLL)」完全对得上。整组 72 个(对称 OLL 的 4 组是 40 个),共 3400 个 case。

(集合外另有 `sv` **SV 7** 一条 spam 公式 —— 8 遍小鱼手 + 一个 U,不属先导任务范围。)

### 已落地的代码改动

1. `lib/alg_validation.ts` —— 判据 = **精确还原**;`face` 集合**不再单独拦末尾 U**(见 §4.3',那条规则
   对 face 是可证明的死代码,而且和"必须还原"互斥,导致需要收尾 AUF 的公式两种写法都存不进来);
   `f2l` 集合继续拦(它的判据不看顶层,末尾 U 永远多余)。
2. `lib/alg_display.ts` —— 新增 `displayAlg()`,显示/复制时剥掉收尾 AUF。接了
   `AlgCategoryView`(公式表 + 剪贴板)、`AlgsPanel`(/sim 列表)、`PllPerformerOverlay`。
   **管理端(AlgEditor / ValidationReportModal)故意不接** —— 那里要看库里的真值。
3. `lib/cube3.ts` 的 `mirrorAlg` —— 修好(见 §4.1),12 个测试。
4. 两处 `<VisualCube>` 缩略图补传 `setup`(`AlgCategoryView:174`、`_trainer/trainer-components:252` ×2)。
   **810 个 case 的 `setup ≠ inverse(首条公式)`** —— 例:OLL 2 的首条公式带 `y'`,于是卡片把标准
   OLL 2 图案整体转了 90°。这些卡片本来就画错了;传 `setup` 既修好它,又让它对改写免疫。
5. `tests/alg_leading_y_rewrite.test.ts` —— 15 个测试,锁死 §4.3' 的定理本身 + 规范形 + `displayAlg`。

5. `lib/pll-fingertricks.test.ts` —— `PLL_MAIN_ALGS` 快照刷新(21 条里 **8** 条变了:
   E / F / Gc / Ja / Ra / Ua / Ub / Z)。`seen.has('y')` 那条断言**反过来了** —— 现在没有一条
   PLL 主公式带 `y` 了,改成断言「不再需要 y 指法,但 `FINGERTRICKS.y` 仍在表里」。
   顺带一个实打实的好处:`Ua`/`Ub`/`Z` 本来就缺收尾 AUF,桌宠演示跑完停在**没还原**的魔方上;
   现在补齐了,真的停在还原态。

### 灌库(**已完成,2026-07-13**)

- [x] `pg_dump` 备份 `alg_cases` + `alg_sets` → 服务器 `/root/archive/alg-pre-canonicalize-2026-07-13.sql.gz`
      + 本机异地副本 `D:\backups\cuberoot-pg\`(349 KB,5993 行)
- [x] 生产跑 `.tmp/fix_bad_ll_cases.sql`(4 条 UPDATE)—— 先修坏数据,再重跑规范化
- [x] 生产跑 `.tmp/canonicalize_ll_algs.sql`(3518 条 UPDATE,单个事务)
- [x] 刷新 `PLL_MAIN_ALGS` 快照
- [x] push 前端代码

> **顺序有讲究**:规范化 SQL 是**整块换 `algs` jsonb** 的,所以必须**先修坏数据 → 再重跑
> 规范化 → 再灌**。反过来的话,规范化会把修好的数据整块覆盖回去。

> **复用提醒**:`lib/recon_first_stage.ts:124` 的 `relabelY(body, k)` = `normalize([y^-k, …moves, y^k])`
> 已经是一个可用的 ROTATE 实现(靠 cubing.js simplify 让转体自己抵消),**私有未导出**。
> 需要 ROTATE 时提取它,别另写查表版。

---

## 6''. Phase 0 —— 状态轨道 join(**已完成,2026-07-13**)

**结论:表的 3915 行与站上 3918 个 case 完全对上,零残留。**

```
对上 3896 行  |  站上没有的新 case 19 个  |  定不了的 0 个
站上 3918 个 case 全部被认领(3918 / 3918)
```

> **⚠ 这个结果是订正过的。** 第一版 join 只跑了 `pll` / `zbll` / `1lll`(3893 个 case),**漏了 `ell`**,
> 于是报出「24 个新 case」—— 其中 5 个 `ELL-X` **站上 `ell` 里本来就有**。把 `ell` 纳进来之后:
> 3918 个 case / 3896 个不同的态 / **19** 个真正缺的(全部 Subset=1LLL)。
>
> 纳入 `ell` 还逼出两个 join 逻辑的 bug,**两个都是「分组」这个抽象用错了地方**:
> 1. **组是状态的属性,不是 set 的属性。** `ell` 的 25 个横跨多个 OLL 组,按 set 名给它们一个
>    虚构的「ELL 组」,22 个 X 组的行就去抢 25 个 ell 槽位,17 个匹配不上 → 假「定不了」。
>    改成按每个 case 自己的**朝向类**分桶。
> 2. **朝向类 → OLL 字母必须多数派,不能「第一行胜出」。** 那 19 条坏公式的朝向是错的,
>    一条就能把整个 OLL 组的桶带偏 —— 实测 `L+` / `O-` / `T2` 三组各 72 个**整组丢失**,
>    被误报成「站上没有」。每个朝向类有 ~72 行,一两条坏的翻不了盘。

脚本:`core/packages/alg-build/{ll_ident,sheet_notation,phase0_join}.mjs`
产出:`.tmp/phase0/mapping.json`(matched / newCases / badAlgs / 各判据的失败清单)

### case 身份 = 16 折轨道(不是 4 折)★

公式 `A` 解态 `S` ⟺ `S = A⁻¹`。若 `A' = U^a·A·U^b` 仍解同一个 case,则 `S' = U^-b · S · U^-a`。
⟹ **轨道 = `{ U^p · S · U^q : p,q ∈ Z4 }`,16 个态。**

> ⚠ `fix_bad_ll_cases.mjs` 里的 `cls` 只归一**后** AUF(4 折)。那在"组内查空位"够用(只要单射),
> 但**做 join 不够** —— 表和站的公式可能差一个**前** AUF,那改的是 setup 的**左**乘,4 折会把
> 同一个 case 判成两个。左乘不用在 pattern 上实现:setup 是**字符串**,拼 `U^p <setup> U^q` 就是左乘。

整体转体不必单列:`y = U·Dw'`,LL 态 `X` 与 `Dw` 可交换 ⟹ `y^k·X·y^-k = U^k·X·U^-k`,**y 共轭本来就在这 16 个里**。

### 三个独立来源逐格吻合 ★

| | 纯群论枚举 | 表格自己 | 站上 |
|---|---|---|---|
| LL 态 | **62208** | — | — |
| 轨道(= case) | **3916** | 3915 + 还原态 | 3896 + 19 |
| 朝向类(= OLL) | **58** | 58 | 49 + ZBLL 7 + PLL |
| 每类的轨道数 | **72×51, 40×5, 22×2** | 72×51, 40×5, X=22, PLL=21 | 一致 |

`2×22 + 5×40 + 51×72 = 3916` ✓。两个 **22** 类正是 **OLL skip**(21 个 PLL + 还原)和 **OLL 20**。
枚举器 `enum_ll_orbits.mjs` 只用群论造态,**不碰表也不碰站** —— 所以这是真独立判据。

`22` 不是巧合:OLL 20 = 角全朝向 + 四棱全翻,朝向数组对**任意** U 转不变 ⟹ Z4×Z4 整个 16 元群
都稳定它 ⟹ 288/16 = 18 + Burnside 不动点 = **22**。一般 OLL 的稳定子只有反对角(4 元)⟹ 288/4 = 72。

### 四条判据(全部封闭)

| | 判据 | 结果 |
|---|---|---|
| ① | 状态轨道:表的公式解的 case ↔ 站的 setup 的 case,16 折 key 相等 | 3896 / 3915 |
| ② | 朝向类 == 声明的 `OLL` 列 | 9 行不符 → 全是坏公式 |
| ③ | **站上 1lll 的组号 == OLL 号**(xlsx `OLL` sheet 的字母→号映射) | **49 / 49 全一致 ✓** |
| ④ | 角置换类 == 声明的 `CP` 列 | **329 组全内部一致 ✓** |
| 附 | `Speedcubedb no.`(472 行)↔ 站上 zbll case 名 | 465 / 472(7 条是表侧脏数据,见下) |

> ⚠ **CP 必须在 OLL 框架内算**。直接在 16 折轨道上取角置换最小值只剩 3 个双陪集,而 CP 有 6 类。
> 正解:先取让**朝向数组**最小的那个**后** AUF(把框架钉死),再在 4 个**前** AUF 上取最小。24/4 = 6 ✓
>
> 前 AUF **不改朝向数组**(`ori_T[i] = ori_S[perm_Uq[i]]`,U 不拧角不翻棱 ⟹ `U^p` 的贡献恒为 0);
> 后 AUF 把它整体**轮换**。这条是 ② 和 ④ 的共同地基。

OCLL 字母 → 站上 zbll 组(join 学出来的):`U→U  T→T  L→L  H→H  Pi→Pi  S+→S  S-→AS`。

### 站上缺的 19 个 case

`{X: 17, DS-: 1, O+: 1}`,**全部 Subset=1LLL**。

- **17 个 = OLL 20 组里非 ELL 的那批**。OLL 20 一共 22 个 case,另 5 个(`ELL-X`)站上 `ell` 里已有。
- **2 个 = 被站上那两对重复 case 挤掉的位置**。`DS-` = OLL 4、`O+` = OLL 5,
  正是 `1LLL 4 63 == 4 64`、`1LLL 5 21 == 5 22` 所在的组。**这是跑之前就写下的预言,逐行命中。**

对 GT 收口:`3400 − 20(ELL-A/B 从 1lll 删掉)+ 17 = 3397` ✓;`ell` 的 25 个一个不动 ✓

### ★ 表侧的 19 条坏公式(首条公式解的 case ≠ 本行的真 case)

真 case 全部已确定(靠判据 ②③④ + Scramble 列 + 行内公式多数派),**join 不受影响**;
但这 19 行的**公式本身是错的**,得站长改表。完整清单在 `mapping.json` 的 `badAlgs`。

| Self | Name | 毛病 | 那个 case 的真主人 |
|---|---|---|---|
| 1 | PLL-U+ | 解的是 `pll/Ua` | 2/PLL-U- |
| 3048 | I3R5 | **公式不保 F2L**(唯一单 token 修复:第 6 步 `U'`→`U`) | — |
| 3280 / 3419 | M-U1 / O-U8 | **两行的公式互换了**(只差 `F`/`f`) | 彼此 |
| 698 / 701 | DLRB / DLR6 | 解的是同组别的 case | 689/DLF5、686/DLFB |
| 566…3613 | 另 13 行 | 同上 | 见 `mapping.json` |

- **`PLL-U+` 的 alg[0] 是个坏镜像**。`L2 U' S U2' S' U' L2` vs `PLL-U-` 的 `R2 U' S' U2' S U' R2`:
  只翻了 `R↔L` 和 `S` 的量,**没翻 `U` 的量** —— 正确的 LR 镜像应是 `L2 U S U2 S' U L2`。
  该行另有 **9 条**公式都正确指向 `Ub`,靠这个 9:1 的多数派定的案。
  ⚠ 我当时据此推断「表里的 `MIRRORLR` 很可能也漏翻了 U/F/B/D」—— **Phase 1 全表普查证伪了**,
  这是**全表唯一**的坏镜像事件。详见 §4.1 的订正块。
- **9 / 19 条坏公式是「正确地镜像了一条本身就错位的公式」。** 站长的镜像生成器**是对的**,
  错的是喂给它的源行。`3419/O-U8` 与 `3347/O+U9` 互为**逐字正确**的镜像,却双双错位
  (公式其实分别属于 `3280/M-U1` 和 `3208/M+U1`)—— 错位发生在**被镜像之前**,镜像忠实地把它复制到了对侧。
  ⟹ **镜像自洽 ≠ 正确。别指望用镜像关系当校验器兜住整对错位。**
- 5 行的备选公式里已有正确的,直接提前即可;1 行有唯一单 token 修复;其余 13 行要重写。

### 元数据层意外地干净

`Mirror` / `Inv` / `IM` 三个编号列是**完美的群作用**:Mirror 和 Inv 各自是对合(3915/3915),
`IM == Inv∘Mirror == Mirror∘Inv`(3915/3915,零不一致)。剔掉上面那 19 条坏公式后,
INV / MIRROR / IM 三个关系的**残差为零**。

⚠ `Alg of mirror case` / `Alg of inv case` / `Alg of IM` 三列是**纯 VLOOKUP**(3915/3915 逐字等于
目标行的 `Self alg`),**零新信息** —— 别拿它们反推 MIRRORLR,要查就查 `Self alg` 本身。

### 表侧的其它脏数据(报出来,不动)

- `Speedcubedb no.`:4 行只填了裸 `L`(LL7/LL9/LLA/LLB),`L04`/`S+06`/`S-06` 各被两行重复填。
  **join 反而把它们纠正了**(如 216/LB2 填的是 `L04`,实际是 `ZBLL L 41`)。
  另:该列非空 **472** 行,不是之前以为的 474 —— 472 = ZBLL 总数减 21 个 PLL,表的 `Stat` sheet 自己也这么写。
- 2 行括号不配对(13/PLL-Ga 少一个 `)`,1465/C1U6 多一个 `)`)。两行都没带重复指数,
  丢掉括号照常解析,不影响 case 身份 —— 但表该修。

### 记号解析器 `sheet_notation.mjs`

表里公式的**全部**形态(实测,非猜):换握记号 `↑`(U+2191)`↓`(U+2193)`·`(U+00B7);
逗号复合标签 `[ft,key]` `[oh,big,key]`,可紧贴公式(`[key]U'`),可跟在 `=` 后;
`=` 既在行首也在**行中**当分隔符;括号**会嵌套**(`(R U R' U (R U' R' U)2 R U2' R')`);
无空格连写 `M'L` `ML'` `RL` `U'D'`;量 `R2'` `R3` `L4'`;宽层只有小写 `r l u d f b`(**没有** `Rw`)。

⚠ **散文垃圾混在公式格里**:`(by CubeRoot 251028)` `(by RC Hamner)` `(wyh)`(整行或行尾)、
前缀 `from OLL`、关键词 `table`。**署名一律丢弃**(站长 2026-07-13 拍板,不进 meta),
但**必须先识别再丢** —— 直接喂 tokenizer 会当成 junk 炸掉整条公式。

解析器认不出来的东西**一律报错**,绝不静默丢:3915 行 × 6 个公式列,吞掉一个形态就是几十条公式悄悄算错 case。

---

## 7. 已决事项

| # | 决定 |
|---|---|
| A1 | 起手转体见 §4.3。**y→U 已被实证推翻(0/5099)**。起手 y 予以保留;去重语义待定(§9.1) |
| A2 | `Type` 取决于首条公式,仅部分 case 有 |
| A3 | 学习进度功能(`Train` 列)**后置**,这轮不做;`Yiheng Progress` 丢弃 |
| A4 | `COEP` 只有 ZBLL/PLL 有 → 训练器里对其余 case 回退到 `Inv case` |
| A5 | `V3/V4 PDF` 旧公式不上站;`doc No.`/`Old No.` 只进 meta |
| A6 | 无交换子 / 共轭方括号写法 |
| B7 | 保持三分 + 新增 ell,总数 3915(见 §5) |
| B8 | 字母制命名优先,数字制保留 |
| B9 | ELL 单独成 set,25 个(不含 4 个 EPLL) |
| B10 | 富元数据要展示,但**不能让前端拥挤** → 走弹窗 |
| C11 | 先从生产 dump `alg_sets`/`alg_cases` 灌进本地 pg13(docker 5433)跑通全流程,验收后再上生产 |
| C12 | 上生产前 `pg_dump` 备份这两张表 |

---

## 8. 分期

| 期 | 内容 | 验收 |
|---|---|---|
| ~~**0**~~ | ~~状态轨道 join:表 3915 ↔ 站 3918~~ | ✅ **已完成**(§6''):3896 对上 / **19** 个新 case / 0 个定不了;站上 3918 个全部认领。~~24~~ 是漏了 `ell` set 的旧数;`Speedcubedb no.`(**472** 行,不是 474)465/472 交叉验证通过,7 条是表侧脏数据 |
| **1** | 记号 lib(`sheet_notation.mjs` 已有解析器;还要从 `PlayerControls.tsx` 提出 `stripGripMarks`/`stripPushMarks`/`stripHandMarks` → 共享 lib)+ ROTATE / **MIRROR** / HTM / QTM / GEN / DELETE_AUF + **修 `mirrorAlg` bug 并补测试** | 计步器逐行复现表里 3915 个 `SH`/`SQ`;镜像通过 LL-valid 校验;**用修好的 MIRROR 重算 §6'' 那批经镜像传染的坏公式** |
| **2** | 全量校验 + 问题公式的逐条报告(§6'' 已产出 19 条**首条**公式的坏行;还要扫备选公式) | 交站长过目 |
| **3** | Schema:`alg_cases.meta` migration + `AlgEntry` 扩字段 | typecheck + 现有测试全绿 |
| **4** | 导入:直连 PG 生成 `BEGIN…COMMIT` SQL(范本 `core/packages/alg-build/gen_zbls_sql.mjs`) | 本地 pg13 先跑;计数 3915 |
| **5** | UI:OLLCP 主名 + 富元数据弹窗 + 标签筛选 | |
| **6** | Trainer:打乱类型选择器(`Inv case`/`SH*`/`SQ*`/`H*`/`Q*`/`COEP`) | |
| **7**(后置) | 学习进度追踪(按用户存,绑账号) | |

> **导入必须直连 PG,不能走 REST。** 写接口每 IP 每分钟限流 30 次
> (`server/src/utils/recon_helpers.ts:415`),3900 个 case 要 2 小时且会被 429 打断。
> `POST` 还只能 append(`position = MAX+1`),新组会落到队尾、组序错乱。

---

## 9. 待定 / 阻塞

1. **去重语义**(A1 的后续):`y` ≠ `U` 已证。那么 speedcubedb 的 `y2 R U R' …` 和站长的
   `U2 R U R' …` 是**不同的公式**(不同的物理执行)。去重时:
   - (a) 按 §4.3 消去起手转体后再比 —— 数学正确,但比出来大概率不重复;
   - (b) 直接剥掉起手转体、**不**重映射 body,只拿 body 当去重键 —— 匹配得上,但前提是
     speedcubedb 的 body 写在规范朝向下。

   **需要站长定夺。**

2. **OLL 字母 ↔ 数字映射**:从表的 `OLL` sheet 提取(该表有 `OLL` 字母列 + `No.` 数字列)。

### 已定(站长 2026-07-13)

- 起手 `y` → `U`,body 不动,补末尾 AUF(§4.3')。17 个 body 含 x/z 的例外保留原样。
- `setup = inverse(站长首条公式)`;表的 `Scramble` 列存 meta,供训练器「Inv case」模式用。

---

## 9'. 进度日志

- **2026-07-13** — 分析完成。表已导出、公式层+值层齐全;case 轨道理论坐实(ZBLL 7776/16+对称 = 493 ✓);
  镜像/旋转/消转体三条规则实测通过;发现 `mirrorAlg` 线上 bug;全量校验跑出表侧 190 条问题公式。
  **未写入任何生产数据。**

- **2026-07-13(续)** — **先导任务代码完成,SQL 待灌**(§6')。
  - 起手 y→U 的**恒等式证严了**(§4.3':`y = U·Dw'`,`Dw` 与顶层支撑的公式可交换),
    判据从"body 只动顶层块"更正为"**`A` 与 `Dw` 可交换**"(前者充分不必要 —— 净 `y` 的 body 照样可改写)。
    判据在 5567 条可解析公式上与实测 oracle **一致 5567/5567**。
  - **推翻了自己的两个假设**:①「九个集合目标态统一 = 整体还原」是错的(`oll`/`coll`/`cmll`/`ollcp`
    只完成本阶段,1173 条公式对本阶段正确却不还原整方);②「按 `sticker.kind === 'face'` 筛选」是陷阱
    (`vls`/`wv`/`sv` 也是 face,却是最后一槽集合;非 LL 集合 474 条起手 y 公式改写成功率 **0/474**)。
  - 收尾 AUF 的存法**站长拍板方案 1**:存进库,前端 `displayAlg()` 显示时剥掉。
  - 改写 5081 条 + 补收尾 AUF 550 条 + 跳过 18 条;本地 pg13 dry run **5631/5631 全绿**。
  - 顺带揪出 4 条既有坏数据(`pll Ub` 重复丢 y2、`ollcp OLLCP10 2` 末尾 U 应为 U'、`1lll` 两个坏 case)。
  - **未写入任何生产数据。**

- **2026-07-13(三)** — **先导任务收尾:4 条坏数据修掉,规范化灌入生产**(§6')。
  - 备份先行:`alg_cases` + `alg_sets` 单独 pg_dump,服务器 + 本机各一份。
  - 4 条坏数据**全部修好**。两条 1LLL 的 setup/sticker/alg 是**同源互推**的,库内没有独立判据 ——
    改用四条**库外**判据(唯一单 token 修复 / 枚举空位 / 独立修 setup 指向同一态 / 组内 AUF 类完整性),
    四条同时成立才写。第 3、4 条判据要求朝向**对 AUF 归一**(按槽位存的朝向数组会被 U 转轮换)。
  - 顺序:**先修坏数据 → 重跑规范化 → 再灌**(规范化整块换 `algs` jsonb,反过来会覆盖掉修复)。
  - 灌后线上复验:7808 条公式,整还原集合 **0 条不还原**,起手 y 只剩 15 条(全部合法:净旋转带 x/z)。
    Playwright 实证 `/zh/alg/3x3/pll`:**末尾 U 零条**。
  - `sticker` 的颜色串是**死数据**(全站只读 `sticker.kind`,缩略图走 `setup`),坏 case 的没重生成 ——
    它的 facelet 编码复现不出来,硬生成只会造出新的垃圾。
  - 新发现:`1LLL 4 63 == 4 64`、`1LLL 5 21 == 5 22` 是重复 case(**没修**,Phase 0 的活);
    站内 `1lll` = 49 个 OLL 组(缺的 OLL 20–27 正是 ZBLL/PLL 地盘),与站长那张表对得上。

- **2026-07-13(四)** — **Phase 0 完成:状态轨道 join 零残留**(§6'')。
  - case 身份从 4 折**订正为 16 折**(前 AUF × 后 AUF)。4 折在"组内查空位"够用,做 join 会把
    同一个 case 判成两个 —— 两边公式差一个前 AUF 时,改的是 setup 的**左**乘。
  - 结果(**订正后**):**3896 行对上 / 19 个新 case / 0 个定不了;站上 3918 个 case 全部被认领**。
    第一版漏了 `ell` set(站上早就有 25 个),报的是「3891 / 24 个新 case」。
  - 19 个新 case = **17(OLL 20 组里非 ELL 的)+ 2(被站上那两对重复 case 挤掉的,落在 OLL 4 和 5)**。
    **这是跑之前写下的预言,逐行命中。** 对 GT 收口:3400 + 17 − 20 = 3397 ✓,20 + 5 = 25 ✓
  - 三个独立来源逐格吻合:纯群论枚举(62208 态 / 3916 轨道 / 58 朝向类 / 72×51+40×5+22×2)、
    表的 OLL 直方图、站上的数据。`22` 由 Burnside 推出(OLL 20 的朝向对任意 U 转不变 ⟹ 稳定子是
    整个 16 元群 ⟹ 288/16 + 不动点 = 22)。
  - 四条判据全部封闭:轨道 / 朝向类 == OLL / **1lll 组号 == OLL 号(49/49)** / **角置换类 == CP(329/329)**。
    ⚠ CP 必须**在 OLL 框架内**算,直接在 16 折轨道上取最小只剩 3 类。
  - 揪出**表侧 19 条坏公式**(首条公式解错了 case)。真 case 全部已定,join 不受影响,但表要改。
    其中 `PLL-U+` 的 alg[0] 是个**坏镜像**(没翻 U 的量),与 §4.1 站上 `mirrorAlg` 同款 bug;
    坏公式还经镜像**传染**给了别的行(3419 → 3347)。Phase 1 修 MIRROR 时要一起重算。
  - `Speedcubedb no.` 非空是 **472** 行不是 474(474 是之前的误记);该列本身有脏数据,
    join 反而把它纠正了。
  - **未写入任何生产数据。**
