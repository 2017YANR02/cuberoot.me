# Issue #40 详细分析与实施方案

> 本文 = 逐条分析 + 方案 + 待拍板问题清单。分 6 个子任务(T1–T6)。
>
> **进度**(2026-07-25):T1 数学页 ✅ 已改完(三类口径已修正为 306 × 494,见 §1.3c);
> T2 一步/两步 toggle ✅ 已做完;T3 公式/图 toggle ✅ 已铺开;
> **T4 ✅ 已落库**(zbls 305 + wv 27 归一 FR + 四朝向);T5 镜像系统等拍板;T6 长线。

## 0 依赖链(实施中发现,与最初排期不同)

原计划 T2(lsll 二类/三类 toggle)可独立做,**实际不行**:

```
T4 (zbls setup 清洗 + FR 归一)
      └─→ 完整干净的 306 条 Z_φ 公式表
              ├─→ T1 的三类精确值
              └─→ T2 的「二类 case → 三类代表元」映射表
```

> **2026-07-25 更新**:三类的**数字**已不再依赖公式表 —— 良定义的口径是 306 × 494 = 151,164(§1.3c)。
> 但这条依赖链本身仍然成立:T4 依旧是 T2 那张「二类 case → 三类路线」映射表的前置,
> 而 §1.3 那个会漂的商口径也只有在完整公式表下才能量出来。下表保留为当时的实测记录。

### 这条依赖链已被实测证实(T4 落库前后对比)

| | 落库前 | 落库后 |
|---|---|---|
| `lsll-class3.mts` 能匹配的 ZBLS case | 105 / 305 | **305 / 305** |
| 判为「违反 LSLL 前提」 | 200 条 | **0 条** |
| 9 个对称构型里有公式的 | 6 | **9** |
| 商口径在当前公式库下的实例值 | 148,853 | **147,508** |

原因:LSLL 坐标提取要求标准朝向,而落库前 200 个 case 的 setup 带整体转体
(`A-/EO: L' U' L y' U`),提取器认不出 —— 归一抽掉转体正是它的前置。
**旧的 148,853 是拿三分之一公式表算出来的**,已改成 147,508(仍在严格区间
[147,220, 151,164] 内,且比天真值 145,821 大 —— 与理论一致)。

> 踩过的坑:`scripts/.cache/*.json` 会缓存 API 响应,落库后不刷新缓存,脚本读的还是
> 旧数据(我因此一度以为「归一不影响匹配率」)。脚本支持 `ZBLS_JSON=<file>` 覆盖。

另外提醒:issue 说三类「相当于是从第二类里每 4 个选一个代表元」——**做不到**。
三类根本不是二类的商(mid 不作用在局面上,见 §1.3c),583,284 / 151,164 ≈ **3.858** 也不是整 4。

> **2026-07-25 再更新**:T2 落地后连那张「二类 case → 路线」的映射表也不需要了 ——
> 两步浏览的是路线本身,ZBLS 与 ZBLL 两半各自枚举就够(见 T2 节),不必从二类那 58 万条走过来。
> T4 依旧是前置:ZBLS case 的名字与公式来自归一后的 zbls 集。

---

## T1 /math/lsll:数字溯源 + 三类 AUF 计数

### 1.1 现状盘点

页面(`app/[lang]/math/lsll/page.tsx`)已有完整的 9,331,200 → 583,284 推导链。issue 点名的 **384** 出现在 §4 Burnside 表 `(2,2)` 行:它有上下文但**没有推导**,同表的 `16 × 4` 也是裸给的。补法如下(实现时写进页面):

**Fix(a,b),a,b∈{1,3}(奇转,共 4 个)= 16**:
- 顶层 4 角位被 4-循环打转,不动态要求角置换落在 S₄ 中 4-循环的中心化子 = ⟨循环⟩,4 个;
- CO 沿轨道常值 c,总和 4c ≡ c ≡ 0 (mod 3) ⇒ c=0,1 个;
- EP 同理 4 个;EO 常值 e,总和 4e ≡ 0 恒成立 ⇒ 2 个;
- 置换奇偶耦合(4-循环是奇置换,CP·EP 需同奇偶)砍半:4·1·4·2 / 2 = **16** ✓

**Fix(2,2) = 384**:
- U² 把顶层 4 角位分成两个 2-循环,(13)(24) 在 S₄ 的中心化子 = 24/3 = 8 个;
- CO 每 2-轨道常值 c₁,c₂,和 2(c₁+c₂) ≡ 0 (mod 3) ⇒ c₁+c₂≡0,3 个;
- EP 8 个;EO 每轨道常值,和恒偶 ⇒ 4 个;
- 奇偶耦合砍半:8·3·8·4 / 2 = **384** ✓

### 1.2 三个数(按 issue 收敛)

| 类 | 定义 | 值 |
|---|---|---|
| 一类 | 原始态全集 | 9,331,200(已有推导) |
| 二类 | 商 pre-AUF + post-AUF | 583,284(已有推导 + 暴力枚举双验证) |
| 三类 | 两步路线数 = ZBLS case × ZBLL case | **306 × 494 = 151,164**(与公式表无关;见 1.3c。不要用「再商一次 mid」的口径 —— 那个会漂,见 1.3) |

上一轮刚加的"只商单侧 AUF = 2,332,800"章节(§3),按 issue"其他不需要"**删除**(issue 覆盖了上次的口头需求)。

**确认**:/alg/lsll 当前的 583,284 就是二类(两侧 Z₄×Z₄ 商,`lib/lsll/model.ts` 的 `TOTAL_CASES`,与 §4 Burnside 及暴力枚举一致)。

### 1.3 mid-AUF:实测结论 —— 「再商一次」这个口径不是良定义的不变量

> 本节全部由 `packages/client/scripts/lsll-class3.mts` 实证,不是推测。跑一次约 20 秒。

**先把对的结构立住**(脚本逐项验过,全中):

| 量 | 值 | 校验 |
|---|---|---|
| ZBLS 构型空间 \|Φ\| | 1,200 | 定义式枚举 |
| ZBLS case = Φ / pre-AUF | **306** | DB 的 zbls 恰好 305 = 306 − 全解态 ✓ |
| pre-AUF 轨道谱 | 297 自由 + 3 个 \|Stab\|=2 + 6 个 \|Stab\|=4 | 4·297+2·3+6 = 1200 ✓ |
| 每个 fiber ≅ ZBLL 空间 | 7,776 | = 9,331,200/1,200 ✓ |
| fiber 上 mid+post 双侧商 | **494** | = 通行的 ZBLL 493 + 全解态 ✓ |

**再说为什么给不出唯一的精确值。** pre 和 post 是状态空间上的规范作用(右乘 / 左乘 U)。mid 不是:它是「做完 ZBLS 公式之后」的转顶层,在 s 空间里的作用等于右乘 `W·U⁻ᵏ·W⁻¹`,**W = 所选的那条 ZBLS 公式**。换公式,mid 的作用就换。

对 297 个**自由**构型,pre 被完全吸收,fiber 内只剩 mid+post,贡献恒为 494 —— 与公式选择无关。对 9 个有 pre-AUF 对称的构型,stabilizer 会在 fiber 上多诱导一个「右乘 V = Z⁻¹UᵏZ」,而 V 直接依赖 Z。实测(同一构型、同样合法的公式,只在尾部接一条 ZBLL 群元素):

```
O/I  Z = F R U R' U' F'                      → 轨道 19
               Z = F R U R' U' F' + Sune               → 轨道 62
               Z = F R U R' U' F' + T-perm             → 轨道 89
D+/D L         Z = U F' (L' U2 L U')2 F                → 轨道 127
               Z = 同上 + U-perm                        → 轨道 494
```

**所以「商掉三个 AUF」这个口径不产生规范的 case 数。** 它的形状是:

- **硬部分(与任何选择无关)**:297 × 494 = **146,718**
- **严格区间**:∈ **[147,220, 151,164]**(9 个对称构型每个 ∈ [1, 494];全解构型恒为 494,因为那里 Z = 恒等、mid 与 pre 重合)
- **当前站内 zbls 公式库这一实例**:**147,508**
- **issue 的估算 583,284/4 = 145,821 一定偏小** —— 真值必然大于它,原因是 pre/mid/post 三者不构成自由的 (Z₄)³ 作用。

### 1.3c 良定义的三类:不商,而是乘 = 306 × 494 = 151,164

上面那个口径的毛病是把 mid 硬塞成「局面之间的等价关系」。**局面是死的** —— 你插不插那一下 U,魔方一动不动;mid 发生在**解法**上。两步解法里真正在变的是**一对图**:先认出一个 ZBLS case,做完之后看到一个 ZBLL case。两个集合各自规范,于是

> **N₃ = 306 × 494 = 151,164**(扣掉「全部已解」那格 = 151,163)

每个组合都真会出现(固定任一条 Z_φ,它的 fiber 打满整个 ZBLL 空间),所以这个数对**任何**公式表都一样。两条佐证,都已进 `tests/lsll_class3_structure.test.ts` + `scripts/lsll-class3.mts`:

1. **同一构造在末层上复现社区数字**:216 朝向态 / pre-AUF = **58** 个 OLL(57+跳O),288 置换态 / 两侧 AUF = **22** 个 PLL(21+跳P)。没有人把两步末层定义成「1LLL 的 3916÷4 = 979」;它离 58×22 = 1276 差得远,正如 583,284÷4 = 145,821 离 151,164 差得远。OLL 里同样有对称 case(同公式、不同 AUF 进、不同 PLL 出),社区选的就是乘法。
2. **151,164 恰是商口径的上确界**:对称类取满 494 ⟺ 额外生成元 V ∈ ⟨U⟩,而换公式 = 把 V 在 ZBLL 群内共轭一次。扫完 7776 个元素:**9 个对称类全部够得着**(|Stab|=4 → U^±1,|Stab|=2 → U²)。所以商口径永不超过乘积且够得着;它每低一点,都是某条公式把两个本来不同的 ZBLL case 强行粘掉的信息(当前库粘掉 151,164 − 147,508 = **3,656** 个)。

一处诚实说明:一类、二类数的是**局面**,151,164 数的是**两步路线**。这确实是两种量 —— 但三类问的本来就是路线,而这是它唯一良定义的版本。

### 1.3b mid-AUF 的数学地位(推导细节)

pre 和 post 是状态空间上的**规范**群作用:解条件 g·A·Z·M·B·C = e(A=U^a pre,M=U^m mid,C=U^c post),变 a ⇔ 右乘 U,变 c ⇔ 左乘 U。但**变 m ⇔ g′ = conj_{(BC)⁻¹}(U^k)·g,依赖所选的 phase-2 公式** —— 一个置换只有左右两侧,"第三个 AUF"必然要借助解法的两段拆分才有定义。极端反例:允许 phase-1 直接整个复原(技术上也是"解完槽+EO"),则 mid 商完全退化。

**规范化方式(推荐定义)**:与速拧现实一致 —— 每个 ZBLS case φ 固定一条公式 Z_φ("公式表"),mid 作用定义为 s ↦ s·(Z_φ·U·Z_φ⁻¹)。此时结构非常干净:

- ZBLS 构型空间:槽角 5位×3向 = 15,槽棱 5位×2向 = 10,顶层其余 4 棱 EO = 16,总翻转奇偶约束 ÷2 ⇒ **1,200 个 φ**。
- 商 pre:Burnside 不动点 f(U)=f(U³)=6、f(U²)=12(槽块必须在槽内 + EO 周期化 + 奇偶),(1200+24)/4 = **306**。DB 的 zbls set 恰好 305 个 case = 306 − 1(全解态不算 case)—— 数据与理论闭环 ✓。
- 固定 Z_φ 后,fiber(共享 φ 的 7,776 个态)与 ZBLL 态空间(24·24·27/2 = 7,776)双射;mid = 右乘 U、post = 左乘 U 作用其上,双侧商 = **494 = 通行的 ZBLL 493 + 全解态**(与 DB 闭环 ✓)。
- φ 的 pre-轨道:自由 1,188/4 = 297 类、U²-对称 3 类、全对称 6 类(共 306)。自由类每类贡献 494:

  **商口径 = 297 × 494 + (9 个对称类的贡献) = 146,718 + Δ**,Δ ∈ [502, 4,446]

  9 个对称类的 φ 都是"槽块归位"构型,类内还要再商掉 stabilizer 诱导的作用 —— 这一项就是公式依赖的来源。**Δ 的上端 9 × 494 = 4,446 可达**(§1.3c 的共轭类扫描),于是商口径的上确界 = 146,718 + 4,446 = 151,164 = 306 × 494,与良定义的乘积口径重合。
- **用户的估算 583,284/4 = 145,821 会被略微超过**(不是不足):mid 商在 fiber 上"整除得太狠"的直觉不成立,因为 pre 动的是 φ、mid/post 动的是 fiber 内部,三者不是自由的 (Z₄)³ 积作用。

### 1.4 一条撤回的结论 + 一条新增的校验

**撤回**:本文早先版本称「zbls 有一批 setup 根本不是合法 LSLL 态(`M' U M U2 R' F R`、`L' U L f R U R' U' f' U` 等),只有 85/305 可用」,并称 f2l 有 51 条 `setup+alg` 做不完 F2L。**那是假警报** —— 我的探针没有接站上的记号规整层 `normalizeAlg`,把 `FS'` / `MR` / `ML'` 这类**上游粘连记号**和串首的 `↓` 换握标当成了坏数据。接上之后 zbls **305/305 全部合法**。

**新增的校验(有价值,保留)**:站上现有的「校验」问的是「`setup + alg` 能不能到目标态」,它抓不到「**setup 本身画出来的图对不对**」—— 一条 setup 就算把别的槽也搅了,只要 alg 顺手修回来,现有校验照样全绿。为此新增 `lib/alg_precondition.ts`(与 `alg_goals.ts` 对偶:那边管「做完之后」,这边管「做之前」),并用 `scripts/scan-precondition.mts` 扫了全库:

| set | 结果 | set | 结果 |
|---|---|---|---|
| f2l / zbls / wv / sv / vls / cls | 0 不合法 | oll / pll / coll / zbll / 1lll / ell / anti-pll | 0 不合法 |
| adv-f2l / fruf | 前提标 `none`(它们的 case 本就允许多槽 / DF 在外) | **ollcp** | **1 条** |

**合计 1 / 5158**。唯一那条:

```
ollcp / OLLCP27 2    "U R2' D' R U' R' D R' U R' U' z'"   ← 底两层没解干净
```

前提表按 set 登记(`SET_PRECONDITION`),没登记的一律放行,不误伤。带 24 朝向容忍 —— 库里有 112 条 zbls setup 末尾留着 `y`/`y'`,不容忍会全体误判。

页面改版:§3 删,新增"三类 AUF"章节(pre/mid/post 定义、mid 非规范性说明、N₃ 推导 + 精确值),§4 表补 16/384 推导。

---

## T2 /alg/lsll:一步 / 两步 toggle ✅ 已完成(2026-07-25)

> 原方案写在"三类 = 二类的商、取代表元"的假设上,那个假设是错的(§1.3c)。
> 落地按修正后的口径重做:三类不是二类的子集,而是**两步路线** = (ZBLS case, ZBLL case) 有序对。

- **交互**:页头 `PillToggle`「一步 / 两步」(用户看得懂的说法,二类/三类的数学名留在 /math/lsll),
  `useQueryState('cls', parseAsStringEnum(['2','3']).withDefault('2').withOptions({ history: 'push' }))`,
  hub 与 `[group]` 各一个,链接互相带 `?cls=3`。
- **没有代表元、也不需要映射表**。两半各自枚举即可,合起来 306 × 494 = 151,164 —— 常数级数据,
  不用分块 JSON、不进 `stats/`、不占 bundle。`lib/lsll/class3.ts`:
  - ZBLS 构型 φ = (槽角位+朝向, 槽棱位, 5 个棱位的 EO),1,200 个,商 pre-AUF → **306**;
  - ZBLL 态 = 槽对归位 + EO 全正,7,776 个,`canonicalKey` 双侧商 → **494**;
  - 两半各自出图(ZBLS 图只画朝向:正 = U 面黄、翻 = 侧面黄;ZBLL 图走 plan 视图)。
- **浏览顺序照两步解法的顺序**:`[group]?cls=3` 先列本大类的 ZBLS case(TT/CS/ES 各 8、
  D±/O 各 4、F/C± 各 2),挑一个再看它后面的 494 个 ZBLL case(48/页,11 页);
  单条路线 `/alg/lsll/route?z=&l=`(静态壳 + query,同 case 页,免 Vercel 配额)。
- **接站内公式库**:`scripts/gen-lsll-zbll-overlay.mts` → `lib/lsll/zbll_algs.json`,
  zbll 集 472 + pll 集 21 = **493 全部命中且零碰撞**(剩下那 1 个就是跳过)。
  与 zbls 那支(305/306)是一对。
- **train 不动**。旧方案的"三类只抽代表元"随代表元一起作废;真要练两步,前半段 = zbls 库训练器、
  后半段 = zbll/pll 库训练器,都已存在,不在 LSLL 页重造。
- **两条闭环当回归**(`tests/lsll_class3_model.test.ts`,12 条):纯组合枚举 vs 人工录入的公式集,
  两边独立 —— 306 个构型的 42 组分布逐组等于 zbls 库(+O 那格全解)、305 条 setup 落到 305 个不同构型
  且大类判定一致;494 个 ZBLL case 恰好盖住 493 条库引用。数字口径本身仍锁在 `lsll_class3_structure.test.ts`。

---

## T3 公式/图 toggle 铺到 /alg 全部列表页

现状:`components/AlgCategoryView.tsx` 已有 `cards`(只看图)/`full`(公式内联)PillToggle,偏好存 localStorage(全站生效),服务所有 DB-backed `[puzzle]/[set]` 页。

方案:把这个开关抽成 `components/AlgViewModeToggle`(共享 localStorage key,保持现语义),铺到不走 AlgCategoryView 的列表页:

| 页面 | 处理 |
|---|---|
| `/alg/lsll/[group]`(LsllGroupClient) | 接入 toggle(图模式=现状网格,公式模式=内联展示反演解法/收录公式) |
| `/alg/lsll/train` | 不适用(训练流,非列表) |
| `/alg/roux` | 盘点后接入(实现期核对现状) |
| `/alg/3bld/*`、`/alg/skewb-trainer` | 盘点后逐页接入;确无"case 列表"形态的页豁免 |
| `/alg/commutator`、`/alg/progress` | 预计豁免(非 case 列表),盘点确认 |

原则:凡"case 网格/列表"页都有该 toggle;实现первый步是逐页盘点表(交付到 PR 描述)。

---

## T4 ZBLS/WV:LS 统一 FR 位 + 多向(y 转体)图与公式

> **状态:2026-07-24 已落库**。脚本 `scripts/normalize-slot-to-fr.mts`(`--send`),
> 落库前备份在 `.tmp/alg-backup/3x3-{zbls,wv}-before-20260724.json`。

### 实测结果

| | zbls | wv |
|---|---|---|
| case 数 | 305 | 27 |
| 原槽位分布 | FR 169 / FL 89 / BR 44 / 槽已还原 3 | FR 27(本来就齐) |
| setup 残留整体转体 | y' 99 / y 7 / y2 6 | 无 |
| 挪槽(setup + 公式都改) | 133 | 0 |
| 剥掉的孤儿前导转体 | 132 条公式(101 个 case) | 0 |
| 公式数 1 组 → 4 组 | 438 → 1752 | 98 → 392 |

归一后 302 个 case 槽在 FR、3 个是「槽已还原」(不是槽位问题),残留转体 0。

### 三件容易漏的

1. **`standard` 也是公式**。它是 `algs[0][0]` 剥掉起手 AUF 和转体前缀的版本(`C-/C T'`
   的 alg0 = `y U' (↓L' U L' U') (B' U B L2)`,standard 是去掉 `y U'` 的部分),缩略图
   fallback(`algs.flat()[0]?.alg ?? sample.standard`)和 `.alg-case-standard` 都吃它 ——
   面标一变必须跟着重贴。270 条,全过。
2. **孤儿前导转体**。上游把负号子组录成「setup 转到 FL 展示 + 公式开头先转回来」,
   这两个是一对。只抽 setup 那半边 = 归一做了一半:槽在 FR 了,公式却要先空转一下才动
   (103 / 305 个 case 的 FR 组**没有一条**能原地做)。判据是「前缀**恰好**等于
   `y^-ypow`」而不是「有没有 y」—— `A+/VP` 五条公式各带不同前缀,那是上游有意的视角
   变体(f2l 库里同样有 `y' r' U' R U M'`),一律清会把它们毁掉。剥掉之后重贴用 `R_m`
   而非 `R_t`,推导见脚本内注释。
3. **`ypow` 落库即丢失**。归一依赖 setup 里那个整体转体,写完库里就没了 —— 中途改规则
   要重跑,必须用 `--from=<备份>` 当输入,否则孤儿判据静默失效。(第一轮落了 168 个才
   发现孤儿问题,只能停下来从备份重跑。)

### 前端零改动

`AlgCategoryView` 的 y 旋转控件是**数据驱动**的(`oriNames.length > 1` 就出,`oriIdx` 有
`< c.algs.length` 兜底),`AlgCaseView` 同理 —— 数据一改控件自动出现,不用动组件。
`components/AlgCaseMetaContent.tsx` / trainer / `/alg/progress` 全走 `algs[0]` 或
`algs.flat()[0]`,四向后取到的仍是 FR 首条。

唯一要改的下游是 `scripts/gen-lsll-zbls-overlay.mts` 的 `algCount`:原本全组求和,
四向后会把「这个 case 有几种解法」虚报成 4 倍,改成只数 `algs[0]`(改完重新生成,
JSON 内容与落库前逐字节相同 —— 正好证明四向没污染这条链)。

### 现状(落库前的核实记录)

- zbls 305 case:A− 等负号子组 setup 带 `y'` 尾巴(即 FL 位展示),oriNames 全空(无多向);subgroups = A±…X± + F/S/T/O(不成对的即自镜像族)。
- wv 27 case:单向、无 subgroup,slot 在 FR(setup 如 `L' U2' R U' R' U2' L U'`)。
- f2l 是范本:`oriNames: ["Front Right","Front Left","Back Left","Back Right"]`,`algs` 为按向二维数组,`AlgCategoryView` 已有 y-chip 切换(`oriAdjustSetup` 给 setup 追加 `ORI_SUFFIX[oriIdx]`)、`AlgCaseView` 已支持多向 —— **全部复用,零新轮子**。

### 步骤

1. 脚本(cubing.js 模拟 setup)逐 case 判定槽位;FL case 做 y 共轭归一:setup、algs 全部按旋转重写字母,`validateAlgCase` 全量校验后经 admin API(`X-Admin-Key`,key 读 `.password.md`)批量 PUT。
2. 为 zbls/wv 全部 case 填 `oriNames`(与 f2l 同四向)并把 algs 升为按向二维;其余三向初始公式 = 旋转重写(后续被 T5 镜像系统持续充实)。
3. zbls 的 `trainerKey` 与 `lib/lsll/zbls_algs.json` 依赖 setup:重跑 `scripts/gen-lsll-zbls-overlay.mts` + 回归测试。
4. GET 缓存 1h:批量改完 bump 提示,admin 走 fresh。

---

## T5 镜像系统(f2l + 一切单槽 set + LSLL)

### 5.0 实证结果(`scripts/f2l-mirror-probe.mts`,已跑)

| 验证项 | 结果 |
|---|---|
| **S(前后镜)= M(左右镜) ∘ y²** | **41 / 41 成立** ✅ → 镜像家族确实只是**一对** case,「前后镜像情况」= 同一伙伴的另一个 y-view |
| **镜像重写表正确性**(setup 与 alg 一起重写后仍做完 F2L) | **492 / 492 通过** ✅(全库 164 条 × 3 个镜像,一条不漏) |
| 同字母 +/− 互为左右镜像 | 41 条里 6 条直接命中;其余因指纹按「整个顶层」比而未判定(镜像会把槽从 FR 送到 FL,须先按槽归一再比)—— **不是反例**,判定留到实现期(与 T4 的归一逻辑同一套) |
| 对照组 | **0 条脏数据**,f2l 库 164 条 `setup + alg` 全部做完 F2L ✅ |

**一条撤回的结论(2026-07-26)**:本节早先写过「f2l 库里有 51 条 setup+alg 做不完 F2L,T5 落地前要先清」。**这 51 条是探针脚本自己的假警报,不是脏数据** —— 判据当时要求收尾时魔方朝向不变,而 F2L 公式带 `y'` / `d` 开头是家常便饭(换个槽拧),做完后魔方整个偏过去了,底两层照样是解好的。站内验证器 `lib/alg_goals.ts` 的 `CUBE_ORIENTATIONS`(24 个整体转体)本来就是为这件事准备的,探针没用它。修正后:对照组 51 → **0**,而那 51 条进入测试池反倒把镜像重写的样本从 339 抬到 492,依然全过。逐条复核见 `scripts/f2l-dirty-audit.mts`(现输出 113 条零转体 + 51 条含净转体 + 0 条真错)。**T5 没有前置的数据清理任务。**
| 公式分布 | 不含 F/B **102 条**(可补 3 个镜像);含 F 不含 B **61 条**(只补左右镜);含 B **1 条**(可补 3 个) |

**踩到并修掉的一个坑**(重写表的正确写法,实现时直接照抄):面层镜像一律翻转向,但 **slice 不是** —— 轴与镜面法向平行时,轴自己也被翻了一次,和手性那次抵消:

| 变换 | 面层 | M | S | E |
|---|---|---|---|---|
| LR(左右镜) | R↔L,翻 | **不翻** | 翻 | 翻 |
| FB(前后镜) | F↔B,翻 | 翻 | **不翻** | 翻 |
| y²(纯旋转) | F↔B、R↔L,不翻 | **翻** | **翻** | 不翻 |

按「全部翻转」写会漏掉 12 条含 M/S 的公式(修正前 327/339,修正后 339/339)。

### 5.1 由此得到的结构(现已实证,非推理)

在"槽统一 FR"的归一化坐标下,**左右镜像和前后镜像给出同一个伙伴 case**:M-镜像∘y′ = S-镜像∘y(因为 S = M∘y²,而 y² 与 M 交换)。即镜像家族不是 4 个 case,而是 **一对 {c, c*}**(自镜像时退化为单个)。"左右/前后/左右+前后"三种镜像的区别体现在**公式(与其所属的 y-view)**层面:

| 公式重写 | 落点 | 字母映射(节选,全表脚本生成并用 KPuzzle 随机态等价性测试验证) |
|---|---|---|
| M(左右镜) | c* 的 FL-view(视图两两对调 FR↔FL、BL↔BR) | R↔L 反向,U/D/F/B 反向,F 仍是 F —— **不产生 B** |
| S(前后镜) | c* 的 BR-view(FR↔BR、FL↔BL) | F↔B 反向,U/D/R/L 反向 —— **F ⇒ B** |
| y²(左右+前后) | **c 自己**的 BL-view(纯旋转,非镜像) | F↔B、R↔L 同向 —— **F ⇒ B** |

锚定检验:`U R U' R'` → M:`U' L' U L`(FL-view)✓ 标准 F2L 镜像;S:`U' R' U R`(BR-view)✓;这与用户"含 F 前后镜像会出 B"完全吻合。F2L 的 A+/A− 命名恰是 {c, c*} 配对;ZBLS 的 ±号子组、自镜像的 F/S/T/O 同构地对上。

### 5.2 F/B 规则(形式化)

对一条公式 A(字面检测 move 族):

- A 不含 F 族、不含 B 族 → 自动生成 **M、S、y² 三份**;
- A 含 F 族、不含 B 族 → 只生成 **M**;
- A 含 B 族 → 视为"该 case 下 B 顺手",生成 **三份**。

**F 族 = `F` 与 `f` 两族(含 `F2/F'/f2/f'`),就这两个**(2026-07-26 拍板)。`S`、`z` 不算 —— 前后镜的镜面法向与它们的轴平行,轴自己那次翻转与手性那次抵消,所以 `S → S`、`z → z'`,**镜像结果里不会冒出 B**,没有理由少生成两份。`f` = `F` + `S`,前后镜会变 `b`,归 F 族。B 族对称同理(`B`/`b` 算,`S`/`z` 不算)。这与 §5.0 表里 "FB:S 不翻" 是同一件事。

所有自动生成条必须过 `validateAlgCase` 才落库;失败即跳过并告警(镜像表正确时不应失败,失败=映射 bug 信号)。

### 5.3 数据模型

- `alg_cases` 加列 `mirror_case_id BIGINT NULL REFERENCES alg_cases(id)`(互指;自镜像指自己)。一次性脚本按 subgroup 命名规则(±对合)+ 状态级校验(镜像 setup 归一后比对)建链。
- alg entry(JSONB 内)加 `mid`(lineage uuid,四向同源公式共享)+ `gen: 'lr'|'fb'|'y2'`(自动生成标记,人工原创无此字段)。JSONB 可扩展,**无需迁移 algs 结构**。
- LSLL(58 万,不进 DB):镜像关系 = σ 作用于 canonical key,纯前端现算(`lib/lsll/mirror.ts`),case 页显示"镜像 case →"链接即可,不参与 DB 同步。**✅ 已完成(2026-07-26)**,细节见下。

#### 5.3a LSLL 镜像已落地(2026-07-26)

`lib/lsll/mirror.ts` + `tests/lsll_mirror.test.ts`(14 条)。三件当时想当然会踩的:

1. **镜面不能用「左右镜」** —— M 平面把最后一槽从 FR 送到 FL,出来的根本不是 LSLL 态。要用**过 FR 与 BL 两条棱的对角面**(`U→U D→D F↔R L↔B`),它把槽钉在原地;这就是 §5.1 说的「左右镜 ∘ y」。
2. **棱的翻向不是原样照搬** —— EO 记法在 F↔R 下不对称(`R` 一枚棱不翻,镜像 `F'` 翻四枚)。逐贴纸推出来是 `eo' = eo + [位置在 E 层] + [块是 E 层块]`;角是 `co' = -co`。判据:`mirrorAlg('R') === "F'"` 且两边末态逐块相等,500 条随机公式对撞。
3. **镜像公式要补 AUF** —— σ(U^a·s·U^b) = U^-a·σ(s)·U^-b,而两个 case 页各自显示各自等价类的 canonical 代表元,AUF 一般对不上。实测:`U2 R U' R'` 的裸镜像 `U2 F' U F` 在镜像 case 页判「没有解掉」。`mirrorAlgForCase()` 试 4 个前置 U 并与开头的 U 合并;往返实测 A+ `U2 R U' R'` ⇄ A− `U F' U F`。

**σ 的不动点 F = 432**(`scripts/lsll-mirror-census.mts` 全量枚举):S 96、O 192、F 144,T **一个都没有**(与 S 只差槽棱翻不翻,却是 0 —— 已显式锁进测试,别当漏算)。大类配对恰好是字母的 ± 对合,S/T/O/F 自映。

### 5.4 同步语义(server 端,`utils/alg_mirror.ts`)

- **新增公式**(POST case alg):按 5.2 生成镜像条写入 c* / c 对应 view;**M 份插入与源相同 index(保持左右镜顺序一致),S/y² 份放末尾**(issue 原话)。目标 case 不存在时自动创建(命名走 ±对合规则)。
- **编辑公式**(PUT):按 `mid` 找同源条,应用对应重写**无条件覆盖**(2026-07-26 拍板:一律覆盖,不做"人工改过就脱钩"的分叉)。
- **删除**:同步删同 `mid` 条。
- **覆盖是有代价的,UI 必须说清楚**:带 `gen` 标记的公式行在编辑器里显式标"自动镜像",并提示改动会在源公式下次编辑时被覆盖 —— 想保留自己的指法请新建一条(新条无 `mid`,不参与同步)。
- **自镜像 case**:落回同 case 的对应 view;字面重合则去重跳过 —— 正是 issue 说的"同一个情况下自动添加多条公式"。
- **范围**:主库 `alg_cases` **与** 社区提交区 `alg_submissions` 都镜像(2026-07-26 拍板)。
- **提交区的镜像条必须带溯源**(用户要求):`gen` + `mid` 之外再存 `srcId`(源提交条 id),**作者仍是原提交人**(不是"系统"),UI 上显式写"自动镜像自 〈case〉第 N 条"并可点回源条。
- **镜像条不计入贡献计数**:提交量 / 排行 / "我贡献了 N 条" 一律只数无 `gen` 标记的原创条,否则提交一条自动变四条,计数虚高四倍。

### 5.5 排序语义

- case 内拖拽重排(现有 `handleAlgDragEnd`)→ server 同步重排 **c* 中同 `mid` 的 M-份**(左右镜像顺序永远一致);**S/y² 份的顺序不动**(issue:前后镜像好不好做与原 case 无关,排序独立)。
- 实现:PUT 检测"仅顺序变化" → 传播;或独立 reorder-algs 端点(实现期择一,倾向后者,语义干净)。

### 5.6 UIUX —— 定 U1(2026-07-26 拍板)

case 详情页(`AlgCaseView`)加"镜像"区:c* 缩略卡 + 链接;每条公式行尾一个镜像图标,点击行内展开三条镜像公式对照(M/S/y²,各自标注落在哪个 view);列表页(`AlgCategoryView`)full 模式下公式行同样带展开。移动端天然适配(行内折叠),桌面/窄屏同一套布局。

行内展开的形状:

```
镜像 case:  [缩略图] A−  →

公式
  1. U R U' R'                    ⧉
  2. R' F R F'                    ⧉  ← 点开
     ├ 左右镜  A− FL-view  F' L' F L
     ├ 前后镜  A− BR-view  B' R' B R
     └ y²      A+ BL-view  L' B L B'
```

已否决的 U2(详情页并排双栏 c | c*,同 `mid` 条互相高亮):桌面效果好,但窄屏必须退化成 U1 的折叠,等于两套布局。

### 5.7 适用 set 清单(实现前逐一核对槽位)

f2l、adv-f2l、zbls、wv、cls、vls、sbls、sv(候选,凡"有且仅有一个 F2L 槽");zbll/oll/pll/coll/1lll 等纯 LL 集不适用。LSLL 走 5.3 的前端方案。

---

## T6 LSLL Rust 求解器续建 + 镜像减半

**直接回答 issue 的问题:对,能减半。** σ(镜像对合)保 LSLL 态空间、保步数(镜像解 = 逐字母重写,长度不变),在二类商上 well-defined(σUσ = U′,正规化 AUF 群)。求解量 = (583,284 + F)/2,F = 自镜像 case 数(枚举可得,预计 10²–10⁴ 量级,T1 的枚举脚本顺带产出)。三类模式下同理再叠加。

**F 已经算出来了(2026-07-26):F = 432**,求解清单 = (583,284 + 432) / 2 = **291,858**,省 49.96%。
全量枚举见 `scripts/lsll-mirror-census.mts`;σ 的实现与回归见 §5.3a。

**但要说清楚**:PoC 已实证轻量 4-PDB 启发式在 20M 节点内 50/50 全超时 —— 减半是常数因子,救不了单 case 分钟级的核心瓶颈。路线:大表(EDGE6 ≈ 4,258 万态级别的 PDB)+ σ 对称约减(表本身也能省一半)+ batch 流水。工期与磁盘/内存预算在大表 PoC 之后另报(遵守 ≤14 线程)。

---

## 7 实施顺序(每阶段独立可验,依赖前后衔接)

1. **P1** T1 数学页 + N₃ 枚举脚本(产出:精确 N₃、代表元映射、σ 不动点数 F)—— 其它任务的数据地基。
2. **P2** T4 ZBLS/WV FR 归一 + 多向(T5 的前提:view 结构就位)。
3. **P3** T5 镜像系统(schema → server 同步 → UI)。
4. **P4** T2 lsll toggle + T3 视图开关铺开(吃 P1 产物,彼此独立可并行)。
5. **P5** T6 求解器大表路线(长线,独立推进)。

DB 迁移一条(`mirror_case_id`);zbls/wv 数据批量改走 admin API,可回滚(改前 dump 备份)。push 均先请示(push=上线)。

---

## 8 待拍板问题

### 已拍板

1. ~~**N₃ 定义**~~ —— 作废。不需要"钦定 Z_φ",三类 = 306 × 494 = **151,164**,与公式表无关(§1.3c)。
2. ~~**精确值时机**~~ —— 已出:151,164,T1/T2 均已落地。
3. ~~**镜像家族结构**~~ —— 已实证 41/41(§5.0),伙伴只有一个 case,设计按此。
4. **编辑/删除同步** —— **一律覆盖**(2026-07-26)。不做脱钩/分叉;UI 上标明自动镜像条会被覆盖,要留自己的指法就新建一条(§5.4)。
5. **F 族边界** —— **只有 `F`/`f` 两族算**(2026-07-26)。`S`/`z` 前后镜下不产生 B,不算(§5.2 已改)。

6. **社区提交** —— **也镜像**(2026-07-26),但镜像条必须带溯源(源条 id + 原作者署名 + "自动镜像自 …" 文案),且不计入贡献计数(§5.4)。
7. **UI** —— **U1**(行内折叠,§5.6)。

### 待拍板

8. **T3 豁免清单**(属 T3 不属 T5):盘点后我列表格,默认"非 case 列表页豁免",可以吗?
