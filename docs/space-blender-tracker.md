# Space Blender 迁移跟踪

## 2026-09-10 当前总览与持续迭代清单

用户要求持续迭代，以逐栋 1:1 和精美的网页画面为目标。以下为当前执行入口；后文按日期保留历史记录，其中“当前”“本批”的数量仅指当时快照。25 份 Blender 工程及导出流程已建立，上海 GLB 为 310,655,332 字节；迁移完成不代表建筑复刻完成。外滩 24 栋均有独立模型及材质处理，深度增量集中在亚细亚、上海总会、汇丰、江海关、和平饭店，尚无整栋通过测绘 1:1 验收。

### 完整待办与顺序

| 顺序 | 工作 | 具体剩余项与完成证据 |
| --- | --- | --- |
| 1 | 网页日夜基线与江海关收口 | 已修正檐口接缝与玻璃穿插；三栋共 18 盏可编辑 Blender 灯具已导入网页，六张日夜近景已复查；邻栋灯组改为零强度换位、同步渐变，网页暂停天气也能走完交接。和平饭店铜顶经四轮校正形成下沿局部绿光、上部暗区及暖色冠光，正式日夜及缩放交接已检查。继续修底层偏暗、暖色檐线、冠部比例及近远配光不一致，再扩展正面、斜侧、整栋及沿江对照，不以临时 Blender 灯光替代网页结果 |
| 2 | 参考与尺寸档案 | 逐栋补正面、左右侧、背面、屋顶及夜间实拍，优先建筑师、业主、承建方和可核实的竣工资料；记录来源、拍摄时间或未知、适用立面、已知尺寸与估算项；统一复刻时期，避免混用不同年代的招牌、改造和配光 |
| 3 | 外滩重点五栋 | 江海关完成屋顶连接、钟面刻度、剩余格栅及侧后立面；和平饭店校准铜顶、窗距、浮雕、侧翼和招牌；汇丰继续完整窗轴、穹顶、雕像、柱头细雕及银行招牌；上海总会补准确花饰、屋面和侧窗；亚细亚补转角、柱式及侧后立面；五栋均需材料与真实夜景对照 |
| 4 | 外滩其余十九栋 | 按[已有逐栋表](../design/space/references/shanghai-landmarks.md#外滩逐栋对照登记)逐栋完成体量、楼层、窗轴、入口、屋面、线脚、雕饰、砖石颜色、招牌与灯具；包括有利、日清、通商银行、大北电报、招商局、交通银行、公共服务中心、华俄道胜银行、台湾银行、字林西报、麦加利银行、汇中饭店、中国银行、横滨正金银行、扬子水火保险、怡和洋行、格林邮船、东方汇理银行、英国领事馆；不以共用窗格或程序石材视为整栋完成 |
| 5 | 三件套与陆家嘴全部建筑 | 金茂校准平面宽度、真实层高、逐段退台与塔冠；环球校准收分、顶部开口及内衬；上海中心校准扭转、双层幕墙、内冠与分区。随后按[城市复刻清单](shanghai-replica-tracker.md)深化东方明珠、汤臣一品及其他楼栋；补完整建筑清册、裙房和缺失建筑，不能只做地标后宣称陆家嘴全部完成 |
| 6 | 桥梁与完整双岸 | 复核南浦主桥和螺旋引桥、卢浦、外白渡及目标范围内其他跨河桥梁，补桥塔、索、桁架、桥墩、桥下结构、栏杆、灯具和铭牌；按[双岸范围](shanghai-huangpu-space-tracker.md)补北外滩至前滩、徐汇滨江及徐家汇方向街区、岸墙、台阶、码头、道路、植被和地面衔接；精确目的地仍沿用原跟踪中的待确认项，不自行把相似地名标为已确认 |
| 7 | 材质、招牌与夜景 | 逐栋校准石材、砖、铜、不锈钢和玻璃，补有实际尺度的颜色、粗糙度和法线；招牌核对文字、字形、尺寸、位置、底板、支架与亮灯状态。网页按实拍校正窗光分区、外墙投光、冠部、阴影、曝光和江面倒影；模型已有灯具外壳不代表灯位与配光已完成 |
| 8 | 时间、天气、水面与交互复验 | 保留 00:00–23:59 任意分钟、天气、彩虹和水色选择；复核午夜与昼夜交界、晴雨和极端天气组合，处理全城屋顶遮雨、岸边接触、船尾流、桥下水面以及海岛近岸泡沫、风暴白沫与飞溅。每次更新维持魔方真实转层、摆放与存档，无人机 WASD 平移、R/F 升降、方向键原地镜头转动、自定义速度、滚轮与滑块缩放 |
| 9 | 住宅、公司与海岛 | 上海优先后继续八种风格的建筑内外：客厅、书房、卧室、卫生间及已有庭院、车库、影音室、健身房；逐空间深化家具、五金、织物、镜面和灯光，公司逐张对照用户实拍，尺寸缺失保留估算；检查全部 25 份场景及组合，不遗漏非上海场景 |
| 10 | 网页性能与资产交付 | 将约 311 MB 上海资源按区域拆分并设置远近细节层级，复用实例、压缩模型与纹理；本批修正首次绘制前的着色器编译顺序与重复程序变体，但大模型加载、解析仍需优化。在画面无明显劣化后测加载、内存、帧时间、连续切区释放、低空近景和真实手机。LFS 配置及重资产上传仍暂缓；恢复时完成规则、容量、指针与干净检出验证，另验网页资源发布路径；默认本地提交，不 push |

### 每栋验收与每轮闭环

- 状态分别记录“资料待补、几何待修、材质待修、招牌待修、日景待验、夜景待验、网页待验”，不以一个整体百分比掩盖未知项；沿用已有逐栋档案，不再维护另一份建筑名单。
- 有来源的高度、宽度、层数、窗轴与屋顶关系逐项对照，记录来源值、模型值、误差和资料精度。照片推算、不可见立面及未知年代必须标记，未消除的估算不能勾选为测绘 1:1；后续争取图纸、测量或可用实景重建数据。
- 每轮先写明确缺陷，再保存同机位基线，增量编辑当前 `.blend`，渲染正面、斜侧、近景与日夜候选；发现问题继续修，不因达到三轮次数就自动结束。
- 保存前保留前版和用户修改，导出后核对清单、尺寸与运行绑定；以真实网页截图复验接缝、穿插、闪烁、缺面、文字可读性、色彩与灯光，再更新档案及本地提交，保留其他 AI WIP。
- 结构测试通过只证明导出与交互合同，没有证明实景准确度；Blender 临时检查灯光不能代替网页最终画面。逐栋资料和多视角视觉均通过后，才纳入整条岸线及整片城市的最终验收。
- 当前尚缺完整可靠尺寸、部分立面与现场照明依据，因此继续以 1:1 为目标，不承诺仅凭现有照片就能证明每个细节完全一致；资料不足先自主补查并推进不受影响部分，具体无法获得的内容留在对应建筑档案。

## 2026-09-10 和平饭店铜顶夜景配光

按 Fairmont 业主夜景与 Suicasmo 2025 年整楼夜景校正：铜顶应有下沿局部绿光、上部深绿与暖色小冠。网页此前跳过没有旧 `crown` 参数的作者铜顶，导致近景灯已经启用，屋顶仍保留整面远景补光；已修正建筑灯光 ID 登记条件。四轮候选先纠正绿光打到石墙，再收束坡面亮斑、抬升冠光，并降低上部远景补光。照片日期、许可、估算边界和候选取舍见[夜景参考档案](../design/space/references/peace-crown.md#夜间铜顶配光2026-09-10)。

- 增量脚本 `refine_peace_night_lighting.py` 编辑和平饭店六盏既有原生灯和两份铜材质，不改几何或增加灯池。四盏绿色坡面灯、两盏暖冠灯均为视觉代理，未取得真实灯位与配光。材质 `spaceRoofWash` 使用局部 Y-up 高度和线性 RGB，旧材质未设置时保持原公式；近景仍使用已有渐变权重。
- 已先核对候选及源文件大小、纳秒修改时间，再备份保存。该指纹不是源文件内容哈希；保存前备份 SHA-256 为 `17e0b69efe324e7740ab85e01f9c3b880c3716fbc271f533c582a1913c677904`。源工程 **122,839,524 字节**，新增修订 `peace-night-roof-20260910`；保留旧颜色修订、全部运行对象 ID、其他对象变换与九张内嵌图。已有修订时拒绝重复执行。
- 正常 `batch.ps1 -Asset shanghai` 导出成功，GLB **310,655,332 字节**，SHA-256 **`5f6e9c0eccd806e3ed445c71e1f2ba563eb4c5b4028795a9491538a6136bca87`**，仅增 **228 字节**。BIN 与九张图 URI 完全一致；JSON 79 处变化仅涉及六灯、两份铜材质的补光参数和修订标记，非预期变化为空。schema 2、11,877 个对象、三组作者灯与九张纹理保持。
- 正式 GLB 浏览器全套合同检查 `ok=true`、`errors=[]`，551 网格、204 着色材质、95 组建筑属性、时钟、1,200 交通实例、12 艘船及已有细部首命中检查通过。client `typecheck` 与 `space_shanghai.test.ts` **46 项测试通过**；新增覆盖屋顶登记、实际标准 shader 注入、缺省行为、缓存复用和非法材质参数拒绝。独立只读代码与实拍审图未发现本轮阻断问题。
- 真实 `/zh/space` 确认加载最终哈希、没有候选注入，着色器编译错误为零。已逐张打开查看 [21:00 近景](../.tmp/png/space-night-20260910/peace-final-close-night.png)、[21:00 整栋](../.tmp/png/space-night-20260910/peace-final-preset-night.png)、[12:00 整栋](../.tmp/png/space-night-20260910/peace-final-preset-day.png)和[21:00 远景](../.tmp/png/space-night-20260910/peace-final-far-night.png)。日间六灯全零，夜间下沿局部绿光、暗色上部和暖冠已生效。
- 固定观察方向，镜头距离按 180、240、300、360、240、140 米移动，采集 **15 次实际灯光更新**：三次建筑灯组交接均在六灯强度归零时换位置，其余帧灯位固定，终点过渡均结束。无头浏览器约一秒一帧，未据此认定连续缩放流畅；近远配光差异仍保留为待办。
- 两条新实拍来源已加入中英文共享 JSON；实际中文网页展开来源并核对作者、日期及用途，在 390 像素视口确认无横向溢出，截图 `sources-zh-390-expanded.png`。未把照片制成运行贴图，本轮没有新增第三方代码。

备份、候选、最终审图及 `saved-report.json`、`export-comparison.json`、`final-runtime.json` 均在 `.tmp/png/space-night-20260910/`。Python AST、JSON 解析、LF 和差异检查通过；后台最多 14 线程，未占用桌面。源工程、GLB、贴图与临时图保留本地，LFS 配置及上传继续暂缓，本轮仅本地提交代码、脚本、清单与文档，不 push。

**仍未达标：**近景檐边略泛绿，实拍的暖色卷草及退台檐线更突出；小冠比例仍待标定，远景冠光缩成小亮点。其他楼栋底层和冠部、侧后立面、招牌、整条岸线及连续夜游继续待验。本轮只修正和平饭店屋顶的照明层次，未宣称整栋或全城 1:1、电影级完成。

## 2026-09-10 铜顶与三栋石材颜色编码修正

定位到程序生成器将线性 RGB 直接写入非浮点 sRGB 图像，网页加载 PNG 时再次解码，导致和平饭店铜顶及三栋石材额外偏暗。通过 Blender 5.2.1 的 2×2 实验确认：写入 `0.21404114` 得到 PNG 字节 55，正确编码为 `0.5` 后得到 128。增量脚本 `refine_bund_color_encoding.py` 只转换四张既有颜色图，同步修正原生成函数；法线等数据图不转换。原因、公式、贴图均值及技术来源见[铜顶参考档案](../design/space/references/peace-crown.md#铜顶与三栋石材的颜色编码修正)。

- 先生成候选，在真实网页以固定机位对照和平饭店 12:00 / 21:00 的修改前与候选，再核对源文件指纹、备份并保存。正式源工程为 **122,840,418 字节**，修订 `bund-albedo-srgb-20260910`；重复应用会拒绝。修改前工程和 GLB 各有独立备份，未操作用户桌面窗口。
- 正常 `batch.ps1 -Asset shanghai` 导出成功。GLB 为 **310,655,104 字节**，SHA-256 **`94673644091fd5de399eec23b9624961ea76dfeb84a997e4d1e078eed8525d72`**；清单仍为 schema 2、11,877 个对象、9 张纹理、3 组原生灯。四张最终 PNG 与候选逐像素一致，其余五张图未变；GLB 的 BIN 哈希一致，JSON 仅四张图 URI 和场景修订标记变化，几何、材质参数、灯具及变换保持。
- 最终 GLB 的完整浏览器合同检查 `ok=true`、`errors=[]`：551 网格、204 着色材质、95 组建筑属性、1 个时钟、1,200 交通实例、12 艘船，以及三栋既有细部首命中检查通过。结构检查与像素核对分别记录在 `final-glb-verification.json` 和 `export-comparison.json`。
- 正式导出后重新加载真实 `/zh/space`，确认资源 URL 带最终哈希且没有候选注入；和平饭店、江海关、汇丰在 **12:00 和 21:00 共六张**最终截图均逐张打开复查。和平饭店日间铜顶恢复绿色，夜间可见浅绿色；汇丰和江海关的石材纹理及既有几何可见。证据为 `peace-final-day/night.png`、`customs-final-day/night.png`、`hsbc-final-day/night.png`，位于 `.tmp/png/space-parallel-20260910/color-encoding/`。
- 参考照片 01、03、05 的 EXIF 标记为 **2008-11-14**，时区未知且相机时钟未经独立校验；更正原“拍摄日期未知”，明确文件名 1929 是建筑年代。中英文共享来源 JSON 与档案同步，实际网页“本页来源与建模说明”已展开核对拍摄日期、用途和估算边界，记录 `final-web-runtime.json`。

独立只读审查未发现实质问题；Python AST、来源与清单 JSON 解析、LF 和差异检查通过。源工程、GLB、贴图与临时图仍保留本地，LFS 配置及上传暂缓，仅提交本轮脚本、清单和文档，不 push。

**仍未达标：**夜间和平饭店铜顶偏亮，江海关基座局部亮斑生硬，汇丰底层偏暗且整栋配光不连续；窗格比例、雕像、花饰、招牌及侧后立面仍有待校准项。程序纹理的实际反照率和灯具光度未经现场标定，旧照片不能证明当前色彩与照明，本轮没有将三栋或整条外滩标为 1:1 / 电影级完成。

## 2026-09-10 汇丰柱廊按实拍纠错

重新对照 ScareCriterion12 于 2021-06-20 拍摄的[汇丰大楼正立面](https://commons.wikimedia.org/wiki/File:上海外滩汇丰银行大楼2021.jpg)，确认六柱不是等距排列，而是内侧两组近距成对柱，五个间距依次为宽、窄、宽、窄、宽。此前把它改为等距的判断已在[参考档案](../design/space/references/shanghai-landmarks.md#第九批汇丰六柱间距纠错)明确纠正。新局部柱轴为 `[-12.7, -6.0, -3.4, 3.4, 6.0, 12.7]` 米，保留原外柱跨度；照片未经正射校正，米制数值仍为估算。1920 年拟建正立面未当作竣工测量依据。

- 增量脚本 `refine_hsbc_columns.py` 只移动四根内柱及对应柱脚、柱头，共 41,696 个顶点；两根外柱及 30 组窗饰的 4,320 个顶点保持。保存前核对源文件指纹、顶点分组、面所属关系、UV、拓扑、材质槽、既有世界变换及 11,877 个运行对象 ID；14 处穹顶首命中检查通过。未重跑整栋生成器覆盖后续雕刻。
- 两轮候选纠错后审阅 Blender 日景，保存正式 `design/space/scenes/shanghai.blend`，再从保存文件复查夜景。源文件现为 **123,007,833 字节**；保存前备份 `shanghai-before-paired-columns.blend` 的 SHA-256 为 `010b9ab9d9caf12bb164c70d49d0ea7dce02e674ebe7705d403f3afc866c94e3`。报告 `columns-saved-report.json` 记录 `saved=true`、变更数量与保留检查。
- 正常 `batch.ps1 -Asset shanghai` 导出成功，GLB 为 **310,655,044 字节**，SHA-256 **`d35b14c725c228bca3b5533a42ac1fa1b1fb73e3b87a228a885ed3365331f6f9`**；清单仍为 schema 2、11,877 个对象、9 张纹理、3 组原生灯。实际网页确认 `source=blender`、新柱轴和修订标记已加载；18 盏作者灯仍由 6 盏实时阴影灯复用，不把它记成 18 盏同时启用。
- 最终 GLB 浏览器全套合同检查 `ok=true`：551 网格、204 着色材质、95 组建筑属性、1 个时钟、1,200 交通实例和 12 艘船保持。重点细部检查为 **83 处首命中**，新增六柱柱脚及柱头共 12 处，旧柱槽检查跟随新柱轴；穹顶 14 处、江海关及和平饭店既有表面检查也通过。证据 `columns-glb-verification.json`。
- 实际 `/zh/space` 的 **12:00 日景与 21:00 夜景**，近景和整栋共四图均已逐张打开查看：[日间近景](../.tmp/png/space-parallel-20260910/hsbc/web-columns-close-after-day.png)、[夜间近景](../.tmp/png/space-parallel-20260910/hsbc/web-columns-close-after-night.png)、[日间整栋](../.tmp/png/space-parallel-20260910/hsbc/web-columns-wide-after-day.png)、[夜间整栋](../.tmp/png/space-parallel-20260910/hsbc/web-columns-wide-after-night.png)。近景与修改前保持同一相机，成对柱距可见，未发现新增脱落构件。真实网页“本页来源与建模说明”已展开核对新照片的作者、日期、许可、用途及估算边界；中英文来源共用 JSON，运行记录 `columns-web-runtime.json`。

上述证据与候选均在 `.tmp/png/space-parallel-20260910/hsbc/`。本轮 client `typecheck`、Python AST、来源与清单 JSON 解析、差异检查通过；真实导出和浏览器检查没有用纯数值测试替代审图。源工程、GLB 和临时图继续留本地，LFS 配置与重资产上传暂缓，仅本地提交代码、清单和文档，不 push。

**仍未达标：**三组高窗的比例与完整窗轴需按实拍校准；石材日间偏灰，线脚偏白；夜间局部窗光过亮、底层偏暗、整栋配光不连续。雕像、柱头精确花饰、银行招牌字形与侧后立面继续待做。本轮只纠正已确认的柱列关系，不能据此宣称整栋 1:1 或电影级通过。

## 2026-09-10 单栋照明烘焙研究（独栋已审图，近景未验收）

针对近景聚光灯和远景程序补光配光不同，新增 `probe_facade_bake.py`：复制当前源工程的一栋建筑及原生灯具，保留材质 UV，新增灯光 UV，尝试输出 Cycles 漫反射 EXR、双 UV GLB 和独立候选工程。所有输出只进入临时目录，未改正式 `.blend`、GLB、运行材质或生产资源。

初轮 Python AST 语法检查通过，但 Blender 长时间停在读取城市工程，未执行到样本输出，当时烘焙、UV 接缝、强度换算及多视角视觉均未通过验收。同时测得系统 CPU 满载和约 1.7–2.6 GB 可用内存，其他浏览器与编译进程保持。浏览器 WebGL2 确认为 RTX 3080 Laptop GPU 硬件加速；整页重新加载期间取得的统计仍是加载阶段，不能用来判定城市帧率瓶颈。以下按先后保留失败与恢复记录，后续样本现已实际完成。

初次停滞后已释放本任务两个无头页面的城市场景，并在核对命令行后停止该 Blender 试验进程；原始日志保留。当时尚未取得可宣称改善的画面，后续转向微小校准及独栋实验。复现步骤、来源与明确限制见[烘焙研究记录](../design/space/references/facade-baking.md)。

第二次复核使用不读取任何工程的空白 Blender，仅计划烘焙 64×64 单平面（2 线程、8 samples）。09:54 至 10:00 仍未进入 Python，累计 CPU 2.27 秒；已停止这个经命令行确认的任务进程，保留 `calibration-01-process.json` 和空日志。系统 CPU 100%，可用内存约 5.5 GB，不能将此前停滞归因为城市文件过大；主线程 Ready、短时普通优先级仍未完成启动，仅提供调度受限线索。该次没有新增烘焙或视觉通过项，因此当时继续以微小样本验证启动，没有反复启动大场景。

后续微小样本已恢复执行：`calibration-04` 实际 Cycles 烘焙像素均为有限值 1，材质 UV 哈希保持，GLB 仅有一个平面和两组不同坐标。真实无头 Three.js 加载 EXR 与 GLB 后，Lambert 线性输出在 `lightMapIntensity = π` 时为 `0.5`，强度为 1 时为 `0.1591549367`；UV1 红蓝测试也通过。修复了导出跨场景带入默认立方体的问题，两个导出分支限定当前场景。此项只完成漫反射单位与坐标链路，不代表建筑 PBR、金属反射或城市夜景达标。

已实际读取汇丰源工程的 18 个网格、81,863 个面。单栋试跑发现编辑模式后旧 UV 引用失效、默认 World 节点名称本地化两项兼容问题，已按重新获取 UV 层与节点类型修正。随后修复导入面角不连接导致的 UV 密度不足：仅在临时副本焊接展开，按 292,791 个原始面角 ID 回传 UV，保留原几何和材质 UV。2K 图集三角面积总和由初版约 0.025% 提高到 28.95%，此值不代表去重覆盖率。

`calibration-05` 通过四象限 EXR 和实际 GLB 验证，纠正前轮未被常量贴图检出的上下方向：当前 EXRLoader 链路使用 `flipY = true`，设为 false 的负对照不匹配。`hsbc-05 / 06` 完成 2K、16 / 128 samples 对照，后者约 149.53 秒；真实浏览器正面、斜侧和近景已查看，灯光位置对齐原生灯光，噪声减少，但柱槽仍有黑线、檐口阴影边缘仍块状，近景未通过。

`hsbc-07` 完成 4K、128 samples 烘焙，耗时 700.89 秒，EXR 约 17.0 MB（2K 约 5.4 MB）。三个同机位再次审图，柱身长黑线明显减少，小于一像素的面占实际表面积由 1.40% 降至 0.344%；柱头细缝、暗部噪点和部分阴影边缘仍需修正。原网格几何/三角化哈希、材质 UV 及源文件检查通过，18 组实际 GLB 图元逐角属性保持；证据在 `hsbc-07/report.json`、`geometry-comparison.json`、`runtime.json` 和三张网页检查图。下一步定位剩余接缝、复核原生渲染及反射，再处理正式城市接入；诊断曝光下的独栋图不代表最终夜景，近景和整体 1:1 仍未通过。

后续柱头局部 1K / 2K、EXTEND 扩边、显式法线 UV、关闭法线贴图及关闭阴影终止器几何偏移等对照均未消除细黑线。Blender 内直接回放烘焙 EXR 也能复现，原生无降噪对照更干净；2K 图集像素中心采样未检出内部 UV 重叠，但不等于连续无重叠证明。随后仅关闭柱头与窗雕对象投影，细黑纹基本消失、正常叶饰阴影也丢失，表明问题依赖本对象遮挡；后续继续定位薄片交叠、表面朝向和烘焙取样，不能取消阴影作为修复。局部按材质拆分另检出 4 组小幅法线变化，严格导出属性比较未通过，未用于正式模型。研究脚本已补面角法线哈希，实际 Blender 展开保持检查及“只改法线”的负对照均通过。具体参数、审图和边界见[柱头排查记录](../design/space/references/facade-baking.md#柱头局部图集与黑纹排查)。

上述烘焙研究批次未改写正式上海 `.blend`、GLB 与网页材质；源文件修改时间、大小及材质 UV 检查通过。样本和审图在 `.tmp/png/space-light-bake/`，技术来源及详细证据见[烘焙研究记录](../design/space/references/facade-baking.md)。烘焙批次仅本地提交脚本与文档，不 push，不上传重资产；后续柱廊几何保存另记于上节，整体 1:1 与电影级仍未验收。

## 2026-09-10 相邻建筑灯组交接

此前 `ShanghaiFacadeLighting.update` 在选中建筑变化时直接移动六盏灯，并同时更换立面补光的建筑 ID，导致转向邻栋时光照跳变。现保留六盏阴影灯的预算，旧组减弱并恢复原立面补光，强度归零后才换位置，新组随后渐亮；反向转回可从当前权重继续恢复。距离过渡也使用同一权重，静态阴影只在换位和夜间启用时刷新。

- 暂停动态天气时，原绘制调度不会持续推进灯光渐变。现由 `lightingTransitioning` 延续必要绘制，完成后停下；使用相机更新的帧时间，独立于天气与交通动画。单程 0.3 秒，单帧时间仍限制在 0.1 秒，避免长时间切出后瞬间完成。
- client `typecheck` 和两个 Space 文件 **71 项测试通过**。新增验证覆盖零强度换位、渐变期间静态阴影、反向中断、离开再进入、非法帧间隔及暂停天气下 30/60/120 Hz 的时间一致性；既有本地灯组变换与昼夜测试保持。
- 在实际 `/space` 新实例里，暂停天气、21:00、固定无人机位置转向招商局大楼：汇丰权重 `1 → 2/3 → 1/3`，交接帧新楼权重 `0` 且六盏强度全零，随后新楼 `1/3 → 2/3 → 1`。仅交接帧报告阴影更新；灯位在各自渐变段保持固定，结束后 `transitioning=false`。证据 `handoff-runtime.json`。
- `handoff-after-hsbc.png` 和 `handoff-after-neighbour.png` 已逐张打开检查。未改 `.blend`、GLB、纹理或灯位元数据；沿用前批照片来源，本批无新增外部素材。证据目录仍为 `.tmp/png/space-parallel-20260910/`。

限制：本轮无头浏览器记录约 1 秒一帧，六帧交接约 5 秒，不能声称流畅播放验收；需要另做帧时间分析。近景与远景配光仍不一致，截图中汇丰切回远景补光后更亮，渐变只消除了瞬间跳变；后续需校准统一照明或由 Blender 烘焙，而非继续依赖看向哪栋决定照明。交接零强度帧的额外冻结截图遇到其他并行改动引发页面热重载，未取得截图，不以数值证据冒充中间帧审图。独立审查 Agent 本轮额度不可用，未记为已审。整体 1:1、电影级及连续夜游仍未通过。

## 2026-09-10 三栋原生 Blender 灯具与网页配光

在上一批几何基础上，以实际网页逐轮调整方向、束角、色温与强度，再把候选保存为 Blender 可编辑灯具。和平饭店铜顶的两个可见面现均有照明；江海关钟楼基座的白色强光斑减弱；汇丰穹顶由灰白改为较暖的金色。三栋底层、冠部和邻栋之间的亮度仍不完全连续，本批不标为 1:1 或电影级通过。

- `shanghai.blend` 新增 18 盏原生 SPOT 灯，每栋 6 盏；保留 11,964 个原对象的变换、稳定 ID、选择与相机状态，以及 10 处视口状态。源文件从 122,328,032 增至 122,338,183 字节；保存前的副本为 `shanghai-before-native-light-rigs.blend`，报告 `native-light-rigs-report.json`。
- 灯具位置、旋转、颜色、束角、边缘柔化和距离可直接在 Blender 编辑，网页强度使用 `spaceCandela`。正常导出由 `facade_rig.py` 转换到建筑局部 Y-up 元数据；不向源工程写回派生数据。作者脚本 `author_bund_lighting.py` 已有修订时拒绝覆盖，后续编辑现有灯具。操作见[编辑说明](../design/space/README.md#外滩近景灯光编辑)。灯组是依据既有实拍档案调整的合并视觉代理，不是现场灯位测量；Blender Power 也未做光度标定。
- 网页仍复用 6 盏带阴影灯，并应用当前近景建筑的原生灯组。正常导出成功，清单为 3 组灯、11,877 个导出对象、9 张纹理，GLB 为 **310,654,952 字节**，SHA-256 **`32fcacab9e6c25fb5b3f3c41a43568ccbe19eb677b9b4e2d6aa4bb504039ac75`**；本批只增加 4,968 字节元数据，几何保持。
- 验证通过：client `typecheck`，两个 Space 测试文件 **68 项**，打包器 **2 项**；真实 Blender 检查 **18 盏方向回环**、艺术编辑后的位置导出与非法功率拒绝。最终实际 GLB 浏览器检查 `ok=true`、错误为空，既有 387 处表面检查与城市运行绑定保留。
- 最终在网页逐张打开三栋的 12:00 日景和 21:00 夜景共六张图：`web-peace-light-rigs-final-day.png`、`web-peace-light-rigs-final-night.png`，以及对应 `customs`、`hsbc` 两组。日间所有近景灯强度归零；暂停动态天气后从白天切夜间仍正确开灯；相机小幅环绕且保持同一栋时，灯具世界坐标不变。运行证据为 `web-light-rigs-runtime.json`。

证据与备份均在 `.tmp/png/space-parallel-20260910/`，原生验证日志为 `verify-native-light-rigs.log`。未操作桌面，也未上传重资产。既有照片依据沿用[和平饭店](../design/space/references/peace-crown.md)、[江海关](../design/space/references/customs-junctions.md)与[汇丰](../design/space/references/hsbc-drum.md)档案，本批没有新增外部素材。

明确剩余问题：和平饭店日间铜面仍偏暗；江海关底层与钟楼上部偏暗；汇丰鼓座窗间亮度仍不均。较大角度环绕时，近景灯池会切到相邻建筑，汇丰穹顶随之回到远景补光，存在可见切换；`web-hsbc-light-rigs-final-oblique.png` 已记录，不以同栋小幅环绕通过掩盖它。下一轮先处理选光过渡与上述局部明暗，再验整栋、低空和沿江连贯夜景；真实灯位、测绘尺寸、侧后立面及其他楼栋继续按上方清单推进。

## 2026-09-10 三栋细部合并与网页首帧修复

本批先按实拍分别制作候选并审图，再由唯一协调流程串行保存上海源工程、导出并复查真实网页；没有占用桌面输入。其他 AI 的模拟器、桌宠及共享控件 WIP 保留。独占证据目录为 `.tmp/png/space-parallel-20260910/`，大文件与渲染仍留本地，不配置 LFS、不上传、不 push。

### 建筑迭代结果

| 建筑 | 多轮纠错与实际产物 | 网页复查和未完成项 |
| --- | --- | --- |
| [和平饭店](../design/space/references/peace-crown.md) | 铜顶四面真正开洞，补 40 条百叶、156 道屋脊横缝和 80 个檐口弧纹；第一轮突出的短杆横缝改为第三轮薄接环，新增 3 网格 | `web-peace-day-new.png`、`web-peace-night-new.png` 已打开查看；既有招牌与立面保留，但铜面日间偏暗、夜间冠部照明不足；背面、旗帜、精确浮雕与实测尺寸仍缺 |
| [江海关](../design/space/references/customs-junctions.md) | 六层檐口回折到真实旧几何后界，裁掉 640 面旧收口；复制本楼玻璃材质去金属化、减色偏，补五个可见投光器。首次 GLB 检查又发现旧玻璃穿出两条檐口，第二轮精确裁掉 20 面后重导出，新增 2 网格 | `web-customs-day-final.png`、`web-customs-night-final.png` 已打开查看；接缝与穿插修正，但基座硬明暗分界及底层偏暗仍在。临时 Cycles 灯位 A/B 只证明检查舞台的遮挡，不是最终现场配光 |
| [汇丰大楼](../design/space/references/hsbc-drum.md) | 重做双层八角鼓座、双圆柱门廊、平头窗亭、石栏与平滑穹顶，去掉错误的明显竖肋；实际首命中定位旧圆顶遮窗后原位替换 mesh data，第二轮又修复柱身重叠黑纹，新增 5 网格 | `web-hsbc-day-new.png`、`web-hsbc-night-new.png` 已打开查看；新鼓座和曲面可见，夜间穹顶仍偏灰白、上部配光不均，未达到参考照片的金色层次；雕像、精确柱头及尖顶仍待修 |

六张日夜网页图均为实际 WebGL 渲染，不能替代仍未覆盖的全楼、多角度和实测尺寸验收。两栋截图在最终江海关玻璃局部修正前取得；该后续修正仅作用于江海关玻璃，江海关最终两图在最终哈希上重新取得。新增资料已接入网页统一来源：Asisbiz 三栋照片，以及 Hermann Luyken 2014-11-15 19:21:01 的 CC0 外部穹顶夜景；拍摄日期未知和页面更新时间分开记录，照片未用作运行贴图。中英文来源 DOM 均核对，英文实际 390×844 视口打开 60 条来源并检查新条目，来源宽度 343px，无页面横向溢出，截图 `sources-en-390.png`；此检查不等于真实手机 GPU 性能验收。

### 源工程、导出与结构证据

- 初始源文件为 118,475,772 字节，`mtimeNs=1789039213528978700`；统一保存前备份 `shanghai-before-reviewed-three.blend`，SHA-256 为 `7c53fe1d46125da3ca0080cae162c253cf98ef301fe5d842686838f9ad4f81ca`。第一次合并为 122,302,246 字节；第二次玻璃修正另备份 `shanghai-before-glass-return.blend`。最终源文件 122,328,032 字节，`mtimeNs=1789047773646592600`。两次都核对磁盘指纹后才保存，没有保存临时审图灯光。
- 保留 619 个非空既有稳定 ID、11,942 个原对象变换与 10 处视口状态；新增 10 个网格，最终 629 个非空稳定 ID。原协调报告把 `None` 算入集合而显示 620/630，已修正报告计数，未重跑作者脚本。场景合同、相机、时钟属性及选择状态保留。
- 最终导出 `shanghai.glb` 为 **310,649,984 字节**，SHA-256 **`62ebfad970e257f4be64527f4f9473c7175981846343bf6424e6f8e08693acb6`**，清单 schema 2、Blender 5.2.1 LTS、11,877 个导出对象、9 张纹理。相较本批前增加 2,888,576 字节。正常 `batch.ps1 -Asset shanghai` 成功结束，清单已同步。
- `verify.ts` 新增和平饭店 320、江海关 53、汇丰 14，共 **387 处实际 GLB 表面首命中检查**，同时包含旧几何，统一 0.025 m 容差；发现遮挡先修模型，不扩大误差门槛。最终 `window.verification.ok=true`，既有水面、时钟、交通、三塔、外滩几何与表面合同也通过：551 网格、204 着色材质、95 组建筑属性、1 个时钟、1,200 交通实例和 12 艘船。结果文件 `final-glb-verification.json`。

### 首次绘制阻塞的根因与复验

网页基线 CPU profile `web-shanghai-baseline.cpuprofile` 记录约 542 秒采样，其中 `getProgramInfoLog` 累计约 382 秒，248 次程序链接，城市加载阶段出现约 175 秒长任务。代码在异步资源准备和预编译结束前就允许首帧进入绘制，同时建筑亮度常量产生重复着色器变体。

运行时现在等待城市、住宅及天气准备并应用材质修改，在实际后处理目标及灯光环境下预编译后才显示当前场景；防止早先切换的异步结果覆盖新场景，并延后编译中资源的释放。建筑亮度改为共享 uniform 程序，兼容既有数值 shader 标记，交通模板识别同步更新。

两次重载均未重现数分钟 GL 查询阻塞。第一次修复后 98 次链接、无大于 100 ms 的 GL 查询，但仍有约 11.27 秒和 5.23 秒准备长任务，约 48.5 秒就绪；最终玻璃修正后的暖缓存复查为 96 次链接、最长任务 1,478 ms、最长 GL 查询 650.5 ms、6 次 GL 查询超过 100 ms、无 JS 错误。最终模型 HTTP 解码体积 310,649,984 字节，传输读取历时约 5.8 秒，记录在 `web-runtime-final.json`。两轮缓存和驱动状态不同，不宣称固定倍数提速或消除所有卡顿；分区、解析和远近细节优化仍待做。

适用验证已通过：client `typecheck`；`cube_space.test.ts`、`space_shanghai.test.ts` 两文件 65 项；独立验证脚本 tsgo 检查；三份 Python 语法检查、打包器两项回归、真实 Blender 合并/玻璃修正/最终导出及 GLB 浏览器验证。没有运行与 dev 共用 `.next` 的生产构建，也没有将结构测试视为实景质量通过。

下一轮从三栋的网页实际灯位、方向、束角与明暗层次开始；沿用来源图与同机位日夜证据，先修冠部和基座，再扩展整栋及沿江夜景。其余十九栋、三件套校准、全城范围和其他 Space 场景继续保留在上方完整清单，尚无整栋取得测绘 1:1 验收。

2026-09-09 状态更新：此前代码已提交并 push；随后在本地精修亚细亚和上海总会。本批完成总会窗组与檐口的三轮迭代，已保存工程并导出，未 push。Blender 源工程和网页重资产仍未上传。用户已确定后续用 Git LFS 管理源工程及必要原始贴图，本次暂缓配置和上传；要求已写入根 `AGENTS.md` 与编辑说明。以下各批文件大小和验收记录保留为历史证据。

2026-09-08：本地迁移与基础验收完成；未 push、未上传资产、未启用生产环境。工作方式已切换为后台 Blender Python 和无头浏览器，不激活用户窗口、不发送鼠标键盘输入。所有批处理隐藏窗口、低优先级、最多 14 线程，预览使用 CPU。

## 2026-09-09 后续任务顺序

- [x] 记录后续 AI 必须使用 Git LFS 管理 `.blend` 与必要原始贴图；本次仅文档更新，不改忽略规则、不上传重资产、不触碰其他 AI 的 WIP。
- [ ] 网页日夜基线：先解决此前无头环境 WebGL2 不可用的问题，取得当前上海工程在网页中的实际白天、夜间近景，分别记录 Blender 预览与网页结果。
- [ ] 外滩逐栋精修：已重做亚细亚正面凹廊与阳台、总会双亭及柱廊后墙；本批补总会五组三联拱窗、五组成对窗和檐口。继续校准实测窗轴、柱头、浮雕、坡屋顶细节和侧后立面，随后重点复核和平饭店、汇丰与江海关，再依逐栋档案完成其余建筑。每栋对照有来源的实拍，保存修改前后同机位日夜图。
- [ ] 三件套精度：补可靠的平面、层高与冠部资料，校准金茂宽度和楼层分段、环球顶部开口及收分、上海中心内冠与幕墙比例；资料不足的尺寸保留估算标记。
- [ ] 夜景材质与灯光：按实拍逐栋校正石材色彩、玻璃反射、招牌、亮灯分区和投光层次，并检查江面倒影；用网页实际渲染复核，不用临时 Cycles 检查灯光代替。
- [ ] 网页资产优化：上海当前 GLB 为 295,993,340 字节；按区域拆分、制作远近细节层级、压缩模型和纹理，验证加载、内存、帧率及小于 480px 的真实视口。
- [ ] 其他场景复核：住宅客厅、书房、卧室、卫生间及公司实拍复刻分别审图，海岛近岸泡沫与风暴海面继续在网页验收。
- [ ] 暂缓的资产版本管理与发布：启用 LFS 时配置精确文件规则、提交前与 CI 检查、用量预算并验证干净检出；运行资源完整发布并验证后再启用生产 Blender 路径。

本批新增建模、三轮日景审图和保存后夜间检查，详见文末；没有新增网页 GPU 验收结果，逐栋 1:1 与电影级仍是未通过项。

## 交付与编辑边界

- D 盘仓库 `design/space/scenes/` 有 25 份可编辑 `.blend`：8 种风格各 3 个环境，加上海城市工程；源文件当前 1,238,664,325 字节（约 1.24 GB），不含 `.blend1` 备份。
- `core/packages/client/public/assets/space/blender-v1/` 有 25 个 GLB；首次迁移合计 1,481,293,792 字节，金茂精修后合计 1,486,872,988 字节，三塔与外滩细部后合计 1,516,672,548 字节，入口批次后合计 1,517,944,488 字节，凹廊与双亭后合计 1,527,832,272 字节，本批窗组与檐口后合计 1,528,260,044 字节；共享纹理约 34.5 MB。清单记录大小与 SHA-256。
- 本地开发加载 Blender 静态产物，魔方转动、天气、水面、交通、时间和相机仍由 Three.js 实时运行。日常导出读取已保存的 `.blend`，不会重新抓取旧程序或覆盖源工程。
- 打包器校验 GLB 并去重纹理，写入临时文件后原子替换正式产物。此前磁盘满导致的 ENOSPC 已解除，全量复验已重新完成；按用户要求不增加磁盘余量门槛。
- 网页中英文“来源与致谢”已列出 Blender / Khronos glTF 插件和迁移用途；统一维护 `credits_data.json`。操作步骤、文件位置、场景属性、备份与发布条件见[编辑说明](../design/space/README.md)。

## 验收证据

临时证据在 `.tmp/png/space-blender/`，不进入 Git。

| 检查 | 结果 |
| --- | --- |
| 完整 25 场景无头浏览器检查 | 25/25 通过，错误列表为空，WebRTC 已禁用；检查几何、边界、表面、反射、碰撞与运行绑定 |
| 上海动态绑定 | 417 个网格、79 个着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例和 12 艘船；时间、水面、交通和船运动检查通过 |
| 提交隔离检查 | 从暂存区构建上海验证包，排除其他未提交的灯光改动后再次通过；错误列表为空，WebRTC 已禁用 |
| 25 份 GLB 清单与引用纹理 | 大小、哈希、纹理文件均通过；记录在 `verified-inventory.json` |
| 日常导出回环 | `modern-original` 选中 1,433 对象，导出 57,004,160 字节，哈希与迁移基线完全相同；不保存源工程 |
| 打包器回归 | 2 项通过，覆盖属性/纹理保留、去重与非法输入保留旧产物 |
| 前端检查 | client typecheck 通过；迁移阶段 3 个 Space 测试文件 60 项通过；共享相机坐标调整后重跑 2 文件 26 项通过 |
| 生成物登记 | 登记检查通过，44 项；另有 4 项既有 upstream 来源待补提示，与本次 Blender 资产无关 |
| 清单刷新 | 本地 HTTP 200，`Cache-Control: no-store`，返回对应 GLB 大小与哈希 |
| 来源入口 | 中英文 DOM 与截图验证；本轮窄窗实际宽度为 484px，无横向溢出，不作为 390px 手机或 GPU 画质证据 |

此前通过可见界面检查过 Blender 住宅、网页卫生间、海岛，以及上海白天和 21:00 夜景。切换后台方式后，全量契约检查使用无 GPU 浏览器，因此没有把它计为新的网页光照验收。

## 预览相机与三轮卫生间审图

24 套住宅/公司源工程的预览相机已修正，上海城市工程保持原相机。住宅补充庭院、车库、影音室和健身房视角；公司只保留适用的室内视角。网页与 Blender 采用同一份室内相机坐标，轴向转换由导入脚本完成。

意大利卫生间后台 Cycles 预览进行了三轮：第一轮发现旧相机指向楼梯，改用网页实际机位；第二轮发现室内过暗，把已导入的面光源元数据转换为临时 Blender 灯光；第三轮已能看清浴缸、淋浴区、双台盆和马桶。结果为 `italian-original-bathroom-preview.png`。功率换算与网页专用反射不等价，该图用于检查编辑工程，不代表网页或电影级验收通过。

修正仅更新仍匹配旧导入位置/朝向的预览相机，保留手工修改过的机位，并生成 `.blend1`。没有操纵或保存用户正在打开的 Blender 窗口。若窗口内容比磁盘文件旧且有未保存修改，应先另存副本再比较，不能直接重新加载或覆盖任何一份。

## 仍待完成的美术与发布工作

- 当前是已有场景的可编辑迁移基线，实测 1:1 建筑、电影级材质、灯光和近景细节仍未达标，需要继续逐资产制作与审图。
- 上海 GLB 当前精修后约 305 MB，正式发布前需分区加载、压缩和移动设备验证。
- `.blend`、GLB 和共享纹理被 Git 忽略，代码提交不包含重资产备份。应单独备份源文件和运行资产；首次导入清单不能重建后续手工编辑。
- 生产默认仍用原场景路径。需在构建/部署环境提供完整资源包，验证资源可达后，再在构建时启用 `NEXT_PUBLIC_SPACE_BLENDER=1`；本轮没有发布授权。

来源：[Khronos glTF-Blender-IO](https://github.com/KhronosGroup/glTF-Blender-IO)，使用 Blender 内置插件，未复制插件代码；首次迁移使用 Three.js GLTFExporter。完整既有资料见[来源记录](space-sources.md)及网页来源入口。

## 2026-09-08 金茂 Blender 精修

已对照幕墙承建方的俯拍、塔冠近景、幕墙细部及 SOM 夜景照片，完成首轮细节建模、第二轮竖向构件与塔冠衔接修正、同机位日景复核和夜间材质层次审图。依据和估算边界见[金茂参考档案](../design/space/references/jin-mao.md)。图像在 `.tmp/png/space-jinmao/`；`baseline-day-crown.png` 与 `refined-day-crown.png` 使用相同最终审图环境，`refined-night-full.png` 是孤立模型预览。

- 修改已保存到 `design/space/scenes/shanghai.blend`，76,371,201 字节；旧五个网格保留在隐藏且不导出的 ARCHIVE 集合，旧工程保留为 `.blend1`。后台保存未操作用户窗口。
- 金茂保留原地理变换和对象 ID；新模型 51 个网格、211,360 个 Blender 顶点，88 层分段。实际导出包围盒总高 420.505874 m，包含细构件端部；宽度、统一层高与细部仍是照片估算。
- 已从保存后的工程导出上海 GLB：254,606,284 字节，较基线增加 5,579,196 字节。清单 SHA-256 为 `ab4a61ce3a9087b2753d774871f490c679bd19bb3e05964fafdd0948bf897a80`。新增预览相机与旧模型均未导出。
- 实际 GLB 加载检查通过：463 个网格、79 个夜景着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；新版本标记、88 层、51 个立面网格、UV 和阴影标记均存在。钟表、船运动与天气水面检查通过，资源错误为空。
- 检查最初按对象名称查找失败：glTF 会规范化旧对象名称。验证改为迁移合同的稳定 ID 后通过；导出的建模标记没有丢失。另从暂存区构建验证包，排除其他未提交上海灯光代码后再次通过。
- client typecheck 通过；来源条目中英文实际 DOM 验证通过，实际视口 1184px / 484px 无横向溢出。无头浏览器 WebRTC 已禁，WebGL 不可用，因此本轮未通过网页 GPU 光照与帧率验收，Blender 日夜预览不能替代该验收。

下一步先复查网页金茂日夜近景，再校准平面与层高，随后按同样流程处理环球金融中心、上海中心和外滩逐栋细节。本轮不宣称已达到逐栋 1:1 或电影级。

## 2026-09-08 三件套与外滩 24 栋 Blender 细部

已在上海源工程继续编辑并完成本地导出。金茂保留此前退台、塔冠与 88 层制作成果；环球金融中心补四面实体幕墙框格及顶部开口内衬，降低过强的蓝色轮廓光；上海中心补贴合扭转外皮的银色框格、冠部内层玻璃和退入支撑。建筑师照片、24 栋逐栋检查表与估算项见[上海地标档案](../design/space/references/shanghai-landmarks.md)。本批没有用旧程序重新生成城市。

外滩 24 栋调整石材、玻璃与金属，58 个石材网格增加本地程序法线/粗糙度纹理；22 栋的 696 处开口增加按轮廓裁切的横竖窗档，另外两栋保留既有窗框。保留原地理变换、招牌、时钟及夜间着色属性；英国领事馆坡屋顶修正为棕色。尚未逐栋重做全部屋面与侧后立面，新增微表面不是实物扫描。

实际迭代：先渲染原 24 栋作为基线；第一版三塔暴露出上海中心框格旋转方向错误，按 glTF→Blender 轴转换修正后重渲染；复查整组与亚细亚近景，明确记录两处错误三角山花尚未处理；汇丰首轮夜间近景穹顶欠曝、构图偏移，调整临时相机及独立顶部投光后重新渲染。最终[三塔](../.tmp/png/space-landmarks/refined-day-towers.png)、[外滩总览](../.tmp/png/space-landmarks/refined-day-bund.png)、[汇丰夜间近景](../.tmp/png/space-landmarks/refined-night-12-bund.png)均已逐张查看。检查舞台和灯光不保存、不导出。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 89,189,206 字节；76,371,201 字节的前版保留为 `.blend1`；未操作用户窗口 |
| 网页导出 | 284,405,844 字节，SHA-256 `91a20e8ae808d25f706fc02ef2f87539857b1d1141a3323b3e273f10adae9f37`；新增两张共享程序纹理 |
| 原有标识 | 542 个原有唯一对象 ID 保留，新增 27 个细部网格 |
| 实际 GLB 运行检查 | 490 网格、164 个着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；错误列表为空 |
| 建筑细部 | 金茂 88 层/51 网格保持；上海中心导出总高 632.354 m 且冠内层存在；环球开口两侧穿透及上部实体共 4 条射线通过；24 栋修订标记、696 处窗格、58 个石材贴图/UV 网格存在 |
| 时间与水面 | 既有时钟、船运动、天气水面与交通绑定通过；不代表全天气或全部 1,440 分钟矩阵通过 |
| 代码检查 | client typecheck、独立验证脚本 typecheck、打包器 2 项回归通过；运行验证包取暂存区源码，隔离其他未提交的上海灯光修改 |
| 来源入口 | 中文 1264px、英文 484px 实际 DOM 和截图可见新增 Gensler 条目与 KPF 更新，无整页横向溢出；请求 390px 的浏览器仍返回 484px，不作为手机验收 |
| 资源可达 | 本地清单 HTTP 200 / `no-store`，哈希与 284,405,844 字节的 GLB 一致，两张引用纹理均存在 |

证据位于 `.tmp/png/space-landmarks/` 的 `runtime-verification.json`、`refined-night-12-bund.json` 及图片。无头浏览器 WebRTC 已禁用，WebGL2 不可用，因此没有新的网页 GPU 着色、灯光、帧率或移动设备验收结论。Blender 检查灯光不等于现场灯位，更不等于网页已达到相同效果。

下一批准确度重点：亚细亚阁楼与女儿墙、上海总会山花/圆窗/雨棚、全组柱头及侧后立面、三塔楼层/平面与内冠实测资料，以及网页白天和夜景的实际 GPU 复核。1:1、电影级和游戏级城市画质仍未通过；此批只本地提交，未 push 或上传重资产。

## 2026-09-08 亚细亚与上海总会屋顶入口

已完成这两栋的一批增量几何修改，保存到上海 Blender 工程并重新导出。亚细亚纠正错误三角屋顶，补平顶阁楼、半圆窗洞、成对入口柱和弧形门廊；上海总会补双端山花、双圆窗、中央拱门与金属玻璃坡面雨棚。实拍依据、照片估算尺寸、三轮纠错过程及日夜图见[逐栋档案](../design/space/references/shanghai-landmarks.md#第二批亚细亚与上海总会屋顶和入口)。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 93,515,600 字节；前版 89,189,206 字节保留为 `.blend1`，被替换网格另有非导出 ARCHIVE 副本；后台操作没有激活用户窗口 |
| 网页导出 | 285,677,784 字节，SHA-256 `474b7b176088bc556b1c3272ccc3743e016e77ab03269b1d88d0208cf0c06b69`；较前版增加 1,271,940 字节，两张共享纹理复用 |
| 标识与几何 | 569 个原有稳定 ID 保留，新增 8 个网格；亚细亚导出包围盒高 35.109999 m；上海总会原亭高保持 |
| 新细部射线 | 实际 GLB 的 2 扇阁楼窗、2 处圆窗、2 端山花、2 处相邻窗、雨棚双坡及入口墙体，共 11 条检查通过；雨棚两侧均命中朝上的玻璃面 |
| 既有建筑与运行绑定 | 498 网格、172 个着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；金茂 88 层/51 网格、上海中心冠部、环球 4 条开口射线保持通过；资源错误为空 |
| 石材与窗格 | 24 栋修订保留，62 个石材贴图/UV 网格；裁掉总会入口上方旧三窗，既有拟合窗格计数从 696 减至 693，新两圆窗与门拱单独建模 |
| 代码检查 | client typecheck、独立验证脚本 typecheck 通过；验证包使用暂存区 Space 源码，排除其他未提交的灯光修改 |
| 来源显示 | 中文实际 1248px、英文实际 484px 的来源 DOM 和截图可见两栋新说明、原始链接与 2026-09-08 日期，无整页横向溢出；请求 390px 仍返回 484px，不计为手机验收 |
| 资源可达 | 清单 HTTP 200 / `no-store`，HTTP 哈希、磁盘 GLB 哈希和大小一致，两张纹理文件存在 |

证据在 `.tmp/png/space-bund12/`，包括 `runtime-verification.json`、`inventory.json`、两种语言来源截图、三轮候选图和保存后日夜图。Playwright MCP 本次请求超时，按既定安全启动器使用无头浏览器；WebRTC 已禁，WebGL2 不可用。运行数据和射线通过不包含新的网页 GPU 光照、帧率或移动设备验收，Blender 夜间检查图也不等于现场灯位复原。

剩余精度任务：亚细亚上部凹入阳台、窗轴分组和侧后立面；上海总会真实巴洛克双亭、柱廊后墙、柱头与浮雕；全组可靠尺寸、真实灯位、材质老化和网页日夜近景。逐栋 1:1 与电影级仍未通过。本批只本地提交，不 push；源工程和 GLB 继续被 Git 忽略，代码提交不构成重资产备份。

## 2026-09-09 凹廊与双亭增量精修

继续在已保存的上海工程做增量编辑，没有重新生成城市。对照两栋实拍，完成亚细亚三开间凹廊、成对柱、弧形铁栏阳台和下部石栏阳台；上海总会补六根浅暖石柱、独立后墙窗、四面开拱的双亭和完整坡屋面。来源、估算尺寸与细部欠缺见[第三批参考档案](../design/space/references/shanghai-landmarks.md#第三批亚细亚凹廊与上海总会双亭)。

两栋各完成三轮日景渲染并逐张查看：亚细亚第二轮仍有被切成半截的旧窗，第三轮清除；总会第二轮坡面没有覆盖原楼体深度，第三轮补齐。最终[亚细亚日景](../.tmp/png/space-bund-details/round3-day-1-bund.png)、[总会日景](../.tmp/png/space-bund-details/round3-day-2-bund.png)及保存后的[亚细亚夜间检查](../.tmp/png/space-bund-details/final-night-1-bund.png)、[总会夜间检查](../.tmp/png/space-bund-details/final-night-2-bund.png)均已查看。检查相机和临时灯光不保存、不导出。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 99,912,314 字节，修订 `bund-galleries-20260909`；93,515,600 字节的前版留为 `.blend1`，替换网格另有非导出 ARCHIVE 副本；未操作用户窗口 |
| 网页导出 | 295,565,568 字节，SHA-256 `1a2676ada54ce9a5064d306dae4e1ce2c3d0a1e5b29c7595bd088800ff42a9e0`；比上批增加 9,887,784 字节，两张共享纹理复用 |
| 标识保留 | 保留 576 个原有具名稳定 ID，新增 10 个网格；无单独 ID 的城市实例不计入唯一 ID 数 |
| 新细部射线 | 34 条通过：全部 25 扇后墙玻璃、前景柱及柱间后墙、双亭穿透、两处朝上阳台板和三个深度的坡屋面；实测玻璃后退约 1.85256 / 2.75250 m，设计深度仍是照片估算 |
| 检查点纠正 | 原柱间后墙检测点 x=1.9 实际命中前景柱；保留该点验证柱子，并另用 x=1.6 验证柱间墙体，两处均通过，未修改模型掩盖错误 |
| 既有建筑与运行绑定 | 508 网格、181 个夜景着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；上批 11 条入口射线、环球 4 条开口射线、金茂 88 层/51 网格、上海中心冠部均保持通过，资源错误为空 |
| 石材与窗格 | 24 栋修订保留，68 个石材贴图/UV 网格通过；本批新建 25 扇后墙窗，旧 `fittedWindows=693` 元数据是前批拟合记录，不表示裁切后仍有 693 扇完整旧窗 |
| 代码与资源 | client typecheck、独立验证脚本 typecheck 通过；本地清单 HTTP 200 / `no-store`，HTTP 清单、磁盘 GLB 哈希及大小一致，引用纹理存在 |
| 验证隔离 | 浏览器验证包从暂存区 Space 源码构建，避免混入其他 AI 的未提交源码；WebRTC 已禁用 |

证据存于 `.tmp/png/space-bund-details/`，包括 `runtime-verification.json`、`asset-verification.json`、各轮渲染及保存日志。此次针对变更后的上海资源验证，没有重跑全部 25 场景。无头环境 WebGL2 仍不可用，着色代码检查不等于 GPU 编译或实际画质；CPU 夜间检查灯光也不等于现场灯位。

后续继续校准两栋侧后立面、实测窗轴、总会顶部成对窗及浮雕，复查网页实际日夜近景，再推进和平饭店、汇丰与江海关。当前几何和材质仍有明显简化，1:1 与电影级未通过。本批只提交本任务代码、清单和文档，不 push；不配置 LFS、不上传重资产，保留其他 AI 的 WIP。

## 2026-09-09 上海总会窗组与檐口

读取现有上海工程增量编辑，对照有来源的总会实拍重建五组三联顶层拱窗、两端大拱窗、五组柱廊后墙成对窗，以及细檐口齿饰；六根石柱单独调整为暖色材质。全部 27 扇新窗均有真实开洞、窗套和后退玻璃。照片依据、估算边界及剩余立面问题见[第四批参考档案](../design/space/references/shanghai-landmarks.md#第四批上海总会窗组与檐口)。

完成三轮候选渲染并逐张检查：清除旧齿饰、修正转角补面与墙皮重叠、让檐口接回墙体，第三轮同时复查正面和侧面，再保存工程并渲染夜间结构检查。最终[正面日景](../.tmp/png/space-bund-windows/round3-day-bund.png)、[侧面日景](../.tmp/png/space-bund-windows/round3-day-oblique.png)、[保存后夜间检查](../.tmp/png/space-bund-windows/final-night-bund.png)均已查看。临时舞台和灯光不保存、不导出；夜间图配光偏平、玻璃过亮，不是最终夜景验收。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 103,874,788 字节，修订 `bund-windows-20260909`；99,912,314 字节的前版保留为 `.blend1`，被修改网格另有非导出 ARCHIVE 副本 |
| 网页导出 | 295,993,340 字节，SHA-256 `b56288497fe66d3a200582086fa38fdded549f10a5247d4229abfe92ef2c35dd`；比上批增加 427,772 字节，两张石材纹理继续复用 |
| 标识与边界 | 586 个原有具名稳定 ID 全部保留，新增 4 个网格；总会屋顶局部包围盒高度仍为 28.645 m，其他楼栋地理变换保留 |
| 新窗组射线 | 实际导出 GLB 的 27 扇玻璃、15 处拱肩、5 处成对窗中央窗套、5 处上方墙面和 2 处转角墙皮，共 54 条全部通过；拱窗玻璃后退 0.57 m、矩形窗约 1.8525 m，设计尺寸仍属照片估算 |
| 检查点纠正 | 首次把成对窗中央当后墙，实际先命中凸出石质窗套；改为分别严格验证窗套深度 1.55 m 与上方裸墙深度 1.80 m，未放宽材质或深度条件 |
| 既有建筑与绑定 | 512 网格、185 个夜景着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；金茂 88 层/51 网格、上海中心冠部、环球 4 条开口射线、入口 11 条与凹廊 29 条射线保持通过，资源错误为空 |
| 计数变化 | 凹廊检查剩余 20 扇原新建窗，上部旧 5 扇已由本批 10 扇成对窗取代；24 栋修订与 70 个石材贴图/UV 网格通过。旧 `fittedWindows=693` 继续作为历史记录，不代表完整保留的旧窗数量 |
| 代码检查 | client typecheck 与独立验证脚本 typecheck 通过；实际验证包读取本批工作树源码，Space 运行模块无其他未提交改动 |
| 资源可达 | 本地清单 HTTP 200 / `no-store`，HTTP 清单与磁盘 GLB 的哈希、大小一致，两张引用纹理存在 |
| 来源显示 | 中文实际 1264px、英文实际 484px 的来源 DOM 和截图可见新窗组说明、实拍链接与 2026-09-09 日期，无整页横向溢出；请求 390px 仍返回 484px，不算小于 480px 验收 |

证据存于 `.tmp/png/space-bund-windows/`，含三轮日景、保存后夜间检查、`runtime-verification.json`、`asset-verification.json` 和中英文来源截图。Playwright MCP 连接无响应后使用既定安全无头启动器，WebRTC 已禁用，WebGL2 仍不可用；数据/射线检查通过不等于网页 GPU 着色或帧率验收。

待继续：总会屋顶老虎窗、瓦片、侧后窗列、花饰与转角旧线脚；两栋实测尺寸及网页日夜近景；和平饭店、汇丰、江海关逐栋精修；三件套可靠尺寸；分区与细节层级及其余场景。现有模型仍有简化，1:1 和电影级尚未达标。本批仅本地提交，未 push、未配置 LFS、未上传重资产，未处理暂停的 GitHub Knip 问题；其他 AI 的 WIP 保留。

## 2026-09-09 汇丰柱廊、江海关铜饰与和平饭店铜顶

继续编辑已保存的上海工程，本批聚焦外滩 12、13、20 三栋。对照实拍重做汇丰六柱等距布局、实体凹槽和连续柱头，增加窗下石饰与招牌灯；江海关补多层铜色窗间板、花饰、檐下方格与灯具；和平饭店重建截顶铜屋面、独立顶亭、平行咬口、红铜包边和简化浮雕。三栋主墙新增原创程序石材分缝与颜色/法线图，旧招牌和时钟继续保留。详细来源、照片估算尺寸及三轮纠错见[第五批参考档案](../design/space/references/shanghai-landmarks.md#第五批汇丰柱廊江海关铜饰与和平饭店铜顶)。

三轮检查中纠正了汇丰旧内柱成对布局、移柱后另一块网格仍残留凸条和叶饰、柱头芯不连续的问题；和平饭店补了浮雕层次。三栋日夜近景逐张查看，汇丰第三轮保存后再渲染夜间检查。最终[汇丰日景](../.tmp/png/bund-hero-details/round3-12-day-close.png)、[保存后夜间近景](../.tmp/png/bund-hero-details/saved-12-night-close.png)、[江海关铜饰](../.tmp/png/bund-hero-details/round2-13-day-close.png)和[和平饭店铜顶](../.tmp/png/bund-hero-details/round2-20-day-close.png)供本地查看；临时检查灯光与舞台不保存、不导出。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 112,698,023 字节，修订 `bund-hero-details-20260909`；103,874,788 字节的前版保留为 `.blend1`，被修改网格有非导出 ARCHIVE 副本；后台 CPU 14 线程、低优先级，没有激活桌面窗口 |
| 网页导出 | 301,710,436 字节，SHA-256 `224e9fbe2ddd2cb5faa1ea3c6152112a597b41dc459a399d4cf8f4771a8cc32b`；比前批增加 5,717,096 字节；共引用 9 张本地纹理，其中新增 6 张石材颜色/法线图和 1 张铜板色差图 |
| 标识与边界 | 590 个原有具名稳定 ID 全部保留，新增 13 个细部网格；汇丰、江海关、和平饭店模型上界分别保持 46.3 / 79.2 / 77 m，这些是现有模型约束，不是新实测数据 |
| 新细部射线 | 53 条全部通过：六柱槽峰/槽谷 12 条、旧柱位残留 8 条、12 块铜饰背板与花饰 24 条、四面铜坡及顶亭 8 条、顶亭封顶 1 条；实际导出槽深范围 0.05793765–0.05793788 m，设计槽深仍为照片估算 |
| 材质与灯具 | 三栋主墙的米制 UV、颜色/法线/粗糙度图保留；三组灯面保留 `shanghai-illumination-1.2` 夜间绑定。新增灯具只有外形与发光灯面，网页实际投光仍来自既有共享灯光，不代表逐灯物理模拟 |
| 既有建筑与运行 | 525 网格、194 个夜景着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；金茂 88 层/51 网格、上海中心冠部、环球开口 4 条、入口 11 条、凹廊 29 条、总会窗组 54 条射线继续通过，错误列表为空 |
| 石材检查 | 24 栋修订保持，73 个石材贴图/UV 网格通过；`fittedWindows=693` 仍是前批历史拟合记录，不是裁切后完整旧窗总数 |
| 代码检查 | client typecheck、独立验证脚本 typecheck 通过；验证包使用本批工作树源码，Space 运行模块没有其他未提交改动；未重跑未修改的全部 25 场景 |
| 资源可达 | 本地 HTTP 清单 200 / `no-store`，HTTP 清单与实际磁盘 GLB 哈希和大小一致，9 张引用纹理全部存在 |
| 来源显示 | 三栋新增来源在中文实际 1264px、英文实际 484px 的 DOM 和截图可见，原始链接和 2026-09-09 日期正确，无整页横向溢出；请求 390px 返回 484px，不计为小于 480px 的手机验收 |
| 当前本地体积 | 25 份 `.blend` 合计 1,247,487,560 字节；25 份 GLB 合计 1,533,977,140 字节，分别不含备份与外置纹理；本轮未配置 LFS 或上传 |

本批证据在 `.tmp/png/bund-hero-details/`：`runtime-verification.json`、`asset-verification.json`、中英文来源截图、候选日夜图和保存日志。Playwright MCP 请求长时间无响应，使用既定安全无头启动器完成 DOM、实际 GLB 加载和射线检查，WebRTC 已禁。该环境 WebGL2 不可用，未完成网页 GPU 光照、帧率和移动设备验证；不能把 Blender 检查图当网页最终效果。

仍需继续的准确度任务：江海关七条主窗轴、矩形入口和钟楼三列百叶；和平饭店上部拱窗、退台、全楼窗组与真实浮雕；汇丰穹顶雕像、完整窗轴、柱头细雕与颜色；全组侧后立面、实测尺寸和真实灯位。和平饭店铜顶夜间欠曝、汇丰柱材偏白、部分玻璃偏亮，均记为未通过。1:1 与电影级仍未达标。本批只本地提交脚本、来源、清单和跟踪文档，不 push，不处理暂停的 GitHub Knip 问题，保留其他 AI 的 WIP；被忽略的 `.blend`、GLB 与纹理没有随代码提交备份。

## 2026-09-09 江海关窗带与和平饭店上部立面

继续在已保存工程做增量建模。江海关重建七条窗带、28 扇主窗、21 块铜饰板、五处矩形入口、四根凹槽石柱和钟下三列格栅；和平饭店重建上部 24 扇成组窗、五扇顶层窗、中央拱窗、退台与雕饰实心栏板。既有招牌、铜顶、时钟、灯具和地理变换保留。实拍来源、照片估算尺寸、三轮纠错及五张保存后日夜检查图见[第六批档案](../design/space/references/shanghai-landmarks.md#第六批江海关窗带与和平饭店上部立面)。

首轮发现和平饭店立面缺口、栏板形式错误及装饰过粗；第二轮改实心雕饰栏板、收细线条并调整石色；第三轮补齐退台连接面和江海关檐下背板。最终两栋日夜及钟下近景均已查看，候选图、保存日志和导出验证位于 `.tmp/png/bund-frontages/`。后台 CPU 14 线程、低优先级，没有操作桌面；临时检查舞台和灯光不保存、不导出。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 115,651,594 字节，修订 `bund-frontages-20260909`；前版 112,698,023 字节留为 `.blend1`；被修改网格有非导出 ARCHIVE 副本 |
| 网页导出 | 304,835,044 字节，SHA-256 `37ea78b9806886a0526f910acea4ec0501d3ae1ddefe3dd0d61f7a84fbf6c0a8`；比前批增加 3,124,608 字节，9 张本地纹理复用 |
| 标识与范围 | 603 个原有具名稳定 ID 全部保留，新增 8 个网格；江海关、和平饭店上界仍为 79.2 / 77 m，此数值是模型约束而非现场实测 |
| 新立面检查 | 85 条实际 GLB 射线通过：主窗、夹层窗、入口、石柱间墙体、三列格栅、和平饭店上部全部窗组、中央拱肩与两处朝上退台；首次命中部件和深度严格校验 |
| 既有细部检查 | 铜饰由 12 块更新为 21 块，铜饰/柱槽/铜顶检查共 71 条；环球开口 4 条、入口 11 条、凹廊 29 条、总会窗组 54 条继续通过 |
| 实际加载与绑定 | 533 网格、195 个夜景着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；金茂 88 层/51 网格及上海中心冠部保持，错误列表为空 |
| 石材与窗格 | 24 栋修订、72 个石材贴图/UV 网格通过；旧 `fittedWindows=693` 继续表示历史拟合记录，不是裁切后完整旧窗总数 |
| 代码与资源 | client typecheck、独立验证脚本 typecheck 通过；HTTP 清单 200 / `no-store`，HTTP 清单与实际 GLB 哈希和大小一致，9 张引用纹理全部存在 |
| 来源显示 | 中文实际 1264px、英文实际 484px 的 DOM 可见三栋原始链接、新立面说明及 2026-09-09 日期，无整页横向溢出；请求 390px 仍返回 484px，不计为小于 480px 验收 |
| 当前本地体积 | 25 份 `.blend` 合计 1,250,441,131 字节；25 份 GLB 合计 1,537,101,748 字节，均不含备份与外置纹理 |

本批数据验证来自新导出 GLB，WebRTC 已禁；无头环境 WebGL2 仍不可用，未完成网页 GPU 灯光、帧率或移动设备验收。Blender 夜间检查图仍有玻璃偏亮、铜顶欠曝，不能代表最终网页夜景。仍需继续江海关屋顶侧亭与其余格栅、和平饭店下部完整窗组、汇丰穹顶雕像和细雕、全组侧后立面、可靠尺寸及现场灯位。1:1 与电影级未通过。本批只本地提交脚本、来源、清单和文档，不 push，不配置 LFS、不上传重资产；保留其他 AI 的 WIP，未处理暂停的 GitHub Knip 问题。

## 2026-09-09 和平饭店临江窗带与底层拱窗

对照和平饭店实拍继续编辑现有上海工程，重建中央临江面七层共 63 扇窗、63 块竖向窗间饰板、两扇底层大拱窗、同心弧形与径向金属花格、浅方格饰纹。保留入口灯牌、门与雨棚、上部窗组及铜顶。照片来源、估算边界见[第七批参考档案](../design/space/references/shanghai-landmarks.md#第七批和平饭店临江窗带与底层拱窗)。

完成三轮候选渲染：首轮发现上下窗带黑缝、入口上方旧窗与偏短饰板；第二轮清理旧窗、拉长饰板、收细框线并处理底部接缝；第三轮定位上部侧返面及顶部封口与墙皮相交，切除重叠面、重建连接条。保存后再次检查[整栋日景](../.tmp/png/bund-peace-facade/saved-20-day.png)、[入口与花格](../.tmp/png/bund-peace-facade/saved-entrance-20-day-close.png)和[夜间窗带结构](../.tmp/png/bund-peace-facade/saved-20-night-close.png)，可见黑色矩形边线已消除。临时检查灯光与舞台不保存、不导出；后台低优先级、CPU 14 线程，没有操作用户桌面。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 117,358,098 字节，修订 `peace-riverfront-20260909`；前版 115,651,594 字节留为 `.blend1`，被修改网格保留非导出 ARCHIVE 副本 |
| 网页导出 | 307,041,008 字节，SHA-256 `fdcb344f9eaab55741d129530c4b2d965b4331103fb75d911ec2d94874213583`；增加 2,205,964 字节，9 张本地纹理复用 |
| 标识与范围 | 611 个原有具名稳定 ID 全保留，新增 4 个网格；裁切 2,836 个旧面，和平饭店上界保持 77 m，此为模型约束而非实测 |
| 新结构检查 | 实际 GLB 的 178 条射线全部通过：63 扇玻璃、63 块饰板、28 处窗带间石面、8 处下部接缝、8 处上部侧边及顶边补面、8 处拱窗玻璃与金属花格等；严格校验首次命中部件和深度，容差 0.025 m |
| 既有细部检查 | 环球开口 4 条、入口 11 条、凹廊 29 条、总会窗组 54 条、铜饰/柱槽/铜顶 71 条、上批立面 85 条全部通过 |
| 实际加载与绑定 | 537 网格、196 个夜景着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；金茂 88 层/51 网格、上海中心冠部保持，资源错误为空 |
| 石材与窗格 | 24 栋修订、73 个石材贴图/UV 网格通过；旧 `fittedWindows=693` 为历史拟合记录，不是剩余完整旧窗数量 |
| 代码与资源 | client typecheck 与独立验证脚本 typecheck 通过；HTTP 清单 200 / `no-store`，HTTP 清单、磁盘 GLB 的大小和哈希一致，9 张引用纹理存在 |
| 网页来源 | 中英文来源 DOM 均包含实拍链接、七层 63 扇窗说明与 2026-09-09 日期，无整页横向溢出；实际宽度 1264 / 484px，请求 390px 返回 484px，不计为小于 480px 验收 |
| 当前本地体积 | 25 份 `.blend` 合计 1,252,147,635 字节；25 份 GLB 合计 1,539,307,712 字节，不含备份与外置纹理 |

证据位于 `.tmp/png/bund-peace-facade/`，包含三轮候选、保存后渲染、保存日志、`runtime-verification.json`、`asset-verification.json` 和中英文来源 DOM 报告。使用既定安全无头浏览器且禁用 WebRTC；WebGL2 不可用，本批未完成网页 GPU 灯光、帧率或移动设备验收，也未重跑未修改的全部 25 场景。

夜间检查图仍有玻璃偏亮、铜顶欠曝；侧后立面、准确浮雕、可靠测绘尺寸和现场配光仍需完善，1:1 与电影级尚未通过。后续继续汇丰穹顶雕像与细雕、江海关屋顶侧亭和其余格栅、全组侧后立面及网页日夜近景，再处理分区和细节层级。本批只本地提交脚本、来源、清单及文档，不 push，不配置 LFS、不上传被忽略的 `.blend`、GLB 或纹理；保留其他 AI 的 WIP，未处理暂停的 GitHub Knip 问题。

## 2026-09-10 江海关屋顶侧亭与钟楼基座

本轮开始时磁盘主工程只有 495 个具名稳定 ID，此前精修仍在含 615 个 ID 的 `.blend1` 中。先分别保存 `pre-recovery-user.blend` 与 `pre-recovery-refined.blend` 到 `.tmp/png/bund-next/`，逐项比较共有对象变换，只有地面 `root/1` 的缩放改变；恢复精修源后保留该地面矩阵和用户 10 个 3D 视图设置。没有操作可见 Blender 窗口，用户无需打开软件。恢复证据为同目录 `source-comparison.json`、`recovery.json` 与日志，两份原始快照均保留。

在恢复后的源工程增量完成两处拱门侧亭、三角山花、壁柱、连续倒角檐口与齿饰，补五扇露台窗、平台和铁栏；清除钟楼基座两扇错误百叶，补两侧墩柱、九组饰框和托石。沿用本地九张纹理，保留已有四面时钟及灯具。实拍依据与三轮修正见[第八批参考档案](../design/space/references/shanghai-landmarks.md#第八批江海关屋顶侧亭与钟楼基座)。

三轮候选逐张审阅，修正独立檐条转角断开、侧面法线和山花斜檐填面，并加深与旧亭体连接。保存后重新检查[近景日景](../.tmp/png/bund-next/saved-13-day-close.png)、[整栋日景](../.tmp/png/bund-next/saved-wide-13-day.png)和[夜间结构](../.tmp/png/bund-next/saved-13-night-close.png)。深处旧屋顶交接仍有暗缝，夜间钟楼基座存在生硬横向阴影，尚未通过完整视觉验收。后台渲染使用 CPU、14 线程和低优先级；临时舞台与灯光不保存、不导出。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 118,475,772 字节；修订 `customs-roof-20260910`，恢复后的 117,357,228 字节版本留为 `.blend1`；保存证据 `saved-author-roof.json` 与 `saved.log` |
| 标识与范围 | 原 615 个具名稳定 ID 全保留，新增 4 个网格；裁切 3,148 个旧面，原网格另有非导出 ARCHIVE；79.2 m 模型上界保留，不代表实测 |
| 网页导出 | 307,761,408 字节，SHA-256 `0963f6d17e2c6eff3ccad4c4ed5f7a6ca58cb0c105db8cc3b99b8e2c12e7e0f5`；较上批增加 720,400 字节 |
| 新结构检查 | 37 条实际 GLB 射线通过：双亭玻璃、拱肩、窗套、山花、转角与檐线，五扇露台窗、平台、钟楼墩柱、原百叶位置石面及九组饰框；首次命中部件与深度容差 0.025 m |
| 原有细部与运行 | 环球 4、入口 11、凹廊 29、总会窗组 54、铜饰/柱槽/铜顶 71、上部立面 85、和平饭店窗带 178 条射线均通过；541 网格、199 个夜景材质、95 组建筑属性、1 个时钟、1,200 交通实例及 12 艘船，资源错误为空 |
| 保留的楼栋 | 金茂 88 层/51 网格、上海中心冠部、24 栋外滩修订与 74 个石材贴图/UV 网格通过；`fittedWindows=693` 仍为历史记录 |
| 代码与资源 | client typecheck、验证脚本独立 typecheck、2 项打包测试及 Python 语法检查通过；首次 typecheck 遇开发服务器生成中的 validator，待页面编译结束后重跑通过；本地 HTTP 清单 200 / `no-store`，哈希和大小与磁盘一致，9 张纹理存在 |
| 来源与浏览器 | Playwright MCP 验证中英文来源链接与 2026-09-10 说明；两种语言实际 390px、文档宽 375px，无整页横向溢出；此次 WebGL2 可创建，但数据与射线检查不代表网页最终灯光、帧率或移动 GPU 验收 |
| 本地总量 | 25 份 `.blend` 合计 1,253,265,309 字节；25 份 GLB 合计 1,540,028,112 字节，不含备份和外置纹理 |

证据在 `.tmp/png/bund-next/`：候选及保存后渲染、`runtime-verification.json`、`asset-verification.json`、中英文 390px 来源截图和 DOM 报告。仅检查本轮变更的上海资源，未重跑其他 24 场景。后续继续屋顶深处接缝、钟面刻度、侧后立面与现场配光，再推进汇丰雕刻和整组立面；照片估算仍不能作为测绘 1:1 或电影级完成证明。本轮仅本地提交脚本、清单、来源和跟踪文档；LFS 与重资产上传仍暂缓，不 push，不处理暂停的 GitHub Knip 问题，保留其他 AI 的 WIP。
