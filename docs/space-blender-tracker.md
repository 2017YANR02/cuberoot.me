# Space Blender 迁移跟踪

2026-09-09 状态更新：既有代码已提交并 push；Blender 源工程和网页重资产仍未上传。用户已确定后续用 Git LFS 管理源工程及必要原始贴图，本次暂缓配置和上传；要求已写入根 `AGENTS.md` 与编辑说明。以下 2026-09-08 的提交、文件大小和验收记录保留为历史证据。

2026-09-08：本地迁移与基础验收完成；未 push、未上传资产、未启用生产环境。工作方式已切换为后台 Blender Python 和无头浏览器，不激活用户窗口、不发送鼠标键盘输入。所有批处理隐藏窗口、低优先级、最多 14 线程，预览使用 CPU。

## 2026-09-09 后续任务顺序

- [x] 记录后续 AI 必须使用 Git LFS 管理 `.blend` 与必要原始贴图；本次仅文档更新，不改忽略规则、不上传重资产、不触碰其他 AI 的 WIP。
- [ ] 网页日夜基线：先解决此前无头环境 WebGL2 不可用的问题，取得当前上海工程在网页中的实际白天、夜间近景，分别记录 Blender 预览与网页结果。
- [ ] 外滩逐栋精修：先收尾亚细亚凹入阳台、窗轴及侧后立面，上海总会双亭、柱廊后墙、柱头和浮雕；随后重点复核和平饭店、汇丰与江海关，再依逐栋档案完成其余建筑。每栋对照有来源的实拍，保存修改前后同机位日夜图。
- [ ] 三件套精度：补可靠的平面、层高与冠部资料，校准金茂宽度和楼层分段、环球顶部开口及收分、上海中心内冠与幕墙比例；资料不足的尺寸保留估算标记。
- [ ] 夜景材质与灯光：按实拍逐栋校正石材色彩、玻璃反射、招牌、亮灯分区和投光层次，并检查江面倒影；用网页实际渲染复核，不用临时 Cycles 检查灯光代替。
- [ ] 网页资产优化：上海当前 GLB 为 285,677,784 字节；按区域拆分、制作远近细节层级、压缩模型和纹理，验证加载、内存、帧率及小于 480px 的真实视口。
- [ ] 其他场景复核：住宅客厅、书房、卧室、卫生间及公司实拍复刻分别审图，海岛近岸泡沫与风暴海面继续在网页验收。
- [ ] 暂缓的资产版本管理与发布：启用 LFS 时配置精确文件规则、提交前与 CI 检查、用量预算并验证干净检出；运行资源完整发布并验证后再启用生产 Blender 路径。

本次核对既有记录并安排待办，未新增建模、渲染或 GPU 验收结果；逐栋 1:1 与电影级仍是未通过项。

## 交付与编辑边界

- D 盘仓库 `design/space/scenes/` 有 25 份可编辑 `.blend`：8 种风格各 3 个环境，加上海城市工程；源文件约 1.21 GB，不含 `.blend1` 备份。
- `core/packages/client/public/assets/space/blender-v1/` 有 25 个 GLB；首次迁移合计 1,481,293,792 字节，金茂精修后合计 1,486,872,988 字节，三塔与外滩细部后合计 1,516,672,548 字节，亚细亚与上海总会入口批次后合计 1,517,944,488 字节；共享纹理约 34.5 MB。清单记录大小与 SHA-256。
- 本地开发加载 Blender 静态产物，魔方转动、天气、水面、交通、时间和相机仍由 Three.js 实时运行。日常导出读取已保存的 `.blend`，不会重新抓取旧程序或覆盖源工程。
- 打包器校验 GLB 并去重纹理，写入临时文件后原子替换正式产物。此前磁盘满导致的 ENOSPC 已解除，全量复验已重新完成；按用户要求不增加磁盘余量门槛。
- 网页中英文“来源与致谢”已列出 Blender / Khronos glTF 插件和迁移用途；统一维护 `credits_data.json`。操作步骤、文件位置、场景属性、备份与发布条件见[编辑说明](../design/space/README.md)。

## 验收证据

临时证据在 `.tmp/png/space-blender/`，不进入 Git。

| 检查 | 结果 |
| --- | --- |
| 完整 25 场景无头浏览器检查 | 25/25 通过，错误列表为空，WebRTC 已禁用；检查几何、边界、表面、反射、碰撞与运行绑定 |
| 上海动态绑定 | 417 个网格、79 个着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例和 12 艘船；时间、水面、交通和船运动检查通过 |
| 提交隔离检查 | 从暂存区构建上海验证包，排除其他未提交的灯光改动后再次通过；错误列表为空，WebRTC 已禁用 |
| 25 份 GLB 清单与引用纹理 | 大小、哈希、纹理文件均通过；记录在 `verified-inventory.json` |
| 日常导出回环 | `modern-original` 选中 1,433 对象，导出 57,004,160 字节，哈希与迁移基线完全相同；不保存源工程 |
| 打包器回归 | 2 项通过，覆盖属性/纹理保留、去重与非法输入保留旧产物 |
| 前端检查 | client typecheck 通过；迁移阶段 3 个 Space 测试文件 60 项通过；共享相机坐标调整后重跑 2 文件 26 项通过 |
| 生成物登记 | 登记检查通过，44 项；另有 4 项既有 upstream 来源待补提示，与本次 Blender 资产无关 |
| 清单刷新 | 本地 HTTP 200，`Cache-Control: no-store`，返回对应 GLB 大小与哈希 |
| 来源入口 | 中英文 DOM 与截图验证；本轮窄窗实际宽度为 484px，无横向溢出，不作为 390px 手机或 GPU 画质证据 |

此前通过可见界面检查过 Blender 住宅、网页卫生间、海岛，以及上海白天和 21:00 夜景。切换后台方式后，全量契约检查使用无 GPU 浏览器，因此没有把它计为新的网页光照验收。

## 预览相机与三轮卫生间审图

24 套住宅/公司源工程的预览相机已修正，上海城市工程保持原相机。住宅补充庭院、车库、影音室和健身房视角；公司只保留适用的室内视角。网页与 Blender 采用同一份室内相机坐标，轴向转换由导入脚本完成。

意大利卫生间后台 Cycles 预览进行了三轮：第一轮发现旧相机指向楼梯，改用网页实际机位；第二轮发现室内过暗，把已导入的面光源元数据转换为临时 Blender 灯光；第三轮已能看清浴缸、淋浴区、双台盆和马桶。结果为 `italian-original-bathroom-preview.png`。功率换算与网页专用反射不等价，该图用于检查编辑工程，不代表网页或电影级验收通过。

修正仅更新仍匹配旧导入位置/朝向的预览相机，保留手工修改过的机位，并生成 `.blend1`。没有操纵或保存用户正在打开的 Blender 窗口。若窗口内容比磁盘文件旧且有未保存修改，应先另存副本再比较，不能直接重新加载或覆盖任何一份。

## 仍待完成的美术与发布工作

- 当前是已有场景的可编辑迁移基线，实测 1:1 建筑、电影级材质、灯光和近景细节仍未达标，需要继续逐资产制作与审图。
- 上海 GLB 当前精修后约 286 MB，正式发布前需分区加载、压缩和移动设备验证。
- `.blend`、GLB 和共享纹理被 Git 忽略，代码提交不包含重资产备份。应单独备份源文件和运行资产；首次导入清单不能重建后续手工编辑。
- 生产默认仍用原场景路径。需在构建/部署环境提供完整资源包，验证资源可达后，再在构建时启用 `NEXT_PUBLIC_SPACE_BLENDER=1`；本轮没有发布授权。

来源：[Khronos glTF-Blender-IO](https://github.com/KhronosGroup/glTF-Blender-IO)，使用 Blender 内置插件，未复制插件代码；首次迁移使用 Three.js GLTFExporter。完整既有资料见[来源记录](space-sources.md)及网页来源入口。

## 2026-09-08 金茂 Blender 精修

已对照幕墙承建方的俯拍、塔冠近景、幕墙细部及 SOM 夜景照片，完成首轮细节建模、第二轮竖向构件与塔冠衔接修正、同机位日景复核和夜间材质层次审图。依据和估算边界见[金茂参考档案](../design/space/references/jin-mao.md)。图像在 `.tmp/png/space-jinmao/`；`baseline-day-crown.png` 与 `refined-day-crown.png` 使用相同最终审图环境，`refined-night-full.png` 是孤立模型预览。

- 修改已保存到 `design/space/scenes/shanghai.blend`，76,371,201 字节；旧五个网格保留在隐藏且不导出的 ARCHIVE 集合，旧工程保留为 `.blend1`。后台保存未操作用户窗口。
- 金茂保留原地理变换和对象 ID；新模型 51 个网格、211,360 个 Blender 顶点，88 层分段。实际导出包围盒总高 420.505874 m，包含细构件端部；宽度、统一层高与细部仍是照片估算。
- 已从保存后的工程导出上海 GLB：254,606,284 字节，较基线增加 5,579,196 字节。清单 SHA-256 为 `ab4a61ce3a9087b2753d774871f490c679bd19bb3e05964fafdd0948bf897a80`。新增预览相机与旧模型均未导出。
- 实际 GLB 加载检查通过：463 个网格、79 个夜景着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；新版本标记、88 层、51 个立面网格、UV 和阴影标记均存在。钟表、船运动与天气水面检查通过，资源错误为空。
- 检查最初按对象名称查找失败：glTF 会规范化旧对象名称。验证改为迁移合同的稳定 ID 后通过；导出的建模标记没有丢失。另从暂存区构建验证包，排除其他未提交上海灯光代码后再次通过。
- client typecheck 通过；来源条目中英文实际 DOM 验证通过，实际视口 1184px / 484px 无横向溢出。无头浏览器 WebRTC 已禁，WebGL 不可用，因此本轮未通过网页 GPU 光照与帧率验收，Blender 日夜预览不能替代该验收。

下一步先复查网页金茂日夜近景，再校准平面与层高，随后按同样流程处理环球金融中心、上海中心和外滩逐栋细节。本轮不宣称已达到逐栋 1:1 或电影级。

## 2026-09-08 三件套与外滩 24 栋 Blender 细部

已在上海源工程继续编辑并完成本地导出。金茂保留此前退台、塔冠与 88 层制作成果；环球金融中心补四面实体幕墙框格及顶部开口内衬，降低过强的蓝色轮廓光；上海中心补贴合扭转外皮的银色框格、冠部内层玻璃和退入支撑。建筑师照片、24 栋逐栋检查表与估算项见[上海地标档案](../design/space/references/shanghai-landmarks.md)。本批没有用旧程序重新生成城市。

外滩 24 栋调整石材、玻璃与金属，58 个石材网格增加本地程序法线/粗糙度纹理；22 栋的 696 处开口增加按轮廓裁切的横竖窗档，另外两栋保留既有窗框。保留原地理变换、招牌、时钟及夜间着色属性；英国领事馆坡屋顶修正为棕色。尚未逐栋重做全部屋面与侧后立面，新增微表面不是实物扫描。

实际迭代：先渲染原 24 栋作为基线；第一版三塔暴露出上海中心框格旋转方向错误，按 glTF→Blender 轴转换修正后重渲染；复查整组与亚细亚近景，明确记录两处错误三角山花尚未处理；汇丰首轮夜间近景穹顶欠曝、构图偏移，调整临时相机及独立顶部投光后重新渲染。最终[三塔](../.tmp/png/space-landmarks/refined-day-towers.png)、[外滩总览](../.tmp/png/space-landmarks/refined-day-bund.png)、[汇丰夜间近景](../.tmp/png/space-landmarks/refined-night-12-bund.png)均已逐张查看。检查舞台和灯光不保存、不导出。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 89,189,206 字节；76,371,201 字节的前版保留为 `.blend1`；未操作用户窗口 |
| 网页导出 | 284,405,844 字节，SHA-256 `91a20e8ae808d25f706fc02ef2f87539857b1d1141a3323b3e273f10adae9f37`；新增两张共享程序纹理 |
| 原有标识 | 542 个原有唯一对象 ID 保留，新增 27 个细部网格 |
| 实际 GLB 运行检查 | 490 网格、164 个着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；错误列表为空 |
| 建筑细部 | 金茂 88 层/51 网格保持；上海中心导出总高 632.354 m 且冠内层存在；环球开口两侧穿透及上部实体共 4 条射线通过；24 栋修订标记、696 处窗格、58 个石材贴图/UV 网格存在 |
| 时间与水面 | 既有时钟、船运动、天气水面与交通绑定通过；不代表全天气或全部 1,440 分钟矩阵通过 |
| 代码检查 | client typecheck、独立验证脚本 typecheck、打包器 2 项回归通过；运行验证包取暂存区源码，隔离其他未提交的上海灯光修改 |
| 来源入口 | 中文 1264px、英文 484px 实际 DOM 和截图可见新增 Gensler 条目与 KPF 更新，无整页横向溢出；请求 390px 的浏览器仍返回 484px，不作为手机验收 |
| 资源可达 | 本地清单 HTTP 200 / `no-store`，哈希与 284,405,844 字节的 GLB 一致，两张引用纹理均存在 |

证据位于 `.tmp/png/space-landmarks/` 的 `runtime-verification.json`、`refined-night-12-bund.json` 及图片。无头浏览器 WebRTC 已禁用，WebGL2 不可用，因此没有新的网页 GPU 着色、灯光、帧率或移动设备验收结论。Blender 检查灯光不等于现场灯位，更不等于网页已达到相同效果。

下一批准确度重点：亚细亚阁楼与女儿墙、上海总会山花/圆窗/雨棚、全组柱头及侧后立面、三塔楼层/平面与内冠实测资料，以及网页白天和夜景的实际 GPU 复核。1:1、电影级和游戏级城市画质仍未通过；此批只本地提交，未 push 或上传重资产。

## 2026-09-08 亚细亚与上海总会屋顶入口

已完成这两栋的一批增量几何修改，保存到上海 Blender 工程并重新导出。亚细亚纠正错误三角屋顶，补平顶阁楼、半圆窗洞、成对入口柱和弧形门廊；上海总会补双端山花、双圆窗、中央拱门与金属玻璃坡面雨棚。实拍依据、照片估算尺寸、三轮纠错过程及日夜图见[逐栋档案](../design/space/references/shanghai-landmarks.md#第二批亚细亚与上海总会屋顶和入口)。

| 检查 | 本批结果 |
| --- | --- |
| Blender 保存 | `shanghai.blend` 93,515,600 字节；前版 89,189,206 字节保留为 `.blend1`，被替换网格另有非导出 ARCHIVE 副本；后台操作没有激活用户窗口 |
| 网页导出 | 285,677,784 字节，SHA-256 `474b7b176088bc556b1c3272ccc3743e016e77ab03269b1d88d0208cf0c06b69`；较前版增加 1,271,940 字节，两张共享纹理复用 |
| 标识与几何 | 569 个原有稳定 ID 保留，新增 8 个网格；亚细亚导出包围盒高 35.109999 m；上海总会原亭高保持 |
| 新细部射线 | 实际 GLB 的 2 扇阁楼窗、2 处圆窗、2 端山花、2 处相邻窗、雨棚双坡及入口墙体，共 11 条检查通过；雨棚两侧均命中朝上的玻璃面 |
| 既有建筑与运行绑定 | 498 网格、172 个着色材质、95 组建筑属性、1 个时钟、1,200 个交通实例、12 艘船；金茂 88 层/51 网格、上海中心冠部、环球 4 条开口射线保持通过；资源错误为空 |
| 石材与窗格 | 24 栋修订保留，62 个石材贴图/UV 网格；裁掉总会入口上方旧三窗，既有拟合窗格计数从 696 减至 693，新两圆窗与门拱单独建模 |
| 代码检查 | client typecheck、独立验证脚本 typecheck 通过；验证包使用暂存区 Space 源码，排除其他未提交的灯光修改 |
| 来源显示 | 中文实际 1248px、英文实际 484px 的来源 DOM 和截图可见两栋新说明、原始链接与 2026-09-08 日期，无整页横向溢出；请求 390px 仍返回 484px，不计为手机验收 |
| 资源可达 | 清单 HTTP 200 / `no-store`，HTTP 哈希、磁盘 GLB 哈希和大小一致，两张纹理文件存在 |

证据在 `.tmp/png/space-bund12/`，包括 `runtime-verification.json`、`inventory.json`、两种语言来源截图、三轮候选图和保存后日夜图。Playwright MCP 本次请求超时，按既定安全启动器使用无头浏览器；WebRTC 已禁，WebGL2 不可用。运行数据和射线通过不包含新的网页 GPU 光照、帧率或移动设备验收，Blender 夜间检查图也不等于现场灯位复原。

剩余精度任务：亚细亚上部凹入阳台、窗轴分组和侧后立面；上海总会真实巴洛克双亭、柱廊后墙、柱头与浮雕；全组可靠尺寸、真实灯位、材质老化和网页日夜近景。逐栋 1:1 与电影级仍未通过。本批只本地提交，不 push；源工程和 GLB 继续被 Git 忽略，代码提交不构成重资产备份。
