# Alg 库数据清洗记录

校验器(`lib/alg_goals.ts` + `lib/alg_validation.ts`)按 set 配了目标态之后,全库扫描仍有少量
BAD 项。这些不是判据问题,是逐条跟用户(魔方专家)确认过的真实数据情况,记在这里备查。

## 3x3/zbls —— 已清完(88 → 0)

- 86 个 `Geng ####` case:来自 /recon 复盘自动生成,不需要,整案删除(id 5411-5496)。
- 全库 `=`(上一条等价标记)统一挪到公式最前面,挪动后如果暴露出两个相邻同族转体
  (如 `U2 U'`、`y y'`),就地合并(通用规则,不限 U 族)。`*`(废弃标记)全库直接删光。
  只有 zbls 有这两个标记,别处没有。
- 41 个 case 的 `setup` 字段本身缺一个收尾转动(`U`/`U2`/`U'`/`y`),不是公式错,是
  打乱字段本身没到位;逐个验证补齐。
- 一批 `G/H/K/L/R/T/W/X` 系列的镜像 case(`G D'`/`H IZ`/`K IL`/`R VM`/`X D` 等)
  setup 里 `M`/`M'`/`F`/`F'`/`S`/`S'` 少打或错位了一个撇号,逐条核对原始打乱手动改对。
- 零散几条公式本身写错(`Geng` 系列已经整案删了;`E IB'` 一个招式撇号打错;`U D` 一条是
  跟第一条公式撞车的重复条目,直接删;`VP`(id 6070)两条公式经排查后确认写坏,删除,
  case 保留其余 3 条能用的)。

## 3x3/adv-f2l(高阶 F2L)

搁置,未按 `f2l`(角棱都归位)判据处理。用户确认:AF2L 的终态可能是"整个 F2L 少一个槽"
——不是简单的角棱插槽收尾,判据要另外设计。当前仍用 `f2l` 兜底,483 条中大部分会被判 BAD,
这是已知的判据缺口,不是数据错误,暂不处理。
