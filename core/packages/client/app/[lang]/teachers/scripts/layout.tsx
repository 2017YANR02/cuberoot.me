import { pageMetadata } from '../../../../lib/page-meta';
import './scripts.css';
export const generateMetadata = pageMetadata('teachers/scripts');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
