{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege permissions for the local CubeRoot window",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "opener:allow-open-url",
    "blec:default"
  ]
}
