# 运行 node prepare_config.mjs 自动生成 config.yml；默认数据在仓库同级 scramble/。
# 目录可通过 CUBEROOT_DATA_ROOT / CUBEROOT_WCA_DATA_DIR /
# CUBEROOT_PUZZLE_DATA_DIR / CUBEROOT_XCROSS_DATA_DIR 覆盖。
# 手工修改生成的 config.yml 后会保留你的自定义配置。
