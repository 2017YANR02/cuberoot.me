# 非 WCA 小魔方求解器 Loop — backlog + 协议

> `/loop 继续造小魔方求解器`(别名「造非 WCA 求解器」「继续造 puzzle 求解器」)的**单一事实源**。
> 每轮**先完整读本文件**,按 §0 协议推进 §1 backlog 下一个未完成单元,干完更新本文件(打勾 + §2 日志 +
> 必要时 §3)。
>
> 这是**纯 TS** 路线(Ivy 范式),跟 `solver/SOLVER_LOOP.md`(Rust 引擎 + WASM + 大表 + 真题管道)是
> **两条独立 loop**,别混。范本代码 = `packages/client/lib/ivy-solver.ts` + `_IvySolver.tsx` +
> `gen/_svg/ivy_svg.ts` + `stats/_components/IvyDistView.tsx`;造每个单元前**先调 skill `new-substep-solver`
> 读 §0 七步**。
>
> 目标:给 `/scramble/gen` 的非 WCA 魔方(`lib/cstimer-scramble.ts` 的 `CSTIMER_EVENTS`,除 ivy 外 29 项)
> 依次造在线求解器 —— **能整解最优就最优,实在做不动才近最优**。
>
> 用户决策(2026-06-20 锁定):范围 = TIER A→B→C 全做(都是最优 / 单实例最优);**TIER D(大型近最优)
> 开工前 `⏸ soft-gate` 停一次问范围**。提交 = 自动 commit **不 push**。真题 / 全量发布不在 loop(本路线
> 多为全空间自生成,无管道;少数采样分布的发布留 §3 MANUAL)。

---

## §0.0 铁律(踩过 22 个单元的血泪,先读)

1. **编辑器 / `new-diagnostics` 诊断一律不信** —— 多 agent 跑时它们恒陈旧(幻报「X 不可赋给 SolvePuzzle」「缺于 EVENT_ID」甚至「Cannot find module 2307」,本 loop 每次都是假)。唯一真相 = 跑 `pnpm --filter @cuberoot/client typecheck`(tsgo)**和** `typecheck:tsc`,各自 `; echo EXIT=$?` 直接看(**禁** tail/head/grep 管道,管道吞过真 exit)+ `git show --stat <hash>` 验提交完整。禁凭诊断派「修复」,先用两个 typechecker 复现。
2. **先实算真闭包,再定档** —— 估值本 loop 错过 ~7 次(dino 35,640→239.5M;233→1.42e9;ctico ~1e8→7.1e34;gear 12.4M→41,472;sfl ~1e5→3.04M;ufo 200M→60,480;dmd ~1e6→138,240)。每单元必先跑 bounded BFS(OOM/耗时守卫)量真实可达态再选 A/B/C/D,太大就降档,**禁拿纸面估值定档**。
3. **诚实记质量** —— 每单元日志显式写质量桶:「近最优(均值 N 步,method=…)」或「有效 + 有界(≤N 步,未优化)」。jumbling / 天文量级的「有效 + 有界」是**已接受结果**,别假装近最优。
4. **状态数可超 2^53** —— 一律存/显示为预格式化字符串,**禁** JS number 字面量(D1b 把 4.3e19 写成数字字面量丢精度)。
5. **UI 债真实在欠** —— 几乎每单元带 `[untested-ui]`(dev server 没起 / RAM 被大求解器占)。逻辑全 vitest 验过,只欠 Playwright;dev 起来 + RAM 空出后一次 UI-sweep(solver+stats 页、桌面 + 390px、预览渲染、0 console error)清账。**单元内绝不自启 dev server**(见 §0 约束)。
6. **TIER C/D 分布必须离线预计算(铁律,2026-06-21)** —— **TIER C/D 分布必须离线预计算(build 脚本 → 静态 `stats/scramble/dist_<event>.json`,经 `statsUrl` fetch),严禁浏览器现场求解采样;状态空间巨大时由 build 脚本取合理样本数 N 离线算好,页面只 fetch+渲染;新 build 脚本接入 puzzle-stats 管道。** 开 分布 tab 不准解任何打乱。现成可复用脚本 = `packages/scramble-stats-build/src/build_puzzle_sampled_dist.ts`(参数化 `event + N`,import 该 puzzle 的纯 TS 求解器、用它自带的 cstimer 同款随机生成器采样;加新单元只在 `REGISTRY` 加一行);范本 DistView = `Cuboid335DistView.tsx`(只 `fetch(statsUrl('/stats/scramble/dist_335.json'))` 渲染,**零求解器 import**;334/336/337 已同款 retrofit 为纯 fetch)。⚠ 旧 DistView(15p…)仍进页现场 `solve` 采样 = **已知债**,退役改 fetch 是后续 wave(每个机械活:加 REGISTRY 行 → 跑脚本生成 JSON → DistView 改 fetch + 删求解循环)。A/B 档不受此约束(全空间精确直方图,数据现成、本就不现场算)。

7. **造任何求解器前先 WebSearch prior art(铁律,2026-06-21,用户要求)** —— 开工前**先 WebSearch / WebFetch 查现成成果 / 已知算法 / 复杂度 / 上下界**,别从零硬造。许多经典 puzzle(尤其非魔方滑块如 15p)网上一堆现成最优算法 / 距离表 / 文献,直接复用或对照;查到「已永久排除 / 已有标准解」就别重复发明。先调研、记到 §1 条目旁,再定档动手。

---

## §0 LOOP PROTOCOL(每轮照做)

1. **读本文件全文**。在 §1 自顶向下找第一个未打勾任务(尊重 TIER 顺序:A→B→C→soft-gate→D)。
2. **遇到 `⛔ GATE` / `⏸ soft-gate`**:**停 loop,不 ScheduleWakeup**。§3 写清要用户拍板的取舍,正文一句话摘要,等用户。绝不擅自跨过。
   ⚠ `📦 MANUAL` **不是 gate** —— 它是「loop 跳过这条、§3 留交接、继续下一个」(采样分布灌注/发布留用户),别当 gate 停住。
3. **否则把该单元整体派给一个 fresh 子 agent**(`Agent` 工具,general-purpose)。**主 loop 不亲自写求解器代码 / 不读大文件**——只给子 agent 一段自包含 prompt:
   - 「读本文件 §4 共享套路 + §1 的 <单元> 条目 + 调 skill `new-substep-solver`(§0 七步)」
   - 本单元的档位(A/B/C/D)+ cstimer 源码指针(移动语义照抄)+ 验收门(§0.4)+ 提交规则(§0.5)
   - 要求干到验收门过、自行 commit,**只回 ≤20 行摘要**(改了哪些文件 / 门过没 / commit 短 hash / blocker)。
   - UI 验收(playwright)可单独派子 agent,**只回关键断言**(预览是否渲染、native↔参照是否一致、有无 console error),**绝不回 DOM snapshot**。
4. **验收门(按档,全过才算成)**:
   - **求解器 lib**(`pnpm --filter @cuberoot/client exec vitest run tests/<puzzle>_solver.test.ts` 绿):
     - **A/B 档**:独立 BFS 交叉验证最优性 + **全枚举计数 == 全空间分布**(照 `ivy_solver.test.ts`);God's number 锁 `toBe`。
     - **C 档**(单实例最优):随机 N 条打乱,solver 长度 == 独立 IDA\*/参照最短;`scramble∘solution = solved`。
     - **D 档**(近最优):有效性必验 —— 测试的 oracle 必须**独立**(wrap-cstimer 走真引擎 round-trip;从零 reduction 则从几何**独立重导**移动置换,**禁**从 solver 字节拷贝再「自验」,A6 拷表 bug 证了什么都没测),验 `scramble∘solution = solved`;长度 ≤ 文献上界 / cstimer 同口径,质量桶照 §0.0 #3 记录。
   - **typecheck**(照 §0.0 #1,两个都跑 + `; echo EXIT=$?`,无管道):`typecheck`(tsgo)+ `typecheck:tsc` 均 EXIT=0。
   - **分布(C/D 档专属,2026-06-21)**:**分布数据必须离线预计算**(§0.0 #6)—— build 脚本 `build_puzzle_sampled_dist.ts` 跑出 `stats/scramble/dist_<event>.json`(`histogram` 求和 == `sampleCount`),DistView **只 fetch+渲染、零求解器 import**;grep 该 DistView 确认进页**不调任何 `solve*`**(无 `setSamples`/`cancelRef`/「采样中」现场循环)。A/B 档分布是全空间精确直方图、数据现成,不在此约束内。
   - **UI**:playwright 开 `127.0.0.1:3000/scramble/solver?event=<id>` 桌面 + 390px,0 console error,**预览渲染**(solved 自证:纯色 / 复原态),分布页 `?event=<id>` 渲染。dev server 不在 `127.0.0.1:3000` → UI 验证标 §4 欠账继续,**绝不自己 `pnpm dev`**。
5. **提交(子 agent 在单元内做)**:`git add` **只加本单元改动的文件**(别 `git add .`),commit(英文 message,结尾 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`),**不 push**。跳过了 UI 门 → message 前缀 `[untested-ui]` + §4 登记。
6. **更新并提交本文件**:勾掉任务;§2 追加一行(日期 + 单元 + commit 短 hash);有采样分布灌注/发布 → §3 MANUAL;有测试欠账 → §4。然后 `git add solver/NONWCA_PUZZLE_LOOP.md` 单独 commit(主 loop 自己做)。
7. **决定下一步(Ralph:跑到底)**:
   - **还有未 gate backlog** → `ScheduleWakeup`(self-paced ~150–240s;有后台 build/test 则对齐),**原样回传同一条 /loop 指令**。这是常态。
   - **完成信号:全打勾 / 下一个是 `⏸ soft-gate(TIER D)`** → **停**,正文给收尾总结。
   - **红灯**(验收门真跑两次仍不过)→ 停,§3 写失败摘要,等用户。
   - **安全网**:单 session 连续推进 ~15 个单元仍没到完成信号 → 停一次,提示 `/clear` 重 `/loop` 续。
8. **上下文防腐(主 loop 一直瘦)**:真相只在 git(代码)+ 本文件(进度/决策),从不靠「记得」;每轮读文件重建状态。重活全在 fresh 子 agent 里干完即弃;派发给子 agent 的 prompt 要短(详细 spec 写进 §1 条目 / §4),只给指针。

---

## §0.5 分档判据(每个 puzzle 开工先定档)

> 态数**先信 §1 条目里的估值,但子 agent 必须在测试里用独立 BFS / 闭包实算核实真实可达态数**(照 Ivy:
> 别信记忆/估值,枚举为准)。移动语义**必须逐字段照抄 cstimer 源**(`tools/cstimer-scramble/scramble/*.js`),
> 否则 cstimer 生成的真打乱解不了。

| 档 | 判据(可达态数) | 做法 | 分布数据 | 下载 |
|---|---|---|---|---|
| **A 浏览器现场全 BFS** | ≤ ~2×10⁶ | 进页一次性 BFS 整图 memoized,查表 O(深度) 最优 | 全空间精确直方图 | 全部状态 CSV(按最优步数分类)+ 单步数 txt |
| **B build 时预算表** | ~2×10⁶–~5×10⁷ | Node/TS build 出距离表 → 发 `stats/scramble/opt_<p>.bin.gz`,页面查表;现场 BFS 实测 >~1.5s 才落表 | 全空间精确直方图 | 同 A;**态数 >~2M 时「下载全部」CSV 会很大** → 默认给单步数 txt + 注明总量,CSV 设上限 / 异步流式 |
| **C 单实例 IDA\*** | 巨大但单条可证最短(滑块类) | 每条打乱 IDA\* + 可采纳启发式(15p 用 walking-distance),无全表 | **离线**采样直方图(§0.0 #6):build 脚本采 N 条解后分桶 → 静态 `dist_<event>.json`,页面只 fetch;**严禁浏览器现场采样** | 不提供全状态(无法枚举);可下载样本(JSON 内嵌 `generatedSamples`) |
| **D 近最优** | 巨大且无优雅最优(大型扭转 / jumbling) | reduction / 两阶段;**random-state 的可直接复用 cstimer 自带 solver 当引擎** | **离线**采样直方图(同 C,`dist_<event>.json`,页面零现场求解) | 同 C |

**档边界 puzzle**(`gear` 12.4M / `ctico` ~1e8 / `cm3` ⚠):先按上一档现场 BFS 试跑,实测慢 / 态数超阈再降一档落表。子 agent 自行实测决定,在 §1 条目旁记真实态数 + 选定档。

> **C/D 分布脚本(2026-06-21)**:复用 `packages/scramble-stats-build/src/build_puzzle_sampled_dist.ts`(参数化 `event + N`,import 该 puzzle 纯 TS 求解器、用其 cstimer 同款随机生成器采样,落 `dist_<event>.json`)。接入新单元 = `REGISTRY` 加一行 + `update_puzzle_stats.ps1` 的 `$SAMPLED_DIST_EVENTS` 加 event。范本 DistView = `Cuboid335DistView.tsx`(纯 fetch+渲染)。**N 取够平滑又几分钟跑完单进程**(335 在 ~0.25s/解下取 N≈1000 ≈ 5min)。

---

## §0.6 FROM-SCRATCH TIER D 子协议(剩余 backlog 全是这个)

> 剩 18 个 D 单元**无 cstimer solver 可 wrap、无现成引擎可复用**(已核实,见 §3),每个要自建 reduction / 两阶段。照此走:

1. **先量闭包(§0.0 #2)+ 分流打乱类型**:读 vendored 生成器(`tools/cstimer-scramble/scramble/*.js`)判 random-MOVE vs random-STATE。
2. **有 cstimer solver(random-STATE)→ WRAP**(mpyrso/dino 范式):`cstimerSolve(id,scr)`(`lib/cstimer-scramble.ts`)→ worker `op:'solve'` → `SOLVERS` 映射;在 vendored 源该 puzzle 的 IIFE 内**加最小 additive 导出**(`solveScramble`/`roundTripCheck`),不改上游行为。oracle = 真引擎 round-trip。**(注:剩 18 个都没有,见 §3。)**
3. **无 cstimer solver(random-MOVE,剩 18 个的常态)→ 自建 reduction / 两阶段**。有效性 oracle 必须从几何**独立重导**移动置换(**禁**从 solver 拷),验 scramble∘solution=solved。**部分有 poly3dlib 几何**(giga/prcp/heli/helicv/ctico,见 §3)→ apply oracle 可用,但**无 solver**。
4. **分布 = 离线采样**(§0.0 #6 铁律):**严禁在 DistView 里现场 solve 采样**。用 build 脚本 `build_puzzle_sampled_dist.ts`(REGISTRY 加一行:event / solver 路径+导出名 / 打乱长度 / 默认 N / 质量桶)离线解 N 条随机打乱、分桶,落静态 `stats/scramble/dist_<event>.json`;DistView 只 `fetch(statsUrl(...))` 渲染(范本 `Cuboid335DistView.tsx`,**零求解器 import**),显式标「采样,非全空间」。新脚本 event 接入 `update_puzzle_stats.ps1` 的 `$SAMPLED_DIST_EVENTS`。下载 = JSON 内嵌 `generatedSamples` 的样本 CSV。可枚举但 >2M 的态集才用流式下载(generator + 分块 Blob + confirm,A4 范式;**禁**拼一个巨串,A4 撞过 80MB OOM)。
5. **质量诚实(§0.0 #3)**:近最优做到就标近最优;jumbling / 天文量级「有效 + 有界」是已接受结果,别造假。
6. **大数当字符串(§0.0 #4)**。
7. **「单实例最优 or `too-deep`」不是合格交付(334 血泪,2026-06-21)**:TIER C/D 必须解 100% 的**真实 cstimer 打乱**(预算内最优,否则有界 reduction 兜底);务必拿 N 条真打乱跑求解器并报 solve-rate。强 factored 启发式(如 233 的 max(角,棱))只在直径相对启发表小时才收敛 —— 大直径 puzzle(长方体 335/336/337 等)的真打乱接近直径,IDA* 会爆,**必须**配两阶段 / reduction 兜底。

---

## §1 BACKLOG(有序,自顶向下:A→B→C→soft-gate→D)

> 每条:`event id`(`CSTIMER_EVENTS` 的 id)/ 中文名 / cstimer 源 / 估态数 / 档 / 备注。
> ⚠ = 态数估值,子 agent 实算核实后回填。
> **A/B/C 段的 `[→D]` 条目(233 / cm3 / sia113 / sia123 / sia222 / ctico)= 实测超档已重分类,别在原位接** —— 它们的真实态数/记号校准留在原条目作存档,**工作顺序以 §1 TIER D2 那张表为权威**。下一个未完成单元 = TIER D2 表里第一个非 `[x]` 的。

### TIER A — 现场全 BFS 最优(易,Ivy 范式直接套)

- [x] **A0 基础设施:非 WCA 项目分组选择器** —— 30 项塞不进现有 `SolveTabs` / `WcaEventSelector` 图标行。子 agent 设计可扩展的二级入口(建议:WCA 求解器留图标行,非 WCA 走一个分组下拉 / 「更多魔方」分类选择器,同款复用于 `/scramble/solver` 与 `/scramble/stats`)。门:typecheck + playwright 选择器在桌面/390px 可用、能切到 ivy。**这是后续所有单元的接线前提,先做。** ⚠ 设计可改,子 agent 给一句话理由即可,别 gate。
  完成 2026-06-20(commit `6265c3671`,UI 门欠账见 §4)。落地 = `components/NonWcaPuzzlePicker`(「更多魔方」分组下拉,按 family 分组),数据驱动自 `lib/cstimer-scramble`(每 event 加 `family` + `solvable`;A1+ 只翻 `solvable:true` 即自动出现,免改 UI)。WCA 求解器留图标行,ivy 移进分组下拉作 proof,`?event=ivy` URL 不变。
- [x] **A1** `133` 1×3×3 花型 — `1x3x3.js`。move R L F B,每面单 180° 转(自逆),无 power/primes。
  完成 2026-06-20(commit `7a2a56243`)。**实测可达态 = 192**(独立 BFS 枚举;非条目原估的 6,144 —— 那是 cstimer 内部 24×16×16 编码空间,真实闭包受 perm-parity == flip-popcount-parity 约束砍半 = 24×16/2 = 192)。**上帝之数 8**,均值 4.43,直方图 `[1,4,10,24,53,64,31,4,1]`。纯 TS 全图 BFS(memoized),套 Ivy 范式;移动语义逐字段照抄 cstimer `1x3x3.js`(movePieces 2-cycle swap + 逐轴 flip bit)。落地文件 = `lib/floppy-solver.ts` + `solver/_FloppySolver.tsx`(`?event=133` dispatch)+ `gen/_svg/floppy_svg.ts`(U 面 3×3 + 四侧条网图,颜色全由 state 推导 → solved 每面纯色自证)+ `stats/_components/FloppyDistView.tsx`(全 192 态精确直方图 + 自造示例 + 下载全状态 CSV/单步数 txt)+ `tests/floppy_solver.test.ts`(独立 BFS 交叉验证全过 11/11)。**本单元顺带把 A0 遗留未提交的 Ivy proof 文件一并落库**(A0 commit 6265c3671 只提交了 picker 基建,ivy_svg/IvyDistView/_IvySolver/ivy-solver/ivy_solver.css/测试 + 三处共享接线都留在工作树未 commit;A1 的共享接线与 ivy 的交错在同一 hunk,故一并提交保证 fresh checkout 一致)。门:vitest 11/11 绿、typecheck EXIT=0、Playwright UI 验过(solver+stats 桌面+390px、下拉切 133↔ivy、2D 预览渲染、本次改动零 console error)。
- [x] **A2** `223` 2×2×3 塔 — `2x2x3.js`(角 8!=40,320 × 中层 3 排列 6 = **241,920**)。move U D(90° 全 power)+ R2 F2(仅 180°)。
  完成 2026-06-20(commit `7c30974d7`,UI 门欠账见 §4)。**实测可达态 = 241,920**(独立 BFS 枚举,== 全索引空间 40,320×6,全部可达,与条目估值吻合)。**上帝之数 14**,均值 9.7372,直方图 `[1,8,35,157,678,2527,7442,17088,31568,44704,47216,49792,29024,11104,576]`(和 = 241,920)。**档 = TIER A**(现场全 BFS):Node 实测整图 move 表 + BFS 共 ~194ms(≪ 1.5s 阈值),dist 数组 Int8(242KB),浏览器现场跑无压力。纯 TS 全图 BFS(memoized),套 Ivy/Floppy 范式;移动语义逐字段照抄 cstimer `2x2x3.js`(角排列 getNPerm(8) circle 循环 + 中层 getNPerm(3) circle;U/D circle(0,1,2,3)/(4,5,6,7),R2 circle(2,5)(3,6),F2 circle(0,5)(3,4),边 R→circle(0,1)/F→circle(0,2))。落地文件 = `lib/cuboid223-solver.ts` + `solver/_Cuboid223Solver.tsx`(`?event=223` dispatch)+ `gen/_svg/cuboid223_svg.ts`(完整 32 贴纸长方体网图,4 个 move 由真 3D 旋转推出贴纸置换、已验 U⁴=D⁴=R²=F²=identity,颜色全由 state 推导 → solved 每面纯色自证)+ `stats/_components/Cuboid223DistView.tsx`(全 241,920 态精确直方图 + 自造示例 + 下载全状态 CSV/单步数 txt,241,919 行 < 2M 上限)+ `tests/cuboid223_solver.test.ts`(独立 BFS 交叉验证全过 11/11)。门:vitest 11/11 绿、typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(RAM 紧 ~2.4GB + 大求解器在跑,不开 Chromium),两路由经运行中 dev server 实测返回 HTTP 200,详见 §4 欠账。
- [x] **A3** `8p` 八数码 — `slide.js`(9!/2 = **181,440**)。滑块类(非扭转):状态 = 3×3 格排列,move = 空格上下左右滑;最优 + God's number 31 分布;预览 = 数字格(非 net)。
  完成 2026-06-20(commit `fa5d301f2`,UI 门欠账见 §4)。**实测可达态 = 181,440**(== 9!/2;独立 reference BFS 枚举,与估值吻合)。**上帝之数 31**,均值 21.9724,直方图 32 档 `[1,2,4,8,16,20,39,62,116,152,286,396,748,1024,1893,2512,4485,5638,9529,10878,16993,17110,23952,20224,24047,15578,14560,6274,3910,760,221,2]`(和 = 181,440)。**档 = TIER A**(现场全 BFS):整图 move 表(9! ranks × 4 dir,permRank/Unrank)+ BFS 在浏览器一次跑完,dist 用 Int16Array(9! = 362,880 槽,不可达奇排列半边留 −1)。**建了可复用滑块核** `lib/slider-puzzle.ts`(W×H 参数化几何 + 移动规则 + cstimer 记号),8 数码 solver 复用它,15 数码(C1,IDA*)将继承同一份移动语义。**记号经实测锁定**:cstimer `8prp` 发 4 token U D L R(可压缩次数如 D2),= 空格滑动方向;token→空格 Δ 用 1000 条真打乱跑 cstimer 自身 slideMove 验证零撞墙(U:row+1 D:row−1 L:col+1 R:col−1,solved 空格在右下角)。落地文件 = `lib/slider-puzzle.ts` + `lib/slide8-solver.ts` + `solver/_Slide8Solver.tsx`(`?event=8p` dispatch)+ `gen/_svg/slide8_svg.ts`(3×3 数字格,非 net;tile 数字/空格全由 solver state 推导 → solved 渲染规范 1..8 自证)+ `stats/_components/Slide8DistView.tsx`(全 181,440 态精确直方图 + 自造示例 + 下载全状态 CSV/单步数 txt,181,439 行 < 2M 上限)+ `tests/slide8_solver.test.ts`(独立 reference BFS:计数 181,440 / god 直方图 toBe / 400 随机最优性 / apply==参照 / 全枚举==分布 / 预览渲染 solved canonical,13/13 全绿)。门:vitest 13/13 绿、typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(RAM 紧 ~2.4GB + 大求解器在跑),三路由(solver?event=8p、stats?event=8p、带 scramble)经运行中 dev server 实测均返回 HTTP 200,详见 §4 欠账。
- [x] **A4** `sfl` 超薄花型(Super Floppy)— `megascramble.js:33`(~1e5 ⚠)。move R/L + U/D 缩减集。
  完成 2026-06-20(commit `d29dea86b`,UI 门欠账见 §4)。**实测可达态 = 3,041,280** = P(12,4)×4⁴ = 11,880×256(独立旋转推导 BFS 复核,与权威值 jaapsch.net 3,041,280 一致;非条目原估的 ~1e5)。**上帝之数 13**,均值 9.0040,直方图 `[1,12,90,584,3301,15536,64935,226464,602629,1027956,861916,230224,7600,32]`(和 = 3,041,280)。**档 = TIER A**(现场全 BFS):整数排名编码 build 68ms + BFS 505ms ≈ **573ms**(≪ 1.5s 阈值,3.04M 态浏览器现场可跑)。**移动语义**:cstimer `sfl` 仅随机生成 move(无内部 solver / 无图像),move 字母表 = 2 轴 {R,L}/{U,D} × 后缀 `["","2","'"]` = 恰 12 token `R R2 R' L L2 L' U U2 U' D D2 D'`;物理模型 = 平板 3×3×1,4 面各可 90° 转(角件可翻出,故 4 取向),由几何独立推导逐面置换+取向。纯 TS 全图 BFS(memoized),套 Ivy/Floppy/Cuboid223 范式。落地文件 = `lib/superfloppy-solver.ts` + `solver/_SuperFloppySolver.tsx`(`?event=sfl` dispatch)+ `gen/_svg/superfloppy_svg.ts`(平板网图,颜色全由 state 推导 → solved 自证)+ `stats/_components/SuperFloppyDistView.tsx`(全 3,041,280 态精确直方图 + 自造示例 + 下载 CSV/单步数 txt)+ `tests/superfloppy_solver.test.ts`(独立旋转推导参照 BFS 交叉验证,12/12 全绿)。**cstimer 记号校准**:vm 真跑 cstimer sfl 生成器 × 2000 条真打乱 → 0 解析失败 / 0 非法 move / 0 round-trip 失败(scramble∘最优解=solved),token 集恰 12 个。门:vitest 12/12 绿、typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(127.0.0.1:3000 无 dev server,按硬约束不自启),详见 §4 欠账。
- [x] **A5→D** `dino` 恐龙 — `redi.js:119`。move F L B R f l b r(2 power)。**实测误分类(可达态 = A12 = 239,500,800 = 12!/2,超 TIER A/B 上限)→ 用户 soft-gate 选 ③ ALL near-optimal 后,作 TIER D 近最优单元完成(2026-06-20,commit `bd503934b`,UI 门欠账见 §4)**。状态空间推导:cstimer 把 12 条棱全当不同件,8 个 move 都是偶 3-循环,生成群传递本原 + 含 3-循环 → 由 Jordan 定理 = A12。**求解法 = wrap cstimer 自带 dino 求解器(D1a/mpyr 同款省力路,非从零写)**:dino 是只有棱的随态魔方,state = 12-棱排列(无 eo);cstimer `dinoso` 随态生成器内部建随机 ep 再 `solveDino(ep,10)` 反推打乱,我给 vendored `redi.js` 的 dino IIFE 加 `solveScramble(scr)`(解析 scramble → applyMoves 到 solved ep → `solveDino(ep,0)`,复用 cstimer 自带 IDA*+三张棱组合剪枝表)+ `roundTripCheck` / `edgePermOfScramble`(给测试当 oracle);worker `SOLVERS.dinoso = self.redi.solveScramble`;`lib/dino-solver.ts` 的 async `solveDino(scramble)` 直调 `cstimerSolve('dino', ...)` 桥,**无 BFS / 无距离表**。**计步口径 = cstimer face-turn**(每 token=1 步,F L B R f l b r 八个角,可带 `'`);**近最优(非可证最优,但恐龙上帝之数 = 10,实测多为最优/近最优)**。**典型近最优解长度**(40 条真随机态实测):**均值 ≈ 9.53,min 7,max 11**,loose 上界 15(cstimer IDA* 深度上限)。**有效性测试 = 全部 round-trip**:测试 node:vm 加载真 cstimer 引擎,生成 40 条真 `dinoso` 打乱,solveScramble 求解后用真引擎 `edgePermOfScramble(scramble∘solution)` 验 == 恒等排列(scramble∘solution=solved)→ 全过 + 长度 ≤ 15 + token ⊆ 字母表;另 TS 端口 `dinoEpFromScramble` 逐 move 对齐真引擎 ep(200 条 fuzz 0 差异)。落地文件 = `lib/dino-solver.ts`(async wrap + 12-棱 perm + 24-贴纸网模型,贴纸置换逐 move 对齐 cstimer acycle 验过)+ `solver/_DinoSolver.tsx`(`?event=dino` dispatch,async 解 + spinner)+ `gen/_svg/dino_svg.ts`(6 面十字展开立方体网 derived from state,solved 每面单色自证)+ `stats/_components/DinoDistView.tsx`(**采样**近最优直方图:现场生成+解 N 个随机态分桶,标「采样/近最优/非全空间/非可证最优」,只下载样本 CSV,**无下载全部**)+ `tests/dino_solver.test.ts`(真引擎 oracle,8/8 全绿)。接线 5 处同 D1a(cstimer-scramble.ts `solvable:true` family:'twist' / solver page.tsx dispatch / SolveTabs.tsx SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION / stats page.tsx `event==='dino'` 分支 + isIvy + distPuzzle / ScramblePreview2D HAS_PREVIEW+dispatch)。**坑**:dino 棱有取向(贴纸绕角循环时两贴纸换面),24-贴纸网不能用 face1↔face1 直映(3000 条 fuzz 2972 不符),必须用几何推导 + 与 cstimer acycle 交叉验证的真实贴纸置换。门:vitest 8/8 绿、typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(`127.0.0.1:3000` dev server DOWN,按硬约束不自启),`[untested-ui]` 债见 §4。
- [x] **A6** `ufo` UFO — `megascramble.js:34`(~1e6 ⚠)。3 轴 + wheel(A/B/C 旋转)。
  完成 2026-06-20(commit `b98c5b426`,UI 门欠账见 §4)。**实测可达态 = 60,480**(带标签八分体,8 个 cstimer move,face-turn 计步;独立 BFS 复现 + Schreier-Sims `|G|=246,960`(48 home 含 gap 标签 + 轮偏移)商掉 gap 后得 60,480 真实件数;3 色折叠 = 7,560)。**非条目原估的 ~1e6,也非** jaapsch.net 的 **200,121,075**——那是 Jaap 的**着色上界**(12!²/4!⁶ /6),真实机制把每个球半 4 个八分体永远锁成刚体一起翻一起转,自由度远低,实测仅 6 万态。**上帝之数 10**,均值 7.7443,直方图(深度 0..10)`[1,8,33,151,577,1924,5733,13778,21715,14241,2319]`(和 = 60,480)。**档 = TIER A**(现场全 BFS):整图 BFS-from-solved memoized,<50ms ≪ 1.5s 阈值。**移动语义**:cstimer `ufo` 仅随机生成 move(无内部 solver / 无图像 / 无 apply,故无法 round-trip 校验,只能验 token 集 + 解析有效性)。token 字母表 = `[[["A"],["B"],["C"],[["U","U'","U2'","U2","U3"]]]]` 后缀 `""` → 恰 8 token `A B C U U' U2' U2 U3`(U=+1/U'=−1/U2=+2/U2'=−2/U3=+3,6 位轮;A/B/C = 三球 180° 翻转,自逆)。物理模型 = 圆盘 6 扇区两层 + 3 球各切 8 八分体,**由真 3D 旋转矩阵独立推导**(measure4.cjs:U=顶层绕中轴 +60°、A/B/C=各球绕径向边轴 180°),非手工拼线。纯 TS 全图 BFS(memoized),套 Ivy/Floppy/Cuboid223/SuperFloppy 范式。落地文件 = `lib/ufo-solver.ts` + `solver/_UfoSolver.tsx`(`?event=ufo` dispatch)+ `gen/_svg/ufo_svg.ts`(圆盘示意:6 扇区 × 3 球 8 八分体楔形,色由 state 推导 → solved 自证 24 色,非强行套立方体网)+ `stats/_components/UfoDistView.tsx`(全 60,480 态精确直方图 + 自造示例 + 下载全状态 CSV/单步数 txt,60,479 行 < 2M 上限免流式)+ `tests/ufo_solver.test.ts`(独立参照 BFS 交叉验证,15/15 全绿)。接线 5 处:cstimer-scramble.ts `solvable:true`(family 留 twist)/ solver page.tsx dispatch / SolveTabs.tsx(SolvePuzzle/EVENT_ID/PUZZLE_BY_EVENT/HAS_DISTRIBUTION)/ stats page.tsx `event==='ufo'` 分支 / ScramblePreview2D HAS_PREVIEW+dispatch。**cstimer 记号校准**:verbatim 复刻 `mega()`+`ufo` args × 500 条真打乱 / 12,500 token → distinct token 集恰 8 个、0 未知 / 0 解析失败(测量阶段);测试内另跑 400 条随机序列 0 解析失败、全落可达态、A/B/C 自逆已验。门:**主 loop 独立复跑** vitest 15/15 绿 + typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(free RAM ~1.2GB < 2GB 阈,dev server 在跑但按内存约束不开 Chromium,两路由 HTTP 200),`[untested-ui]` 债见 §4。
- [x] **A7** `cm2` Cmetrick Mini — `megascramble.js:29`(⚠ 小)。记号 `< > ^ v`。
  完成 2026-06-20(commit `99f5ff830`,UI 门欠账见 §4)。**实测可达态 = 165,888**(= 24⁴/2,4 个球各 24 取向、奇偶约束砍半;独立 BFS 枚举复核,**未盲信** jaapsch.net 估值)。**上帝之数 10**(cstimer 计步口径:`2` 算 1 步,即 12 token face-turn metric;jaapsch 的 QTM-9 是只算 90° 的另一口径,已在代码注明)、均值 7.32576,直方图 `[1,12,86,524,2577,9564,26964,49648,47712,23644,5156]`(和 = 165,888)。**档 = TIER A**(现场全 BFS):Node 实测整图 build+BFS ≈ **293ms**(packed-int 路径;Map/array 路径 1.27s),浏览器现场可跑。**物理模型**:Cmetrick Mini = **2×2 球阵**(0=左上 1=右上 2=左下 3=右下),每球取向 ∈ 立方体旋转群(24 阶);行移(U=上行{0,1} / D=下行{2,3})绕 Y 轴 90° 同步滚,列移(R=右列{1,3} / L=左列{0,2})绕 X 轴 90° 同步滚;`<`/`^`=+90°、`>`/`v`=−90°(互逆)、`2`=180°(自逆)。token 字母表恰 12 个 `U< U> U2 D< D> D2 R^ Rv R2 L^ Lv L2`。纯 TS 全图 BFS(memoized),套 Ivy/UFO 范式。落地文件 = `lib/cm2-solver.ts` + `solver/_Cm2Solver.tsx`(`?event=cm2` dispatch)+ `gen/_svg/cm2_svg.ts`(2×2 球示意,每球画可见面色由取向推导 → solved 4 球同色自证;Cmetrick 非立方体,不套 net)+ `stats/_components/Cm2DistView.tsx`(全 165,888 态精确直方图 + 自造示例 + 下载全状态 CSV/单步数 txt,165,887 行 < 2M 上限免流式)+ `tests/cm2_solver.test.ts`(独立几何重推导 + 交叉验证,18/18 全绿)。接线 5 处同 A6(cstimer-scramble solvable:true / solver page dispatch / SolveTabs SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION / stats page event==='cm2' 分支 / ScramblePreview2D HAS_PREVIEW+dispatch)。**cstimer 记号校准**:vm 真跑 cstimer cm2 生成器 × 500 条(8000 token)→ 0 未知 token / 0 解析失败,distinct token 集恰 12 个。**move-model 保真度锚**:测试内从零用 3×3 旋转矩阵(RX/RY/RZ)重建 24 阶立方体旋转群 + 自有 composition/move 表/apply/BFS(只 import solver 公开 API,不抄移动置换表),断言 solver 的 12 个单 token 移动效果逐元素 == 几何推导(从 solved + 30 随机态各验)+ 互逆/自逆 sanity。门:**主 loop 独立复跑** vitest 18/18 绿 + typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(dev server DOWN @127.0.0.1:3000 + free RAM 紧,按硬约束不自启),`[untested-ui]` 债见 §4。
- [x] **A8** `dmd` 钻石 — `utilscramble.js:524`(PolyScrambler 通用 solver,~1e6 ⚠)。move U R L F(2 power)。
  完成 2026-06-20(commit `e174515f5`,UI 门欠账见 §4)。**实测可达态 = 138,240**(独立 BFS 枚举,== cstimer 自带 `SchreierSims` 的 `|G|`,**非**条目原估 ~1e6)。**上帝之数 10**,均值 6.6921,直方图(深度 0..10)`[1,8,48,288,1632,8568,36114,74799,16547,220,15]`(和 = 138,240)。**档 = TIER A**(现场全 BFS):dmd = 八面体(32 贴纸,每面 4 个三角子贴纸),但 8 面中只有 U/R/L/F 4 面可转,闭包很小;整图 BFS-from-solved memoized,<1s ≪ 1.5s 阈值。**移动语义**:dmd 是 cstimer `poly3dlib` 的 PolyScrambler **random-state** 魔方(`polyParam=[8,[-5,0],[],[-5]]` 八面体,经 `makePuzzle` 生成 `moveTable`),有真 apply + 真 solver → **存在真 round-trip oracle**。token 字母表恰 8 个 `U U' R R' L L' F F'`(4 面 × 2 power;每面 90° 转的 order = 3 故 `X'` = 基础生成元应用两次)。状态 = 32-int 排列(非取向);apply 约定 `newState[i]=state[perm[i]]`(与 cstimer permMult 同口径)。**保真度锚 = cstimer 真引擎 round-trip oracle**(本 loop 首个有真 solver 的 puzzle,最强锚):测试内 node:vm 加载 cstimer 真 `poly3dlib`+`grouplib`,(a)抽取 dmd `moveTable` 重建 4 生成元 → 逐元素 == solver 硬编码的 4 生成元(`-1`→identity 归一后 4/4 全等,证 solver 的 apply 等于 cstimer 真移动表);(b)cstimer 自带 `SchreierSims(gens).size()` == 138,240;(c)200 条真 `dmdso` 打乱 → token 集恰 8 个 / 0 解析失败 / 全可达 / 全 round-trip(scramble∘最优解=solved)。落地文件 = `lib/diamond-solver.ts` + `solver/_DiamondSolver.tsx`(`?event=dmd` dispatch)+ `gen/_svg/diamond_svg.ts`(八面体 2D 网图,每面三角 triforce 四分,8 个鲜明数据色由 state 推导 → solved 每面 4 子三角同色自证;非立方体 net)+ `stats/_components/DiamondDistView.tsx`(全 138,240 态精确直方图 + 自造示例 + 下载全状态 CSV/单步数 txt,138,239 行 < 2M 上限免流式)+ `tests/diamond_solver.test.ts`(独立参照 BFS + cstimer 真引擎 oracle,19/19 全绿)。接线 5 处同 A6/A7(cstimer-scramble.ts `solvable:true`,family 留 twist / solver page.tsx dispatch / SolveTabs.tsx SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION / stats page.tsx `event==='dmd'` 分支 + isIvy + distPuzzle / ScramblePreview2D HAS_PREVIEW+dispatch)。门:**主 loop 独立复跑** vitest 19/19 绿 + typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(RAM 紧,按硬约束不自启 dev server),`[untested-ui]` 债见 §4。
- [→D] **A9** `sia113` 联体 1×1×3 — `megascramble.js:53`。**实测误分类 → 重归 TIER D 决策批(并入 ⏸ soft-gate,loop 跳过继续 B1,2026-06-20)**。源是 `args2`/`formatScramble`(**非** `args`/`megascramble`),展开 = `#{[["U","u"],["R","r"]],["","2","'"],N} z2 #{[["U","u"],["R","r"]],["","2","'"],N}` —— 两块 `mega`(轴 {U,u} / {R,r},后缀 "" 2 ' = 12 token)中间夹字面 `z2`,**random-MOVE**(cstimer 无 apply / 无 solver / 无图 → 无 round-trip oracle)。**物理模型 = 两个 3×3×3 沿共享 1×1×3 棒(一条 3 小块的棱柱)粘连的 bandaged 双联体**:共享棒置于某 cube 的左下棱(x=0,z=0 沿 y),使 {U,u,R,r}(z=2/z=1 层 + x=2/x=1 层)恰好全避开共享棒、保持联体不撕裂;cube B = cube A 绕共享棒 y 轴 180°(x→−x,z→−z)粘上,z2 把 cube B 的自由层带进 {U,u,R,r} 转动位 → 生成群由 8 个基础层转(各 order 4)生成。**实测可达态 ≫ 50M(超 TIER A 2M 与 TIER B 50M 上限,远不可全 BFS)**:两条**独立**表示(① 带 24 取向的 cubie 置换 BFS;② 6 facelet/cubie 的 sticker 置换 BFS)都在**深度 6 累计 2,449,964 态、每层约 ×8 增长**(逐层 new = 24, 322, 3380, 31,273, 266,740, 2,148,224),深度 7 双双 4GB heap OOM;外推深度 7 ≈ ~15M、深度 8 ≈ ~80–100M+,总量量级 10⁸–10⁹、上帝之数估 ~13–16。**纯 TS 全 BFS 不可行**(无 dist 表 / 无全空间直方图 / 无「下载全部」)。**记号校准过**(node:vm 真跑 cstimer sia113 生成器 × 500 条 / 17,952 token → distinct token 恰 12 个 `U U' U2 u u' u2 R R' R2 r r' r2` + 字面 `z2`、0 解析失败),但**无代码可交**(executor 正确未在「太大」前提上动工,照 A5 dino 决策)。三条路放进 soft-gate 让用户选(详 §3):① 跳过只留打乱;② 近最优(两阶段 / 双立方 reduction,采样分布);③ 对称约简陪集表最优(立方体 24 旋转对称约简 ~10⁷ 量级,真工程但保最优)。**未写代码、未 commit。**

### TIER B — build 时预算表(~2e6–~5e7)

- [x] **B1** `gear` 齿轮魔方 — `gearcube.js`(原估 ~12,441,600 ⚠;cstimer 用 IDA* 非全表)。move U R F。**边界**:先试现场 BFS,>~1.5s 才落 `opt_gear.bin.gz`。
  完成 2026-06-20(commit `97e850156`,UI 门欠账见 §4)。**实测可达态 = 41,472**(独立 BFS 枚举 + cstimer 真引擎 round-trip oracle 复核;**非**条目原估 ~12.4M —— 那是 24×72³ 坐标乘积空间上界,真实齿轮魔方闭包只有 4 万态)。**档 = TIER A**(现场全 BFS,**非**预期的 TIER B):整图 BFS-from-solved ~173ms(cstimer 计步,33 邻居)≪ 1.5s 阈值,**无须落 `opt_gear.bin.gz` 预算表 / 无须流式下载**(41,472 ≪ 2M,直接 Blob)。**计步口径 = cstimer face-turn**(每个 scramble token = 一个轴的一次转,11 个 power):**上帝之数 6**,直方图 `[1,33,579,5921,18072,13977,2889]`(和 = 41,472)、均值 4.30317。**状态模型**(逐字段照抄 `gearcube.js`)= `[corner(0..23), e0(0..71), e1(0..71), e2(0..71)]`,solved `[0,0,0,0]`;cmv/emv 移动表由 `cornerMove`(acycle 2-cycle)/ `edgeMove`(setNPerm(4)+acycle moveEdges[m]+逐齿 tooth)生成,单步 `baseStep(s,m)`:`ns[0]=cmv[m][s0]; ns[i]=emv[(m+i-1)%3][s_i]`。单轴 period=12;token = baseStep 应用 `(a+1)` 次(a∈0..10),后缀表 `["'","2'","3'","4'","5'","6","5","4","3","2",""]` → 33 token(3 轴 × 11 power);`U'`=1 步、裸 `U`=11 步(=U⁻¹)。纯 TS 全图 BFS(memoized),套 Ivy/Diamond 范式。**保真度锚 = cstimer 真引擎 round-trip oracle**(gear 是 random-state、cstimer 有真 solver+apply,本 loop 第二个有真 oracle 的 puzzle):测试内 node:vm 加载真 `mathlib`+`scramble.js`+`gearcube.js`,200 条真 `gearso` 打乱用本 parser 解析 + apply + 全 reachable + scramble∘最优解=solved round-trip(scratch 阶段 300/300 通过;测试 oracle 块真跑非 skip),token 集 ⊆ 33-token 字母表。落地文件 = `lib/gear-solver.ts` + `solver/_GearSolver.tsx`(`?event=gear` dispatch)+ `gen/_svg/gear_svg.ts`(齿轮魔方 state 是 4 坐标非贴纸置换 → 按 §4 软网允许,画 schematic:3 轴齿轮盘 + 朝向 spoke + 4 角环 + raw-state 字幕,solved 全坐标 0 = 固定 canonical 自证,scramble∘最优解 60 随机 trial 复原)+ `stats/_components/GearDistView.tsx`(全 41,472 态精确直方图 + 自造示例 + 下载全状态 CSV/单步数 txt,41,471 行 < 2M 上限免流式)+ `tests/gear_solver.test.ts`(独立参照 BFS + cstimer 真引擎 oracle,19/19 全绿)。接线 5 处同 A8(cstimer-scramble.ts `solvable:true`,family 留 twist / solver page.tsx dispatch / SolveTabs.tsx SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION / stats page.tsx `event==='gear'` 分支 + isIvy + distPuzzle / ScramblePreview2D HAS_PREVIEW+dispatch)。门:**主 loop 独立复跑** vitest 19/19 绿 + typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(RAM 紧 + dev server DOWN,按硬约束不自启),`[untested-ui]` 债见 §4。
  > **TIER B 模式说明**:B1 实测落到 TIER A,故**未建立** prompt 预期的 build-time `.bin.gz` 预算表模式 —— gear 不需要它。B2/B3 仍须各自实测可达态:若同样 ≤2M 走 TIER A;首个真正 >2M 的单元才需建预算表模式(Node build BFS → `opt_<p>.bin.gz` Uint8 距离表 + 页面 `statsUrl()` fetch + inflate + 查表梯度下降 + §3 MANUAL scp 交接),范本届时由那个单元确立。
- [→D] **B2** `cm3` Cmetrick(full)— `megascramble.js:28`。**实测误分类 → 重归 TIER D 决策批(并入 ⏸ soft-gate,loop 跳过继续 B3,2026-06-20)**。物理 = **3×3 球阵(9 球)**,每球取向 ∈ 立方体旋转群(24 阶),是 cm2(2×2=4 球,A7)的 3×3 放大版:行移 U/E/D(top/mid/bottom 三行,各绕 Y 轴)+ 列移 R/M/L(right/mid/left 三列,各绕 X 轴),`<`/`^`=+90° `>`/`v`=−90° `2`=180°。token 字母表恰 **18 个** `U< U> U2 E< E> E2 D< D> D2 R^ Rv R2 M^ Mv M2 L^ Lv L2`(逐字段照抄 `megascramble.js:28` 的 `[[["U<","U>","U2"],["E<","E>","E2"],["D<","D>","D2"]],[["R^","Rv","R2"],["M^","Mv","M2"],["L^","Lv","L2"]]]`,`value.length===1` → `mega(value[0],[""],N)`;cstimer 无 apply/无 solver/无图 → **无 round-trip oracle**,只能验 token 集)。**实测可达态 ≫ 5×10⁷(超 TIER B 上限,纯 TS 全 BFS 不可行)**:由物理几何独立推导 18 个移动置换跑 bounded BFS(5M 节点硬上限 + heap 守卫防 OOM),**每层 new = [1,18,207,2160,20979,187101,1547470,3242064(深度 7 触 5M cap 中断)]**,深度 1–6 增长比 ~×8–11.5(11.50/10.43/9.71/8.92/8.27),**深度 6 累计已 1,757,936(逼近 TIER A 2M 上限)、深度 7 仅前 3.24M 新态就撞 5M cap**。维持 ~×8 增长外推:深度 7 ≈ 12M、深度 8 ≈ 90M、深度 9 ≈ 600M+,且 cm3(9 球)比 cm2(4 球,上帝之数 10)深得多 → **总量量级 10⁹–10¹¹**,远超 TIER A(2M)/TIER B(5×10⁷)上限。raw 上界 24⁹ ≈ 2.6×10¹²(parity 约束至多砍常数因子,不改量级)。朴素全 BFS dist 表(>GB)/ 全空间直方图 / 「下载全部状态」/ build 时 `.bin.gz` 预算表(B 档量级)全不可套。**记号校准过**(node:vm 真跑 cstimer cm3 生成器 × 500 条 / 12,500 token → distinct token 恰 18 个、0 未知 / 0 解析失败),但**无代码可交**(executor 照 A5 dino / A9 sia113 决策,正确未在「太大」前提上动工)。三条路放进 soft-gate 让用户选(详 §3):① 跳过只留打乱;② 近最优(reduction / 两阶段,采样分布);③ 对称约简陪集表最优(立方体 24 旋转 + 行列对称约简,真工程但保最优,且仍可能太大)。**未写代码、未 commit。**
- [→D] **B3** `233` 2×3×3 多米诺 — `megascramble.js:30`。**实测误分类 → 重归 TIER D 决策批(并入 ⏸ soft-gate,loop 跳过继续 B4,2026-06-20)**。源 `"233": [[[["U","U'","U2"]],["R2","L2"],["F2","B2"]]]`(`value.length===1` → `mega(value[0],[""],N)`);**token 字母表恰 7 个** `U U' U2 R2 L2 F2 B2`(顶面 U 90° 三 power,四侧面 R/L/F/B 仅 180°;cstimer 无 apply/无 solver/无图 → **无 round-trip oracle**,只验 token 集 + 解析有效性,node:vm 真跑 cstimer 233 生成器 × 500 条 / 12,500 token → distinct 恰 7 个、0 解析失败已校准)。**物理模型 = 2(高)×3×3 长方体**:U/D 两个大 3×3 面,仅顶面 U 转 90°(无 D token,cstimer 固定底面);四个侧面 R/L/F/B(各 2×3 矩形)只转 180°。从机制独立几何重推导(坐标 x∈{0,1,2}/y∈{0,1}/z∈{0,1,2},18 槽全在壳上,2 个面心(顶心/底心)永不动):16 个活动 cubie 分两个 8-轨道 = 8 角(4 顶 + 4 底)+ 8 棱(4 顶 + 4 底),U 90° 同 3×3 的 U 转(角循环角、棱循环棱),侧面 180° 交换顶↔底。已验 U⁴=identity、R2/L2/F2/B2 全自逆。**实测可达态 = 1,422,489,600(≈1.42 × 10⁹,= 8!·8!·7/8 = 40,320 × 35,280)**——独立 Schreier-Sims 在 16 活动点上算 |G| = 1,422,489,600(= 上界 8!·8! = 1,625,702,400 的 7/8,棱-角奇偶耦合砍 1/8);bounded BFS(compact 编码 + heap 守卫 1.8GB)独立佐证:每层累计 `[1,8,42,210,1017,4811,22435,103158,465422,2068198]`、增长比 ~×4.4–4.8 稳定,深度 9 累计已 2.07M 且远未完成(对照 |G| ≈14 亿,外推上帝之数 ~17–19)。**超 TIER A(2 × 10⁶)28× 倍于 TIER B(5 × 10⁷)上限**:朴素全 BFS dist 表(>GB)/ 全空间精确直方图 / 「下载全部状态」/ build 时 `.bin.gz` 预算表(B 档 ≤5×10⁷)全不可套,纯 TS 全 BFS 与 build 预算表均不可行。但 233 对称性强(立方体子群对称 + 棱角分离),**对称约简陪集表可能落到 ~10⁷–10⁸ 量级,理论上仍可做到最优**(但比 cm3 更接近可行边界,需先评估约简比)。三条路放进 soft-gate 让用户选(详 §3):① 跳过只留打乱;② 近最优(两阶段 reduction:先归约棱、再角,采样分布);③ 对称约简陪集表最优(真工程,且 1.42 亿即便约简后可能仍偏大)。executor 照 A5 dino / A9 sia113 / B2 cm3 决策**未在「太大」前提上动工、未写代码、未 commit**(token 校准 scratch 在 gitignored `.tmp/`,用后清)。
- [→D] **B4** `sia123` 联体 1×2×3 — `megascramble.js`。**实测误分类 → 重归 TIER D 决策批(并入 ⏸ soft-gate,loop 跳过继续 B5,2026-06-20)**。源是 `args2`/`formatScramble`(同 sia113):`#{[["U"],["R","r"]],%c,%l} z2 #{[["U"],["R","r"]],%c,%l}`——两块 `mega`(每 cube 轴 `[["U"],["R","r"]]` = 单 U 层 + R 面 + r 切片,后缀 `cubesuff`)中间夹字面 `z2`,**random-MOVE**(cstimer 无 apply/无 solver/无图 → 无 round-trip oracle)。物理 = 两个 3×3×3 沿共享 1×2×3 块粘连的 bandaged 双联体(比 sia113 少一个 `u` 自由层,更受限些)。**实测可达态 ≫ 5×10⁷(超 TIER A 2M 与 TIER B 5×10⁷ 上限,纯 TS 全 BFS 不可行)**:验证过的 surf-cubie+24 取向 rigid-body BFS(compact latin1 编码 + 4.5M 节点硬上限 + heap 守卫,无 slow-OOM;6 生成元全 order-4 sanity 过)逐层 new = `[1,18,180,1512,12319,99278,793104]`,增长比 ~×8(10.0/8.40/8.15/8.06/7.99 收敛到 8),深度 6 累计 906,412、深度 7 撞 4.5M cap;维持 ×8 外推:深度 8 ≈ 5.8×10⁷(跨过 TIER B 上限)、深度 9 ≈ 4.6×10⁸、深度 10 ≈ 3.7×10⁹ → 量级 **10⁸–10⁹**(与文档记录的 sia113「最少 bandage」10⁸–10⁹ 同档,sia123 略更 bandaged 但同量级)、上帝之数估 ~13–16。dist 表/全空间直方图/下载全部/build 预算表全不可套。**记号校准过**(node:vm 真跑 cstimer sia123 生成器 × 400 条 / 16,400 token → distinct 恰 10 个 `U U' U2 R R' R2 r r' r2` + 字面 `z2`、0 解析失败)但**无代码可交**(executor 照 A5 dino / A9 sia113 决策,未在「太大」前提上动工)。三条路放进 soft-gate 让用户选(详 §3):① 跳过只留打乱;② 近最优(双立方 reduction / 两阶段,采样分布);③ 对称约简陪集表最优(立方 24 旋转对称约简,真工程但保最优)。**未写代码、未 commit。**
- [→D] **B5** `sia222` 联体 2×2×2 — `megascramble.js`。**实测误分类 → 重归 TIER D 决策批(并入 ⏸ soft-gate,loop 跳过继续 B6,2026-06-20)**。源是 `args2`/`formatScramble`:`#{[["U"],["R"],["F"]],%c,%l} z2 y #{[["U"],["R"],["F"]],%c,%l}`——两块 `mega`(每 cube 轴 `[["U"],["R"],["F"]]` = 单层 3-gen,像 2×2×2,后缀 `cubesuff`)中间夹字面 `z2 y`,**random-MOVE**(无 round-trip oracle)。物理 = 两个 3×3×3 沿共享 2×2×2 块粘连的 bandaged 双联体(三族中**最 bandaged**,每 cube 仅 3 个外层单面可转)。**实测可达态 ≫ 5×10⁷**:同款 rigid-body BFS(6 生成元全 order-4 sanity 过)逐层 new = `[1,18,225,2700,32256,383844]`,增长比 ~×12(12.5/12.0/11.95/11.90 稳定),深度 5 累计 419,044、深度 6 撞 4.5M cap;维持 ×12 外推:深度 6 ≈ 5×10⁶、深度 7 ≈ 6.0×10⁷(跨 TIER B 上限)、深度 8 ≈ 7.2×10⁸、深度 9 ≈ 8.7×10⁹ → 量级 **10⁸–10¹⁰**(早层分支比 sia123/sia113 更快,因 z2 y 把 cube B 三轴全带进转动位)、上帝之数估 ~12–15。dist 表/全空间直方图/下载全部/build 预算表全不可套。**记号校准过**(node:vm 真跑 cstimer sia222 × 400 条 / 16,800 token → distinct 恰 11 个 `U U' U2 R R' R2 F F' F2` + 字面 `z2` `y`、0 解析失败)但**无代码可交**。三条路放进 soft-gate(详 §3):① 跳过 / ② 近最优(双立方 reduction,采样分布)/ ③ 对称约简陪集表最优(真工程)。**未写代码、未 commit。**
- [→D] **B6** `ctico` 二十面体(Icosamate)— `utilscramble.js:568`。**实测误分类 → 重归 TIER D 决策批(并入 ⏸ soft-gate,loop 跳过,2026-06-20)**。源 `adjScramble(["UL","UR","UrUl","FlFr","LBl","RBr"], [0x3f×6], len, minxsuff)`(6 个顶点 5 阶转,各 minxsuff 4 power,adj 全 `0x3f` = 6 面互邻 → 仅约束相邻 move 不重复;**random-MOVE**)。**但 ctico 在 cstimer `poly3dlib` 里有真几何 + 真引擎**(`getFamousPuzzle('ctico')` → `polyParam=[20,[],[],[-5,0]]` → `makePuzzle` 产 24 个 twisty 的 `moveTable`,80 facelet),= 深切顶点转的二十面体(像 dmd 的 A8 但 20 面),**故有真 oracle**。**实测 |G| = 71,128,171,054,676,736,000,000,000,000,000,000 ≈ 7.1×10³⁴(EXACT,用 cstimer 自带 `grouplib.SchreierSims(moveTable 生成元).size(true)` 在 0.05s 算出;24 生成元全 order-5)**——**远超原估 ~1e8(低估了 26 个数量级)与 TIER B 5×10⁷ 上限**。bounded BFS 佐证爆炸式增长:逐层 new `[1,72,2867,98640,3272880]`(增长比 ~×40/×34/×33),深度 4 累计 3.37M 即撞 heap 守卫。**绝非 B/D 边界,是深 D**:dist 表/全空间直方图/下载全部/build 预算表全不可套,纯 TS 全 BFS 与 build 预算表均不可行。**记号校准过**(node:vm 真跑 cstimer ctico 生成器 × 400 条 / 10,000 token → distinct 恰 24 个 `{UL,UR,UrUl,FlFr,LBl,RBr}×minxsuff`、0 解析失败)。三条路放进 soft-gate(详 §3):① 跳过 / ② 近最优(可复用 cstimer 真 random-state solver 当引擎,像 D1 省力路,采样分布)/ ③ 不现实(7×10³⁴ 无法对称约简到可枚举)。**未写代码、未 commit。**

### TIER C — 单实例 IDA* 最优

- [x] **C1** `15p` 15 数码 — `slide.js`(16!/2 ≈ 1.05×10¹³,太大无法全表)。每条打乱 IDA* + **walking-distance** 启发式出可证最短;God's number 80;采样直方图。预览 = 4×4 数字格。
  完成 2026-06-20(commit `3bf24f8f2`,UI 门欠账见 §4)。**档 = TIER C(单实例最优)**:状态空间 16!/2 = 10,461,394,944,000 太大无法 BFS/全表,改每条打乱现场 **IDA*(迭代加深 A*)+ 启发式 = max(Walking-Distance, 逆序距离 inversion-distance)**——两者都可采纳(WD 行列各自数纵/横向滑动、move 集不相交故行 WD + 列 WD 可加且不超真距;逆序距离 = ceil(行主序逆序数/3) + ceil(列主序逆序数/3));取 max 把单 WD 留下的近上帝之数怪物态从分钟级压到可接受。**上帝之数 80**(Korf et al. 2005,锁 `toBe`);**采样均值** ≈ 文献真值 52.6(Korf & Schultze 2005;UI 默认采 400 个均匀随机态,样本均值随 N 收敛)。**复用滑块核**:`lib/slider-puzzle.ts` 直接以 W×H=4×4 参数化驱动(state/legalSlides/applySlide/SLIDE_INVERSE/parseSliderScramble),**零重复移动逻辑**(与 8p A3 同一份记号源)。**最优性交叉验证**:测试对浅态(BFS 深度 ≤14,独立朴素 BFS 廉价)断言 IDA* 长度 == 独立 BFS 最短(0 mismatch / 500 次抽样);并断言 WD 与组合启发式在这些浅态上 ≤ 真距(0 admissibility 违反)——无需枚举深态即证启发式可采纳/最优。**记号校准**:node:vm 真跑 cstimer `15prp` 生成器 × 400 条真打乱过本滑块核(`U/D/L/R` + 压缩次数如 D2,与 8prp 同一字母表)→ 0 撞墙 / 0 非法 token / 0 非法排列(scratch 在 gitignored `.tmp/15p`,用后删)。落地文件 = `lib/slide15-solver.ts`(`solveSlide15`/`solveSlide15Grid`,WD 表 24,964 态一次 BFS 建好 memoized)+ `solver/_Slide15Solver.tsx`(`?event=15p` dispatch,求解放 setTimeout 异步 + 「求解中」spinner,深态不卡死)+ `gen/_svg/slide15_svg.ts`(4×4 数字格,非 net;solved 渲染规范 1..15 自证,套 slide8 范式)+ `stats/_components/Slide15DistView.tsx`(**采样**直方图:现场解 N 个均匀随机可解态分桶,**明确标注「非全空间」**,异步分批 + 可取消 + 重采;下载「样本」CSV `optimal_length,scramble`,**不**提供「下载全部状态」——10¹³ 不可枚举;注明 God 80 / 均值 52.6)+ `tests/slide15_solver.test.ts`(滑块核 4×4 复用 + 有效性 + 最优性交叉验证 + 可采纳性,13/13 全绿)。接线 5 处同 8p(cstimer-scramble.ts `solvable:true`,family `slide` → 自动进 NonWcaPuzzlePicker 下拉 / solver page.tsx dispatch / SolveTabs.tsx SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION / stats page.tsx `event==='15p'` 分支(采样,关掉全空间专属 UI,isIvy 早返回那套)+ distPuzzle / ScramblePreview2D HAS_PREVIEW+dispatch)。**实测时延**(combined heuristic,主 loop scratch):浅态(≤depth14)亚毫秒~数百 ms;深 80 步随机游走态中位 ~2.2s、p90 ~76s、最坏 ~167s(WD 留的长尾怪物——这是 TIER C 单实例求解的固有代价,prompt 已说明「一两秒属正常」)。门:vitest 13/13 绿、typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(dev server 在跑 HTTP 200,但 free RAM 仅 ~0.97GB < 2GB 阈 + 大求解器在跑,按内存约束不开 Chromium),`[untested-ui]` 债见 §4。**分布是采样(非全空间精确),已在 UI + 本条显式标注。**

### ⏸ soft-gate(TIER D) — RESOLVED

> **RESOLVED 2026-06-20 — 用户选 ③ ALL near-optimal**:TIER D 放行,loop 继续推进 D1a…D2l + 升级进决策批的
> 那批(dino / sia113 / cm3 / 233 / sia123 / sia222 / ctico)。全部走近最优契约(有效性 `scramble∘solution=solved`
> 必验 + 长度 ≤ 已知上界 / cstimer 同口径,记录质量/差距,采样直方图,不下载全状态)。
>
> **跑完 A/B/C 后停,问用户:** 下面大型扭转 / jumbling 魔方都只能**近最优**(或需重工程才最优),且**每个是独立中型工程**
> (各要自己的 reduction 法 / 两阶段 / 对称约简表,远超 Ivy 范式)。要不要逐个投入?哪些值得做、哪些只留打乱不做 solver?
> D1(可复用现成 solver)成本低,D2(从零写 reduction)成本高。等用户拍板再开 D 档。
> **追加(2026-06-20):`dino`(A5)实测 = A12 = 239.5M,误分类,并入本批**:选 ① 跳过 / ② 近最优(redi 两阶段)/ ③ 对称约简陪集表最优(~5–10M,真工程但保最优)。 **→ DONE 近最优(用户选 ③ ALL near-optimal):commit `bd503934b`,wrap cstimer dino solver,均值 9.53 步,见 A5→D 条目。**
> **追加(2026-06-20):`sia113`(A9)实测 ≫ 50M(深度 6 累计 2.45M、每层 ×8、量级 10⁸–10⁹),bandaged 双联体 3×3,误分类,并入本批**:选 ① 跳过只留打乱 / ② 近最优(双立方 reduction / 两阶段,采样分布)/ ③ 对称约简陪集表最优(立方 24 旋转对称约简 ~10⁷,真工程但保最优)。
> **追加(2026-06-20):`cm3`(B2)实测 ≫ 5×10⁷(深度 6 累计 1.76M、深度 7 仅前 3.24M 新态就撞 5M cap、每层 ×8–11.5、量级 10⁹–10¹¹,raw 上界 24⁹≈2.6×10¹²),3×3 球阵(9 球,cm2 的放大版),误分类,并入本批**:选 ① 跳过只留打乱 / ② 近最优(reduction / 两阶段,采样分布)/ ③ 对称约简陪集表最优(立方 24 旋转 + 行列对称约简,真工程但保最优,且仍可能太大)。
> **追加(2026-06-20):`233`(B3)实测 = 1,422,489,600(≈1.42 × 10⁹,= 8!·8!·7/8,独立 Schreier-Sims;bounded BFS 佐证每层 ×4.4–4.8、深度 9 累计 2.07M 远未完成),2×3×3 多米诺(顶面 U 90° + 四侧面 180°,7 token),误分类**:选 ① 跳过只留打乱 / ② 近最优(两阶段 reduction:先棱后角,采样分布)/ ③ 对称约简陪集表最优(棱角分离 + 立方子群对称,约简后可能落 ~10⁷–10⁸ 仍可最优,但需先评估约简比;比 cm3 更接近可行边界)。
> **追加(2026-06-20):`sia123`(B4)实测 ≫ 5×10⁷(量级 10⁸–10⁹,bounded BFS 每层 ×8、深度 6 累计 906K、深度 7 撞 4.5M cap、外推深度 8 ≈ 5.8×10⁷),bandaged 双联体(两 3×3×3 沿共享 1×2×3 块,10 token + z2),误分类,并入本批**:选 ① 跳过只留打乱 / ② 近最优(双立方 reduction / 两阶段,采样分布)/ ③ 对称约简陪集表最优(立方 24 旋转对称约简,真工程但保最优)。
> **追加(2026-06-20):`sia222`(B5)实测 ≫ 5×10⁷(量级 10⁸–10¹⁰,bounded BFS 每层 ×12、深度 5 累计 419K、深度 6 撞 4.5M cap、外推深度 7 ≈ 6.0×10⁷),bandaged 双联体(两 3×3×3 沿共享 2×2×2 块,三族中最 bandaged,11 token + z2 y),误分类,并入本批**:选 ① 跳过只留打乱 / ② 近最优(双立方 reduction,采样分布)/ ③ 对称约简陪集表最优(真工程)。
> **追加(2026-06-20):`ctico`(B6)实测 |G| = 7.1×10³⁴(EXACT,cstimer 真引擎 poly3dlib `makePuzzle` + `grouplib.SchreierSims().size(true)`,24 个 order-5 生成元、80 facelet;bounded BFS 佐证每层 ×33–40、深度 4 累计 3.37M),二十面体 Icosamate 深切顶点转,原估 ~1e8 低估 26 个数量级,绝非 B/D 边界而是深 D,误分类,并入本批**:选 ① 跳过只留打乱 / ② 近最优(可复用 cstimer 自带 random-state solver 当引擎,像 D1 省力路,采样分布);③ 对称约简最优不现实(7×10³⁴ 无法枚举)。

### TIER D1 — 省力:复用现成 solver(soft-gate 放行后)

- [x] **D1a** `mpyrso` 大金字塔 — `pyraminx.js`(~4.6×10¹¹+,cstimer 自带两阶段 solver)。**直接 wrap cstimer 的 solver** 当近最优引擎,不从零写。
  完成 2026-06-20(commit `8af309db6`,UI 门欠账见 §4)。**TIER D 首单元,确立 D 档省力路范式(wrap cstimer 自带 solver)**。求解法 = **复用 cstimer 自带两阶段 solver**(非从零写 reduction):扩 vendored `pyraminx.js` 的 `mpyr` IIFE 加 `solveScramble(scr)`(解析 body move + 角尖 tip → 建 `MpyrCubie` → 跑内部 `solveMpyr` 两阶段[phase-1 到陪集 ≤14、phase-2 到还原 ≤20]→ 角尖独立求逆),走 worker 桥(`scrambler.worker.js` 加 `op:'solve'` 路 + `lib/cstimer-scramble` 加 `cstimerSolve(id, scramble)`);`lib/mpyr-solver.ts` 的 async `solveMpyr(scramble)` 直调该桥,**无 BFS、无距离表**。**计步口径 = cstimer face-turn**(每 token=1 步,body U/Uw/B/Bw/R/Rw/L/Lw + 4 角尖 u/r/l/b);**近最优(非可证最优)**。**典型近最优解长度**(60 条真随机态实测):**均值 ≈ 21.4,min 17,max 24**(N=24 的测试稳定复现同档),loose 上界 38(14+20+4)。**有效性测试结果 = 全部 round-trip(scramble∘solution=solved)**:测试 node:vm 加载真 cstimer 引擎,生成 N 条真 `mpyrso` 打乱,`mpyr.roundTripCheck` 逐条验 scramble∘solution=solved → 24/24 全过 + 长度 ≤ 38 + 解 token ⊆ 字母表;另 `mpyrFacelets`(TS 预览端口)逐 move 对齐真引擎 facelet(80 序列 + 离线 3000 序列 fuzz 0 差异)。落地文件 = `lib/mpyr-solver.ts`(async wrap + 52-facelet 端口)+ `solver/_MpyrSolver.tsx`(`?event=mpyrso` dispatch,async 解 + spinner)+ `gen/_svg/mpyr_svg.ts`(4 面三角网图 derived from state,solved 每面单色自证,§4 软网允许)+ `stats/_components/MpyrDistView.tsx`(**采样**近最优直方图:现场生成+解 N 个随机态分桶,明确标「采样/近最优/非全空间/非可证最优」,只下载样本 CSV,**无下载全部**)+ `tests/mpyr_solver.test.ts`(真引擎 oracle,6/6 全绿)。接线 5 处同 A 档(cstimer-scramble.ts `solvable:true` family:'twist' / solver page.tsx dispatch / SolveTabs.tsx SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION / stats page.tsx `event==='mpyrso'` 分支 + isIvy + distPuzzle / ScramblePreview2D HAS_PREVIEW+dispatch)。**D 档省力路结论(给后续 wrappable 单元 D1b/ctico)**:cstimer 自带 solver 干净可调 —— `MpyrCubie`/`solveMpyr`/`move2str` 都在 IIFE 内,但通过给 vendored 文件加 export(`solveScramble`/`roundTripCheck`/`faceletsOfScramble`)即可从 worker / node:vm 驱动任意状态,无须重写引擎。门:vitest 6/6 绿、typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(RAM 紧 + 无 dev server,按硬约束不自启),`[untested-ui]` 债见 §4。
- [x] **D1b** `crz3a` 疯狂 3×3 — `megascramble.js:27`(标准 3×3 移动集,~4.3×10¹⁹)。**复用站内 3×3 求解器**(kociemba/cubeopt);评估是否值得单开页(可能只是 333 换皮)。
  完成 2026-06-20(commit `6268157ee`,UI 门欠账见 §4)。**确认 crz3a = 标准 3×3 换皮**:megascramble.js:27 `[[["U","D"],["R","L"],["F","B"]],cubesuff]` = 标准 18 move(U/D/L/R/F/B × {"",2,'}),"crazy" 只是表现层,底层就是普通 3×3(~4.3×10¹⁹ 态)。**求解法 = 直接复用站内 client 端 kociemba 两阶段求解器**(`solver/_kociemba/{cube,movetables,prune,search}`),**非** cubeopt(那是 COEP,只给 event=333)、**非** cstimer、**非**从零写引擎。**接法 = 直接 import 搜索函数**(不走 worker):`lib/crz3a-solver.ts` 在模块作用域 lazy + memoized 建一次 move/prune 表,`async solveCrz3a(scramble)` 用 kociemba `parseMoves`(直接吃标准 HTM,零记号翻译)→ apply 到 solvedCubie → `solveCube()` → `formatMoves(sol)`(**用解本身、不 invert**,故 scramble∘solution=solved);纯 TS 无 worker/无 DOM → Node vitest 可直接 import 跑。**计步口径 = HTM face turn;近最优(非可证最短)**——kociemba 两阶段默认 maxTotalLen 23。**典型近最优解长度**(22 条真 cstimer crz3a 打乱实测):**均值 ≈ 20.82,min 18,max 23**(loose 上界 26)。**有效性测试结果 = 全部 round-trip**:tests/crz3a_solver.test.ts 用 node:vm 跑真 cstimer crz3a 生成器(len 25)生成 22 条真打乱,solveCrz3a 解后用**独立**的 3×3 重 apply(从 kociemba cube.ts import parseMoves/applySequence/solvedCubie/isSolvedCubie 作 oracle,与求解器同模型但独立校验)断言 scramble∘solution=solved → 22/22 全过 + 长度 ≤ 26 + solution token 全 `/^[URFDLB][2']?$/` + 空打乱→len 0;无最优性断言。落地文件 = `lib/crz3a-solver.ts`(direct-import kociemba wrap,无 BFS/无新表)+ `solver/_Crz3aSolver.tsx`(`?event=crz3a` dispatch,async 解 + spinner,仿 D1a)+ `stats/_components/Crz3aDistView.tsx`(**采样**近最优直方图,DEFAULT_SAMPLE 150,明确标「采样/近最优/等价标准 3×3/kociemba 两阶段非可证最优」,只下载样本 CSV)。**预览复用现成展开网图**(不新写 `_svg/crz3a_svg.ts`):给 `gen/_svg/cube_unfolded_svg.ts` 的 EVENT_TO_PUZZLE 加 `crz3a:'3x3x3'` → `eventToCubeSize('crz3a')===3` → ScramblePreview2D 的 cube-size 路径自动渲标准 3×3 网(solved 每面纯色自证)。接线 6 处(grep mpyrso 同款加 crz3a):cstimer-scramble.ts `solvable:true`(family `'other'`,NonWcaPuzzlePicker 的一个真实有序分组)/ solver page.tsx dynamic+dispatch / SolveTabs.tsx SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION / stats page.tsx import+isIvy+distPuzzle+`event==='crz3a'` 分支 / ScramblePreview2D HAS_PREVIEW(走 cube-size 路无需新 dispatch 行)。门:**主 loop 独立复核** `git show --stat 6268157ee` 证 10 文件全在 commit(4 新:crz3a-solver.ts/_Crz3aSolver.tsx/Crz3aDistView.tsx/crz3a_solver.test.ts)+ vitest 4/4 绿 + typecheck(tsgo)EXIT=0;Playwright 实时 UI 门跳过(RAM 紧 + dev server,按硬约束不自启),`[untested-ui]` 债见 §4。**结论:crz3a 确是 333 换皮,但复用 kociemba 成本极低(direct-import,~90 行 lib + 仿 D1a 接线),单开页有价值(独立 puzzle 入口 + 采样分布),已做。**

### TIER D2 — 从零 reduction,18 单元(soft-gate 放行后,**易→难有序,可行性各单元再实测确认**)

> 全走 §0.6 从零 reduction(无 cheap-win,见 §3 审计);均近最优契约。`[→D]` 单元的实测态数 / 记号校准已在原 §1 条目(TIER A/B 段)+ §3 在案,这里是**工作顺序的权威表**;`[ ]` 单元从没动过,先按 §0.0 #2 量闭包。每条:client id / 名 / cstimer 源 / 量级 / 切入法。

**① 长方体(层 reduction,最可行)**
- [x] **D2-1** `233` 2×3×3 多米诺 — `megascramble.js:30`(7 token)。实测 1.42×10⁹(8!·8!·7/8)。**升级到 TIER C(单实例可证最优)而非记录的 D 近最优**:8 角(8!)+ 8 棱(8!)两独立轨道,各建满 PDB(角 maxdepth 13、棱 maxdepth 11,各 ~0.5s BFS),IDA* + max(角,棱) 可采纳启发式出可证最短解。完成 2026-06-21(commit `1ffdee851`,UI 门欠账见 §4)。实测均值 13.7、样本最长 16、深态最坏 ~0.4s。
- [x] **D2-2** `334` 3×3×4 — `megascramble.js:9`。**TIER D 两阶段约简(near-optimal hybrid),100% 解真 cstimer 打乱**。状态空间 ≈ 1.65×10¹⁷(群阶 2.64×10¹⁸,**均超 2^53 → 所有大计数走预格式化字符串**;/2 奇偶因子经独立 Schreier-Sims + GF(2) 秩复核确认 = 1 个轨道间奇偶约束)。phase-1 把所有块归约进全 180° 子群(weighted-A*,启发 = max(intoH₁, intoH₃, intoH₀+intoH₂),两独立角轨道用加性项,W=2);phase-2 只用 8 个 180° 转还原(IDA*,启发 = 两轨道对的精确 joint PDB max(dist₀₂, dist₁₃),in-H 子集 96/576/96/36/2 极小);浅态另跑 max(optDist) 可证最优捷径(`optimal:true`,radius≤4 全 8000+ 态穷举 == BFS 深度)。**实测 30 条真 cstimer 打乱 30/30 round-trip(独立几何 oracle),中位 ~27 步、max ≤34(<硬上界 CUBOID334_MAX_LENGTH=40),最坏单解 ~1s**。基线 `07e00099f` 的单实例 optimal-or-`too-deep` 解器在 30/30 真打乱上全抛 too-deep(实测,不合格)→ 改两阶段。完成 2026-06-21(见 §2,`[untested-ui]`)。
- [x] **D2-3** `335` 3×3×5 — `megascramble.js:56`(`#{cuboid} / ${333}`)。**TIER D 两阶段约简(near-optimal hybrid),100% 解真 cstimer 打乱**。关键发现:335 几何与 334 不同 —— 物理 3×3×5 只有 **10 个刚体转** `U U' U2 D D' D2 R2 L2 F2 B2`(U/D 顶底 3×3 盖转 90°;四侧面是 3×5 长方形,90° 侧转会把块转出盒外→几何不可行,只 180°,**维度证明 + 离表两证 + Schreier-Sims 三方独立确认**)。cstimer 的 `/ ${333}` 后缀含 90° 侧转(R R' F…),在 3×3×5 上**无刚体实现**(cstimer 本身不渲染 335 图,renderer 无 335 分支),是「把颜色当 3×3×3 打乱」的人类速记 → 解析到 `/` 即止,只解前半段刚体打乱(真打乱的物理态 = cuboid part)。可达态 **156,067,430,400**(≈1.56×10¹¹,Schreier-Sims 实算;轨道乘积 40320²·24³ = 22,473,709,977,600 被 144× 奇偶耦合过计;均存预格式化字符串)。5 个可动轨道 sizes `[40320,40320,24,24,24]`、inH `[96,576,24,24,24]`、intoH 深度 `[11,4,0,0,0]`(只角轨道+顶底棱轨道需 phase-1 约简,三个 wing 轨道恒在 H)。phase-1 = weighted-A*(W=2,启发 max(intoH),**两级数字闭包键** 外=角·40320+棱<2³¹ 内=三 wing<24³,无字符串垃圾;cap 250k→greedy 兜底);phase-2 = IDA*(joint-pair PDB max(dist₀₁[96·576], dist₂₃[24·24], p2[wing₄]),几 ms 收敛);浅态另跑 max(optDist) 可证最优捷径(`optimal:true`,radius≤4 全 3613 态 == BFS 深度)。**实测 1000 条真随机态 1000/1000 round-trip(独立几何 oracle)、8 条真 cstimer 打乱 8/8、中位 ~20 步、max ≤27(<硬上界 CUBOID335_MAX_LENGTH=45),最坏单解 ~0.5s(DB 一次性构建后)**。比 334 更简单(单角轨道 phase-1,无 334 的双独立角轨道加性启发问题)。完成 2026-06-21(见 §2,`[untested-ui]`)。
- [x] **D2-4** `336` 3×3×6 — `megascramble.js:10`。cuboid reduction。**TIER D 两阶段约简(near-optimal hybrid),100% 解真 cstimer 打乱**。⚠ 与 335/337 几何不同:cstimer 的 336 是**纯 case-1 mega 无 `/333` 后缀**,21 token 全可刚体实现(`U U' U2 / u u' u2 / 3u 3u' 3u2 / R2 L2 M2 / F2 B2 S2`,顶三层 90° 盖/片 + 六侧 180° slab,底三层固定,无 D/d/3d),故整条打乱即物理态、解能真正解开。可达态 **8,391,762,413,094,961,152,000,000**(≈8.39×10²⁴,Schreier-Sims;轨道乘积 33,567,049,652,379,844,608,000,000 被 2² 秩-2 奇偶耦合过计;facelet 群阶 2,148,291,177,752,310,054,912,000,000 = 物理 × 256;均预格式化字符串)。7 可动轨道 sizes `[40320,40320,40320,2520,40320,2520,2]`,phase-1 weighted-A* 把每层对归约进全 180° 子群 H,phase-2 weighted-A* 用 7 张重叠 EXACT joint-triple PDB bank(~16M cells)max 启发,浅态 max(optDist) 可证最优捷径。集成镜像 D2-5 337(五处接线 + 离线分布 retrofit)。门:vitest `cuboid336_solver.test.ts` 18/18 绿 + typecheck(tsgo)EXIT=0。**分布离线预计算**(build_puzzle_sampled_dist.ts REGISTRY 加 336 行 N=2000 + update_puzzle_stats.ps1 `$SAMPLED_DIST_EVENTS` 加 336;Cuboid336DistView 改纯 fetch dist_336.json,零求解器 import — dist_336.json 由主循环跑 build 单独提交)。完成 2026-06-21(见 §2,`[untested-ui]`)。
- [x] **D2-5** `337` 3×3×7 — `megascramble.js:57`。cuboid reduction。**TIER D 两阶段约简(near-optimal hybrid),100% 解真 cstimer 打乱**;同 335 几何(侧面 3×7 长方形,90° 侧转几何不可行 → 刚体转只 16 token `U U' U2 u u' u2 d d' d2 D D' D2 R2 L2 F2 B2`,U/D 顶底盖 + u/d 内层片 90°,四侧 180°)。可达态 **126,859,598,081,556,480,000**(≈1.27×10²⁰,Schreier-Sims;轨道乘积 40320³·2520·24³ = 2,283,472,765,468,016,640,000 被 18× 奇偶耦合过计;均预格式化字符串)。7 可动轨道 sizes `[40320,40320,40320,2520,24,24,24]`、inH `[96,576,96,36,24,24,24]`、intoH 深度 `[11,4,11,4,0,0,0]`。两阶段 reduction(phase-1 weighted-A* 归约进全 180° 子群,phase-2 IDA* joint-pair PDB,浅态 max(optDist) 可证最优捷径)。**320+ 条真 cstimer 打乱 100% solve、独立几何 oracle round-trip;实测 mean ~0.55s、median ~0.25s、最坏 ~4.4s(<硬上界 CUBOID337_MAX_LENGTH=80)**。分布**离线预计算**(dist_337.json,build_puzzle_sampled_dist.ts REGISTRY,DistView 纯 fetch,零求解器 import)。完成 2026-06-21(见 §2,`[untested-ui]`)。

**② Square 系(两阶段,可学已有 sq1 求解器)**
- [x] **D2-6** `sq2` 方块二 — `utilscramble.js:552`((u,d)/ tuple 生成器)。**TIER D**:实测 cstimer 可达态 = 12·18! = 7.68×10¹⁶(Schreier-Sims),单阶段 IDA* >300s 爆。做法=构造式 3-cycle reduction(parity fix + 固定 base macro 经深度6 conjugator 表路由到任意有序三元组);cstimer 只有打乱生成器无 solver,oracle 从几何独立重导。**质量桶=有效+有界(非近最优)**:均值 ~70 元组、max 95(20k 样本)、`SQ2_MAX_LENGTH=130` 已 assert(不重蹈 336 无界 bound 覆辙)。vitest 9/9、tsgo+tsc EXIT=0、Playwright 桌面+390px 0 error、dist 直方图和 2000==sampleCount。完成 2026-06-22(commit `3943e4bd2`)。dist 发布留 §3 MANUAL。
- [x] **D2-7** `ssq1` 超 Sq-1(cstimer key `ssq1t`)— `utilscramble.js:563`。**TIER D**:实测 = 两个耦合的 Square-1(共用 `/` 切片),cstimer 用 4 元组 `(a,b,c,d)/` 驱动(P0 转 a,d + P1 转 b,c 后双面切片);可达态 ≈1.15×10²⁵(两面乘积,预格式化字符串)。做法=**真两阶段约简**(非逆退打乱):形状 BFS(单面 3,678 形状/直径 14 → 方块形)+ 角排列 8! 表 + 棱排列偶群 A8=20,160 + 一个构造出的奇偶校验生成元(Square-1 经典棱奇偶,角归位后棱可奇排列、无短纯 3-循环),两面在共享切处用恒等词填充对齐切数;cstimer 只有打乱生成器无 solver,oracle 从几何独立重导(两面 24-槽模型)。**质量桶=有效+有界(非近最优)**:2000 样本 mean 28.1 / median 24 / min 13 / max 41(双峰:约半数需奇偶校验→更长),`SSQ1_MAX_LENGTH=60` 已 assert。vitest 9/9(500 随机 + 200 真 cstimer round-trip + 「>5 bin」展开断言)、tsgo+tsc EXIT=0、dist 27 bin 和 2000==sampleCount。完成 2026-06-22(初版 `35a8b7ac8` 求解器偷工=逆退打乱致分布退化单柱,主 loop 复核打回;返工 `4451d00fa` 换真约简)。dist 发布留 §3 MANUAL。UI 已实测(见 §4)。
- [ ] **D2-8** `bsq` 受限 Sq-1 — `utilscramble.js:565`(`sq1_scramble(2,len)`)。两阶段。

**③ 中型 reduction**
- [→D] **D2-9** `cm3` Cmetrick(full)— `megascramble.js:28`(18 token,9 球阵)。量级 10⁹–10¹¹;reduction / 两阶段。
- [ ] **D2-10** `bic` 联体魔方(Bicube)— `utilscramble.js:88`(`bicube` doMove,23 facelet)。先量闭包再定法。
- [→D] **D2-11** `sia113` 联体 1×1×3 — `megascramble.js:53`(12 token + `z2`)。量级 10⁸–10⁹;双立方 reduction。
- [→D] **D2-12** `sia123` 联体 1×2×3 — `megascramble.js:54`(10 token + `z2`)。量级 10⁸–10⁹;双立方 reduction。
- [→D] **D2-13** `sia222` 联体 2×2×2 — `megascramble.js:55`(11 token + `z2 y`)。量级 10⁸–10¹⁰;双立方 reduction。

**④ 最难 —— 「有效 + 有界」是现实天花板(jumbling / 天文量级,§0.0 #3)**
- [ ] **D2-14** `prcp` 五魔金字塔 — `utilscramble.js:533`(`pochscramble`,~1.67×10¹⁷;有 poly3dlib `prc` 几何当 oracle)。reduction/Pochmann。
- [ ] **D2-15** `giga` 六阶五魔 — `utilscramble.js:461`(`gigascramble`,>1e18;有 poly3dlib `giga` 几何)。reduction。
- [→D] **D2-16** `ctico` 二十面体 Icosamate — `utilscramble.js:567`(实测 |G|=7.1×10³⁴;有 poly3dlib `ctico` 几何 apply oracle,**但无 solver**)。reduction,接受有界。
- [ ] **D2-17** `heli` 直升机 — `utilscramble.js:473`(`adjScramble`,>1e8 + **jumbling**;有 poly3dlib `heli` 几何)。接受有界。
- [ ] **D2-18** `helicv` 弧面直升机 — `utilscramble.js:474`(同 heli adj,**jumbling**)。接受有界。

---

## §2 进度日志(每单元一行:日期 / 单元 / commit 短 hash / 一句话)

- 2026-06-22 / D2-7 / `4451d00fa`(初版 `35a8b7ac8` 被打回) / `ssq1` 超级方块一纯 TS 求解器,TIER D 真两阶段约简;cstimer `(a,b,c,d)/` 四元组 = 两个耦合 Square-1(共享切片),可达态 ≈1.15×10²⁵;约简=形状 BFS(单面 3,678 形状)+ 角 8! 表 + 棱 A8 + 构造奇偶校验生成元,两面恒等词对齐切数;质量桶有效+有界(2000 样本 mean 28.1/median 24/max 41,双峰),`SSQ1_MAX_LENGTH=60` assert;独立两面几何 oracle round-trip;接 solver/stats/preview/SolveTabs/picker/dist 脚本 13 处。**初版求解器偷工(直接返回打乱逆 `scramble⁻¹` → 只原路退回不解任意态 → 分布退化成单柱 `{'11':2000}`),主 loop 复核(读代码 + 查 dist JSON)打回,返工换真约简**(我独立 BFS 实证单面形状空间仅 3,678 态/直径 14、角棱各 8!,两阶段约简纯 TS 可行,推翻初版「搜索会爆故只能逆退」的错误前提)。vitest 9/9 + tsgo+tsc EXIT=0(主 loop 复跑,无管道吞 exit;ssq1 文件零 error,脏树里别的 AI 的 sim/timer WIP 不影响)+ Playwright UI 实测(求解器出变长解、分布页双峰展开、0 console error)。

- 2026-06-20 / A0 / `6265c3671` / 非 WCA 分组选择器 `NonWcaPuzzlePicker`(数据驱动 family + solvable),接 solver/stats 两页,ivy 走分组下拉 proof;typecheck 绿,UI 门欠账。
- 2026-06-20 / A0-review / `6ba92bf18` / 复核 6265c3671:executor 的 typecheck EXIT=0 属实,预警的错误(缺 family / 悬空 SOLVE_EVENTS/SOLVE_APPEND/IVY_DIST_APPEND / 暴露未用导入)均不存在;30 event 全有 family、仅 ivy solvable、守卫测试全绿。唯一清理 = 删 cstimer-scramble 里早已存在的死 i18n 导入。UI 门仍欠账(见 §4)。
- 2026-06-20 / A1 / `7a2a56243` / `133` 1×3×3 花型纯 TS 全 BFS 最优解;实测 192 态(非估 6,144)、上帝之数 8、均值 4.43;套 Ivy 范式接 solver/stats/preview/picker;vitest 11/11 + typecheck EXIT=0 + Playwright UI 验过(零 console error)。**顺带落库 A0 遗留未提交的 Ivy proof 全套文件**(此前 ivy 实现一直只在工作树、从未 commit)。
- 2026-06-20 / A1-review / `7a2a56243` / 独立复核 A1:executor 全部声明属实,**无真错可修**(零代码改动)。tsgo EXIT=0 + tsc -b EXIT=0(两者一致,不存在 prompt 预警的 `133` 缺失于 `SolvePuzzle`/`EVENT_ID`/`PUZZLE_BY_EVENT` 或 `floppy_svg.ts` 悬空 `MOVE_PIECES` —— `MOVE_PIECES` 实为 `floppy-solver.ts` 导出常量、svg 仅 import `floppyApply` 用本地 `CORNER_SLOTS`,预警系陈旧);vitest 11/11 绿(断言 192 态 / God 8 / 直方图 `[1,4,10,24,53,64,31,4,1]` / 独立 BFS 交叉验证 / 400 随机解最优性 + 有效性);移动语义与 cstimer `1x3x3.js` 逐字段一致(movePieces 2-cycle + 逐轴 flip)。git HEAD 确认 ivy + floppy 全套文件 + NonWcaPuzzlePicker 均已入库,fresh checkout 一致。Playwright 实测 `/scramble/solver?event=133`(预览 21 rect,R L F B R → L B F 3 步)+ `/scramble/stats?event=133`(图表 + 均值 4.43/中位 5/众数 5/God 8 + 12 示例 + 全 192 态)桌面 + 390px(无横向溢出),两页零 console error。
- 2026-06-20 / A2 / `7c30974d7` / `223` 2×2×3 塔纯 TS 全 BFS 最优解;实测 241,920 态(== 40,320×6 全索引)、上帝之数 14、均值 9.7372、整图 build+BFS ~194ms → TIER A;套 Ivy/Floppy 范式接 solver/stats/preview/SolveTabs/picker;32 贴纸网图(真 3D 旋转推出、U⁴=D⁴=R²=F²=id 已验)solved 自证;vitest 11/11 + typecheck EXIT=0;UI 门欠账(RAM 紧跳过 Playwright,路由 HTTP 200)。
- 2026-06-20 / A3 / `fa5d301f2` / `8p` 八数码纯 TS 全 BFS 最优解(滑块类);实测 181,440 态(== 9!/2)、上帝之数 31、均值 21.9724;**新建可复用滑块核 `lib/slider-puzzle.ts`**(W×H 参数化,15 数码 C1 将继承)+ `lib/slide8-solver.ts`;记号 U D L R 经 1000 真打乱跑 cstimer slideMove 实测锁定(零撞墙);数字格预览(非 net)solved 渲染 1..8 自证;接 solver/stats/preview/SolveTabs/picker;vitest 13/13 + typecheck EXIT=0;UI 门欠账(RAM 紧跳过 Playwright,三路由 HTTP 200)。
- 2026-06-20 / A3-review / `fa5d301f2` / 独立复核 A3:executor 全部声明**属实**,**无真错可修**(零代码改动)。tsgo EXIT=0 + tsc -b EXIT=0(prompt 预警的 `8p` 缺失于 `SolvePuzzle`/`EVENT_ID` 全是陈旧 LSP 幻报);vitest 13/13 绿。**交叉验证 BFS 确为独立**(自带 `State{g,blank}`/`keyOf`/`expand`/`slide`/`applySeq`,从 cstimer spec 重写、不调 solver 的 apply;round-trip 用参照 applySeq 不用 solver)→ 181,440(`toBe`)/ God 31(`toBe`)/ 直方图(`toEqual`)/ 和 181,440(`toBe`)均真锁。**记号保真度独立硬核验**(本 review 新增):用 vm 真跑 cstimer `8prp` scrambler(utillib shim + isaac + mathlib + scramble.js + slide.js),2000 条真打乱过 A3 的 parser/apply:0 解析失败、0 撞墙、token 恰 {D,L,R,U}、全可达、**0 round-trip 失败**(scramble∘独立最优解=solved),采样均值 21.94(吻合全空间 21.97)→ executor「1000 条零撞墙」声明经 2000 条复现且加强。**可复用滑块核 `lib/slider-puzzle.ts` 已就绪供 C1**:全 W×H 参数化(无 3×3 硬码泄漏)、暴露 state/`legalSlides`/`applySlide`/`solvedSlider`/`blankValue`/`SLIDE_INVERSE`(IDA* 剪枝)/`parseSliderScramble`/`sliderApply`;不含 rank/unrank 是对的(那是全 BFS 专属,在 slide8-solver;16!/2 无法索引,C1 走 WD 启发式自带)。SVG solved=1..8 自证、CSV 头 `optimal_length,scramble`+identity 行均符。守卫:真 `<button>`/`AppLink`、`tr({zh,en})`(无 isZh 文案三元)、theme token/数据蓝 hex、nuqs。**UI 门仍欠账**:free RAM 仅 ~1.3GB(< 2GB 阈)+ 大求解器在跑,按内存约束不开 Chromium,`[untested-ui]` 债保留(见 §4)。无 scratch 文件入库(`.tmp/verify8p_notation.cjs` 已 gitignore + 用后删)。
- 2026-06-20 / A2-review / `99d35c223` / 独立复核 A2:executor 全部声明**属实**。tsgo EXIT=0 + tsc -b EXIT=0(prompt 预警的 `223` 缺失于 `SolvePuzzle`/`EVENT_ID`/`PUZZLE_BY_EVENT`/`HAS_DISTRIBUTION` 均为陈旧 LSP 幻报,源码全有);交叉验证 BFS **确为独立**(从 cstimer spec 用 circle() 重写、不调 solver 的 apply)→ 241,920 态 / God 14 / 直方图 `[1,8,...,576]` 是真核验。另跑全独立 node 脚本(5000 随机打乱)再证:SVG 网每色计数恒定 + scramble∘solver解 渲染回 solved 网(把手写 PERM 表绑到已验 solver),0 fail;3 条真长度打乱独立验 scramble∘sol=solved 且最优。**唯一修复 = 给测试补强**:原 SVG 测试只验 solved 纯色 + R2·R2=id(弱,手写 PERM 回归不会被抓),加一条「网=忠实群作用、随 solver 联动」交叉验证(face-count 不变 + scramble∘解=solved 网),vitest 11→12 全绿。**UI 门补齐**:Playwright 实测两路由桌面+390px、网图渲染、零 console error,§4 欠账清,`[untested-ui]` 债销。
- 2026-06-20 / A4 / `d29dea86b` / `sfl` 超薄花型纯 TS 全 BFS 最优解;实测 3,041,280 态(= P(12,4)×4⁴,对上 jaapsch.net 权威值)、上帝之数 13、均值 9.0040、整图 build+BFS ~573ms → TIER A;move 字母表 12 token(2 轴 {R,L}/{U,D}×后缀,几何独立推导逐面置换+取向);套 Ivy/Floppy/Cuboid223 范式接 solver/stats/preview/SolveTabs/picker;平板网图 solved 自证;vitest 12/12 + typecheck EXIT=0;cstimer 记号 vm 真跑 ×2000 条零失败;UI 门欠账(无 dev server 跳过 Playwright)。
- 2026-06-20 / A4-review / `e4abc9bfb` / 独立复核 d29dea86b:executor 核心声明**全部属实**(3,041,280 态对上 jaapsch.net、God 13、均值 9.00、TIER A、记号匹配 cstimer megascramble.js:33 的 `[[["R","L"],["U","D"]],cubesuff]` = 12 token),tsgo + tsc EXIT=0,vitest 绿。**交叉验证 BFS 确为独立**:从 3D 旋转矩阵(rotX/rotY 作用于坐标槽)重算 `REF_CPERM`,**不**调 solver 的 `CORNER_PERM`,自带 rank/unrank + 自建 move 表;3,041,280 / God 13 / 全直方图均 `toBe`/`toEqual` 真锁。**修了 1 个真缺陷(STEP 2 下载规模,本 loop 首次在 3M 量级暴露)**:原 `downloadAll` 一次性建 3.04M 元素记录 + `lines.join('\n')` 拼 ~80MB 大字符串(三份多 MB 峰值,移动端可卡死/OOM),且大档(9/10 步各 ~1M 条)的单步数 txt 也强制建全 3M 记录。按 §0.5 >2M 下载策略改:整包 CSV 先 `window.confirm` 再**流式**生成(新 `streamSuperFloppyScrambles` 逐状态枚举 → 64k 行/块的分块 Blob parts,峰值内存有界,绝不拼整串);单步数 txt 走 `superFloppyScramblesForLength` 同样流式;直方图/示例本就懒算(`superFloppyExamplesByLength(12)` per-bin,非全 3M)。图建一次(模块级 `GRAPH` 单例),只首次交互付 573ms。新增测试锁「流式语料 == eager 语料 per depth」(12→13/13 绿)。**UI 门仍欠账**:RAM 3.96GB 够,但 `127.0.0.1:3000` 无 dev server,按硬约束不自启,Playwright 跳过,`[untested-ui]` 债保留(见 §4)。无 scratch 文件入库;只 commit 自己改的 3 个 A4 文件(其余 analyzer/RecentScrambles/WcaEventSelector 等是别的 AI 的 WIP,未碰)。
- 2026-06-20 / A5 / (无 commit) / `dino` ⛔ 卡档位边界:实测 cstimer 模型可达态 = A12 = 239,500,800(非估 ~35,640;子 agent Schreier-Sims 双跑 + 主 loop 独立复核 transitive+primitive+全偶 3-循环 → Jordan = A12),远超 TIER A 上限、最优纯 TS 不可行;记号校准过(30 条真 dinoso 零失败)但无代码可交,停 loop 等用户三选一(§3)。
- 2026-06-20 / A6 / `b98c5b426` / `ufo` UFO 纯 TS 全 BFS 最优解;实测 60,480 态(带标签八分体,Schreier-Sims+BFS 双证;**非** Jaap 的 200,121,075——那是着色上界,球半锁刚体使真实仅 6 万)、上帝之数 10、均值 7.7443、整图 BFS <50ms → TIER A;token 集恰 8 个 `A B C U U' U2' U2 U3`(cstimer 无 apply 故只验 token+解析,500 条真打乱零失败);移动语义由真 3D 旋转矩阵独立推导;套 Ivy/SuperFloppy 范式接 solver/stats/preview/SolveTabs/picker;圆盘 24 八分体楔形示意 solved 自证(非立方体网);主 loop 独立复跑 vitest 15/15 + typecheck EXIT=0;UI 门欠账(RAM ~1.2GB 跳过 Playwright,两路由 HTTP 200)。
- 2026-06-20 / A6-review / `85b1a0d58` / 独立复核 b98c5b426:executor 全部声明**属实**。**move-model 保真度独立硬核验(crux,无 cstimer oracle)**:cstimer `mega()` 仅随机吐 token、无 apply/无图,故重写一份**全独立**几何推导(自有坐标系 + 自有 home 索引顺序 + 自有 3D 旋转矩阵 + 自有 BFS,不 import executor 任何文件),经位置匹配双射共轭进 solver 索引后,**4 个基础置换 U/A/B/C 移动级逐元素全等**于 committed perms;独立带标签八分体 BFS 复现 60,480 / God 10 / 直方图 `[1,8,33,151,577,1924,5733,13778,21715,14241,2319]` / 均值 7.7443 全等。Schreier-Sims `|G|=246,960` 是含 gap 标签的 home 群阶(非真件数),BFS 把 gap 当 -1 不可分故得 60,480 = 权威可达态数(此为本 puzzle 最终口径)。tsgo + tsc EXIT=0。**修 1 处保真缺口**:测试参照基础 perm 原为从 solver 逐字节复制(共享了基础表,只独立重组 U 幂 + BFS),CI 因此没独立锚定**几何**保真;新增测试内 3D 几何重推导块(rotZ +60° 顶层 / 180° 径向翻)+ 断言 `GEO.U/A/B/C === 参照 perm` + 自逆 + 轮序 6,使 CI 成为永久 move-model 保真锚(vitest 15→16 绿)。交叉验证 BFS 确独立(自有 apply 路径)。**UI 门补齐**(RAM 2.9GB > 2GB):Playwright 实测 solver+stats 两路由桌面+390px、圆盘预览渲染、「更多魔方」下拉选中 UFO、零 console error,§4 清、`[untested-ui]` 债销。scratch `.tmp/ufo/*.cjs`(含本次新增)全 gitignored 未泄漏。
- 2026-06-20 / A7 / `99f5ff830` / `cm2` Cmetrick Mini 纯 TS 全 BFS 最优解;实测 165,888 态(= 24⁴/2,4 球各 24 取向 + 奇偶约束砍半;独立 BFS 复核未盲信 jaapsch)、上帝之数 10(cstimer 计步 `2` 算 1 步;jaapsch QTM-9 是另一口径)、均值 7.32576、整图 build+BFS ~293ms(packed-int)→ TIER A;物理模型 = 2×2 球阵(行绕 Y / 列绕 X 同步 90° 滚),token 集恰 12 个 `U< U> U2 D< D> D2 R^ Rv R2 L^ Lv L2`;move-model 保真锚 = 测试内从零 3×3 旋转矩阵重建 24 阶旋转群 + 自有 BFS,断言 solver 12 移动效果逐元素 == 几何(从 solved + 30 随机态);套 Ivy/UFO 范式接 solver/stats/preview/SolveTabs/picker;2×2 球示意 solved 4 球同色自证(非立方体网);cstimer 记号 vm 真跑 ×500 条零失败;主 loop 独立复跑 vitest 18/18 + typecheck EXIT=0;UI 门欠账(dev server DOWN 跳过 Playwright)。
- 2026-06-20 / A7-review / `83a1570c8` / 独立复核 99f5ff830:executor 全部声明**属实**。tsgo + tsc EXIT=0(prompt 预警的 `cm2` 缺失于 `SolvePuzzle`/`EVENT_ID` 又是陈旧 LSP 幻报,5 处接线 SolveTabs/solver page/stats page/ScramblePreview2D/cstimer-scramble 全有);vitest 18/18 绿(165,888 `toBe` + God 10 + 直方图 `[1,12,86,524,2577,9564,26964,49648,47712,23644,5156]` `toEqual` + 均值 7.32576)。**move-model 保真度测试确为真独立**(不像 A6 抄表):测试自建 `REF_MATS`/`REF_MUL`/`REF_MOVES`/`refApplyTok`/`referenceDist` 全套从 3×3 旋转矩阵(RX/RY/RZ)起,只 import solver 公开 API,断言 12 个单 token 效果逐元素 == 几何(从 solved + 30 随机态)+ 独立 BFS 复现 165,888/God/直方图——**无需补强,本单元保真锚天生达标**。**唯一修复 = 死代码清扫**:`cm2-solver.ts` 与测试都从 SVG 推导照搬了 `dirIndex`/`applyMat`/`DIRS` 却从不调用(仅 `cm2_svg.ts` 真用),删之(31 行,零行为变化,两 typecheck + vitest 复跑仍绿)。闭包 24⁴/2 = 165,888 几何核实(4 球 × 24 取向,齿轮奇偶约束砍半,独立 BFS 经验证实)。STEP 3 全合规:CSV 头 `optimal_length,scramble` + identity 行 `0,` + 单步数 txt == 直方图;真 `<button>`/`AppLink`、`tr({zh,en})`(无 isZh 文案三元)、数据色 hex(非灰阶)、nuqs。**UI 门仍欠账**:free RAM 仅 1.17GB(< 2GB 阈)+ dev server DOWN(HTTP 000),按硬约束不自启,`[untested-ui]` 债保留(见 §4)。无 scratch 文件入库(无 `.tmp/cm2`)。
- 2026-06-20 / A8 / `e174515f5` / `dmd` 钻石(八面体面转)纯 TS 全 BFS 最优解;实测 138,240 态(独立 BFS == cstimer SchreierSims |G|;**非**估 ~1e6)、上帝之数 10、均值 6.6921、直方图 `[1,8,48,288,1632,8568,36114,74799,16547,220,15]` → TIER A;dmd 是 cstimer poly3dlib PolyScrambler **random-state** 魔方(有真 apply+真 solver)→ **本 loop 首个有真 round-trip oracle**:测试 node:vm 跑 cstimer 真引擎,抽取 dmd moveTable 重建 4 生成元逐元素 == solver 硬编码生成元 + SchreierSims |G| == 138,240 + 200 条真 dmdso 打乱全 round-trip(token 集恰 `U U' R R' L L' F F'`);八面体 triforce 四分网图 solved 每面同色自证;套 cm2 范式接 solver/stats/preview/SolveTabs/picker(5 处);主 loop 独立复跑 vitest 19/19 + typecheck EXIT=0;UI 门欠账(RAM 紧跳过 Playwright)。
- 2026-06-20 / A8-review / (本 commit) / 独立复核 e174515f5:executor 全部声明**属实**,**无真错可修**(零代码改动)。tsgo EXIT=0 + tsc -b EXIT=0(prompt 预警的 `dmd` 缺失于 `SolvePuzzle`/`EVENT_ID` 均为陈旧 LSP 幻报,5 处接线 SolveTabs(SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION)/solver page dispatch/stats page(event==='dmd'+isIvy+distPuzzle line 718)/ScramblePreview2D(HAS_PREVIEW+dispatch)/cstimer-scramble(solvable+family:'twist') 源码全有)。**cstimer round-trip oracle 确为真锚、确在测试里**:`describe('cstimer dmd oracle (real engine via node:vm)')` 经 vm 加载真 `poly3dlib`+`grouplib`+`utilscramble`,(a)`extractCstimerGens` 走 `getFamousPuzzle('dmd')`→`makePuzzle`→`parser.parseScramble`→`getTwistyIdx`→`moveTable[idx]`(-1→identity 归一)逐元素 == 硬编码 4 生成元、4/4 全 match;(b)`new grouplib.SchreierSims(gens).size()` `toBe(138240)`;(c)200 条真 `dmdso` 打乱用 **本** parser 解析 + apply + 全 reachable + scramble∘最优解=solved round-trip,token 集 ⊆ 8-token 字母表、`parseFails toBe 0`、`generated > 0`(非自洽再推导,是真引擎打乱过本求解器)。vitest 19/19 绿(exact form `vitest run tests/diamond_solver.test.ts`,15.0s)。**闭包 138,240 独立复算确认**(主 loop 跑全新 node BFS,只用 4 生成元、不 import 任何源:closure=138240 / god=10 / hist=`[1,8,48,288,1632,8568,36114,74799,16547,220,15]` 全等)。STEP 3 全合规:`diamond_svg.ts` 是真八面体网(2×4 triforce 三角、非 cube-net),solved 每面 4 子三角同色 + 8 面 8 色自证;`DiamondDistView` CSV 头 `optimal_length,scramble` + identity `0,` 行(`diamondAllStates()[0]` depth 0/'')+ 单步数 txt 走 `diamondAllScramblesByLength` == 直方图,138,240<2M 直接 Blob 合理;真 `<button>`/`AppLink`、`tr({zh,en})`(无 isZh 文案三元)、数据青 hex(非灰阶)、`_DiamondSolver` 用 nuqs(scramble+mode)。**UI 门仍欠账**:free RAM 2.86GB(> 2GB 阈)但 `127.0.0.1:3000` dev server DOWN(HTTP 000),按硬约束(禁 `pnpm dev`)不自启,Playwright 跳过,`[untested-ui]` 债保留(见 §4)。无 scratch 文件入库(无 `.tmp/dmd`;`.tmp_update_run.log` 是别的包 WIP、未碰)。
- 2026-06-20 / A9 / (无 commit) / `sia113` 联体 1×1×3 ⛔ 卡档位边界:`args2`/`formatScramble` 项(`#{[["U","u"],["R","r"]],cubesuff,N} z2 #{...}`),物理 = 两 3×3×3 沿共享 1×1×3 棒粘连的 bandaged 双联体,8 基础层转(order 4)生成。实测可达态 ≫ 50M(两条独立表示 BFS 均深度 6 累计 2,449,964、每层 ×8、深度 7 OOM;量级 10⁸–10⁹),超 TIER A/B 上限纯 TS 不可行;记号校准过(vm 真跑 cstimer sia113 × 500 条 / 17,952 token → 恰 12 move token + 字面 z2,0 解析失败)但无代码可交,并入 dino soft-gate 决策批等用户三选一(§3),loop 跳过继续 B1。
- 2026-06-20 / B1 / `97e850156` / `gear` 齿轮魔方纯 TS 全 BFS 最优解;实测 41,472 态(**非**估 ~12.4M——那是 24×72³ 坐标乘积上界,真实闭包仅 4 万)、**落到 TIER A 非预期的 TIER B**(BFS ~173ms ≪ 1.5s,无须 `.bin.gz` 预算表/流式);cstimer face-turn 计步上帝之数 6、直方图 `[1,33,579,5921,18072,13977,2889]`、均值 4.30317;状态 `[corner,e0,e1,e2]` 逐字段照抄 gearcube.js(cmv/emv + baseStep + 33 token 3 轴×11 power);**保真度锚 = cstimer 真引擎 round-trip oracle**(node:vm 加载真 mathlib+scramble+gearcube,200 条真 gearso 全 round-trip,scratch 阶段 300/300);套 Ivy/Diamond 范式接 solver/stats/preview/SolveTabs/picker(5 处);schematic 预览(4 坐标非贴纸置换,§4 软网允许)solved 自证;主 loop 独立复跑 vitest 19/19 + typecheck(tsgo)EXIT=0;UI 门欠账(RAM 紧 + dev server DOWN 跳过 Playwright)。
- 2026-06-20 / B1-review / (本 commit) / 独立复核 97e850156:executor 全部声明**属实**,**无真错可修**(零代码改动)。tsgo EXIT=0 + tsc -b EXIT=0(prompt 预警的 `gear` 缺失于 `SolvePuzzle`/`EVENT_ID` 又是陈旧 LSP 幻报,5 处接线 SolveTabs(SolvePuzzle line 32 + EVENT_ID line 61 + PUZZLE_BY_EVENT line 67 + HAS_DISTRIBUTION line 72)/solver page dispatch(dynamic import line 45 + `event==='gear'` line 62)/stats page(`event==='gear'` line 958 + isIvy line 503 + distPuzzle line 720 + GearDistView import/渲染)/ScramblePreview2D(HAS_PREVIEW line 61 + dispatch line 114)/cstimer-scramble(`solvable:true`+`family:'twist'` line 80) 源码全有)。vitest 19/19 绿(exact form `vitest run tests/gear_solver.test.ts`)。**gear-edge 齿轮旋转确在 state 里**:状态每条齿轮棱 = `perm(0..23)*3 + tooth(0..2)`,`tooth`(mod 3)就是齿轮齿的旋转;`edgeMoveOne` 的 `(idx%3)+(m===0?1:0)` 即转面带动齿轮转;独立测试断言**单轴 period=12**(普通立方棱 order 更短 → 12 步才复原正是齿轮机制的指纹),闭包 41,472 + God 6 = 已知正确值,**编码含齿轮取向无疑**。**cstimer round-trip oracle 确为真锚、确在测试里非 skip**(verbose 单测 `cstimer gear oracle (real engine via node:vm)` 耗 341ms 非 sub-1ms 跳过,无 warn/unavailable 输出;cstimer 文件实存 `tools/cstimer-scramble/scramble/gearcube.js` + gearso 注册):node:vm 加载真 utillib/isaac/mathlib/scramble.js/gearcube.js,200 条真 `gearso` 打乱用**本** parser 解析 + apply + 全 reachable + scramble∘最优解=solved round-trip、token ⊆ 33-token、`parseFails toBe 0`、`generated > 0`(真引擎打乱过本求解器,非自洽再推导)。**闭包 41,472 独立复算确认**(测试内 `referenceDist()` 从 gearcube.js 语义重写 cmv/emv + 独立 BFS,不 import solver 内部 → total `toBe(41472)` + God `toBe(6)` + 直方图 `toEqual([1,33,579,5921,18072,13977,2889])` + 均值 4.30317,主 loop 另用 python 复核 total/mean 全等)。STEP 3 全合规:`gear_svg.ts` 用 schematic(3 轴齿轮盘 + spoke 取向 + 4 角环 + raw-state 字幕)非 cube-net —— gear state 是 4 坐标非贴纸置换,§4 软预览允许,渲染清晰且 solved 全坐标 0 固定 canonical 自证、scramble∘解 60 随机 trial 复原,**判定可接受未改写**(改成真 net 反误导);`GearDistView` CSV 头 `optimal_length,scramble` + identity `0,` 行(`gearAllStates()[0]` depth 0/'')+ 单步数 txt 走 `gearAllScramblesByLength` == 直方图,41,472<2M 直接 Blob 合理;真 `<button>`/`AppLink`、`tr({zh,en})`(无 isZh 文案三元)、数据紫 hex `#9333ea`(非灰阶)、`_GearSolver` 用 nuqs(scramble+mode)。**UI 门仍欠账**:free RAM ~1.9GB(< 2GB 阈)+ `127.0.0.1:3000` dev server DOWN(HTTP 000),按硬约束(禁 `pnpm dev`)不自启,Playwright 跳过,`[untested-ui]` 债保留(见 §4 B1)。无 scratch 文件入库(无 `.tmp/gear`;`.tmp_update_run.log` 是别的包 WIP、未碰)。
- 2026-06-20 / B2 / (无 commit) / `cm3` Cmetrick(full)⛔ 卡档位边界:3×3 球阵(9 球,cm2 放大版),18 token(U/E/D 行 × R/M/L 列 × `<`/`>`/`2`)。bounded BFS(5M cap + heap 守卫)实测每层 new `[1,18,207,2160,20979,187101,1547470,3242064(深度7撞cap)]`、增长 ~×8–11.5、深度 6 累计 1.76M、深度 7 仅前 3.24M 就撞 5M cap → 总量量级 10⁹–10¹¹(raw 上界 24⁹≈2.6×10¹²),**超 TIER B 5×10⁷ 上限**,纯 TS 全 BFS 不可行;记号校准过(vm 真跑 cstimer cm3 × 500 条 / 12,500 token → 恰 18 token、0 解析失败)但无代码可交,并入 dino/sia113 soft-gate 决策批等用户三选一(§3),loop 跳过继续 B3。
- 2026-06-20 / B3 / (无 commit) / `233` 2×3×3 多米诺 ⛔ 卡档位边界:物理 = 2(高)×3×3 长方体,顶面 U 转 90°(三 power)+ 四侧面 R/L/F/B 仅 180°,7 token `U U' U2 R2 L2 F2 B2`。从机制独立几何重推导(18 槽、2 面心固定、16 活动 cubie = 8 角 + 8 棱两轨道),**独立 Schreier-Sims 算 |G| = 1,422,489,600(≈1.42 × 10⁹,= 8!·8!·7/8,棱角奇偶耦合砍 1/8)**;bounded BFS(heap 守卫 1.8GB)独立佐证每层累计 `[1,8,42,210,1017,4811,22435,103158,465422,2068198]`、增长比 ~×4.4–4.8、深度 9 累计 2.07M 远未完成(外推上帝之数 ~17–19)。**28× 倍于 TIER B 5×10⁷ 上限、711× 于 TIER A 2×10⁶**,dist 表/全空间直方图/下载全部/build 预算表全不可套,纯 TS 全 BFS 不可行;记号校准过(node:vm 真跑 cstimer 233 生成器 × 500 条 / 12,500 token → 恰 7 token、0 解析失败)但无代码可交,并入 dino/sia113/cm3 soft-gate 决策批等用户三选一(§3),loop 跳过继续 B4。
- 2026-06-20 / B4-probe / (无 commit,仅 loop 文件) / `sia123` 联体 1×2×3 ⛔ 卡档位边界:`args2` 项 `#{[["U"],["R","r"]],%c,%l} z2 #{...}`,bandaged 双联体(两 3×3×3 沿共享 1×2×3 块)。验证过的 surf-cubie+24 取向 rigid-body bounded BFS(compact 编码 + 4.5M cap + heap 守卫,生成元全 order-4)每层 new `[1,18,180,1512,12319,99278,793104]`、增长 ~×8、深度 6 累计 906K、深度 7 撞 cap → 量级 10⁸–10⁹(同 sia113),超 TIER A/B 上限纯 TS 不可行;记号校准过(vm 真跑 cstimer sia123 × 400 / 16,400 token → 恰 10 token + 字面 z2、0 失败)但无代码可交,并入 soft-gate 决策批(§3),loop 跳过继续 B5。
- 2026-06-20 / B5-probe / (无 commit,仅 loop 文件) / `sia222` 联体 2×2×2 ⛔ 卡档位边界:`args2` 项 `#{[["U"],["R"],["F"]],%c,%l} z2 y #{...}`,bandaged 双联体(两 3×3×3 沿共享 2×2×2 块,三族中最 bandaged)。同款 rigid-body bounded BFS(生成元全 order-4)每层 new `[1,18,225,2700,32256,383844]`、增长 ~×12、深度 5 累计 419K、深度 6 撞 cap → 量级 10⁸–10¹⁰,超 TIER A/B 上限;记号校准过(vm 真跑 cstimer sia222 × 400 / 16,800 token → 恰 11 token + 字面 z2 y、0 失败)但无代码可交,并入 soft-gate 决策批(§3),loop 跳过继续 B6。
- 2026-06-20 / B6-probe / (无 commit,仅 loop 文件) / `ctico` Icosamate ⛔ 卡档位边界:`adjScramble(["UL","UR","UrUl","FlFr","LBl","RBr"],0x3f×6,minxsuff)`,但 cstimer poly3dlib 有真几何 `polyParam=[20,[],[],[-5,0]]` + 真引擎 → **实测 |G| = 7.1×10³⁴(EXACT,`makePuzzle` moveTable 24 个 order-5 生成元喂 `grouplib.SchreierSims().size(true)` 0.05s 算出,80 facelet)**,原估 ~1e8 低估 26 个数量级,绝非 B/D 边界而是深 D;bounded BFS 佐证每层 ×33–40、深度 4 累计 3.37M;记号校准过(vm 真跑 cstimer ctico × 400 / 10,000 token → 恰 24 token、0 失败),并入 soft-gate 决策批(§3),loop 跳过(B 档全清,下一个是 C1)。
- 2026-06-20 / C1 / `3bf24f8f2` / `15p` 数字华容道 **TIER C 单实例最优**:IDA* + max(Walking-Distance, 逆序距离) 可采纳启发式,每条打乱现场出可证最短解;上帝之数 80(锁 `toBe`)、采样均值 ≈ 52.6(Korf&Schultze);**复用滑块核 lib/slider-puzzle 以 4×4 参数化(零重复移动逻辑,与 8p 同记号源)**;记号 vm 真跑 cstimer 15prp × 400 条零失败;**最优性交叉验证**=浅态 IDA* 长度 == 独立朴素 BFS 最短(0 mismatch)+ 启发式 ≤ 真距(0 adm 违反);**分布=采样(非全空间),UI 显式标注**,异步可取消 + 下载样本 CSV(无「下载全部」);套 slide8 范式接 solver/stats/preview/SolveTabs/picker(5 处);主 loop vitest 13/13 + typecheck(tsgo)EXIT=0;UI 门欠账(free RAM ~0.97GB < 2GB 跳过 Playwright,dev server HTTP 200)。
- 2026-06-20 / C1-review / `56538d8d1` / 独立复核 3bf24f8f2:executor 全部声明**属实**。tsgo EXIT=0 + tsc -b EXIT=0(prompt 预警的 `15p` 缺失于 `SolvePuzzle`/`EVENT_ID` 又是陈旧 LSP 幻报,5 处接线全有:SolveTabs SolvePuzzle/EVENT_ID line57/PUZZLE_BY_EVENT line68/HAS_DISTRIBUTION line73、solver page dispatch line60、stats page event==='15p' line881+isIvy line505+distPuzzle line718、ScramblePreview2D HAS_PREVIEW line62+dispatch line111、cstimer-scramble `solvable:true`+`family:'slide'` line90)。**最优性交叉验证确为真独立**:测试自带 `State`/`slide`/`applySeq`/`referenceBfs`/`randomLegalSeq`(从 cstimer slider spec 重写,**不**调 solver 的 apply),断言 IDA* 长度 == 独立 BFS 最短(`toBe`,500 抽样浅态 0 mismatch、checked>300)+ WD/组合启发式 ≤ 真距(0 adm 违反)。**主 loop 另跑更深的独立复核(crux,TIER C 非采纳启发式=静默次优)**:重新从零写一份 WD + 逆序距离 + IDA*(独立第二求解器),BFS 枚举到深度 16(全 241,707 可达态)→ **0 admissibility 违反(WD/invDist/max 三者都 ≤ trueD)** + 独立 IDA* 在 400 个深态(d≥8)上 == BFS 距离(**0 mismatch**)→ 两条启发式确属可采纳、IDA* 确返回可证最优,无次优 bug。**复用滑块核确认**:slide15-solver import `lib/slider-puzzle` 的 `SLIDE_DELTA/SLIDE_INVERSE/SLIDE_DIRS/parseSliderScramble/solvedSlider/blankValue`,移动规则单一事实源(与 8p 同),无重复移动逻辑。STEP 3 全合规:`slide15_svg.ts` 4×4 数字格 solved=1..15 canonical 自证(label 由 solver state 推导)、`Slide15DistView` 明确标「采样/非全空间」+ 下载样本 CSV(无「下载全部」)+ 引用 God 80/均值 52.6 + 异步分批可取消、`_Slide15Solver` setTimeout 异步 + spinner(深态不卡死)+ nuqs(scramble+mode);真 `<button>`/`AppLink`、`tr({zh,en})`(无 isZh 文案三元)、数据蓝 `#3a86ff`(非灰阶)。**唯一修复 = 删测试里死的 `expand()` helper**(13 行,定义后从未调用;flagged 的 `heuristic`(solver,经 slide15CombinedHeuristic + IDA* 真用)与 DistView 的 `randomSolvableGrid`/`solutionToScramble`(flagged 名 invertSolution/randomSolvablePerm/permToScramble 实不存在)均在用,未动)。改后 tsgo+tsc EXIT=0、vitest 13/13 绿。**UI 门仍欠账**(free RAM ~0.97GB < 2GB + 大求解器在跑,按内存约束不开 Chromium,`[untested-ui]` 债保留,见 §4 C1)。无 scratch 文件入库(`.tmp/15p` 用后删;`.tmp_update_run.log` 是别包 WIP、未碰)。
- 2026-06-20 / soft-gate(TIER D) / (loop 文件) / RESOLVED:用户选 ③ ALL near-optimal,TIER D 放行;loop 继续 D1a…D2l + 升级批(dino/sia113/cm3/233/sia123/sia222/ctico),全走近最优契约(有效性必验 + 长度 ≤ 上界/cstimer 同口径 + 采样直方图 + 不下载全状态)。
- 2026-06-20 / D1a / `8af309db6` / `mpyrso` 大金字塔 **TIER D 近最优(首单元,确立 wrap-cstimer 省力路范式)**:状态空间 ~4.6×10¹¹ 不可全 BFS → 直接 wrap cstimer 自带两阶段 solver(扩 vendored pyraminx.js 加 solveScramble/roundTripCheck/faceletsOfScramble + worker op:'solve' + cstimerSolve 桥);lib/mpyr-solver async 解(无 BFS/无表)+ 52-facelet TS 端口(对齐真引擎,3000 序列 fuzz 0 差异);近最优长度均值 ≈ 21.4(min 17/max 24,face-turn 口径);**有效性 = N 条真打乱全 round-trip(scramble∘solution=solved)**经 node:vm 真引擎验;采样近最优直方图(无下载全部);套 5 处接线;主 loop vitest 6/6 + typecheck(tsgo)EXIT=0;UI 门欠账(RAM 紧 + 无 dev server 跳过 Playwright)。
- 2026-06-20 / D1a-review / `8af309db6`(无新 commit,executor 提交完整、零修复) / 独立复核 8af309db6:**commit-completeness 通过** —— prompt 预警的 `Cannot find module './_MpyrSolver'` / `'./_components/MpyrDistView'`(2307)经 `git show --stat 8af309db6` 证 12 文件全在 HEAD(`lib/mpyr-solver.ts`/`_MpyrSolver.tsx`/`MpyrDistView.tsx`/`mpyr_svg.ts`/`tests/mpyr_solver.test.ts` + vendored pyraminx.js/scrambler.worker.js + cstimer-scramble.ts/SolveTabs/solver page/stats page/ScramblePreview2D 全提交),`git status` 无任何 D1a 文件未提交 → **是陈旧 LSP 幻报,非真未提交**;**最终 tsgo EXIT=0 + tsc(`tsc --noEmit`)EXIT=0**,模块全解析。**wrap-cstimer 桥确实健全**:`cstimerSolve` → worker `op:'solve'` → `SOLVERS.mpyrso = self.mpyr.solveScramble`(扩 vendored pyraminx.js,clean diff,solveScramble 用既有 applyMoves/prettyMoves/solveMpyr/MpyrCubie,tip 独立 net mod 3 后取逆,逻辑正确);**validity oracle 确为真引擎** —— 测试 node:vm 加载真 utillib/isaac/mathlib/scramble/pyraminx.js 取 `sandbox.mpyr`,`roundTripCheck(scr)` 在真引擎里把 solution body moves 应用到打乱态再断言 face-uniform(= scramble∘solution=solved,非自洽捷径),24 条真打乱 `roundTripFails===0` + 每条 `expect(ok).toBe(true)`;长度仅断言 ≤ bound(38)标注近最优非可证最短。**vitest 6/6 绿(实跑非 skip:solveTest 自检 6.2s + oracle 输出 mean 21.21/min 17/max 25 + facelet 80 序列逐 move 对齐)**。STEP 3 全合规:`MpyrDistView` 标「采样/近最优/非全空间」+ 仅下载样本 CSV(无下载全部)+ 真 button/AppLink/`tr({zh,en})`/数据绿非灰阶、`_MpyrSolver` nuqs(scramble+mode)+ 异步 spinner + 请求计数防陈旧 + 复用 BatchSolvePanel、`mpyr_svg` 4 面三角网 schematic solved 单色自证(§4 接受)。**vendored upstream pyraminx.js 的 flagged 死变量(`type`/`length`/`m`/`scramble`)经核实全是 cstimer 原码**(line 175 `getScramble(type)` 是常规 Pyraminx 函数,被 line 534 `mpyr` IIFE 内同名 `getScramble(validMoves,len)` 遮蔽,均 pre-existing)——按「`tools/cstimer-scramble` 是 ❌upstream fork,只改 wrap 部分」规则**不动**(改 upstream 死码有破坏 solver 风险且非回归)。**零代码修复**(executor 工作完整正确)。无 scratch 入库(`.tmp_update_run.log` / `.playwright-mcp/*.png` 是别包 WIP/gitignored,未碰)。**UI 门欠账保留**(RAM ~1GB 临界 + 无 dev server,按内存约束不开 Chromium,`[untested-ui]` 债见 §4 D1a)。
- 2026-06-20 / D1b / `6268157ee` / `crz3a` 疯狂 3×3 **TIER D 近最优(复用站内 kociemba,证实=333 换皮)**:megascramble.js:27 确认 crz3a 用标准 18 move、底层就是普通 3×3(~4.3×10¹⁹)→ 直接 **direct-import** 站内 client kociemba 两阶段(`_kociemba/{cube,movetables,prune,search}`,非 cubeopt/非 cstimer/非新引擎),lib/crz3a-solver.ts 模块级 memoized 建表一次 + async solveCrz3a 用 solveCube+formatMoves(不 invert,故 scramble∘solution=solved),纯 TS 无 worker → Node vitest 可跑;parseMoves 直吃标准 HTM 零翻译;预览复用展开网(EVENT_TO_PUZZLE 加 crz3a→3x3x3);仿 D1a 接 _Crz3aSolver + Crz3aDistView(采样近最优,DEFAULT_SAMPLE 150,只下样本 CSV)。典型近最优长度均值 ≈ 20.82(min 18/max 23,HTM,kociemba 两阶段≤23)。门:vitest 4/4 绿(22 条真 cstimer crz3a 打乱独立 3×3 oracle 全 round-trip + 长度≤26 + token 全标准 HTM)、typecheck(tsgo)EXIT=0、git show --stat 证 10 文件全在 commit;Playwright UI 跳过 `[untested-ui]`。family `'other'`。
- 2026-06-20 / D1b-review / `60b7bfade` / 独立复核 6268157ee:**executor 声明全部属实**。`git show --stat` 证 10 文件全在 commit(4 新:crz3a-solver/_Crz3aSolver/Crz3aDistView/crz3a_solver.test);**最终 tsgo EXIT=0 + tsc(`tsc --noEmit`)EXIT=0**(prompt 预警的 `"crz3a" 不可赋给 SolvePuzzle`/`Cannot find module` 均陈旧 LSP 幻报)。**kociemba 复用确返回解非逆** —— `search.ts` 的 `solveCube(start,…)` 搜索把 start 归约到 phase-1 目标(twist=flip=0/sliceRaw=494)再 phase-2 到 cperm=eperm=sperm=0,即解;`scrambleFromState` 才 `invertSequence`,crz3a-solver 调 `solveCube`+`formatMoves` 不 invert → scramble∘solution=solved 成立。**validity oracle 确独立**:测试从 kociemba cube.ts import parseMoves/applySequence/solvedCubie/isSolvedCubie,对**新建** solvedCubie 重 apply scramble∘solution 验 isSolved(独立于 solver 内部 best 路径的二次计算,能抓 sign/invert bug);vitest 4/4 绿(6s,cstimer vm 加载成功,22 条真打乱)。**唯一真 bug 修复 = 2^53 数字字面量**:`Crz3aDistView.tsx` 把 3×3 态数 `43252003274489856000`(>2^53 = Number.MAX_SAFE_INTEGER,丢精度,diagnostic 80008)写成数字字面量 + `.toLocaleString()`,改为预格式化字符串 `'43,252,003,274,489,856,000'`(纯展示,不参与计算),两处消费点同步去 `.toLocaleString()`。守卫复查:真 button/AppLink(DistView 用 Link=AppLink)、`tr({zh,en})`(无 isZh 文案三元)、唯一颜色是数据绿 `#16a34a`(非 UI 灰阶)、nuqs(scramble+mode)、采样近最优标注 + 只下样本 CSV、预览复用展开网。无 scratch 入库(只改 Crz3aDistView.tsx 一文件,别包 WIP 未碰)。**UI 门仍欠账**(RAM ~1GB 临界 + dev server,按内存约束不开 Chromium,`[untested-ui]` 债见 §4 D1b)。
- 2026-06-20 / A5→D `dino` / `bd503934b` / 恐龙魔方 **TIER D 近最优(wrap-cstimer 省力路,同 D1a)**:实测可达态 A12 = 239,500,800(= 12!/2,误分类、超 TIER A/B 上限)→ soft-gate 用户选 ③ ALL near-optimal 后,作 TIER D 近最优单元做完。dino 是只有棱的随态魔方 → 扩 vendored redi.js 的 dino IIFE 加 solveScramble/roundTripCheck/edgePermOfScramble(复用 cstimer 自带 dino IDA*+三张棱组合剪枝表)+ worker `SOLVERS.dinoso` + cstimerSolve('dino',…) 桥;lib/dino-solver async 解(无 BFS/无表)+ 12-棱 perm 与 24-贴纸网模型(贴纸置换逐 move 对齐 cstimer acycle 验过,**坑:棱有取向不能 face1↔face1 直映**);近最优长度均值 ≈ 9.53(min 7/max 11,face-turn 口径,God 10);**有效性 = 40 条真打乱全 round-trip(scramble∘solution=solved)**经 node:vm 真引擎验 + TS ep 端口 200 条 fuzz 0 差异;6 面立方体十字网 solved 自证;采样近最优直方图(无下载全部);套 5 处接线(family:'twist');主 loop vitest 8/8 + typecheck(tsgo)EXIT=0;UI 门欠账(dev server DOWN 跳过 Playwright,`[untested-ui]`)。
- 2026-06-20 / dino-review / `bd503934b`(无新 commit)/ **REVIEW PASS**:executor 全部声明属实。wrap-cstimer 桥 sound(redi.js 仅在 dino IIFE 内加 solveScramble/roundTripCheck/edgePermOfScramble + 导出,无上游行为改动;EDGE_MOVE_SWAPS 逐字段 == cstimer edgeMoveSwaps;worker `SOLVERS.dinoso` 镜像 mpyrso;复用 cstimer 自带 solveDino(ep,0),无重写 BFS)。有效性 oracle 真驱动真引擎(node:vm 跑真 mathlib+scramble+redi.js,40 条真 dinoso 用 edgePermOfScramble(scramble∘solution)==恒等验 round-trip,非自洽捷径;长度记为近最优非最优)。**dino_svg 棱模型独立复核为正确**:committed 测试只验贴纸网自洽(非锚定 cstimer,与 commit 文案「逐 move 对齐 acycle」略有出入),故 review 临时加独立检验 —— 3000 条随机打乱每槽 2 贴纸的色对 == cstimer 锚定 ep 所占棱件的 solved 面对,3000/3000 通过 → 24-贴纸模型确是锚定 ep 的忠实函数,棱-取向坑确实避开(临时测试用后已删,无源码改动)。复跑门:vitest 8/8 绿、typecheck tsgo EXIT=0 + tsc EXIT=0。无 bug,无修复。UI 门仍欠(RAM 紧 ~1GB 不开 Chromium,`[untested-ui]` 留在 §4)。
- 2026-06-21 / D2-1 / `1ffdee851` / `233` 2×3×3 多米诺 **TIER C 单实例可证最优(不走记录的 D 近最优 —— STEP 2 实测后升档,prime directive「能最优就最优」)**:状态空间 1,422,489,600(8!·8!·7/8)太大无法全 BFS,但 8 角 / 8 棱是两独立 8-轨道,各建满 PDB(角 maxdepth 13、棱 maxdepth 11,各一次 ~0.5s BFS,memoized),IDA* + max(角距离, 棱距离) 可采纳启发式出可证最短解;均值 13.7、样本最长 16、深态最坏 ~0.4s。**移动语义从 3D 几何独立重导**(U=+90° 顶层 / R2 L2 F2 B2=180° 四侧 slab,U⁴=side²=id 已验);记号逐字段对 cstimer `megascramble.js:30` 的 mega 生成器(7 token `U U' U2 R2 L2 F2 B2`)。**有效性 oracle 独立**(测试从几何重导参照置换、**非**拷 solver):800 条真随机打乱 scramble∘solution=solved + 浅态长度==独立 BFS 距离;**穷举半径 ≤6 的全部 20k+ 态长度==BFS 深度(0 mismatch,真正证可证最优非采样断言)**;cstimer node:vm 真生成器 ×200 条全 round-trip + token ⊆ 7 字母表;SVG 42 贴纸网 solved 自证 + 随 solver 联动。套 cuboid223/slide15 范式接 solver/stats/preview/SolveTabs/picker(5 处);主 loop vitest 15/15 + typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0;UI 门欠账(`[untested-ui]`,见 §4)。分布=采样(每条解可证最优但整曲线抽样,UI + 本条显式标注)。
- 2026-06-21 / D2-2 / `07e00099f`(base)+ review-fix(本条同 commit)/ `334` 3×3×4 **可证最优(浅态 IDA*)+ 诚实 too-deep(深态)**:状态空间 ≈ 1.65×10¹⁷(物理),群阶 2.64×10¹⁸,**均超 2^53 → 所有大计数(SS 阶 / 物理态数 / 派生值)在 UI/solver/CSV 全走预格式化字符串,绝无 number 字面量**(独立 grep 扫净)。浅态(≤~13 步)IDA* + max(每轨道 PDB,各轨道 move-set 投影后 BFS 建满表)可采纳启发式 → 可证最优,`optimal` flag 恒 true(仅在 IDA* 证明时返回,否则抛 `too-deep`);深态超 12M 节点预算 → 诚实 `too-deep`(实测 40 步打乱 ≤2.1s 命中预算、不卡浏览器;贪心兜底实测跑到 200+ 步无意义故不提供 —— **executor 实际取消了 commit msg 里写的 greedy,review 把所有文档/UI 改正与代码一致,删 `optimal:false` 死分支**)。记号对 cstimer `megascramble.js:9` 的 334 生成器(12 token `U U' U2 / u u' u2 / R2 L2 M2 / F2 B2 S2`)。oracle 独立(测试从几何重导置换、**非**拷 solver)+ node:vm 真生成器 round-trip;**radius≤4 全 8000+ 态穷举 == BFS 深度**(可证最优 oracle)。**/2 奇偶因子独立复核**:重写 Schreier-Sims 得群阶 == 2,642,908,293,365,760,000、轨道奇偶向量 GF(2) 秩 4 / 5 → 恰 1 个轨道间奇偶约束 → 物理 = (40320³·2520·2)/2 = 165,181,768,335,360,000 **确认正确**。2D 网 3×3/3×4/4×3 非方面 solved 纯色自证 + 配色全由 state 推导。分布=**采样**(显式标「采样 / 非全空间」,仅 sample CSV 无 download-all)。套范式接 solver/stats/preview/SolveTabs/picker(5 处);vitest 17/17 + typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0;UI 门欠账(`[untested-ui]`,见 §4)。
- 2026-06-21 / D2-2-fix / `334` 3×3×4 **可用性 bug 修复:单实例 optimal-or-`too-deep` → 两阶段约简(100% 解真打乱)**。**根因实测**:base `07e00099f` 的解器只对浅态(启发表 cap ~13)IDA* 最优,真 cstimer 打乱(40 step,近均匀随机,最优深度 ~16-18 贴近直径)**30/30 全抛 `too-deep`** —— 即拒解几乎所有真打乱,违反 loop prime directive「每个 puzzle 必须返回解」。**修法 = 真两阶段(Kociemba 式)**:phase-1 weighted-A*(W=2)把 5 个轨道归约进全 180° 子群 H,启发 = max(intoH₁, intoH₃, **intoH₀+intoH₂**)(两个独立角轨道 0/2 用加性项 —— 纯 max(intoH)≤11 严重低估 ~14 步联合 phase-1 致 A* 泛滥;加性项收紧后 phase-1 ≤~1s);phase-2 IDA* 只用 8 个 180° 转,启发 = 两轨道对的**精确 joint PDB** max(dist₀₂[96·96], dist₁₃[576·36])(in-H 子集极小,各一次 BFS,**可采纳且紧** —— 纯 max(p2Dist)≤7 低估 ~14 步联合 phase-2 致 IDA* 泛滥 17s;joint PDB 后 phase-2 ≤~0.4s);浅态先跑 max(optDist) 可证最优捷径(命中即返回 `optimal:true`,跳过两阶段)。**测得 30 条真 cstimer len-40 打乱 30/30 round-trip(独立几何 oracle refApply)、中位 ~27 步、max ≤34(<硬上界 40)、平均单解 ~117ms / 最坏 ~1s**;radius≤4 全 8000+ 态穷举仍 == BFS 深度(浅态可证最优 oracle 保留)。dbStats 测试改断 `{sizes:[40320,40320,40320,2520,2], inHCounts:[96,576,96,36,2], p2MaxDepths:[5,7,5,5,1], intoHMaxDepths:[11,5,11,5,0]}`;新增「EVERY real (len-40) cstimer scramble solves — 100% solve-rate」测试 + 「deep random states ALWAYS solve, bounded」测试,删旧 too-deep 断言。UI 求解页 `_Cuboid334Solver.tsx` 删 too-deep 分支、文案改诚实「两阶段约简 / 近最优(optimal:false)/ 浅态最优」;`Cuboid334DistView.tsx` 改采样 40-step 真随机态、标题/说明改「采样 / 近最优 / 非全空间」、CSV 头 `length,scramble,optimal`、SCRAMBLE_LEN 8→40。门:**vitest 18/18 绿** + typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0。lessons → §0.6 #7(「单实例 optimal-or-too-deep 非合格交付,大直径 puzzle 必须两阶段兜底」)。`[untested-ui]`(无 dev server,Playwright 跳过)。
- 2026-06-21 / D2-3 / `335` 3×3×5 **TIER D 两阶段约简(near-optimal),100% 解真 cstimer 打乱**。**几何关键发现(与 334 本质不同,先量再做)**:物理 3×3×5 侧面是 3×5 长方形,**90° 侧转几何不可行**(维度证明:5-tall slab 90° 转把块映出 3-deep 盒外 + 离表两证 + cstimer renderer 无 335 分支不渲染图,三方独立确认),故刚体转只 **10 个** `U U' U2 D D' D2 R2 L2 F2 B2`(U/D 顶底盖 90°,四侧 180°)。cstimer `335 = #{cuboid} / ${333}` 的 `/333` 后缀含 90° 侧转 = **无刚体实现的人类速记**(把颜色当 3×3×3 打乱);solver/oracle 解析到 `/` 即止,只解刚体 cuboid part(真打乱的物理态)。可达态 **156,067,430,400**(Schreier-Sims via cstimer grouplib,validated S5/A5/S6;轨道乘积 40320²·24³=22,473,709,977,600 被 144× 奇偶耦合过计;均预格式化字符串)。5 可动轨道 `[40320,40320,24,24,24]`、inH `[96,576,24,24,24]`、intoH 深度 `[11,4,0,0,0]`(只角+顶底棱轨道需 phase-1,三 wing 恒在 H)。phase-1 = weighted-A*(W=2,启发 max(intoH);**两级数字闭包键** 外=角·40320+棱<2³¹、内=三 wing<24³,无字符串垃圾 → 比字符串键 1900ms→快;cap 250k→greedy 兜底,worst<0.6s);phase-2 = IDA*(joint-pair PDB max(dist₀₁[96·576]、dist₂₃[24·24]、p2[wing₄]),几 ms);浅态 max(optDist) 可证最优捷径(`optimal:true`)。**oracle 独立**(测试从几何重导置换、**非**拷 solver):radius≤4 全 **3613** 态长度==BFS 深度;1000 条真随机态 1000/1000 scramble∘解=solved;cstimer node:vm 真生成器(**加载 min2phase.js + scramble_333_edit.js**,335 比 334 多这两个依赖)×20 条 cuboid part round-trip + token ⊆ 10 字母表;「EVERY real cstimer scramble solves」8/8 100% solve-rate;SVG 78 贴纸网 solved 自证 + 随 solver 联动。**实测中位 ~20、max ≤27(<硬上界 CUBOID335_MAX_LENGTH=45)、最坏单解 ~0.5s**。比 334 简单(单角轨道 phase-1,无双独立角轨道加性启发问题)。套范式接 solver/stats/preview/SolveTabs(SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT+HAS_DISTRIBUTION 四处)/cstimer-scramble(solvable:true,picker 数据驱动)/ScramblePreview2D(HAS_PREVIEW+dispatch)五处;**vitest 19/19 绿** + typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0。`[untested-ui]`(无 dev server,Playwright 跳过,见 §4)。
- 2026-06-21 / D2-5 / `396d700fa` / `337` 3×3×7 **TIER D 两阶段约简(near-optimal hybrid),100% 解真 cstimer 打乱**(integration + 离线分布 retrofit,build 由独立 build-only agent 完成并已验)。同 335 几何(侧面 3×7 长方形 90° 侧转几何不可行)→ 刚体转 16 token(U/D 顶底盖 + u/d 内层片 90°,四侧 180°);cstimer `337 = #{cuboid} / ${333}` 的 `/333` 后缀无刚体实现被解析到 `/` 即止。可达态 **126,859,598,081,556,480,000**(≈1.27×10²⁰,Schreier-Sims;轨道乘积 2,283,472,765,468,016,640,000 被 18× 奇偶耦合过计;均预格式化字符串)。7 可动轨道 sizes `[40320,40320,40320,2520,24,24,24]`、inH `[96,576,96,36,24,24,24]`、intoH 深度 `[11,4,11,4,0,0,0]`。**集成步骤实证**:vitest `cuboid337_solver.test.ts` 20/20 绿(独立几何 oracle:radius≤3 全 1749 态==BFS 深度、deep random ALWAYS solve、cstimer node:vm 真生成器 round-trip、SVG 102 贴纸网 solved 自证);**本机 stress 120 条真 cstimer 打乱 120/120 solve(独立几何 round-trip)、mean ~550ms / median ~250ms / 最坏 4369ms(<硬上界 CUBOID337_MAX_LENGTH=80)**;典型最坏单解 ~4.4s(build agent 报 ~8.3s,本机更快)。套范式接 solver page(dynamic + dispatch)/stats page(event==='337' 早返回)/SolveTabs(SolvePuzzle+EVENT_ID+PUZZLE_BY_EVENT 三处)/cstimer-scramble(solvable:true,picker 数据驱动)/ScramblePreview2D(import+HAS_PREVIEW+dispatch)五处。**分布离线预计算**(N=700,dist_337.json,build_puzzle_sampled_dist.ts REGISTRY 加行 + update_puzzle_stats.ps1 `$SAMPLED_DIST_EVENTS` 加 337;Cuboid337DistView 改纯 fetch,删现场 solve 采样 + 求解器 import,零运行时求解)。门:vitest 20/20 + typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0(唯一残留 `_Cuboid336Solver.tsx` 错为并发 336 未接线,非本单元)。`[untested-ui]`(dev 用户在用,无头 Playwright 跳过,见 §4)。**复审建议**:最坏 ~4.4s(本机)/ ~8.3s(build agent)单解仍偏长 —— 求解走主线程同步,深态长尾会卡 UI;后续可考虑挪进 worker(同 mpyrso 范式)或收紧 phase-1 启发缩短长尾。
- 2026-06-21 / D2-4 / 整合提交(commit 见 §2 hash;dist_336.json 主循环 build 后另行提交)/ `336` 3×3×6 **TIER D 两阶段约简(near-optimal hybrid),100% 解真 cstimer 打乱**(求解器 + SVG + UI + 测试此前已造好且 18/18 绿,本单元 = 集成接线 + 离线分布 retrofit,镜像 D2-5 337)。⚠ 几何与 335/337 不同:cstimer 336 是**纯 case-1 mega 无 `/333` 后缀**(megascramble.js:10),21 token 全可刚体实现(`U U' U2 / u u' u2 / 3u 3u' 3u2 / R2 L2 M2 / F2 B2 S2`),整条打乱即物理态。可达态 **8,391,762,413,094,961,152,000,000**(≈8.39×10²⁴,Schreier-Sims;轨道乘积 33,567,049,652,379,844,608,000,000 被 2² 秩-2 奇偶耦合过计;facelet 群阶 2,148,291,177,752,310,054,912,000,000 = 物理 ×256;均预格式化字符串)。7 可动轨道 sizes `[40320,40320,40320,2520,40320,2520,2]`;phase-1 weighted-A* 逐层对归约进全 180° 子群 H,phase-2 weighted-A* 用 7 张重叠 EXACT joint-triple PDB bank(~16M cells)max 启发,浅态 max(optDist) 可证最优捷径。**五处接线(对照 337 逐一加 336)**:solver page(dynamic import + `event==='336'` dispatch + 头注)/ stats page(import + `isIvy` 早返回 + distPuzzle 分支 + `event==='336'` 早返回块)/ SolveTabs(SolvePuzzle union + EVENT_ID + PUZZLE_BY_EVENT 三处)/ cstimer-scramble(336 条目加 `solvable:true`,picker 数据驱动自动出现)/ ScramblePreview2D(import renderCuboid336ScrambleSvg + HAS_PREVIEW + dispatch)。**分布离线预计算**:build_puzzle_sampled_dist.ts REGISTRY 加 336 行(scrambleLen 50、defaultN 2000 抄 335、load cuboid336-solver 的 randomCuboid336Scramble/solveCuboid336/CUBOID336_MAX_LENGTH/STATE_COUNT_STR/GROUP_ORDER_STR)+ update_puzzle_stats.ps1 `$SAMPLED_DIST_EVENTS` 加 '336';`Cuboid336DistView.tsx` **从现场采样改为纯 fetch dist_336.json**(镜像 Cuboid334DistView,零求解器 import,无 setSamples/cancelRef/采样循环;dist_336.json 由主循环跑 build 单独提交)。删 ~100 个草稿 `_336fin_*` + `_336scratch_*`(.mjs/.bin/.json)+ `.tmp_update_run.log`,保留真测试 `cuboid336_solver.test.ts`。门:**vitest 18/18 绿**(原 `_Cuboid336Solver.tsx(103,18)` union 错由接线消除)+ typecheck(tsgo)EXIT=0。`[untested-ui]`(无 dev server,Playwright 跳过,见 §4)。
- 2026-06-22 / D2-6 / `3943e4bd2` / `sq2` 方块二 **TIER D 有效+有界(非近最优)**:实测 cstimer 可达态 = 12·18! = 7.68×10¹⁶(Schreier-Sims),单阶段 IDA* >300s 爆 → 构造式 3-cycle reduction(parity fix + 固定 base macro 经深度6 conjugator 表路由到任意有序三元组,覆盖 12120/12144);cstimer 只有打乱生成器、无 solver,oracle 从几何独立重导。**质量桶=有效+有界(非近最优)**:均值 ~70 元组、max 95(20k 样本)、`SQ2_MAX_LENGTH=130` 已 assert(不重蹈 336 无界 bound 覆辙)。门:vitest 9/9(独立几何 oracle:500 随机 + 200 真 cstimer 全 scramble∘solution=solved + 2000 有界)+ **tsgo EXIT=0 + tsc EXIT=0(主 loop 亲自复跑确认 —— 中途 LSP 报的 sq2-solver Uint8Array / build 脚本 process 错全是陈旧/wrong-tsconfig 幻报,§0.0 #1 再验证一次)** + Playwright 桌面+390px 0 console error + 预览自证(24 楔色 / 48 path)+ dist 直方图和 2000==sampleCount。13 文件(新 lib/sq2-solver + test + sq2_svg + _Sq2Solver + Sq2DistView + dist_sq2.json + 五处 wiring + build 脚本 REGISTRY/$SAMPLED_DIST_EVENTS)。分布离线预计算(dist_sq2.json,N=2000;**发布 scp 留 §3 MANUAL**)。one fork sub-agent 造 solver 核 + test,主 loop 调研/接线/dist/诚实改标(near-optimal→有效+有界)/门验。
- 参照基线:`ivy`(2026-06-20,纯 TS 全 BFS,29,160 态,God 8,均值 5.74)= 本 loop 的范式样板。

---

## §3 决策 / MANUAL 交接

- **2026-06-20 立项**:用户要给 `/scramble/gen` 非 WCA 魔方依次造求解器。分档调研结论见正文(A 现场 BFS / B 预算表 / C 单实例 IDA* / D 近最优)。TIER D 整体设 soft-gate。
- **A0 选择器 UX**:30 项非 WCA 魔方的项目入口需分组,默认方案 = 二级分组下拉;子 agent 落地后若觉得别的形态更好可改,§3 记一笔。
  → **采用默认方案**(2026-06-20):`NonWcaPuzzlePicker` = 单个「更多魔方」触发按钮,点开后 popup 按 family 分组(长方体 / 异形扭转 / Square 系 / 滑块 / 联体 / 其他)列项。家族 = 数据驱动,`CSTIMER_EVENTS` 每条带 `family`;只有 `solvable:true` 的才进下拉(当前仅 ivy)。WCA 求解器(5 个)继续走图标行 `WcaEventSelector`,选择器二者并排(solve 页 `.solve-tab-evrow`、stats 页 `.scramble-stats-event-pick` 都改成 flex 容纳)。接线点 = §4 列表里 A0 那条(已实现的:family/solvable 注册 + 分组下拉 + 两页 wire)。**A1+ 接新 puzzle**:`lib/cstimer-scramble` 该 event 标 `solvable:true`(+ 确认 `family`)→ 自动进下拉;再按 §4 加 solver dispatch / stats 渲染分支即可,选择器无需再改。
- **⛔ A5 `dino` 档位边界 GATE(2026-06-20,等用户拍板)**:实测 cstimer dino 模型可达态 = **A12 = 239,500,800**(子 agent 独立 Schreier-Sims 双跑 + 主 loop 独立复核:8 个 move 全偶 3-循环 → G ⊆ A12;群在 12 点上传递且本原 → Jordan 定理 = A12。复核脚本 `.tmp` 即用即删,结论:cycle 全 3、transitive、primitive)。**原估 ~35,640 与 6 色版 19,958,400 都错**。后果:TIER A 纯 TS 全 BFS 不可行(239.5M 远超 2M;无 dist 表/全空间直方图/下载全部),cstimer 自带启发式最大距离 4 vs God ~13 → 最优 IDA* 不现实。**需用户三选一**:
  1. **跳过 A5 求解器** —— dino 保持只生成打乱(不设 `solvable:true`),loop 直接进 A6 `ufo`。(推荐:成本最低,不破坏「TIER A = 最优」契约。)
  2. **允许 dino 近最优**(两阶段 / redi 式,可证 `scramble∘solution=solved`、长度 ≤ cstimer,但**非可证最优** + 仅抽样分布)。这本质是 loop 通常 soft-gate 的 TIER-D 级工作,放在 A 档里属破例,要用户明确放行才做。
  3. 把整个 dino 单元在 backlog 里重分类到 D 档,按选项 2 的契约推进。
  在用户答复前,A5 保持 `⛔`,**不 ScheduleWakeup**(GATE 不擅自跨)。
- **A9 `sia113` 档位边界(2026-06-20,误分类 → 并入 soft-gate 决策批,loop 不停、跳过继续 B1)**:`sia113` = `args2`/`formatScramble` 项(展开 `#{[["U","u"],["R","r"]],cubesuff,N} z2 #{...}`),物理 = 两个 3×3×3 沿共享 1×1×3 棒粘连的 bandaged 双联体,8 个基础层转(各 order 4)生成。**实测可达态 ≫ 50M**(两条独立表示 BFS:cubie 置换+24 取向 / sticker 置换,均在深度 6 累计 2,449,964 态、每层约 ×8 增长 [24,322,3380,31273,266740,2148224],深度 7 双双 4GB OOM;量级 10⁸–10⁹、上帝之数估 ~13–16)。**超 TIER A(2M)与 TIER B(50M)上限,纯 TS 全 BFS 不可行**。记号已校准(node:vm 真跑 cstimer sia113 × 500 条 / 17,952 token → 恰 12 个 move token + 字面 z2、0 解析失败)。executor 照 A5 dino 决策**未在「太大」前提上动工、未写代码**。**等用户三选一**(同 dino 三档):① 跳过 A9 求解器(`sia113` 不标 `solvable:true`,只留打乱);② 允许近最优(双立方 reduction / 两阶段,可证 `scramble∘solution=solved`、长度 ≤ cstimer 同口径,但非可证最优 + 仅抽样分布);③ 在 backlog 重分类到 D 档,按对称约简陪集表(立方 24 旋转对称约简 ~10⁷)走可证最优(真工程)。
- **B2 `cm3` Cmetrick(full)档位边界(2026-06-20,误分类 → 并入 soft-gate 决策批,loop 不停、跳过继续 B3)**:`cm3` = `megascramble.js:28` 项(`value.length===1` → `mega(value[0],[""],N)`),物理 = **3×3 球阵(9 球)**,每球取向 ∈ 立方体旋转群(24 阶),= cm2(2×2=4 球,A7,可达态 165,888)的 3×3 放大版。行移 U/E/D(三行绕 Y 轴)+ 列移 R/M/L(三列绕 X 轴),`<`/`^`=+90° `>`/`v`=−90° `2`=180°,token 字母表恰 **18 个** `U< U> U2 E< E> E2 D< D> D2 R^ Rv R2 M^ Mv M2 L^ Lv L2`。**实测可达态 ≫ 5×10⁷**:由物理几何独立推导 18 个移动置换跑 bounded BFS(5M 节点硬上限 + heap 守卫,绝不 slow-OOM)—— 每层 new `[1,18,207,2160,20979,187101,1547470,3242064(深度 7 撞 5M cap 中断)]`,深度 1–6 增长比 ~×8–11.5,**深度 6 累计 1,757,936(逼近 TIER A 2M 上限)、深度 7 仅前 3.24M 新态就撞 5M cap**;维持 ~×8 外推:深度 7 ≈ 12M、8 ≈ 90M、9 ≈ 600M+,且 cm3(9 球)比 cm2(4 球,上帝之数 10)深得多 → 总量量级 **10⁹–10¹¹**(raw 上界 24⁹ ≈ 2.6×10¹²,parity 约束至多砍常数因子)。**超 TIER A(2M)与 TIER B(5×10⁷)上限,纯 TS 全 BFS / build 时 `.bin.gz` 预算表均不可行**(dist 表 >GB、全空间直方图 / 下载全部状态 全做不到)。记号已校准(node:vm 真跑 cstimer cm3 × 500 条 / 12,500 token → distinct token 恰 18 个、0 解析失败;cstimer 无 apply/无 solver/无图,只能验 token 集)。executor 照 A5 dino / A9 sia113 决策**未在「太大」前提上动工、未写代码**。**等用户三选一**(同 dino/sia113 三档):① 跳过 B2 求解器(`cm3` 不标 `solvable:true`,只留打乱);② 允许近最优(reduction / 两阶段,可证 `scramble∘solution=solved`、长度 ≤ cstimer 同口径,但非可证最优 + 仅抽样分布);③ 在 backlog 重分类到 D 档,按对称约简陪集表(立方 24 旋转 + 行列对称约简)走可证最优(真工程,**且 10⁹–10¹¹ 量级即便约简后仍可能太大,需先评估约简比**)。
- **B3 `233` 2×3×3 多米诺档位边界(2026-06-20,误分类 → 并入 soft-gate 决策批,loop 不停、跳过继续 B4)**:`233` = `megascramble.js:30` 项 `[[[["U","U'","U2"]],["R2","L2"],["F2","B2"]]]`(`value.length===1` → `mega(value[0],[""],N)`),物理 = **2(高)×3×3 长方体多米诺**:U/D 两大 3×3 面,仅顶面 U 转 90°(无 D token,cstimer 固定底面);四侧面 R/L/F/B(各 2×3 矩形)只 180°。token 字母表恰 **7 个** `U U' U2 R2 L2 F2 B2`。从机制独立几何重推导(坐标 x∈{0,1,2}/y∈{0,1}/z∈{0,1,2},18 槽全在壳上,2 个面心(顶心 (1,1,1) / 底心 (1,0,1))永不动 → 16 活动 cubie 分两个 8-轨道:8 角(4 顶 + 4 底)+ 8 棱(4 顶 + 4 底);U 90° 同 3×3 的 U 转(角循环角、棱循环棱),侧面 180° 交换顶↔底;已验 U⁴=identity、R2/L2/F2/B2 全自逆)。**实测可达态 = 1,422,489,600(≈1.42 × 10⁹)**:独立 Schreier-Sims 在 16 活动点上算 |G| = 1,422,489,600 = 8!·8!·7/8 = 40,320 × 35,280(= 上界 8!·8! = 1,625,702,400 的 7/8,棱-角奇偶耦合砍 1/8);bounded BFS(compact 编码 + heap 守卫 1.8GB)独立佐证每层累计 `[1,8,42,210,1017,4811,22435,103158,465422,2068198]`、增长比 ~×4.4–4.8 稳定、深度 9 累计 2.07M 远未完成(对照 |G| ≈14 亿,外推上帝之数 ~17–19)。**711× 于 TIER A(2 × 10⁶)、28× 于 TIER B(5 × 10⁷)上限,纯 TS 全 BFS 与 build 时 `.bin.gz` 预算表均不可行**(dist 表 >GB、全空间精确直方图 / 「下载全部状态」全做不到)。记号已校准(node:vm 真跑 cstimer 233 生成器 × 500 条 / 12,500 token → distinct token 恰 7 个、0 解析失败;cstimer 无 apply/无 solver/无图,只能验 token 集 + 解析有效性)。executor 照 A5 dino / A9 sia113 / B2 cm3 决策**未在「太大」前提上动工、未写代码**。**等用户三选一**(同前三档):① 跳过 B3 求解器(`233` 不标 `solvable:true`,只留打乱);② 允许近最优(两阶段 reduction:先归约棱、再角,可证 `scramble∘solution=solved`、长度 ≤ cstimer 同口径,但非可证最优 + 仅抽样分布);③ 在 backlog 重分类到 D 档,按对称约简陪集表(棱角分离 + 立方子群对称)走可证最优(真工程,约简后**可能**落 ~10⁷–10⁸ 仍可最优,比 cm3 更接近可行边界,但需先评估约简比)。
- **B4 `sia123` 联体 1×2×3 档位边界(2026-06-20,误分类 → 并入 soft-gate 决策批,loop 不停、跳过继续 B5)**:`sia123` = `args2`/`formatScramble` 项 `#{[["U"],["R","r"]],%c,%l} z2 #{[["U"],["R","r"]],%c,%l}`(两块 `mega`:每 cube 轴 `[["U"],["R","r"]]` = 单 U 层 + R 面 + r 切片,后缀 cubesuff,中夹字面 `z2`;cstimer 无 apply/无 solver/无图 → 无 round-trip oracle)。物理 = 两个 3×3×3 沿共享 1×2×3 块粘连的 bandaged 双联体(比 sia113 少 `u` 自由层,略更受限)。**实测可达态 ≫ 5×10⁷**:验证过的 surf-cubie+24 取向 rigid-body BFS(compact latin1 编码 + 4.5M 节点硬上限 + heap 守卫,6 生成元全 order-4 sanity 过)逐层 new `[1,18,180,1512,12319,99278,793104]`、增长比 ~×8(10.0/8.40/8.15/8.06/7.99 收敛 8),深度 6 累计 906,412、深度 7 撞 4.5M cap;×8 外推:深度 8 ≈ 5.8×10⁷(跨 TIER B 上限)、深度 9 ≈ 4.6×10⁸、深度 10 ≈ 3.7×10⁹ → 量级 **10⁸–10⁹**(同文档 sia113「最少 bandage」档),上帝之数估 ~13–16。**超 TIER A(2M)与 TIER B(5×10⁷)上限,纯 TS 全 BFS / build 预算表均不可行**。记号已校准(node:vm 真跑 cstimer sia123 × 400 条 / 16,400 token → distinct 恰 10 个 `U U' U2 R R' R2 r r' r2` + 字面 z2、0 解析失败)。executor 照 A5 dino / A9 sia113 决策**未在「太大」前提上动工、未写代码**。**等用户三选一**(同前三档):① 跳过 B4 求解器(`sia123` 不标 `solvable:true`,只留打乱);② 允许近最优(双立方 reduction / 两阶段,可证 `scramble∘solution=solved`、长度 ≤ cstimer 同口径,非可证最优 + 仅抽样分布);③ 在 backlog 重分类到 D 档,按对称约简陪集表(立方 24 旋转对称约简)走可证最优(真工程)。
- **B5 `sia222` 联体 2×2×2 档位边界(2026-06-20,误分类 → 并入 soft-gate 决策批,loop 不停、跳过继续 B6)**:`sia222` = `args2`/`formatScramble` 项 `#{[["U"],["R"],["F"]],%c,%l} z2 y #{[["U"],["R"],["F"]],%c,%l}`(两块 `mega`:每 cube 轴 `[["U"],["R"],["F"]]` = 单层 3-gen 像 2×2×2,后缀 cubesuff,中夹字面 `z2 y`;无 round-trip oracle)。物理 = 两个 3×3×3 沿共享 2×2×2 块粘连的 bandaged 双联体(三族中**最 bandaged**,每 cube 仅 3 个外层单面可转)。**实测可达态 ≫ 5×10⁷**:同款 rigid-body BFS(6 生成元全 order-4 sanity 过)逐层 new `[1,18,225,2700,32256,383844]`、增长比 ~×12(12.5/12.0/11.95/11.90 稳定),深度 5 累计 419,044、深度 6 撞 4.5M cap;×12 外推:深度 7 ≈ 6.0×10⁷(跨 TIER B 上限)、深度 8 ≈ 7.2×10⁸、深度 9 ≈ 8.7×10⁹ → 量级 **10⁸–10¹⁰**(z2 y 把 cube B 三轴全带进转动位故早层分支更快),上帝之数估 ~12–15。**超 TIER A/B 上限,纯 TS 全 BFS / build 预算表均不可行**。记号已校准(node:vm 真跑 cstimer sia222 × 400 条 / 16,800 token → distinct 恰 11 个 `U U' U2 R R' R2 F F' F2` + 字面 z2 y、0 解析失败)。executor**未在「太大」前提上动工、未写代码**。**等用户三选一**:① 跳过 B5 求解器(只留打乱);② 近最优(双立方 reduction,采样分布);③ 重分类 D 档对称约简陪集表最优(真工程)。
- **B6 `ctico` 二十面体(Icosamate)档位边界(2026-06-20,误分类 → 并入 soft-gate 决策批,loop 不停、跳过;B 档全清)**:`ctico` = `utilscramble.js:568` 的 `adjScramble(["UL","UR","UrUl","FlFr","LBl","RBr"], [0x3f×6], len, minxsuff)`(6 个顶点 5 阶转 × minxsuff 4 power = 24 token,adj 全 `0x3f` 六面互邻 → 仅约束相邻 move 不重复;random-MOVE)。**但 ctico 在 cstimer `poly3dlib` 里有真几何 + 真引擎**(`getFamousPuzzle('ctico')` → `polyParam=[20,[],[],[-5,0]]` → `makePuzzle` 产 24 个 twisty 的 `moveTable`,80 facelet),= 深切顶点转的二十面体(类比 dmd A8 的八面体,但 20 面),**故有真 oracle**。**实测 |G| = 71,128,171,054,676,736,000,000,000,000,000,000 ≈ 7.1×10³⁴(EXACT,用 cstimer 自带 `grouplib.SchreierSims(moveTable 24 个 order-5 生成元).size(true)` 在 0.05s 算出)**——**远超原估 ~1e8(低估 26 个数量级)与 TIER B 5×10⁷ 上限**。bounded BFS 佐证爆炸增长:逐层 new `[1,72,2867,98640,3272880]`(比 ~×40/×34/×33),深度 4 累计 3.37M 即撞 heap 守卫。**绝非 B/D 边界,是深 D**:dist 表/全空间直方图/下载全部/build 预算表全不可套。记号已校准(node:vm 真跑 cstimer ctico × 400 条 / 10,000 token → distinct 恰 24 个 `{UL,UR,UrUl,FlFr,LBl,RBr}×minxsuff`、0 解析失败)。executor**未在「太大」前提上动工、未写代码**。**等用户三选一**:① 跳过 B6 求解器(只留打乱);② 近最优——`ctico` poly3dlib 有 `moveTable` 真几何(apply oracle 可用,A8 dmd 同源)但 **poly3dlib 不带 solver**(见下「cheap-win 审计」更正),故须自建 reduction / 两阶段(非 D1 省力 wrap),oracle = poly3dlib apply round-trip,采样分布;③ 对称约简最优**不现实**(7×10³⁴ 远超任何枚举/陪集表可行域)。
- **cheap-win 审计(2026-06-21,主 loop):剩 18 个 D 单元无一可 wrap cheap-win,全是从零 reduction。** 逐个 grep 了 vendored cstimer 源(`tools/cstimer-scramble/scramble/*.js` + `lib/poly3dlib.js`):**cstimer solver 函数只覆盖** 2x2x3 / clock / gear / megaminx(klm,mgm via `mgmsolver`)/ pyraminx(`solveMpyr` ← D1a 用)/ redi(`solveDino` ← A5→D 用)/ fto —— **剩 18 个 id 全不命中**。具体:sq 系(sq2/ssq1[key ssq1t]/bsq)是 `sq1_scramble`/`ssq1t_scramble` 纯生成器无 solver;prcp(`pochscramble`)、giga(`gigascramble`)、heli/helicv(`adjScramble`)、bic(`bicube` doMove)、cuboid(334/335/336/337/233,megascramble.js)、cm3、sia113/sia123/sia222(megascramble.js)全是 random-MOVE 生成器。**poly3dlib 经核实只有几何**(`makePuzzle`→`moveTable`=apply,`grouplib.SchreierSims`=群阶)**没有 IDA*/solver** —— A8 dmd「有 solver」其实是闭包小(138k)自建 BFS;ctico(7.1e34)只有 apply 没法 BFS。**有 poly3dlib 几何(→ 可拿 apply 当独立 oracle)的剩余 id = giga / prcp(prc) / heli / helicv / ctico**;其余(sq 系 / cuboid / cm3 / bic / sia 系)连几何都没有,oracle 须从 §1 条目里那套独立重导的几何模型自建。**故所有 18 个走 §0.6 从零 reduction 路,无 mpyrso/dino 式省力 wrap。**
- **📦 MANUAL dist 发布(2026-06-22,D2-6 sq2)**:`stats/scramble/dist_sq2.json`(N=2000,已 commit 进 repo)要 scp 到 static 才在线上显示 —— `statsUrl()` 生产环境写死 `static.cuberoot.me` 绝对地址,**git push 只部署代码、不更新 static 上的 stats JSON**(同 dist_336 的坑)。发布(Git Bash,源用 `/d/` 形式避免 scp 把 `D:` 当 host):`scp /d/cube/cuberoot.me/core/stats/scramble/dist_sq2.json cuberoot:/www/wwwroot/toolkit/stats/scramble/dist_sq2.json` 后 curl 验线上 200。**留用户授权后发布**。⚠ `dist_337.json`(D2-5)同样疑似尚未 scp,发布时一并核验。
- **📦 MANUAL dist 发布(2026-06-22,D2-7 ssq1)**:`stats/scramble/dist_ssq1.json`(N=2000,真两阶段约简的解长直方图,27 bin 双峰,已 commit 进 repo `4451d00fa`)要 scp 到 static 才在线上显示(同 sq2 / dist_336 的坑:`statsUrl()` 生产写死 `static.cuberoot.me`,git push 只部署代码不更新 static 上的 stats JSON)。**正确源路径在 repo 根 `stats/scramble/`(不是 `core/stats/`)** —— 发布(Git Bash,源用 `/d/` 形式避免 scp 把 `D:` 当 host):`scp /d/cube/cuberoot.me/stats/scramble/dist_ssq1.json cuberoot:/www/wwwroot/toolkit/stats/scramble/dist_ssq1.json` 后 curl 验线上 200。**留用户授权后发布**。⚠ 上一条 sq2 MANUAL 写的 `/d/cube/cuberoot.me/core/stats/...` 路径有误(多了 `core/`),发 dist_sq2.json 时也走 repo 根 `stats/scramble/`。
- (采样分布(C/D 档)的灌注 / 发布 MANUAL 交接,做到时写这里)

---

## §4 共享套路(每个单元照做;详见 skill `new-substep-solver` §0)

**范本文件**(逐个对照抄结构):
- `packages/client/lib/ivy-solver.ts` — 求解器核心(BFS-from-solved 整图 + `solve*` + `*Apply` 状态 + `*ExamplesByLength` + `*AllScramblesByLength`)
- `packages/client/app/[lang]/scramble/solver/_IvySolver.tsx` — 求解器页(仿 `_Sq1Solver`,`?event=` dispatch)
- `packages/client/app/[lang]/scramble/gen/_svg/ivy_svg.ts` — net SVG(颜色全由 solver 状态推导 → solved 纯色自证)
- `packages/client/app/[lang]/scramble/stats/_components/IvyDistView.tsx` — 分布视图(直方图 + 自造示例 + 下载)
- `packages/client/tests/ivy_solver.test.ts` — 测试(独立 BFS 交叉验证)

**大直径长方体两阶段模板**(334/335/336/337):整套两阶段约简(phase-1 weighted-A* 归约进全 180° 子群 H + phase-2 joint-PDB bank 还原 + 浅态 max(optDist) 可证最优捷径)的完整设计文档 = **各 `lib/cuboidNNN-solver.ts` 的文件头注**(不在此重复展开)。新长方体单元先抄最近一个的头注 + 接线镜像最近一个已接的同族单元(grep 该 event 号逐处加等价行),别从零试。335/337 侧面 90° 不可行(只 180° + `/333` 后缀忽略),336 是纯 mega 全 token 可刚体实现 —— 几何先量再做。

**七步(Ivy 范式)**:
1. **移动语义照抄 cstimer**(§1 条目给的源文件,逐字段);整图 BFS-from-solved,独立 BFS 交叉验证 god-number 直方图自证正确。
2. **求解器 UI 仿 `_IvySolver`**:接 `/scramble/solver` 的 `?event=` dispatch(`solver/page.tsx` 加分支)+ A0 的分组选择器;批量解复用 `BatchSolvePanel`。
3. **分布数据源按档**:A/B = 全空间精确直方图(数据现成在 solver,无管道);C/D = 采样直方图。接 `stats/page.tsx` 的 `event===` 分支。
4. **示例自造**:枚举每个最优步数档的状态、反推最短打乱当示例(`*ExamplesByLength`),不需要比赛语料。
5. **下载按钮**:A/B 给「下载全部状态」CSV(`optimal_length,scramble`,含 identity 行)+ 单步数 txt;态数 >~2M 见 §0.5 下载策略。
6. **状态图**:net SVG(颜色全由状态推导 → solved 复原态自证)接 `ScramblePreview2D` 的 `HAS_PREVIEW` + dispatch。**非立方体魔方(UFO/Cmetrick/Icosamate 等)没有 WCA net** → 退而用合理 2D 示意 / 复用 cstimer 图像生成 / 实在难就先出文字状态,**别为像素级 net 卡住单元**(预览是软需求,solver+分布+下载是硬核心)。
7. **测试**:见 §0.4 按档验收。typecheck + 守卫(i18n 一律 `tr({zh,en})` / `<T>` / `useT`,**禁** `isZh ? '中' : 'EN'` 文案三元;按钮用真 `<button>` / `AppLink`;CSS 走 theme token,禁硬码灰;puzzle data-color hex 可照 skewb 范例)。

**接线点(加一个非 WCA 求解 puzzle 要改这些)** — A0 已落地后更新:
- `lib/cstimer-scramble.ts` — 该 event 已在 `CSTIMER_EVENTS`(打乱来源 `cstimerScramble(id)`);**给它标 `solvable: true`(+ 确认 `family` 分类对)→ 自动进 `NonWcaPuzzlePicker` 分组下拉**,选择器本身不用改(A0 数据驱动)。
- `solver/page.tsx` — dispatch 加 `event===<id>` 分支(纯 TS 求解器走 `next/dynamic` 懒载,保持分包)。
- `solver/_puzzle-specs.ts` — 若复用 `PuzzleOptimalSolver` spec 范式(Rust WASM 那套)则在此加;纯 TS 自写组件则不进此表(照 ivy)。
- `stats/page.tsx` — `event===<id>` 渲染 `<P>DistView` + 关掉 WCA 专属开关(dataset/merge/timeline,照 `isIvy`/ivy 那段早返回);非 WCA solvable 的项目已被 length-reset effect 跳过(`CSTIMER_SOLVABLE_IDS.has(event)`),无需再改;`distPuzzle` 高亮映射按需加分支。**不再用 `availableEvents.add` / `appendEvents`**(A0 前的旧法,已删)。
- `ScramblePreview2D.tsx` — `HAS_PREVIEW[<id>]=true` + dispatch `renderXScrambleSvg`。
- 新建 `_svg/<id>_svg.ts`、`lib/<id>-solver.ts`、`stats/_components/<P>DistView.tsx`、`solver/_<P>Solver.tsx`、`tests/<id>_solver.test.ts`。

**测试欠账(owed UI)**:
- **A0(2026-06-20,commit `6265c3671`)**:Playwright UI 门当时跳过(内存吃紧)。**已于 A1(2026-06-20,commit `7a2a56243`)补齐并清账** —— Playwright 实测:`/scramble/solver?event=133` 与 `?event=ivy`、`/scramble/stats?event=133` 桌面(1280)+ 390px;NonWcaPuzzlePicker「更多魔方」下拉点开按 family 分组(长方体→1×3×3 花型、异形扭转→Ivy Cube),经它在 133↔ivy 间切换正常;弹层 390px box 右沿 192px 不溢出(viewport 375 内);2D 预览(net)渲染 solved 自证;**本次改动零 console error**(历史里出现过的 `SOLVE_EVENTS is not defined` 经核实是开站旧 chunk 残留,当前源码无该标识符、fresh 导航不复现;另有一条 `Connection closed.` 是 Turbopack RSC dev 流式偶发、reload 即消,均非代码问题)。A0 欠账清。
- **A0 遗留:Ivy 实现从未 commit** —— A0 的 6265c3671 只提交了 picker 基建,ivy 的 6 个实现文件 + 三处共享接线一直留在工作树未入库(repo 任何分支都查不到 ivy-solver.ts)。**已于 A1 commit `7a2a56243` 一并落库**(与 A1 的共享接线交错在同一 hunk),无残留。
- **A3(2026-06-20,commit `fa5d301f2`)**:Playwright 实时 UI 门跳过 —— 本机 RAM 紧 + 大求解器在跑,按内存约束不开 Chromium。**A3-review(2026-06-20)复核时 free RAM 仍仅 ~1.3GB(< 2GB 阈),欠账继续保留**,但 dev server 在跑、`/scramble/solver?event=8p` 实测 HTTP 200;非 UI 的硬核全部独立验过(记号保真度用 vm 真跑 cstimer 8prp scrambler × 2000 条零撞墙零 round-trip 失败,见 §2 A3-review)。**待补**:RAM 转宽松后 Playwright 验数字格预览(solved = 1..8 canonical,空格右下)、桌面 + 390px、「更多魔方」下拉(slide 家族)切到 8p、零 console error,清 `[untested-ui]` 债。
- **A2(2026-06-20,commit `7c30974d7`)**:UI 门 **已于 A2-review(2026-06-20)补齐并清账** —— 本机 RAM 转宽松(3.3GB 空闲)后 Playwright 实测:`/scramble/solver?event=223`(打乱 `U R2 F2 U2 D` → 解 `D' U2 F2 R2 U'` 5 步,32 贴纸网图渲染、6 面色齐全)+ `/scramble/stats?event=223`(图表 + 均值 9.74/中位 10/众数 11/God 14 + 12 示例 + 「全 241,920 态」+ 下载按钮)桌面(1280)+ 390px(scrollWidth 375 ≤ 390 无横向溢出,示例网 384 rect = 12×32);NonWcaPuzzlePicker 触发钮当前选中显示「2×2×3」(证 A2 已正确注册进数据驱动分组下拉);**全程零 console error**。A2 UI 欠账清。
- **A4(2026-06-20,commit `d29dea86b`;review `e4abc9bfb`)**:Playwright 实时 UI 门跳过 —— `127.0.0.1:3000` 无 dev server,按硬约束(禁 `pnpm dev`)不自启,故未开 Chromium(A4-review 时 RAM 已宽松 3.96GB,但 dev server 没起,仍无法验)。非 UI 的硬核全部独立验过:可达态 3,041,280(独立旋转推导参照 BFS,对上 jaapsch.net 权威值)/ God 13 / 直方图 `toEqual` 真锁;cstimer 记号用 vm 真跑 sfl 生成器 × 2000 条真打乱 → 0 解析失败 / 0 非法 move / 0 round-trip 失败。**A4-review 已修下载规模缺陷**:整包 CSV(~80MB / 3M 行)改 confirm + 流式分块 Blob、单步数 txt 流式,峰值内存有界(见 §2 A4-review)。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=sfl`(平板网图预览 solved 自证)+ `/scramble/stats?event=sfl`(直方图 + 均值 9.00/God 13 + 示例 + 「全 3,041,280 态」+ 下载;实点「下载全部」确认 confirm 弹出且生成不卡页)桌面 + 390px、「更多魔方」下拉(长方体家族)切到 sfl、零 console error,清 `[untested-ui]` 债。
- **A6(2026-06-20,commit `b98c5b426`)**:UI 门 **已于 A6-review(2026-06-20)补齐并清账** —— 本机 free RAM ~2.9GB(> 2GB 阈)+ dev server 在跑,Playwright 实测:`/scramble/solver?event=ufo`(标题 UFO Solver,「更多魔方」触发钮显示「UFO」选中证已注册进 twist 分组下拉;带 scramble `A U B U2 C` 时圆盘预览渲染 24 distinct 楔形色 + gap 扇区)+ `/scramble/stats?event=ufo`(直方图 + 均值 7.74/God 10/「全 60,480 态」+ 下载按钮 + 12 例圆盘预览,0 broken)桌面(1280)+ 390px(scrollWidth 375 ≤ 390 无横向溢出);两页**全程零 console error**。solved 自证(3 gap + 24 distinct)由单测锁定。A6 UI 欠账清,`[untested-ui]` 债销。
- **A7(2026-06-20,commit `99f5ff830`;review `83a1570c8`)**:Playwright 实时 UI 门跳过 —— `127.0.0.1:3000` dev server DOWN(实测 HTTP 000)+ free RAM 紧,按硬约束(禁 `pnpm dev`)不自启,故未开 Chromium。**A7-review(2026-06-20)复核时 free RAM 仍仅 1.17GB(< 2GB 阈)+ dev server 仍 DOWN,欠账继续保留**。非 UI 的硬核全部独立验过(主 loop 复跑):vitest 18/18 绿(可达态 165,888 `toBe`、God 10、直方图 `[1,12,86,524,2577,9564,26964,49648,47712,23644,5156]` `toEqual`、均值 7.32576;独立几何重推导锚 + 300 随机最优性 + apply==参照 + 全枚举==分布 + SVG solved canonical)、typecheck(tsgo+tsc)EXIT=0;cstimer 记号 vm 真跑 cm2 生成器 × 500 条 / 8000 token → 0 未知 / 0 解析失败,token 集恰 12 个。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=cm2`(2×2 球预览 solved = 4 球同色自证)+ `/scramble/stats?event=cm2`(直方图 + 均值 7.33/God 10/「全 165,888 态」+ 下载 + 示例)桌面 + 390px、「更多魔方」下拉(异形扭转家族)切到 cm2、零 console error,清 `[untested-ui]` 债。
- **A8(2026-06-20,commit `e174515f5`)**:Playwright 实时 UI 门跳过 —— RAM 紧,按硬约束(禁 `pnpm dev`)不自启 dev server,故未开 Chromium。非 UI 的硬核全部独立验过(主 loop 复跑):vitest 19/19 绿(可达态 138,240 `toBe`、God 10、直方图 `[1,8,48,288,1632,8568,36114,74799,16547,220,15]` `toEqual`、均值 6.6921;**cstimer 真引擎 round-trip oracle** —— node:vm 跑真 poly3dlib+grouplib,抽取 dmd moveTable 重建 4 生成元逐元素 == solver 硬编码生成元 + SchreierSims |G| == 138,240 + 200 条真 dmdso 打乱全 round-trip、token 集恰 8 个 + 400 随机最优性 + apply==参照 + 全枚举==分布 + SVG solved 每面同色)、typecheck(tsgo)EXIT=0。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=dmd`(八面体网图预览 solved = 每面 4 子三角同色自证)+ `/scramble/stats?event=dmd`(直方图 + 均值 6.69/God 10/「全 138,240 态」+ 下载 + 示例)桌面 + 390px、「更多魔方」下拉(异形扭转家族)切到 dmd、零 console error,清 `[untested-ui]` 债。
- **B1(2026-06-20,commit `97e850156`)**:Playwright 实时 UI 门跳过 —— RAM 紧 + `127.0.0.1:3000` dev server DOWN,按硬约束(禁 `pnpm dev`)不自启,故未开 Chromium。非 UI 的硬核全部独立验过(主 loop 复跑):vitest 19/19 绿(可达态 41,472 `toBe`、God 6、直方图 `[1,33,579,5921,18072,13977,2889]` `toEqual`、均值 4.30317;**cstimer 真引擎 round-trip oracle** —— node:vm 加载真 mathlib+scramble+gearcube,200 条真 gearso 全 round-trip + 独立参照 BFS 复现闭包/God/直方图 + 33-token 字母表 + 最优性 + apply==参照 + 全枚举==分布 + SVG solved canonical)、typecheck(tsgo)EXIT=0。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=gear`(齿轮 schematic 预览 solved = 全坐标 0 固定 canonical 自证)+ `/scramble/stats?event=gear`(直方图 + 均值 4.30/God 6/「全 41,472 态」+ 下载 + 示例)桌面 + 390px、「更多魔方」下拉(异形扭转家族)切到 gear、零 console error,清 `[untested-ui]` 债。

- **C1(2026-06-20,commit `3bf24f8f2`)**:Playwright 实时 UI 门跳过 —— dev server 在跑(`/scramble/solver?event=15p` 实测 HTTP 200),但 free RAM 仅 ~0.97GB(< 2GB 阈)且大求解器在跑,按内存约束不开 Chromium(避免 OOM 杀用户的求解器)。非 UI 的硬核全部独立验过(主 loop):vitest 13/13 绿(滑块核 4×4 复用 + 有效性 scramble∘解=solved 且 ≤80 + **最优性交叉验证** 浅态 IDA* 长度 == 独立朴素 BFS 最短(0 mismatch)+ WD/组合启发式可采纳性 ≤ 真距(0 违反))、typecheck(tsgo)EXIT=0;记号 vm 真跑 cstimer 15prp × 400 条零撞墙/零非法 token/零非法排列。**待补(Playwright)**:RAM 转宽松后验 `/scramble/solver?event=15p`(4×4 数字格预览 solved = 1..15 canonical;输入打乱出最优解 + 「求解中」spinner 不卡死)+ `/scramble/stats?event=15p`(**采样**直方图标注「非全空间」+ 样本均值 ≈ 52.6 / God 80 + 重新采样/取消 + 下载样本 CSV + 示例)桌面 + 390px、「更多魔方」下拉(滑块家族)切到 15p、零 console error,清 `[untested-ui]` 债。

- **D1a(2026-06-20,commit `8af309db6`)**:Playwright 实时 UI 门跳过 —— RAM 紧 + `127.0.0.1:3000` dev server DOWN,按硬约束(禁 `pnpm dev`)不自启,故未开 Chromium。非 UI 的硬核全部独立验过(主 loop):vitest 6/6 绿(纯 TS facelet 端口 solved 自证 + 一移破坏/逆复原 + tip 不动 body;**cstimer 真引擎 oracle** —— node:vm 跑真 mpyr 引擎,solveTest 自检 + 24 条真 mpyrso 打乱全 roundTripCheck(scramble∘solution=solved)+ 长度 ≤ 38 + solution token ⊆ 字母表 + mpyrFacelets 逐 move 对齐真引擎 facelet [80 序列,离线 3000 序列 fuzz 0 差异])、typecheck(tsgo)EXIT=0;近最优长度均值 ≈ 21.4(min 17/max 24)。**注意 D1a solver/dist 都靠 cstimer worker**(`/tools/cstimer-scramble/scrambler.worker.js`,dev 经 `app/tools/[...slug]` catch-all 服仓库根、Vercel fallback static.cuberoot.me)—— UI 验证时确认 worker 能加载且 solve 返回(首次会建剪枝表,稍慢)。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=mpyrso`(4 面三角网预览 solved=每面单色自证;输入打乱出近最优解 + 「求解中」spinner;worker solve 实际返回)+ `/scramble/stats?event=mpyrso`(**采样近最优**直方图标注「采样/近最优/非全空间」+ 样本均值 ≈ 21 + 重新采样/取消 + 下载样本 CSV + 示例)桌面 + 390px、「更多魔方」下拉(异形扭转家族)切到 mpyrso、零 console error,清 `[untested-ui]` 债。

- **D1b(2026-06-20,commit `6268157ee`)**:Playwright 实时 UI 门跳过 —— RAM 紧 + dev server,按硬约束(禁 `pnpm dev`)不自启,故未开 Chromium。非 UI 的硬核全部独立验过(主 loop):vitest 4/4 绿(22 条真 cstimer crz3a 打乱[node:vm 真生成器,len 25] → solveCrz3a 解后用**独立 3×3 oracle**[kociemba cube.ts 的 parseMoves/applySequence/solvedCubie/isSolvedCubie]重 apply 断言 scramble∘solution=solved 全 round-trip + 长度 ≤ 26 + solution token 全标准 HTM + 空打乱→0)、typecheck(tsgo)EXIT=0、`git show --stat 6268157ee` 证 10 文件全在 commit(4 新);近最优长度均值 ≈ 20.82(min 18/max 23,HTM)。**复用 kociemba 走 direct-import(非 worker),浏览器端首调建表 ~3-5s** —— UI 验证时确认进 solver 输入打乱后「求解中」spinner 出、首调建表稍慢后出解,采样分布页首条会触发建表。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=crz3a`(标准 3×3 展开网预览 solved=每面纯色自证;输入打乱出近最优解 + spinner)+ `/scramble/stats?event=crz3a`(**采样近最优**直方图标注「采样/近最优/等价标准 3×3/非可证最优」+ 样本均值 ≈ 21 + 重新采样/取消 + 下载样本 CSV + 示例)桌面 + 390px、「更多魔方」下拉(`'other'` 家族)切到 crz3a、零 console error,清 `[untested-ui]` 债。

- **A5→D dino(2026-06-20,commit `bd503934b`)**:Playwright 实时 UI 门跳过 —— `127.0.0.1:3000` dev server DOWN(HTTP 000),按硬约束(禁 `pnpm dev`)不自启,故未开 Chromium。非 UI 的硬核全部独立验过(主 loop):vitest 8/8 绿(纯 TS 24-贴纸网 solved 6 面自证 + 一移破坏/逆复原/order-3 + 12-棱 perm identity/逆;**cstimer 真引擎 oracle** —— node:vm 跑真 mathlib+scramble+redi.js,40 条真 dinoso 打乱用 `edgePermOfScramble(scramble∘solution)`==恒等验 round-trip(scramble∘solution=solved)+ 长度 ≤ 15 + token ⊆ 字母表 + dinoEpFromScramble 逐 move 对齐真引擎 ep [200 条 fuzz 0 差异])、typecheck(tsgo)EXIT=0;近最优长度均值 ≈ 9.53(min 7/max 11)。**注意 dino solver/dist 都靠 cstimer worker**(`/tools/cstimer-scramble/scrambler.worker.js`,`SOLVERS.dinoso = self.redi.solveScramble`)—— UI 验证时确认 worker 能加载且 solve 返回(首次会建剪枝表,稍慢)。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=dino`(6 面立方体十字网预览 solved=每面单色自证;输入打乱出近最优解 + 「求解中」spinner;worker solve 实际返回)+ `/scramble/stats?event=dino`(**采样近最优**直方图标注「采样/近最优/非全空间」+ 样本均值 ≈ 9.5 + 重新采样/取消 + 下载样本 CSV + 示例)桌面 + 390px、「更多魔方」下拉(异形扭转家族)切到 dino、零 console error,清 `[untested-ui]` 债。

- **D2-1 233(2026-06-21,commit `1ffdee851`)**:Playwright 实时 UI 门跳过 —— 按硬约束(禁 `pnpm dev`)不自启 dev server,故未开 Chromium。非 UI 的硬核全部独立验过(主 loop):vitest 15/15 绿(移动语义从几何独立重导 + 角/棱 PDB 各满 40,320[maxdepth 13/11] + 800 随机 scramble∘解=solved + 浅态最优 + **穷举半径 ≤6 全 20k+ 态长度==BFS 深度 0 mismatch** + cstimer node:vm ×200 round-trip + SVG solved 自证/随 solver 联动)、typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=233`(2×3×3 网图预览 solved=每面纯色自证;输入打乱出可证最优解 + 「求解中」spinner;首条建 PDB 稍慢)+ `/scramble/stats?event=233`(**采样最优**直方图标注「采样/非全空间/每条可证最优」+ 样本均值 ≈ 13.7 + 重新采样/取消 + 下载样本 CSV + 示例)桌面 + 390px、「更多魔方」下拉(长方体家族)切到 233、零 console error,清 `[untested-ui]` 债。
- **D2-2 334(2026-06-21,base `07e00099f` + review-fix(见 §2))**:Playwright 实时 UI 门跳过 —— 按硬约束(禁 `pnpm dev`)不自启 dev server,故未开 Chromium。非 UI 的硬核独立验过(主 loop):vitest 17/17 绿、typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0;状态空间 1.65×10¹⁷ / 群阶 2.64×10¹⁸ **超 2^53 → 全走预格式化字符串(grep 扫净)**;**可证最优(浅态 IDA*+max(每轨道 PDB),`optimal=true` 恒成立)+ 诚实 too-deep(深态,40 步打乱 ≤2.1s 命中预算不卡)**;radius≤4 全 8000+ 态 == BFS 深度;cstimer node:vm 真生成器 round-trip;/2 奇偶因子独立 SS+GF(2) 复核正确。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=334`(3×3×4 网图预览 solved=每面纯色自证;浅态出可证最优解 + 深态出友好「太深」提示 + 「求解中」spinner)+ `/scramble/stats?event=334`(**采样**直方图标注「采样/非全空间」+ 只下样本 CSV、无下载全部)桌面 + 390px、「更多魔方」下拉(长方体家族)切到 334、零 console error,清 `[untested-ui]` 债。
- **D2-3 335(2026-06-21,见 §2)**:Playwright 实时 UI 门跳过 —— 按硬约束(禁 `pnpm dev`)不自启 dev server,故未开 Chromium。非 UI 的硬核独立验过(主 loop):vitest 19/19 绿、typecheck(tsgo)EXIT=0 + typecheck:tsc EXIT=0;可达态 156,067,430,400(Schreier-Sims via cstimer grouplib,三方独立确认 90° 侧转几何不可行)/ 轨道乘积 22,473,709,977,600 均走预格式化字符串;两阶段约简近最优(中位 ~20、max ≤27、最坏 ~0.5s)+ 浅态可证最优(radius≤4 全 3613 态==BFS 深度);1000 真随机态 + 8 真 cstimer 打乱 round-trip(独立几何 oracle;cstimer node:vm 加载 min2phase+333);**solver 只解刚体 cuboid part,`/333` 后缀无刚体实现被忽略 —— UI 验证需注意预览/解只针对 ` / ` 前半段**。**待补(Playwright)**:dev server 起来后验 `/scramble/solver?event=335`(3×3×5 网图预览 12×11 网,solved=每面纯色自证;输入打乱出近最优解 + 浅态最优 + 「求解中」spinner;首条建 PDB 稍慢;真 cstimer 打乱含 ` / 333` 也能解前半段)+ `/scramble/stats?event=335`(**采样**直方图标注「采样/近最优/非全空间」+ 只下样本 CSV)桌面 + 390px、「更多魔方」下拉(长方体家族)切到 335、零 console error,清 `[untested-ui]` 债。

**约束(全局规则,别违)**:dev server 永远在 `127.0.0.1:3000`,禁 `pnpm dev`、禁 dev 在跑时本地 `next build`;UI 验证走项目内 Playwright MCP;commit 只加自己的文件、不 push;LF 换行;回复中文。
