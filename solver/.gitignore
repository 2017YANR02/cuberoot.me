/target/
/tables/
*.bin
.tmp/
# 仅根目录运行时产物;testdata/ 下的 golden csv 是源,需 commit
/scramble_*_*.csv
Cargo.lock


# build artifacts (regenerated by build_wasm.ps1; deployed copy lives in cuberoot.me/tools/solver/rust-cross/)
/pkg-web/
/pkg-node/
