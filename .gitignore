# 已删除的 Ruby 统计管线（本地可能残留数据文件）
_stats_build/

# 部署产物（deploy_mirror.yml 临时目录）
_deploy/

# Gemini agent data
.data_cache/

# 数据库配置（含密码，禁止提交）
database.yml

# 粒子系统归档版本（调试用，不提交）
assets/js/particles_v*.js
# API response cache for upcoming comps fetcher
.upcoming_cache/

# Python bytecode cache
__pycache__/

# 敏感凭据（含数据库密码，禁止提交）
.secrets.md
.password.md

CubeAlgWB*.csv
CubeAlgWB*.xlsx

# Python/Ruby 本地调试产物
recon_dump.txt
.bundle/

.comp_names_zh_cache/



# 比赛名对照文件（本地参考用）
comp_name_diff.txt


# Claude Code 本机专属配置（权限白名单，不共享）
.claude/settings.local.json
core/.claude/settings.local.json
# Claude Code agent worktrees（每个数 GB，纯本机临时产物）
.claude/worktrees/
# Playwright MCP 临时日志
.playwright-mcp/

# alg-build 公式识别 log（2MB+，每次 build 全量重写，repo 膨胀快；需要时本地生成）
core/packages/alg-build/data/alg_detections.log

# 临时/本地工作目录（本地调试/解压/中间产物用 + 删除前的"后悔药"暂存区）
tmp/
.tmp/

# stats-build 中间产物 (historical_ranks 等)
core/packages/stats-build/output/
# wca_stats_extra 增量 build 拉取的服务器旧指纹快照(CI 临时文件)
core/packages/stats-build/prev_fp.tsv

# past-comp snapshots dump (~800MB 17k 场,走 dump_comps.yml 直接 scp 到 server)
stats/comp/

# 每场预计算十字步数表 (~100MB 上万文件,scramble 周更生成,只 scp 到 static 不进 git)
# comp_steps/ = std;comp_steps_<variant>/ = eo/pseudo/pseudo_pair/pair/f2leo/pseudo_f2leo
stats/scramble/comp_steps*/
# 难度 tab「下载全部」全量语料 gz (~30MB/阶段,build_scramble_bundle.mjs 生成,只 scp 到 static)
stats/scramble/bundles/
# 难度 steps 索引宽表 CSV (~747MB,build_scramble_steps.ts 产,\copy 灌 PG wca_scramble_steps,不进 git)
stats/scramble/steps/
# /timer 最优打乱导出 CSV (export_puzzle_optimal.mjs 产 ~55MB,\copy 灌 PG,不进 git) + 跑批日志
core/packages/scramble-stats-build/wca_optimal_puzzle.csv
core/packages/scramble-stats-build/.lenopt.log

# git-bash (MSYS2) 崩溃栈转储,纯本机噪音
*.stackdump

# video-title 工具(gen_title TS,在 server 包 src/tools/)的本地缓存(WCA 排名 / 本地名,缺了会自动重拉)
core/packages/server/src/tools/rankings_cache.json
core/packages/server/src/tools/wca_local_names_cache.json
core/packages/server/src/tools/test_output.txt

.cc-connect/

# vendored cubelib FMC solver build output
fmc/target/
fmc/**/target/
