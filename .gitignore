# 已删除的 Ruby 统计管线（本地可能残留数据文件）
_stats_build/

# 部署产物（deploy_mirror.yml 临时目录）
_deploy/

# Gemini agent data
.data_cache/

# 数据库配置（含密码，禁止提交）
database.yml

# 粒子系统归档版本（调试用，不提交）
assets/js/particles_v*.js
# API response cache for upcoming comps fetcher
.upcoming_cache/

# Python bytecode cache
__pycache__/

# 敏感凭据（含数据库密码，禁止提交）
.secrets.md

CubeAlgWB*.csv
CubeAlgWB*.xlsx

# Python/Ruby 本地调试产物
recon_dump.txt
.bundle/

.comp_names_zh_cache/



# 比赛名对照文件（本地参考用）
comp_name_diff.txt

.gitnexus
AGENTS.md
CLAUDE.md

# Claude Code 本机专属配置（权限白名单，不共享）
.claude/settings.local.json

# /alg 模块的图片资源：由 alg-build 从 D:\cube\CubeRoot docx 本地生成
# ~640 MB / 53K files，不 commit；部署时单独 rsync 到云端
# catalog.json 和 posts/*.json 照常 commit
stats/data/alg/media/

# alg-build 公式识别 log（2MB+，每次 build 全量重写，repo 膨胀快；需要时本地生成）
core/packages/alg-build/data/alg_detections.log
